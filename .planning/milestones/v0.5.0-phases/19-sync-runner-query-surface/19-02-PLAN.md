---
phase: 19-sync-runner-query-surface
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - pycopg/etl.py
autonomous: true
requirements: [ETL-10, ETL-11, ETL-15, ETL-17]
must_haves:
  truths:
    - "db.etl.run(pipeline) returns a RunResult re-SELECTed from the just-written row (ETL-10/D-11)"
    - "db.etl.run(pipeline, dry_run=True) skips load, writes no pipeline_runs row, returns RunResult(status='dry_run', rows_loaded=0, run_id=None) (ETL-15/D-08/D-09)"
    - "db.etl.history(name, limit=100) returns list[RunResult], newest-first (ETL-11/D-06)"
    - "db.etl.last_run(name) returns the most recent RunResult or None (ETL-17/D-07)"
    - "Phase 17 run-log isolation and Phase 18 atomic load write paths are unchanged"
  artifacts:
    - path: "pycopg/etl.py"
      provides: "run()->RunResult + dry_run branch, history(), last_run()"
      contains: "def history"
  key_links:
    - from: "ETLAccessor.run()"
      to: "queries.ETL_GET_RUN"
      via: "re-SELECT by run_id after _end_run, _row_to_result"
      pattern: "ETL_GET_RUN"
    - from: "ETLAccessor.history()"
      to: "queries.ETL_LIST_RUNS"
      via: "dict_row cursor, [name, limit], _row_to_result per row"
      pattern: "ETL_LIST_RUNS"
    - from: "ETLAccessor.last_run()"
      to: "queries.ETL_GET_LAST_RUN"
      via: "dict_row cursor, [name], _row_to_result or None"
      pattern: "ETL_GET_LAST_RUN"
---

<objective>
Wire the user-facing accessor surface onto the Phase 17/18 machinery: change `run()` to return a
`RunResult` (re-SELECT by run_id), add the `dry_run` early fork, and add the `history()` and
`last_run()` read methods. All construction goes through the single `_row_to_result` mapper from
Plan 01 — zero drift. The hardened write paths (run-log isolation, atomic load) are untouched.

Purpose: deliver ETL-10/11/15/17 as a thin return/query layer (D-10/D-11/D-12).
Output: `pycopg/etl.py` — `run()` signature/return + dry_run branch, `history()`, `last_run()`.
</objective>

<artifacts_this_phase_produces>
Full Phase 19 NEW-symbol set (exclude from drift verification — none pre-existed):
`RunResult` (dataclass, Plan 01), `_row_to_result` (mapper, Plan 01), `ETL_GET_RUN` (constant, Plan 01),
`ETLAccessor.history` (method, this plan), `ETLAccessor.last_run` (method, this plan),
the `dry_run` param + branch (this plan), and the `run() -> RunResult` return change (this plan).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-sync-runner-query-surface/19-CONTEXT.md
@.planning/phases/19-sync-runner-query-surface/19-RESEARCH.md
@.planning/phases/19-sync-runner-query-surface/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: run() return upgrade + dry_run early fork</name>
  <files>pycopg/etl.py</files>
  <read_first>
    - 19-CONTEXT.md (D-05, D-08, D-09, D-11, D-12 — LOCKED dry_run + re-SELECT semantics)
    - 19-RESEARCH.md (sections "Exact Current Shape of ETLAccessor.run()" incl. the seam-location table, "The two seams Phase 19 needs", "Common Pitfalls" 1/2/3)
    - 19-PATTERNS.md (section "ETLAccessor.run() return upgrade", incl. Seam A and Seam B)
    - pycopg/etl.py lines 579-781 (full run() body): line 579 signature, line 640 name=pipeline.name, line 641 self.init(), line 642 _start_run, line 678 rows_extracted=len(df), lines 708-710 empty-df early return, line 780 _end_run, line 781 return run_id
    - pycopg/etl.py lines 514-520 (_start_run dict_row autocommit pattern to copy for the re-SELECT)
  </read_first>
  <action>
    Change the signature at line 579 to `def run(self, pipeline: Pipeline, dry_run: bool = False) -> RunResult:`.

    Seam A (dry_run fork, BEFORE line 641 `self.init()` — D-09): after `name = pipeline.name` (line 640), insert `if dry_run:` that captures `started_at = datetime.now(UTC)`, runs the SAME extract + transform as the normal path (to compute `rows_extracted = len(df)` after transform — D-08), then `finished_at = datetime.now(UTC)`, and returns `RunResult(run_id=None, pipeline_name=name, status="dry_run", rows_extracted=rows_extracted, rows_loaded=0, started_at=started_at, finished_at=finished_at, error=None)`. The fork writes NO pipeline_runs row — it must NOT call init/_start_run/_end_run (Pitfall 1).

    Seam B (return upgrade — D-11): introduce a small private helper `_fetch_run_result(self, run_id: int) -> RunResult` (RESEARCH Open Question 1 recommendation) that opens `self._db.connect(autocommit=True)` with a `dict_row` cursor, executes `queries.ETL_GET_RUN` with `[run_id]`, fetches one row, returns `_row_to_result(row)`. Replace BOTH `return run_id` sites with `return self._fetch_run_result(run_id)`: the empty-DataFrame early return at line 710 (after its `_end_run(run_id, "success", rows_extracted, 0)`) AND the success path at line 781 (after `_end_run` at line 780). Pitfall 2: there are exactly two `return run_id` sites — change both.

    Use `queries.ETL_GET_RUN` (keyed on run_id), NOT ETL_GET_LAST_RUN (Pitfall 3 — pipeline_name re-SELECT is unsafe under concurrent runs).

    Do NOT touch: init/_start_run/_end_run bodies, the extract block, the transform chain, the existence check, the load-SQL builders, the atomic load block (lines 761-767), or the exception handler (lines 769-778 re-raises immediately — no RunResult needed on the failure path).
  </action>
  <acceptance_criteria>
    - `pycopg/etl.py` `run()` signature is `run(self, pipeline: Pipeline, dry_run: bool = False) -> RunResult`.
    - The dry_run branch appears before the `self.init()` call (no init/_start_run/_end_run reachable when dry_run is True).
    - Zero `return run_id` statements remain in `run()`; both former sites return via `_fetch_run_result`.
    - `_fetch_run_result` uses `queries.ETL_GET_RUN`.
    - `uv run ruff check pycopg/etl.py` is clean.
  </acceptance_criteria>
  <verify>
    <automated>python - <<'PY'
import re, pathlib
src = pathlib.Path("pycopg/etl.py").read_text()
m = re.search(r"def run\(self.*?(?=\n    def |\nclass |\Z)", src, re.S)
body = m.group(0)
assert "dry_run: bool = False) -> RunResult" in body, "signature not upgraded"
assert "return run_id" not in body, "stale return run_id remains"
assert "ETL_GET_RUN" in src, "ETL_GET_RUN not used"
i_dry = body.index("if dry_run")
i_init = body.index("self.init()")
assert i_dry < i_init, "dry_run fork must precede self.init()"
print("run() shape OK")
PY
uv run ruff check pycopg/etl.py</automated>
  </verify>
  <done>run() returns RunResult on both success paths via _fetch_run_result+ETL_GET_RUN; the dry_run branch forks before init(); shape check + ruff pass.</done>
</task>

<task type="auto">
  <name>Task 2: history() and last_run() read methods</name>
  <files>pycopg/etl.py</files>
  <read_first>
    - 19-CONTEXT.md (D-06, D-07, D-12 — LOCKED history/last_run signatures and mapper reuse)
    - 19-RESEARCH.md (section "dict_row Pattern Already in etl.py" with the history()/last_run() reference bodies; "Common Pitfalls" 6)
    - 19-PATTERNS.md (section "ETLAccessor.history() and ETLAccessor.last_run()")
    - pycopg/etl.py lines 514-577 (_start_run/_end_run autocommit dict_row pattern to mirror), line 443 onward (ETLAccessor — add the two methods as peers of run())
    - pycopg/queries.py (ETL_LIST_RUNS binds [name, limit]; ETL_GET_LAST_RUN binds [name])
  </read_first>
  <action>
    Add `history(self, name: str, limit: int = 100) -> list[RunResult]` (D-06): open `self._db.connect(autocommit=True)` with a `dict_row` cursor, execute `queries.ETL_LIST_RUNS` with `[name, limit]`, `fetchall()`, return `[_row_to_result(row) for row in rows]`. Newest-first ordering is already in the constant — do NOT re-author SQL. `limit=100` default cap (D-06).

    Add `last_run(self, name: str) -> RunResult | None` (D-07): open the same autocommit dict_row connection, execute `queries.ETL_GET_LAST_RUN` with `[name]`, `fetchone()`, return `_row_to_result(row) if row is not None else None` (None when no runs exist — SC-3). Use ETL_GET_LAST_RUN directly (Claude's discretion D-07 — dedicated constant, avoids the [0]-index idiom).

    Both methods MUST use `autocommit=True` (Pitfall 6 — read-only, not inside a session transaction). `%s`-bound `name`/`limit` only — no identifier interpolation. numpydoc docstrings on both.

    Do NOT modify run() (Task 1) or any write path.
  </action>
  <acceptance_criteria>
    - `pycopg/etl.py` defines `history(self, name: str, limit: int = 100) -> list[RunResult]` and `last_run(self, name: str) -> RunResult | None`.
    - `history` binds `ETL_LIST_RUNS` with `[name, limit]` and maps via `_row_to_result`; `last_run` binds `ETL_GET_LAST_RUN` with `[name]` and returns None when no row.
    - Both open `connect(autocommit=True)` with a `dict_row` cursor.
    - `uv run ruff check pycopg/etl.py` is clean.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && python - <<'PY'
import inspect, pathlib
from pycopg.etl import ETLAccessor
hs = str(inspect.signature(ETLAccessor.history))
ls = str(inspect.signature(ETLAccessor.last_run))
assert "limit" in hs and "100" in hs, hs
assert "name" in ls, ls
src = pathlib.Path("pycopg/etl.py").read_text()
assert "ETL_LIST_RUNS" in src and "ETL_GET_LAST_RUN" in src
print("history/last_run signatures OK")
PY
uv run ruff check pycopg/etl.py</automated>
  </verify>
  <done>history() and last_run() exist with the locked signatures, bind the existing constants via autocommit dict_row connections, and map through _row_to_result; signature check + ruff pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> read queries | `history(name)` / `last_run(name)` take user-supplied `name`; it travels only as a `%s` value, never an identifier. |
| caller -> run() / dry_run | dry_run path performs extract+transform and writes nothing to pipeline_runs; no new write surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Injection (Tampering) | history/last_run/re-SELECT | mitigate | `name`/`limit`/`run_id` bound as `%s` params; ETL_LIST_RUNS/ETL_GET_LAST_RUN/ETL_GET_RUN have no identifier interpolation. ASVS V5. |
| T-19-04 | Tampering | dry_run branch | mitigate | dry_run forks before init/_start_run/_end_run (D-09) — writes no pipeline_runs row; cannot leave a 'running' row or corrupt run-log state. Verified by SC-4 no-row test in Plan 03. |
| T-19-05 | Information Disclosure | read connection isolation | mitigate | history/last_run use a dedicated autocommit connection (Pitfall 6) — they do not join an active session transaction and cannot read uncommitted session data. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (REQUIREMENTS.md Out-of-Scope). No install step. |
</threat_model>

<success_criteria>
- `run()` returns `RunResult` on both success paths via `_fetch_run_result` + `ETL_GET_RUN` (D-11); no `return run_id` remains.
- `dry_run=True` forks before `init()`, writes no row, returns `RunResult(status='dry_run', rows_loaded=0, run_id=None)` (D-08/D-09).
- `history(name, limit=100) -> list[RunResult]` newest-first via `ETL_LIST_RUNS` (D-06).
- `last_run(name) -> RunResult | None` via `ETL_GET_LAST_RUN` (D-07).
- Both verify `<automated>` blocks exit 0; `ruff check pycopg/etl.py` clean.
</success_criteria>

<output>
Create `.planning/phases/19-sync-runner-query-surface/19-02-SUMMARY.md` when done.
</output>
