---
phase: 18-load-modes-extract
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - tests/test_etl_accessor.py
autonomous: true
requirements: [ETL-02, ETL-03, ETL-04, ETL-05, ETL-06, ETL-16]
must_haves:
  truths:
    - "Extract from a SQL source and a table source (with extract_limit) both produce the expected DataFrame rows in the target after run()"
    - "append re-run doubles the target row count; a missing append target raises ETLTargetNotFoundError"
    - "replace re-run leaves only the latest extract's rows; a mid-load error leaves the ORIGINAL rows intact (replace_atomic_rollback); a missing replace target is created"
    - "upsert re-run updates existing and inserts new rows with no duplicates; a missing upsert target raises ETLTargetNotFoundError"
    - "transform=single-callable transforms rows before load; a transform exception raises ETLTransformError and writes a pipeline_runs row with status='failed' and non-null error fields"
    - "A NaN/NaT cell in the extracted DataFrame lands in the target as SQL NULL, not a float-NaN literal"
    - "A failed load rolls back while the failed pipeline_runs row is still committed (ETL-09 non-regression at the run() level)"
    - "The Phase 17 string-arg run() callers are migrated to run(Pipeline(...)) and the full suite is green"
  artifacts:
    - path: "tests/test_etl_accessor.py"
      provides: "Integration tests for extract, all 3 load modes, transform chain, NaN→NULL, atomicity, ETL-09 run()-level guard; migrated Phase 17 callers"
      contains: "replace_atomic_rollback"
  key_links:
    - from: "tests/test_etl_accessor.py"
      to: "db.etl.run(Pipeline(...))"
      via: "integration assertions against real pycopg_test target tables"
      pattern: "db\\.etl\\.run\\(Pipeline\\("
    - from: "tests/test_etl_accessor.py replace_atomic_rollback"
      to: "post-failure target row state"
      via: "assert baseline rows preserved, target not empty"
      pattern: "replace_atomic_rollback"
---

<objective>
Prove the Plan 02 `run(pipeline)` body against the real `pycopg_test` database with integration
tests covering all six success criteria, AND migrate the three Phase 17 tests that call
`db.etl.run("string")` to the new `run(Pipeline(...))` contract (RESEARCH Q5 — they break
otherwise). The crown-jewel test is `replace_atomic_rollback`: it catches the RESEARCH Q1 landmine
(a mid-load failure must leave the target's ORIGINAL rows intact, not empty).

Purpose: The atomicity and idempotency behaviors only manifest against a real DB — these
integration tests are the behavioral gate for the phase (Plan 02's `inspect`-based checks are only
the structural gate). Migrating the Phase 17 callers keeps the full suite green under the new
signature.
Output: New integration test cases in `tests/test_etl_accessor.py` and migrated Phase 17 callers.
</objective>

<artifacts_this_phase_produces>
NEW test symbols (no production symbols created here):

- New integration test methods in `tests/test_etl_accessor.py` (extract SQL/table+limit, append
  double-count + missing-target, replace latest-only + atomic-rollback + creates-missing, upsert
  no-duplicates + missing-target, transform single + transform-error-failed-run, NaN→NULL, and a
  run()-level ETL-09 failed-load-rolls-back-but-run-row-committed variant). NEW.
- MIGRATED Phase 17 callers: `test_first_run_auto_creates` (line ~210) and
  `test_run_writes_full_row` (line ~222) change `db.etl.run("auto")` / `db.etl.run("demo")` to
  `db.etl.run(Pipeline(name="auto", ...))` / `db.etl.run(Pipeline(name="demo", ...))`. CHANGED.

(Consumes the Plan 02 `run(pipeline)` contract and the Plan 01 builders indirectly.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-load-modes-extract/18-RESEARCH.md
@.planning/phases/18-load-modes-extract/18-VALIDATION.md
@.planning/phases/18-load-modes-extract/18-01-SUMMARY.md
@.planning/phases/18-load-modes-extract/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Phase 17 string-arg run() callers to run(Pipeline(...))</name>
  <files>tests/test_etl_accessor.py</files>
  <read_first>
    - tests/test_etl_accessor.py — the file being modified; read `test_first_run_auto_creates`
      (lines 205-218, calls `db.etl.run("auto")` at line 210) and `test_run_writes_full_row`
      (lines 220-236, calls `db.etl.run("demo")` at line 222 and asserts `pipeline_name == "demo"`).
      Read the existing fixtures `db` (18-23) and `cleanup_pipeline_runs` (25-32), and the module
      imports (1-11).
    - pycopg/etl.py — the new `run(self, pipeline: Pipeline) -> int` signature (Plan 02) and the
      `Pipeline` dataclass fields (name/source/target/load_mode/...). A Pipeline needs a `target`;
      for these run-log-only tests use a trivial pipeline whose extract+load is a harmless no-op or
      a tiny round-trip — see action.
    - .planning/phases/18-load-modes-extract/18-RESEARCH.md — Q5 (the signature change + which tests
      break: lines 448-454).
  </read_first>
  <action>
    Add `from pycopg.etl import Pipeline` to the import block (it currently imports only
    `ETLAccessor`). Migrate the two breaking callers:

    - `test_run_writes_full_row`: replace `run_id = db.etl.run("demo")` with a Pipeline that
      exercises a real extract+load round-trip whose `pipeline_name` is `"demo"`. Create a small
      source+target so the run produces a committed `pipeline_runs` row: e.g. seed a scratch source
      table with 1 row, build `Pipeline(name="demo", source=<scratch_source>, target=<scratch_target>,
      load_mode="replace")` (replace auto-creates the missing target), then assert the
      `pipeline_runs` row has `pipeline_name == "demo"`, `status == "success"`,
      `rows_extracted >= 1`, `rows_loaded >= 1`, and `watermark is None`. Drop the scratch tables in
      cleanup (follow the `uuid.uuid4().hex[:8]` scratch-naming + `DROP TABLE IF EXISTS ... CASCADE`
      pattern already used at lines 248-253, 292).
    - `test_first_run_auto_creates`: replace `db.etl.run("auto")` with
      `db.etl.run(Pipeline(name="auto", source=<scratch_source>, target=<scratch_target>,
      load_mode="replace"))` after seeding a 1-row scratch source; keep the assertion that
      `pipeline_runs` exists afterward. Drop scratch tables in cleanup.

    Keep `test_failed_run_commits_despite_load_rollback` and `test_failed_run_commits_inside_session`
    AS-IS — they call `_start_run`/`_end_run` directly (not `run("string")`), so they do not break.
    Do not change the unit tests in `TestETLAccessorUnit` (they exercise `_start_run`/`_end_run`/`init`,
    not `run()`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_etl_accessor.py -k "first_run_auto_creates or run_writes_full_row" -x -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_etl_accessor.py` contains `from pycopg.etl import Pipeline` (or `Pipeline` in the
      existing `pycopg.etl` import) and no remaining `db.etl.run("` string-literal call:
      `grep -c 'db\.etl\.run("' tests/test_etl_accessor.py` is 0.
    - `uv run pytest tests/test_etl_accessor.py -k "first_run_auto_creates or run_writes_full_row" -x -o addopts=""` exits 0.
    - `test_run_writes_full_row` still asserts `pipeline_name == "demo"` and `watermark is None`.
  </acceptance_criteria>
  <done>The two Phase 17 string-arg callers pass a Pipeline, the full row + auto-create assertions still hold, and no string-literal run() call remains.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests — extract, append/replace/upsert (incl. replace_atomic_rollback), transform chain, NaN→NULL, ETL-09 run()-level guard</name>
  <files>tests/test_etl_accessor.py</files>
  <read_first>
    - tests/test_etl_accessor.py — the file being modified; read `TestETLAccessorIntegration`
      (189-393): the `db` + `cleanup_pipeline_runs` fixtures, the scratch-table naming/cleanup
      idiom (`uuid.uuid4().hex[:8]`, `CREATE TABLE`, `DROP TABLE IF EXISTS ... CASCADE`, all
      `autocommit=True`), and the working atomic seam reference at lines 344-348.
    - pycopg/etl.py — the Plan 02 `run(pipeline)` body: extract (`to_dataframe` + `:lim`), the
      transform chain + `ETLTransformError`, the three load modes, the existence check
      (`ETLTargetNotFoundError` for append/upsert missing target), and the NaN→None conversion.
      Read the `Pipeline` fields you will instantiate.
    - .planning/phases/18-load-modes-extract/18-VALIDATION.md — the "Per-Task Verification Map"
      (the exact `-k` test-name slugs to use: `extract_sql`, `extract_table_limit`,
      `transform_single`, `transform_error_failed_run`, `append_double_count`,
      `append_missing_target`, `replace_latest_only`, `replace_atomic_rollback`,
      `replace_creates_missing`, `upsert_no_duplicates`, `upsert_missing_target`, `nan_to_null`)
      and the "Critical atomicity test" description.
    - .planning/phases/18-load-modes-extract/18-RESEARCH.md — the "Critical atomicity test" (lines
      553-558): seed baseline → run a replace whose load fails mid-INSERT → assert the target STILL
      holds the baseline (NOT empty). And Q2 (NaN→None expectation) + Q3 (existence/create).
    - pycopg/exceptions.py — `ETLTransformError`, `ETLTargetNotFoundError` (to import for
      `pytest.raises`).
  </read_first>
  <action>
    Add integration test methods to `TestETLAccessorIntegration` (each uses the `db` +
    `cleanup_pipeline_runs` fixtures, real `pycopg_test`, scratch tables named with
    `uuid.uuid4().hex[:8]`, dropped in a finally/cleanup). Use the test-name slugs from
    18-VALIDATION.md so the validation `-k` commands resolve. Import `ETLTransformError` and
    `ETLTargetNotFoundError` from `pycopg.exceptions`.

    - `test_extract_sql_source`: seed a scratch source table (2 rows); run
      `Pipeline(name=..., source="SELECT * FROM <src>", target=<tgt>, load_mode="replace")`; assert
      the target holds 2 rows matching the source.
    - `test_extract_table_limit`: seed a scratch source (3 rows); run a pipeline with
      `source=<src>` (table source), `extract_limit=2`, `load_mode="replace"`; assert exactly 2 rows
      landed and `rows_extracted == 2` in the `pipeline_runs` row.
    - `test_transform_single`: seed source (1 row, `val=1`); `transform` = a named function that
      adds a column or doubles `val`; assert the transformed value is in the target.
    - `test_transform_error_failed_run`: `transform` = a function that raises; assert
      `pytest.raises(ETLTransformError)` and that the resulting `pipeline_runs` row (find it via
      `pipeline_name`) has `status='failed'` with non-null `error_message`/`error_traceback`, and
      that the error message names the failing step (substring `transform step 1`).
    - `test_append_double_count`: pre-create the target (so it exists); run an append pipeline twice;
      assert the target row count doubles.
    - `test_append_missing_target`: run an append pipeline against a non-existent target; assert
      `pytest.raises(ETLTargetNotFoundError)`.
    - `test_replace_latest_only`: run a replace pipeline twice with different source contents; assert
      only the second run's rows remain.
    - `test_replace_atomic_rollback` (HIGHEST VALUE): pre-create the target with a baseline row
      (e.g. `val=1`); run a replace pipeline whose load fails mid-INSERT (force the failure — e.g. a
      transform that returns a row that violates a target constraint, OR a target with a CHECK/NOT
      NULL that the extracted row breaks). Assert (a) the run raised, (b) the target STILL contains
      the baseline row and is NOT empty (`1 in vals`), proving TRUNCATE+INSERT rolled back together.
      With the broken session-only seam the target would be `[]`; the correct yielded-conn seam
      preserves the baseline.
    - `test_replace_creates_missing`: run a replace pipeline against a non-existent target; assert it
      is created and populated with the extract's rows.
    - `test_upsert_no_duplicates`: pre-create the target WITH a unique constraint on the conflict
      key; run an upsert pipeline twice (second run changes an existing row's non-key column and adds
      a new key); assert no duplicate keys and the updated value is present.
    - `test_upsert_missing_target`: run an upsert pipeline against a non-existent target; assert
      `pytest.raises(ETLTargetNotFoundError)`.
    - `test_nan_to_null`: build a source whose extracted DataFrame has a NaN/NaT cell in a nullable
      column (e.g. seed a source with a NULL, or a transform that injects `np.nan`); run a replace
      pipeline; assert the target cell is SQL NULL (`row[col] is None`), not a float-NaN.
    - `test_run_level_failed_load_rolls_back_but_run_committed` (ETL-09 run()-level guard): run a
      pipeline whose load fails (reuse the atomic-rollback failure mechanism); assert the target is
      unchanged AND a `pipeline_runs` row with `status='failed'` is committed (queried after the
      raise). This is the run()-level analog of the existing
      `test_failed_run_commits_despite_load_rollback`.

    Use named helper functions (not lambdas) for transforms so error messages are readable. Keep
    each test self-contained with its own scratch tables and cleanup. Tests run against real PG —
    do NOT mock the load path (Pitfall 12).
  </action>
  <verify>
    <automated>uv run pytest tests/test_etl_accessor.py -k "extract or append or replace or upsert or transform or nan_to_null or run_level_failed" -x -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_etl_accessor.py -k replace_atomic_rollback -x -o addopts=""` exits 0
      (the crown-jewel atomicity test passes — target retains baseline after mid-load failure).
    - `uv run pytest tests/test_etl_accessor.py -k "extract_sql or extract_table_limit" -x -o addopts=""` exits 0.
    - `uv run pytest tests/test_etl_accessor.py -k "append_double_count or append_missing_target" -x -o addopts=""` exits 0.
    - `uv run pytest tests/test_etl_accessor.py -k "replace_latest_only or replace_creates_missing" -x -o addopts=""` exits 0.
    - `uv run pytest tests/test_etl_accessor.py -k "upsert_no_duplicates or upsert_missing_target" -x -o addopts=""` exits 0.
    - `uv run pytest tests/test_etl_accessor.py -k "transform_single or transform_error_failed_run" -x -o addopts=""` exits 0.
    - `uv run pytest tests/test_etl_accessor.py -k nan_to_null -x -o addopts=""` exits 0.
    - `tests/test_etl_accessor.py` contains `replace_atomic_rollback`, `ETLTargetNotFoundError`,
      and `ETLTransformError`.
    - `uv run ruff check tests/test_etl_accessor.py` exits 0.
  </acceptance_criteria>
  <done>All six success criteria are proven by integration tests against real PG, the replace_atomic_rollback landmine is caught, NaN→NULL holds, and the ETL-09 run()-level isolation guard passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real `pycopg_test` DB | Integration tests mutate scratch tables in a disposable test DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Tampering | scratch tables left behind between tests | mitigate | Each test creates uniquely-named scratch tables (`uuid.uuid4().hex[:8]`) and drops them in cleanup; `cleanup_pipeline_runs` drops the run-log table. No injection surface — tests are not the validation gate (Plan 01 covers SC-6). |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (zero new deps). |
</threat_model>

<verification>
- `uv run pytest tests/test_etl_accessor.py -x -q -o addopts=""` green (incl. migrated Phase 17 tests).
- `uv run pytest tests/test_etl.py tests/test_etl_accessor.py tests/test_sql_injection.py -x -q -o addopts=""` green (per-wave sample).
- PHASE GATE before /gsd-verify-work: `uv run pytest` green (incl. `--cov-fail-under=94`);
  `uv run ruff check pycopg tests`; `uv run black --check pycopg tests`.
  (Note: 3 full-suite DB tests are pre-existing flaky in the local env per project MEMORY — not
  Phase 18 regressions; if they fail, confirm they fail on `main` too before treating as a gate miss.)
</verification>

<success_criteria>
- Extract (SQL + table + extract_limit) verified against real PG.
- append double-count + missing-target → `ETLTargetNotFoundError` verified.
- replace latest-only + auto-create + `replace_atomic_rollback` (the landmine) verified.
- upsert no-duplicates + missing-target → `ETLTargetNotFoundError` verified.
- transform single applied; transform error → `ETLTransformError` + failed `pipeline_runs` row,
  naming the failing step.
- NaN/NaT → SQL NULL verified.
- ETL-09 run()-level: failed load rolls back while the failed run row commits.
- Phase 17 string-arg callers migrated; full suite green.
</success_criteria>

<output>
Create `.planning/phases/18-load-modes-extract/18-03-SUMMARY.md` when done.
</output>
