---
phase: 20-async-parity-wiring-release
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - pyproject.toml
  - docs/conf.py
  - uv.lock
  - CHANGELOG.md
  - MIGRATION.md
autonomous: false
requirements: [ETL-12, ETL-13]
must_haves:
  truths:
    - "Version is 0.5.0 in pyproject.toml, docs/conf.py, and uv.lock; pycopg.__version__ resolves to 0.5.0 after uv sync"
    - "CHANGELOG.md has a [0.5.0] entry summarizing the ETL feature set (Phases 16-20) with a compare link; MIGRATION.md has a v0.5.0 New Features section (no breaking changes)"
    - "Full-suite coverage gate passes: uv run pytest reports coverage >= 94% with the bare full-suite command (NOT -o addopts=\"\")"
    - "Docstring gate passes: uv run interrogate pycopg --fail-under 95 --quiet exits 0"
    - "Sphinx -W build is clean: uv run sphinx-build -W --keep-going -b html docs docs/_build/html exits 0"
    - "v0.5.0 is tagged, pushed, and a GitHub Release is published, triggering the OIDC trusted PyPI publish (human sign-off)"
  artifacts:
    - path: "pyproject.toml"
      provides: "version = 0.5.0"
      contains: "0.5.0"
    - path: "docs/conf.py"
      provides: "release = 0.5.0"
      contains: "0.5.0"
    - path: "CHANGELOG.md"
      provides: "[0.5.0] release entry"
      contains: "[0.5.0]"
    - path: "uv.lock"
      provides: "pycopg pinned at 0.5.0"
      contains: "0.5.0"
  key_links:
    - from: "pyproject.toml version"
      to: "uv.lock pycopg version"
      via: "uv lock after bump"
      pattern: "version = \"0.5.0\""
    - from: "git tag v0.5.0 + GitHub Release"
      to: ".github/workflows/publish.yml"
      via: "on: release: types: [published] → uv build → pypa/gh-action-pypi-publish (OIDC)"
      pattern: "v0.5.0"
---

<objective>
Run the v0.5.0 release playbook (exact mirror of the Phase 15 / v0.4.0 release). Bump the version everywhere, refresh the lockfile, write the CHANGELOG + MIGRATION entries, pass the three release GATES (coverage >= 94, interrogate >= 95, Sphinx -W clean), then tag, push, and publish to PyPI via GitHub Release.

Purpose: Ship pycopg v0.5.0 (final phase of the ETL milestone). All ETL code, tests, and docs are green from Plans 01-02; this plan packages and publishes.
Output: bumped version, refreshed `uv.lock`, CHANGELOG/MIGRATION entries, green gates, `v0.5.0` tag, GitHub Release → PyPI.

NOTE: This plan is `autonomous: false` because the publish step is irreversible and outward-facing — it requires explicit human sign-off before tagging/publishing. Do NOT auto-publish to PyPI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-async-parity-wiring-release/20-RESEARCH.md
@.planning/phases/20-async-parity-wiring-release/20-VALIDATION.md
@pyproject.toml
@docs/conf.py
@CHANGELOG.md
@MIGRATION.md
</context>

<artifacts_this_phase_produces>
This plan modifies existing release files only — no new code symbols. Version moves `0.4.0 → 0.5.0`; CHANGELOG gains a `[0.5.0]` section + compare link; MIGRATION gains a v0.5.0 New Features section; `uv.lock` re-pins pycopg to `0.5.0`. The tag `v0.5.0` and GitHub Release are new VCS/release artifacts.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.5.0, refresh lockfile, write CHANGELOG + MIGRATION</name>
  <files>pyproject.toml, docs/conf.py, uv.lock, CHANGELOG.md, MIGRATION.md</files>
  <read_first>
    - `pyproject.toml` — line 6 `version = "0.4.0"` (the source of truth; `pycopg/__init__.py.__version__` resolves from installed metadata, so no code edit needed there per RESEARCH §16).
    - `docs/conf.py` — line 17 `release = '0.4.0'`.
    - `CHANGELOG.md` — the Keep-a-Changelog format: the empty `## [Unreleased]` section near the top, the `## [0.4.0] - 2026-06-14` entry as the structure template (Added/Changed/...), and the compare-link footer at the bottom (`[Unreleased]: ...compare/v0.4.0...HEAD`, `[0.4.0]: ...compare/v0.3.1...v0.4.0`).
    - `MIGRATION.md` — the existing structure; v0.5.0 is purely additive (no breaking changes), so a short "New Features" section mirroring the v0.3.0 New Features style is correct (RESEARCH §18).
    - `.github/workflows/publish.yml` — note the `uv lock --check` step runs in CI before build; the lockfile MUST be current or publish fails.
    - `pycopg/etl.py` and the ROADMAP Phase 16-20 entries — source the ETL feature list for the CHANGELOG entry (run/history/last_run/dry_run; append/replace/upsert; transform None/callable/list; pipeline_runs auto-create + init(); RunResult; ETLAccessor/AsyncETLAccessor/RunResult/Pipeline exports; full sync/async parity).
  </read_first>
  <action>
    Set `version = "0.5.0"` in `pyproject.toml` (line 6) and `release = '0.5.0'` in `docs/conf.py` (line 17). Do NOT edit `pycopg/__init__.py` — `__version__` auto-resolves from package metadata after sync (RESEARCH §16).

    Run `uv lock` to re-pin pycopg to `0.5.0` in `uv.lock`, then `uv sync --all-extras --dev` so the installed metadata reflects `0.5.0` (this makes `pycopg.__version__ == "0.5.0"`).

    In `CHANGELOG.md`: add a `## [0.5.0] - 2026-06-15` section directly under `## [Unreleased]` (leave `[Unreleased]` empty above it), following the v0.4.0 entry structure. Under `### Added`, summarize the ETL feature set delivered across Phases 16-20: the `db.etl.*` / `async_db.etl.*` namespace (run, history, last_run, dry_run) with full sync/async parity; `Pipeline` frozen dataclass (source SQL/table, load_mode append/replace/upsert, conflict_columns, transform None/callable/list); `pipeline_runs` run-tracking table auto-created on first run with explicit `db.etl.init()`; `RunResult`; `ETLAccessor`, `AsyncETLAccessor`, `RunResult`, `Pipeline` exported from the top level; `ETLError`/`ETLTargetNotFoundError`/`ETLTransformError` exception hierarchy; async transform dispatch via `asyncio.to_thread`. Note "zero new runtime dependencies". Update the compare-link footer: change `[Unreleased]` to `compare/v0.5.0...HEAD` and add `[0.5.0]: https://github.com/alkimya/pycopg/compare/v0.4.0...v0.5.0`.

    In `MIGRATION.md`: add a short v0.5.0 "New Features (no breaking changes)" section noting the additive ETL layer and the new top-level exports — no migration steps required for existing users.

    Run `uv run ruff check pycopg tests` to confirm nothing broke.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && grep -q '^version = "0.5.0"' pyproject.toml && grep -q "release = '0.5.0'" docs/conf.py && grep -q "^## \[0.5.0\]" CHANGELOG.md && grep -q "compare/v0.4.0...v0.5.0" CHANGELOG.md && grep -A2 '^name = "pycopg"' uv.lock | grep -q '0.5.0' && uv lock --check && uv run python -c "import pycopg; assert pycopg.__version__ == '0.5.0', pycopg.__version__; print('version OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` line `version = "0.5.0"`; `docs/conf.py` `release = '0.5.0'`.
    - `uv.lock` pins pycopg `0.5.0` and `uv lock --check` exits 0.
    - `pycopg.__version__ == "0.5.0"` after `uv sync`.
    - `CHANGELOG.md` has a `## [0.5.0]` section and a `compare/v0.4.0...v0.5.0` link; MIGRATION.md has a v0.5.0 section.
  </acceptance_criteria>
  <done>
    Version is 0.5.0 across pyproject/conf.py/uv.lock/runtime metadata; CHANGELOG [0.5.0] + MIGRATION v0.5.0 written; lockfile current.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pass the three release GATES — coverage >= 94, interrogate >= 95, Sphinx -W clean [BLOCKING]</name>
  <files>(no source edits — gate verification; fix code/docstrings/docs if a gate fails)</files>
  <read_first>
    - `pyproject.toml` — `[tool.pytest.ini_options]` `addopts` line 90 (`--cov-fail-under=94`); `[tool.interrogate]` (fail-under=95, exclude tests/docs). Confirm the bare `uv run pytest` carries the coverage flags.
    - `.planning/phases/20-async-parity-wiring-release/20-VALIDATION.md` — the coverage-gate caveat (Pitfall 5) and the known-flaky-test note.
    - Project memory: the pycopg-flaky-db-tests note — the ~3 pre-existing full-suite DB failures (`TestBehavioralParity::test_create_constructor_parity`, `TestAsyncIntegration::test_async_transaction_fix`, `TestPostGISErrorHandling::test_create_spatial_index_name_parameter`) that are environment-specific and NOT regressions.
  </read_first>
  <action>
    GATE A — COVERAGE (SC-5): run the bare full suite `uv run pytest` (NOT `-o addopts=""` — Pitfall 5: that disables the cov plugin and reports nothing). The gate is `--cov-fail-under=94`. CRITICAL interpretation step: the local env has ~3 pre-existing flaky DB failures that are NOT Phase 20 regressions and NOT in the ETL code path. If `uv run pytest` exits non-zero, inspect WHICH tests failed: if the failures are exactly the three known-flaky tests listed in `<read_first>` (and the two `TestAsyncParity` tests are now GREEN, since Plan 01 wired `async_db.etl`), the ETL work is sound — re-run those three in isolation to confirm they are environment flakiness, and read the printed coverage percentage to confirm >= 94%. Any OTHER failure, or any failure in `test_etl*.py` / `test_parity.py::TestEtlParity`, is a real regression that must be fixed before proceeding. Do NOT lower the coverage threshold. If ETL async code is under-covered, the behavioral tests from Plan 02 should be exercising it; add coverage there if needed.

    GATE B — DOCSTRINGS (SC-5): run `uv run interrogate pycopg --fail-under 95 --quiet`. If it fails, the most likely cause is a missing numpydoc docstring on `AsyncETLAccessor` or one of its methods (added in Plan 01) — add the missing docstrings until the gate passes.

    GATE C — SPHINX (SC-5): run `uv run sphinx-build -W --keep-going -b html docs docs/_build/html`. Must exit 0 (warnings-as-errors). If it fails on an orphan-document warning, confirm `etl` is in the `docs/index.md` toctree (Plan 02 Task 2); fix any cross-reference or syntax warning in `docs/etl.md`.

    Record the measured coverage % and the identities of any flaky failures in the SUMMARY so verification can distinguish them from regressions.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run interrogate pycopg --fail-under 95 --quiet && uv run sphinx-build -W --keep-going -b html docs docs/_build/html && uv run pytest</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` (bare full suite) reports coverage >= 94%; any failures are exclusively the documented pre-existing flaky DB tests (NOT ETL/parity tests), confirmed in isolation.
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
    - `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
  </acceptance_criteria>
  <done>
    All three release gates pass; coverage ratchet (>= 94) held; docstring coverage >= 95; Sphinx -W clean. Flaky-vs-regression distinction recorded in SUMMARY.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Tag v0.5.0, push, and publish to PyPI via GitHub Release [IRREVERSIBLE — human sign-off]</name>
  <files>(release VCS actions — git tag, push, GitHub Release; no repo-file edits beyond the version-bump commit from Task 1)</files>
  <what-built>
    Version bumped to 0.5.0 across pyproject.toml / docs/conf.py / uv.lock; `pycopg.__version__ == "0.5.0"`; CHANGELOG [0.5.0] and MIGRATION v0.5.0 written; all three release gates green (coverage >= 94, interrogate >= 95, Sphinx -W clean). A local `uv build` produced `dist/pycopg-0.5.0-py3-none-any.whl` and `dist/pycopg-0.5.0.tar.gz` for pre-tag verification.
  </what-built>
  <action>
    BLOCKING HUMAN CHECKPOINT — do NOT execute the release sequence until the user types "approved". Publishing to PyPI is irreversible (a version cannot be re-uploaded).

    Pre-checkpoint (autonomous, safe): run `uv build` to produce `dist/pycopg-0.5.0*` and `uv lock --check` to confirm the lockfile is current. Present these results to the user.

    On "approved" only, perform the release sequence (RESEARCH §23): commit the version bump + CHANGELOG/MIGRATION if not already committed; `git tag v0.5.0`; `git push origin <current-branch>` and `git push origin v0.5.0`; create the GitHub Release for tag `v0.5.0` via `gh release create v0.5.0 --title "v0.5.0"` with the CHANGELOG [0.5.0] body. Publishing the Release triggers `.github/workflows/publish.yml` (`on: release: types: [published]`) → `uv build` → `pypa/gh-action-pypi-publish@release/v1` via OIDC trusted publishing. After publish, confirm `pip install pycopg==0.5.0` resolves and the ETL page renders on ReadTheDocs (manual-only post-merge confirmations per 20-VALIDATION.md).
  </action>
  <how-to-verify>
    Before approving, confirm:
    1. The version-bump + CHANGELOG/MIGRATION commit is on a branch (not committing directly to a protected default unless intended) and reviewed.
    2. `uv build` succeeds locally: `cd /home/loc/workspace/pycopg && uv build` produces `dist/pycopg-0.5.0*`.
    3. `uv lock --check` exits 0 (CI publish gate requires a current lockfile).
    4. You are ready to publish v0.5.0 to PyPI — this is IRREVERSIBLE (a version cannot be re-uploaded to PyPI).

    On approval, the executor performs the release sequence (RESEARCH §23):
    - Commit the version bump + CHANGELOG/MIGRATION if not already committed.
    - `git tag v0.5.0`
    - `git push origin <branch>` and `git push origin v0.5.0`
    - Create the GitHub Release for tag `v0.5.0` (e.g. `gh release create v0.5.0 --title "v0.5.0" --notes-from-tag` or with the CHANGELOG [0.5.0] body). Publishing the Release triggers `.github/workflows/publish.yml` (`on: release: types: [published]`) → `uv build` → `pypa/gh-action-pypi-publish@release/v1` via OIDC trusted publishing.

    After publish (manual-only post-merge confirmations, per 20-VALIDATION.md):
    - Confirm `pip install pycopg==0.5.0` resolves from PyPI.
    - Confirm the ETL page renders on ReadTheDocs.
  </how-to-verify>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv build && uv lock --check && ls dist/pycopg-0.5.0-py3-none-any.whl dist/pycopg-0.5.0.tar.gz</automated>
  </verify>
  <done>
    After human approval: `v0.5.0` tag pushed, GitHub Release published, PyPI publish workflow triggered; `pip install pycopg==0.5.0` resolves and the ETL page is live on ReadTheDocs.
  </done>
  <resume-signal>Type "approved" to tag + push + create the GitHub Release (publishes to PyPI), or describe blocking issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → PyPI / GitHub Release | publishing an artifact to a public index (supply-chain boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering / Supply chain | PyPI publish artifact | mitigate | Publish goes through `pypa/gh-action-pypi-publish@release/v1` using OIDC trusted publishing (no long-lived token); `uv lock --check` enforces a current lockfile before build. The publish is gated behind a blocking human checkpoint (Task 3). |
| T-20-05 | Repudiation | release provenance | accept | GitHub Release + signed git tag provide an auditable release record; standard for the project (Phase 15 precedent). |
| T-20-SC | Tampering | npm/pip/cargo installs | n/a | No NEW packages installed; only the existing pycopg package is built/published (RESEARCH §Package Legitimacy Audit: N/A). |

Per RESEARCH §Security Domain: Phase 20 adds no new runtime attack surface. The publish path reuses the audited Phase 15 OIDC workflow.
</threat_model>

<verification>
- Version 0.5.0 everywhere; `uv lock --check` clean; `pycopg.__version__ == "0.5.0"`.
- Three gates green: `uv run pytest` (coverage >= 94), `uv run interrogate pycopg --fail-under 95 --quiet`, `uv run sphinx-build -W --keep-going -b html docs docs/_build/html`.
- `v0.5.0` tagged + pushed; GitHub Release published; PyPI publish workflow triggered (post-merge confirmation: `pip install pycopg==0.5.0`).
</verification>

<success_criteria>
- SC-5 fully satisfied: docs render (-W clean) + live on RTD; interrogate >= 95; full-suite coverage >= 94 (ratchet held); CHANGELOG + MIGRATION updated; `pycopg==0.5.0` tagged and published to PyPI.
- v0.5.0 milestone shipped (final phase complete).
</success_criteria>

<output>
Create `.planning/phases/20-async-parity-wiring-release/20-03-SUMMARY.md` when done.
</output>
