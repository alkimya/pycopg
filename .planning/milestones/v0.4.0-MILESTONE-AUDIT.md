---
milestone: v0.4.0
milestone_name: Quality & Spatial Helpers
audited: 2026-06-14
status: passed
scores:
  requirements: 46/46
  phases: 7/7
  integration: 14/14
  flows: 4/4
nyquist:
  compliant_phases: [11, 14]
  partial_phases: [9, 13, 15]
  missing_phases: [10, 12]
  overall: partial
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 12-refactoring-brancher-les-abstractions
    items:
      - "Coverage ratchet stretch goal 95 not met: gate honestly flipped 90->92 in Phase 12 (D-07 'never freeze an unmet gate'), then 92->94 by Phase 14 (measured 94.09%). REF-01..05 ARE satisfied (VERIFICATION passed); the unchecked ROADMAP box on Phase 12 reflects the unmet coverage-95 stretch target, not an unsatisfied requirement. Remaining ~1pt to 95 is DB/IO paths (subprocess, engine.dispose) structurally out of scope."
  - phase: cross-cutting
    items:
      - "TableNotFound is defined in pycopg/exceptions.py and exported in __all__ but has ZERO internal raise sites (confirmed: grep 'raise TableNotFound' pycopg/ -> 0). Exported for user-facing `except` blocks only. Non-blocking."
  - phase: 15-release-v0-4-0-pypi-rtd
    items:
      - "docs/conf.py sets `release = '0.4.0'` but has no explicit `version =` short-version line; Sphinx defaults the short version to empty string in the HTML theme header. Cosmetic only — RTD build is green and content is correct. Surfaced by the integration checker on this audit run."
  - phase: documentation-tracking
    items:
      - "SUMMARY frontmatter inconsistently records requirements: some use `requirements:`, some `requirements_completed:`, some only `dependency_graph.provides`, Phase 9 omits the field entirely. The 3-source cross-check still resolves cleanly because VERIFICATION.md (authoritative) enumerates every REQ-ID as SATISFIED. Tracking-hygiene note for future milestones, not a coverage gap."
  - phase: nyquist
    items:
      - "Phases 10 and 12 have no VALIDATION.md; phases 9, 13, 15 have VALIDATION.md with nyquist_compliant: false (release/tooling/doc phases are smoke/manual-verified by design). Optional backfill: /gsd-validate-phase 10 and /gsd-validate-phase 12."
---

# v0.4.0 Milestone Audit — Quality & Spatial Helpers

**Audited:** 2026-06-14 (re-run — independent fresh audit)
**Status:** ✅ PASSED
**Scope:** Phases 9–15 (the v0.4.0 milestone; Phases 1–7 belong to v0.3.0, hotfix v0.3.1).
**Definition of done:** Ship v0.4.0 on PyPI + ReadTheDocs with full sync/async parity,
spatial helpers, numpydoc docs, residual security fixes, and uv tooling — all under a
coverage ratchet.

This audit re-reads all 7 phase VERIFICATION.md files, performs the 3-source requirements
cross-reference, re-runs the cross-phase integration checker, and independently spot-checks the
live codebase (version, tag, spatial accessors, inheritance, exports). It supersedes the prior
in-completion audit and confirms the same verdict, plus one newly-surfaced cosmetic warning.

## Requirements Coverage — 46/46 satisfied

Cross-referenced across three independent sources (REQUIREMENTS.md traceability ↔ phase
VERIFICATION.md ↔ SUMMARY frontmatter). VERIFICATION.md is authoritative; the status matrix
resolves every REQ-ID to **satisfied** (passed VERIFICATION → satisfied).

| Group | IDs | Phase | VERIFICATION | Traceability | Final |
|-------|-----|-------|--------------|--------------|-------|
| Tooling | TOOL-01..05 | 9 | passed | `[x]` ×5 | ✅ satisfied |
| Security | SEC-01..06 | 10 | passed | `[x]` ×6 | ✅ satisfied |
| Parity | PAR-01..09 | 11 | passed | `[x]` ×9 | ✅ satisfied |
| Refactoring | REF-01..05 | 12 | passed (1 override) | `[x]` ×5 | ✅ satisfied |
| Documentation | DOC-06..12 | 13 | passed | `[x]` ×7 | ✅ satisfied |
| Spatial | SPAT-01..06 | 14 | passed | `[x]` ×6 | ✅ satisfied |
| Release | REL-01..06 | 15 | passed | `[x]` ×6 | ✅ satisfied |

**Orphan detection:** none. Every REQ-ID in the traceability table maps to exactly one phase
that carries a `passed` VERIFICATION.md enumerating it as SATISFIED.

**Naming note:** Phase 14 plan/SUMMARY frontmatter uses the `SPA-*` prefix while
REQUIREMENTS.md / ROADMAP use `SPAT-*`. This is a label variant mapped 1:1 — not a coverage
gap (SPAT-01..06 all Complete, VERIFICATION passed). Recorded for tidy-up.

**REF-05 note:** the milestone ROADMAP framed Phase 12 with a "coverage ratchet → 95" stretch
label. The gate was honestly flipped to 92 (Phase 12) then 94 (Phase 14) against measured
coverage, per the locked D-07 convention "never freeze an unmet gate." REF-05 itself is
satisfied (gate raised and passing); the 95 figure was an unmet stretch target, tracked as
tech debt below — it does not make any requirement unsatisfied.

## Phase Verifications — 7/7 passed

| Phase | VERIFICATION | Status | Notes |
|-------|--------------|--------|-------|
| 9 — Migration uv | 09-VERIFICATION.md | passed | 13/13 truths; pure tooling/CI/doc phase |
| 10 — Sécurité résiduelle | 10-VERIFICATION.md | passed | 10/10 truths; B1/B2/B3/B5 + SEC-05 |
| 11 — Parité sync/async | 11-VERIFICATION.md | passed | 9/9 PAR reqs; C1/C2/C3 closed |
| 12 — Refactoring | 12-VERIFICATION.md | passed | 5/5 (1 user-approved override: gate 92 not 95) |
| 13 — Qualité documentaire | 13-VERIFICATION.md | passed | 6/6; re-verified after gap closure (2cd22be) |
| 14 — Spatial helpers | 14-VERIFICATION.md | passed | 11/11 truths; spatial.py + both accessors |
| 15 — Release v0.4.0 | 15-VERIFICATION.md | passed | 6/6; v0.4.0 live on PyPI + RTD |

All 7 phases verified. One override (Phase 12 coverage gate) recorded and user-accepted.

## Cross-Phase Integration — 14/14 wired, 0 broken, 4/4 E2E flows

(from gsd-integration-checker, re-run on this audit)

**Connected:** 14 cross-phase connections. **Orphaned exports:** 0. **Missing connections:** 0.

1. Public import surface — WIRED. `from pycopg import Database, AsyncDatabase, PooledDatabase, AsyncPooledDatabase, SpatialAccessor, AsyncSpatialAccessor` + Config, Migrator + 8 exceptions + 4 util functions; every `__all__` name resolves to a real definition.
2. Inheritance chain — WIRED. `Database(DatabaseBase, QueryMixin)` and `AsyncDatabase(DatabaseBase, QueryMixin)`; `from_env`/`from_url`/`__repr__`/`_build_batch_insert_sql` resolve from base.py.
3. queries.py constants — WIRED. 28 refs (database.py) / 29 refs (async_database.py); no call site still inlines a constant's SQL.
4. Spatial accessors — WIRED. `db.spatial` / `async_db.spatial` lazy properties resolve to `SpatialAccessor` / `AsyncSpatialAccessor`; PostGIS guard via `has_extension` (sync at construction, async via `_check_postgis()` on every method).
5. 11 spatial helpers — WIRED at parity (contains, within, intersects, dwithin, distance, nearest, area, perimeter, centroid, buffer, transform on both accessors); `validate_identifiers` + `%s` params throughout.
6. Exceptions — WIRED. ExtensionNotAvailable (7+7 raise sites), DatabaseExists (1+1), InvalidIdentifier (via validate_identifiers); TableNotFound exported for user `except` only (no internal raise — benign).
7. Version coherence — WIRED. `importlib.metadata` → 0.4.0; pyproject 0.4.0; docs/conf.py release 0.4.0; no leftover 0.3.1.
8. Sync/async allow-list — WIRED. Only documented divergence: `engine`/`async_engine` and async-only `listen`.

**E2E flows (static-verified, no live DB):** `import pycopg; __version__ → 0.4.0`; `Database.from_env()` constructs; `db.spatial.contains(...)` → accessor → builder → execute; async equivalent symmetric. **4/4 complete, 0 broken.**

## Independent Codebase Spot-Checks (orchestrator)

| Check | Result |
|-------|--------|
| `pyproject.toml` version | `0.4.0` ✓ |
| Coverage gate | `--cov-fail-under=94` ✓ |
| git tag | `v0.4.0` present ✓ |
| `__version__` mechanism | `importlib.metadata.version("pycopg")` ✓ |
| `pycopg/spatial.py` | 2730 lines; `SpatialAccessor` + `AsyncSpatialAccessor` + `_resolve_geometry` ✓ |
| Inheritance | both classes `(DatabaseBase, QueryMixin)` ✓ |
| Exports | spatial accessors + DatabaseExists in `__all__` ✓ |
| `raise TableNotFound` in pycopg/ | 0 (confirms benign tech debt) ✓ |
| `version =` in docs/conf.py | absent (only `release =`) — new cosmetic warning ✓ |

## Tech Debt (non-blocking)

- **Phase 12 coverage-95 stretch unmet:** gate is honest at 94 (measured 94.09%), not the 95 stretch label. REF-01..05 satisfied. Remaining ~1pt is DB/IO paths out of scope.
- **TableNotFound** has no internal raise site (exported for user `except` only). Benign.
- **docs/conf.py missing `version =`** short-version line (only `release` set) — cosmetic HTML-theme display; RTD green. *(new this audit)*
- **SUMMARY frontmatter** inconsistently records requirements across phases — tracking-hygiene note; resolved cleanly by VERIFICATION cross-check.
- **SPA-/SPAT- prefix variance** between Phase 14 plans and REQUIREMENTS.md — label tidy-up.
- **Nyquist:** phases 10, 12 lack VALIDATION.md; 9/13/15 are `nyquist_compliant: false` (release/tooling/doc phases smoke/manual-verified by design). Optional backfill via `/gsd-validate-phase 10` / `/gsd-validate-phase 12`.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Classification |
|-------|---------------|-----------|----------------|
| 9 | exists | false (draft) | PARTIAL (tooling — smoke) |
| 10 | missing | — | MISSING |
| 11 | exists | true (verified) | COMPLIANT |
| 12 | missing | — | MISSING |
| 13 | exists | false (draft) | PARTIAL (docs — interrogate gate) |
| 14 | exists | true (ready) | COMPLIANT |
| 15 | exists | false (draft) | PARTIAL (release — smoke/manual) |

**Overall:** partial. Discovery-only (this audit never auto-runs `/gsd-validate-phase`).

## Verdict

**PASSED.** All 46 v1 requirements satisfied (3-source cross-reference, 0 unsatisfied, 0
orphaned), all 7 phases verified (1 user-accepted override), cross-phase integration clean
(14/14 wired, 0 broken, 4/4 E2E flows complete), and v0.4.0 is live and installable from PyPI
with RTD green. Accumulated tech debt is non-blocking and recorded above. The v0.4.0 milestone
is ready to archive.
