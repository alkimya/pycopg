---
phase: 09-migration-uv-outillage-projet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .python-version
autonomous: false
requirements: [TOOL-01, TOOL-02]
must_haves:
  truths:
    - "Contributor can provision the dev env with a single command: uv sync --all-extras --dev (D-03)"
    - "Dev tools (pytest/ruff/black/...) are no longer published to PyPI as a [dev] extra"
    - "uv.lock and .python-version are committed for reproducible dev + CI environments"
    - "uv sync --locked succeeds against the committed lockfile (lockfile is fresh)"
  artifacts:
    - path: "pyproject.toml"
      provides: "PEP 735 [dependency-groups].dev table; user-facing extras retained; no [project.optional-dependencies].dev"
      contains: "[dependency-groups]"
    - path: "uv.lock"
      provides: "Universal cross-platform lockfile, committed"
    - path: ".python-version"
      provides: "Local dev interpreter pin (3.12)"
      contains: "3.12"
  key_links:
    - from: "pyproject.toml [dependency-groups].dev"
      to: "uv sync --dev"
      via: "uv resolves dev group"
      pattern: "\\[dependency-groups\\]"
    - from: "uv.lock"
      to: "uv sync --locked"
      via: "lockfile freshness validation in CI"
      pattern: "uv sync --locked"
---

<objective>
Migrate the dependency model in `pyproject.toml` from classic pip extras to the uv-native model, then generate and commit the reproducibility artifacts (`uv.lock`, `.python-version`). This is the foundation every other plan in the phase syncs against.

Concretely: move the five dev tools (pytest, pytest-cov, pytest-asyncio, black, ruff) out of `[project.optional-dependencies].dev` and into a PEP 735 `[dependency-groups].dev` table, delete the old `dev` extra entirely (D-01, D-02), keep the user-facing extras (`dotenv`/`geo`/`timescale`/`all`) unchanged, then run `uv lock` and `uv python pin 3.12` and commit both generated files.

Purpose: Satisfies TOOL-01 (one-command contributor setup via `uv sync --all-extras --dev`) and TOOL-02 (committed `uv.lock` + `.python-version`). Without this, the CI and doc plans have nothing to sync against.
Output: Modified `pyproject.toml`, new committed `uv.lock`, new committed `.python-version`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-migration-uv-outillage-projet/09-CONTEXT.md
@.planning/phases/09-migration-uv-outillage-projet/09-RESEARCH.md
@.planning/phases/09-migration-uv-outillage-projet/09-PATTERNS.md
@.planning/phases/09-migration-uv-outillage-projet/09-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This phase (across all four plans) creates the following new symbols/artifacts. The source-grounding pass MUST treat these as newly created (not drift):

- New files: `uv.lock`, `.python-version`, `.github/workflows/tests.yml`, `Makefile`
- New `pyproject.toml` table: `[dependency-groups]` (with `dev` key)
- New `README.md` section: `## Development`
- New `Makefile` targets: `install`, `test`, `lint`, `format`, `build`

This plan (09-01) specifically produces: the `[dependency-groups].dev` table in `pyproject.toml`, `uv.lock`, `.python-version`. It also DELETES `[project.optional-dependencies].dev`.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy gate for the five dev packages (package-install precondition)</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/09-migration-uv-outillage-projet/09-RESEARCH.md (## Package Legitimacy Audit — all five packages tagged [ASSUMED] because slopcheck was unavailable)
    - pyproject.toml (current [project.optional-dependencies].dev block, lines 61-67)
  </read_first>
  <what-built>
    Nothing is installed yet. RESEARCH.md tagged pytest, pytest-cov, pytest-asyncio, black, and ruff as [ASSUMED] (slopcheck binary was unavailable on the research machine). These are the exact same five packages already present in the current `[project.optional-dependencies].dev` block — this migration moves them to `[dependency-groups].dev`, it does not add new packages. Per the package legitimacy gate, an [ASSUMED] package requires human confirmation before `uv lock`/`uv sync` resolves and installs it.
  </what-built>
  <how-to-verify>
    Confirm each package is the genuine, well-known PyPI project (all are industry-standard, multi-year history):
    1. pytest — https://pypi.org/project/pytest/
    2. pytest-cov — https://pypi.org/project/pytest-cov/
    3. pytest-asyncio — https://pypi.org/project/pytest-asyncio/
    4. black — https://pypi.org/project/black/
    5. ruff — https://pypi.org/project/ruff/
  </how-to-verify>
  <action>
    BLOCKING package-legitimacy checkpoint (auto-approval disabled — never auto-advanced regardless of workflow.auto_advance). Present the five [ASSUMED] packages (pytest, pytest-cov, pytest-asyncio, black, ruff) and their pypi.org URLs above to the human. Do NOT run `uv lock` or `uv sync` (Task 3) until the human types "approved". These are the same five packages already declared in the current `[project.optional-dependencies].dev` block — no new package is introduced. If the human flags any package, stop and surface the concern instead of proceeding.
  </action>
  <verify>
    <automated>grep -Fq 'pytest' pyproject.toml && grep -Fq 'pytest-cov' pyproject.toml && grep -Fq 'pytest-asyncio' pyproject.toml && grep -Fq 'black' pyproject.toml && grep -Fq 'ruff' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - Human has confirmed all five packages are legitimate (or flagged any concern)
    - No new package beyond these five is introduced into the dev group (the five names already appear in pyproject.toml today)
  </acceptance_criteria>
  <done>Human approved the five dev packages; no package beyond pytest/pytest-cov/pytest-asyncio/black/ruff is added; resolution (Task 3) may proceed.</done>
  <resume-signal>Type "approved" to proceed with the pyproject migration and lockfile generation, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Migrate pyproject.toml to PEP 735 [dependency-groups].dev</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (CURRENT FILE — see lines 45-67 for the [project.optional-dependencies] block, lines 84-100 for pytest/coverage config to preserve)
    - .planning/phases/09-migration-uv-outillage-projet/09-PATTERNS.md (### pyproject.toml — MODIFY — has the exact before/after TOML shape)
    - .planning/phases/09-migration-uv-outillage-projet/09-RESEARCH.md (## Architecture Patterns → Pattern 1: PEP 735 [dependency-groups]; § Code Examples → pyproject.toml after migration)
  </read_first>
  <action>
    In `pyproject.toml`, perform a clean migration of the dev dependencies (D-01, D-02):
    1. DELETE the entire `dev = [...]` entry from `[project.optional-dependencies]` (currently lines 61-67). Do not leave a `dev = []` stub — remove it completely (D-02, no dual source of truth).
    2. KEEP the user-facing extras `dotenv`, `geo`, `timescale`, `all` in `[project.optional-dependencies]` exactly as-is — they are published to PyPI.
    3. ADD a new top-level `[dependency-groups]` table (place it directly after the `[project.optional-dependencies]` block) with a `dev` key listing the same five tools and the same version specifiers: `pytest>=7.0.0`, `pytest-cov>=4.0.0`, `pytest-asyncio>=0.23.0`, `black>=23.0.0`, `ruff>=0.1.0`. Use the exact shape in the PATTERNS.md "After migration" excerpt.
    4. Do NOT change `[build-system]` (stays hatchling), `[tool.pytest.ini_options]` (keep `--cov-fail-under=70` exactly — the ratchet starts Phase 10, not here), or `[tool.coverage.*]`.
    5. Do NOT add a `[tool.uv]` section — Claude's Discretion per RESEARCH.md Open Question 3 is to start without one.
    Refer to PATTERNS.md for the exact TOML shape; do not invent new specifiers.
  </action>
  <verify>
    <automated>grep -q '^\[dependency-groups\]' pyproject.toml && ! ( awk '/^\[project.optional-dependencies\]/{f=1} /^\[dependency-groups\]/{f=0} f' pyproject.toml | grep -qE '^dev *=' ) && grep -q -- '--cov-fail-under=70' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '\[dependency-groups\]' pyproject.toml` succeeds
    - The `[project.optional-dependencies]` block no longer contains a `dev =` key (`awk` slice of that block does NOT match `^dev *=`)
    - `[project.optional-dependencies]` still contains `dotenv`, `geo`, `timescale`, `all`
    - `grep -q -- '--cov-fail-under=70' pyproject.toml` still succeeds (gate unchanged)
    - No `[tool.uv]` section added; `[build-system]` still `requires = ["hatchling"]`
  </acceptance_criteria>
  <done>pyproject.toml has `[dependency-groups].dev` with the five tools, no `dev` extra in optional-dependencies, user extras intact, coverage gate at 70, hatchling backend unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Generate and commit uv.lock and .python-version</name>
  <files>uv.lock, .python-version</files>
  <read_first>
    - pyproject.toml (MUST be the already-migrated file from Task 2 — the lock resolves against it)
    - .planning/phases/09-migration-uv-outillage-projet/09-PATTERNS.md (### uv.lock — CREATE; ### .python-version — CREATE — generation commands)
    - .planning/phases/09-migration-uv-outillage-projet/09-RESEARCH.md (## Architecture Patterns → Pattern 2: uv.lock and .python-version; Pitfall 7: uv.lock Not Committed)
  </read_first>
  <action>
    From the repo root `/home/loc/workspace/pycopg`, run the uv generation commands against the migrated `pyproject.toml` (only after the Task 1 legitimacy checkpoint is approved):
    1. `uv python pin 3.12` — creates `.python-version` containing `3.12` (Claude's Discretion default, matches RTD + publish.yml).
    2. `uv lock` — generates the universal cross-platform `uv.lock` resolving the dependencies + `[dependency-groups].dev`.
    Do NOT hand-author either file. Both MUST be committed (Pitfall 7: CI uses `uv sync --locked` which errors if `uv.lock` is absent). Stage them with `git add uv.lock .python-version pyproject.toml`.
  </action>
  <verify>
    <automated>test -f .python-version && grep -q '3.12' .python-version && test -f uv.lock && uv sync --locked >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `.python-version` exists and contains `3.12` (`cat .python-version` shows `3.12`)
    - `uv.lock` exists at repo root
    - `uv sync --locked` exits 0 (lockfile is fresh against the migrated pyproject.toml)
    - `uv sync --all-extras --dev` exits 0 and creates `.venv/` (full contributor environment resolves — TOOL-01; this is the D-03 contributor setup command)
    - Both `uv.lock` and `.python-version` are staged for commit (`git ls-files --cached` will include them after commit)
  </acceptance_criteria>
  <done>uv.lock and .python-version exist, are committed, and `uv sync --locked` plus `uv sync --all-extras --dev` both succeed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry → local lockfile resolution | `uv lock` fetches package metadata/hashes from PyPI; untrusted package names cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | `uv lock` / `uv sync` resolving the five dev packages | mitigate | Package Legitimacy Gate: Task 1 is a `checkpoint:human-verify` (blocking-human, never auto-approved) confirming pytest/pytest-cov/pytest-asyncio/black/ruff on pypi.org before any resolve/install. These are the same packages already in the repo today; risk is the missing slopcheck verification only. Lockfile pins exact versions + hashes after approval. |
| T-09-LOCK | Tampering | stale/absent `uv.lock` consumed downstream by CI | accept | `uv.lock` committed; CI (plan 09-02) uses `uv sync --locked` which fails fast on a stale lock. No silent staleness path. |

No high-severity threats. T-09-SC is low (industry-standard tools, multi-year history) and gated by the human checkpoint.
</threat_model>

<verification>
- `grep -q '\[dependency-groups\]' pyproject.toml` (TOOL-01)
- `awk` slice of `[project.optional-dependencies]` contains no `dev =` key (TOOL-01, D-02)
- `grep -q -- '--cov-fail-under=70' pyproject.toml` (coverage gate untouched)
- `cat .python-version` contains `3.12` (TOOL-02)
- `git ls-files uv.lock` non-empty after commit (TOOL-02)
- `uv sync --locked` exits 0; `uv sync --all-extras --dev` exits 0 with `.venv/` created (TOOL-01/02)
</verification>

<success_criteria>
- TOOL-01: `uv sync --all-extras --dev` provisions the full dev environment from the migrated pyproject; `[dependency-groups].dev` present; old `dev` extra gone.
- TOOL-02: `uv.lock` and `.python-version` (`3.12`) committed; `uv sync --locked` succeeds.
- Coverage gate unchanged at 70; build backend stays hatchling; no `[tool.uv]` section added.
</success_criteria>

<output>
Create `.planning/phases/09-migration-uv-outillage-projet/09-01-SUMMARY.md` when done.
</output>
