---
phase: 09-migration-uv-outillage-projet
plan: 04
type: execute
wave: 3
depends_on: ["09-01"]
files_modified:
  - CLAUDE.md
  - README.md
  - Makefile
autonomous: true
requirements: [TOOL-05]
must_haves:
  truths:
    - "Contributor docs (CLAUDE.md) use uv commands, not pip install -e / venv activation"
    - "CLAUDE.md no longer references the stale path solaris/pycopg or version 0.2.0"
    - "README has a Development section with uv commands; Installation section keeps pip install pycopg"
    - "A Makefile exposes install/test/lint/format/build targets delegating to uv"
    - "No contributor doc references pip install pycopg[dev] (the removed extra)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Contributor instructions using uv; correct path and version"
      contains: "uv sync --all-extras --dev"
    - path: "README.md"
      provides: "Development section (uv) + preserved Installation section (pip)"
      contains: "## Development"
    - path: "Makefile"
      provides: "Thin uv wrapper targets install/test/lint/format/build"
      contains: ".PHONY"
  key_links:
    - from: "Makefile targets"
      to: "uv commands"
      via: "thin delegation (uv sync / uv run / uv build)"
      pattern: "uv (sync|run|build)"
    - from: "README ## Development"
      to: "uv sync --all-extras --dev"
      via: "contributor setup command"
      pattern: "uv sync --all-extras --dev"
---

<objective>
Bring the contributor documentation in line with the real uv toolchain established by plan 09-01. Three artifacts (D-08): fix `CLAUDE.md` (stale path, stale version, pip/venv commands → uv), add a `## Development` section to `README.md` (uv commands) while keeping the user-facing `## Installation` section on `pip install pycopg`, and create a `Makefile` of thin uv wrapper targets.

Purpose: Satisfies TOOL-05 (contributor docs use uv; end-user docs keep pip). Maintains the hard boundary: users → `pip install pycopg`, contributors → `uv`.
Output: Modified `CLAUDE.md`, modified `README.md`, new `Makefile`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-migration-uv-outillage-projet/09-CONTEXT.md
@.planning/phases/09-migration-uv-outillage-projet/09-RESEARCH.md
@.planning/phases/09-migration-uv-outillage-projet/09-PATTERNS.md
@.planning/phases/09-migration-uv-outillage-projet/09-VALIDATION.md
@CLAUDE.md
@README.md
</context>

<artifacts_this_phase_produces>
This plan (09-04) produces: a new `Makefile` (targets `install`/`test`/`lint`/`format`/`build`) and a new `## Development` section in `README.md`. It also rewrites the stale `## Version` + `## Environnement` blocks of `CLAUDE.md`. The source-grounding pass MUST treat the `Makefile`, the new `## Development` README section, and the five Makefile targets as newly created (not drift). The uv commands referenced here (`uv sync --all-extras --dev`, `uv run pytest`, `uv build`) were made real by plan 09-01; this plan depends on 09-01.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create Makefile with thin uv wrapper targets</name>
  <files>Makefile</files>
  <read_first>
    - .planning/phases/09-migration-uv-outillage-projet/09-PATTERNS.md (### Makefile — CREATE — full content pattern + TAB-indentation requirement)
    - .planning/phases/09-migration-uv-outillage-projet/09-RESEARCH.md (## Architecture Patterns → Pattern 5 Makefile thin wrappers)
  </read_first>
  <action>
    Create a `Makefile` at the repo root with thin uv wrapper targets (D-10), following the PATTERNS.md "### Makefile — CREATE" content exactly. Required: a `.PHONY: install test lint format build` line, then targets:
    - `install:` → `uv sync --all-extras --dev`
    - `test:` → `uv run pytest`
    - `lint:` → `uv run ruff check pycopg tests`
    - `format:` → `uv run black pycopg tests` then `uv run ruff check --fix pycopg tests`
    - `build:` → `uv build`
    CRITICAL: recipe lines MUST be indented with a literal TAB character, not spaces (Make requirement) — otherwise `make` errors with "missing separator". No logic in the Makefile beyond these delegations.
  </action>
  <verify>
    <automated>test -f Makefile && grep -q '^\.PHONY:.*install.*test.*lint.*format.*build' Makefile && printf 'tab-check: ' && grep -Pq '^\t' Makefile && make -n install test lint format build >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Makefile` succeeds
    - `.PHONY` line lists `install test lint format build`
    - Recipe lines are TAB-indented (`grep -Pq '^\t' Makefile` succeeds — no "missing separator")
    - `make -n install test lint format build` exits 0 and echoes `uv sync --all-extras --dev`, `uv run pytest`, `uv run ruff check`, `uv run black`, `uv build`
  </acceptance_criteria>
  <done>Makefile exists with TAB-indented `install`/`test`/`lint`/`format`/`build` targets that delegate to uv; `make -n` dry-run of all five exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CLAUDE.md contributor commands to uv</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (CURRENT — stale `## Version` v0.2.0 at line 13; `## Environnement` block lines 16-25 with `solaris/pycopg` path + `source venv/activate` + `pip install -e ".[all,dev]"`)
    - .planning/phases/09-migration-uv-outillage-projet/09-PATTERNS.md (### CLAUDE.md — MODIFY — exact before/after content with corrected version + uv commands)
    - .planning/phases/09-migration-uv-outillage-projet/09-RESEARCH.md (Pitfall 4 — `dev` extra still referenced after removal; grep for `[dev]`)
  </read_first>
  <action>
    Edit `CLAUDE.md` per D-09, using the PATTERNS.md "### CLAUDE.md — MODIFY" after-content:
    1. Fix the stale path in the `## Environnement` code block: `cd /home/loc/workspace/solaris/pycopg && source venv/activate` → `cd /home/loc/workspace/pycopg` (no venv activation; uv manages `.venv`).
    2. Replace `pip install -e ".[all,dev]"` with `uv sync --all-extras --dev`.
    3. Replace `pytest tests/ -v` with uv-run equivalents: `uv run pytest tests/ -x -q` (quick) and `uv run pytest` (full suite with coverage gate); add `uv run ruff check pycopg tests` (lint) and `uv run black pycopg tests` (format).
    4. Bump the stale version mention `**pycopg v0.2.0**` → `**pycopg v0.3.1**` (matches pyproject `version = "0.3.1"`).
    5. Ensure NO `pip install pycopg[dev]`, no `pip install -e`, no `solaris/pycopg`, no bare `venv/` activation remains anywhere in the file (Pitfall 4).
    Leave the `## Règles importantes`, `## API principale`, and `## Documentation` sections intact (note: rule 2 mentioning `pycopg/venv/` may be left as-is per D-12 — the old local `venv/` is not forcibly removed; do not introduce NEW `pip install -e`/activation instructions).
  </action>
  <verify>
    <automated>test "$(grep -c 'solaris/pycopg' CLAUDE.md)" = "0" && ! grep -q 'pip install -e' CLAUDE.md && ! grep -q 'pip install pycopg\[dev\]' CLAUDE.md && grep -q 'uv sync --all-extras --dev' CLAUDE.md && grep -q 'v0.3.1' CLAUDE.md && ! grep -q 'v0.2.0' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'solaris/pycopg' CLAUDE.md` = 0 (stale path gone — TOOL-05)
    - `! grep -q 'pip install -e' CLAUDE.md` (no pip editable install)
    - `! grep -q 'pip install pycopg\[dev\]' CLAUDE.md` (removed extra not referenced — Pitfall 4)
    - `grep -q 'uv sync --all-extras --dev' CLAUDE.md` (uv setup command present)
    - `grep -q 'v0.3.1' CLAUDE.md` and `! grep -q 'v0.2.0' CLAUDE.md` (version bumped)
  </acceptance_criteria>
  <done>CLAUDE.md uses uv commands, correct repo path, version v0.3.1; no stale `solaris/pycopg`, `pip install -e`, `pip install pycopg[dev]`, or v0.2.0 mention.</done>
</task>

<task type="auto">
  <name>Task 3: Add README Development section (keep pip Installation)</name>
  <files>README.md</files>
  <read_first>
    - README.md (CURRENT — `## Installation` section lines 10-24 with `pip install pycopg*` to PRESERVE; `## Quick Start` begins line 26)
    - .planning/phases/09-migration-uv-outillage-projet/09-PATTERNS.md (### README.md — MODIFY — Installation to preserve + new Development section content + boundary rule)
    - .planning/phases/09-migration-uv-outillage-projet/09-RESEARCH.md (Pitfall 4 — no `[dev]` extra in docs)
  </read_first>
  <action>
    Edit `README.md` per D-11, using the PATTERNS.md "### README.md — MODIFY" Development-section content:
    1. KEEP the existing `## Installation` section (lines 10-24) exactly as-is — it shows `pip install pycopg` / `pycopg[dotenv]` / `pycopg[geo]` / `pycopg[all]` for END USERS. Do NOT change these to uv.
    2. ADD a new `## Development` section (insert after `## Installation`, before `## Quick Start`) for CONTRIBUTORS using uv: clone + `cd pycopg` + `uv sync --all-extras --dev`, then `uv run pytest` (tests), `uv run ruff check pycopg tests` (lint), `uv run black pycopg tests` (format), `uv build` (wheel + sdist). Link to https://docs.astral.sh/uv/.
    3. Maintain the hard boundary: Installation = pip (users), Development = uv (contributors). Do NOT introduce `pip install pycopg[dev]` anywhere.
  </action>
  <verify>
    <automated>grep -q '^## Development' README.md && grep -q 'uv sync --all-extras --dev' README.md && grep -q 'pip install pycopg' README.md && ! grep -q 'pip install pycopg\[dev\]' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '## Development' README.md` (contributor section added — TOOL-05)
    - `grep -q 'uv sync --all-extras --dev' README.md` (uv setup in Development)
    - `grep -q 'pip install pycopg' README.md` (user Installation preserved — D-11)
    - `! grep -q 'pip install pycopg\[dev\]' README.md` (removed extra not referenced)
    - Installation section still contains `pip install pycopg[dotenv]`, `[geo]`, `[all]` (unchanged)
  </acceptance_criteria>
  <done>README has a `## Development` section with uv contributor commands; the `## Installation` section still shows `pip install pycopg` for end users; no `[dev]` extra referenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → contributor shell | Commands a contributor copy-pastes into a shell cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-DOC | Tampering (stale-instruction) | contributor docs referencing the removed `dev` extra or stale paths | mitigate | Grep gates assert no `pip install pycopg[dev]`, no `pip install -e`, no `solaris/pycopg` remain after edits (Pitfall 4). Prevents a contributor from running a command that errors with `No such extra 'dev'`. |
| T-09-DOC-BND | Information Disclosure (none) / consistency | user vs contributor command boundary in README | accept | Boundary is documentation-only (users `pip install pycopg`, contributors `uv`); no secret or privileged surface. Asserted by grep gates (both `pip install pycopg` and `uv sync` present). |

No high-severity threats. Docs-only changes; no executable surface, no secrets.
</threat_model>

<verification>
- `grep -c 'solaris/pycopg' CLAUDE.md` = 0 (TOOL-05)
- `! grep -q 'pip install -e' CLAUDE.md` (TOOL-05)
- `grep -q '## Development' README.md` (TOOL-05)
- `make -n install test lint format build` exits 0 (TOOL-05)
- `grep -q 'pip install pycopg' README.md` (user doc keeps pip — TOOL-05)
- No `pip install pycopg[dev]` in CLAUDE.md or README.md (Pitfall 4)
</verification>

<success_criteria>
- TOOL-05: CLAUDE.md, README `## Development`, and Makefile all use uv contributor commands; README `## Installation` keeps `pip install pycopg`; no doc references the removed `[dev]` extra; CLAUDE.md path/version corrected.
</success_criteria>

<output>
Create `.planning/phases/09-migration-uv-outillage-projet/09-04-SUMMARY.md` when done.
</output>
