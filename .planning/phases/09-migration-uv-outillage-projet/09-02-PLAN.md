---
phase: 09-migration-uv-outillage-projet
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .github/workflows/tests.yml
autonomous: true
requirements: [TOOL-03]
must_haves:
  truths:
    - "A CI test workflow exists and runs the existing pytest suite under uv"
    - "The workflow tests against a real Postgres with PostGIS + TimescaleDB (no skips)"
    - "The workflow runs the Python 3.11 / 3.12 / 3.13 matrix"
    - "CI installs dependencies with uv sync --locked (fails fast on a stale lockfile)"
  artifacts:
    - path: ".github/workflows/tests.yml"
      provides: "CI test workflow: timescaledb-ha service + setup-uv@v8.2.0 + uv sync --locked + uv run pytest, matrix 3.11/3.12/3.13"
      contains: "astral-sh/setup-uv@v8.2.0"
  key_links:
    - from: ".github/workflows/tests.yml service container"
      to: "tests/conftest.py db_config fixture"
      via: "PGHOST/PGUSER/PGPASSWORD/PGPORT env + pycopg_test database"
      pattern: "PGHOST"
    - from: ".github/workflows/tests.yml dependency step"
      to: "uv.lock"
      via: "uv sync --locked"
      pattern: "uv sync --locked"
---

<objective>
Create `.github/workflows/tests.yml` — a brand-new CI test workflow (the repo has none today; only `publish.yml` exists). The workflow spins up a `timescale/timescaledb-ha:pg17` Postgres service, creates the `pycopg_test` database and its `postgis`/`timescaledb` extensions via a `psql` step, installs the project with `uv sync --locked --all-extras --dev`, and runs `uv run pytest` across the Python 3.11/3.12/3.13 matrix.

Purpose: Satisfies TOOL-03 (CI test workflow runs under uv and passes). It also becomes the safety net for phases 10-14. This is created, not migrated (D-04).
Output: New `.github/workflows/tests.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-migration-uv-outillage-projet/09-CONTEXT.md
@.planning/phases/09-migration-uv-outillage-projet/09-RESEARCH.md
@.planning/phases/09-migration-uv-outillage-projet/09-PATTERNS.md
@.planning/phases/09-migration-uv-outillage-projet/09-VALIDATION.md
@tests/conftest.py
</context>

<artifacts_this_phase_produces>
This plan (09-02) produces: the new file `.github/workflows/tests.yml`. It is a net-new CI artifact (no test workflow exists today — the only existing workflow is `publish.yml`). The source-grounding pass MUST treat `.github/workflows/tests.yml` as newly created, not drift. It depends on the `[dependency-groups].dev` table, `uv.lock`, and `.python-version` created by plan 09-01.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/tests.yml (uv + timescaledb-ha + matrix)</name>
  <files>.github/workflows/tests.yml</files>
  <read_first>
    - .github/workflows/publish.yml (CURRENT — structural analog: `name:`/`on:`/`jobs:`/`steps:` + `actions/checkout@v4` shape to mirror)
    - tests/conftest.py (SOURCE OF TRUTH for the CI service: db_config reads PGHOST/PGUSER/PGPASSWORD/PGPORT, database name hardcoded `pycopg_test`)
    - .planning/phases/09-migration-uv-outillage-projet/09-PATTERNS.md (### .github/workflows/tests.yml — CREATE — full YAML pattern to copy verbatim; ### Postgres service env vars shared pattern)
    - .planning/phases/09-migration-uv-outillage-projet/09-RESEARCH.md (## Architecture Patterns → Pattern 3 tests.yml; Pitfalls 1, 2, 3, 5, 6; Assumptions A1-A4)
  </read_first>
  <action>
    Create `.github/workflows/tests.yml` following the verbatim YAML pattern in PATTERNS.md "### .github/workflows/tests.yml — CREATE". Required concrete values:
    - Triggers: `on: push` (branches `[main]`) and `pull_request` (branches `[main]`).
    - Job `test` on `runs-on: ubuntu-latest` with `strategy.matrix.python-version: ["3.11", "3.12", "3.13"]` (D-06 — all three classifier versions).
    - `services.postgres.image: timescale/timescaledb-ha:pg17` (D-05; pin `pg17`, NOT pg15/pg16 — Pitfall 2 startup panic). Service env `POSTGRES_PASSWORD: postgres`, `POSTGRES_USER: postgres`. Health options: `--health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5`. `ports: - 5432:5432`.
    - Steps in order: `actions/checkout@v4`; then `astral-sh/setup-uv@v8.2.0` (NOT `@v6` — that tag does not exist, RESEARCH Pitfall 1) with `python-version: ${{ matrix.python-version }}` and `enable-cache: true`; then `uv sync --locked --all-extras --dev` (use `--locked`, NOT `--frozen` — Pitfall 5, fails fast on stale lock); then a DB-setup step running `psql` against `localhost` to `CREATE DATABASE pycopg_test`, `CREATE EXTENSION IF NOT EXISTS postgis`, `CREATE EXTENSION IF NOT EXISTS timescaledb` (with `PGPASSWORD: postgres` in that step's env — DO NOT mount `/docker-entrypoint-initdb.d/` scripts, Pitfall 3); then the test step running `uv run pytest` (D-07: no extra flags — `addopts` incl. `--cov-fail-under=70` is auto-picked from pyproject, Pitfall 6) with env `PGHOST: localhost`, `PGUSER: postgres`, `PGPASSWORD: postgres`, `PGPORT: "5432"` (the four vars conftest.py reads).
    Keep `workflow_dispatch` available implicitly is optional; the validation task below relies on a manual trigger, so ALSO add `workflow_dispatch:` to the `on:` block so the workflow can be triggered manually before relying on a push (D-14 risk-mitigation pattern for new CI).
    Do NOT bump `actions/checkout` past `@v4` (Node 20→24 bump deferred to Phase 15, D-15). Do NOT add any explicit coverage flags to the pytest step.
  </action>
  <verify>
    <automated>test -f .github/workflows/tests.yml && grep -q 'astral-sh/setup-uv@v8.2.0' .github/workflows/tests.yml && grep -q 'timescale/timescaledb-ha:pg17' .github/workflows/tests.yml && grep -q 'uv sync --locked' .github/workflows/tests.yml && grep -q '"3.11"' .github/workflows/tests.yml && grep -q '"3.13"' .github/workflows/tests.yml && grep -q 'pycopg_test' .github/workflows/tests.yml && ! grep -q 'setup-uv@v6' .github/workflows/tests.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/tests.yml` succeeds
    - `grep -q 'astral-sh/setup-uv@v8.2.0' .github/workflows/tests.yml` and `! grep -q 'setup-uv@v6'`
    - `grep -q 'timescale/timescaledb-ha:pg17' .github/workflows/tests.yml`
    - `grep -q 'uv sync --locked' .github/workflows/tests.yml` and `! grep -q 'uv sync --frozen'`
    - Matrix contains `"3.11"`, `"3.12"`, `"3.13"`
    - The four env vars `PGHOST`, `PGUSER`, `PGPASSWORD`, `PGPORT` are present in the test step
    - `psql` step creates `pycopg_test` + `postgis` + `timescaledb` extensions (not via init-script mount)
    - `uv run pytest` step has no `--cov` flag added (gate inherited from pyproject)
    - The workflow YAML parses (e.g. `python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/tests.yml'))"` exits 0)
  </acceptance_criteria>
  <done>tests.yml exists, pins setup-uv@v8.2.0 + timescaledb-ha:pg17, runs the 3.11/3.12/3.13 matrix, sets up pycopg_test via psql, syncs `--locked`, and runs `uv run pytest` with the four PG env vars.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the CI workflow runs green via manual dispatch</name>
  <files>.github/workflows/tests.yml</files>
  <read_first>
    - .github/workflows/tests.yml (the file created in Task 1)
    - .planning/phases/09-migration-uv-outillage-projet/09-VALIDATION.md (## Manual-Only Verifications — tests.yml matrix verification instructions; per-task map rows 9-tests-01/02)
    - .planning/phases/09-migration-uv-outillage-projet/09-RESEARCH.md (## Open Questions Q1 — timescaledb-ha pg17 startup reliability; fallback tags pg17-ts2.19.0-oss or timescale/timescaledb:latest-pg17)
  </read_first>
  <action>
    Validate the new workflow actually runs green before it is relied upon (mirrors the D-14 risk-mitigation pattern). After the workflow file is committed to a branch, trigger it (push to the branch or `gh workflow run tests.yml` via `workflow_dispatch`) and observe the matrix result. Confirm all three matrix jobs (3.11/3.12/3.13) complete green with NO skipped spatial/hypertable tests (the timescaledb-ha image must provide both PostGIS and TimescaleDB — Assumptions A1/A4). If the Postgres service never becomes healthy (timescaledb-tune panic — Pitfall 2 / Open Question Q1), apply the documented fallback: pin to `pg17-ts2.19.0-oss` or `timescale/timescaledb:latest-pg17`, re-run, and record which tag was used in the SUMMARY. This is a CI observation; capture the run URL/status in the SUMMARY.
  </action>
  <verify>
    <automated>gh run list --workflow=tests.yml --limit 1 --json conclusion --jq '.[0].conclusion' 2>/dev/null | grep -qx success || echo "CI observation — record run URL + conclusion in SUMMARY (workflow_dispatch)"</automated>
  </verify>
  <acceptance_criteria>
    - The `tests.yml` workflow has been triggered (push or `workflow_dispatch`)
    - All 3 matrix jobs (3.11/3.12/3.13) report `success` (CI observation — `gh run list --workflow=tests.yml` shows `success`, or run URL recorded in SUMMARY)
    - No spatial/hypertable tests skipped due to a missing extension (PostGIS + TimescaleDB both active)
    - If a fallback image tag was needed, it is recorded in the SUMMARY with the reason
  </acceptance_criteria>
  <done>tests.yml has been run via CI; the 3.11/3.12/3.13 matrix is green with PostGIS + TimescaleDB active and no extension-skipped tests; run status recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions marketplace → CI runner | Third-party actions (`setup-uv`, `checkout`) execute with runner privileges; tag references cross here |
| CI runner → Postgres service container | Test DB credentials (`postgres`/`postgres`) cross here over localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-CI | Tampering | `astral-sh/setup-uv` action reference in tests.yml | mitigate | Pin to the immutable full-version tag `astral-sh/setup-uv@v8.2.0` (supply-chain hardening; floating `@v6`/major tags removed at v8.0.0 and `@v6` never existed). `actions/checkout@v4` also pinned. |
| T-09-PGPW | Information Disclosure | hardcoded `postgres` test password in service + step env | accept | Ephemeral CI container, no PII, destroyed at job end; password lives only in step/service env, never a repo secret. Standard practice for service containers per RESEARCH § Security Domain. |

No high-severity threats. This phase adds no new repo secrets.
</threat_model>

<verification>
- `test -f .github/workflows/tests.yml` (TOOL-03)
- `grep -q 'astral-sh/setup-uv@v8.2.0' .github/workflows/tests.yml` and `! grep -q 'setup-uv@v6'` (T-09-CI mitigation)
- `grep -q 'uv sync --locked'` and `! grep -q 'uv sync --frozen'`
- Matrix `"3.11"`/`"3.12"`/`"3.13"` present
- All 3 matrix jobs green via CI run (TOOL-03; manual `workflow_dispatch` / push observation)
</verification>

<success_criteria>
- TOOL-03: `.github/workflows/tests.yml` exists, runs under uv (`uv sync --locked` + `uv run pytest`), and the 3.11/3.12/3.13 matrix passes against a real PostGIS+TimescaleDB Postgres with no extension-skipped tests.
- setup-uv pinned to `@v8.2.0`; lockfile enforced with `--locked`; init via `psql` step (no init-script mount); Node-version bump deferred (checkout stays `@v4`).
</success_criteria>

<output>
Create `.planning/phases/09-migration-uv-outillage-projet/09-02-SUMMARY.md` when done.
</output>
