---
phase: 14-spatial-helpers-phase-8-r-alis-e
plan: 04
type: execute
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - pyproject.toml
  - tests/test_spatial.py
autonomous: true
requirements: [SPA-06]
must_haves:
  truths:
    - "The full suite passes the coverage gate after spatial.py and its tests land"
    - "Total coverage is measured; the cliquet gate is flipped upward only if the achieved value supports it (never frozen at an unmet value)"
    - "interrogate ≥ 95 and Sphinx -W remain green with the new spatial public surface"
  artifacts:
    - path: "pyproject.toml"
      provides: "coverage gate value (--cov-fail-under) consistent with measured coverage"
      contains: "cov-fail-under"
  key_links:
    - from: "pyproject.toml --cov-fail-under"
      to: "measured total coverage"
      via: "gate flipped up only when achieved (cliquet, never lowered)"
      pattern: "cov-fail-under"
---

<objective>
Close the phase coverage contract. After the spatial module and its tests land (14-01 + 14-02), measure total coverage, fill any residual gaps with DB-free builder/accessor tests, and apply the cliquet rule on `--cov-fail-under`: flip the gate upward toward the milestone target of 95 ONLY if the measured value supports it; otherwise leave it at the current 92 and document why (RESEARCH Open Question 1, REF-05/D-07 precedent: "never freeze an unmet gate"). Confirm interrogate ≥ 95 and Sphinx `-W` remain green with the new public surface.

Purpose: The milestone target is "maintain ≥ 95" but the actual gate is currently 92 (measured 93% at research time). The pure builders are a strong coverage driver — this plan determines empirically whether Phase 14 can responsibly raise the cliquet, without forcing an unmet gate.
Output: a coverage-validated phase with the gate set to a value that is actually achieved, and green quality gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-spatial-helpers-phase-8-r-alis-e/14-RESEARCH.md
@.planning/phases/14-spatial-helpers-phase-8-r-alis-e/14-01-SUMMARY.md
@.planning/phases/14-spatial-helpers-phase-8-r-alis-e/14-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces no new code symbols. It may add residual coverage-fill test cases to existing `tests/test_spatial.py` classes (TestBuilders / TestGeometryResolver) and adjusts the `--cov-fail-under` value in `pyproject.toml`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Measure coverage, fill residual gaps, apply cliquet decision</name>
  <files>tests/test_spatial.py, pyproject.toml</files>
  <read_first>
    - pyproject.toml (the `[tool.pytest.ini_options]` / coverage config with `--cov-fail-under=92`)
    - .planning/phases/14-spatial-helpers-phase-8-r-alis-e/14-RESEARCH.md (Open Question 1: coverage gate decision; Pitfall 5: coverage regression from uncovered branches)
    - .planning/phases/12-refactoring-brancher-les-abstractions/12-CONTEXT.md (cliquet discipline "measure then flip"; REF-05 deferred-95 precedent)
    - tests/test_spatial.py (existing builder/resolver tests — extend for any missed branch)
  </read_first>
  <action>
Run the full suite with coverage and inspect the term-missing report:
  `uv run pytest -o addopts="" --cov=pycopg --cov-report=term-missing` (this bypasses the addopts gate so you see the true number; the configured gate runs via plain `uv run pytest`).

For any `pycopg/spatial.py` lines reported as missed that are DB-free reachable (builder branches, resolver error paths, into= validation, scalar into="gdf" ValueError raised before execution), add targeted DB-free test cases to the existing `TestBuilders` / `TestGeometryResolver` / `TestGuard` classes in `tests/test_spatial.py`. Do NOT add integration tests merely to chase coverage — prefer pure unit tests (Pitfall 5).

Then apply the cliquet decision on `--cov-fail-under` in `pyproject.toml`:
  - Read the measured TOTAL coverage percentage.
  - If TOTAL ≥ 95: set `--cov-fail-under=95` (flip up toward the milestone target).
  - Else if 92 ≤ TOTAL < 95: set `--cov-fail-under` to the floor of the measured TOTAL but never below the current 92 and never above the achieved value (e.g. measured 93.x → 93). NEVER set it above what `uv run pytest` actually passes.
  - Never lower the gate below 92 (cliquet is monotonic).
  Record the measured number and the chosen gate value (with rationale) in the SUMMARY, explicitly noting whether the 95 milestone target was reached or remains deferred (mirroring the REF-05 disposition).

Finally confirm the documentation gates still pass with the new public surface: `uv run interrogate pycopg` (≥ 95) and the Sphinx `-W` build (the spatial classes/methods carry numpydoc docstrings from 14-01/14-02, so this should be green — if Sphinx `-W` errors on the new symbols, fix the docstrings rather than weakening the gate).
  </action>
  <verify>
    <automated>uv run pytest -o addopts="" -q && uv run pytest -q && uv run interrogate pycopg</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` (with the configured `--cov-fail-under`) passes — the gate value is one the suite actually meets (modulo the 3 known pre-existing flaky DB tests, which are not regressions).
    - Source assertion: `grep -oE "cov-fail-under=[0-9]+" pyproject.toml` shows a value ≥ 92 and ≤ the measured TOTAL coverage; if measured TOTAL ≥ 95 the value is 95.
    - `uv run interrogate pycopg` reports ≥ 95% (new spatial surface fully documented).
    - The SUMMARY records: measured TOTAL coverage %, the chosen gate value, and whether 95 was reached or deferred.
    - No test was weakened or skipped to pass the gate; coverage gains came from DB-free unit tests.
  </acceptance_criteria>
  <done>Total coverage is measured; residual DB-free gaps are filled; the cliquet gate is set to an achieved value (flipped to 95 only if met, else held with rationale); interrogate and Sphinx -W are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Config/test change only; no new runtime input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Repudiation | coverage gate frozen above achieved value | mitigate | Gate flipped up only when `uv run pytest` passes at that value; cliquet rule forbids freezing an unmet gate (D-07 precedent) |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No installs; config + test change only |
</threat_model>

<verification>
- `uv run pytest` green at the configured gate.
- `uv run interrogate pycopg` ≥ 95; Sphinx `-W` build green.
- Gate value in pyproject.toml is ≤ measured coverage and ≥ 92.
</verification>

<success_criteria>
- Coverage maintained at the cliquet floor (≥ 92), gate flipped to 95 only if achieved; quality gates green (SPA-06; ROADMAP "coverage maintained" criterion).
</success_criteria>

<output>
Create `.planning/phases/14-spatial-helpers-phase-8-r-alis-e/14-04-SUMMARY.md` when done.
</output>
