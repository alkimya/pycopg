---
phase: 14-spatial-helpers-phase-8-r-alis-e
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-spatial-helpers/08-DESIGN.md
autonomous: true
requirements: [SPA-01]
must_haves:
  truths:
    - "08-DESIGN.md no longer lists 4 open points to resolve — each of the former 'Points à trancher' is replaced by its locked resolution citing the decision ID"
    - "08-DESIGN.md reflects the final API surface: into=, geometry input forms, unit=, where=/order_by=/limit=, geom default, srid default, ref= EXISTS semantics"
    - "08-DESIGN.md status line marks the design as realized in Phase 14"
  artifacts:
    - path: ".planning/phases/08-spatial-helpers/08-DESIGN.md"
      provides: "Updated validated design reflecting D-01..D-12"
      contains: "D-01"
  key_links:
    - from: ".planning/phases/08-spatial-helpers/08-DESIGN.md §5"
      to: "14-CONTEXT.md decisions D-01..D-12"
      via: "each open point replaced by its resolution + decision ID"
      pattern: "D-0[1-9]|D-1[0-2]"
---

<objective>
Resolve the four open Phase 8 design points by updating `.planning/phases/08-spatial-helpers/08-DESIGN.md` to reflect the locked decisions D-01..D-12. This satisfies SPA-01 and ROADMAP success criterion #1. This plan is documentation-only and independent of the code plans — it runs in Wave 1 in parallel with 14-01.

Purpose: 08-DESIGN.md is the canonical design source of truth that downstream readers (and Phase 15 release docs) consult. It currently still lists "4 points à trancher" and "décisions encore ouvertes" — these must become resolved decisions so the design matches what Phase 14 ships.
Output: an updated 08-DESIGN.md whose §5 and §9 reflect every locked decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-spatial-helpers-phase-8-r-alis-e/14-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan produces no code symbols — it updates an existing planning document only. (Code symbols for Phase 14 are listed in 14-01 / 14-02 plans.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update 08-DESIGN.md with resolved decisions D-01..D-12</name>
  <files>.planning/phases/08-spatial-helpers/08-DESIGN.md</files>
  <read_first>
    - .planning/phases/08-spatial-helpers/08-DESIGN.md (the full document — current §3 API table, §5 "Points à TRANCHER", §9 "Décisions encore ouvertes")
    - .planning/phases/14-spatial-helpers-phase-8-r-alis-e/14-CONTEXT.md (the <decisions> block — D-01..D-12 verbatim resolutions)
  </read_first>
  <action>
Edit `.planning/phases/08-spatial-helpers/08-DESIGN.md` to convert the open questions into locked resolutions. Concretely:

§5 "Points à TRANCHER" → rename to "Points tranchés (D-01..D-12)" and replace each of the 4 proposals with its resolution, citing the decision ID:
  1. Type de retour (`into=`): `into="rows"` default (list[dict]) + `into="gdf"` delegating to `to_geodataframe` (D-01); `into="gdf"` on scalar helpers (area, perimeter, distance, centroid) raises ValueError (D-02); `columns: list[str] | None` validated (D-03); no public `db.spatial.sql(...)` (D-04); `into="query"` deferred to ETL milestone.
  2. Géométrie en entrée: 4 forms `point=/wkt=/geojson=/ref=`, mutually exclusive, one internal resolver, all `%s` (D-05); geom column param `geom` default `"geometry"` (D-06); `srid=4326` default (D-07); `ref=` = EXISTS semantics, `within` keeps its dedicated two-table join (D-08).
  3. Unités (`unit=`): `unit="m"` default (`::geography`) + `unit="srid"` (D-09), exposed only on dwithin/distance/area/perimeter/buffer (D-10).
  4. Filtres additionnels: `where: str | None` raw fragment per `_build_select_sql` convention (D-11) + full `where=`/`order_by=`/`limit=` trio on filtering helpers (D-12).

§3 "Surface d'API": update the signature row(s) where defaults changed — `geom="geometry"` default, `srid=4326` default, and the `into=`/`unit=`/`where=`/`order_by=`/`limit=` params per the decisions above (keep the ~11-helper table intact; this is a surface refresh, not a rewrite).

§9 "Décisions encore ouvertes": remove the "4 points de la section 5" bullet (now resolved); resolve the parameter-name bullet (`geom` default `"geometry"` per D-06) and the `db.spatial.sql(...)` bullet (dropped per D-04). If nothing remains open, replace §9 content with a short "Toutes les décisions tranchées en Phase 14 (D-01..D-12, voir 14-CONTEXT.md)."

Status line at top: update to indicate the design is realized in Phase 14 (e.g. "Status: Design validé et réalisé en Phase 14 (D-01..D-12)").

Do NOT change the structuring decisions in §2 (helpers nommés, namespace accesseur, PostGIS d'abord — locked, not under discussion). Keep the document's existing structure and French prose style. Reference 14-CONTEXT.md as the decision source.
  </action>
  <verify>
    <automated>grep -Eq "D-01" .planning/phases/08-spatial-helpers/08-DESIGN.md && ! grep -qi "Points à TRANCHER" .planning/phases/08-spatial-helpers/08-DESIGN.md && echo "OK design updated"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "Points à TRANCHER" .planning/phases/08-spatial-helpers/08-DESIGN.md` returns 0 (the open-points heading is gone).
    - Source assertion: every decision ID D-01 through D-12 is cited — `grep -oE "D-0[1-9]|D-1[0-2]" .planning/phases/08-spatial-helpers/08-DESIGN.md | sort -u | wc -l` returns 12.
    - Source assertion: the document mentions `geom` default `"geometry"`, `srid=4326`, `into="rows"`, `into="gdf"`, `unit="m"`, and `where=` resolutions.
    - Source assertion: §9 no longer references "4 points de la section 5" as open — `grep -i "encore ouvertes" .planning/phases/08-spatial-helpers/08-DESIGN.md` either absent or its body states all decisions are resolved.
    - The status line indicates Phase 14 realization.
  </acceptance_criteria>
  <done>08-DESIGN.md reflects every locked decision D-01..D-12, has no remaining open design points, and marks the design as realized in Phase 14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change to a planning artifact; no code, no untrusted input, no execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Tampering | 08-DESIGN.md content drift vs locked decisions | mitigate | Each updated point cites its decision ID (D-01..D-12) traceable to 14-CONTEXT.md; verify gate counts all 12 IDs |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No installs; documentation-only plan |
</threat_model>

<verification>
- `grep` gate confirms D-01..D-12 cited and "Points à TRANCHER" removed.
- Document still readable as the design source of truth (structure preserved).
</verification>

<success_criteria>
- The 4 open Phase 8 design points are resolved in 08-DESIGN.md with decision-ID traceability (SPA-01, ROADMAP criterion #1).
</success_criteria>

<output>
Create `.planning/phases/14-spatial-helpers-phase-8-r-alis-e/14-03-SUMMARY.md` when done.
</output>
