---
phase: 29-release-v0-7-0
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified: []
autonomous: true
requirements: [REL-07]
must_haves:
  truths:
    - "Full test suite passes with coverage at or above 94 percent"
    - "interrogate docstring coverage is at or above 95 percent"
    - "Sphinx builds clean under -W against the finalized 0.7.0 docs"
    - "Importing pycopg under -W error::DeprecationWarning succeeds (no stubs left to fire)"
  artifacts:
    - path: ".planning/phases/29-release-v0-7-0/29-02-GATES.md"
      provides: "Recorded pass/fail output of every quality gate"
  key_links:
    - from: "docs/conf.py release 0.7.0"
      to: "Sphinx -W build"
      via: "renders the finalized version + CHANGELOG/MIGRATION"
      pattern: "sphinx-build"
---

<objective>
Run every v0.7.0 quality gate to GREEN against the finalized release artifacts from
Plan 01, and record the outputs. Gates: pytest coverage at or above 94 percent,
interrogate at or above 95 percent, Sphinx `-W` clean, and `-W error::DeprecationWarning`
import green (proves no deprecation stubs remain after Phase 25's hard removal).

Purpose: REL-07 / Success Criterion 2 — all gates must pass before tag + publish.
A gate failure is a BLOCKING finding to surface, NOT something to silently patch by
changing feature code (scope fence: no source edits unless a gate fails, and even then
surface it rather than reduce scope).
Output: A GATES.md record of each gate's command and result; all gates green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
- .planning/phases/29-release-v0-7-0/29-02-GATES.md: recorded gate outputs (planning artifact; exclude from drift-check)
No source or release artifact is modified by this plan unless a gate fails and the
executor must surface a fix; any such fix is reported as a blocking finding.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run coverage and docstring gates green</name>
  <files>.planning/phases/29-release-v0-7-0/29-02-GATES.md</files>
  <read_first>
    - pyproject.toml `[tool.pytest.ini_options]` addopts (has `--cov-fail-under=94`) and `[tool.interrogate]` (`fail-under = 95`)
    - .github/workflows/tests.yml (canonical CI invocations: `uv run pytest`; `uv run interrogate pycopg --fail-under 95 --quiet`)
    - .planning/STATE.md Blockers/Concerns (2 named pre-existing flaky DB tests: test_async_transaction_fix, test_create_spatial_index_name_parameter)
    - CLAUDE.md (uv-based commands)
  </read_first>
  <action>
    Run the canonical coverage gate `uv run pytest` (coverage threshold is wired via
    `--cov-fail-under=94` in addopts — do not invent flags). Run the docstring gate
    `uv run interrogate pycopg --fail-under 95 --quiet` (matching CI). Capture the coverage
    percentage line and the interrogate result into 29-02-GATES.md. If only the 2 named
    pre-existing flaky DB tests fail (fixture-isolation, not v0.7.0 code), re-run them
    targeted with `-o addopts=""` to confirm they pass in isolation and note them as
    known-flaky; any OTHER failure is a BLOCKING finding — record it and stop, do not edit
    feature code to force a pass.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run interrogate pycopg --fail-under 95 --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` exits 0 with a coverage line at or above 94 percent
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0
    - 29-02-GATES.md records both commands, their exit status, and the measured coverage percentage
    - Any failure beyond the 2 named pre-existing flaky tests is recorded as a blocking finding
  </acceptance_criteria>
  <done>Coverage gate and interrogate gate both green; results recorded in GATES.md.</done>
</task>

<task type="auto">
  <name>Task 2: Run Sphinx -W and deprecation-warning gates green</name>
  <files>.planning/phases/29-release-v0-7-0/29-02-GATES.md</files>
  <read_first>
    - .github/workflows/tests.yml ("Build Sphinx docs" step: `uv pip install -r docs/requirements.txt` then `uv run sphinx-build -W --keep-going -b html docs docs/_build/html`)
    - docs/requirements.txt (sphinx, myst-parser, furo, sphinx-copybutton, sphinx-intl)
    - docs/conf.py (now release 0.7.0 after Plan 01; `show_warning_types = True`)
  </read_first>
  <action>
    Ensure doc build deps are present (`uv pip install -r docs/requirements.txt`), then run
    the canonical Sphinx gate `uv run sphinx-build -W --keep-going -b html docs docs/_build/html`
    — `-W` turns warnings into errors so any broken cross-reference or rST/MyST issue fails the
    build. Then run the deprecation-warning gate: `uv run python -W error::DeprecationWarning -c "import pycopg"`
    which must exit 0, proving no `@deprecated_alias` stubs remain to fire a DeprecationWarning
    after Phase 25's hard removal. Record both commands and their exit status in 29-02-GATES.md.
    If Sphinx fails on a warning, record the exact warning as a blocking finding (it likely points
    at a docs source issue introduced by the CHANGELOG/MIGRATION edits or a stale reference) — surface
    it; do not suppress warnings to force a green build.
  </action>
  <verify>
    <automated>uv run sphinx-build -W --keep-going -b html docs docs/_build/html && uv run python -W error::DeprecationWarning -c "import pycopg"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0
    - `uv run python -W error::DeprecationWarning -c "import pycopg"` exits 0
    - 29-02-GATES.md records both commands and their exit status
    - Any Sphinx warning is recorded verbatim as a blocking finding (no warning suppression)
  </acceptance_criteria>
  <done>Sphinx -W build and the deprecation-warning import gate both green; results recorded in GATES.md.</done>
</task>

</tasks>

<verification>
- All four gates green: pytest coverage at or above 94, interrogate at or above 95, Sphinx -W clean, deprecation-import clean.
- 29-02-GATES.md records each command, exit status, and the coverage percentage.
- No feature/source code modified to force a pass; any failure surfaced as a blocking finding.
</verification>

<success_criteria>
- Success Criterion 2 satisfied: coverage at or above 94, interrogate at or above 95, Sphinx -W clean, `-W error::DeprecationWarning` green.
</success_criteria>

<output>
Create `.planning/phases/29-release-v0-7-0/29-02-SUMMARY.md` when done.
</output>
