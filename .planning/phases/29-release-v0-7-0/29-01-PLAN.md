---
phase: 29-release-v0-7-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - docs/conf.py
  - CHANGELOG.md
  - MIGRATION.md
autonomous: true
requirements: [REL-07]
must_haves:
  truths:
    - "Version reads 0.7.0 in pyproject.toml AND docs/conf.py"
    - "CHANGELOG [0.7.0] has both a Breaking entry and an Added entry, with a real ISO release date (not TBD)"
    - "CHANGELOG Added entry describes only the shipped incremental-ETL surface (no deferred/out-of-scope claims)"
    - "MIGRATION v0.6 to v0.7 guide includes a brief incremental-usage upgrade note pointing to docs/etl.md"
  artifacts:
    - path: "pyproject.toml"
      provides: "Release version string 0.7.0"
      contains: 'version = "0.7.0"'
    - path: "docs/conf.py"
      provides: "Sphinx release version 0.7.0"
      contains: "release = '0.7.0'"
    - path: "CHANGELOG.md"
      provides: "Finalized [0.7.0] Breaking + Added section with real date"
      contains: "### Added"
    - path: "MIGRATION.md"
      provides: "v0.6 to v0.7 guide with incremental-usage upgrade note"
  key_links:
    - from: "CHANGELOG.md [0.7.0] Added"
      to: "docs/etl.md Incremental loading section"
      via: "pointer reference"
      pattern: "etl"
---

<objective>
Finalize release artifacts for v0.7.0: bump version in the two source-of-truth files,
complete the CHANGELOG `[0.7.0]` entry (add the missing `### Added` block for incremental
ETL, replace `- TBD` with today's ISO date), and add a brief incremental-usage upgrade
note to the existing MIGRATION v0.6 to v0.7 guide.

Purpose: REL-07 requires version bumped in both sources, CHANGELOG `[0.7.0]`
(Breaking/Added) finalized, and MIGRATION v0.6 to v0.7 complete before gates run.
Output: Updated pyproject.toml, docs/conf.py, CHANGELOG.md, MIGRATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
Convergence drift-check MUST exclude these intentional release-artifact changes:

- pyproject.toml: `version = "0.6.0"` becomes `version = "0.7.0"`
- docs/conf.py: `release = '0.6.0'` becomes `release = '0.7.0'`
- CHANGELOG.md: new `### Added` block under `[0.7.0]`; `## [0.7.0] - TBD` becomes `## [0.7.0] - 2026-06-22`; footer compare-link update if present
- MIGRATION.md: brief incremental-usage upgrade note in the v0.6 to v0.7 section
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.7.0 in both source-of-truth files</name>
  <files>pyproject.toml, docs/conf.py</files>
  <read_first>
    - pyproject.toml (line ~7: `version = "0.6.0"`)
    - docs/conf.py (line ~17: `release = '0.6.0'`)
    - .planning/ROADMAP.md "### Phase 29" Success Criterion 1
  </read_first>
  <action>
    Change the `[project]` `version = "0.6.0"` line in pyproject.toml to `version = "0.7.0"`
    (exactly one version key — no single-source refactor; version lives in exactly 2 files
    per the scope fence). Change `release = '0.6.0'` to `release = '0.7.0'` in docs/conf.py
    (single quotes, existing style). Do NOT touch dependency pins, classifiers, or Python
    version targets.
  </action>
  <verify>
    <automated>grep -q '^version = "0.7.0"' pyproject.toml && grep -q "release = '0.7.0'" docs/conf.py && ! grep -q '"0.6.0"' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^version = "0.7.0"' pyproject.toml` exits 0
    - `grep "release = '0.7.0'" docs/conf.py` exits 0
    - `grep '"0.6.0"' pyproject.toml` exits non-zero
  </acceptance_criteria>
  <done>Both files declare version 0.7.0; no stray 0.6.0 version string remains.</done>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG [0.7.0] Added section and finalize release date</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md top ~60 lines (`## [0.7.0] - TBD` already has a `### Breaking` block)
    - CHANGELOG.md footer reference-link block (`[Unreleased]: .../compare/v0.6.0...HEAD`)
    - .planning/REQUIREMENTS.md ETL-INC-01..12 (shipped surface) plus "Out of Scope" and "Future Requirements" tables (what NOT to claim)
    - .planning/ROADMAP.md "### Phase 29" Success Criterion 3
  </read_first>
  <action>
    Insert a `### Added` section immediately AFTER the existing `### Breaking` block under
    `## [0.7.0]` (Breaking first, Added second, matching the Keep a Changelog order used
    elsewhere). Describe ONLY the shipped incremental-ETL surface: declarative watermark-based
    incremental loading via the single new `Pipeline.incremental_column` field; requires
    `load_mode="upsert"` (append/replace forbidden at construction with `ValueError`);
    high-water mark equals `max(incremental_column)` from the raw extracted batch before
    transforms; `>` exclusive boundary; watermark persisted as a typed JSONB envelope in the
    already-reserved `pipeline_runs.watermark` column (zero new runtime deps); first run is a
    full load then records the watermark; failed loads do not advance it and empty batches
    preserve it; `RunResult.watermark_used` / `watermark_recorded` fields surfaced (also via
    `history()` / `last_run()`); `dry_run=True` previews the filter and new max without writing
    a run row; full sync/async parity. Point to docs/etl.md rather than duplicating the guide.
    Do NOT mention deferred/out-of-scope items: no initial_watermark, no configurable greater-
    or-equal boundary, no multi-column watermarks, no CDC/WAL, no scheduler/DAG, no cross-DB
    sources. Then change `## [0.7.0] - TBD` to `## [0.7.0] - 2026-06-22`. If the footer defines
    `[Unreleased]: .../compare/v0.6.0...HEAD`, update it to compare against `v0.7.0` and add a
    `[0.7.0]: .../compare/v0.6.0...v0.7.0` link in the existing style; leave unchanged if absent.
  </action>
  <verify>
    <automated>grep -q '^## \[0.7.0\] - 2026-06-22' CHANGELOG.md && ! grep -q '0.7.0\] - TBD' CHANGELOG.md && sed -n '/^## \[0.7.0\]/,/^## \[0.6.0\]/p' CHANGELOG.md | grep -q '### Added' && sed -n '/^## \[0.7.0\]/,/^## \[0.6.0\]/p' CHANGELOG.md | grep -q '### Breaking' && ! sed -n '/^## \[0.7.0\]/,/^## \[0.6.0\]/p' CHANGELOG.md | grep -Eqi 'initial_watermark|CDC|scheduler|multi-column'</automated>
  </verify>
  <acceptance_criteria>
    - The `[0.7.0]` block (between `## [0.7.0]` and `## [0.6.0]`) contains both `### Breaking` and `### Added`
    - Header reads `## [0.7.0] - 2026-06-22`; no `[0.7.0] - TBD` remains anywhere
    - The `[0.7.0]` block contains none of: initial_watermark, CDC, WAL, scheduler, DAG, multi-column, greater-or-equal boundary marker
    - The Added entry references docs/etl.md and does not paste the full incremental guide
  </acceptance_criteria>
  <done>CHANGELOG `[0.7.0]` has Breaking + Added, a real date, only shipped-surface claims, and a docs/etl.md pointer.</done>
</task>

<task type="auto">
  <name>Task 3: Add incremental-usage upgrade note to MIGRATION v0.6 to v0.7</name>
  <files>MIGRATION.md</files>
  <read_first>
    - MIGRATION.md top ~108 lines (the "Migration Guide: v0.6.0 to v0.7.0" section ending with "Getting Help" before the v0.5 to v0.6 guide)
    - docs/etl.md `## Incremental loading` section (already complete from Phase 28 — do NOT duplicate it)
    - .planning/ROADMAP.md "### Phase 29" Success Criterion 3 ("incremental usage notes")
  </read_first>
  <action>
    First READ the existing v0.6 to v0.7 section. It currently covers only the breaking
    alias removal. Add a short subsection (a few sentences, e.g. "## New in v0.7.0:
    Incremental ETL") near the end of the v0.6 to v0.7 guide, BEFORE the "Getting Help"
    block. It is a brief upgrade-relevant pointer only: state that v0.7.0 adds optional
    watermark-based incremental loading via `Pipeline(incremental_column=..., load_mode="upsert")`,
    that it is additive and non-breaking (the `pipeline_runs.watermark` column was already
    reserved), and link to the docs/etl.md `Incremental loading` section for the full guide.
    Do NOT duplicate the worked example, watermark-column requirements, or backfill/reset
    content from docs/etl.md — that is the canonical guide and lives there per ETL-INC-12.
  </action>
  <verify>
    <automated>awk '/^# Migration Guide: v0.6/{f=1} /^# Migration Guide: v0.5/{f=0} f' MIGRATION.md | grep -qi 'incremental' && grep -qi 'etl' MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The v0.6 to v0.7 section of MIGRATION.md mentions incremental loading and links to docs/etl.md
    - The 56-name flat to accessor table is still present and unchanged
    - The note is brief (a short subsection), not a duplicate of the docs/etl.md guide
  </acceptance_criteria>
  <done>MIGRATION v0.6 to v0.7 has a brief incremental-usage note pointing to docs/etl.md; the 56-name table is intact.</done>
</task>

</tasks>

<verification>
- Version 0.7.0 in pyproject.toml and docs/conf.py; no 0.6.0 version string remains.
- CHANGELOG `[0.7.0]` has Breaking + Added, dated 2026-06-22, shipped-surface-only, points to docs/etl.md.
- MIGRATION v0.6 to v0.7 has an incremental-usage pointer; 56-name table intact.
</verification>

<success_criteria>
- pyproject.toml and docs/conf.py both declare 0.7.0 (Success Criterion 1, version-bump half).
- CHANGELOG `[0.7.0]` Breaking + Added finalized with real date (Success Criterion 3, CHANGELOG half).
- MIGRATION v0.6 to v0.7 complete with the 56-name table AND incremental usage notes (Success Criterion 3, MIGRATION half).
</success_criteria>

<output>
Create `.planning/phases/29-release-v0-7-0/29-01-SUMMARY.md` when done.
</output>
