---
phase: 40-release-v0-10-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - docs/conf.py
  - CHANGELOG.md
autonomous: true
requirements: [REL-10]

must_haves:
  truths:
    - "After install, pycopg.__version__ resolves to 0.10.0 (all three version sources agree)"
    - "CHANGELOG documents a hardening + performance release with no new public API"
    - "All 4 release gates are green before any tag or publish happens"
  artifacts:
    - path: "pyproject.toml"
      provides: "Canonical version source bumped to 0.10.0"
      contains: 'version = "0.10.0"'
    - path: "uv.lock"
      provides: "Lockfile pycopg entry bumped to 0.10.0 and uv lock --check clean"
      contains: 'version = "0.10.0"'
    - path: "docs/conf.py"
      provides: "Sphinx release string bumped to 0.10.0"
      contains: "release = '0.10.0'"
    - path: "CHANGELOG.md"
      provides: "[0.10.0] entry with ### Changed and ### Fixed (Keep a Changelog), no ### Added"
      contains: "## [0.10.0]"
  key_links:
    - from: "pyproject.toml version"
      to: "uv.lock pycopg entry version"
      via: "uv lock regeneration"
      pattern: 'version = "0.10.0"'
    - from: "pyproject.toml version"
      to: "installed package metadata __version__"
      via: "importlib.metadata.version('pycopg')"
      pattern: "0.10.0"
---

<objective>
Prepare the v0.10.0 release: bump the version in all THREE canonical sources
(`pyproject.toml`, `uv.lock`, `docs/conf.py`), author the `[0.10.0]` CHANGELOG
entry in strict Keep a Changelog form (`### Changed` + `### Fixed` only, per D-01),
and verify the 4 release gates are green. This is the prep-and-verify half of the
release; the terminal human-gated publish lives in Plan 40-02.

Purpose: Gates-green-before-ship is a hard precondition. This plan produces a
committed, version-bumped, changelog-complete tree with all gates verified, so
Plan 40-02 only has to tag and publish.

Output: bumped version strings in three files, a `[0.10.0]` CHANGELOG section,
and a recorded pass of all 4 gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-release-v0-10-0/40-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
This is a RELEASE phase. The following are deliverables of THIS phase, not
pre-existing artifacts — exclude them from any source-grounding / drift check:

- The bumped version strings in `pyproject.toml`, `uv.lock`, `docs/conf.py` (all `0.10.0`).
- The new `[0.10.0]` section in `CHANGELOG.md`.
- (Produced in Plan 40-02) the annotated git tag `v0.10.0` and the published PyPI
  artifacts (wheel + sdist for 0.10.0).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump version in all three canonical sources</name>
  <files>pyproject.toml, docs/conf.py, uv.lock</files>
  <read_first>
    - pyproject.toml (line 7 currently reads version = "0.9.0")
    - docs/conf.py (line 17 currently reads release = '0.9.0')
    - uv.lock (pycopg entry near line 698: name = "pycopg" then version = "0.9.0", source = { editable = "." })
    - .planning/phases/40-release-v0-10-0/40-CONTEXT.md § domain + § canonical_refs (the THREE version sources; uv.lock is the new third source per REL-10)
  </read_first>
  <action>
    Edit `pyproject.toml` line 7 from version = "0.9.0" to version = "0.10.0".
    Edit `docs/conf.py` line 17 from release = '0.9.0' to release = '0.10.0'
    (preserve the single-quote style already in the file).
    Do NOT hand-edit `uv.lock`; instead regenerate it by running `uv lock` so its
    pycopg editable entry picks up the new pyproject version. Then run
    `uv lock --check` to confirm the lockfile is current (the publish workflow runs
    `uv lock --check` and will fail if it drifts). Confirm `uv lock` produced ONLY
    the pycopg-version churn (no unrelated dependency upgrades); if it pulled
    unrelated changes, regenerate cleanly. Do NOT touch README.md — its single
    "v0.9.0" mention (line ~90) is a feature-introduced label ("flat CRUD helpers
    (v0.9.0)"), not a current-version string, and D-03 forbids docs content edits.
    `__version__` stays dynamic via importlib.metadata — no source edit there.
  </action>
  <verify>
    <automated>grep -q 'version = "0.10.0"' pyproject.toml && grep -q "release = '0.10.0'" docs/conf.py && grep -A1 'name = "pycopg"' uv.lock | grep -q 'version = "0.10.0"' && uv lock --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'version = "0.10.0"' pyproject.toml` matches line 7
    - `grep -n "release = '0.10.0'" docs/conf.py` matches line ~17
    - the pycopg entry in `uv.lock` reads `version = "0.10.0"`
    - `uv lock --check` exits 0 (lockfile current, no drift)
    - `grep -c '0\.9\.0' pyproject.toml docs/conf.py` shows 0 stale 0.9.0 version strings in those two files
    - README.md is unchanged (no version-string edit)
  </acceptance_criteria>
  <done>All three canonical sources read 0.10.0; uv lock --check is clean; no stale 0.9.0 version string remains in pyproject.toml or docs/conf.py.</done>
</task>

<task type="auto">
  <name>Task 2: Author the [0.10.0] CHANGELOG entry (Changed + Fixed only)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the empty `[Unreleased]` section at line 8, the `[0.9.0] - 2026-06-25` and `[0.8.0]` dated entries for style — the new entry goes BETWEEN [Unreleased] and [0.9.0])
    - .planning/phases/40-release-v0-10-0/40-CONTEXT.md § decisions (D-01 strict Keep a Changelog, D-02 qualitative perf, D-04 stay silent on v1.0.0)
    - .planning/phases/37-dette-audit/37-DECISIONS.md (the 5 BLOCKERs CR-01..CR-05, ruff N818/W291/F841/E722, TableNotFound raise site in truncate_table, advisory-warning closures) — source for ### Fixed
    - .planning/phases/38-performance-copy/38-01-SUMMARY.md (from_dataframe Hybrid DDL+COPY, sync+async) — source for ### Changed
    - .planning/phases/38-performance-copy/38-02-SUMMARY.md (ETL append/replace COPY load seam) — source for ### Changed
    - .planning/phases/38-performance-copy/38-03-SUMMARY.md (insert_batch placeholder hoist) — source for ### Changed
    - .planning/phases/39-couverture-benchmarks/39-01-SUMMARY.md (coverage ratchet 94→95) — source for ### Changed
    - benchmarks/README.md (the doc the CHANGELOG points to for reproducible COPY measurement — D-02; do NOT copy its illustrative sample numbers)
  </read_first>
  <action>
    Insert a new `## [0.10.0] - 2026-06-26` section between the existing
    `## [Unreleased]` line and `## [0.9.0] - 2026-06-25`, matching the heading and
    spacing style of the `[0.9.0]`/`[0.8.0]` entries. Use STRICT Keep a Changelog
    subsections — ONLY `### Changed` and `### Fixed` (per D-01). Do NOT add an
    `### Added` section (this is the first release with no new public API — the
    absence is intentional) and do NOT add a non-standard `Performance` section.
    Per D-04, make NO forward-looking statement about v1.0.0, no deprecations.

    In `### Changed`, fold in the COPY performance work and the coverage ratchet:
    `from_dataframe` (sync + async) now streams rows via the psycopg COPY protocol
    using a Hybrid DDL+COPY approach (`head(0).to_sql` for schema, COPY for rows),
    preserving the `if_exists`/`index`/`primary_key`/`dtype` contract; the ETL
    `append`/`replace` load path streams inline via COPY on the transaction cursor
    instead of materializing `astype(object)` + `to_dict(orient="records")` (upsert
    path unchanged); `insert_batch` hoists the invariant row placeholder out of the
    per-row loop (byte-exact behavior); the coverage ratchet was raised from 94% to
    95%. Per D-02, describe the COPY gains QUALITATIVELY (e.g. "higher-throughput
    bulk insertion for large DataFrames") with NO hardcoded speedup numbers, and
    add a pointer to `benchmarks/` (and `benchmarks/README.md`) for reproducible,
    on-demand measurement. The `validate_identifiers` regression caught and fixed
    in-milestone (commit 863e894) is internal churn and per D-01 does NOT get its
    own line.

    In `### Fixed`, list the Phase 37 debt closures: the 5 audit BLOCKERs from the
    AUDIT-01 code review (explain() format whitelist, identifier validation before
    from_dataframe/from_geodataframe, pg_dump/pg_restore CLI flag-injection guard,
    pg_restore missing-file routing) fixed; the three previously-flaky tests
    (`test_async_transaction_fix`, `test_create_spatial_index_name_parameter`, and
    the bound-param watermark test) now pass deterministically under
    pytest-randomly via fixture-isolation fixes; residual ruff errors
    (N818/W291/F841/E722) resolved so `ruff check pycopg tests` is clean;
    `TableNotFound` now has a real raise site in `truncate_table` (sync + async),
    resolving the exported-but-never-raised inconsistency; advisory warnings from
    v0.8/v0.9 closed or justified. Keep bullets user-facing and concise; group
    multi-item bullets sensibly rather than one line per internal fix.
  </action>
  <verify>
    <automated>grep -q '## \[0.10.0\] - 2026-06-26' CHANGELOG.md && grep -A40 '## \[0.10.0\]' CHANGELOG.md | grep -q '### Changed' && grep -A40 '## \[0.10.0\]' CHANGELOG.md | grep -q '### Fixed' && ! (sed -n '/## \[0.10.0\]/,/## \[0.9.0\]/p' CHANGELOG.md | grep -q '### Added')</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '## \[0.10.0\] - 2026-06-26' CHANGELOG.md` matches, positioned between `[Unreleased]` and `[0.9.0]`
    - the `[0.10.0]` section contains a `### Changed` subsection and a `### Fixed` subsection
    - the `[0.10.0]` section contains NO `### Added` and NO `Performance` subsection (verified by scanning the slice from `[0.10.0]` to `[0.9.0]`)
    - the `### Changed` text mentions COPY routing for from_dataframe, ETL load, and insert_batch, and the 94→95 coverage ratchet
    - the `### Changed` text contains a pointer to `benchmarks/` and NO hardcoded speedup number (no "Nx" / "% faster" figure)
    - the `### Fixed` text references the audit BLOCKERs, flaky-test de-flake, ruff cleanup, and TableNotFound raise site
    - no mention of v1.0.0, "freeze", or "deprecation" anywhere in the `[0.10.0]` section
  </acceptance_criteria>
  <done>CHANGELOG.md has a Keep-a-Changelog-compliant `[0.10.0]` entry with Changed + Fixed only, qualitative COPY perf with a benchmarks pointer, debt fixes listed, and no v1.0.0 signaling.</done>
</task>

<task type="auto">
  <name>Task 3: Verify all 4 release gates are green</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - pyproject.toml [tool.pytest.ini_options] (addopts already carries --cov-fail-under=95) and [tool.interrogate] (fail-under = 95)
    - .github/workflows/tests.yml (canonical gate invocations: `uv run pytest`, `uv run interrogate pycopg --fail-under 95 --quiet`, `uv run sphinx-build -W --keep-going -b html docs docs/_build/html`)
    - .planning/phases/40-release-v0-10-0/40-CONTEXT.md § domain (the 4 gates: coverage ≥95, interrogate 100%, Sphinx -W, -W error::DeprecationWarning)
    - $HOME/.claude/projects/-home-loc-workspace-pycopg/memory/MEMORY.md note: run DB-touching tests with PGDATABASE=pycopg_test2 (default pycopg_test DB is broken); 3 pre-existing PostGIS-env failures in test_postgis_errors.py are env limitations, NOT regressions
  </read_first>
  <action>
    Run the 4 canonical release gates and confirm each is green BEFORE Plan 40-02
    tags or publishes. Use PGDATABASE=pycopg_test2 for any DB-touching run (the
    default pycopg_test DB is broken — see MEMORY).

    Gate 1 (coverage ≥95): `PGDATABASE=pycopg_test2 uv run pytest` — must exit 0
    with the built-in `--cov-fail-under=95` satisfied (Phase 39 measured 95.74%).
    The 3 pre-existing PostGIS-env failures in tests/test_postgis_errors.py are a
    known environment limitation (PostGIS absent in pycopg_test2) documented in
    MEMORY — if they are the ONLY failures, treat the gate as green and record that
    in the SUMMARY; if PostGIS is available, the run should be fully clean.

    Gate 2 (interrogate 100%): `uv run interrogate pycopg --fail-under 95 --quiet`
    — must exit 0 (measured 100%).

    Gate 3 (Sphinx -W clean): install docs deps then build warning-free —
    `uv pip install -r docs/requirements.txt` then
    `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` — must exit 0
    with no warnings escalated to errors.

    Gate 4 (-W error::DeprecationWarning green): run the suite with deprecation
    warnings escalated to errors —
    `PGDATABASE=pycopg_test2 uv run pytest -W error::DeprecationWarning` — must exit
    0 (same pre-existing PostGIS-env caveat as Gate 1).

    Do NOT tag or publish here — that is Plan 40-02. This task only verifies and
    records gate results.
  </action>
  <verify>
    <automated>uv run interrogate pycopg --fail-under 95 --quiet && PGDATABASE=pycopg_test2 uv run pytest -q 2>&1 | tail -5 | grep -Eq 'passed|Required test coverage'</automated>
  </verify>
  <acceptance_criteria>
    - `PGDATABASE=pycopg_test2 uv run pytest` reports coverage ≥95% and exits 0 (or fails ONLY on the 3 known pre-existing PostGIS-env tests, recorded explicitly in the SUMMARY)
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0
    - `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0 with no warnings
    - `PGDATABASE=pycopg_test2 uv run pytest -W error::DeprecationWarning` exits 0 (or fails ONLY on the same 3 known PostGIS-env tests)
    - the SUMMARY records each of the 4 gate outcomes with its exact command and result (no subjective "looks good")
  </acceptance_criteria>
  <done>All 4 gates verified green (coverage ≥95, interrogate 100%, Sphinx -W clean, deprecation-error suite clean), with the known PostGIS-env caveat explicitly recorded; the tree is ready for Plan 40-02 to tag and publish.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tree → published PyPI artifact | version strings + changelog become an immutable public release; a wrong/stale version ships a mislabeled package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | Tampering | version-source drift (pyproject.toml / uv.lock / docs/conf.py disagree) | mitigate | Task 1 bumps all three and runs `uv lock --check`; Plan 40-02 clean-venv smoke asserts `__version__ == "0.10.0"` end-to-end |
| T-40-01-02 | Information disclosure | CHANGELOG content (no secrets, public-by-design) | accept | CHANGELOG is intentionally public; no secret material is added |
| T-40-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new runtime deps this milestone (project constraint); `uv lock --check` confirms no unexpected lockfile churn; no [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- All three version sources read 0.10.0 and `uv lock --check` is clean.
- CHANGELOG `[0.10.0]` is Keep-a-Changelog-compliant (Changed + Fixed only).
- All 4 gates verified green (with the documented PostGIS-env caveat).
</verification>

<success_criteria>
- `pyproject.toml`, `uv.lock`, and `docs/conf.py` all read 0.10.0; `uv lock --check` exits 0.
- `CHANGELOG.md` contains a `## [0.10.0] - 2026-06-26` entry with `### Changed` and `### Fixed`, no `### Added`, qualitative COPY perf with a `benchmarks/` pointer, and no v1.0.0 signaling.
- Coverage ≥95%, interrogate 100%, Sphinx `-W` clean, `-W error::DeprecationWarning` green (PostGIS-env caveat recorded).
</success_criteria>

<output>
Create `.planning/phases/40-release-v0-10-0/40-01-SUMMARY.md` when done.
</output>
