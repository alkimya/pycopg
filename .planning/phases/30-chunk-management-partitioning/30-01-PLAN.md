---
phase: 30-chunk-management-partitioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/exceptions.py
  - pycopg/queries.py
  - tests/test_timescale.py
autonomous: true
requirements: [TS-ADV-04, TS-ADV-05]
must_haves:
  truths:
    - "TimescaleError is importable from pycopg.exceptions and subclasses PycopgError"
    - "queries.py defines TSDB_SHOW_CHUNKS with the literal %%I.%%I regclass JOIN key and ORDER BY range_start ASC"
    - "tests/test_timescale.py exists with a ts_db sync skip-fixture, an async_ts_db skip-fixture, and Wave 0 test stubs for all 4 methods so per-task verify commands resolve"
  artifacts:
    - path: "pycopg/exceptions.py"
      provides: "class TimescaleError(PycopgError)"
      contains: "class TimescaleError(PycopgError)"
    - path: "pycopg/queries.py"
      provides: "TSDB_SHOW_CHUNKS SQL constant (and TSDB_DROP_CHUNKS if used)"
      contains: "TSDB_SHOW_CHUNKS"
    - path: "tests/test_timescale.py"
      provides: "ts_db + async_ts_db skip-fixtures and Wave 0 test scaffold"
      min_lines: 40
  key_links:
    - from: "tests/test_timescale.py"
      to: "tests/test_database_integration.py ts_db fixture"
      via: "ported create-extension-or-skip pattern"
      pattern: "has_extension\\(.timescaledb.\\)"
---

<objective>
Land the Phase 30 foundation that every method task depends on: the milestone-wide `TimescaleError`
exception, the `TSDB_SHOW_CHUNKS` SQL constant (with correct `%%I` escaping and `range_start ASC`
ordering), and the brand-new `tests/test_timescale.py` test file with sync + async skip-fixtures and
Wave 0 test stubs.

Purpose: Per the Nyquist rule, the new test file MUST exist before method tasks so their `-k` verify
commands resolve to real (initially-skipping/xfailing) tests instead of "no tests collected". This
plan also unblocks the shared `TimescaleError` (D-09) and the SHOW_CHUNKS query reused by both
`show_chunks` and `drop_chunks` (D-04/D-05).
Output: `class TimescaleError(PycopgError)` in `pycopg/exceptions.py`; `TSDB_SHOW_CHUNKS` (and
optionally `TSDB_DROP_CHUNKS`) in `pycopg/queries.py`; `tests/test_timescale.py` with `ts_db` +
`async_ts_db` fixtures and Wave 0 stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-chunk-management-partitioning/30-CONTEXT.md
@.planning/phases/30-chunk-management-partitioning/30-RESEARCH.md
@.planning/phases/30-chunk-management-partitioning/30-PATTERNS.md
@.planning/phases/30-chunk-management-partitioning/30-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TimescaleError to exceptions.py + TSDB_SHOW_CHUNKS to queries.py</name>
  <files>pycopg/exceptions.py, pycopg/queries.py</files>
  <read_first>
    - pycopg/exceptions.py:1-70 (whole file — PycopgError base at :6-9; ExtensionNotAvailable/TableNotFound one-liner+pass form at :24-33; ETLError hierarchy at :54-69)
    - pycopg/queries.py — the TIMESCALEDB section around lines 224-243 (LIST_HYPERTABLES, HYPERTABLE_INFO) and TABLE_SIZES at :198-203, to mirror the exact `%%I` escaping convention and constant placement
    - 30-PATTERNS.md §"New exception TimescaleError" (the exact one-liner-docstring + pass form) and §"SQL constant %%I escaping"
    - 30-RESEARCH.md §"Recommended SHOW_CHUNKS SQL (live-verified)" and §"Code Examples → show_chunks builder"
  </read_first>
  <action>
    In pycopg/exceptions.py, add `class TimescaleError(PycopgError):` with the one-line docstring
    "Error raised by TimescaleDB management operations." followed by `pass`, placed after the existing
    subclasses (per D-09 — milestone-wide, reused in Phases 31-32; do NOT name it HypertableNotEmpty).
    Follow the exact form of ExtensionNotAvailable/TableNotFound (no __init__).

    In pycopg/queries.py, add a `TSDB_SHOW_CHUNKS` constant in the TIMESCALEDB section next to
    LIST_HYPERTABLES/HYPERTABLE_INFO. The SQL body (live-verified per RESEARCH, D-04/D-05):
    `SELECT c.chunk_schema || '.' || c.chunk_name AS chunk_name FROM show_chunks('{schema}.{table}'{older_arg}{newer_arg}) AS sc JOIN timescaledb_information.chunks c ON format('%%I.%%I', c.chunk_schema, c.chunk_name)::regclass = sc ORDER BY c.range_start ASC`.
    CRITICAL: write the JOIN-key format mask as the literal `%%I.%%I` (psycopg eats a single `%`; this
    matches the existing HYPERTABLE_INFO/TABLE_SIZES convention). The `{schema}`/`{table}` and the
    conditional `{older_arg}`/`{newer_arg}` fragments are interpolated by the builder in Plan 02 — model
    this constant as a Python format-template string (or a documented partial) so the builder can append
    `, older_than => %s::interval` / `, older_than => %s` (and the newer_than equivalents) per D-02.
    Optionally add a `TSDB_DROP_CHUNKS` template for the `SELECT drop_chunks('{schema}.{table}'{args})`
    side-effect call (constant name is Claude's discretion per CONTEXT). Add a brief comment above the
    constant explaining the `%%I` escaping (mirror timescale.py:272-275's comment style).
  </action>
  <verify>
    <automated>uv run python -c "from pycopg.exceptions import TimescaleError, PycopgError; assert issubclass(TimescaleError, PycopgError)" && uv run python -c "from pycopg import queries; assert '%%I.%%I' in queries.TSDB_SHOW_CHUNKS and 'range_start ASC' in queries.TSDB_SHOW_CHUNKS"</automated>
  </verify>
  <acceptance_criteria>
    - pycopg/exceptions.py contains `class TimescaleError(PycopgError)`
    - `from pycopg.exceptions import TimescaleError` succeeds and `issubclass(TimescaleError, PycopgError)` is True
    - pycopg/queries.py contains `TSDB_SHOW_CHUNKS` with the literal `%%I.%%I` in the regclass JOIN key
    - `TSDB_SHOW_CHUNKS` contains `ORDER BY c.range_start ASC` (oldest-first per D-05)
    - `TSDB_SHOW_CHUNKS` contains `show_chunks('{schema}.{table}'` and `timescaledb_information.chunks`
    - `uv run ruff check pycopg/exceptions.py pycopg/queries.py` exits 0
  </acceptance_criteria>
  <done>TimescaleError importable + subclasses PycopgError; TSDB_SHOW_CHUNKS present with %%I escaping and range_start ASC ordering; lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_timescale.py with sync + async skip-fixtures and Wave 0 stubs</name>
  <files>tests/test_timescale.py</files>
  <read_first>
    - tests/test_database_integration.py:836-889 (TestDatabaseTimescaleCoverage: the ts_db skip-fixture at :839, plus the FeatureNotSupported tolerance block at :866-878 and the sync monkeypatch-on-accessor pattern at :880-889)
    - tests/test_async_database.py:2334-2367 (mock has_extension SQL-shape async unit-test pattern + the negative extension-missing-raises analog)
    - 30-PATTERNS.md §"Test File Scaffold (tests/test_timescale.py — NEW)" (both layers, with exact fixture + mock shapes)
    - 30-VALIDATION.md §"Wave 0 Requirements" and §"Per-Task Verification Map" (the -k command names: show_chunks, drop_chunks, add_dimension, reorder)
    - pyproject.toml — confirm `asyncio_mode = "auto"` so async tests need no per-test marker
  </read_first>
  <action>
    Create tests/test_timescale.py as the new home for all v0.8.0 advanced-TSDB tests (D-10). Do NOT
    move any existing v0.6.0 basic-TSDB tests. Port the `ts_db` sync skip-fixture verbatim from
    test_database_integration.py:839 (create-extension-or-skip: skip if has_extension is False after a
    create_extension attempt). Add an `async_ts_db` async equivalent using the async db fixture +
    `await ...has_extension(...)`. Add module-level `from psycopg.errors import FeatureNotSupported`
    for the reorder-policy tolerance used by Plan 03.

    Add Wave 0 test stubs so each method's `-k` verify command in later plans resolves to a real test
    (not "no tests collected"). For each of the four method keywords — show_chunks, drop_chunks,
    add_dimension, reorder — add at least one placeholder test marked `@pytest.mark.xfail(reason="Wave 0
    scaffold — implemented in Plan 02/03", strict=False)` (or `pytest.skip(...)` inside the body) so the
    file collects green now and the real assertions land in Plans 02/03 by replacing the stub bodies.
    Keep the stub names keyword-matchable: e.g. test_show_chunks_*, test_drop_chunks_*,
    test_add_dimension_*, test_add_reorder_policy_*. Do NOT add real DB assertions here — that is Plans
    02/03's job; this task only guarantees the file + fixtures + collectable stubs exist.
  </action>
  <verify>
    <automated>uv run pytest tests/test_timescale.py -q -o addopts="" && uv run pytest tests/test_timescale.py -k "show_chunks or drop_chunks or add_dimension or reorder" -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_timescale.py exists and `uv run pytest tests/test_timescale.py -q -o addopts=""` exits 0 (no errors/failures; xfail/skip allowed)
    - File defines a `ts_db` sync fixture mirroring test_database_integration.py:839 (create-extension-or-skip)
    - File defines an `async_ts_db` async fixture awaiting `has_extension("timescaledb")`
    - File imports `FeatureNotSupported` from psycopg.errors
    - `uv run pytest tests/test_timescale.py -k show_chunks -q -o addopts=""` collects at least 1 test
    - `uv run pytest tests/test_timescale.py -k reorder -q -o addopts=""` collects at least 1 test
    - `uv run ruff check tests/test_timescale.py` exits 0
  </acceptance_criteria>
  <done>tests/test_timescale.py collects green with ts_db + async_ts_db fixtures and keyword-matchable Wave 0 stubs for all 4 methods; FeatureNotSupported imported.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 portion)

- `pycopg/exceptions.py` → `class TimescaleError(PycopgError)` (milestone-wide; reused Phases 31-32)
- `pycopg/queries.py` → `TSDB_SHOW_CHUNKS` (and optionally `TSDB_DROP_CHUNKS`) SQL constant(s)
- `tests/test_timescale.py` → NEW file: `ts_db` + `async_ts_db` skip-fixtures, `FeatureNotSupported`
  import, Wave 0 stubs for show_chunks / drop_chunks / add_dimension / add_reorder_policy

(Remaining phase artifacts — the 8 accessor methods — are delivered by Plans 02 and 03.)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → accessor SQL | Table/schema/column identifiers and chunk-bound values cross into composed SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering / Injection | `TSDB_SHOW_CHUNKS` constant (`{schema}`/`{table}` interpolation) | mitigate | Identifiers are validated via `validate_identifiers` at the call site (Plan 02) before interpolation; chunk-bound VALUES are bound as `%s`/`%s::interval`, never interpolated. This constant carries no runtime values itself. |
| T-30-02 | Tampering | No new package installs this phase | accept | Zero new dependencies (CLAUDE.md mandate); no npm/pip/cargo install tasks — package legitimacy gate N/A. |

SQL-injection surface is minimal: all identifiers pass `validate_identifiers`/`validate_identifier`
and all runtime values bind as `%s`. No new dependency or external service is introduced.
</threat_model>

<verification>
- `uv run python -c "from pycopg.exceptions import TimescaleError"` succeeds
- `TSDB_SHOW_CHUNKS` present with `%%I.%%I` and `range_start ASC`
- `uv run pytest tests/test_timescale.py -q -o addopts=""` green (collects fixtures + stubs)
- `uv run ruff check pycopg tests` exits 0
</verification>

<success_criteria>
- `TimescaleError(PycopgError)` exists and is importable (D-09)
- `TSDB_SHOW_CHUNKS` SQL constant correct per D-04/D-05 with proper `%%I` escaping
- `tests/test_timescale.py` exists with sync + async skip-fixtures and collectable Wave 0 stubs for all 4 method keywords
- Lint clean; no existing tests moved
</success_criteria>

<output>
Create `.planning/phases/30-chunk-management-partitioning/30-01-SUMMARY.md` when done.
</output>
