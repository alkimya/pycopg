---
phase: 30-chunk-management-partitioning
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - pycopg/timescale.py
  - tests/test_timescale.py
autonomous: true
requirements: [TS-ADV-04, TS-ADV-05]
must_haves:
  truths:
    - "User can call db.timescale.show_chunks('ht') and receive a list[str] of fully-qualified chunk names sorted oldest-first; older_than/newer_than filter the subset"
    - "User can call db.timescale.drop_chunks('ht', older_than='30 days') and the matching chunks are gone; dry_run=True returns the would-be-dropped list without deleting"
    - "drop_chunks raises ValueError BEFORE any DB round-trip when both older_than and newer_than are None"
    - "async_db.timescale.show_chunks and drop_chunks mirror the sync methods and AWAIT both the has_extension guard and execute"
  artifacts:
    - path: "pycopg/timescale.py"
      provides: "show_chunks + drop_chunks on TimescaleAccessor and AsyncTimescaleAccessor"
      contains: "def show_chunks"
    - path: "tests/test_timescale.py"
      provides: "mock SQL-shape unit tests + live-DB integration tests for show_chunks/drop_chunks (sync + async)"
      contains: "show_chunks"
  key_links:
    - from: "pycopg/timescale.py show_chunks"
      to: "queries.TSDB_SHOW_CHUNKS"
      via: "self._db.execute(sql, params)"
      pattern: "TSDB_SHOW_CHUNKS"
    - from: "drop_chunks dry_run path"
      to: "show_chunks capture query"
      via: "capture-before-mutate; dry_run short-circuits before drop"
      pattern: "dry_run"
---

<objective>
Deliver the two chunk-listing methods — `show_chunks` (TS-ADV-04) and `drop_chunks` (TS-ADV-05) — on
BOTH `TimescaleAccessor` (sync) and `AsyncTimescaleAccessor` (async), using the SQL constant and
TimescaleError from Plan 01. Add mock SQL-shape unit tests and live-DB integration tests by replacing
the Plan 01 Wave 0 stubs.

Purpose: These are the read / read-then-mutate methods. They share the `TSDB_SHOW_CHUNKS` query: the
type-driven `%s` cast (D-02), oldest-first fully-qualified return shape (D-04/D-05), the both-None
`ValueError` safety guard (D-03), and the capture-before-drop dry_run pattern.
Output: 4 new methods (2 sync + 2 async) in `pycopg/timescale.py`; show_chunks/drop_chunks test
coverage in `tests/test_timescale.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-chunk-management-partitioning/30-CONTEXT.md
@.planning/phases/30-chunk-management-partitioning/30-RESEARCH.md
@.planning/phases/30-chunk-management-partitioning/30-PATTERNS.md
@.planning/phases/30-chunk-management-partitioning/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement show_chunks + drop_chunks on sync and async accessors</name>
  <files>pycopg/timescale.py</files>
  <behavior>
    - show_chunks('ht') with both bounds None → list[str] of fully-qualified chunk names, oldest-first by range_start (e.g. _hyper_N_10_chunk sorts AFTER _hyper_N_9_chunk, not lexically)
    - show_chunks('ht', older_than='30 days') → builder appends ", older_than => %s::interval" and binds the string param
    - show_chunks('ht', older_than=datetime(...)) → builder appends ", older_than => %s" (bare, no ::interval) and binds the datetime
    - show_chunks with both older_than and newer_than → params list is [older_than, newer_than] in that exact order (matching {older_arg}{newer_arg} append order)
    - drop_chunks('ht', older_than=None, newer_than=None) → raises ValueError before any execute call
    - drop_chunks('ht', older_than='30 days', dry_run=True) → returns the captured show_chunks list, no drop_chunks() SRF executed
    - drop_chunks('ht', older_than='30 days', dry_run=False) → captures list via show_chunks query FIRST, then executes SELECT drop_chunks(...), returns the captured list
    - async show_chunks/drop_chunks awaits has_extension guard AND execute; ValueError guard stays plain (no await)
  </behavior>
  <read_first>
    - pycopg/timescale.py:45-94 (create_hypertable — canonical guard→validate→execute body) and the hypertable_info method around :245-277 (read-returning-rows shape + the %%I caller comment at :272-275)
    - pycopg/timescale.py async section: AsyncTimescaleAccessor.create_hypertable (~:297-346) and add_retention_policy (~:441-476) — the EXACT `await has_extension` + `await execute` shape (the missing-await is the recurring Phase 23 gotcha)
    - pycopg/etl.py:527-588 (_build_incremental_extract_sql — the repo's reference for appending `%s` + matching param in lockstep; especially :585-586)
    - pycopg/utils.py:78,107,125 (validate_identifier / validate_identifiers / validate_interval)
    - pycopg/queries.py — TSDB_SHOW_CHUNKS (and TSDB_DROP_CHUNKS if added) from Plan 01
    - 30-PATTERNS.md §"TimescaleAccessor.show_chunks", §"TimescaleAccessor.drop_chunks", §"AsyncTimescaleAccessor.* (all 4 async mirrors)"
    - 30-RESEARCH.md §"D-05 Resolution", §"drop_chunks Resolution"
  </read_first>
  <action>
    Add `show_chunks(table, older_than=None, newer_than=None, schema="public") -> list[str]` and
    `drop_chunks(table, older_than=None, newer_than=None, schema="public", dry_run=False) -> list[str]`
    to BOTH TimescaleAccessor and AsyncTimescaleAccessor. Type hint bounds as `str | datetime | None`
    (import datetime for type hints; per D-01).

    show_chunks body (mirror hypertable_info shape): guard `has_extension("timescaledb")` →
    `validate_identifiers(table, schema)` → build the SQL from queries.TSDB_SHOW_CHUNKS by interpolating
    `{schema}`/`{table}` and the conditional bound fragments, building the params list in LOCKSTEP and in
    older-then-newer order (D-02 footgun). Per-bound branch: a `str` bound emits fragment
    `, older_than => %s::interval` (resp. `newer_than`) and appends the string param; a `datetime` bound
    emits bare `, older_than => %s` and appends the datetime; `None` emits neither fragment nor param.
    Execute `self._db.execute(sql, params)` and return `[r["chunk_name"] for r in rows]`. Do NOT sort in
    Python — ordering comes from `ORDER BY range_start ASC` in the SQL.

    drop_chunks body (capture-before-mutate, D-03): FIRST `if older_than is None and newer_than is None:
    raise ValueError(...)` BEFORE the extension guard so a bad call never needs a connection. Then guard
    → validate_identifiers → run the same TSDB_SHOW_CHUNKS-shaped capture query to get the ordered list
    while chunks still exist. If `dry_run`: return the captured list without dropping. Else:
    `self._db.execute("SELECT drop_chunks('{schema}.{table}'{args})", params)` (args/params built the same
    way as show_chunks bounds), then return the captured list. Do NOT JOIN the drop_chunks() SRF to the
    info view (it returns text not regclass and rows vanish post-drop). Mark drop_chunks DESTRUCTIVE /
    IRREVERSIBLE in the docstring Notes section (numpydoc). Factor the bound-fragment+param building into
    a shared private helper to avoid duplication between show_chunks and drop_chunks if convenient
    (planner discretion per CONTEXT).

    Async mirrors: identical logic with `await self._db.schema.has_extension(...)` AND
    `await self._db.execute(...)`. validate_identifiers / the ValueError + type-branch logic stay plain
    (sync — no await). Verify by hand every async path awaits both the guard and execute.
    numpydoc docstrings on all 4 methods (interrogate ≥95 gate).
  </action>
  <verify>
    <automated>uv run pytest tests/test_timescale.py -k "show_chunks or drop_chunks" -q -o addopts="" && uv run pytest tests/test_parity.py -k accessor_parity -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - `def show_chunks` and `def drop_chunks` exist on BOTH TimescaleAccessor and AsyncTimescaleAccessor (4 defs total)
    - `grep -n "async def show_chunks" pycopg/timescale.py` shows the async def awaits `self._db.schema.has_extension` before building SQL (verify by reading the body)
    - `grep -n "async def drop_chunks" pycopg/timescale.py` shows the async def awaits `has_extension` and `await self._db.execute`
    - drop_chunks raises ValueError when older_than is None and newer_than is None with no DB call (assert via mock that execute is never called)
    - show_chunks builds params in [older_than, newer_than] order (assert via mock SQL-shape test)
    - `uv run pytest tests/test_parity.py -k accessor_parity -q -o addopts=""` exits 0 (no asymmetry introduced)
    - `uv run ruff check pycopg/timescale.py` exits 0
  </acceptance_criteria>
  <done>show_chunks + drop_chunks present on both classes with correct guards/awaits, type-driven %s casts, both-None ValueError, and capture-before-drop dry_run; parity test green.</done>
</task>

<task type="auto">
  <name>Task 2: Test show_chunks + drop_chunks (mock SQL-shape + live-DB integration, sync + async)</name>
  <files>tests/test_timescale.py</files>
  <read_first>
    - tests/test_timescale.py (current Wave 0 stubs + ts_db/async_ts_db fixtures from Plan 01)
    - tests/test_async_database.py:2334-2367 (mock has_extension SQL-shape async unit test + extension-missing-raises negative)
    - tests/test_database_integration.py:836-889 (ts_db fixture usage + live-DB hypertable setup patterns)
    - 30-PATTERNS.md §"Test File Scaffold" (Layer 1 live + Layer 2 mock, with exact mock shapes incl. params-order assertion)
    - 30-RESEARCH.md §"Live output (both-None, 12-chunk hypertable)" (oldest-first assertion: _10 AFTER _9) and §"Live-verified drop"
  </read_first>
  <action>
    Replace the Plan 01 Wave 0 stubs for show_chunks and drop_chunks with real tests (two layers, D-11):

    Mock SQL-shape unit tests (no live DB; mirror test_async_database.py:2334): mock
    `schema.has_extension` (AsyncMock(return_value=True) for async), mock execute to return rows like
    `[{"chunk_name": "..."}]`, call the method, then assert the generated SQL contains `show_chunks(` and
    `ORDER BY c.range_start ASC`; assert the bound fragment is `%s::interval` for a str bound and bare
    `%s` for a datetime bound; assert `params == [older_than, newer_than]` ordering. For drop_chunks add a
    mock test asserting (a) both-None raises ValueError and execute is NEVER called, and (b) dry_run=True
    does NOT issue `SELECT drop_chunks(` while dry_run=False does, and both return the captured list.

    Live-DB integration tests (gated by ts_db / async_ts_db skip-fixtures): create a small hypertable
    with several chunks (insert rows spanning multiple chunk intervals), call show_chunks, assert the
    returned names are fully-qualified (`_timescaledb_internal._hyper_` prefix) AND oldest-first — assert
    the range ordering, not just membership (a _10 chunk must come AFTER _9, NOT lexical). Then call
    drop_chunks with dry_run=True and assert nothing was dropped (show_chunks count unchanged); call
    drop_chunks with a real older_than bound and assert the matching chunks are gone and the returned list
    matches the dropped names. Add async equivalents using async_ts_db. Keep all four `-k` keywords
    (show_chunks, drop_chunks) matchable. Remove the xfail markers on these stubs now that they assert
    real behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/test_timescale.py -k "show_chunks or drop_chunks" -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_timescale.py -k show_chunks -q -o addopts=""` exits 0
    - `uv run pytest tests/test_timescale.py -k drop_chunks -q -o addopts=""` exits 0
    - A mock test asserts show_chunks params == [older_than, newer_than] in that order
    - A mock test asserts drop_chunks(both None) raises ValueError AND execute is never called
    - A mock test asserts dry_run=True does not emit `SELECT drop_chunks(` while dry_run=False does
    - A live test asserts oldest-first range ordering (not lexical) on a multi-chunk hypertable, or skips cleanly if TSDB absent
    - Async equivalents present for show_chunks and drop_chunks
    - `uv run ruff check tests/test_timescale.py` exits 0
  </acceptance_criteria>
  <done>Mock + live tests for show_chunks/drop_chunks (sync + async) pass or skip cleanly; ordering, params-order, both-None ValueError, and dry_run no-op all asserted.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 portion)

- `pycopg/timescale.py` → `TimescaleAccessor.show_chunks`, `TimescaleAccessor.drop_chunks`,
  `AsyncTimescaleAccessor.show_chunks`, `AsyncTimescaleAccessor.drop_chunks`
- `tests/test_timescale.py` → mock SQL-shape + live-DB integration tests for show_chunks/drop_chunks
  (sync + async), replacing the Plan 01 Wave 0 stubs

(TS-ADV-10 parity for these two methods is auto-satisfied via the already-registered ACCESSOR_PAIRS —
no registry change. add_dimension / add_reorder_policy land in Plan 03.)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → show_chunks/drop_chunks SQL | table/schema identifiers + older_than/newer_than bound values cross into composed SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Tampering / SQL injection | `show_chunks`/`drop_chunks` `{schema}.{table}` interpolation | mitigate | `validate_identifiers(table, schema)` before interpolation; chunk-bound VALUES bound as `%s`/`%s::interval`, never f-string-interpolated. |
| T-30-04 | Denial of Service / data loss | `drop_chunks` both-bounds-None full-table wipe | mitigate | `ValueError` raised before any DB round-trip when both bounds are None (D-03); `dry_run` preview short-circuits before the drop; docstring marks DESTRUCTIVE/IRREVERSIBLE. |
| T-30-05 | Repudiation / silent guard bypass | async `has_extension` guard missing `await` | mitigate | Explicit acceptance criterion that each async method awaits both the guard and execute (recurring Phase 23 gotcha; parity test does not catch missing await). |

SQL-injection surface minimal — identifiers validated, values parameterized. No new dependency.
</threat_model>

<verification>
- `uv run pytest tests/test_timescale.py -k "show_chunks or drop_chunks" -q -o addopts=""` green
- `uv run pytest tests/test_parity.py -k accessor_parity -q -o addopts=""` green
- `uv run ruff check pycopg tests` exits 0
</verification>

<success_criteria>
- show_chunks returns oldest-first, fully-qualified list[str]; filters work (TS-ADV-04)
- drop_chunks: both-None ValueError before round-trip, dry_run preview without deleting, real drop returns dropped names (TS-ADV-05)
- Async mirrors await both guard and execute
- All tests pass or skip cleanly; parity green; lint clean
</success_criteria>

<output>
Create `.planning/phases/30-chunk-management-partitioning/30-02-SUMMARY.md` when done.
</output>
