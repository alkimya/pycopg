---
phase: 30-chunk-management-partitioning
plan: 03
type: execute
wave: 3
depends_on: ["30-01", "30-02"]
files_modified:
  - pycopg/timescale.py
  - tests/test_timescale.py
autonomous: true
requirements: [TS-ADV-08, TS-ADV-09]
must_haves:
  truths:
    - "User can call db.timescale.add_dimension('ht', 'device_id', partition_type='hash', number_partitions=4) and the dimension is registered on TSDB 2.28 (by_hash/by_range form, succeeds on populated hypertables)"
    - "add_dimension raises ValueError at construction time (before any round-trip) for the wrong type/param combination (hash↔number_partitions / range↔chunk_interval mutual exclusivity)"
    - "add_dimension for an already-existing dimension (with if_not_exists=False) raises pycopg-domain TimescaleError"
    - "User can call db.timescale.add_reorder_policy('ht', 'idx'); the generated SQL is asserted by a mock unit test; the live test tolerates FeatureNotSupported under Apache license"
    - "All 4 Phase-30 methods have async counterparts verified by test_accessor_parity; coverage ratchet ≥94% holds"
  artifacts:
    - path: "pycopg/timescale.py"
      provides: "add_dimension + add_reorder_policy on TimescaleAccessor and AsyncTimescaleAccessor"
      contains: "def add_dimension"
    - path: "tests/test_timescale.py"
      provides: "mock SQL-shape unit tests (authoritative for reorder) + live-DB integration with license tolerance"
      contains: "add_reorder_policy"
  key_links:
    - from: "pycopg/timescale.py add_dimension"
      to: "TimescaleError"
      via: "attempt-DDL → catch duplicate-dimension → re-raise TimescaleError (if_not_exists=False)"
      pattern: "TimescaleError"
    - from: "add_reorder_policy live test"
      to: "psycopg.errors.FeatureNotSupported"
      via: "try/except tolerance (Apache license)"
      pattern: "FeatureNotSupported"
---

<objective>
Deliver the two partitioning/policy DDL methods — `add_dimension` (TS-ADV-08) and `add_reorder_policy`
(TS-ADV-09) — on BOTH accessors, completing the 4-method Phase 30 surface so `test_accessor_parity`
(TS-ADV-10) fully passes. Add mock SQL-shape unit tests (authoritative for add_reorder_policy under the
Apache license) and live-DB integration tests with FeatureNotSupported tolerance.

Purpose: add_dimension uses the modern TSDB 2.28 `by_hash`/`by_range` builder (D-06), construction-time
`ValueError` mutual-exclusivity (D-07), and the RESHAPED D-08 behavior — wrap the duplicate-dimension DB
error (not the nonexistent non-empty error) as `TimescaleError`. add_reorder_policy is Community-license
only, so the mock test is the authoritative SQL assertion and the live test tolerates FeatureNotSupported
(D-12).
Output: 4 new methods (2 sync + 2 async) in `pycopg/timescale.py`; add_dimension/add_reorder_policy test
coverage in `tests/test_timescale.py`; full TS-ADV-10 parity green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-chunk-management-partitioning/30-CONTEXT.md
@.planning/phases/30-chunk-management-partitioning/30-RESEARCH.md
@.planning/phases/30-chunk-management-partitioning/30-PATTERNS.md
@.planning/phases/30-chunk-management-partitioning/30-01-SUMMARY.md
@.planning/phases/30-chunk-management-partitioning/30-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement add_dimension + add_reorder_policy on sync and async accessors</name>
  <files>pycopg/timescale.py</files>
  <behavior>
    - add_dimension('ht', 'device_id', partition_type='hash', number_partitions=4) → SELECT add_dimension('public.ht', by_hash('device_id', 4), if_not_exists => true)
    - add_dimension('ht', 'ts2', partition_type='range', chunk_interval='7 days') → SELECT add_dimension('public.ht', by_range('ts2', INTERVAL '7 days'), if_not_exists => true)
    - add_dimension(partition_type='hash', number_partitions=None) → ValueError at construction, no DB call (hash requires number_partitions)
    - add_dimension(partition_type='hash', chunk_interval='7 days') → ValueError (hash forbids chunk_interval)
    - add_dimension(partition_type='range', chunk_interval=None) → ValueError (range requires chunk_interval)
    - add_dimension(partition_type='range', number_partitions=4) → ValueError (range forbids number_partitions)
    - add_dimension on an already-existing dimension with if_not_exists=False → TimescaleError (wraps the duplicate-dimension DB error, TS160)
    - add_reorder_policy('ht', 'idx') → SELECT add_reorder_policy('public.ht', 'idx', if_not_exists => true) AS job_id
    - async variants await has_extension guard AND execute; ValueError/mutual-exclusivity guards stay plain
  </behavior>
  <read_first>
    - pycopg/timescale.py:45-94 (create_hypertable — canonical builder; by_hash/by_range mirrors this INTERVAL '{...}' interpolation) and add_retention_policy (~:187-222, the policy-DDL analog for add_reorder_policy)
    - pycopg/timescale.py async section: AsyncTimescaleAccessor.create_hypertable (~:297-346) and add_retention_policy (~:441-476) — EXACT `await has_extension` + `await execute` shape
    - pycopg/exceptions.py — TimescaleError from Plan 01
    - pycopg/utils.py:78,107,125 (validate_identifier / validate_identifiers / validate_interval)
    - 30-PATTERNS.md §"TimescaleAccessor.add_dimension + add_reorder_policy" (incl. the D-08 reshape note: broad except around the execute, re-raise TimescaleError, only exercised with if_not_exists=False)
    - 30-RESEARCH.md §"add_dimension Resolution" + §"Finding 1" (by_hash/by_range live-verified; duplicate-dimension TS160 surfaces as a generic psycopg DatabaseError subclass) + §"add_reorder_policy Resolution" + §"Finding 2"
  </read_first>
  <action>
    Add `add_dimension(table, column, partition_type="hash", number_partitions=None,
    chunk_interval=None, schema="public", if_not_exists=True)` and `add_reorder_policy(table,
    index_name, schema="public", if_not_exists=True)` to BOTH TimescaleAccessor and
    AsyncTimescaleAccessor.

    add_dimension body: FIRST the D-07 mutual-exclusivity ValueError BEFORE any guard/round-trip —
    partition_type="hash" requires number_partitions and forbids chunk_interval; partition_type="range"
    requires chunk_interval and forbids number_partitions; any other partition_type is also a ValueError.
    Then guard has_extension → `validate_identifiers(table, schema)` + `validate_identifier(column)`. Build
    the dimension expression: hash → `by_hash('{column}', {int(number_partitions)})`; range →
    `validate_interval(chunk_interval)` then `by_range('{column}', INTERVAL '{chunk_interval}')`. Append
    `, if_not_exists => true` when if_not_exists. Execute
    `SELECT add_dimension('{schema}.{table}', {dim}{ne})`. D-08 RESHAPE: wrap the execute in a broad
    `except Exception` (TS160 "already a dimension" surfaces as a generic psycopg DatabaseError/
    OperationalError subclass) and re-raise as `TimescaleError` with a clear message. Note that with
    if_not_exists=True a duplicate dimension returns a NOTICE (no error), so the TimescaleError path is
    only reachable with if_not_exists=False — keep the catch narrow enough not to swallow the
    ExtensionNotAvailable guard (raise the guard before the try). Do NOT add a non-empty-hypertable
    pre-check — the modern builder succeeds on populated hypertables (Finding 1).

    add_reorder_policy body (mirror add_retention_policy): guard → `validate_identifiers(table, schema,
    index_name)` → execute `SELECT add_reorder_policy('{schema}.{table}', '{index_name}', if_not_exists =>
    true) AS job_id`. Let FeatureNotSupported propagate (the live test tolerates it; do NOT swallow it in
    the method).

    Async mirrors: identical logic with `await self._db.schema.has_extension(...)` AND
    `await self._db.execute(...)`. The add_dimension try/except wraps the `await self._db.execute(...)`.
    Verify by hand every async path awaits both. numpydoc docstrings on all 4 methods (interrogate ≥95).
  </action>
  <verify>
    <automated>uv run pytest tests/test_timescale.py -k "add_dimension or reorder" -q -o addopts="" && uv run pytest tests/test_parity.py -k accessor_parity -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - `def add_dimension` and `def add_reorder_policy` exist on BOTH TimescaleAccessor and AsyncTimescaleAccessor (4 defs total)
    - add_dimension raises ValueError (no DB call) for each of the 4 mutual-exclusivity violations (hash w/o number_partitions, hash w/ chunk_interval, range w/o chunk_interval, range w/ number_partitions)
    - add_dimension generates `by_hash('device_id', 4)` for hash and `by_range('ts2', INTERVAL '7 days')` for range (assert via mock SQL-shape test)
    - add_dimension re-raises a duplicate-dimension DB error as `TimescaleError` (assert via mock: execute raises a psycopg DatabaseError subclass → method raises TimescaleError)
    - add_reorder_policy generates `add_reorder_policy('public.ht', 'idx', if_not_exists => true)` (assert via mock)
    - async add_dimension/add_reorder_policy await both has_extension and execute (verify by reading the bodies)
    - `uv run pytest tests/test_parity.py -k accessor_parity -q -o addopts=""` exits 0 with all 4 Phase-30 methods mirrored
    - `uv run ruff check pycopg/timescale.py` exits 0
  </acceptance_criteria>
  <done>add_dimension + add_reorder_policy on both classes; construction-time ValueErrors; dup-dimension→TimescaleError; by_hash/by_range + reorder SQL shapes correct; async awaits both; full parity green.</done>
</task>

<task type="auto">
  <name>Task 2: Test add_dimension + add_reorder_policy (mock authoritative + live with license tolerance)</name>
  <files>tests/test_timescale.py</files>
  <read_first>
    - tests/test_timescale.py (current state after Plan 02 — Wave 0 stubs for add_dimension/reorder still present)
    - tests/test_database_integration.py:836-889 (ts_db fixture + the FeatureNotSupported try/except tolerance at :866-878, plus the sync monkeypatch-on-accessor analog at :880-889)
    - tests/test_async_database.py:2334-2367 (mock has_extension SQL-shape async unit test)
    - 30-PATTERNS.md §"Test File Scaffold" (Layer 1 live + license tolerance; Layer 2 mock authoritative for reorder; the dup-dimension re-raise note)
    - 30-RESEARCH.md §"Finding 1" (dup-dimension TS160) + §"Finding 2" (Apache license / FeatureNotSupported) + §"add_reorder_policy Resolution" (job-row columns when licensed)
  </read_first>
  <action>
    Replace the Plan 01 Wave 0 stubs for add_dimension and add_reorder_policy with real tests (two
    layers, D-11):

    Mock SQL-shape unit tests (authoritative for add_reorder_policy per D-12): mock has_extension +
    execute, call add_dimension(hash) and assert generated SQL contains `by_hash('device_id', 4)` and
    `if_not_exists => true`; call add_dimension(range) and assert `by_range('ts2', INTERVAL '7 days')`.
    Assert each of the 4 mutual-exclusivity ValueErrors raises with execute NEVER called. Add a mock test
    where execute raises a psycopg DatabaseError subclass and assert add_dimension re-raises TimescaleError
    (only with if_not_exists=False). For add_reorder_policy, assert the generated SQL contains
    `add_reorder_policy(` and the index name and `if_not_exists => true` — this mock test is the
    authoritative assertion for TS-ADV-09. Add async mock equivalents (AsyncMock has_extension).

    Live-DB integration tests (ts_db / async_ts_db gated): add_dimension on a real hypertable with rows
    already inserted, assert the dimension registers (query timescaledb_information.dimensions or call
    succeeds returning a job_id); call add_dimension again on the SAME column with if_not_exists=False and
    assert it raises TimescaleError. For add_reorder_policy: wrap the live call in
    `try/except FeatureNotSupported: pass` (mirror test_database_integration.py:866-878); guard the
    job-row assertion (timescaledb_information.jobs) + `CALL run_job(job_id)` behind that tolerance so they
    only exercise on a Community/timescale-licensed build and stay green/skipped on the local Apache build.
    Remove the xfail markers now that real assertions land. Keep `-k add_dimension` and `-k reorder`
    matchable.
  </action>
  <verify>
    <automated>uv run pytest tests/test_timescale.py -k "add_dimension or reorder" -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_timescale.py -k add_dimension -q -o addopts=""` exits 0
    - `uv run pytest tests/test_timescale.py -k reorder -q -o addopts=""` exits 0
    - A mock test asserts add_dimension(hash) SQL contains `by_hash('device_id', 4)` and add_dimension(range) contains `by_range('ts2', INTERVAL '7 days')`
    - Mock tests assert all 4 mutual-exclusivity ValueErrors raise with execute never called
    - A mock test asserts a DB-error from execute is re-raised as TimescaleError (if_not_exists=False)
    - The authoritative add_reorder_policy mock test asserts the SQL contains `add_reorder_policy(` and `if_not_exists => true`
    - The live reorder test wraps the call in `try/except FeatureNotSupported: pass`
    - Async equivalents present for add_dimension and add_reorder_policy
    - `uv run ruff check tests/test_timescale.py` exits 0
  </acceptance_criteria>
  <done>Mock (authoritative) + live (license-tolerant) tests for add_dimension/add_reorder_policy (sync + async) pass or skip cleanly; ValueErrors, dup-dimension→TimescaleError, and SQL shapes all asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite gate — parity + coverage ratchet + lint</name>
  <files>tests/test_timescale.py</files>
  <read_first>
    - pyproject.toml — the coverage ratchet config (fail-under / addopts) and asyncio_mode
    - tests/test_parity.py:24-60 (ACCESSOR_PAIRS at :25 + test_accessor_parity)
    - .planning/STATE.md §"Performance Metrics" (coverage baseline 95.11%, ratchet ≥94%)
  </read_first>
  <action>
    Run the full suite to confirm Phase 30 holds all gates before /gsd-verify-work. Confirm
    test_accessor_parity passes with all 4 new methods mirrored on both classes (TS-ADV-10 — no
    ACCESSOR_PAIRS edit needed; the only failure mode is a typo'd name present on one class but not the
    other). Confirm the coverage ratchet (≥94%) still holds — if any new branch (the type-driven cast
    branches, the dry_run short-circuit, the dup-dimension except, the license-tolerant live path) is
    uncovered and drops coverage below the ratchet, add a targeted mock test to cover it. Run lint +
    format. Do NOT touch any file other than tests/test_timescale.py in this task (e.g. add a missing
    coverage test); if a coverage gap traces to a method bug, note it for the SUMMARY rather than editing
    timescale.py here. Note the 2 known pre-existing flaky full-suite DB tests
    (test_async_transaction_fix, test_create_spatial_index_name_parameter) and the ~2.7% flaky bound-param
    test — re-run targeted with `-o addopts=""` if they flake; they are not Phase 30 regressions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_parity.py -k accessor_parity -q -o addopts="" && uv run pytest && uv run ruff check pycopg tests</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_parity.py -k accessor_parity -q -o addopts=""` exits 0 (all 4 methods mirrored)
    - `uv run pytest` exits 0 (full suite green; coverage ratchet ≥94% held — re-run targeted with `-o addopts=""` only if a known flaky test trips)
    - `uv run ruff check pycopg tests` exits 0
    - `uv run black --check pycopg tests` exits 0 (or formatting applied)
  </acceptance_criteria>
  <done>Full suite green, TS-ADV-10 parity confirmed for all 4 methods, coverage ratchet ≥94% held, lint + format clean.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 portion + phase total)

Plan 03:
- `pycopg/timescale.py` → `TimescaleAccessor.add_dimension`, `TimescaleAccessor.add_reorder_policy`,
  `AsyncTimescaleAccessor.add_dimension`, `AsyncTimescaleAccessor.add_reorder_policy`
- `tests/test_timescale.py` → mock (authoritative) + live (license-tolerant) tests for
  add_dimension/add_reorder_policy (sync + async)

### Full Phase 30 artifact list (all plans)
- `pycopg/timescale.py`: `show_chunks`, `drop_chunks`, `add_dimension`, `add_reorder_policy` —
  each on BOTH `TimescaleAccessor` and `AsyncTimescaleAccessor` (8 method defs total)
- `pycopg/queries.py`: `TSDB_SHOW_CHUNKS` (and optionally `TSDB_DROP_CHUNKS`)
- `pycopg/exceptions.py`: `class TimescaleError(PycopgError)`
- `tests/test_timescale.py`: NEW file — sync + async skip-fixtures, mock SQL-shape unit tests, and
  live-DB integration tests for all 4 methods
- TS-ADV-10 parity: auto-satisfied via the already-registered `(TimescaleAccessor,
  AsyncTimescaleAccessor)` entry in `tests/test_parity.py` ACCESSOR_PAIRS — NO registry change.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → add_dimension/add_reorder_policy SQL | table/schema/column/index_name identifiers + number_partitions/chunk_interval cross into composed DDL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Tampering / SQL injection | add_dimension `{column}`/`{table}`/`{schema}` and `INTERVAL '{chunk_interval}'` interpolation | mitigate | `validate_identifier(column)` + `validate_identifiers(table, schema)`; `validate_interval(chunk_interval)`; `number_partitions` cast via `int()`. |
| T-30-07 | Tampering / SQL injection | add_reorder_policy `{index_name}`/`{table}`/`{schema}` interpolation | mitigate | `validate_identifiers(table, schema, index_name)` before interpolation. |
| T-30-08 | Error handling / information disclosure | duplicate-dimension DB error surface | mitigate | Caught broadly and re-raised as a clean pycopg-domain `TimescaleError` (D-08 reshape) — no raw DB internals leak to callers; ExtensionNotAvailable guard raised before the try so it is not swallowed. |
| T-30-09 | Repudiation / silent guard bypass | async `has_extension` guard missing `await` | mitigate | Explicit acceptance criterion that each async method awaits both the guard and execute (Phase 23 gotcha). |

SQL-injection surface minimal — identifiers validated, intervals validated, integers cast. No new
dependency or external service is introduced this phase.
</threat_model>

<verification>
- `uv run pytest tests/test_timescale.py -k "add_dimension or reorder" -q -o addopts=""` green
- `uv run pytest tests/test_parity.py -k accessor_parity -q -o addopts=""` green (all 4 methods mirrored)
- `uv run pytest` full suite green, coverage ratchet ≥94% held
- `uv run ruff check pycopg tests` + `uv run black --check pycopg tests` clean
</verification>

<success_criteria>
- add_dimension: by_hash/by_range form (D-06), construction-time mutual-exclusivity ValueError (D-07), dup-dimension→TimescaleError (D-08 reshape) (TS-ADV-08)
- add_reorder_policy: mock SQL-shape authoritative, live test tolerates FeatureNotSupported (D-12) (TS-ADV-09)
- All 4 Phase-30 methods mirrored async; test_accessor_parity green (TS-ADV-10)
- Coverage ratchet ≥94% held; lint + format clean
</success_criteria>

<output>
Create `.planning/phases/30-chunk-management-partitioning/30-03-SUMMARY.md` when done.
</output>
