---
phase: 03-async-admin-backup-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/async_database.py
  - tests/test_async_database.py
autonomous: true

must_haves:
  truths:
    - "User can call async drop_table() to drop a table"
    - "User can call async create_index() and drop_index() for index management"
    - "User can call async list_indexes() and list_constraints() for table inspection"
    - "User can call async drop_schema() for schema removal"
    - "User can call async table_sizes() for storage stats"
    - "User can call async create_database() and drop_database() for database lifecycle"
  artifacts:
    - path: "pycopg/async_database.py"
      provides: "9 new async DDL/admin methods"
      contains: "async def drop_table"
    - path: "tests/test_async_database.py"
      provides: "Unit tests for all 9 new methods"
      contains: "TestAsyncDatabaseDDL"
  key_links:
    - from: "AsyncDatabase.drop_table"
      to: "self.execute"
      via: "await self.execute(f'DROP TABLE ...')"
      pattern: "await self\\.execute.*DROP TABLE"
    - from: "AsyncDatabase.create_database"
      to: "psycopg.AsyncConnection.connect"
      via: "admin connection with autocommit=True"
      pattern: "AsyncConnection\\.connect.*autocommit=True"
---

<objective>
Add 9 DDL/admin/stats async methods to AsyncDatabase: drop_table, create_index, drop_index, list_indexes, list_constraints, drop_schema, table_sizes, create_database, drop_database. Plus unit tests for each.

Purpose: Achieves async parity for DDL operations (ASYNC-09, ASYNC-10, ASYNC-11 partial, ASYNC-12, ASYNC-13, ASYNC-24 partial, ASYNC-25) — the straightforward async execute conversions.

Output: 9 new methods on AsyncDatabase + unit tests covering happy paths and error cases.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-async-admin-backup-parity/03-RESEARCH.md
@pycopg/async_database.py
@pycopg/database.py
@tests/test_async_database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DDL/admin/stats async methods to AsyncDatabase</name>
  <files>pycopg/async_database.py</files>
  <action>
Add 9 async methods to AsyncDatabase, matching the exact signatures and behavior from Database (database.py). Place them in logical sections with section header comments matching the Database convention.

**Group 1 — Simple async execute (add after the existing schema methods, before EXTENSIONS section):**

1. `async def drop_table(self, name, schema="public", if_exists=True, cascade=False) -> None` — Direct conversion of Database.drop_table (line 835). Use validate_identifiers, build IF EXISTS/CASCADE clauses, await self.execute().

2. `async def create_index(self, table, columns, schema="public", name=None, unique=False, method="btree", if_not_exists=True) -> None` — Direct conversion of Database.create_index (line 1014). Handle str|list[str] columns, validate_identifiers + validate_index_method, build CREATE INDEX SQL, await self.execute().

3. `async def drop_index(self, name, schema="public", if_exists=True) -> None` — Direct conversion of Database.drop_index (line 1057). Build DROP INDEX SQL, await self.execute().

4. `async def list_indexes(self, table, schema="public") -> list[dict]` — Direct conversion of Database.list_indexes (line 1068). Execute pg_index/pg_class/pg_am join query, return result.

5. `async def list_constraints(self, table, schema="public") -> list[dict]` — Direct conversion of Database.list_constraints (line 1092). Execute pg_constraint join query, return result.

6. `async def drop_schema(self, name, if_exists=True, cascade=False) -> None` — Direct conversion of Database.drop_schema (line 732). validate_identifier, build clauses, await self.execute().

7. `async def table_sizes(self, schema="public", limit=20) -> list[dict]` — Direct conversion of Database.table_sizes (line 1586). Execute the pg_size_pretty/pg_total_relation_size query. Note: use %%I for psycopg percent escaping in format() calls, exactly as sync version does.

**Group 2 — Admin connection with autocommit (add in a new DATABASE ADMINISTRATION section):**

8. `async def create_database(self, name, owner=None, template="template1") -> None` — Adapted from Database.create_database (line 576). Must use a separate admin connection to postgres database with autocommit=True. Use: `self.config.with_database("postgres")` then `async with await psycopg.AsyncConnection.connect(**admin_config.connect_params(), autocommit=True) as conn:`. validate_identifier for name, owner, template.

9. `async def drop_database(self, name, if_exists=True) -> None` — Adapted from Database.drop_database (line 599). Same admin connection pattern. CRITICAL: Must terminate existing connections first using pg_terminate_backend query before DROP DATABASE, exactly as sync version does.

**Important implementation notes:**
- Import `validate_index_method` from `pycopg.utils` at the top of the file (add to existing imports if not present).
- All type hints must match Database signatures exactly (use Optional, list, str | list[str], etc.).
- Docstrings follow Google-style format with Args, Returns, Example sections matching Database conventions.
- Section headers use the `# =========================================================================` style.
- For create_database/drop_database, import psycopg at module level is already done — use `psycopg.AsyncConnection.connect()`.

**Note on requirements:**
- ASYNC-11 mentions create_table but Database does NOT have create_table — skip it (users create tables via execute() or from_dataframe).
- ASYNC-24 mentions index_sizes but Database does NOT have index_sizes — skip it (table_sizes already includes index_size column).
- schema_exists already exists on AsyncDatabase — do not re-add.
  </action>
  <verify>
Run: `python -c "from pycopg.async_database import AsyncDatabase; import inspect; methods = ['drop_table','create_index','drop_index','list_indexes','list_constraints','drop_schema','table_sizes','create_database','drop_database']; [print(f'{m}: async={inspect.iscoroutinefunction(getattr(AsyncDatabase, m))}') for m in methods]"` — all 9 must show async=True.
  </verify>
  <done>All 9 async methods exist on AsyncDatabase, are coroutine functions, and have matching signatures to their Database counterparts.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for DDL/admin/stats methods</name>
  <files>tests/test_async_database.py</files>
  <action>
Add unit tests for all 9 new methods. Use the existing test patterns from the file (create_async_cursor_mock, create_async_conn_mock helpers, patch on psycopg.AsyncConnection.connect).

**Add TestAsyncDatabaseDDL class (~12 test methods):**

1. `test_drop_table_basic` — Mock execute, call drop_table("users"), verify execute called with "DROP TABLE IF EXISTS public.users".
2. `test_drop_table_cascade` — Call with cascade=True, verify CASCADE in SQL.
3. `test_create_index_basic` — Call create_index("users", "email"), verify CREATE INDEX SQL with btree method.
4. `test_create_index_unique_multi_column` — Call with unique=True, columns=["a","b"], verify UNIQUE and multi-column SQL.
5. `test_drop_index_basic` — Call drop_index("idx_users_email"), verify DROP INDEX SQL.
6. `test_list_indexes` — Mock execute returning list of dicts, call list_indexes("users"), verify return value.
7. `test_list_constraints` — Mock execute returning list of dicts, call list_constraints("users"), verify return value.
8. `test_drop_schema_basic` — Call drop_schema("analytics"), verify DROP SCHEMA SQL.
9. `test_drop_schema_cascade` — Call with cascade=True, verify CASCADE.
10. `test_table_sizes` — Mock execute returning table size dicts, verify return value.

**Add TestAsyncDatabaseAdmin class (~4 test methods):**

11. `test_create_database` — Patch psycopg.AsyncConnection.connect to return mock async connection+cursor. Call create_database("testdb"). Verify connect called with autocommit=True and postgres database. Verify CREATE DATABASE SQL executed.
12. `test_create_database_with_owner` — Call with owner="appuser", verify OWNER clause in SQL.
13. `test_drop_database` — Same patching pattern. Call drop_database("testdb"). Verify pg_terminate_backend called first, then DROP DATABASE.
14. `test_drop_database_if_not_exists` — Call with if_exists=False, verify no IF EXISTS in SQL.

**Testing patterns:**
- For DDL methods: patch `db.execute` as AsyncMock, verify SQL string and params.
- For admin methods: patch `psycopg.AsyncConnection.connect` to return an async context manager yielding a mock connection with mock cursor. Use asynccontextmanager pattern from existing tests.
- Use the existing `config` fixture for creating AsyncDatabase instances.
- All test methods are `async def` with `@pytest.mark.asyncio` (or rely on the pytest-asyncio auto mode if configured).
  </action>
  <verify>
Run: `python -m pytest tests/test_async_database.py -v --tb=short -q` — all tests pass including the ~14 new ones. No regressions in existing 46 tests.
  </verify>
  <done>At least 14 new test methods added covering all 9 DDL/admin/stats methods. All existing + new tests pass.</done>
</task>

</tasks>

<verification>
1. `python -c "from pycopg.async_database import AsyncDatabase; methods = ['drop_table','create_index','drop_index','list_indexes','list_constraints','drop_schema','table_sizes','create_database','drop_database']; missing = [m for m in methods if not hasattr(AsyncDatabase, m)]; print(f'Missing: {missing}' if missing else 'All 9 methods present')"` — must show "All 9 methods present"
2. `python -m pytest tests/test_async_database.py -v --tb=short -q` — all tests pass
3. `python -m pytest tests/ -v --tb=short -q` — no regressions in full test suite
</verification>

<success_criteria>
- All 9 async methods exist on AsyncDatabase with correct signatures matching Database
- create_database/drop_database use admin connection with autocommit=True
- drop_database terminates existing connections before dropping
- At least 14 new unit tests covering all methods
- All existing tests continue to pass (46+ in test_async_database.py)
</success_criteria>

<output>
After completion, create `.planning/phases/03-async-admin-backup-parity/03-01-SUMMARY.md`
</output>
