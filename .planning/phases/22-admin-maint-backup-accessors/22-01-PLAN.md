---
phase: 22-admin-maint-backup-accessors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/admin.py
  - pycopg/maint.py
  - pycopg/backup.py
autonomous: true
requirements: [ADM-01, MNT-01, BKP-01]

must_haves:
  truths:
    - "pycopg/admin.py defines AdminAccessor (11 methods) and AsyncAdminAccessor (11 methods)"
    - "pycopg/maint.py defines MaintAccessor (6 methods) and AsyncMaintAccessor (6 methods)"
    - "pycopg/backup.py defines BackupAccessor (4 public + _psql_restore) and AsyncBackupAccessor (4 public + _psql_restore)"
    - "create_role sibling calls route through self._db.admin.* (accessor), never the flat alias self._db.role_exists/grant_role"
    - "All moved bodies are byte-for-byte verbatim apart from self.->self._db. rewrites (D-06)"
    - "All identifier validators travel with the moved bodies (no SQL-injection regression)"
  artifacts:
    - path: "pycopg/admin.py"
      provides: "AdminAccessor + AsyncAdminAccessor (roles & permissions)"
      contains: "class AdminAccessor"
      min_lines: 250
    - path: "pycopg/maint.py"
      provides: "MaintAccessor + AsyncMaintAccessor (size & maintenance)"
      contains: "class MaintAccessor"
      min_lines: 120
    - path: "pycopg/backup.py"
      provides: "BackupAccessor + AsyncBackupAccessor (dump/restore/CSV)"
      contains: "class BackupAccessor"
      min_lines: 200
  key_links:
    - from: "pycopg/admin.py::AdminAccessor.create_role"
      to: "self._db.admin.role_exists / self._db.admin.grant_role"
      via: "sibling-accessor delegation (Bucket A, D-02)"
      pattern: "self\\._db\\.admin\\.(role_exists|grant_role)"
    - from: "pycopg/backup.py::BackupAccessor.pg_restore"
      to: "self._psql_restore"
      via: "private companion call (Bucket P)"
      pattern: "self\\._psql_restore"
---

<objective>
Create the three new accessor modules — `pycopg/admin.py`, `pycopg/maint.py`,
`pycopg/backup.py` — each holding both a sync `<Name>Accessor` and an async
`Async<Name>Accessor`, mirroring `pycopg/timescale.py` exactly. Move the 21
method bodies (sync + async) verbatim from `Database`/`AsyncDatabase`, applying
only the `self.`→`self._db.` rewrite rule (D-03), the two sibling-accessor
rewrites in `create_role` (D-02), and moving the private `_psql_restore`
companion alongside `pg_restore`.

Purpose: This wave produces the real implementations that Wave 2 will wire up as
lazy properties and that the flat `db.*` stubs will delegate to. It is the
second application of the LOCKED Phase 21 pattern — replicate line-for-line; do
not refactor, extract builders, or "improve" any SQL (D-06).

Output: 3 new module files. No changes to `database.py`/`async_database.py` yet
(those are Wave 2). At the end of this wave the new accessor classes are
importable and instantiable but not yet reachable via `db.admin`/`db.maint`/`db.backup`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-admin-maint-backup-accessors/22-CONTEXT.md
@.planning/phases/22-admin-maint-backup-accessors/22-RESEARCH.md
@pycopg/timescale.py
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This section is consumed by the drift-verification pass to exclude
newly-created symbols. Phase 22 (all 3 waves) creates:

**New modules (Wave 1):** `pycopg/admin.py`, `pycopg/maint.py`, `pycopg/backup.py`

**New accessor classes (Wave 1):**
- `AdminAccessor`, `AsyncAdminAccessor` (admin.py)
- `MaintAccessor`, `AsyncMaintAccessor` (maint.py)
- `BackupAccessor`, `AsyncBackupAccessor` (backup.py)
- private `_psql_restore` method on `BackupAccessor` / `AsyncBackupAccessor`

**Cache fields + properties on Database/AsyncDatabase (Wave 2):**
- `_admin`, `_maint`, `_backup` cache fields
- `admin`, `maint`, `backup` lazy `@property` accessors

**21 @deprecated_alias stub functions (Wave 2)** replacing the flat method defs:
- admin (11): `create_role`, `drop_role`, `role_exists`, `list_roles`, `alter_role`, `grant_role`, `revoke_role`, `grant`, `revoke`, `list_role_members`, `list_role_grants`
- maint (6): `size`, `table_size`, `table_sizes`, `vacuum`, `analyze`, `explain`
- backup (4): `pg_dump`, `pg_restore`, `copy_to_csv`, `copy_from_csv`

**New test modules (Wave 3):** `tests/test_admin_aliases.py`, `tests/test_maint_aliases.py`, `tests/test_backup_aliases.py`

**New ACCESSOR_PAIRS entries (Wave 3):** `(AdminAccessor, AsyncAdminAccessor)`, `(MaintAccessor, AsyncMaintAccessor)`, `(BackupAccessor, AsyncBackupAccessor)`

**__init__.py exports (Wave 2):** the 6 accessor class names added to `__all__`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create pycopg/admin.py (AdminAccessor + AsyncAdminAccessor, 11 methods each)</name>
  <files>pycopg/admin.py</files>
  <read_first>
    - pycopg/timescale.py (the module template: module docstring, `from __future__ import annotations`, `TYPE_CHECKING` guard for Database/AsyncDatabase, `__init__(self, db)` storing `self._db`, both sync + async accessor classes in one module)
    - pycopg/database.py lines 1854-2227 (the 11 sync admin method bodies: create_role 1854, drop_role 1928, role_exists 1942, list_roles 1958, alter_role 1974, grant_role 2043, revoke_role 2059, grant 2072, revoke 2133, list_role_members 2193, list_role_grants 2209)
    - pycopg/async_database.py admin bodies (per RESEARCH async parity table: role_exists 1273, list_roles 1289, create_role 1309, drop_role 1385, alter_role 1399, grant 1470, revoke 1531, grant_role 1591, revoke_role 1609, list_role_members 1622, list_role_grants 1638)
    - 22-RESEARCH.md "D-03 Self-Call Classification Table → Admin Accessor" (every self-call classified Bucket A vs B) and "Import Matrix for New Modules → pycopg/admin.py imports needed"
    - pycopg/base.py (confirm `build_role_options` signature) and pycopg/utils.py (confirm the 5 validators)
  </read_first>
  <action>
    Create `pycopg/admin.py` modeled exactly on `timescale.py`. Module docstring
    follows the timescale form ("Admin accessor classes for db.admin.* / async_db.admin.*").

    Imports (per RESEARCH Import Matrix — replicate exactly):
    `from __future__ import annotations`; `from typing import TYPE_CHECKING`;
    `from pycopg import queries`; `from pycopg.base import build_role_options`;
    `from pycopg.utils import (validate_identifier, validate_identifiers, validate_object_type, validate_privileges, validate_timestamp)`;
    and the `if TYPE_CHECKING:` guard importing `Database` from `pycopg.database`
    and `AsyncDatabase` from `pycopg.async_database`.

    Define `class AdminAccessor:` with `__init__(self, db: Database)` storing
    `self._db = db` (copy the timescale `__init__` docstring shape). Move the 11
    sync admin method bodies VERBATIM (D-06), applying only these rewrites:
    - Bucket B (core-flat): every `self.execute(...)` → `self._db.execute(...)`;
      every `self.cursor(autocommit=True)` → `self._db.cursor(autocommit=True)`.
    - Bucket A (sibling-accessor, D-02 — create_role ONLY): `self.role_exists(name)`
      → `self._db.admin.role_exists(name)` and `self.grant_role(role, name)` →
      `self._db.admin.grant_role(role, name)`. These MUST route through `.admin.`,
      NOT the flat `self._db.role_exists`/`self._db.grant_role` (which would emit an
      internal DeprecationWarning and break the -W error gate).

    Define `class AsyncAdminAccessor:` with `__init__(self, db: AsyncDatabase)`,
    moving the 11 async bodies verbatim with the same rewrite buckets, preserving
    `await`/`async with`: `await self.role_exists(name)` → `await self._db.admin.role_exists(name)`,
    `await self.grant_role(role, name)` → `await self._db.admin.grant_role(role, name)`,
    `async with self.cursor(autocommit=True)` → `async with self._db.cursor(autocommit=True)`,
    `await self.execute(...)` → `await self._db.execute(...)`.

    Keep all moved numpydoc docstrings and all `validate_*` / `build_role_options`
    calls unchanged. Do NOT extract builders, do NOT touch any inline SQL or
    `queries.*` reference. No `pycopg.exceptions` import is needed for admin.
  </action>
  <verify>
    <automated>uv run python -c "from pycopg.admin import AdminAccessor, AsyncAdminAccessor; import inspect; s=[n for n,_ in inspect.getmembers(AdminAccessor) if not n.startswith('_')]; a=[n for n,_ in inspect.getmembers(AsyncAdminAccessor) if not n.startswith('_')]; assert len(s)==11, s; assert len(a)==11, a; print('admin OK', sorted(s))"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "class AdminAccessor" pycopg/admin.py` returns 1; `grep -c "class AsyncAdminAccessor" pycopg/admin.py` returns 1.
    - Count: the import-and-introspect verify command above exits 0 (11 sync public methods, 11 async public methods).
    - D-02 guard (CRITICAL): `grep -nE 'self\._db\.(role_exists|grant_role)\(' pycopg/admin.py` returns NOTHING (flat sibling alias must not be called). `grep -c 'self\._db\.admin\.role_exists' pycopg/admin.py` returns 2 (1 sync + 1 async create_role); `grep -c 'self\._db\.admin\.grant_role' pycopg/admin.py` returns 2.
    - Imports: `grep -c 'from pycopg.base import build_role_options' pycopg/admin.py` returns 1; the 5 validators are imported from pycopg.utils.
    - Behavior: `uv run pytest tests/test_sql_injection.py -k 'role or grant or revoke' -x -q -o addopts=""` exits 0 (validators preserved). (Call-sites still use flat names here; migration is Wave 3 — flat names still resolve via the old defs until Wave 2.)
    - Import safety: `uv run python -c "import pycopg.admin"` exits 0 (no circular import).
  </acceptance_criteria>
  <done>pycopg/admin.py exists with AdminAccessor and AsyncAdminAccessor, each exposing exactly 11 public methods moved verbatim; the only sibling rewrites are the 4 create_role calls routing through self._db.admin.*; no flat self._db.role_exists/grant_role calls exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create pycopg/maint.py (MaintAccessor + AsyncMaintAccessor, 6 methods each)</name>
  <files>pycopg/maint.py</files>
  <read_first>
    - pycopg/timescale.py (module template — same as Task 1)
    - pycopg/database.py lines 1697-1853 (sync maint bodies: size 1697, table_size 1720, table_sizes 1747, vacuum 1769, analyze 1803, explain 1818)
    - pycopg/async_database.py maint bodies (per RESEARCH: size 1657, table_size 1680, table_sizes 1707 in the first cluster; vacuum 2168, analyze 2202, explain 2217 in the second cluster — both clusters form the full async MaintAccessor surface, no self-calls between them)
    - 22-RESEARCH.md "D-03 Self-Call Classification Table → Maint Accessor" (all Bucket B; note `self.config.database` attribute access → `self._db.config.database`) and "Import Matrix → pycopg/maint.py imports needed"
  </read_first>
  <action>
    Create `pycopg/maint.py` modeled on `timescale.py`. Module docstring follows
    the timescale form ("Maintenance & size accessor classes for db.maint.* / async_db.maint.*").

    Imports (per RESEARCH Import Matrix): `from __future__ import annotations`;
    `from typing import TYPE_CHECKING`; `from pycopg import queries`;
    `from pycopg.utils import validate_identifiers`; and the `if TYPE_CHECKING:`
    guard for `Database`/`AsyncDatabase`. No `base` helpers, no exceptions.

    Define `class MaintAccessor:` (`__init__(self, db: Database)` → `self._db = db`)
    and `class AsyncMaintAccessor:` (`__init__(self, db: AsyncDatabase)`). Move the
    6 sync + 6 async bodies VERBATIM (D-06). All self-calls are Bucket B:
    every `self.execute(...)` → `self._db.execute(...)` (sync), `await self.execute(...)`
    → `await self._db.execute(...)` (async); and the attribute access
    `self.config.database` → `self._db.config.database` in `size`.

    Keep all `queries.*` constants (DATABASE_SIZE_PRETTY/DATABASE_SIZE,
    TABLE_SIZE_PRETTY/TABLE_SIZE, TABLE_SIZES), `validate_identifiers` calls, and
    numpydoc docstrings unchanged. No sibling-accessor (Bucket A) or cross-accessor
    rewrites exist in maint.
  </action>
  <verify>
    <automated>uv run python -c "from pycopg.maint import MaintAccessor, AsyncMaintAccessor; import inspect; s=[n for n,_ in inspect.getmembers(MaintAccessor) if not n.startswith('_')]; a=[n for n,_ in inspect.getmembers(AsyncMaintAccessor) if not n.startswith('_')]; assert sorted(s)==['analyze','explain','size','table_size','table_sizes','vacuum'], s; assert sorted(a)==sorted(s), a; print('maint OK', sorted(s))"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "class MaintAccessor" pycopg/maint.py` returns 1; `grep -c "class AsyncMaintAccessor" pycopg/maint.py` returns 1.
    - Count: the introspect verify command exits 0 with exactly the 6 names `analyze, explain, size, table_size, table_sizes, vacuum` on both classes.
    - Rewrite completeness: `grep -nE 'self\.execute\(' pycopg/maint.py` returns NOTHING (all rewritten to self._db.execute); `grep -nE 'self\.config\.' pycopg/maint.py` returns NOTHING (rewritten to self._db.config). `grep -c 'self\._db\.config\.database' pycopg/maint.py` returns >= 2 (sync + async size).
    - Imports: `grep -c 'from pycopg.utils import validate_identifiers' pycopg/maint.py` returns 1.
    - Import safety: `uv run python -c "import pycopg.maint"` exits 0.
  </acceptance_criteria>
  <done>pycopg/maint.py exists with MaintAccessor and AsyncMaintAccessor each exposing exactly 6 verbatim-moved public methods; all self.execute/self.config rewritten to self._db.*; no stray flat self-calls remain.</done>
</task>

<task type="auto">
  <name>Task 3: Create pycopg/backup.py (BackupAccessor + AsyncBackupAccessor, 4 public + _psql_restore companion)</name>
  <files>pycopg/backup.py</files>
  <read_first>
    - pycopg/timescale.py (module template — same as Task 1)
    - pycopg/database.py lines 2228-2540 (sync backup bodies: pg_dump 2228, pg_restore 2291, _psql_restore 2369 (private companion), copy_to_csv 2395, copy_from_csv 2469)
    - pycopg/async_database.py backup bodies (per RESEARCH: pg_dump 2253, pg_restore 2322, _psql_restore 2406, copy_to_csv 2438, copy_from_csv 2524)
    - 22-RESEARCH.md "D-03 Self-Call Classification Table → Backup Accessor", "`_psql_restore` Private Companion — Special Note", "Subprocess / AsyncIO Pattern in BackupAccessor", and "Import Matrix → pycopg/backup.py imports needed"
    - 22-RESEARCH.md Pitfalls 2, 3, 4 (forgetting _psql_restore; asyncio not imported; self.config not rewritten)
  </read_first>
  <action>
    Create `pycopg/backup.py` modeled on `timescale.py`. Module docstring follows
    the timescale form ("Backup & restore accessor classes for db.backup.* / async_db.backup.*").

    Imports (per RESEARCH Import Matrix, plus the asyncio note): `from __future__
    import annotations`; `import asyncio` at MODULE level (AsyncBackupAccessor uses
    asyncio.create_subprocess_exec / asyncio.subprocess.PIPE / asyncio.to_thread —
    Pitfall 3); any of `import os`, `from pathlib import Path`, `from typing import
    TYPE_CHECKING, Literal`, `from collections.abc import Sequence` that the moved
    body signatures actually reference (read the bodies — include only what is
    used); `from pycopg.base import build_pg_dump_cmd, build_pg_restore_cmd`;
    `from pycopg.utils import validate_identifiers, validate_csv_option`; and the
    `if TYPE_CHECKING:` guard for `Database`/`AsyncDatabase`.

    NOTE on sync subprocess import: the sync `pg_dump`/`pg_restore`/`_psql_restore`
    bodies do `import subprocess` LOCALLY inside the method. Preserve that verbatim
    (D-06) — do NOT hoist it to module level.

    Define `class BackupAccessor:` (`__init__(self, db: Database)`) and
    `class AsyncBackupAccessor:` (`__init__(self, db: AsyncDatabase)`). Move the 4
    public bodies (`pg_dump`, `pg_restore`, `copy_to_csv`, `copy_from_csv`) AND the
    private `_psql_restore` companion into BOTH classes (Pitfall 2 — forgetting it
    causes AttributeError at restore). Rewrites:
    - Attribute access `self.config.*` (host/port/user/database/password) →
      `self._db.config.*` everywhere in pg_dump/pg_restore/_psql_restore (Pitfall 4).
    - `self.cursor()` → `self._db.cursor()` in copy_to_csv/copy_from_csv (Bucket B);
      async `async with self.cursor()` → `async with self._db.cursor()`.
    - async copy_to_csv `await self.execute(...)` (row-count SELECT) → `await self._db.execute(...)`.
    - Bucket P: inside `pg_restore`, `self._psql_restore(input_file)` STAYS as
      `self._psql_restore(input_file)` (sync) / `await self._psql_restore(input_file)`
      (async) — both methods are on the same accessor; no rewrite.
    - Cursor-level `cur.execute(...)` / `cur.rowcount` / `cur.copy(...)` stay as-is
      (not db-level calls).
    - Do NOT introduce `shell=True`; preserve the existing list-form
      `subprocess.run([...])` / `asyncio.create_subprocess_exec(...)` exactly.

    `_psql_restore` is private — it does NOT get a flat stub on Database (Wave 2
    only stubs the 4 public names). Keep all numpydoc docstrings.
  </action>
  <verify>
    <automated>uv run python -c "from pycopg.backup import BackupAccessor, AsyncBackupAccessor; import inspect; s=[n for n,_ in inspect.getmembers(BackupAccessor) if not n.startswith('_')]; a=[n for n,_ in inspect.getmembers(AsyncBackupAccessor) if not n.startswith('_')]; assert sorted(s)==['copy_from_csv','copy_to_csv','pg_dump','pg_restore'], s; assert sorted(a)==sorted(s), a; assert hasattr(BackupAccessor,'_psql_restore') and hasattr(AsyncBackupAccessor,'_psql_restore'), 'missing _psql_restore'; print('backup OK', sorted(s))"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "class BackupAccessor" pycopg/backup.py` returns 1; `grep -c "class AsyncBackupAccessor" pycopg/backup.py` returns 1.
    - Count + companion: the introspect verify exits 0 (exactly 4 public methods `copy_from_csv, copy_to_csv, pg_dump, pg_restore`; `_psql_restore` present on both classes).
    - _psql_restore call preserved: `grep -c 'self\._psql_restore' pycopg/backup.py` returns >= 4 (1 def + 1 call per sync/async = 4 minimum).
    - config rewrite: `grep -nE 'self\.config\.' pycopg/backup.py` returns NOTHING; `grep -c 'self\._db\.config\.' pycopg/backup.py` returns >= 1.
    - asyncio at module level: `grep -nE '^import asyncio' pycopg/backup.py` returns a match (Pitfall 3).
    - Security: `grep -nE 'shell\s*=\s*True' pycopg/backup.py` returns NOTHING (list-form subprocess preserved).
    - Imports: `grep -c 'from pycopg.base import build_pg_dump_cmd, build_pg_restore_cmd' pycopg/backup.py` returns 1.
    - Import safety + behavior: `uv run python -c "import pycopg.backup"` exits 0; `uv run pytest tests/test_subprocess_env.py -x -q -o addopts=""` exits 0 (subprocess env/list-form preserved; call-sites still flat until Wave 3).
  </acceptance_criteria>
  <done>pycopg/backup.py exists with BackupAccessor and AsyncBackupAccessor each exposing 4 public methods plus the private _psql_restore companion; all self.config rewritten to self._db.config; asyncio imported at module level; no shell=True introduced; _psql_restore call inside pg_restore preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → SQL identifier | role/table/schema names interpolated into DDL/SQL inside admin & maint bodies |
| caller → subprocess argv | host/port/user/database/path values reach pg_dump/pg_restore/psql process args in backup bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering (SQL injection) | admin.py / maint.py moved bodies | mitigate | The verbatim move MUST carry every `validate_identifier`/`validate_identifiers`/`validate_object_type`/`validate_privileges`/`validate_timestamp` call. Acceptance criteria assert the validator imports exist; `tests/test_sql_injection.py` stays green (Task 1 verify). |
| T-22-02 | Tampering (argument/command injection) | backup.py pg_dump/pg_restore/_psql_restore | mitigate | Preserve the existing list-form `subprocess.run([...])` / `asyncio.create_subprocess_exec(...)`; explicit acceptance criterion `grep shell=True returns nothing`; `tests/test_subprocess_env.py` stays green (Task 3 verify). |
| T-22-03 | Elevation of privilege (internal deprecated path) | admin.py create_role sibling calls | mitigate | D-02 guard: sibling calls route through `self._db.admin.*`, never the flat alias — prevents the moved code from silently invoking a deprecated path. Asserted by `grep self._db.role_exists/grant_role returns nothing`. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all imports are intra-package (`pycopg.base`/`utils`/`queries`) or stdlib (`asyncio`/`subprocess`/`os`/`pathlib`/`csv`). No new dependency surface. |

Residual risk: LOW. Bodies move verbatim; all validators and list-form subprocess
calls travel unchanged; no new external input paths are introduced. Block on `high`
— none present.
</threat_model>

<verification>
- `uv run python -c "import pycopg.admin, pycopg.maint, pycopg.backup"` exits 0 (no circular imports — all three follow the timescale TYPE_CHECKING pattern).
- The three introspect verify commands (one per task) confirm 11 / 6 / 4 public methods per class with sync↔async name parity.
- `grep -nE 'self\._db\.(role_exists|grant_role)\(' pycopg/admin.py` returns nothing (D-02 guard).
- `uv run pytest tests/test_sql_injection.py tests/test_subprocess_env.py -x -q -o addopts=""` exits 0 (validators + subprocess pattern preserved).
</verification>

<success_criteria>
- 3 new module files exist: `pycopg/admin.py`, `pycopg/maint.py`, `pycopg/backup.py`.
- AdminAccessor/AsyncAdminAccessor = 11 public methods each; MaintAccessor/AsyncMaintAccessor = 6 each; BackupAccessor/AsyncBackupAccessor = 4 public + `_psql_restore`.
- All bodies moved verbatim (D-06) with only `self.`→`self._db.` rewrites; the only Bucket-A rewrites are the 4 create_role sibling calls through `self._db.admin.*`.
- No flat-alias sibling calls (`self._db.role_exists`/`self._db.grant_role`) anywhere.
- `_psql_restore` moved with `pg_restore`; `asyncio` imported at backup.py module level; no `shell=True`.
- `tests/test_sql_injection.py` and `tests/test_subprocess_env.py` stay green.
</success_criteria>

<output>
Create `.planning/phases/22-admin-maint-backup-accessors/22-01-SUMMARY.md` when done.
</output>
