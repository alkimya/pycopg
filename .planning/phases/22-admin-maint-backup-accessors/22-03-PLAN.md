---
phase: 22-admin-maint-backup-accessors
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - tests/test_admin_aliases.py
  - tests/test_maint_aliases.py
  - tests/test_backup_aliases.py
  - tests/test_parity.py
  - tests/test_database.py
  - tests/test_async_database.py
  - tests/test_database_integration.py
  - tests/test_sql_injection.py
  - tests/test_subprocess_env.py
autonomous: true
requirements: [ADM-01, MNT-01, BKP-01]

must_haves:
  truths:
    - "Each of the 21 flat aliases warns (correct message + stacklevel=2) and delegates, proven DB-free"
    - "test_parity passes with AdminAccessor/MaintAccessor/BackupAccessor pairs registered"
    - "Existing call-sites use db.admin.*/db.maint.*/db.backup.* (main suite emits no internal DeprecationWarning)"
    - "Full suite is green with coverage >= 94%"
    - "The alias tests isolate the warning so -W error::DeprecationWarning does not break"
  artifacts:
    - path: "tests/test_admin_aliases.py"
      provides: "11 sync + 11 async admin alias tests"
      contains: "AdminAccessor"
    - path: "tests/test_maint_aliases.py"
      provides: "6 sync + 6 async maint alias tests"
      contains: "MaintAccessor"
    - path: "tests/test_backup_aliases.py"
      provides: "4 sync + 4 async backup alias tests"
      contains: "BackupAccessor"
    - path: "tests/test_parity.py"
      provides: "3 appended ACCESSOR_PAIRS entries"
      contains: "AdminAccessor, AsyncAdminAccessor"
  key_links:
    - from: "tests/test_*_aliases.py"
      to: "db._admin / db._maint / db._backup"
      via: "MagicMock injection into cache field"
      pattern: "db\\._(admin|maint|backup) = mock"
    - from: "tests/test_parity.py::ACCESSOR_PAIRS"
      to: "test_accessor_parity"
      via: "parametrized registry"
      pattern: "\\(AdminAccessor, AsyncAdminAccessor\\)"
---

<objective>
Close the phase: author the three DB-free alias test modules, append the three
pairs to the `ACCESSOR_PAIRS` parity registry, migrate the existing call-sites to
the new accessor paths, and hold all gates (full suite green, coverage ≥ 94%,
`-W error::DeprecationWarning` not broken). This wave proves ADM-01 / MNT-01 /
BKP-01: every flat alias warns + delegates, and every new accessor has sync↔async
parity.

Purpose: The alias tests both prove the warn+delegate behavior and supply the
coverage that keeps the `--cov-fail-under=94` ratchet green for the 21 thin stubs.
Migrating call-sites keeps the main suite quiet (no internal DeprecationWarning
noise). Replicate the LOCKED Phase 21 test isolation pattern exactly (RESEARCH
"Gate Configuration" + `test_timescale_aliases.py`).

Output: 3 new test files, edits to `test_parity.py`, and call-site migrations
across 5 existing test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-admin-maint-backup-accessors/22-CONTEXT.md
@.planning/phases/22-admin-maint-backup-accessors/22-RESEARCH.md
@.planning/phases/22-admin-maint-backup-accessors/22-VALIDATION.md
@tests/test_timescale_aliases.py
@tests/test_parity.py
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
See 22-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase symbol
list. This wave produces: `tests/test_admin_aliases.py`,
`tests/test_maint_aliases.py`, `tests/test_backup_aliases.py`, and the 3 new
`ACCESSOR_PAIRS` entries `(AdminAccessor, AsyncAdminAccessor)`,
`(MaintAccessor, AsyncMaintAccessor)`, `(BackupAccessor, AsyncBackupAccessor)`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the 3 DB-free alias test modules + append 3 ACCESSOR_PAIRS</name>
  <files>tests/test_admin_aliases.py, tests/test_maint_aliases.py, tests/test_backup_aliases.py, tests/test_parity.py</files>
  <read_first>
    - tests/test_timescale_aliases.py (the EXACT template: `_SYNC_ALIAS_ARGS`/`_ASYNC_ALIAS_ARGS` dicts; the parametrized sync test injecting `db._timescale = MagicMock(spec=TimescaleAccessor)`; the async test using `AsyncMock`; the `warnings.catch_warnings(record=True)` + `simplefilter("always")` + filter-on-message isolation; the stacklevel=2 basename assertions; the `assert_called_once_with(*args)` delegation check)
    - tests/test_parity.py lines 1-22 (imports + `ACCESSOR_PAIRS` registry — append only)
    - pycopg/admin.py, pycopg/maint.py, pycopg/backup.py (Wave 1 — confirm each public method's signature to build minimal valid sample-arg tuples; for backup use no-live-DB-safe args since the accessor is mocked)
    - 22-RESEARCH.md "Test Pattern" and "Gate Configuration → Phase 21 -W error gotcha" (the isolation MUST be per-test catch_warnings + message filter; do NOT add a global `filterwarnings` that converts all DeprecationWarnings to errors)
  </read_first>
  <action>
    Create three test modules, each a faithful copy of `test_timescale_aliases.py`
    retargeted to its accessor (Claude's discretion: 3 separate files mirrors
    Phase 21 most closely — use 3 files):

    - `tests/test_admin_aliases.py` — `class TestAdminAliases`; `_SYNC_ALIAS_ARGS`
      / `_ASYNC_ALIAS_ARGS` covering all 11 admin methods with minimal valid args
      (e.g. `create_role: ("r",)`, `role_exists: ("r",)`, `grant: (...)` matching
      the moved signature); inject `db._admin = MagicMock(spec=AdminAccessor)` (sync)
      and `MagicMock(spec=AsyncAdminAccessor)` with each method set to `AsyncMock()`
      (async); assert warning category is `DeprecationWarning`, message contains
      `f"db.admin.{name}"` and `"v0.7.0"`, basename is the test file (not
      aliases.py / database.py), and `assert_called_once_with(*args)`.
    - `tests/test_maint_aliases.py` — `class TestMaintAliases`; 6 methods (`size`,
      `table_size`, `table_sizes`, `vacuum`, `analyze`, `explain`); inject
      `db._maint`; message contains `f"db.maint.{name}"`.
    - `tests/test_backup_aliases.py` — `class TestBackupAliases`; 4 methods
      (`pg_dump`, `pg_restore`, `copy_to_csv`, `copy_from_csv`); inject
      `db._backup`; message contains `f"db.backup.{name}"`. Do NOT test
      `_psql_restore` (private; no flat alias).

    Use the EXACT warning-isolation pattern from the template (per-test
    `catch_warnings(record=True)` + `simplefilter("always")` + filter the recorded
    list to the alias-specific message). Do NOT introduce any global
    `filterwarnings` config.

    Then edit `tests/test_parity.py`: add the three imports
    `from pycopg.admin import AdminAccessor, AsyncAdminAccessor`,
    `from pycopg.maint import MaintAccessor, AsyncMaintAccessor`,
    `from pycopg.backup import BackupAccessor, AsyncBackupAccessor`, and APPEND
    `(AdminAccessor, AsyncAdminAccessor)`, `(MaintAccessor, AsyncMaintAccessor)`,
    `(BackupAccessor, AsyncBackupAccessor)` to `ACCESSOR_PAIRS`. The
    `test_accessor_parity` parametrized test runs them automatically — no other
    change.
  </action>
  <verify>
    <automated>uv run pytest tests/test_admin_aliases.py tests/test_maint_aliases.py tests/test_backup_aliases.py tests/test_parity.py::test_accessor_parity -x -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `grep -c "class TestAdminAliases" tests/test_admin_aliases.py` == 1; `TestMaintAliases` and `TestBackupAliases` likewise == 1 in their files.
    - Coverage of all 21 aliases: `_SYNC_ALIAS_ARGS` in test_admin_aliases.py has 11 keys, test_maint_aliases.py has 6, test_backup_aliases.py has 4 (verifiable by `uv run python -c "import tests.test_admin_aliases as m; assert len(m._SYNC_ALIAS_ARGS)==11"` etc., or by counting parametrized cases collected).
    - Registry: `grep -c "(AdminAccessor, AsyncAdminAccessor)" tests/test_parity.py` == 1; same for the maint and backup pairs.
    - No global filterwarnings introduced: `grep -c "filterwarnings" tests/test_admin_aliases.py tests/test_maint_aliases.py tests/test_backup_aliases.py` == 0.
    - Test command: the verify command above exits 0 (all alias + parity tests pass DB-free).
  </acceptance_criteria>
  <done>Three alias test modules exist (11+6+4 sync, 11+6+4 async), each proving warn+stacklevel+delegate via injected mocks; ACCESSOR_PAIRS has the 3 new pairs and test_accessor_parity passes for all 5 pairs.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate existing call-sites to db.admin.*/db.maint.*/db.backup.* across 5 test files</name>
  <files>tests/test_database.py, tests/test_async_database.py, tests/test_database_integration.py, tests/test_sql_injection.py, tests/test_subprocess_env.py</files>
  <read_first>
    - 22-RESEARCH.md "Gate Surface — Migration Call-Sites" (the per-file flat-name reference inventory: test_database.py ~98 lines, test_async_database.py ~164 lines (largest), test_database_integration.py ~15, test_sql_injection.py ~18, test_subprocess_env.py ~17)
    - The exact list of 21 flat method names to migrate: admin (create_role, drop_role, role_exists, list_roles, alter_role, grant_role, revoke_role, grant, revoke, list_role_members, list_role_grants), maint (size, table_size, table_sizes, vacuum, analyze, explain), backup (pg_dump, pg_restore, copy_to_csv, copy_from_csv)
    - tests/test_timescale_aliases.py (confirm: alias-warn tests deliberately call the FLAT name — migration must NOT touch the alias test files)
  </read_first>
  <action>
    In each of the 5 test files, rewrite call-sites that invoke a flat method on a
    Database/AsyncDatabase instance to the accessor path:
    `db.create_role(...)` → `db.admin.create_role(...)`, `db.vacuum(...)` →
    `db.maint.vacuum(...)`, `db.pg_dump(...)` → `db.backup.pg_dump(...)`, and so on
    for all 21 names (use the accessor→method mapping above). Apply the same to
    async instances (`adb.size(...)` → `adb.maint.size(...)` etc.).

    Scope discipline:
    - Migrate ONLY genuine method-call sites on a db instance. Do NOT rewrite
      string literals, comments, or unrelated identifiers that happen to share a
      name (e.g. a local variable `size`, a column named `analyze`). Verify each
      edit is a `<db>.<method>(` call.
    - Do NOT touch `tests/test_timescale_aliases.py` / the 3 new `*_aliases.py`
      files — those intentionally exercise the deprecated FLAT path and must keep
      calling the flat names.
    - Backup methods (`pg_dump`/`pg_restore`/`copy_to_csv`/`copy_from_csv`) in
      test_subprocess_env.py / test_database_integration.py: migrate the call to
      `db.backup.*`; the subprocess behavior is unchanged (bodies moved verbatim).

    The goal: after migration, running the FULL suite emits no pycopg-internal
    DeprecationWarning from these files (only the dedicated alias tests trigger the
    warnings, and they isolate them).
  </action>
  <verify>
    <automated>uv run pytest tests/test_database.py tests/test_async_database.py tests/test_sql_injection.py tests/test_subprocess_env.py -q -o addopts="" -W error::DeprecationWarning -k "not alias"</automated>
  </verify>
  <acceptance_criteria>
    - No residual flat admin/maint/backup calls in the migrated files: for each of the 21 names, `grep -nE '(^|[^.a-zA-Z_])(db|adb|database|async_db)[a-zA-Z_0-9]*\.(create_role|drop_role|role_exists|list_roles|alter_role|grant_role|revoke_role|grant|revoke|list_role_members|list_role_grants|size|table_size|table_sizes|vacuum|analyze|explain|pg_dump|pg_restore|copy_to_csv|copy_from_csv)\(' tests/test_database.py` (and the other 4 files) shows the call is prefixed by `.admin.`/`.maint.`/`.backup.` — i.e. no bare `db.create_role(` style flat call remains. (Spot-check a sample of the 21 names per file.)
    - Alias tests untouched: `git diff --name-only` does NOT include `tests/test_timescale_aliases.py`; the 3 new `*_aliases.py` still call flat names.
    - Behavior under -W error: the verify command exits 0 (migrated suites run with `-W error::DeprecationWarning` and `-k "not alias"` without any internal pycopg DeprecationWarning being raised as an error).
  </acceptance_criteria>
  <done>All 21 flat call-sites across the 5 test files are migrated to db.admin.*/db.maint.*/db.backup.*; the alias test files are untouched; the migrated suites run clean under -W error::DeprecationWarning (excluding the alias tests).</done>
</task>

<task type="auto">
  <name>Task 3: Hold the gates — full suite green, coverage >= 94%, -W error::DeprecationWarning clean</name>
  <files>tests/test_admin_aliases.py, tests/test_maint_aliases.py, tests/test_backup_aliases.py</files>
  <read_first>
    - 22-RESEARCH.md "Gate Configuration" (`--cov-fail-under=94`; interrogate ≥ 95; the Phase 21 `-W error` isolation pattern) and "Common Pitfalls" (Pitfall 1: internal alias warning leaking)
    - 22-VALIDATION.md "Sampling Rate" + "Per-Task Verification Map" (full suite after each wave; -W error gate before verify)
    - .planning/STATE.md Blockers/Concerns (2 pre-existing flaky DB tests: test_async_transaction_fix, test_create_spatial_index_name_parameter — UndefinedTable fixture-isolation, NOT v0.6.0 regressions; use `-o addopts=""` for targeted runs)
  </read_first>
  <action>
    Run the full gate set and resolve any failures that this phase introduced (do
    NOT chase the 2 documented pre-existing flaky DB tests):

    1. Full suite + coverage gate: `uv run pytest`. Must be green with coverage
       ≥ 94%. If a thin stub or accessor line is uncovered, ADD the missing case to
       the relevant `*_aliases.py` module (the DB-free alias tests are the coverage
       source for the stubs; the moved bodies are covered by the migrated suites).
    2. `-W error::DeprecationWarning` gate: confirm no pycopg-internal
       DeprecationWarning leaks. The classic leak is Pitfall 1 — a moved body
       calling a flat alias. If the gate trips, trace it to the offending
       `self._db.<flat>` call in admin.py/maint.py/backup.py and fix it to
       `self._db.<accessor>.<flat>` (this should already be correct from Wave 1;
       this task only verifies).
    3. interrogate ≥ 95: every accessor method kept its numpydoc docstring; every
       stub has the one-line "Deprecated: use ..." docstring; every new test has a
       docstring. If interrogate drops below 95, add the missing docstrings.

    Do NOT lower `--cov-fail-under`, do NOT add a global `filterwarnings`, do NOT
    `# noqa`/skip to pass. The only acceptable edits in this task are adding missing
    test cases or docstrings.
  </action>
  <verify>
    <automated>uv run pytest && uv run pytest tests/test_admin_aliases.py tests/test_maint_aliases.py tests/test_backup_aliases.py tests/test_parity.py -q -o addopts="" -W error::DeprecationWarning</automated>
  </verify>
  <acceptance_criteria>
    - Full suite: `uv run pytest` exits 0 with coverage ≥ 94% (the run prints `Required test coverage of 94% reached` or does not fail the `--cov-fail-under=94` gate). The 2 documented pre-existing flaky tests are the ONLY tolerated failures and, if they appear, are run separately with `-o addopts=""` to confirm they are the known UndefinedTable fixture-isolation issue and not new regressions.
    - -W error gate: `uv run pytest tests/test_admin_aliases.py tests/test_maint_aliases.py tests/test_backup_aliases.py tests/test_parity.py -q -o addopts="" -W error::DeprecationWarning` exits 0 (no internal pycopg DeprecationWarning raised as error; alias warnings are isolated per Phase 21 pattern).
    - interrogate: `uv run interrogate pycopg -f 95` exits 0 (≥ 95% docstring coverage).
    - Lint/format: `uv run ruff check pycopg tests` exits 0; `uv run black --check pycopg tests` exits 0.
    - No gate weakening: `grep -c 'cov-fail-under=94' pyproject.toml` still == 1 (unchanged); `grep -c 'filterwarnings' pyproject.toml` unchanged from before this phase.
  </acceptance_criteria>
  <done>Full suite green with coverage ≥ 94%; the -W error::DeprecationWarning gate passes with the alias tests isolated; interrogate ≥ 95; ruff + black clean; no gate threshold was lowered and no global filterwarnings was added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → accessor (mocked) | alias tests inject MagicMock accessors; no live DB / no real input crosses |
| CI gate → suite | coverage + -W error gates guard against regression and internal deprecated-path leaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | Tampering (regression of validators) | migrated test_sql_injection.py call-sites | mitigate | Call-site migration must preserve the injection assertions verbatim — only the call path changes (`db.x` → `db.admin.x`). `tests/test_sql_injection.py` must stay green post-migration (Task 2 + Task 3 verify). |
| T-22-07 | Repudiation (silent internal deprecated call) | moved accessor bodies | mitigate | The `-W error::DeprecationWarning` gate (Task 3) converts any leaked internal pycopg DeprecationWarning into a hard failure, catching a missed Bucket-A rewrite (Pitfall 1). |
| T-22-08 | Tampering (subprocess regression) | migrated test_subprocess_env.py call-sites | mitigate | Backup behavior is unchanged (verbatim move); `tests/test_subprocess_env.py` stays green to confirm the list-form subprocess/env handling survived the move + migration. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pytest/pytest-cov/pytest-asyncio/interrogate already present (Phase 21 infra). No new dependency surface. |

Residual risk: LOW. This wave is tests + call-path migration only; the security
posture is enforced by keeping the pre-existing security suites green and adding
the `-W error` gate. Block on `high` — none present.
</threat_model>

<verification>
- `uv run pytest tests/test_admin_aliases.py tests/test_maint_aliases.py tests/test_backup_aliases.py tests/test_parity.py -x -q -o addopts=""` exits 0 (warn+delegate + parity proven).
- `uv run pytest` exits 0 with coverage ≥ 94% (the 21 thin stubs are exercised by the alias tests; moved bodies by the migrated suites).
- `uv run pytest ... -W error::DeprecationWarning` exits 0 on the alias + parity modules (no internal deprecated-path leak; Phase 21 isolation replicated).
- `tests/test_sql_injection.py` and `tests/test_subprocess_env.py` stay green after call-site migration.
- `uv run interrogate pycopg -f 95`, `uv run ruff check pycopg tests`, `uv run black --check pycopg tests` all exit 0.
</verification>

<success_criteria>
- 3 new alias test modules (11+6+4 sync, 11+6+4 async) prove each flat alias warns (correct message + stacklevel=2) and delegates.
- `test_parity` passes with all 5 ACCESSOR_PAIRS (timescale, etl, admin, maint, backup) registered.
- All 21 flat call-sites across the 5 existing test files migrated to accessor paths; alias test files left calling flat names.
- Full suite green, coverage ≥ 94%, interrogate ≥ 95, ruff + black clean.
- `-W error::DeprecationWarning` does not break — alias warnings are isolated per the Phase 21 pattern; no internal pycopg DeprecationWarning leaks.
- No gate threshold lowered; no global `filterwarnings` added.
</success_criteria>

<output>
Create `.planning/phases/22-admin-maint-backup-accessors/22-03-SUMMARY.md` when done.
</output>
