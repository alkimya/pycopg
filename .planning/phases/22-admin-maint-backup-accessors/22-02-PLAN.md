---
phase: 22-admin-maint-backup-accessors
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - pycopg/database.py
  - pycopg/async_database.py
  - pycopg/__init__.py
autonomous: true
requirements: [ADM-01, MNT-01, BKP-01]

must_haves:
  truths:
    - "db.admin / db.maint / db.backup resolve to lazily-cached accessor instances (sync)"
    - "async_db.admin / async_db.maint / async_db.backup resolve to lazily-cached accessor instances (async)"
    - "Each of the 21 flat db.* names is now a thin @deprecated_alias stub delegating to its accessor"
    - "Each of the 21 flat async_db.* names is an async @deprecated_alias stub"
    - "The 6 new accessor classes are exported via pycopg.__init__ __all__"
    - "_psql_restore is NOT exposed as a flat stub (private companion)"
  artifacts:
    - path: "pycopg/database.py"
      provides: "_admin/_maint/_backup cache fields, 3 lazy properties, 21 deprecated_alias stubs"
      contains: "def admin(self)"
    - path: "pycopg/async_database.py"
      provides: "async _admin/_maint/_backup cache fields, 3 lazy properties, 21 async deprecated_alias stubs"
      contains: "def admin(self)"
    - path: "pycopg/__init__.py"
      provides: "exports for the 6 new accessor classes"
      contains: "AdminAccessor"
  key_links:
    - from: "pycopg/database.py::Database.admin (property)"
      to: "pycopg.admin.AdminAccessor"
      via: "lazy construct + cache on self._admin"
      pattern: "AdminAccessor\\(self\\)"
    - from: "pycopg/database.py flat stubs"
      to: "db.<accessor>.<method>"
      via: "@deprecated_alias decorator delegation"
      pattern: "@deprecated_alias\\(\"(admin|maint|backup)\\."
---

<objective>
Wire the three Wave 1 accessors into `Database` and `AsyncDatabase`, and replace
the 21 flat method definitions with thin `@deprecated_alias` stubs. This is the
shared-surface wave (D-05): the `__init__` cache fields, the lazy properties, the
stub replacements, and the `__init__.py` exports all live here so the three
accessors do not collide on the same files in parallel.

Purpose: After this wave the public API is `db.admin.*` / `db.maint.*` /
`db.backup.*` (real implementations from Wave 1), while every legacy flat name
(`db.create_role`, `db.vacuum`, `db.pg_dump`, …) still works and emits a
`DeprecationWarning` naming the new path. Replicate the LOCKED timescale wiring
verbatim (RESEARCH "Wiring Touch-Points") — three times, one per accessor.

Output: edits to `database.py`, `async_database.py`, `__init__.py`. No test
changes yet (Wave 3).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-admin-maint-backup-accessors/22-CONTEXT.md
@.planning/phases/22-admin-maint-backup-accessors/22-RESEARCH.md
@pycopg/aliases.py
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
See 22-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase symbol
list. This wave specifically produces: the `_admin`/`_maint`/`_backup` cache
fields and `admin`/`maint`/`backup` lazy properties on Database/AsyncDatabase,
the 21 `@deprecated_alias` stub functions (replacing the flat method defs), and
the 6 `__init__.py` `__all__` export entries.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire sync Database (cache fields, 3 lazy properties, 11+6+4 deprecated_alias stubs)</name>
  <files>pycopg/database.py</files>
  <read_first>
    - pycopg/database.py lines 54-60 (TYPE_CHECKING block) — current imports ETLAccessor/SpatialAccessor/TimescaleAccessor
    - pycopg/database.py lines 84-88 (`__init__` cache fields, `self._timescale` at 88)
    - pycopg/database.py lines 275-292 (the `timescale` lazy `@property` — the exact template to replicate 3×)
    - pycopg/database.py lines 1669-1691 (the 6 existing timescale `@deprecated_alias` stubs — the exact stub template)
    - pycopg/database.py lines 1697-2227 (the 6 maint + 11 admin method bodies being REPLACED by stubs — these bodies were moved to maint.py/admin.py in Wave 1)
    - pycopg/database.py lines 2228-2540 (the 4 backup public bodies + `_psql_restore` being REPLACED — _psql_restore is DELETED here, not stubbed, because it is private and now lives only on BackupAccessor)
    - pycopg/aliases.py (confirm the `@deprecated_alias("accessor.method")` contract)
    - 22-RESEARCH.md "Wiring Touch-Points → database.py wiring (W2)" and "`_psql_restore` Private Companion — Special Note"
  </read_first>
  <action>
    Three edits to `pycopg/database.py`, replicating the timescale wiring:

    (1) TYPE_CHECKING block (after line 60): add
    `from pycopg.admin import AdminAccessor`,
    `from pycopg.maint import MaintAccessor`,
    `from pycopg.backup import BackupAccessor`.

    (2) `__init__` cache fields (after line 88, after `self._timescale`): add
    `self._admin: AdminAccessor | None = None`,
    `self._maint: MaintAccessor | None = None`,
    `self._backup: BackupAccessor | None = None`.

    (3) Lazy properties (after the `timescale` property block, ~line 292): add 3
    `@property` blocks `admin`, `maint`, `backup`, each EXACTLY mirroring the
    timescale property — a numpydoc docstring, an `if self._<name> is None:` guard
    that does `from pycopg.<name> import <Name>Accessor` then
    `self._<name> = <Name>Accessor(self)`, and `return self._<name>`.

    (4) Replace the 21 flat method bodies with `@deprecated_alias` stubs, in place,
    matching the timescale stub form exactly:
    ```
    @deprecated_alias("admin.create_role")
    def create_role(self, *args, **kwargs):
        """Deprecated: use ``db.admin.create_role`` instead."""
    ```
    Do this for all 11 admin methods (`admin.<method>`), all 6 maint methods
    (`maint.<method>`), all 4 backup public methods (`backup.<method>`). DELETE the
    private `_psql_restore` body entirely — it is NOT stubbed (it is private and
    now lives only on BackupAccessor; the backup.pg_restore accessor method calls
    its own `self._psql_restore`).

    Preserve the existing section-header comments (e.g. "SIZE & STATS", "ROLES &
    PERMISSIONS", "BACKUP & RESTORE") so the file stays navigable.
  </action>
  <verify>
    <automated>uv run python -c "
from pycopg import Database
from pycopg.admin import AdminAccessor
from pycopg.maint import MaintAccessor
from pycopg.backup import BackupAccessor
import inspect, warnings
src = inspect.getsource(Database)
for acc, methods in {'admin':['create_role','drop_role','role_exists','list_roles','alter_role','grant_role','revoke_role','grant','revoke','list_role_members','list_role_grants'],'maint':['size','table_size','table_sizes','vacuum','analyze','explain'],'backup':['pg_dump','pg_restore','copy_to_csv','copy_from_csv']}.items():
    for m in methods:
        assert f'@deprecated_alias(\"{acc}.{m}\")' in src, f'missing stub {acc}.{m}'
assert 'def admin(self)' in src and 'def maint(self)' in src and 'def backup(self)' in src
assert '_psql_restore' not in src, 'private companion must NOT remain on Database'
print('sync wiring OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Properties: `grep -c '    def admin(self)' pycopg/database.py` returns 1; same for `maint`/`backup`. Each property body contains `AdminAccessor(self)` / `MaintAccessor(self)` / `BackupAccessor(self)` respectively.
    - Cache fields: `grep -c 'self\._admin: AdminAccessor | None = None' pycopg/database.py` returns 1; same for `_maint`/`_backup`.
    - Stubs: `grep -c '@deprecated_alias("admin\.' pycopg/database.py` returns 11; `grep -c '@deprecated_alias("maint\.' pycopg/database.py` returns 6; `grep -c '@deprecated_alias("backup\.' pycopg/database.py` returns 4 (= 21 total, validate against 21 NOT 22).
    - Private companion removed: `grep -c '_psql_restore' pycopg/database.py` returns 0 (deleted, not stubbed).
    - Behavior: the introspect verify command above exits 0.
    - Import safety: `uv run python -c "from pycopg import Database; d=object.__new__(Database)"` style not needed — `uv run python -c "import pycopg.database"` exits 0.
  </acceptance_criteria>
  <done>Database has _admin/_maint/_backup cache fields, admin/maint/backup lazy properties mirroring timescale, and exactly 21 @deprecated_alias stubs (11 admin + 6 maint + 4 backup) replacing the moved bodies; _psql_restore removed from Database.</done>
</task>

<task type="auto">
  <name>Task 2: Wire async AsyncDatabase (cache fields, 3 lazy properties, 21 async deprecated_alias stubs)</name>
  <files>pycopg/async_database.py</files>
  <read_first>
    - pycopg/async_database.py lines 53-60 (TYPE_CHECKING block)
    - pycopg/async_database.py lines 83-87 (`__init__` cache fields, `self._timescale` at 87)
    - pycopg/async_database.py lines 135-152 (the async `timescale` lazy `@property` template)
    - pycopg/async_database.py lines 1245-1267 (the 6 async timescale `@deprecated_alias` stubs — note `async def`)
    - pycopg/async_database.py admin/maint/backup bodies being replaced (admin 1273-1651; maint 1657-1723 + 2168-2247; backup 2253-2597 incl. _psql_restore at 2406)
    - 22-RESEARCH.md "Wiring Touch-Points → async_database.py wiring (W2)"
  </read_first>
  <action>
    Mirror Task 1 in `pycopg/async_database.py`, using the async templates:

    (1) TYPE_CHECKING block (after line 60): add
    `from pycopg.admin import AsyncAdminAccessor`,
    `from pycopg.maint import AsyncMaintAccessor`,
    `from pycopg.backup import AsyncBackupAccessor`.

    (2) `__init__` cache fields (after line 87): add
    `self._admin: AsyncAdminAccessor | None = None`,
    `self._maint: AsyncMaintAccessor | None = None`,
    `self._backup: AsyncBackupAccessor | None = None`.

    (3) Lazy properties (after the async `timescale` property, ~line 152): add 3
    `@property` blocks `admin`/`maint`/`backup`, each mirroring the async timescale
    property: `if self._<name> is None: from pycopg.<name> import Async<Name>Accessor;
    self._<name> = Async<Name>Accessor(self)`; `return self._<name>`. (Properties
    are plain `@property` — the timescale async property is not `async def`;
    replicate exactly.)

    (4) Replace the 21 flat async method bodies with async stubs in the timescale
    async form:
    ```
    @deprecated_alias("admin.create_role")
    async def create_role(self, *args, **kwargs):
        """Deprecated: use ``async_db.admin.create_role`` instead."""
    ```
    All 11 admin + 6 maint + 4 backup, using `async def` and the `async_db.<...>`
    docstring wording. The `@deprecated_alias` decorator handles the `await`
    internally (aliases.py async branch). DELETE the async `_psql_restore` body
    entirely (private; lives only on AsyncBackupAccessor).

    Preserve existing section-header comments.
  </action>
  <verify>
    <automated>uv run python -c "
from pycopg import AsyncDatabase
import inspect
src = inspect.getsource(AsyncDatabase)
for acc, methods in {'admin':['create_role','drop_role','role_exists','list_roles','alter_role','grant_role','revoke_role','grant','revoke','list_role_members','list_role_grants'],'maint':['size','table_size','table_sizes','vacuum','analyze','explain'],'backup':['pg_dump','pg_restore','copy_to_csv','copy_from_csv']}.items():
    for m in methods:
        assert f'@deprecated_alias(\"{acc}.{m}\")' in src, f'missing async stub {acc}.{m}'
assert 'def admin(self)' in src and 'def maint(self)' in src and 'def backup(self)' in src
assert '_psql_restore' not in src, 'private companion must NOT remain on AsyncDatabase'
print('async wiring OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Properties: `grep -c '    def admin(self)' pycopg/async_database.py` returns 1; same for `maint`/`backup`; each body references `AsyncAdminAccessor(self)` / `AsyncMaintAccessor(self)` / `AsyncBackupAccessor(self)`.
    - Cache fields: `grep -c 'self\._admin: AsyncAdminAccessor | None = None' pycopg/async_database.py` returns 1; same for `_maint`/`_backup`.
    - Stubs: `grep -c '@deprecated_alias("admin\.' pycopg/async_database.py` returns 11; `maint\.` returns 6; `backup\.` returns 4 (21 total). All 21 stubs use `async def`: `grep -c 'async def' near each stub` — verified by the introspect command.
    - Private companion removed: `grep -c '_psql_restore' pycopg/async_database.py` returns 0.
    - Behavior: the introspect verify exits 0; `uv run python -c "import pycopg.async_database"` exits 0.
  </acceptance_criteria>
  <done>AsyncDatabase has async _admin/_maint/_backup cache fields, admin/maint/backup lazy properties, and 21 async @deprecated_alias stubs (async def, async_db.* docstrings); async _psql_restore removed from AsyncDatabase.</done>
</task>

<task type="auto">
  <name>Task 3: Export the 6 new accessor classes from pycopg/__init__.py</name>
  <files>pycopg/__init__.py</files>
  <read_first>
    - pycopg/__init__.py (current imports + `__all__`; per RESEARCH lines 27-61 export TimescaleAccessor/AsyncTimescaleAccessor and the spatial/etl accessors — follow that exact form)
    - 22-RESEARCH.md "Wiring Touch-Points → pycopg/__init__.py wiring (W2)"
  </read_first>
  <action>
    Add 3 import lines alongside the existing accessor imports:
    `from pycopg.admin import AdminAccessor, AsyncAdminAccessor`,
    `from pycopg.maint import MaintAccessor, AsyncMaintAccessor`,
    `from pycopg.backup import BackupAccessor, AsyncBackupAccessor`.

    Add the 6 class names to `__all__` (group them under `# Admin`, `# Maint`,
    `# Backup` comments mirroring how Timescale/Spatial/ETL are grouped):
    `AdminAccessor`, `AsyncAdminAccessor`, `MaintAccessor`, `AsyncMaintAccessor`,
    `BackupAccessor`, `AsyncBackupAccessor`.

    (Formal README/Sphinx/CHANGELOG export hardening is Phase 24 — this is only the
    `__all__` entries the pattern requires.)
  </action>
  <verify>
    <automated>uv run python -c "import pycopg; [getattr(pycopg, n) for n in ['AdminAccessor','AsyncAdminAccessor','MaintAccessor','AsyncMaintAccessor','BackupAccessor','AsyncBackupAccessor']]; assert all(n in pycopg.__all__ for n in ['AdminAccessor','AsyncAdminAccessor','MaintAccessor','AsyncMaintAccessor','BackupAccessor','AsyncBackupAccessor']); print('exports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Imports: `grep -c 'from pycopg.admin import AdminAccessor, AsyncAdminAccessor' pycopg/__init__.py` returns 1; same for maint and backup lines.
    - __all__: the verify command exits 0 (all 6 names are both importable from `pycopg` and present in `pycopg.__all__`).
    - `uv run python -c "import pycopg"` exits 0 (no import error from the new lines).
  </acceptance_criteria>
  <done>pycopg/__init__.py imports and re-exports all 6 new accessor classes; each is reachable as pycopg.<Name> and listed in __all__.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → flat alias → accessor | the 21 deprecated stubs forward caller args to accessor methods unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering (arg passthrough) | 21 @deprecated_alias stubs | accept | Stubs are generic `(*args, **kwargs)` passthroughs to the accessor; no parsing/transformation occurs, so no new injection surface is created. The accessor methods retain all validators (covered by Plan 01 T-22-01). |
| T-22-05 | Information disclosure (leaked private API) | `_psql_restore` | mitigate | `_psql_restore` is DELETED from Database/AsyncDatabase (not stubbed) so the private helper is not re-exposed on the public surface. Acceptance criteria assert `_psql_restore` count == 0 in both files. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — wiring uses only intra-package imports already vetted in Plan 01. |

Residual risk: LOW. This wave adds no input parsing and no external dependency;
it only routes calls. Block on `high` — none present.
</threat_model>

<verification>
- `uv run python -c "import pycopg"` exits 0 (all three edited files import cleanly, no circular import regressions).
- Sync + async introspect verify commands confirm 21 stubs each (11 admin + 6 maint + 4 backup) and the 3 lazy properties on both classes.
- `_psql_restore` absent from both database.py and async_database.py.
- `uv run pytest tests/test_parity.py -x -q -o addopts=""` exits 0 (existing parity still holds; new pairs added in Wave 3).
</verification>

<success_criteria>
- `db.admin`/`db.maint`/`db.backup` and the async equivalents return lazily-cached accessor instances.
- Exactly 21 flat `@deprecated_alias` stubs on Database and 21 on AsyncDatabase (validate against 21, not 22), each naming the correct `<accessor>.<method>` target.
- `_psql_restore` removed from both Database and AsyncDatabase.
- The 6 accessor classes are exported from `pycopg.__init__` and present in `__all__`.
- `import pycopg` succeeds with no circular-import error.
</success_criteria>

<output>
Create `.planning/phases/22-admin-maint-backup-accessors/22-02-SUMMARY.md` when done.
</output>
