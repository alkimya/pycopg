---
phase: 21-infrastructure-timescale-accessor
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - pycopg/database.py
  - pycopg/async_database.py
  - pycopg/__init__.py
autonomous: true
requirements: [REORG-02, TS-01]
must_haves:
  truths:
    - "db.timescale returns a cached TimescaleAccessor (lazy, mirrors db.spatial/db.etl)"
    - "async_db.timescale returns a cached AsyncTimescaleAccessor"
    - "the old flat db.create_hypertable (and 5 others) still exist, warn with a DeprecationWarning, and delegate to db.timescale.*"
    - "async_db.create_hypertable etc. remain coroutine functions (iscoroutinefunction True) that warn + await-delegate"
    - "from pycopg import TimescaleAccessor, AsyncTimescaleAccessor succeeds"
  artifacts:
    - path: "pycopg/database.py"
      provides: "_timescale field + lazy timescale property + 6 deprecated stubs"
      contains: "def timescale("
    - path: "pycopg/async_database.py"
      provides: "_timescale field + lazy timescale property + 6 async deprecated stubs"
      contains: "def timescale("
    - path: "pycopg/__init__.py"
      provides: "TimescaleAccessor + AsyncTimescaleAccessor exports in __all__"
      contains: "TimescaleAccessor"
  key_links:
    - from: "Database.timescale property"
      to: "pycopg.timescale.TimescaleAccessor"
      via: "lazy import + cache on self._timescale"
      pattern: "self\\._timescale = TimescaleAccessor\\(self\\)"
    - from: "Database.create_hypertable stub"
      to: "deprecated_alias decorator"
      via: "@deprecated_alias(\"timescale.create_hypertable\")"
      pattern: "@deprecated_alias\\(\"timescale\\."
---

<objective>
Wire the foundation modules from plan 01 into the public surface (REORG-02, TS-01):

1. `Database` / `AsyncDatabase`: add the `_timescale` cache field + a lazy `timescale` property mirroring the existing `_spatial`/`_etl` pattern exactly.
2. REPLACE the 6 sync + 6 async timescale method bodies with thin `@deprecated_alias("timescale.<method>")` stubs (the real logic now lives in the plan-01 accessor — D-05/D-06).
3. `pycopg/__init__.py`: export `TimescaleAccessor` / `AsyncTimescaleAccessor` in `__all__` (export precedent; formal README/Sphinx is Phase 24).

Purpose: After this plan, `db.timescale.*` works AND the legacy flat `db.create_hypertable(...)` still works while emitting a `DeprecationWarning` — zero breaking change for existing callers.
Output: edited `pycopg/database.py`, `pycopg/async_database.py`, `pycopg/__init__.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21-infrastructure-timescale-accessor/21-RESEARCH.md
@.planning/phases/21-infrastructure-timescale-accessor/21-PATTERNS.md
@.planning/phases/21-infrastructure-timescale-accessor/21-01-SUMMARY.md
</context>

## Artifacts this phase produces (this plan delivers the wiring + export rows)

| Symbol / File | Plan |
|---------------|------|
| `deprecated_alias`, `TimescaleAccessor`, `AsyncTimescaleAccessor` | 01 (consumed here) |
| `timescale` lazy property + `_timescale` field on `Database` / `AsyncDatabase` | **02** |
| 6 + 6 deprecated flat stubs (`@deprecated_alias("timescale.<m>")`) | **02** |
| `TimescaleAccessor`/`AsyncTimescaleAccessor` in `pycopg/__init__.py __all__` | **02** |
| alias tests / parity registry / call-site migration | 03 |

<tasks>

<task type="auto">
  <name>Task 1: Wire timescale into Database (sync) — field, property, 6 deprecated stubs</name>
  <files>pycopg/database.py</files>
  <read_first>
    - `pycopg/database.py` lines 54-59 (`if TYPE_CHECKING:` block — add `from pycopg.timescale import TimescaleAccessor`), lines 85-86 (`self._spatial`/`self._etl` fields), lines 231-271 (`spatial` + `etl` lazy properties — the EXACT template), lines 1648-1885 (the 6 method bodies to DELETE and replace with stubs; block ends before `size` at 1886). Also the module-top `from pycopg.*` import group (~lines 30-50) — add `from pycopg.aliases import deprecated_alias`.
    - `.planning/phases/21-infrastructure-timescale-accessor/21-PATTERNS.md` lines 206-280 — the field/property/stub edit map with exact analog line anchors.
    - `.planning/phases/21-infrastructure-timescale-accessor/21-RESEARCH.md` § "Existing Code — Precise Edit Map" → `pycopg/database.py` edits 1-5; § "Code Examples" → the 6 concrete stub definitions (verbatim).
  </read_first>
  <action>
    Edit `pycopg/database.py`: (1) Add `from pycopg.aliases import deprecated_alias` to the module-top `from pycopg.*` import group. (2) In the `if TYPE_CHECKING:` block (lines 54-59) add `from pycopg.timescale import TimescaleAccessor`. (3) After `self._etl: ETLAccessor | None = None` (line 86) add `self._timescale: TimescaleAccessor | None = None`. (4) After the `etl` property (ends line 271) add a `timescale` property COPIED from the `etl` property shape: numpydoc docstring, `if self._timescale is None:` → lazy `from pycopg.timescale import TimescaleAccessor` → `self._timescale = TimescaleAccessor(self)` → `return self._timescale`. (5) DELETE the entire TIMESCALEDB block at lines 1648-1885 and replace with the 6 thin stubs from 21-RESEARCH.md "Code Examples" verbatim: each is `@deprecated_alias("timescale.<method>")` + `def <method>(self, *args, **kwargs):` + a one-line docstring `"""Deprecated: use ``db.timescale.<method>`` instead."""` (the one-liner keeps interrogate at 100% — Footgun 1, option a). The 6 methods: `create_hypertable`, `enable_compression`, `add_compression_policy`, `add_retention_policy`, `list_hypertables`, `hypertable_info`. No `pass` body needed (decorator replaces the function). Run `uv run black pycopg/database.py` + `uv run ruff check pycopg/database.py`.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg &amp;&amp; uv run python -c "import inspect; from pycopg import Database; from pycopg.timescale import TimescaleAccessor; assert isinstance(inspect.getattr_static(Database,'timescale'), property); src=inspect.getsource(Database.create_hypertable); assert 'create_hypertable' in Database.__dict__ and hasattr(Database,'create_hypertable')" &amp;&amp; uv run ruff check pycopg/database.py &amp;&amp; uv run interrogate pycopg/database.py --fail-under 95</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@deprecated_alias("timescale\.' pycopg/database.py` == 6 (one per method).
    - `grep -c 'def timescale(' pycopg/database.py` == 1; `grep -c 'self\._timescale = TimescaleAccessor(self)' pycopg/database.py` == 1.
    - The old inline TIMESCALEDB SQL is gone: `grep -c 'SELECT create_hypertable(' pycopg/database.py` == 0 (moved to `timescale.py`).
    - `uv run python -c "from pycopg import Database"` exits 0; no circular-import error.
    - `uv run ruff check pycopg/database.py` exits 0; `uv run interrogate pycopg/database.py --fail-under 95` exits 0.
  </acceptance_criteria>
  <done>`Database` has a lazy `timescale` property + `_timescale` field; the 6 flat methods are now `@deprecated_alias`-decorated stubs delegating to `db.timescale.*`; the inline timescale SQL is removed; imports + ruff + interrogate pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire timescale into AsyncDatabase (async) — field, property, 6 async deprecated stubs</name>
  <files>pycopg/async_database.py</files>
  <read_first>
    - `pycopg/async_database.py` lines 53-59 (`if TYPE_CHECKING:` block — add `from pycopg.timescale import AsyncTimescaleAccessor`), lines 84-85 (`self._spatial`/`self._etl` fields), lines 97-131 (`spatial` + `etl` lazy properties — template), lines 1224-1461 (the 6 async bodies to DELETE; block ends before `role_exists` at 1462). Also the module-top import group — add `from pycopg.aliases import deprecated_alias`.
    - `.planning/phases/21-infrastructure-timescale-accessor/21-PATTERNS.md` lines 284-333 — the async edit map (mirror of database.py) with `async def` stubs.
    - `.planning/phases/21-infrastructure-timescale-accessor/21-RESEARCH.md` § "Existing Code — Precise Edit Map" → `pycopg/async_database.py` edits 1-5.
    - `pycopg/database.py` (the just-edited sync version from Task 1) — mirror its structure exactly.
  </read_first>
  <action>
    Edit `pycopg/async_database.py` mirroring Task 1: (1) add `from pycopg.aliases import deprecated_alias` to module-top imports. (2) In `if TYPE_CHECKING:` (lines 53-59) add `from pycopg.timescale import AsyncTimescaleAccessor`. (3) After `self._etl: AsyncETLAccessor | None = None` (line 85) add `self._timescale: AsyncTimescaleAccessor | None = None`. (4) After the `etl` property (line 131) add an async `timescale` property copied from the async `etl` property shape: numpydoc docstring, `if self._timescale is None:` → lazy `from pycopg.timescale import AsyncTimescaleAccessor` → `self._timescale = AsyncTimescaleAccessor(self)` → `return self._timescale` (the property itself is a plain `def`, not `async def` — same as `etl`). (5) DELETE the async TIMESCALEDB block at lines 1224-1461 and replace with 6 stubs that are `@deprecated_alias("timescale.<method>")` + `async def <method>(self, *args, **kwargs):` + one-line docstring `"""Deprecated: use ``async_db.timescale.<method>`` instead."""`. CRITICAL: each stub MUST be `async def` so `inspect.iscoroutinefunction(fn)` is True inside the decorator, selecting the `async_wrapper` branch (Pitfall 1 / Footgun 3) — this is what keeps `test_parity` and `iscoroutinefunction` correct. Run `uv run black pycopg/async_database.py` + `uv run ruff check pycopg/async_database.py`.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg &amp;&amp; uv run python -c "import inspect; from pycopg import AsyncDatabase; from pycopg.timescale import AsyncTimescaleAccessor; assert isinstance(inspect.getattr_static(AsyncDatabase,'timescale'), property); assert inspect.iscoroutinefunction(AsyncDatabase.create_hypertable), 'stub must stay a coroutine function'; [inspect.iscoroutinefunction(getattr(AsyncDatabase,m)) or (_ for _ in ()).throw(AssertionError(m)) for m in ('enable_compression','add_compression_policy','add_retention_policy','list_hypertables','hypertable_info')]" &amp;&amp; uv run ruff check pycopg/async_database.py &amp;&amp; uv run interrogate pycopg/async_database.py --fail-under 95</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@deprecated_alias("timescale\.' pycopg/async_database.py` == 6; `grep -c 'async def create_hypertable' pycopg/async_database.py` ≥ 1.
    - `grep -c 'def timescale(' pycopg/async_database.py` == 1; `grep -c 'self\._timescale = AsyncTimescaleAccessor(self)' pycopg/async_database.py` == 1.
    - `inspect.iscoroutinefunction(AsyncDatabase.create_hypertable)` is True for all 6 (verify command asserts this) — proves the async branch + `async def` stub.
    - Old async inline SQL gone: `grep -c 'create_hypertable(' pycopg/async_database.py` does NOT include the moved `SELECT create_hypertable(` literal (that lives in `timescale.py`).
    - `uv run python -c "from pycopg import AsyncDatabase"` exits 0; ruff + interrogate (≥95) pass.
  </acceptance_criteria>
  <done>`AsyncDatabase` has a lazy `timescale` property + `_timescale` field; the 6 async flat methods are `async def` `@deprecated_alias` stubs that stay coroutine functions; inline async timescale SQL removed; imports + ruff + interrogate pass.</done>
</task>

<task type="auto">
  <name>Task 3: Export TimescaleAccessor + AsyncTimescaleAccessor from pycopg</name>
  <files>pycopg/__init__.py</files>
  <read_first>
    - `pycopg/__init__.py` lines 10, 26, 40-56 — the existing `ETLAccessor`/`SpatialAccessor` import lines and the `__all__` block (the exact export pattern to mirror).
    - `.planning/phases/21-infrastructure-timescale-accessor/21-PATTERNS.md` lines 337-367 — the `__init__.py` add-2-exports pattern.
  </read_first>
  <action>
    Edit `pycopg/__init__.py`: add `from pycopg.timescale import AsyncTimescaleAccessor, TimescaleAccessor` after the spatial import (line 26). In `__all__`, add a `# TimescaleDB` comment block with `"TimescaleAccessor"` and `"AsyncTimescaleAccessor"` after the ETL block (lines 54-55), matching the existing comment-grouped style. This is the export-precedent only (CONTEXT D-07 note); the formal README/Sphinx/CHANGELOG hardening is Phase 24 — do NOT touch docs here. Run `uv run ruff check pycopg/__init__.py`.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg &amp;&amp; uv run python -c "import pycopg; from pycopg import TimescaleAccessor, AsyncTimescaleAccessor; assert 'TimescaleAccessor' in pycopg.__all__ and 'AsyncTimescaleAccessor' in pycopg.__all__" &amp;&amp; uv run ruff check pycopg/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `from pycopg import TimescaleAccessor, AsyncTimescaleAccessor` exits 0.
    - `"TimescaleAccessor"` and `"AsyncTimescaleAccessor"` both present in `pycopg.__all__` (verify command asserts).
    - `grep -c 'from pycopg.timescale import' pycopg/__init__.py` == 1.
    - `uv run ruff check pycopg/__init__.py` exits 0; no README/Sphinx/CHANGELOG files touched (`git diff --name-only` shows only `pycopg/__init__.py` for this task).
  </acceptance_criteria>
  <done>Both accessor classes are importable from `pycopg` and listed in `__all__`; only the export landed (docs deferred to Phase 24).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → flat `db.create_hypertable(...)` | legacy call path now routes through the decorator + lazy accessor |
| `database.py` ↔ `timescale.py` | runtime lazy import inside the property avoids a circular import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering / DoS | circular import between `database.py` and `timescale.py` | mitigate | `timescale.py` imports `Database`/`AsyncDatabase` only under `TYPE_CHECKING`; the `timescale` property uses a function-local lazy import (mirrors `etl`). Acceptance: `from pycopg import Database, AsyncDatabase` imports clean. |
| T-21-02 | Tampering (SQL injection) | delegated timescale SQL | mitigate | Stubs carry NO SQL; all SQL + `validate_*` guards live in plan-01 `timescale.py`. The stub is a pure passthrough — no new injection surface. |
| T-21-01 | Tampering / Elevation | `@deprecated_alias("timescale.<m>")` literals | mitigate | All 6 target-path strings are hardcoded source literals; none caller-derived (no `eval`/`getattr` on user input). |
| T-21-SC | Tampering | pip installs | accept | No new packages this phase. |
</threat_model>

<verification>
- `uv run python -c "from pycopg import Database, AsyncDatabase, TimescaleAccessor, AsyncTimescaleAccessor"` exits 0 (no circular import).
- `inspect.iscoroutinefunction(AsyncDatabase.create_hypertable)` is True; `inspect.iscoroutinefunction(Database.create_hypertable)` is False.
- `uv run ruff check pycopg/database.py pycopg/async_database.py pycopg/__init__.py` exits 0.
- `uv run interrogate pycopg --fail-under 95` exits 0 (one-line stub docstrings keep the gate green — Footgun 1).
- `db.timescale` and `async_db.timescale` resolve to the accessor classes (full warn+delegate behavior is asserted in plan 03).
</verification>

<success_criteria>
- `db.timescale` / `async_db.timescale` are lazy cached accessor properties.
- The 6 sync + 6 async flat methods are deprecated stubs that warn + delegate; async stubs stay coroutine functions.
- `TimescaleAccessor`/`AsyncTimescaleAccessor` are exported in `__all__`.
- No circular import; ruff + interrogate green. Plan 03 can now write the alias/parity tests and migrate call-sites.
</success_criteria>

<output>
Create `.planning/phases/21-infrastructure-timescale-accessor/21-02-SUMMARY.md` when done
</output>
