---
phase: 21-infrastructure-timescale-accessor
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - tests/test_timescale_aliases.py
  - tests/test_parity.py
  - tests/test_database_integration.py
  - tests/test_async_database.py
  - tests/test_sql_injection.py
autonomous: true
requirements: [REORG-02, REORG-03, REORG-04, TS-01]
must_haves:
  truths:
    - "Each of the 6 flat aliases (sync + async) has a test asserting it warns (correct message + caller-anchored stacklevel) AND delegates to db.timescale.*"
    - "test_parity has a data-driven ACCESSOR_PAIRS registry seeded with (TimescaleAccessor, AsyncTimescaleAccessor) and a parametrized parity test"
    - "All 27 existing timescale call-sites are migrated to db.timescale.* so the main suite emits no DeprecationWarning noise"
    - "Full suite passes and pytest -W error::DeprecationWarning passes; coverage stays >=94%"
  artifacts:
    - path: "tests/test_timescale_aliases.py"
      provides: "TestTimescaleAliases: per-alias warn+delegate tests (sync + async)"
      contains: "class TestTimescaleAliases"
      min_lines: 80
    - path: "tests/test_parity.py"
      provides: "ACCESSOR_PAIRS registry + parametrized test_accessor_parity"
      contains: "ACCESSOR_PAIRS"
  key_links:
    - from: "tests/test_timescale_aliases.py"
      to: "db.create_hypertable (flat alias)"
      via: "pytest warns + mocked db.timescale accessor asserts delegation"
      pattern: "DeprecationWarning"
    - from: "tests/test_parity.py ACCESSOR_PAIRS"
      to: "(TimescaleAccessor, AsyncTimescaleAccessor)"
      via: "parametrized inspect.getmembers parity check"
      pattern: "ACCESSOR_PAIRS"
---

<objective>
Deliver the full test + coverage layer that proves Phase 21 (REORG-02, REORG-03, REORG-04, TS-01):

1. NEW `tests/test_timescale_aliases.py` (D-09): for each of the 6 flat aliases, assert it BOTH warns (correct message, caller-anchored `stacklevel`) AND delegates to `db.timescale.*`. Covers sync + async. This is the coverage that holds `--cov-fail-under=94` for the thin stubs.
2. `tests/test_parity.py` (D-10): build a data-driven `ACCESSOR_PAIRS` registry seeded with `(TimescaleAccessor, AsyncTimescaleAccessor)` feeding ONE parametrized `inspect.getmembers` parity test (both directions). Phases 22-23 just APPEND pairs.
3. Migrate the 27 existing timescale call-sites (D-08) to `db.timescale.*` across 3 test files so the main suite stays quiet (REORG-04).
4. Final gate: full suite green + `pytest -W error::DeprecationWarning` green + coverage ≥94%.

Purpose: Lock in the no-breaking-change contract, the parity guarantee, and the coverage ratchet — the four ROADMAP success criteria.
Output: 1 new test file + 4 edited test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21-infrastructure-timescale-accessor/21-RESEARCH.md
@.planning/phases/21-infrastructure-timescale-accessor/21-PATTERNS.md
@.planning/phases/21-infrastructure-timescale-accessor/21-VALIDATION.md
@.planning/phases/21-infrastructure-timescale-accessor/21-02-SUMMARY.md
</context>

## Artifacts this phase produces (this plan delivers the test rows)

| Symbol / File | Plan |
|---------------|------|
| `tests/test_timescale_aliases.py` — `TestTimescaleAliases` (D-09) | **03** |
| `ACCESSOR_PAIRS` registry + `test_accessor_parity` in `tests/test_parity.py` (D-10) | **03** |
| 27 migrated call-sites across 3 test files (D-08) | **03** |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_timescale_aliases.py — per-alias warn + delegate (D-09)</name>
  <files>tests/test_timescale_aliases.py</files>
  <read_first>
    - `.planning/phases/21-infrastructure-timescale-accessor/21-RESEARCH.md` § "Pattern 6: Dedicated alias test class" and § "Footgun 2" (the `warnings.catch_warnings(record=True)` + `w[0].filename` stacklevel assertion). NOTE: the research snippet uses `mocker` (pytest-mock) — pytest-mock is NOT a dependency here. Use stdlib `unittest.mock` instead.
    - `tests/test_async_database.py` lines 1-50 — the EXISTING repo mocking style: `from unittest.mock import MagicMock, AsyncMock, patch, PropertyMock`. Mirror it (NO `mocker`).
    - `tests/test_database_integration.py` lines 14-23 — the `db` fixture (real Database on `pycopg_test`); lines 840-885 — the `ts_db` fixture + timescale lifecycle test (delegation-result reference).
    - `pycopg/timescale.py` (plan 01) — `TimescaleAccessor`/`AsyncTimescaleAccessor` for `spec=` in mocks.
  </read_first>
  <behavior>
    - For each sync alias in {create_hypertable, enable_compression, add_compression_policy, add_retention_policy, list_hypertables, hypertable_info}: `db.<alias>(*sample_args)` emits exactly one `DeprecationWarning` whose message contains `db.timescale.<alias>` and `v0.7.0`, whose `filename` is THIS test file (not `aliases.py`/`database.py`), and which forwards the same args to a mocked `db.timescale.<alias>` returning the mock value.
    - For each async alias: `await async_db.<alias>(*sample_args)` does the same against a mocked `async_db.timescale.<alias>` (AsyncMock); message names `async_db.timescale.<alias>`.
  </behavior>
  <action>
    Create `tests/test_timescale_aliases.py`. Module docstring. Imports: `os`, `warnings`, `pytest`, `from unittest.mock import MagicMock, AsyncMock`, `from pycopg.timescale import TimescaleAccessor, AsyncTimescaleAccessor`. Patch the accessor WITHOUT pytest-mock by setting the cached field directly: `db._timescale = MagicMock(spec=TimescaleAccessor)` (the lazy property returns the cached field when non-None, so no real DB/extension needed); async uses `async_db._timescale = MagicMock(spec=AsyncTimescaleAccessor)` with the target method as an `AsyncMock`. Write `class TestTimescaleAliases` with one parametrized sync test over the 6 names and one parametrized async test over the 6 names. Each test: open `warnings.catch_warnings(record=True)` + `warnings.simplefilter("always")`, call `getattr(db, name)(*sample_args)`, then assert `len(w)==1`, `w[0].category is DeprecationWarning`, `f"db.timescale.{name}"` (or `async_db.timescale.{name}`) in `str(w[0].message)`, `"v0.7.0" in str(w[0].message)`, `"test_" in os.path.basename(w[0].filename)` AND `"aliases.py" not in w[0].filename` AND `"database.py" not in w[0].filename` (stacklevel proof — Footgun 2), and `getattr(db._timescale, name).assert_called_once_with(*sample_args)`. Mirror for async with `await` + `AsyncMock`. Pick minimal valid sample args per method (e.g. `("events","ts")` for create_hypertable, `()` for list_hypertables). Keep tests DB-free (mocked accessor). Run `uv run black tests/test_timescale_aliases.py` + `uv run ruff check tests/test_timescale_aliases.py`.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg &amp;&amp; uv run pytest tests/test_timescale_aliases.py -x -q -o addopts="" &amp;&amp; uv run ruff check tests/test_timescale_aliases.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_timescale_aliases.py` contains `class TestTimescaleAliases`.
    - `uv run pytest tests/test_timescale_aliases.py -x -q -o addopts=""` exits 0 with ≥12 cases (6 sync + 6 async).
    - Asserts message content (`db.timescale.<m>` + `v0.7.0`), caller-anchored `filename` (`"aliases.py" not in w[0].filename`), AND delegation (`assert_called_once_with`).
    - Uses `unittest.mock`, NOT pytest-mock: `grep -c 'mocker' tests/test_timescale_aliases.py` == 0.
    - `uv run ruff check tests/test_timescale_aliases.py` exits 0.
  </acceptance_criteria>
  <done>All 6 sync + 6 async flat aliases are proven to warn (correct message + caller-anchored stacklevel) and delegate; tests are DB-free via `unittest.mock`; pytest + ruff green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the data-driven ACCESSOR_PAIRS parity registry (D-10)</name>
  <files>tests/test_parity.py</files>
  <read_first>
    - `tests/test_parity.py` lines 1-11 (imports) and lines 466-503 (`TestEtlParity` — the `inspect.getmembers` pattern to generalise).
    - `.planning/phases/21-infrastructure-timescale-accessor/21-PATTERNS.md` lines 371-444 — the verified `ACCESSOR_PAIRS` + `test_accessor_parity` parametrized pattern (transcribe).
    - `.planning/phases/21-infrastructure-timescale-accessor/21-RESEARCH.md` § "Pattern 5" and § "Open Questions" #1 (folding `TestEtlParity` is optional/low-risk).
  </read_first>
  <action>
    Edit `tests/test_parity.py`: add `from pycopg.timescale import AsyncTimescaleAccessor, TimescaleAccessor` to imports. Define module-level `ACCESSOR_PAIRS = [(TimescaleAccessor, AsyncTimescaleAccessor)]` with a comment "Phases 22-24 APPEND here". Add `@pytest.mark.parametrize("sync_cls, async_cls", ACCESSOR_PAIRS)` function `test_accessor_parity` computing public-member sets via `inspect.getmembers` (names not starting with `_`) for both classes and asserting `sync - async == set()` AND `async - sync == set()` with informative messages (both directions — D-10). OPTIONAL low-risk cleanup (do it ONLY if it stays a pure append + delete with zero behavior change): append `(ETLAccessor, AsyncETLAccessor)` to `ACCESSOR_PAIRS` and DELETE the redundant `TestEtlParity` class; if any doubt, leave `TestEtlParity` untouched and add the registry alongside it. Run `uv run black tests/test_parity.py` + `uv run ruff check tests/test_parity.py`.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg &amp;&amp; uv run pytest tests/test_parity.py -x -q -o addopts="" &amp;&amp; uv run ruff check tests/test_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ACCESSOR_PAIRS' tests/test_parity.py` ≥ 2 (definition + parametrize use).
    - `(TimescaleAccessor, AsyncTimescaleAccessor)` is the first entry; `test_accessor_parity` checks BOTH directions (missing-in-async AND extra-in-async).
    - `uv run pytest tests/test_parity.py -x -q -o addopts=""` exits 0 (timescale parity passes; ETL parity still covered whether folded or kept).
    - If folded: `grep -c 'class TestEtlParity' tests/test_parity.py` == 0 AND `(ETLAccessor, AsyncETLAccessor)` in `ACCESSOR_PAIRS`. If not folded: `TestEtlParity` still present + green.
    - `uv run ruff check tests/test_parity.py` exits 0.
  </acceptance_criteria>
  <done>`tests/test_parity.py` has the `ACCESSOR_PAIRS` registry seeded with the timescale pair + a parametrized both-directions parity test; ETL parity remains covered; pytest + ruff green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate 27 timescale call-sites + final no-noise / coverage gate (D-08, REORG-04)</name>
  <files>tests/test_database_integration.py, tests/test_async_database.py, tests/test_sql_injection.py</files>
  <read_first>
    - `.planning/phases/21-infrastructure-timescale-accessor/21-RESEARCH.md` § "Existing Test Call-Sites to Migrate (D-08)" — the exact line-by-line migration tables for all 27 call-sites (5 + 18 + 4).
    - `.planning/phases/21-infrastructure-timescale-accessor/21-PATTERNS.md` lines 494-505 — the mechanical `db.<m>(...)` → `db.timescale.<m>(...)` rewrite with file/line ranges.
    - `tests/test_database_integration.py` lines 840-885 (the `ts_db` timescale block, 5-6 sites).
    - `tests/test_async_database.py` lines 2224-2397 (`TestAsyncDatabaseTimescaleDB`) + 2847-2850 (18 sites total).
    - `tests/test_sql_injection.py` lines 124, 130, 200, 205 (4 sites: `add_compression_policy`/`add_retention_policy` — the injection-guard tests that MUST still pass, threat T-21-02).
  </read_first>
  <action>
    Mechanically rewrite all 27 timescale call-sites from the flat name to the accessor path: `db.<method>(...)` → `db.timescale.<method>(...)` and `await db.<method>(...)` → `await db.timescale.<method>(...)`, for the 6 timescale methods only. Sites: `tests/test_database_integration.py` (~858, 860, 863, 872, 873-874, 883 — including the `ExtensionNotAvailable` test at 883, now `db.timescale.create_hypertable`, same guard/exception); `tests/test_async_database.py` (`TestAsyncDatabaseTimescaleDB` 2224-2397 + 2847-2850 — these mock `db.has_extension`/`db.execute`, still effective because the accessor routes through `self._db`); `tests/test_sql_injection.py` (124, 130, 200, 205 — migrated calls MUST STILL raise on injection because `validate_*` guards now live in the accessor, threat T-21-02). Do NOT change assertions/expected results — only the call path. Do NOT add `filterwarnings` to `pyproject.toml` (D-08: migration alone keeps the suite quiet — RESEARCH confirmed). The ONLY place that may still call the flat deprecated names is `tests/test_timescale_aliases.py` (Task 1). Run `uv run black` + `uv run ruff check` on the 3 edited files, then run the full-suite + no-noise gate.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg &amp;&amp; uv run pytest -W error::DeprecationWarning -q -o addopts="" tests/test_async_database.py tests/test_sql_injection.py tests/test_parity.py tests/test_timescale_aliases.py</automated>
  </verify>
  <acceptance_criteria>
    - All 27 call-sites use the accessor path: `grep -rEc '(await )?(ts_db|db|sync_db|async_db)\.(create_hypertable|enable_compression|add_compression_policy|add_retention_policy|list_hypertables|hypertable_info)\(' tests/test_database_integration.py tests/test_async_database.py tests/test_sql_injection.py` == 0 (no remaining FLAT calls — all now go through `.timescale.`).
    - `tests/test_sql_injection.py` still raises on the injection inputs (migrated tests green — threat T-21-02 regression guard).
    - `uv run pytest -W error::DeprecationWarning -q -o addopts="" tests/test_async_database.py tests/test_sql_injection.py` exits 0 (REORG-04 no-noise gate: migrated tests emit no DeprecationWarning).
    - `uv run pytest` (full suite, with the project `--cov-fail-under=94`) exits 0 — coverage held ≥94% by the new alias tests (REORG-04). NOTE: the 2 known-flaky DB tests (`test_async_transaction_fix`, `test_create_spatial_index_name_parameter`) are pre-existing and unrelated; if they fail, re-run them in isolation per the STATE.md blocker note — they do not gate this phase.
    - `pyproject.toml` has NO new `filterwarnings` entry (`grep -c 'filterwarnings' pyproject.toml` unchanged from baseline 0).
  </acceptance_criteria>
  <done>All 27 call-sites are migrated to `db.timescale.*`; the migrated suite passes under `-W error::DeprecationWarning`; injection tests still pass; full suite green with coverage ≥94%; no `filterwarnings` added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test caller → migrated `db.timescale.*` injection inputs | malicious identifiers/intervals must still be rejected by the relocated guards |
| flat alias → DeprecationWarning channel | warnings must be emitted (alias tests) yet absent from the main suite (migration) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Tampering (SQL injection) | migrated `tests/test_sql_injection.py` timescale calls | mitigate | The 4 injection tests are migrated to `db.timescale.add_compression_policy`/`add_retention_policy` and MUST still raise — proves the `validate_*` guards survived the verbatim move into the accessor. Acceptance: `test_sql_injection.py` green after migration. |
| T-21-04 | Information disclosure / noise | DeprecationWarning leakage into the main suite | mitigate | All 27 non-alias call-sites migrated (D-08); the no-noise gate `pytest -W error::DeprecationWarning` passes; only `test_timescale_aliases.py` calls the flat names. |
| T-21-SC | Tampering | pip installs | accept | No new packages; tests use stdlib `unittest.mock` (already present), not new pytest-mock. |
</threat_model>

<verification>
- `uv run pytest tests/test_timescale_aliases.py tests/test_parity.py -x -q -o addopts=""` exits 0 (quick gate from VALIDATION.md).
- `uv run pytest -W error::DeprecationWarning -q -o addopts="" tests/test_async_database.py tests/test_sql_injection.py` exits 0 (REORG-04 no-noise gate).
- `uv run pytest` (full suite) exits 0 with coverage ≥94% (excluding the 2 documented pre-existing flaky DB tests).
- `uv run ruff check tests/` exits 0.
</verification>

<success_criteria>
- Every flat alias (6 sync + 6 async) is proven to warn (correct message + caller-anchored stacklevel) and delegate (D-09).
- `ACCESSOR_PAIRS` registry seeds the timescale pair and the parametrized parity test passes both directions (D-10/REORG-03).
- All 27 call-sites migrated; `-W error::DeprecationWarning` gate passes (REORG-04); injection tests still pass (TS-01/T-21-02).
- Full suite green; coverage ≥94%; no `filterwarnings` added. Phase 21 success criteria 1-4 all satisfied.
</success_criteria>

<output>
Create `.planning/phases/21-infrastructure-timescale-accessor/21-03-SUMMARY.md` when done
</output>
