---
phase: 21-infrastructure-timescale-accessor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/aliases.py
  - pycopg/timescale.py
autonomous: true
requirements: [REORG-01, TS-01]
must_haves:
  truths:
    - "D-01: deprecated_alias takes a target path STRING (e.g. 'timescale.create_hypertable') and resolves the lazy accessor on self via getattr at call time — no hardcoded function references, so v0.7.0 removal = delete the decorated stub"
    - "D-02: each flat stub is a generic (*args, **kwargs) one-liner passthrough; the decorator does the warn + delegate"
    - "D-03: deprecated_alias produces an async wrapper for async stubs (iscoroutinefunction stays True) that awaits the delegated coroutine, and a sync wrapper otherwise — sync+async parity is mandatory"
    - "D-04: the DeprecationWarning message names the new db.<accessor>.<method> path and uses stacklevel=2 so the warning points at the caller's line, not inside the decorator"
    - "TimescaleAccessor.create_hypertable (and the 5 other methods) run the original SQL through self._db.execute and guard on self._db.has_extension('timescaledb')"
    - "AsyncTimescaleAccessor mirrors TimescaleAccessor method-for-method with await"
  artifacts:
    - path: "pycopg/aliases.py"
      provides: "deprecated_alias(target_path) decorator factory (sync + async branch)"
      contains: "def deprecated_alias("
      min_lines: 25
    - path: "pycopg/timescale.py"
      provides: "TimescaleAccessor + AsyncTimescaleAccessor with the 6 timescale methods moved verbatim"
      contains: "class TimescaleAccessor"
      min_lines: 200
  key_links:
    - from: "pycopg/aliases.py"
      to: "self.<accessor>.<method>"
      via: "getattr(self, accessor_name) then getattr(accessor, method_name)"
      pattern: "getattr\\(self, accessor_name\\)"
    - from: "pycopg/timescale.py"
      to: "self._db.execute / self._db.has_extension"
      via: "moved method bodies route through the parent database"
      pattern: "self\\._db\\.(execute|has_extension)"
---

<objective>
Create the two net-new foundation modules for the v0.6.0 accessor reorganisation:

1. `pycopg/aliases.py` — the reusable `@deprecated_alias` decorator (REORG-01) that warns + delegates, branching on `inspect.iscoroutinefunction`. Phases 22-24 import this UNCHANGED.
2. `pycopg/timescale.py` — `TimescaleAccessor` + `AsyncTimescaleAccessor` holding the 6 TimescaleDB methods (TS-01), moved VERBATIM from `database.py` / `async_database.py` with `self.` → `self._db.` rewrites (D-06).

Purpose: Establish the end-to-end pattern (decorator + accessor) as the proof later phases replicate mechanically. Both modules are stand-alone (no edits to `database.py` yet) so this plan is independently testable by import.
Output: `pycopg/aliases.py`, `pycopg/timescale.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21-infrastructure-timescale-accessor/21-CONTEXT.md
@.planning/phases/21-infrastructure-timescale-accessor/21-RESEARCH.md
@.planning/phases/21-infrastructure-timescale-accessor/21-PATTERNS.md
</context>

## Artifacts this phase produces (full phase scope — this plan delivers items 1-2)

| Symbol / File | Plan | Notes |
|---------------|------|-------|
| `deprecated_alias` (decorator) — `pycopg/aliases.py` | **01** | sync + async branch, `stacklevel=2`, `functools.wraps` |
| `TimescaleAccessor`, `AsyncTimescaleAccessor` — `pycopg/timescale.py` | **01** | 6 methods each, moved verbatim |
| `timescale` lazy property + `_timescale` field on `Database` / `AsyncDatabase` | 02 | mirrors `_spatial`/`_etl` |
| 6 + 6 deprecated flat stubs replacing the old method bodies | 02 | `@deprecated_alias("timescale.<m>")` |
| `TimescaleAccessor`/`AsyncTimescaleAccessor` in `pycopg/__init__.py __all__` | 02 | export precedent (formal docs = Phase 24) |
| `tests/test_timescale_aliases.py` (alias warn+delegate, D-09) | 03 | |
| `ACCESSOR_PAIRS` registry symbol in `tests/test_parity.py` (D-10) | 03 | seeded with the timescale pair |
| 27 migrated call-sites (D-08) | 03 | |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the deprecated_alias decorator module</name>
  <files>pycopg/aliases.py</files>
  <read_first>
    - `.planning/phases/21-infrastructure-timescale-accessor/21-RESEARCH.md` § "Pattern 1: deprecated_alias decorator (complete implementation)" — the VERIFIED, complete source to transcribe (do not reinvent). Also § "Footgun 2" (stacklevel=2) and § "Footgun 3" (async branch).
    - `.planning/phases/21-infrastructure-timescale-accessor/21-PATTERNS.md` lines 54-112 — same verified impl plus "Key rules verified by research".
    - `pycopg/etl.py` lines 1-43 — module-docstring + `from __future__ import annotations` boilerplate (NOTE: `aliases.py` needs NO `TYPE_CHECKING` block).
  </read_first>
  <behavior>
    - Sync warn: a `def`-stub decorated `@deprecated_alias("timescale.create_hypertable")` emits one `DeprecationWarning` whose message contains `db.timescale.create_hypertable` and `v0.7.0`.
    - Sync delegate: forwards `*args, **kwargs` verbatim to `self.timescale.create_hypertable` and returns its value.
    - Async branch: an `async def` stub yields a wrapper with `inspect.iscoroutinefunction(wrapper) is True`; awaiting it warns then `await`s the accessor coroutine.
    - Stacklevel: recorded warning's `filename` is the caller's file, not `aliases.py`.
    - Metadata: `wrapper.__name__` and `wrapper.__doc__` come from the stub (`functools.wraps`).
    (Formally asserted in plan 03's `tests/test_timescale_aliases.py`.)
  </behavior>
  <action>
    Create `pycopg/aliases.py` transcribing the VERIFIED implementation from 21-RESEARCH.md Pattern 1 (do NOT redesign). Module docstring required (interrogate gate). Top: `from __future__ import annotations`, then `import functools`, `import inspect`, `import warnings`. Define `def deprecated_alias(target_path: str)` with a numpydoc docstring (Summary/Parameters/Returns). Inside: build `msg` naming `db.{target_path}` and the deprecated flat `db.{fn.__name__}`, mention removal in `v0.7.0`; `accessor_name, method_name = target_path.split(".", 1)`. Branch on `inspect.iscoroutinefunction(fn)`: True → `@functools.wraps(fn) async def async_wrapper(self, *args, **kwargs)` that does `warnings.warn(msg, DeprecationWarning, stacklevel=2)` THEN `accessor = getattr(self, accessor_name)` THEN `return await getattr(accessor, method_name)(*args, **kwargs)`; False → identical `sync_wrapper` without `async`/`await`. CRITICAL: `stacklevel=2` (Footgun 2); warn BEFORE resolving the accessor; the async wrapper MUST itself be `async def` (Footgun 3 / Pitfall 1). Do NOT use `eval`/`exec`/format on the path — only `str.split` + `getattr` (threat T-21-01). Run `uv run black pycopg/aliases.py` and `uv run ruff check pycopg/aliases.py`.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg &amp;&amp; uv run python -c "from pycopg.aliases import deprecated_alias; assert callable(deprecated_alias)" &amp;&amp; uv run ruff check pycopg/aliases.py &amp;&amp; uv run interrogate pycopg/aliases.py --fail-under 95</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def deprecated_alias(' pycopg/aliases.py` == 1; file contains `stacklevel=2` and `inspect.iscoroutinefunction(`.
    - `grep -v '^#' pycopg/aliases.py | grep -c 'eval(\|exec('` == 0 (no dynamic eval — threat T-21-01).
    - `cd /home/loc/workspace/pycopg && uv run python -c "from pycopg.aliases import deprecated_alias"` exits 0.
    - `uv run ruff check pycopg/aliases.py` exits 0; `uv run interrogate pycopg/aliases.py --fail-under 95` exits 0.
    - Full warn+delegate behavior is exercised by plan 03; here the module imports and the verified impl is transcribed (NOT "looks correct").
  </acceptance_criteria>
  <done>`pycopg/aliases.py` exists, imports cleanly, has the sync+async branch with `stacklevel=2`, resolves the path with only `getattr`/`split` (no eval/exec), passes ruff + interrogate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create timescale.py with TimescaleAccessor + AsyncTimescaleAccessor (verbatim move)</name>
  <files>pycopg/timescale.py</files>
  <read_first>
    - `pycopg/database.py` lines 1648-1885 — the 6 SYNC bodies to move verbatim: `create_hypertable` (1648), `enable_compression` (1699), `add_compression_policy` (1753), `add_retention_policy` (1790), `list_hypertables` (1827), `hypertable_info` (1848); block ends before `size` at 1886.
    - `pycopg/async_database.py` lines 1224-1461 — the 6 ASYNC bodies: `create_hypertable` (1224), `enable_compression` (1275), `add_compression_policy` (1329), `add_retention_policy` (1366), `list_hypertables` (1403), `hypertable_info` (1424); block ends before `role_exists` at 1462.
    - `pycopg/etl.py` lines 1-43 (boilerplate), 510-540 (`ETLAccessor.__init__`), 1009-1042 (`AsyncETLAccessor.__init__`) — EXACT accessor shape (`self._db = db`, NO extension guard at construction).
    - `pycopg/utils.py` lines 47, 76, 94 — `validate_identifier` / `validate_identifiers` / `validate_interval` signatures.
    - `pycopg/exceptions.py` — `ExtensionNotAvailable`.
    - `.planning/phases/21-infrastructure-timescale-accessor/21-PATTERNS.md` lines 116-202 — the verbatim-move pattern with the `self.` → `self._db.` rewrite example.
  </read_first>
  <behavior>
    - `TimescaleAccessor(db).create_hypertable(...)` produces byte-identical SQL to the old `db.create_hypertable(...)` (same f-string, same `INTERVAL` literal, same `if_not_exists`/`migrate_data` flags) and routes through `db.execute`.
    - Each method first calls `self._db.has_extension("timescaledb")` and raises `ExtensionNotAvailable` if absent (guard preserved verbatim).
    - The `validate_identifiers` / `validate_interval` guards still execute before any SQL.
    - `AsyncTimescaleAccessor` exposes the identical 6 public method names with `await self._db.execute(...)`.
  </behavior>
  <action>
    Create `pycopg/timescale.py`. Module docstring (interrogate). Imports per 21-RESEARCH.md boilerplate: `from __future__ import annotations`; `from typing import TYPE_CHECKING`; `from pycopg.exceptions import ExtensionNotAvailable`; `from pycopg.utils import` the validators actually referenced by the moved bodies (`validate_identifiers`, `validate_interval`, and `validate_identifier` only if used); `if TYPE_CHECKING:` → `from pycopg.async_database import AsyncDatabase` and `from pycopg.database import Database`. Define `class TimescaleAccessor` with class docstring and `def __init__(self, db: Database) -> None: self._db = db`. MOVE the 6 sync bodies VERBATIM from `database.py:1648-1885` — keep existing numpydoc docstrings, signatures, inline f-string SQL UNCHANGED (D-06). ONLY edits permitted: every `self.has_extension(` → `self._db.has_extension(` and every `self.execute(` → `self._db.execute(` (Pitfall 3). Do NOT extract SQL builders; do NOT alter SQL; do NOT drop the `validate_*` guards (threat T-21-02). Then `class AsyncTimescaleAccessor` with `def __init__(self, db: AsyncDatabase) -> None: self._db = db` and MOVE the 6 async bodies from `async_database.py:1224-1461` the same way (`self.` → `self._db.`, `await self._db.execute(...)`). Run `uv run black pycopg/timescale.py` and `uv run ruff check pycopg/timescale.py`.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg &amp;&amp; uv run python -c "import inspect; from pycopg.timescale import TimescaleAccessor as T, AsyncTimescaleAccessor as A; e={'create_hypertable','enable_compression','add_compression_policy','add_retention_policy','list_hypertables','hypertable_info'}; s={n for n,_ in inspect.getmembers(T) if not n.startswith('_')}; a={n for n,_ in inspect.getmembers(A) if not n.startswith('_')}; assert s==e and a==e, (s,a)" &amp;&amp; uv run ruff check pycopg/timescale.py &amp;&amp; uv run interrogate pycopg/timescale.py --fail-under 95</automated>
  </verify>
  <acceptance_criteria>
    - File contains `class TimescaleAccessor` AND `class AsyncTimescaleAccessor`.
    - `grep -c 'self\.has_extension(\|self\.execute(' pycopg/timescale.py` == 0 (every `self.` rewritten — Pitfall 3); `grep -c 'self\._db\.execute(' pycopg/timescale.py` == 6.
    - `grep -c 'validate_interval(\|validate_identifiers(' pycopg/timescale.py` ≥ 4 (security guards travelled — threat T-21-02).
    - The verify set-equality passes (both accessors expose exactly the 6 method names).
    - `uv run ruff check pycopg/timescale.py` exits 0; `uv run interrogate pycopg/timescale.py --fail-under 95` exits 0.
    - SQL strings unchanged vs `database.py` originals except the `self.` → `self._db.` rewrite.
  </acceptance_criteria>
  <done>`pycopg/timescale.py` exists with both accessors exposing the 6 methods; bodies are verbatim moves with only `self.` → `self._db.` rewrites; `validate_*` guards + `has_extension` checks intact; ruff + interrogate pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `deprecated_alias(target_path)` | `target_path` is a SOURCE literal, never caller-supplied at runtime |
| caller → timescale method args | table/schema/column names + interval strings cross into f-string SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering / Elevation | `deprecated_alias` path resolution (`pycopg/aliases.py`) | mitigate | Resolve via `target_path.split(".",1)` + `getattr(self, accessor_name)` only; NO `eval`/`exec`/format-on-path; `target_path` is a hardcoded source literal, never caller-controlled. Acceptance: `grep -c 'eval(\|exec(' pycopg/aliases.py` == 0. |
| T-21-02 | Tampering (SQL injection) | moved timescale f-string SQL (`pycopg/timescale.py`) | mitigate | `validate_identifiers`/`validate_interval` guards move VERBATIM with the bodies and run before any SQL; verbatim move must not drop a guard. Acceptance: guard-count grep ≥ 4 (injection tests migrated + run green in plan 03). |
| T-21-SC | Tampering | pip installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none). Pure-Python additions only. |
</threat_model>

<verification>
- `uv run python -c "from pycopg.aliases import deprecated_alias; from pycopg.timescale import TimescaleAccessor, AsyncTimescaleAccessor"` exits 0.
- `uv run ruff check pycopg/aliases.py pycopg/timescale.py` exits 0.
- `uv run interrogate pycopg --fail-under 95` exits 0.
- `git diff --name-only` lists ONLY `pycopg/aliases.py` and `pycopg/timescale.py` (no `database.py`/`__init__.py` edits — those are plan 02).
</verification>

<success_criteria>
- `pycopg/aliases.py` defines `deprecated_alias` with verified sync+async branch, `stacklevel=2`, no eval/exec.
- `pycopg/timescale.py` defines both accessors, each with the 6 methods moved verbatim (only `self.` → `self._db.`), guards intact.
- Both modules import cleanly and pass ruff + interrogate. Plan 02 can wire them into `Database`/`AsyncDatabase`.
</success_criteria>

<output>
Create `.planning/phases/21-infrastructure-timescale-accessor/21-01-SUMMARY.md` when done
</output>
