---
phase: 32-query-helpers-parity-verification
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - tests/test_timescale.py
  - tests/test_parity.py
autonomous: true
requirements: [TS-ADV-06, TS-ADV-07, TS-ADV-10]
must_haves:
  truths:
    - "Mock SQL-shape tests prove time_bucket renders `time_bucket(%s, <col>) AS bucket` and routes into='df' through to_dataframe(sql=named, params=dict) and into='rows' through execute (D-01, D-02, D-03)"
    - "A mock test proves into='gdf' raises ValueError before any DB call on both helpers (D-03)"
    - "A mock test proves time_bucket_gapfill binds start/finish TWICE — params == [bucket_width, start, finish, start, finish] (D-10)"
    - "A mock test proves the async helpers await has_extension (AsyncMock asserts awaited) — no missing-await regression (D-07)"
    - "A live test asserts REAL time_bucket output (bucket column present, deterministic) since time_bucket is Apache-free (D-08, planner-verified)"
    - "A live test for time_bucket_gapfill passes Python datetime start/finish and tolerates FeatureNotSupported on the Apache build (planner-verified D-08 correction), asserting real gap-filled NULL-padded output only on Community/TSL builds"
    - "test_accessor_parity passes for the timescale pair and an explicit assertion lists the exact 9 v0.8.0 method names so a dropped/renamed method fails loudly (TS-ADV-10, D-11)"
    - "Full suite passes with coverage ratchet >=94% and no new autocommit branches introduced (D-12)"
  artifacts:
    - path: "tests/test_timescale.py"
      provides: "TestTimeBucketMock / TestTimeBucketGapfillMock mock SQL-shape classes + live integration tests (time_bucket REAL, gapfill license-tolerant)"
      contains: "def test_time_bucket"
    - path: "tests/test_parity.py"
      provides: "explicit 9-name v0.8.0 timescale surface assertion"
      contains: "time_bucket_gapfill"
  key_links:
    - from: "TestTimeBucketMock (into='df')"
      to: "db.to_dataframe call_args"
      via: "MagicMock(db.to_dataframe) asserting named-bind dict"
      pattern: "to_dataframe"
    - from: "test_accessor_parity"
      to: "TimescaleAccessor / AsyncTimescaleAccessor"
      via: "ACCESSOR_PAIRS already-registered pair (no registry change)"
      pattern: "test_accessor_parity"
    - from: "explicit 9-name assertion"
      to: "the 9 v0.8.0 TimescaleAccessor methods"
      via: "named set membership check"
      pattern: "time_bucket_gapfill"
---

<objective>
Verify the two query helpers from plan 01 with the two-layer test pattern (mock
SQL-shape + live integration) and confirm TS-ADV-10 full sync/async parity across
all 9 new v0.8.0 methods.

Purpose: Close TS-ADV-06, TS-ADV-07, and TS-ADV-10 with executable proof — SQL
shape, `into=` routing, the gapfill double-bind, the async `await` audit, real
`time_bucket` output, license-tolerant gapfill output, the bidirectional parity
set-diff, and one explicit 9-name surface assertion — while holding the coverage
ratchet ≥94%.

Output: new mock + live test classes in `tests/test_timescale.py` and one explicit
9-name assertion in `tests/test_parity.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-query-helpers-parity-verification/32-CONTEXT.md
@.planning/phases/32-query-helpers-parity-verification/32-PATTERNS.md
@.planning/phases/32-query-helpers-parity-verification/32-01-PLAN.md
</context>

<critical_findings>
PLANNER LIVE-VERIFICATION (2026-06-23, local TimescaleDB 2.28.0, `timescaledb.license = apache`):

- `time_bucket` is Apache-free → its live test asserts REAL output (deterministic
  `bucket` column, aggregate values). D-08 holds for `time_bucket`.
- **D-08 IS WRONG FOR `time_bucket_gapfill`.** On the local Apache 2.28.0 build,
  `time_bucket_gapfill` raises `psycopg.errors.FeatureNotSupported`
  (`...not supported under the current "apache" license`). `locf()`/`interpolate()`
  are reachable only inside gapfill, so they are gated identically. The gapfill live
  test MUST therefore use the Phase-31 license-tolerant pattern
  (try/except FeatureNotSupported), with the MOCK SQL-shape test as authoritative —
  it must NOT assert real output unconditionally as D-08's framing assumed. The
  analog is `tests/test_timescale.py:1395-1419`
  (`test_create_continuous_aggregate_live`). `FeatureNotSupported` is already imported
  at test_timescale.py:26 (currently `# noqa: F401`) — this test uses it.
</critical_findings>

<tasks>

<task type="auto">
  <name>Task 1: Mock SQL-shape unit tests for time_bucket / time_bucket_gapfill</name>
  <files>tests/test_timescale.py</files>
  <read_first>
    - tests/test_timescale.py:1-30 (imports — `MagicMock`, `AsyncMock`, `FeatureNotSupported`, `datetime`), :115-260 (`TestShowChunksMock` — the exact MagicMock(spec=SchemaAccessor) + `db.execute.call_args[0]` pattern, sync + async)
    - pycopg/timescale.py (the just-added `time_bucket`, `time_bucket_gapfill`, `_run`, builders — assert against their real SQL/params)
    - pycopg/database.py:899-935 (to_dataframe sink — for the `into="df"` mock: patch `db.to_dataframe` and read its kwargs)
    - .planning/phases/32-query-helpers-parity-verification/32-CONTEXT.md (D-01, D-02, D-03, D-10)
  </read_first>
  <action>
    Add `class TestTimeBucketMock` and `class TestTimeBucketGapfillMock` to
    `tests/test_timescale.py`, mirroring `TestShowChunksMock`
    (MagicMock(spec=SchemaAccessor) with `has_extension → True`, assign `db._schema`).

    For `into="rows"`: set `db.execute = MagicMock(return_value=[...])`, call the
    method, read `sql, params = db.execute.call_args[0]`, assert:
    - `"time_bucket(%s," in sql` and `"AS bucket" in sql` (D-01)
    - `"GROUP BY bucket ORDER BY bucket" in sql`
    - for `time_bucket`: `params == ["1 hour"]`
    - for gapfill: `sql.count("%s") == 5` and `params == ["1 hour", start, finish, start, finish]`
      with `start`/`finish` being `datetime(...)` objects (D-10); assert the WHERE has
      `>= %s` and `< %s`.

    For `into="df"` (default): set `db.to_dataframe = MagicMock(return_value=<df-ish>)`,
    call the method, read `db.to_dataframe.call_args.kwargs`, assert `sql=` contains
    `:p0` (named binds, NOT `%s`) and `params=` is a dict whose values match the
    positional params in order (D-02). Cover `where=` present (asserts `WHERE` /
    ` AND (` fragment in sql) and absent.

    Add a test that `into="gdf"` raises `ValueError` on BOTH helpers and that
    `db.execute` / `db.to_dataframe` were NOT called (guard runs before SQL, D-03).
    Add an extension-absent test (has_extension → False) raising
    `ExtensionNotAvailable` with no DB call (mirror `test_show_chunks_no_extension_raises`).

    Add async mocks (`MagicMock(spec=AsyncSchemaAccessor)` with
    `has_extension = AsyncMock(return_value=True)`, `db.execute = AsyncMock(...)` /
    `db.to_dataframe = AsyncMock(...)`) for both helpers, asserting the same SQL/params
    shape AND `mock_schema.has_extension.assert_awaited_once_with("timescaledb")`
    (proves the await — D-07). `asyncio_mode="auto"` means no per-test marker needed.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run pytest tests/test_timescale.py -k "TimeBucket and Mock" -q -o addopts="" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_timescale.py` contains `class TestTimeBucketMock` and `class TestTimeBucketGapfillMock`
    - a test asserts `time_bucket` SQL contains `time_bucket(%s,` and `AS bucket` and params `== ["1 hour"]`
    - a test asserts `time_bucket_gapfill` SQL has exactly 5 `%s` and params `== ["1 hour", start, finish, start, finish]` with `datetime` start/finish (the double-bind, D-10)
    - a test asserts `into="df"` calls `to_dataframe(sql=<has :p0>, params=<dict>)` (named binds, not `%s`) on both helpers
    - a test asserts `into="gdf"` raises `ValueError` and neither `execute` nor `to_dataframe` was called, on both helpers
    - async mock tests assert `has_extension` was AWAITED (`assert_awaited_once_with("timescaledb")`)
    - `uv run pytest tests/test_timescale.py -k "TimeBucket and Mock" -q -o addopts=""` exits 0
    - `cd /home/loc/workspace/pycopg && uv run ruff check tests/test_timescale.py` exits 0
  </acceptance_criteria>
  <done>Mock SQL-shape tests (sync + async) prove the SQL shape, the fixed `bucket` alias, named-bind df routing, the gapfill double-bind, the `into="gdf"` ValueError before any DB call, the extension guard, and the awaited async guard — all green with no live DB.</done>
</task>

<task type="auto">
  <name>Task 2: Live integration tests — time_bucket REAL, gapfill license-tolerant</name>
  <files>tests/test_timescale.py</files>
  <read_first>
    - tests/test_timescale.py:36-107 (`db`, `ts_db`, `async_ts_db` fixtures + `_make_hypertable` helper)
    - tests/test_timescale.py:440-500 (`test_show_chunks_returns_list` etc. — REAL-output live test pattern, drop-in-finally)
    - tests/test_timescale.py:1388-1432 (`test_create_continuous_aggregate_live` — the try/except FeatureNotSupported license-tolerant pattern for the GAPFILL live test ONLY)
    - .planning/phases/32-query-helpers-parity-verification/32-CONTEXT.md (D-08 — and this plan's critical_findings correcting it for gapfill)
  </read_first>
  <action>
    Add live integration tests using the `ts_db` / `async_ts_db` skip-fixtures and the
    `_make_hypertable(ts_db, table, days=...)` helper, dropping the table in a `finally`.

    time_bucket (Apache-free → REAL assertions, D-08 holds): create a small hypertable,
    call `ts_db.timescale.time_bucket(table, "ts", "1 day", "avg(val) AS avg_val")`
    (default `into="df"`) and assert the returned object has a `bucket` column and
    >= 1 row; call again with `into="rows"` and assert `list[dict]` each containing a
    `bucket` key. Add an async twin via `async_ts_db` (and a sync `Database(async_ts_db.config)`
    to build the hypertable, as the cagg async live test does at ~1426).

    time_bucket_gapfill (TSL-gated on the local Apache build — PLANNER-VERIFIED, this
    CORRECTS D-08): MIRROR `test_create_continuous_aggregate_live` (test_timescale.py:1395-1419).
    Build a hypertable with a deliberate time gap; call
    `ts_db.timescale.time_bucket_gapfill(table, "ts", "1 hour", start, finish, "locf(avg(val)) AS lo")`
    passing Python `datetime` objects for `start`/`finish` (ROADMAP criterion #2 — NOT
    literals). Wrap the call in `try: <assert real NULL-padded gap-filled output: at
    least one bucket row whose aggregate is NULL/None, bucket column present> except
    FeatureNotSupported: pass`. The mock test (Task 1) is authoritative for SQL shape.
    Add an async twin. Do NOT assert real output unconditionally — that fails on the
    Apache CI build.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run pytest tests/test_timescale.py -k "(time_bucket and live) or (TimeBucket and Live)" -q -o addopts="" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - a live test calls `ts_db.timescale.time_bucket(...)` with `into="df"` and asserts the result has a `bucket` column and >= 1 row (REAL output — no try/except)
    - a live test calls `time_bucket` with `into="rows"` and asserts `list[dict]` with a `bucket` key in each row
    - the gapfill live test passes Python `datetime` `start`/`finish` and is wrapped in `try/except FeatureNotSupported` (asserts real NULL-padded gap-filled output inside `try`, tolerates the Apache license gate in `except`)
    - async twins exist for both helpers using `async_ts_db`
    - every live test drops its table/view in `finally`
    - `uv run pytest tests/test_timescale.py -k "(time_bucket and live) or (TimeBucket and Live)" -q -o addopts=""` exits 0 (live tests either pass or skip; gapfill tolerates FeatureNotSupported on the Apache build)
    - `cd /home/loc/workspace/pycopg && uv run ruff check tests/test_timescale.py` exits 0
  </acceptance_criteria>
  <done>Live tests assert REAL `time_bucket` output (Apache-free) and run the gapfill path with `datetime` bound params under license-tolerant try/except, all green or skipped on the local Apache build, tables cleaned up.</done>
</task>

<task type="auto">
  <name>Task 3: Explicit 9-name parity assertion + full-suite ratchet gate</name>
  <files>tests/test_parity.py</files>
  <read_first>
    - tests/test_parity.py:1-62 (`ACCESSOR_PAIRS` at 24 with the timescale pair already at line 25; `test_accessor_parity` at 35 — auto-covers the 9 methods, NO registry change)
    - pycopg/timescale.py (confirm the EXACT public method names before writing the named set)
    - .planning/phases/32-query-helpers-parity-verification/32-CONTEXT.md (D-11)
  </read_first>
  <action>
    Add ONE new test function to `tests/test_parity.py`,
    `test_timescale_v080_surface()`, asserting that BOTH `TimescaleAccessor` and
    `AsyncTimescaleAccessor` expose the exact 9 new v0.8.0 method names as a named set
    (D-11) so a silently dropped/renamed method fails loudly. The verified 9 names
    (planner-confirmed against the live class via grep of public defs):
    `show_chunks`, `drop_chunks`, `add_dimension`, `add_reorder_policy`,
    `create_continuous_aggregate`, `refresh_continuous_aggregate`,
    `add_continuous_aggregate_policy`, `time_bucket`, `time_bucket_gapfill`.
    Build a `frozenset` of these names; assert it `<=` the public-member set of each
    class (`{n for n,_ in inspect.getmembers(cls) if not n.startswith("_")}`). Do NOT
    touch `ACCESSOR_PAIRS` (timescale pair already registered) and do NOT add
    per-method signature parity (deferred, D-11). Add a numpydoc docstring.

    Do NOT modify `tests/test_parity.py` `test_accessor_parity` — it already covers the
    bidirectional set-diff for the pair.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run pytest tests/test_parity.py -q -o addopts="" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_parity.py` contains a new `def test_timescale_v080_surface(` asserting the named 9-method frozenset is a subset of BOTH classes' public members
    - the named set is exactly the 9 strings listed in the action (no guessed/extra names)
    - `uv run pytest tests/test_parity.py -q -o addopts=""` exits 0 (both `test_accessor_parity[TimescaleAccessor-AsyncTimescaleAccessor]` and the new 9-name test pass)
    - `ACCESSOR_PAIRS` is unchanged (no registry edit)
    - FULL SUITE GATE: `cd /home/loc/workspace/pycopg && uv run pytest tests/test_timescale.py tests/test_parity.py -q -o addopts=""` exits 0 (carry-forward: shared-builder changes must run both files)
    - COVERAGE RATCHET: `cd /home/loc/workspace/pycopg && uv run pytest` exits 0 with `--cov-fail-under=94` satisfied (the 2 named pre-existing flaky DB tests `test_async_transaction_fix` / `test_create_spatial_index_name_parameter` are the only tolerated failures — re-run targeted with `-o addopts=""` if they flake; they are NOT Phase-32 regressions)
    - `cd /home/loc/workspace/pycopg && uv run ruff check tests/test_parity.py` exits 0
  </acceptance_criteria>
  <done>The explicit 9-name surface assertion passes for both classes, `test_accessor_parity` stays green for the timescale pair with no registry change, and the full suite passes with coverage ≥94%.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 02)

New test symbols (all NEW — exclude from drift verification):

- `tests/test_timescale.py`: `TestTimeBucketMock`, `TestTimeBucketGapfillMock` (mock SQL-shape classes, sync + async) and live integration test functions for `time_bucket` (REAL) and `time_bucket_gapfill` (license-tolerant), sync + async
- `tests/test_parity.py`: `test_timescale_v080_surface` (explicit 9-name v0.8.0 surface assertion)

No production symbols are added in this plan. `ACCESSOR_PAIRS` is unchanged.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → test DB | tests connect to the local `pycopg_test` DB; create/drop throwaway hypertables named with `uuid4()` suffixes, dropped in `finally` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32T-01 | Tampering (test residue) | live tests create hypertables | mitigate | each live test drops its table/view in a `finally` block (mirrors existing show_chunks/cagg live tests) |
| T-32T-02 | Information disclosure | DB credentials in tests | accept | tests read creds from env / conftest defaults (postgres/postgres on local pycopg_test); no new secret surface, identical to all existing DB tests |
| T-32T-SC | Tampering | npm/pip/cargo installs | n/a | zero new test dependencies; no package installs in this phase |
</threat_model>

<verification>
- `uv run pytest tests/test_timescale.py -k "TimeBucket" -q -o addopts=""` exits 0 (mock + live)
- `uv run pytest tests/test_parity.py -q -o addopts=""` exits 0 (set-diff parity + 9-name assertion)
- `uv run pytest tests/test_timescale.py tests/test_parity.py -q -o addopts=""` exits 0 (shared-builder carry-forward gate)
- `uv run pytest` exits 0 with coverage ≥94% (ratchet held; only the 2 named pre-existing flaky DB tests tolerated)
- `uv run ruff check tests/test_timescale.py tests/test_parity.py` clean
</verification>

<success_criteria>
- Mock SQL-shape tests prove the `bucket` alias, named-bind df routing, the gapfill double-bind, the `into="gdf"` ValueError, and the awaited async guard
- `time_bucket` live tests assert REAL output; `time_bucket_gapfill` live tests use `datetime` binds under license-tolerant try/except
- `test_accessor_parity` passes for the timescale pair (no registry change) and the explicit 9-name assertion fails loudly on any dropped/renamed method
- Coverage ratchet ≥94% holds; no new autocommit branches introduced
</success_criteria>

<output>
Create `.planning/phases/32-query-helpers-parity-verification/32-02-SUMMARY.md` when done
</output>
