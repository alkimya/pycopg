---
phase: 38-performance-copy
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - pycopg/database.py
  - pycopg/async_database.py
  - tests/test_database.py
autonomous: true
requirements: [PERF-03, PERF-05]
must_haves:
  truths:
    - "insert_batch (sync) computes row_placeholders once before the per-row loop, not per row (D-05)"
    - "insert_batch (async) mirrors the same hoist (PERF-05 parity)"
    - "the produced INSERT SQL string and inserted rows are byte-exact identical to before — no behaviour change"
    - "a non-regression test pins the byte-exact behaviour"
  artifacts:
    - path: "pycopg/database.py"
      provides: "insert_batch with row_placeholders hoisted out of the per-row loop"
      contains: "row_placeholders"
    - path: "pycopg/async_database.py"
      provides: "async insert_batch with the same hoist"
      contains: "row_placeholders"
    - path: "tests/test_database.py"
      provides: "byte-exact non-regression test for insert_batch placeholder hoist"
      contains: "insert_batch"
  key_links:
    - from: "pycopg/database.py insert_batch"
      to: "row_placeholders"
      via: "single computation before `for row in batch`"
      pattern: "row_placeholders = \", \".join"
    - from: "pycopg/async_database.py insert_batch"
      to: "row_placeholders"
      via: "single computation before `for row in batch`"
      pattern: "row_placeholders = \", \".join"
---

<objective>
Hoist the loop-invariant `row_placeholders = ", ".join(["%s"] * len(columns))` computation OUT of the per-row loop in `insert_batch`, for both `Database` (database.py L983) and `AsyncDatabase` (async_database.py L624). This is a strictly byte-exact micro-optimization: the produced SQL and inserted rows are identical; only the computation site moves from inside the loop to once per batch.

Purpose: Remove redundant per-row recomputation of an invariant string (PERF-03), with a non-regression test pinning byte-exact behaviour. Maintain sync/async parity (PERF-05).
Output: hoisted `row_placeholders` (sync + async); a byte-exact non-regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-performance-copy/38-CONTEXT.md
@.planning/phases/38-performance-copy/38-PATTERNS.md
@.planning/phases/38-performance-copy/38-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan introduces NO new public symbols. It moves one line within two existing methods. New symbols:

| Symbol | File | Kind | Purpose |
|--------|------|------|---------|
| `test_insert_batch_placeholder_hoist_regression` (or equivalent name) | `tests/test_database.py` | test function | PERF-03 byte-exact non-regression: asserts the INSERT SQL / inserted rows are identical to the pre-hoist behaviour. |

`insert_batch` (sync + async) signatures are UNCHANGED. Behaviour is byte-exact (non-breaking + no observable change).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Hoist row_placeholders out of the per-row loop in insert_batch (sync + async, D-05)</name>
  <files>pycopg/database.py, pycopg/async_database.py</files>
  <read_first>
    - pycopg/database.py L973-993 (sync `insert_batch` batching loop — `row_placeholders` currently recomputed at L983 inside `for row in batch:`)
    - pycopg/async_database.py L616-634 (async `insert_batch` — same invariant recomputed at L624)
    - .planning/phases/38-performance-copy/38-PATTERNS.md "`insert_batch` micro-opt" sync (L127-145) + async (L245-261) — exact target hoist
    - .planning/phases/38-performance-copy/38-CONTEXT.md D-05
    - .planning/phases/38-performance-copy/38-RESEARCH.md "Pattern 4" + insert_batch before/after (L286-298, L559-576)
  </read_first>
  <action>
    In sync `insert_batch` (database.py), move the line `row_placeholders = ", ".join(["%s"] * len(columns))` from inside `for row in batch:` (currently L983) to immediately before that loop (once per batch). The loop body keeps `placeholders.append(f"({row_placeholders})")` and `params.extend(row.get(col) for col in columns)`. `len(columns)` is invariant across rows in a batch, so the produced `row_placeholders` string is identical — this is the byte-exact requirement (D-05). The variable is computed inside the `for i in range(0, len(rows), batch_size):` outer batch loop (columns is fixed for the whole call, so it could also be hoisted above the outer loop — either is byte-exact; follow 38-PATTERNS.md which hoists it just above the per-row loop). Apply the IDENTICAL hoist in async `insert_batch` (async_database.py L624). Change ONLY the computation site — no other edit, no signature change. Per D-05, no additional decision is required.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run ruff check pycopg/database.py pycopg/async_database.py && uv run interrogate -f 100 pycopg/database.py pycopg/async_database.py</automated>
    <automated>cd /home/loc/workspace/pycopg && PGDATABASE=pycopg_test2 uv run pytest tests/test_database.py -k "insert_batch" tests/test_database_integration.py -k "insert_batch" -x -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: in both `database.py` and `async_database.py`, the line `row_placeholders = ", ".join(["%s"] * len(columns))` appears BEFORE `for row in batch:` (not indented under it). Verify with `grep -n -B1 "for row in batch" pycopg/database.py` showing the placeholder assignment precedes the loop, and the assignment no longer appears inside the loop body.
    - The `for row in batch:` body retains `placeholders.append(f"({row_placeholders})")` and `params.extend(row.get(col) for col in columns)` unchanged.
    - `insert_batch` method signatures (sync + async) are byte-identical to before (non-breaking).
    - All existing insert_batch tests pass (`tests/test_database.py -k insert_batch` and `tests/test_database_integration.py -k insert_batch`) under `PGDATABASE=pycopg_test2 ... -o addopts=""`.
    - `uv run ruff check` exits 0; `uv run interrogate -f 100` passes.
  </acceptance_criteria>
  <done>row_placeholders computed once before the per-row loop in both sync and async insert_batch; SQL byte-exact; existing tests + gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Byte-exact non-regression test for insert_batch placeholder hoist (PERF-03)</name>
  <files>tests/test_database.py</files>
  <read_first>
    - tests/test_database.py L855-895 (`test_insert_batch_basic`, `test_insert_batch_empty`, `test_insert_batch_with_conflict` — mock_psycopg pattern + how the executed SQL is captured from the mock cursor)
    - tests/test_database.py L1425-1440 (`test_insert_batch_uses_config_default_batch_size`, `test_insert_batch_explicit_batch_size_overrides_config` — batch-size assertion pattern)
    - tests/test_database_integration.py L82-113 (`test_insert_batch_and_select` — real-DB round-trip pattern)
    - .planning/phases/38-performance-copy/38-PATTERNS.md "insert_batch non-regression" (L453-457)
    - .planning/phases/38-performance-copy/38-VALIDATION.md PERF-03 row (byte-exact non-regression)
  </read_first>
  <action>
    Add a non-regression test (e.g. `test_insert_batch_placeholder_hoist_regression`) to `tests/test_database.py` following the existing `test_insert_batch_basic` mock_psycopg pattern. Insert a small multi-row batch (e.g. 3 rows, 2 columns) and capture the SQL string passed to `cur.execute` from the mock; assert the generated SQL contains exactly the expected `VALUES (%s, %s), (%s, %s), (%s, %s)` shape (one `(%s, %s)` tuple per row, `len(columns)` placeholders each) AND that `params` is the flattened row values in column order — proving the hoist did not change the produced SQL or parameter ordering. Keep it deterministic and fast (mock-based; no timing assertions, D-06). This is the PERF-03 non-regression pin.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run pytest tests/test_database.py -k "insert_batch_placeholder_hoist" -x -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_database.py` contains a passing non-regression test that asserts the `insert_batch` INSERT SQL has exactly one `(%s, ...)` placeholder tuple per row with `len(columns)` placeholders each, and that `params` flattens row values in column order.
    - The test is mock-based and deterministic; no timing/throughput assertions.
    - Test passes under `-o addopts=""`.
  </acceptance_criteria>
  <done>Byte-exact non-regression test pins insert_batch SQL/params shape and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → insert_batch (table/schema/column identifiers) | Identifiers interpolated into the INSERT SQL — identifier surface (UNCHANGED by this plan) |
| caller → insert_batch (row values) | Row values bound as `%s` parameters — parameterized, never string-formatted (UNCHANGED) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-07 | Tampering (SQL injection via identifier) | `insert_batch` INSERT SQL (`INSERT INTO {schema}.{table} ({cols_str}) VALUES ...`) | accept (already mitigated, unchanged) | `insert_batch` already calls `validate_identifiers(table, schema)` and `validate_identifier(col)` for every column BEFORE building any SQL (database.py L964-968). This plan moves ONLY the invariant `row_placeholders` string assignment — it touches no identifier-handling code and introduces no new interpolation. The builder-pur invariant is unchanged. |
| T-38-08 | Tampering (SQL injection via value) | row values bound as `%s` | accept (unchanged) | Values are bound as `%s` parameters via `cur.execute(sql, params)` — never string-interpolated. The hoist does not change `params` content or ordering (pinned by the non-regression test). |
| T-38-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (zero new runtime deps). No install task, no legitimacy checkpoint required. |
</threat_model>

<verification>
- Byte-exact: the produced INSERT SQL and `params` are identical to pre-hoist (pinned by the non-regression test); `row_placeholders` is invariant across rows in a batch.
- Builder-pur unchanged: `validate_identifiers` / `validate_identifier` still run first; no identifier code touched.
- Sync/async parity: identical hoist in both; `test_parity` / `test_accessor_parity` stay green in the wave-merge full suite.
</verification>

<success_criteria>
- `row_placeholders` is computed once before the per-row loop in both sync and async `insert_batch` (D-05).
- The produced SQL and inserted rows are byte-exact identical; a non-regression test pins this.
- `insert_batch` signatures unchanged (non-breaking, no observable change).
- `uv run ruff check pycopg` exits 0; `uv run interrogate -f 100 pycopg` passes; coverage ratchet stays at 94 (pyproject.toml UNCHANGED).
</success_criteria>

<output>
Create `.planning/phases/38-performance-copy/38-03-SUMMARY.md` when done. Note the byte-exact guarantee and the non-regression test name.
</output>
