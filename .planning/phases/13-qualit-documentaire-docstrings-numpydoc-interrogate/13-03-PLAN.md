---
phase: 13-qualit-documentaire-docstrings-numpydoc-interrogate
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - pycopg/database.py
autonomous: true
requirements: [DOC-06]
must_haves:
  truths:
    - "Every Google-style docstring in database.py (~77 sites) is in numpydoc shallow format"
    - "No docstring in database.py contains an Args:, Returns:, Raises:, or Example:/Examples: Google-style label"
    - "No Examples section exists anywhere in database.py docstrings (D-06)"
    - "interrogate stays >= 95 and the Sphinx napoleon guard is warning-free for database.py"
  artifacts:
    - path: "pycopg/database.py"
      provides: "numpydoc-formatted public docstrings"
      contains: "Parameters\n    ----------"
  key_links:
    - from: "pycopg/database.py docstrings"
      to: "sphinx napoleon numpydoc parser"
      via: "Parameters/Returns/Raises numpydoc sections"
      pattern: "Parameters"
---

<objective>
Migrate every Google-style docstring in `pycopg/database.py` (~77 sites, the largest single module) from Google style (`Args:`/`Returns:`/`Raises:`/`Example:`) to numpydoc shallow format (Summary + Parameters + Returns + Raises, NO Examples) per D-06. This is the dedicated database.py plan mandated by D-07 (module-by-module, no big-bang). Docs-only change: no logic, no signatures touched.

Purpose: Homogeneous, Sphinx-rendered API docs; the napoleon guard (Plan 01) validates the form, interrogate validates coverage.
Output: `pycopg/database.py` with all public docstrings converted, message text preserved where Raises sections reference exceptions, Examples sections deleted.
</objective>

<artifacts_this_phase_produces>
- No new symbols — this plan only reformats existing docstrings in `pycopg/database.py`. (Exception conversions and imports come from Plan 02; the napoleon guard and interrogate gate come from Plan 01.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-CONTEXT.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Google-style docstrings to numpydoc in database.py (classmethods, properties, public methods through ~line 1100)</name>
  <files>pycopg/database.py</files>
  <read_first>
    - pycopg/database.py (the full module — every public method/classmethod/property docstring; the first half through ~line 1100)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md (§ pycopg/database.py — the exact Google→numpydoc before/after example for create_from_env, and the Migration rules / Shared Patterns: Numpydoc Section Headers with exact dash counts)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-RESEARCH.md (§ Pattern 1 numpydoc shallow target; § Pitfall 3 numpydoc section alignment)
  </read_first>
  <action>
    For each Google-style docstring in the first half of database.py (through ~line 1100), convert: `Args:` → a `Parameters` header underlined with exactly 10 dashes; `Returns:` → `Returns` underlined with exactly 7 dashes; `Raises:` → `Raises` underlined with exactly 6 dashes. Reformat each parameter as `name : type` on its own line followed by a 4-space-indented description ending in a period; append `, optional` to the type for keyword/default params and `, by default <value>` in the description for defaults. DELETE every `Example:` / `Examples:` section entirely (D-06 — no Examples). Keep the one-line Summary and any free-text body paragraphs. Preserve the exact parameter names from each function signature (napoleon warns if a documented param name does not match the signature — Pitfall 3). Do NOT change any code, signature, type hint, or the exception messages converted in Plan 02. Add a blank line after each section underline before the first entry. After editing, run interrogate and a clean Sphinx build to catch malformed sections early.
  </action>
  <verify>
    <automated>grep -nE "^\s*(Args|Example|Examples):" pycopg/database.py | head; echo "google-labels-remaining(first-half check via full grep below)"; uv run interrogate pycopg --fail-under 95 --quiet; echo "interrogate exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - In the migrated region, `grep -nE "^\s*Args:" pycopg/database.py` returns nothing for already-migrated methods.
    - No `Example:` or `Examples:` label remains in the migrated region.
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
    - `uv run python -c "import pycopg.database"` exits 0 (no syntax error from malformed docstrings).
  </acceptance_criteria>
  <done>First half of database.py docstrings are numpydoc shallow; no Args/Example labels; interrogate green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining database.py docstrings (~line 1100 to end) and verify whole-module + Sphinx guard</name>
  <files>pycopg/database.py</files>
  <read_first>
    - pycopg/database.py (second half from ~line 1100 to end — includes TimescaleDB/PostGIS methods whose Raises sections now reference ExtensionNotAvailable/DatabaseExists from Plan 02)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md (§ Shared Patterns — Numpydoc Section Headers; dash counts)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-01-SUMMARY.md (which Sphinx guard mechanism — `-W` or stderr-grep — was empirically chosen)
  </read_first>
  <action>
    Apply the same Google→numpydoc migration rules to the remaining database.py docstrings (from ~line 1100 to end of file). For methods that raise the converted exceptions (the TimescaleDB/PostGIS extension methods and `create`), add or update a `Raises` section naming `ExtensionNotAvailable` / `DatabaseExists` with a one-line "when raised" description — do NOT add Examples. Delete every remaining `Example:`/`Examples:` section. After the whole module is migrated, run the full module verification: confirm zero Google-style `Args:`/`Returns:`/`Raises:`/`Example:` labels remain anywhere in database.py, interrogate >= 95, and a clean Sphinx build (using the mechanism recorded in 13-01-SUMMARY) is warning-free.
  </action>
  <verify>
    <automated>grep -cE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/database.py; echo "^ MUST be 0 (no Google-style labels left)"; uv run interrogate pycopg --fail-under 95 --quiet && echo "interrogate OK"; uv run pip install -r docs/requirements.txt -q && uv run sphinx-build -W --keep-going -b html docs docs/_build/html 2>&1 | tail -5; echo "sphinx exit: ${PIPESTATUS[1]}"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/database.py` returns 0 (Google labels fully removed; numpydoc uses underlined headers, not colon labels).
    - `grep -c "Parameters" pycopg/database.py` is > 0 (numpydoc sections present).
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
    - The clean Sphinx build (chosen guard mechanism) exits 0 / is warning-free for database.py docstrings.
    - `uv run pytest tests/test_database.py tests/test_database_integration.py -x -o addopts="" -q` exits 0 (no behavior regression).
  </acceptance_criteria>
  <done>Entire database.py is numpydoc shallow; no Google labels; interrogate + Sphinx guards green; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Docstring-only reformatting introduces no trust boundary; no inputs, endpoints, or generated SQL change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Information Disclosure | Docstring text rendered in public Sphinx docs | accept | Docstrings describe public API behavior only; no secrets, credentials, or internal-only details are added — Examples (which could leak patterns) are removed, not added (D-06) |
| T-13-03-SC | Tampering | package installs | N/A | No package installs in this plan |
</threat_model>

<verification>
- `grep -cE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/database.py` returns 0.
- `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
- Clean Sphinx build is warning-free for database.py.
- `uv run pytest tests/test_database.py tests/test_database_integration.py -x -o addopts="" -q` exits 0.
</verification>

<success_criteria>
- All ~77 database.py docstrings migrated to numpydoc shallow, no Examples (DOC-06).
- interrogate >= 95 and Sphinx napoleon guard warning-free.
- No behavior change (tests green).
</success_criteria>

<output>
Create `.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-03-SUMMARY.md` when done.
</output>
