---
phase: 13-qualit-documentaire-docstrings-numpydoc-interrogate
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - pycopg/async_database.py
autonomous: true
requirements: [DOC-06]
must_haves:
  truths:
    - "Every Google-style docstring in async_database.py (~68 sites) is in numpydoc shallow format"
    - "No docstring in async_database.py contains an Args:, Returns:, Raises:, or Example:/Examples: Google-style label"
    - "No Examples section exists anywhere in async_database.py docstrings (D-06)"
    - "interrogate stays >= 95 and the Sphinx napoleon guard is warning-free for async_database.py"
  artifacts:
    - path: "pycopg/async_database.py"
      provides: "numpydoc-formatted public docstrings"
      contains: "Parameters\n    ----------"
  key_links:
    - from: "pycopg/async_database.py docstrings"
      to: "sphinx napoleon numpydoc parser"
      via: "Parameters/Returns/Raises numpydoc sections"
      pattern: "Parameters"
---

<objective>
Migrate every Google-style docstring in `pycopg/async_database.py` (~68 sites) from Google style to numpydoc shallow format (Summary + Parameters + Returns + Raises, NO Examples) per D-06. This is the dedicated async_database.py plan mandated by D-07. Runs in parallel with Plan 03 (disjoint file). Docs-only change; no logic or signatures touched. The class docstring's `Example:` block (lines 62-77) and the `__init__` `Args:` block (lines 83-85) are included in scope.

Purpose: Sync/async docstring parity in the same numpydoc form; napoleon guard validates form, interrogate validates coverage.
Output: `pycopg/async_database.py` with all public docstrings converted, the class-level Example deleted, Raises sections referencing the Plan-02 exception types.
</objective>

<artifacts_this_phase_produces>
- No new symbols — this plan only reformats existing docstrings in `pycopg/async_database.py`. (The `async_engine -> AsyncEngine` annotation and exception conversions are Plan 02; gates are Plan 01.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-CONTEXT.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Google-style docstrings to numpydoc in async_database.py (class docstring + first half through ~line 1100)</name>
  <files>pycopg/async_database.py</files>
  <read_first>
    - pycopg/async_database.py (class docstring with `Example:` at lines 58-78; `__init__` `Args:` at lines 81-85; `async_engine` property at lines 90-97; first half through ~line 1100)
    - pycopg/database.py (sync analog — the same methods already migrated in Plan 03 are the reference for wording/format; mirror them)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md (§ pycopg/async_database.py + § Shared Patterns — Numpydoc Section Headers, exact dash counts)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-RESEARCH.md (§ Pattern 1 + § Pitfall 3 numpydoc section alignment)
  </read_first>
  <action>
    Convert each Google-style docstring in the class docstring and first half of async_database.py (through ~line 1100): `Args:` → `Parameters` + 10-dash underline; `Returns:` → `Returns` + 7-dash underline; `Raises:` → `Raises` + 6-dash underline; parameter format `name : type` + 4-space-indented description ending in a period, `, optional` for keyword/default params, `, by default <value>` for defaults. DELETE the class-level `Example:` block (lines ~62-77) and every other `Example:`/`Examples:` section (D-06). Preserve exact signature parameter names (napoleon matches them — Pitfall 3). Do NOT touch the `async_engine` annotation or `_async_engine` type (already done in Plan 02) beyond reformatting its docstring. Do NOT change any code or signature. Mirror the wording of the corresponding sync method docstrings in database.py for consistency where the methods are parallel.
  </action>
  <verify>
    <automated>grep -nE "^\s*(Args|Example|Examples):" pycopg/async_database.py | head; uv run interrogate pycopg --fail-under 95 --quiet; echo "interrogate exit: $?"; uv run python -c "import pycopg.async_database; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The class docstring no longer contains an `Example:` block.
    - In the migrated region, no `Args:` or `Example:`/`Examples:` label remains.
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
    - `uv run python -c "import pycopg.async_database"` exits 0.
  </acceptance_criteria>
  <done>Class docstring + first half of async_database.py are numpydoc shallow; no Args/Example labels; interrogate green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining async_database.py docstrings (~line 1100 to end) and verify whole-module + Sphinx guard</name>
  <files>pycopg/async_database.py</files>
  <read_first>
    - pycopg/async_database.py (second half from ~line 1100 to end — TimescaleDB/PostGIS methods whose Raises sections reference ExtensionNotAvailable from Plan 02)
    - pycopg/database.py (already-migrated sync analogs for the same second-half methods)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-01-SUMMARY.md (chosen Sphinx guard mechanism)
  </read_first>
  <action>
    Apply the same Google→numpydoc rules to the remaining async_database.py docstrings (from ~line 1100 to end). For the extension methods and async `create`, add/update a `Raises` section naming `ExtensionNotAvailable` / `DatabaseExists` with a one-line description — no Examples. Delete every remaining `Example:`/`Examples:` section. After the whole module is migrated, verify: zero Google-style `Args:`/`Returns:`/`Raises:`/`Example:` labels remain anywhere in async_database.py, interrogate >= 95, and the chosen Sphinx guard build is warning-free.
  </action>
  <verify>
    <automated>grep -cE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/async_database.py; echo "^ MUST be 0"; uv run interrogate pycopg --fail-under 95 --quiet && echo "interrogate OK"; uv run pip install -r docs/requirements.txt -q && uv run sphinx-build -W --keep-going -b html docs docs/_build/html 2>&1 | tail -5; echo "sphinx exit: ${PIPESTATUS[1]}"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/async_database.py` returns 0.
    - `grep -c "Parameters" pycopg/async_database.py` is > 0.
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
    - The clean Sphinx build (chosen guard mechanism) is warning-free for async_database.py docstrings.
    - `uv run pytest tests/test_async_database.py -x -o addopts="" -q` exits 0 (no behavior regression).
  </acceptance_criteria>
  <done>Entire async_database.py is numpydoc shallow; no Google labels; interrogate + Sphinx guards green; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Docstring-only reformatting introduces no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | Docstring text rendered in public Sphinx docs | accept | Public-API descriptions only; no secrets added; Examples removed (D-06) |
| T-13-04-SC | Tampering | package installs | N/A | No package installs in this plan |
</threat_model>

<verification>
- `grep -cE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/async_database.py` returns 0.
- `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
- Clean Sphinx build warning-free for async_database.py.
- `uv run pytest tests/test_async_database.py -x -o addopts="" -q` exits 0.
</verification>

<success_criteria>
- All ~68 async_database.py docstrings migrated to numpydoc shallow, no Examples (DOC-06).
- interrogate >= 95 and Sphinx napoleon guard warning-free.
- No behavior change (tests green).
</success_criteria>

<output>
Create `.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-04-SUMMARY.md` when done.
</output>
