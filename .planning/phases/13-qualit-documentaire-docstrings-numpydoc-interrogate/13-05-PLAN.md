---
phase: 13-qualit-documentaire-docstrings-numpydoc-interrogate
plan: 05
type: execute
wave: 3
depends_on: ["13-01"]
files_modified:
  - pycopg/base.py
  - pycopg/config.py
  - pycopg/utils.py
  - pycopg/migrations.py
  - pycopg/pool.py
autonomous: true
requirements: [DOC-06]
must_haves:
  truths:
    - "Every Google-style docstring in base.py, config.py, utils.py, migrations.py, pool.py is in numpydoc shallow format (~42 sites total)"
    - "None of these 5 modules contains an Args:, Returns:, Raises:, or Example:/Examples: Google-style label"
    - "No Examples section exists in any of these 5 modules (D-06)"
    - "interrogate stays >= 95 and the Sphinx napoleon guard is warning-free for all 5 modules"
  artifacts:
    - path: "pycopg/base.py"
      provides: "numpydoc-formatted shared-base docstrings (inherited by Database/AsyncDatabase)"
      contains: "Parameters"
    - path: "pycopg/utils.py"
      provides: "numpydoc-formatted utility docstrings"
      contains: "Parameters"
  key_links:
    - from: "pycopg/base.py docstrings"
      to: "sphinx napoleon numpydoc parser"
      via: "Parameters/Returns/Raises numpydoc sections"
      pattern: "Parameters"
---

<objective>
Migrate the Google-style docstrings in the 5 remaining modules — `base.py` (~5), `config.py` (~7), `utils.py` (~10), `migrations.py` (~7), `pool.py` (~13) — to numpydoc shallow format per D-06. This is the grouped "rest" plan mandated by D-07. Runs in parallel with Plans 03 and 04 (disjoint files). `base.py` is the Phase-12 shared socle (`DatabaseBase`, `QueryMixin`); migrating it gives Database and AsyncDatabase correctly-formatted inherited docstrings.

Purpose: Complete the docstring migration across the whole package so interrogate + the Sphinx guard pass for the entire codebase.
Output: 5 source modules with all public docstrings in numpydoc shallow form, no Examples.
</objective>

<artifacts_this_phase_produces>
- No new symbols — this plan only reformats existing docstrings in base.py, config.py, utils.py, migrations.py, pool.py.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-CONTEXT.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate base.py, config.py, utils.py docstrings to numpydoc</name>
  <files>pycopg/base.py, pycopg/config.py, pycopg/utils.py</files>
  <read_first>
    - pycopg/base.py (5 Google-style docstrings; `__init__` `Args:` at lines 25-29; `from_env`/`from_url` classmethods with both `Args:` and `Returns:`)
    - pycopg/config.py (~7 Google-style docstrings)
    - pycopg/utils.py (~10 Google-style docstrings — all util functions have `Args:`; some have `Raises:`)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md (§ pycopg/base.py before/after example; § pycopg/utils.py; § pycopg/config.py; § Shared Patterns — Numpydoc Section Headers with exact dash counts)
  </read_first>
  <action>
    Convert every Google-style docstring in base.py, config.py, and utils.py: `Args:` → `Parameters` + 10-dash underline; `Returns:` → `Returns` + 7-dash underline; `Raises:` → `Raises` + 6-dash underline; parameter format `name : type` + 4-space-indented description ending in a period, `, optional` for keyword/default params, `, by default <value>` for defaults. DELETE any `Example:`/`Examples:` sections (D-06). Preserve exact signature parameter names. For utils.py functions that document a `Raises:` (e.g. validate_identifier raising InvalidIdentifier), keep the `Raises` section naming the existing exception type — no new exception types, no Examples. Do NOT change any code, signature, or behavior.
  </action>
  <verify>
    <automated>grep -cE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/base.py pycopg/config.py pycopg/utils.py; echo "^ each MUST be 0"; uv run python -c "import pycopg.base, pycopg.config, pycopg.utils; print('import ok')"; uv run interrogate pycopg --fail-under 95 --quiet; echo "interrogate exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/base.py pycopg/config.py pycopg/utils.py` reports 0 for each file.
    - `grep -c "Parameters" pycopg/base.py` and `pycopg/utils.py` are > 0.
    - `uv run python -c "import pycopg.base, pycopg.config, pycopg.utils"` exits 0.
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
  </acceptance_criteria>
  <done>base.py, config.py, utils.py docstrings are numpydoc shallow; no Google labels; interrogate green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate migrations.py + pool.py docstrings and run the whole-package Sphinx guard</name>
  <files>pycopg/migrations.py, pycopg/pool.py</files>
  <read_first>
    - pycopg/migrations.py (~7 Google-style docstrings; `Args:`/`Returns:`/`Raises:` pattern around lines 40-65)
    - pycopg/pool.py (~13 Google-style docstrings)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md (§ pycopg/migrations.py; § pycopg/pool.py; § Shared Patterns dash counts)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-01-SUMMARY.md (chosen Sphinx guard mechanism)
  </read_first>
  <action>
    Apply the same Google→numpydoc migration rules to migrations.py and pool.py. For migrations.py methods that raise `MigrationError`, keep a `Raises` section naming it — no new types, no Examples. Delete any `Example:`/`Examples:` sections. Then run the WHOLE-PACKAGE final check for this plan's scope: confirm zero Google-style labels remain across all 5 modules, interrogate >= 95 for the whole `pycopg` package, and the chosen Sphinx guard build is warning-free.
  </action>
  <verify>
    <automated>grep -cE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/migrations.py pycopg/pool.py pycopg/base.py pycopg/config.py pycopg/utils.py; echo "^ each MUST be 0"; uv run interrogate pycopg --fail-under 95 --quiet && echo "interrogate OK"; uv run pip install -r docs/requirements.txt -q && uv run sphinx-build -W --keep-going -b html docs docs/_build/html 2>&1 | tail -5; echo "sphinx exit: ${PIPESTATUS[1]}"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s*(Args|Returns|Raises|Example|Examples):"` reports 0 for migrations.py, pool.py, base.py, config.py, utils.py.
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
    - The clean Sphinx build (chosen guard mechanism) is warning-free for these 5 modules.
    - `uv run pytest tests/test_migrations.py tests/test_pool.py tests/test_config.py -x -o addopts="" -q` exits 0 (no behavior regression; adjust to actual test file names if different).
  </acceptance_criteria>
  <done>migrations.py + pool.py migrated; all 5 grouped modules numpydoc shallow; interrogate + Sphinx guards green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Docstring-only reformatting introduces no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Information Disclosure | Docstring text rendered in public Sphinx docs | accept | Public-API descriptions only; no secrets added; Examples removed (D-06) |
| T-13-05-SC | Tampering | package installs | N/A | No package installs in this plan |
</threat_model>

<verification>
- `grep -cE "^\s*(Args|Returns|Raises|Example|Examples):"` returns 0 for all 5 modules.
- `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
- Clean Sphinx build warning-free for all 5 modules.
- Targeted tests for these modules pass.
</verification>

<success_criteria>
- All ~42 docstrings in the 5 grouped modules migrated to numpydoc shallow, no Examples (DOC-06).
- interrogate >= 95 and Sphinx napoleon guard warning-free.
- No behavior change (tests green).
</success_criteria>

<output>
Create `.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-05-SUMMARY.md` when done.
</output>
