---
phase: 13-qualit-documentaire-docstrings-numpydoc-interrogate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - docs/conf.py
  - docs/api-autodoc.md
  - docs/index.md
  - pycopg/exceptions.py
  - pycopg/__init__.py
  - tests/test_version.py
  - .github/workflows/tests.yml
autonomous: false
requirements: [DOC-07, DOC-08, DOC-10, DOC-11]
must_haves:
  truths:
    - "`uv run interrogate pycopg --fail-under 95 --quiet` exits 0 against the current codebase"
    - "`pycopg.__version__` is resolved from installed package metadata, not a hardcoded literal"
    - "`DatabaseExists` exception type exists, inherits PycopgError only, and is exported from the package"
    - "A Sphinx autodoc surface exists so napoleon actually parses pycopg docstrings during the build"
    - "It is empirically known whether `sphinx-build -W` escalates napoleon malformed-section warnings to a non-zero exit; the CI guard uses a mechanism proven to fail on a malformed numpydoc section"
    - "CI runs interrogate (blocking), the Sphinx napoleon guard (blocking), and mypy (non-blocking)"
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.interrogate] + [tool.mypy] config and interrogate/mypy dev deps"
      contains: "[tool.interrogate]"
    - path: "pycopg/exceptions.py"
      provides: "DatabaseExists exception type"
      contains: "class DatabaseExists(PycopgError)"
    - path: "docs/api-autodoc.md"
      provides: "autodoc surface that exercises napoleon on pycopg modules"
    - path: "tests/test_version.py"
      provides: "DOC-10 version test"
    - path: ".github/workflows/tests.yml"
      provides: "interrogate (blocking) + sphinx guard (blocking) + mypy (non-blocking) CI steps"
  key_links:
    - from: "pycopg/__init__.py"
      to: "importlib.metadata.version"
      via: "version('pycopg') with PackageNotFoundError fallback"
      pattern: "importlib.metadata"
    - from: ".github/workflows/tests.yml"
      to: "uv run interrogate pycopg --fail-under 95"
      via: "blocking CI step"
      pattern: "interrogate"
    - from: ".github/workflows/tests.yml"
      to: "sphinx napoleon guard"
      via: "blocking CI step (sphinx-build -W or stderr-grep fallback)"
      pattern: "sphinx-build"
---

<objective>
Lay the measurement and tooling foundation for the documentary-quality phase, BEFORE any docstring is migrated. Add interrogate + mypy (dev deps + config), wire all three CI gates (interrogate blocking, Sphinx napoleon guard blocking, mypy non-blocking), measure the interrogate baseline, fix `__version__` via importlib.metadata (DOC-10), and add the `DatabaseExists` exception type (D-02) plus a Sphinx autodoc surface so the D-08 napoleon guard has Python docstrings to parse.

This plan also resolves the phase's biggest open risk (RESEARCH Open Question 1 / Assumption A2): the existing Sphinx docs are hand-written Markdown with NO autodoc directives, so a plain `sphinx-build -W` never touches a single Python docstring and would be a no-op guard. This plan creates an autodoc page and empirically verifies the guard actually fails on a malformed numpydoc section.

Purpose: Establish the gates that validate the docstring migration (Plans 03-05) and the runtime artifact (`DatabaseExists`) that the exception conversion (Plan 02) depends on.
Output: Configured pyproject.toml, two new exception/version source patches, a new version test, an autodoc docs page, and three CI steps.
</objective>

<artifacts_this_phase_produces>
Symbols and files this phase introduces (exclude from drift/source-grounding checks — they are new, not pre-existing):
- `pycopg.exceptions.DatabaseExists` — new exception class (this plan)
- `tests/test_version.py` — new test module (this plan)
- `docs/api-autodoc.md` — new Sphinx autodoc page (this plan)
- `[tool.interrogate]` and `[tool.mypy]` (+ `[[tool.mypy.overrides]]`) — new pyproject.toml sections (this plan)
- `interrogate` and `mypy` — new dev dependencies (this plan)
- CI steps: "Check docstring coverage", "Build Sphinx docs (napoleon non-regression)", "mypy type check" (this plan)
- `pycopg.async_database.AsyncEngine` annotation on `async_engine` (Plan 02)
- `DatabaseExists` raised at `create()` sites; `ExtensionNotAvailable` raised at the 14 extension-check sites (Plan 02)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-CONTEXT.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-RESEARCH.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy for interrogate and mypy (slopcheck gate)</name>
  <read_first>
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-RESEARCH.md (§ Package Legitimacy Audit — both packages tagged [ASSUMED] because slopcheck was unavailable at research time)
  </read_first>
  <action>
    Pause for the human legitimacy gate. RESEARCH.md marked both `interrogate` and `mypy` as `[ASSUMED]` (slopcheck not installable at research time), so per the package legitimacy protocol the human MUST confirm both on pypi.org before any install runs. No automation in this task — present the verification steps below and wait for "approved".
  </action>
  <what-built>
    Nothing automated yet — this is the mandatory pre-install legitimacy gate for the two [ASSUMED] dev packages.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/interrogate/ — confirm it is the docstring-coverage tool by Lynn Root (`github.com/econchick/interrogate`), ~6 years old, widely used.
    2. Open https://pypi.org/project/mypy/ — confirm it is the official static type checker maintained by the `python/mypy` org.
    3. Confirm the planned versions (`interrogate>=1.7.0`, `mypy>=2.1.0`) match the current published releases.
  </how-to-verify>
  <verify>
    <human-check>Human confirms both packages are legitimate on pypi.org and types "approved"; if suspicious, the plan stops before any install.</human-check>
  </verify>
  <acceptance_criteria>
    - Human types "approved" after confirming both packages are legitimate on pypi.org.
    - If either package is suspicious, human responds with the concern and the plan stops before the install task.
  </acceptance_criteria>
  <done>Human approved both packages OR raised a concern that halts the install tasks.</done>
  <resume-signal>Type "approved" to proceed with installing interrogate and mypy, or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add interrogate + mypy dev deps and [tool.*] config, then measure the interrogate baseline</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current `[dependency-groups]` dev list at lines 62-69; existing `[tool.*]` sections at lines 80-103 — append AFTER `[tool.coverage.report]`)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-RESEARCH.md (§ Configuration Details — exact `[tool.interrogate]` and `[tool.mypy]` config to copy)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md (§ pyproject.toml — verified config block to append)
  </read_first>
  <action>
    Add `"interrogate>=1.7.0"` and `"mypy>=2.1.0"` to the `dev` list under `[dependency-groups]`. Append a `[tool.interrogate]` section with exactly: ignore-init-method=true, ignore-init-module=false, ignore-magic=false, ignore-semiprivate=false, ignore-private=false, ignore-module=false, ignore-property-decorators=false, fail-under=95, exclude=["tests","docs","setup.py"], verbose=0, quiet=false, color=true (per DOC-07, verified from interrogate's own pyproject.toml). Append a `[tool.mypy]` section with python_version="3.11", warn_return_any=true, warn_unused_configs=true, ignore_missing_imports=false, disallow_untyped_defs=false, check_untyped_defs=true, no_implicit_optional=true (permissive per D-05/DOC-11 — NO `strict`). Append `[[tool.mypy.overrides]]` blocks with ignore_missing_imports=true for modules: sqlalchemy.*, psycopg.*, psycopg_pool.*, geopandas.*, geoalchemy2.*, shapely.*, tenacity.*. Then run `uv sync --all-extras --dev` and `uv run interrogate pycopg --verbose` to measure the current baseline (RESEARCH Open Question 3 / Assumption A5: public coverage is 100% by AST but the interrogate default scope includes private/magic). Record the measured baseline percentage in the SUMMARY. If the measured baseline is below 95, DO NOT lower fail-under — instead document which private/magic symbols are undocumented and add their docstrings (this is in-scope foundation work), keeping fail-under=95 per the milestone convention.
  </action>
  <verify>
    <automated>uv sync --all-extras --dev && uv run interrogate pycopg --fail-under 95 --quiet; echo "interrogate exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `[tool.interrogate]` with `fail-under = 95`.
    - `pyproject.toml` contains `[tool.mypy]` with `disallow_untyped_defs = false` and at least the 7 listed `[[tool.mypy.overrides]]` modules.
    - `dev` list in `[dependency-groups]` contains `interrogate>=1.7.0` and `mypy>=2.1.0`.
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
    - SUMMARY records the measured interrogate baseline percentage.
  </acceptance_criteria>
  <done>interrogate + mypy installed and configured; interrogate passes at fail-under 95; baseline recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Add DatabaseExists exception, fix __version__ via importlib.metadata, add version test</name>
  <files>pycopg/exceptions.py, pycopg/__init__.py, tests/test_version.py</files>
  <read_first>
    - pycopg/exceptions.py (current hierarchy lines 1-38; append after `MigrationError` at line 38)
    - pycopg/__init__.py (line 63 `__version__ = "0.3.1"`; import block lines 47-55; `__all__` lines 64-89 — `"__version__"` MUST stay in `__all__` at line 88)
    - tests/test_exceptions.py (lines 1-45 — import + class-structure pattern for the new test file)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md (§ pycopg/exceptions.py, § pycopg/__init__.py, § tests/test_version.py — exact target code)
  </read_first>
  <action>
    In `pycopg/exceptions.py`, add `class DatabaseExists(PycopgError)` with a one-line docstring `"""Database already exists."""` and a `pass` body, single inheritance only per D-03/D-02 (NOT `(PycopgError, ValueError)`). In `pycopg/__init__.py`, replace the hardcoded `__version__ = "0.3.1"` at line 63 with a try/except that does `from importlib.metadata import version, PackageNotFoundError` then `__version__ = version("pycopg")`, with `except PackageNotFoundError: __version__ = "0.0.0+unknown"` (per D-09; verified `version("pycopg")` returns "0.3.1" in the uv env). Add `DatabaseExists` to the `from pycopg.exceptions import (...)` block AND to `__all__` in the Exceptions group. Keep `"__version__"` in `__all__`. Create `tests/test_version.py` with a `TestVersion` class asserting: `pycopg.__version__` is a non-empty `str`, it splits into >=2 dot-separated parts, and `"__version__" in pycopg.__all__` (DOC-10). Do NOT assert an exact version literal (the value now derives from package metadata).
  </action>
  <verify>
    <automated>uv run pytest tests/test_version.py tests/test_exceptions.py -o addopts="" -q && uv run python -c "import pycopg; from pycopg.exceptions import DatabaseExists, PycopgError; assert issubclass(DatabaseExists, PycopgError); assert not issubclass(DatabaseExists, ValueError); assert isinstance(pycopg.__version__, str) and pycopg.__version__; print('ok', pycopg.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `pycopg/exceptions.py` contains `class DatabaseExists(PycopgError)`.
    - `DatabaseExists` is NOT a subclass of `ValueError` (single inheritance per D-03).
    - `pycopg/__init__.py` contains `importlib.metadata` and no longer contains the literal `__version__ = "0.3.1"`.
    - `"__version__"` and `"DatabaseExists"` both appear in `pycopg.__all__`.
    - `uv run pytest tests/test_version.py -o addopts="" -q` exits 0.
  </acceptance_criteria>
  <done>DatabaseExists exported; __version__ derives from importlib.metadata; version test green.</done>
</task>

<task type="auto">
  <name>Task 4: Add napoleon numpy flag + Sphinx autodoc surface, then empirically verify the D-08 guard catches a malformed numpydoc section</name>
  <files>docs/conf.py, docs/api-autodoc.md, docs/index.md</files>
  <read_first>
    - docs/conf.py (extensions list lines 21-28 — `sphinx.ext.napoleon` already present at line 25; autodoc options lines 92-98; add napoleon block after the extensions list)
    - docs/index.md (toctree — the new autodoc page must be reachable from a toctree or autodoc never runs)
    - docs/api-reference.md (current API page is hand-written Markdown with NO autodoc directives — this is why a plain sphinx-build never parses docstrings; the new page fixes that)
    - docs/requirements.txt (sphinx, myst-parser, furo, sphinx-copybutton — the deps the CI guard installs)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-RESEARCH.md (§ Open Questions 1, Pitfall 2, Assumption A2 — guard must actually exercise docstrings; autodoc may need autodoc_mock_imports for optional deps)
  </read_first>
  <action>
    Add `napoleon_numpy_docstring = True` and `show_warning_types = True` to `docs/conf.py` (after the extensions list). Keep `napoleon_google_docstring` at its default True during migration (do NOT disable it now — disabling mid-migration breaks unconverted modules; final disable is Plan 06 per Pattern 3). Create `docs/api-autodoc.md` as a MyST page that uses an `{eval-rst}` fenced block containing `.. automodule::` directives for `pycopg.database`, `pycopg.async_database`, `pycopg.base`, `pycopg.config`, `pycopg.utils`, `pycopg.migrations`, `pycopg.pool`, and `pycopg.exceptions` (with `:members:`), so napoleon actually parses every migrated docstring during the build. Add this page to the toctree in `docs/index.md`. If the build raises ImportError on optional deps (geopandas/geoalchemy2/shapely) per Pitfall 2, add `autodoc_mock_imports = ["geopandas", "geoalchemy2", "shapely"]` to conf.py. Then run the EMPIRICAL guard check (RESEARCH Open Question 1 / Assumption A2): temporarily insert a deliberately malformed numpydoc section (e.g. a `Parameters` header with the wrong number of dashes) into one docstring, run `uv run pip install -r docs/requirements.txt && uv run sphinx-build -W --keep-going -b html docs docs/_build/html`, and confirm it exits non-zero. If `-W` does NOT escalate the napoleon warning to a failure (Sphinx #9142), fall back to a guard that runs `sphinx-build` without `-W` and greps stderr for `WARNING`, exiting non-zero on any match — record which mechanism was chosen in the SUMMARY. Revert the deliberate malformation before finishing. The clean build (no malformation) MUST exit 0.
  </action>
  <verify>
    <automated>uv run pip install -r docs/requirements.txt -q && uv run sphinx-build -W --keep-going -b html docs docs/_build/html 2>&1 | tail -5; echo "clean-build exit: ${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/conf.py` contains `napoleon_numpy_docstring = True`.
    - `docs/api-autodoc.md` contains `.. automodule:: pycopg.database` (and the other 7 modules) inside an `{eval-rst}` block.
    - The new page is referenced from a toctree in `docs/index.md`.
    - SUMMARY documents the empirical result: whether `sphinx-build -W` caught a deliberately malformed numpydoc section, and which guard mechanism (`-W` vs stderr-grep) Plan 06 / CI must use.
    - The clean Sphinx build (no deliberate malformation) exits 0.
  </acceptance_criteria>
  <done>napoleon numpy flag enabled; autodoc surface parses all pycopg docstrings; D-08 guard mechanism empirically chosen and proven to fail on a malformed section.</done>
</task>

<task type="auto">
  <name>Task 5: Wire CI gates — interrogate (blocking), Sphinx napoleon guard (blocking), mypy (non-blocking)</name>
  <files>.github/workflows/tests.yml</files>
  <read_first>
    - .github/workflows/tests.yml (existing steps lines 32-58; the "Run tests" step at lines 52-58 — append the new steps after it; deps installed via `uv sync --locked --all-extras --dev` at line 42)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md (§ .github/workflows/tests.yml — exact step blocks to append)
    - 13-01 Task 4 SUMMARY note (which Sphinx guard mechanism was empirically chosen: `-W` or stderr-grep fallback)
  </read_first>
  <action>
    Append three steps after the "Run tests" step. (1) "Check docstring coverage" — `run: uv run interrogate pycopg --fail-under 95 --quiet` (blocking, DOC-07). (2) "Build Sphinx docs (napoleon non-regression)" — install docs deps with `uv run pip install -r docs/requirements.txt` then run the guard command chosen in Task 4: `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` if `-W` was proven to catch napoleon warnings, OR the stderr-grep fallback if not (blocking, DOC-08/D-08). (3) "mypy type check" — set `continue-on-error: true` and `run: uv run mypy pycopg/` (NON-blocking per D-05/DOC-11). `continue-on-error: true` MUST be on the mypy step ONLY — interrogate and sphinx-build stay blocking. Because `uv sync` uses `--locked`, ensure `uv.lock` is regenerated (it should already be updated by Task 2's `uv sync`); commit the updated lockfile alongside this change.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*#' .github/workflows/tests.yml | grep -c "interrogate pycopg --fail-under 95"; grep -c "continue-on-error: true" .github/workflows/tests.yml; grep -c "sphinx-build" .github/workflows/tests.yml</automated>
  </verify>
  <acceptance_criteria>
    - `tests.yml` contains a step running `uv run interrogate pycopg --fail-under 95 --quiet` with no `continue-on-error`.
    - `tests.yml` contains a Sphinx build step (matching the Task 4 mechanism) with no `continue-on-error`.
    - `tests.yml` contains a mypy step `uv run mypy pycopg/` WITH `continue-on-error: true`.
    - `continue-on-error: true` appears exactly once (mypy step only).
    - `uv.lock` reflects the new interrogate/mypy deps.
  </acceptance_criteria>
  <done>Three CI steps wired: interrogate + sphinx blocking, mypy non-blocking; lockfile updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine / CI runner → PyPI | New dev dependencies (interrogate, mypy) and docs deps are fetched from PyPI during `uv sync` / `pip install -r docs/requirements.txt` |
| source → raised exception message | `DatabaseExists` carries a message string surfaced to callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | npm/pip installs (interrogate, mypy, docs deps) | mitigate | slopcheck + blocking human checkpoint (Task 1) for the [ASSUMED] packages; versions pinned `>=1.7.0` / `>=2.1.0` and locked in `uv.lock` (`uv sync --locked`) |
| T-13-02 | Information Disclosure | `DatabaseExists` message text | accept | Message is the verbatim, pre-existing `"Database '{name}' already exists"` string — only the Python type changes, no new information exposed (RESEARCH § Security Domain) |
| T-13-03 | Tampering | docs/api-autodoc.md autodoc importing pycopg in CI | accept | autodoc imports the project's own already-trusted code under the existing `--all-extras` env; optional deps mocked via `autodoc_mock_imports` if needed, no third-party code executed beyond Sphinx itself |
| T-13-SC | Tampering | pip/uv installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages (Task 1) |
</threat_model>

<verification>
- `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
- `uv run pytest tests/test_version.py tests/test_exceptions.py -o addopts="" -q` exits 0.
- `uv run python -c "from pycopg.exceptions import DatabaseExists, PycopgError; assert issubclass(DatabaseExists, PycopgError)"` exits 0.
- Clean `sphinx-build` exits 0; the malformed-section experiment proved the guard mechanism fails non-zero.
- `tests.yml` has interrogate + sphinx blocking and mypy non-blocking.
</verification>

<success_criteria>
- interrogate + mypy installed, configured, baseline measured (DOC-07, DOC-11).
- `__version__` derives from importlib.metadata (DOC-10).
- `DatabaseExists` exists and is exported (foundation for Plan 02 / DOC-09).
- napoleon_numpy_docstring enabled + autodoc surface created (DOC-08).
- D-08 Sphinx guard mechanism empirically chosen and wired blocking in CI; interrogate blocking; mypy non-blocking.
</success_criteria>

<output>
Create `.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-01-SUMMARY.md` when done. MUST record: measured interrogate baseline %, and which Sphinx guard mechanism (`-W` vs stderr-grep) was empirically chosen for D-08.
</output>
