---
phase: 13-qualit-documentaire-docstrings-numpydoc-interrogate
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - pycopg/database.py
  - pycopg/async_database.py
  - tests/test_database_integration.py
  - tests/test_async_database.py
autonomous: true
requirements: [DOC-09, DOC-12]
must_haves:
  truths:
    - "The 14 extension-not-installed sites raise ExtensionNotAvailable instead of RuntimeError"
    - "The 2 'Database already exists' sites raise DatabaseExists instead of ValueError"
    - "Exception message text is preserved verbatim — only the raised type changes"
    - "API-misuse / state / subprocess errors are NOT converted (stay ValueError/RuntimeError per D-01)"
    - "Tests that asserted RuntimeError/ValueError on the converted sites now assert the new types and pass"
    - "async_engine property is annotated -> AsyncEngine via a TYPE_CHECKING guard"
  artifacts:
    - path: "pycopg/database.py"
      provides: "ExtensionNotAvailable + DatabaseExists raises at 7 domain sites"
      contains: "ExtensionNotAvailable"
    - path: "pycopg/async_database.py"
      provides: "ExtensionNotAvailable + DatabaseExists raises at 7 sites; async_engine -> AsyncEngine"
      contains: "def async_engine(self) -> AsyncEngine"
  key_links:
    - from: "pycopg/database.py"
      to: "pycopg.exceptions.ExtensionNotAvailable"
      via: "import + raise at extension-check sites"
      pattern: "raise ExtensionNotAvailable"
    - from: "pycopg/async_database.py"
      to: "pycopg.exceptions.DatabaseExists"
      via: "import + raise at create() already-exists site"
      pattern: "raise DatabaseExists"
    - from: "pycopg/async_database.py"
      to: "sqlalchemy.ext.asyncio.AsyncEngine"
      via: "TYPE_CHECKING import for return annotation"
      pattern: "AsyncEngine"
---

<objective>
Convert the domain-business exceptions to real pycopg types (DOC-09 / V2) and annotate the `async_engine` property (DOC-12 / TY2). This is a precise, inventoried change: exactly 14 `RuntimeError("... extension not installed")` → `ExtensionNotAvailable`, exactly 2 `ValueError("Database '...' already exists")` → `DatabaseExists`, and the matching `pytest.raises(...)` assertions updated in the SAME plan (D-04). All other RuntimeError/ValueError sites are programmer-API / state / subprocess errors and are deliberately left untouched (D-01).

Purpose: Callers can catch specific pycopg exception types; the `async_engine` return type matches its sync counterpart (`engine -> Engine`).
Output: Two source modules with converted raises + module-level exception import + AsyncEngine annotation; two test modules with updated assertions.
</objective>

<artifacts_this_phase_produces>
- `pycopg.async_database.async_engine` return annotation `-> AsyncEngine` (this plan)
- 14 `raise ExtensionNotAvailable(...)` sites + 2 `raise DatabaseExists(...)` sites (this plan)
- `from pycopg.exceptions import ExtensionNotAvailable, DatabaseExists` added to database.py and async_database.py (this plan; these modules had NO `pycopg.exceptions` import before)
- `DatabaseExists` (defined in Plan 01 — consumed here, not new in this plan)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-CONTEXT.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-RESEARCH.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert 7 domain exceptions in database.py (ExtensionNotAvailable + DatabaseExists) and update tests</name>
  <files>pycopg/database.py, tests/test_database_integration.py</files>
  <read_first>
    - pycopg/database.py (NO `from pycopg.exceptions import` block exists yet — add one near the other `from pycopg...` imports; raise sites at lines 167, 1370, 1524, 1560, 1607, 1638, 1657, 1675)
    - tests/test_database_integration.py (line 869 — `pytest.raises(RuntimeError, match="TimescaleDB extension not installed")`; add the exceptions import at top)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-RESEARCH.md (§ Exception Conversion Inventory — the 7 database.py sites, the NOT-to-convert table, and the Tests-to-Update table)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md (§ pycopg/database.py — exact before/after raise patterns; import addition)
  </read_first>
  <action>
    Add `from pycopg.exceptions import DatabaseExists, ExtensionNotAvailable` to database.py imports (this module currently imports nothing from pycopg.exceptions). Convert the single `raise ValueError(f"Database '{name}' already exists")` at line 167 → `raise DatabaseExists(...)`, message text unchanged. Convert the 6 `raise RuntimeError("... extension not installed ...")` sites (PostGIS at the from_geodataframe site ~line 1370; TimescaleDB at create_hypertable ~1524, enable_compression ~1560, add_compression_policy ~1607, add_retention_policy ~1638, list_hypertables ~1657, hypertable_info ~1675) → `raise ExtensionNotAvailable(...)`, each message string preserved verbatim per D-01/specifics. DO NOT convert: the `RuntimeError("Already in session mode")`, the `ValueError("Specify either table or sql")`, `ValueError("Invalid ON DELETE/UPDATE action")`, CRS/SRID `ValueError`s, or the `RuntimeError("pg_dump/pg_restore/psql ... failed")` subprocess sites (D-01 — these are programmer/tooling errors). In tests/test_database_integration.py add `from pycopg.exceptions import ExtensionNotAvailable, DatabaseExists` and change the assertion at line 869 from `pytest.raises(RuntimeError, ...)` to `pytest.raises(ExtensionNotAvailable, match="TimescaleDB extension not installed")` (D-04). Leave subprocess-failure assertions (test_database.py pg_dump/pg_restore ~523/610) unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/test_database_integration.py -k "timescale or postgis or extension or hypertable" -x -o addopts="" -q 2>&1 | tail -15; grep -c "raise ExtensionNotAvailable\|raise DatabaseExists" pycopg/database.py</automated>
  </verify>
  <acceptance_criteria>
    - `pycopg/database.py` contains `from pycopg.exceptions import` with `ExtensionNotAvailable` and `DatabaseExists`.
    - `grep -c "raise ExtensionNotAvailable" pycopg/database.py` returns 6; `grep -c "raise DatabaseExists" pycopg/database.py` returns 1.
    - `grep -c "Already in session mode" pycopg/database.py` still shows a `RuntimeError` raise (NOT converted).
    - tests/test_database_integration.py line 869 now uses `pytest.raises(ExtensionNotAvailable, ...)`.
    - The targeted pytest selection exits 0 (no RuntimeError-vs-ExtensionNotAvailable mismatch).
  </acceptance_criteria>
  <done>database.py raises real domain types at the 7 inventoried sites; message text preserved; matching test updated and green.</done>
</task>

<task type="auto">
  <name>Task 2: Convert 7 domain exceptions in async_database.py, annotate async_engine, and update async tests</name>
  <files>pycopg/async_database.py, tests/test_async_database.py</files>
  <read_first>
    - pycopg/async_database.py (`from __future__ import annotations` already at line 7; `TYPE_CHECKING` block at lines 52-54 — extend it; `async_engine` property at lines 90-97 has NO return annotation; `_async_engine = None` at line 88; raise sites at lines 160, 1078, 1114, 1161, 1192, 1211, 1229, 1806; NO `pycopg.exceptions` import yet)
    - pycopg/database.py (line 231 `@property def engine(self) -> Engine:` — the sync analog pattern for the annotation)
    - pycopg/migrations.py (lines 19, 25, 32-34 — established `from __future__ import annotations` + `if TYPE_CHECKING:` import-guard pattern to mirror)
    - tests/test_async_database.py (lines 740, 2248, 2295, 2317, 2339, 2372, 2396 = RuntimeError→ExtensionNotAvailable; line 2616 = ValueError→DatabaseExists; add exceptions import at top)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-RESEARCH.md (§ Exception Conversion Inventory + Pitfall 5 on TYPE_CHECKING import)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-PATTERNS.md (§ pycopg/async_database.py — annotation target + TYPE_CHECKING guard)
  </read_first>
  <action>
    Add `from pycopg.exceptions import DatabaseExists, ExtensionNotAvailable` to async_database.py imports. Convert the `raise ValueError(f"Database '{name}' already exists")` at line 160 → `raise DatabaseExists(...)`. Convert the 6 TimescaleDB `raise RuntimeError("... extension not installed ...")` sites (create_hypertable ~1078, enable_compression ~1114, add_compression_policy ~1161, add_retention_policy ~1192, list_hypertables ~1211, hypertable_info ~1229) and the 1 PostGIS site (from_geodataframe ~1806) → `raise ExtensionNotAvailable(...)`, messages verbatim. DO NOT convert the "Already in session mode" RuntimeError (~319), "Specify either table or sql" / "Invalid ON DELETE" ValueErrors, or the pg_dump/pg_restore/psql RuntimeError subprocess failures (D-01). For TY2/DOC-12: add `from sqlalchemy.ext.asyncio import AsyncEngine` inside the existing `if TYPE_CHECKING:` block (NOT a top-level import — preserve the lazy `create_async_engine` import inside the method body, per Pitfall 5), annotate the property `def async_engine(self) -> AsyncEngine:`, and change `self._async_engine = None` (line 88) to `self._async_engine: AsyncEngine | None = None` (safe because `from __future__ import annotations` is already active). In tests/test_async_database.py add `from pycopg.exceptions import ExtensionNotAvailable, DatabaseExists`, change the 7 `pytest.raises(RuntimeError, match="...extension not installed")` (lines 740, 2248, 2295, 2317, 2339, 2372, 2396) to `pytest.raises(ExtensionNotAvailable, ...)`, and change line 2616 `pytest.raises(ValueError, match="already exists")` to `pytest.raises(DatabaseExists, match="already exists")`. Leave pg_dump/pg_restore RuntimeError assertions unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/test_async_database.py -k "timescale or postgis or extension or hypertable or create_raises" -x -o addopts="" -q 2>&1 | tail -15; grep -c "raise ExtensionNotAvailable\|raise DatabaseExists" pycopg/async_database.py; grep -c "def async_engine(self) -> AsyncEngine" pycopg/async_database.py</automated>
    <automated>uv run mypy pycopg/async_database.py 2>&1 | grep -i "async_engine" || echo "no async_engine mypy errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "raise ExtensionNotAvailable" pycopg/async_database.py` returns 7; `grep -c "raise DatabaseExists" pycopg/async_database.py` returns 1.
    - `pycopg/async_database.py` contains `def async_engine(self) -> AsyncEngine:` and `AsyncEngine` is imported under `if TYPE_CHECKING:` (not at module top level).
    - `self._async_engine: AsyncEngine | None = None` present.
    - tests/test_async_database.py has `pytest.raises(ExtensionNotAvailable, ...)` at the 7 extension sites and `pytest.raises(DatabaseExists, ...)` at line 2616.
    - The targeted pytest selection exits 0; `uv run python -c "import pycopg.async_database"` raises no import/circular-import error.
  </acceptance_criteria>
  <done>async_database.py raises real domain types; async_engine annotated AsyncEngine via TYPE_CHECKING; async tests updated and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source → raised exception type/message | The Python type of raised exceptions changes; message strings are preserved verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Information Disclosure | Converted exception messages | accept | Message text is preserved byte-for-byte (D-01/specifics); only the Python class changes — no new information is disclosed (RESEARCH § Security Domain) |
| T-13-02-02 | Denial of Service | Callers doing `except ValueError/RuntimeError` on converted sites | accept | Documented breaking change (D-03); CHANGELOG/MIGRATION notes land in Phase 15 (REL-03); pre-1.0, clean hierarchy preferred over hybrid double-inheritance |
| T-13-02-SC | Tampering | package installs | N/A | No new packages installed in this plan (interrogate/mypy added in Plan 01) |
</threat_model>

<verification>
- `uv run pytest tests/test_database_integration.py tests/test_async_database.py -k "timescale or postgis or extension or hypertable or create_raises" -x -o addopts="" -q` exits 0.
- `grep -c "raise ExtensionNotAvailable" pycopg/database.py` = 6 and `pycopg/async_database.py` = 7; `raise DatabaseExists` = 1 each.
- `import pycopg.async_database` raises no circular import.
- Full targeted suite passes; subprocess/state/API-misuse errors remain unchanged.
</verification>

<success_criteria>
- 14 ExtensionNotAvailable + 2 DatabaseExists conversions done with messages preserved (DOC-09).
- Non-domain errors untouched (D-01).
- Tests updated in the same plan (D-04) and green.
- async_engine annotated -> AsyncEngine (DOC-12).
</success_criteria>

<output>
Create `.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-02-SUMMARY.md` when done.
</output>
