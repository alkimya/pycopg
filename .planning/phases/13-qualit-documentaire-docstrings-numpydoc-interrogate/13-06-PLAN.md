---
phase: 13-qualit-documentaire-docstrings-numpydoc-interrogate
plan: 06
type: execute
wave: 4
depends_on: ["13-02", "13-03", "13-04", "13-05"]
files_modified:
  - docs/conf.py
autonomous: true
requirements: [DOC-06, DOC-08, DOC-09]
must_haves:
  truths:
    - "napoleon_google_docstring is explicitly False now that every module is migrated (final cleanup, D-06/Pattern 3)"
    - "The whole-phase gate passes: full test suite green, interrogate >= 95, Sphinx napoleon guard warning-free, mypy runs (non-blocking)"
    - "No Google-style docstring labels remain anywhere in pycopg/"
  artifacts:
    - path: "docs/conf.py"
      provides: "napoleon_google_docstring = False (Google parsing disabled post-migration)"
      contains: "napoleon_google_docstring = False"
  key_links:
    - from: "docs/conf.py"
      to: "sphinx napoleon"
      via: "numpy-only docstring parsing after migration complete"
      pattern: "napoleon_google_docstring = False"
---

<objective>
Final cleanup and whole-phase gate. Now that every module is migrated to numpydoc (Plans 03/04/05), explicitly disable Google-style parsing in Sphinx (`napoleon_google_docstring = False`) per D-06 / RESEARCH Pattern 3 — this MUST come last, because disabling it before migration is complete breaks any remaining Google-style docstring. Then run the consolidated phase gate to prove all of DOC-06..DOC-12 hold together: full suite green, interrogate >= 95, Sphinx napoleon guard warning-free with Google parsing OFF, and mypy runs cleanly (non-blocking).

Purpose: Lock numpy-only parsing and confirm the whole phase is internally consistent before verification.
Output: `docs/conf.py` with Google parsing disabled; a consolidated gate result recorded in the SUMMARY.
</objective>

<artifacts_this_phase_produces>
- `napoleon_google_docstring = False` line in docs/conf.py (this plan — finalizes the napoleon config introduced in Plan 01)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-CONTEXT.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-RESEARCH.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-01-SUMMARY.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-03-SUMMARY.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-04-SUMMARY.md
@.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disable napoleon_google_docstring and run the consolidated phase gate</name>
  <files>docs/conf.py</files>
  <read_first>
    - docs/conf.py (the `napoleon_numpy_docstring = True` block added in Plan 01 — add `napoleon_google_docstring = False` next to it)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-RESEARCH.md (§ Pattern 3 — disable Google parsing only AFTER migration complete; anti-pattern: "Removing napoleon_google_docstring before migration is complete")
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-01-SUMMARY.md (chosen Sphinx guard mechanism)
    - .planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-03-SUMMARY.md, 13-04-SUMMARY.md, 13-05-SUMMARY.md (confirm all module migrations complete before disabling Google parsing)
  </read_first>
  <action>
    Add `napoleon_google_docstring = False` to docs/conf.py immediately after the `napoleon_numpy_docstring = True` line. This is the final cleanup step that locks numpy-only parsing (D-06 discretion item — research confirmed it is safe only once all modules are migrated). Then run the consolidated whole-phase gate: (1) full test suite `uv run pytest` (the 3 known-flaky local DB tests may fail in the local env per project memory — note them but they do not block; CI is the authority); (2) `uv run interrogate pycopg --fail-under 95 --quiet`; (3) the chosen Sphinx guard build (from 13-01-SUMMARY) with Google parsing now OFF, which MUST stay warning-free; (4) `uv run mypy pycopg/` (non-blocking — record output, do not fail the plan on mypy errors per D-05). Record all four results in the SUMMARY as the phase-gate evidence.
  </action>
  <verify>
    <automated>grep -c "napoleon_google_docstring = False" docs/conf.py; uv run interrogate pycopg --fail-under 95 --quiet && echo "interrogate OK"; uv run pip install -r docs/requirements.txt -q && uv run sphinx-build -W --keep-going -b html docs docs/_build/html 2>&1 | tail -5; echo "sphinx exit: ${PIPESTATUS[1]}"</automated>
    <automated>grep -rcE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/*.py | grep -v ':0$' || echo "no Google-style labels remain in pycopg/"</automated>
    <automated>uv run mypy pycopg/ 2>&1 | tail -5; echo "(mypy non-blocking per D-05)"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/conf.py` contains `napoleon_google_docstring = False`.
    - `grep -rcE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/*.py` shows 0 for every module (no Google-style labels anywhere).
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
    - The Sphinx guard build is warning-free with Google parsing disabled.
    - mypy output recorded in SUMMARY (errors allowed — non-blocking).
    - `uv run pytest -o addopts=""` or `uv run pytest` is green except the 3 documented pre-existing local-env DB failures (noted, not regressions).
  </acceptance_criteria>
  <done>Google parsing disabled; whole-phase gate evidence (tests, interrogate, sphinx, mypy) recorded; no Google labels remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A single Sphinx config flag and verification commands; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-01 | Tampering | docs/conf.py napoleon config | accept | Single boolean flag change; the Sphinx guard build (re-run in this plan) immediately proves the config does not break the docs build |
| T-13-06-SC | Tampering | package installs | N/A | No package installs in this plan (docs deps already pinned in docs/requirements.txt; installed read-only for the build) |
</threat_model>

<verification>
- `grep -c "napoleon_google_docstring = False" docs/conf.py` returns 1.
- `grep -rcE "^\s*(Args|Returns|Raises|Example|Examples):" pycopg/*.py` is 0 for every module.
- `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
- Sphinx guard build warning-free with Google parsing off.
- mypy recorded (non-blocking); full suite green except documented pre-existing local failures.
</verification>

<success_criteria>
- napoleon_google_docstring disabled post-migration (DOC-06/DOC-08).
- Whole-phase gate evidence recorded across DOC-06..DOC-12.
- No Google-style labels remain anywhere in pycopg/.
</success_criteria>

<output>
Create `.planning/phases/13-qualit-documentaire-docstrings-numpydoc-interrogate/13-06-SUMMARY.md` when done, recording the four consolidated gate results (pytest, interrogate, sphinx, mypy).
</output>
