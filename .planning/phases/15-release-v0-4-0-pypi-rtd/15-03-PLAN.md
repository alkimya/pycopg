---
phase: 15-release-v0-4-0-pypi-rtd
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/tests.yml
  - .github/workflows/publish.yml
autonomous: true
requirements: [REL-05]
tags: [ci, github-actions, node24]

must_haves:
  truths:
    - "No GitHub Actions step pins a node20 (v4) checkout/upload/download-artifact action"
    - "actions/checkout is bumped to the node24 major version in both workflows"
    - "upload-artifact and download-artifact are bumped to matching node24 major versions in publish.yml"
    - "Both workflow YAML files remain syntactically valid"
  artifacts:
    - path: ".github/workflows/tests.yml"
      provides: "node24 checkout action"
      contains: "actions/checkout@"
    - path: ".github/workflows/publish.yml"
      provides: "node24 checkout + artifact actions"
      contains: "actions/upload-artifact@"
  key_links:
    - from: ".github/workflows/publish.yml"
      to: "actions/upload-artifact"
      via: "pinned action version"
      pattern: "actions/upload-artifact@v[56]"
---

<objective>
Bump the GitHub Actions that still run on Node 20 to their Node 24 equivalents (REL-05):
`actions/checkout@v4` → v6 in both workflows, and `actions/upload-artifact@v4` /
`actions/download-artifact@v4` → matching node24 majors in publish.yml. Verify the
`astral-sh/setup-uv` and `pypa/gh-action-pypi-publish` versions' node24 status before
deciding whether they need a bump.

Purpose: GitHub deprecated Node 20 for Actions (deprecation observed during the v0.3.1
release; removal scheduled). Bumping now removes the deprecation warnings and keeps the
publish workflow on supported runtimes before the v0.4.0 release fires it.

Output: updated `.github/workflows/tests.yml` and `.github/workflows/publish.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md
@.planning/phases/15-release-v0-4-0-pypi-rtd/15-VALIDATION.md
</context>

## Artifacts this phase produces

This plan modifies existing workflow files only (no new files). The intentional changes are:
- `actions/checkout@v4` → `@v6` in `tests.yml` (line 33) and `publish.yml` (line 12).
- `actions/upload-artifact@v4` → node24 major in `publish.yml` (line 24).
- `actions/download-artifact@v4` → node24 major in `publish.yml` (line 37).
- Possibly a `setup-uv` and/or `gh-action-pypi-publish` version bump if verification shows they still run node20.

<tasks>

<task type="auto">
  <name>Task 1: Verify node24 action versions, then bump the deprecated node20 actions in both workflows</name>
  <files>.github/workflows/tests.yml, .github/workflows/publish.yml</files>
  <read_first>
    - .github/workflows/tests.yml (the file being modified — `actions/checkout@v4` at line 33, `astral-sh/setup-uv@v8.2.0` at line 36)
    - .github/workflows/publish.yml (the file being modified — `actions/checkout@v4` at line 12, `astral-sh/setup-uv@v8.2.0` at line 15, `actions/upload-artifact@v4` at line 24, `actions/download-artifact@v4` at line 37, `pypa/gh-action-pypi-publish@release/v1` at line 43)
    - .planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md (§H — exact old→new mapping table + the items flagged for verification: setup-uv and gh-action-pypi-publish node24 status; Assumptions A1/A2/A3)
  </read_first>
  <action>
    First, VERIFY the current node24 major versions (RESEARCH §H flags these as needing
    confirmation — versions move fast, research is valid until 2026-07-14). Use WebFetch on the
    releases pages and use the latest stable node24 major:
    - `https://github.com/actions/checkout/releases` — confirm the latest major (RESEARCH says v6, node24).
    - `https://github.com/actions/upload-artifact/releases` — confirm the latest major (RESEARCH says v6, node24).
    - `https://github.com/actions/download-artifact/releases` — confirm the matching node24 major (RESEARCH A3 assumes v6).
    - `https://github.com/astral-sh/setup-uv/releases` — check whether v8.2.0 already runs node24; if a newer release added node24, note the version (RESEARCH A1 / Open Question 1).
    - `https://github.com/pypa/gh-action-pypi-publish/releases` — check whether `release/v1` already targets node24 (RESEARCH A2).

    Then apply the confirmed bumps (RESEARCH §H mapping table):
    - `tests.yml` line 33: `actions/checkout@v4` → the confirmed node24 major (e.g. `@v6`).
    - `publish.yml` line 12: `actions/checkout@v4` → same confirmed major.
    - `publish.yml` line 24: `actions/upload-artifact@v4` → confirmed node24 major.
    - `publish.yml` line 37: `actions/download-artifact@v4` → confirmed node24 major (match upload-artifact's family).
    - `astral-sh/setup-uv`: bump to the verified node24-supporting version ONLY if v8.2.0 does
      not already support node24; otherwise leave at `@v8.2.0` and record the finding in the SUMMARY.
    - `pypa/gh-action-pypi-publish@release/v1`: if the floating `release/v1` already points to a
      node24 build, leave it; otherwise pin to the verified node24 release. Record the finding.

    Do NOT change any other workflow logic (matrix, services, env, step names, the OIDC
    `id-token: write` permission, the `pypi` environment) — version pins only.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; [yaml.safe_load(open(f)) for f in ('.github/workflows/tests.yml','.github/workflows/publish.yml')]; print('yaml-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'actions/checkout@v4' .github/workflows/` returns nothing.
    - `grep -rn 'actions/upload-artifact@v4' .github/workflows/` returns nothing; `grep -rn 'actions/download-artifact@v4' .github/workflows/` returns nothing.
    - `grep -rEn 'actions/checkout@v[56]' .github/workflows/tests.yml .github/workflows/publish.yml` matches in both files.
    - `grep -rEn 'actions/(upload|download)-artifact@v[56]' .github/workflows/publish.yml` matches both artifact actions.
    - Both YAML files parse: the automated `yaml.safe_load` check prints `yaml-ok`.
    - The SUMMARY records the verified node24 status (and any version bump or "already node24, left as-is" decision) for `astral-sh/setup-uv` and `pypa/gh-action-pypi-publish`.
    - `publish.yml` still contains `id-token: write`, `environment: pypi`, and `pypa/gh-action-pypi-publish@` (OIDC trusted-publishing surface unchanged).
  </acceptance_criteria>
  <done>
    All node20 (v4) checkout/upload/download-artifact pins are bumped to verified node24 majors
    in both workflows; setup-uv and pypi-publish node24 status verified and handled; both YAML
    files remain valid and the OIDC publish surface is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions supply chain | Third-party actions execute in CI with repo permissions; their version pins are the trust anchor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | GitHub Action version pins | mitigate | Bump only to official `actions/*` and `astral-sh/setup-uv` / `pypa/*` publisher tags; verify each target version against its official GitHub releases page (WebFetch) before pinning. Do not introduce third-party/unofficial actions. |
| T-15-03b | Elevation of privilege | floating vs pinned action tags | accept | `actions/*` are bumped to floating major tags (`@v6`) and `gh-action-pypi-publish@release/v1` stays floating — consistent with the existing repo convention and the v0.3.1 release that already shipped via this surface. SHA-pinning is a known hardening option but is out of scope for this release (no evidence of compromise; flagged in RESEARCH §H). |
</threat_model>

<verification>
- `grep -rn 'actions/checkout@v4' .github/workflows/` returns nothing.
- `grep -rn 'actions/upload-artifact@v4' .github/workflows/` and `download-artifact@v4` return nothing.
- Both workflow YAML files parse cleanly.
- OIDC trusted-publishing surface in publish.yml is unchanged.
</verification>

<success_criteria>
- REL-05 satisfied: GitHub Actions bumped from Node 20 to Node 24 (verified versions), workflows valid, publish OIDC surface intact.
</success_criteria>

<output>
Create `.planning/phases/15-release-v0-4-0-pypi-rtd/15-03-SUMMARY.md` when done
</output>
