---
phase: 15-release-v0-4-0-pypi-rtd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/postgis.md
  - docs/database.md
  - docs/api-autodoc.md
  - docs/api-reference.md
  - docs/spatial.md
  - docs/index.md
autonomous: true
requirements: [REL-01]
tags: [docs, sphinx, spatial, postgis]

must_haves:
  truths:
    - "docs/postgis.md shows db.spatial.* helpers instead of raw execute() for the 7 single-operation PostGIS examples"
    - "pycopg.spatial appears in the Sphinx autodoc surface so SpatialAccessor/AsyncSpatialAccessor have generated API docs"
    - "A narrative docs/spatial.md page exists and is reachable from the index toctree"
    - "docs/api-reference.md lists the 11 db.spatial.* helper methods"
    - "uv run sphinx-build -W --keep-going exits 0 with the new pages and autodoc surface"
  artifacts:
    - path: "docs/spatial.md"
      provides: "Narrative page for the db.spatial.* accessor (11 helpers, into=, geometry forms, unit=)"
      min_lines: 80
    - path: "docs/postgis.md"
      provides: "PostGIS examples using db.spatial.* helpers"
      contains: "db.spatial."
    - path: "docs/api-autodoc.md"
      provides: "Sphinx automodule directive for pycopg.spatial"
      contains: ".. automodule:: pycopg.spatial"
    - path: "docs/index.md"
      provides: "toctree including the spatial page"
      contains: "spatial"
    - path: "docs/api-reference.md"
      provides: "Hand-written reference section for db.spatial.* helpers"
      contains: "db.spatial"
  key_links:
    - from: "docs/index.md"
      to: "docs/spatial.md"
      via: "toctree entry"
      pattern: "^spatial$"
    - from: "docs/api-autodoc.md"
      to: "pycopg/spatial.py"
      via: "automodule directive"
      pattern: "automodule:: pycopg\\.spatial"
---

<objective>
Bring the Sphinx documentation up to date with the Phase 14 spatial helpers (REL-01):
replace the raw `execute(...)` PostGIS examples in `docs/postgis.md` with the
`db.spatial.*` helper calls, add `pycopg.spatial` to the autodoc surface, create the
missing `docs/spatial.md` narrative page, wire it into the index toctree, and add a
hand-written `db.spatial.*` section to `docs/api-reference.md`.

Purpose: The Phase 14 accessor (`pycopg/spatial.py`, 11 helpers) shipped with full
numpydoc docstrings but no narrative docs and no autodoc surface — RTD currently has zero
API documentation for `SpatialAccessor`/`AsyncSpatialAccessor`. This plan closes that gap
so the v0.4.0 docs reflect the actual public API.

Output: updated `docs/postgis.md`, `docs/database.md`, `docs/api-autodoc.md`,
`docs/api-reference.md`; new `docs/spatial.md`; updated `docs/index.md` toctree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md
@.planning/phases/15-release-v0-4-0-pypi-rtd/15-VALIDATION.md
</context>

## Artifacts this phase produces

These are NEW artifacts created by this plan — drift/coverage verification must treat them
as intentionally added (not stray):

- `docs/spatial.md` — new narrative page for the `db.spatial.*` accessor.
- `.. automodule:: pycopg.spatial` directive added to `docs/api-autodoc.md`.
- `spatial` toctree entry added to `docs/index.md` (between `postgis` and `timescaledb`).
- New `## Spatial Helpers (db.spatial.*)` section in `docs/api-reference.md`.

<tasks>

<task type="auto">
  <name>Task 1: Replace raw execute() PostGIS examples in docs/postgis.md (and docs/database.md) with db.spatial.* helpers</name>
  <files>docs/postgis.md, docs/database.md</files>
  <read_first>
    - docs/postgis.md (the file being modified — read the full "Common Spatial Operations" section, lines 143–263, and the "Coordinate Reference Systems" / "Setting SRID" sections)
    - docs/database.md (lines 350–360 — the secondary ST_DWithin to_geodataframe block)
    - pycopg/spatial.py (for the EXACT public helper signatures — read the SpatialAccessor methods: contains line 1078, dwithin line 1300, distance line 1386, area line 1538, perimeter line 1602, centroid line 1666, buffer line 1727, transform line 1794, intersects line 1223. Use the real keyword-only parameter names: point=, geom=, srid=, distance=, unit=, to_srid=, columns=, order_by=, limit=, into=)
    - .planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md (§B — exact block-by-block current→new mapping with line ranges)
  </read_first>
  <action>
    Per RESEARCH §B, replace the 7 single-operation raw `execute(...)` fenced blocks in
    `docs/postgis.md` "Common Spatial Operations" with `db.spatial.*` helper calls, using
    the real signatures from `pycopg/spatial.py`:

    1. "Point in Polygon" (lines 147–156): replace with `db.spatial.contains("parcels", geom="geometry", point=(-122.4, 37.8), srid=4326, columns=["id", "name"])`.
    2. "Distance Queries" (lines 161–175): replace with TWO clean examples — `db.spatial.dwithin("parcels", geom="geometry", point=(-122.4, 37.8), distance=1000, unit="m")` for the filter, and `db.spatial.distance("parcels", geom="geometry", point=(-122.4, 37.8), unit="m", columns=["id", "name"], order_by="distance")` for the distance column (the helpers split filter and distance; note this in prose).
    3. "Buffer" (lines 193–199): replace with `db.spatial.buffer("locations", geom="geometry", distance=100, unit="m", columns=["id", "name"])`. Note the output column is named `buffer`.
    4. "Centroid" (lines 205–211): replace with `db.spatial.centroid("parcels", geom="geometry", columns=["id", "name"])`. Note the returned columns are `centroid_x` / `centroid_y` (not lon/lat).
    5. "Area and Perimeter" (lines 217–225): replace with `db.spatial.area("parcels", geom="geometry", unit="m", columns=["id", "name"], order_by="area", limit=10)` and a separate `db.spatial.perimeter("parcels", geom="geometry", unit="m", columns=["id", "name"])` (helpers compute area and perimeter separately).
    6. "Transforming CRS → In database" (lines 240–244): replace with `db.spatial.transform("parcels", geom="geometry", to_srid=3857)`. Note the output column is `geometry_transformed`.
    7. "With SQL Query → DWithin with params" (lines 84–95): replace with `db.spatial.dwithin("parcels", point=(-122.4, 37.8), distance=1000, into="gdf")`.

    DO NOT convert (keep as raw execute / to_geodataframe, per RESEARCH §B, with a one-line
    prose note that `db.spatial.*` covers single-table operations and raw SQL remains correct
    for complex aggregates):
    - "Intersection" block (lines 181–188) — ST_Intersection area aggregate (self-join, no helper).
    - "Setting SRID → Update existing geometry" (lines 257–262) — DML UPDATE, no helper.
    - "With SQL Query → ST_Within envelope" (lines 72–82) — ST_MakeEnvelope, no helper.
    - "Example: Spatial Analysis" (lines 265–307) — multi-table spatial join + SUM(ST_Area) aggregate.

    In `docs/database.md` (lines 355–358): replace the inline `to_geodataframe(sql="...ST_DWithin...")`
    example with `db.spatial.dwithin("parcels", point=(-122.4, 37.8), distance=1000, into="gdf")`.

    Do NOT touch the "Creating Spatial Tables", "Reading Spatial Data → Full Table",
    "Spatial Indexes", or "Listing Geometry Columns" sections — those are not helper-covered.
  </action>
  <verify>
    <automated>uv run sphinx-build -W --keep-going -b html docs docs/_build/html</automated>
  </verify>
  <acceptance_criteria>
    - `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
    - `grep -c 'db.spatial.contains' docs/postgis.md` ≥ 1 ; `grep -c 'db.spatial.dwithin' docs/postgis.md` ≥ 1 ; `grep -c 'db.spatial.distance' docs/postgis.md` ≥ 1 ; `grep -c 'db.spatial.buffer' docs/postgis.md` ≥ 1 ; `grep -c 'db.spatial.centroid' docs/postgis.md` ≥ 1 ; `grep -c 'db.spatial.area' docs/postgis.md` ≥ 1 ; `grep -c 'db.spatial.perimeter' docs/postgis.md` ≥ 1 ; `grep -c 'db.spatial.transform' docs/postgis.md` ≥ 1.
    - In the "Point in Polygon", "Distance Queries", "Buffer", "Centroid", "Area and Perimeter", and "Transforming CRS → In database" sections of `docs/postgis.md`, no raw `db.execute(` fenced block referencing ST_Contains / ST_DWithin (filter) / ST_Buffer / ST_Centroid / ST_Area-only / ST_Transform remains (those 7 are now helper calls).
    - The "Intersection", "Update existing geometry", ST_MakeEnvelope, and "Example: Spatial Analysis" blocks are still present as raw SQL with a prose note (intentionally kept).
    - `grep -c 'db.spatial.dwithin' docs/database.md` ≥ 1 ; the raw `ST_DWithin` inline `to_geodataframe` example at the former line 358 is replaced.
  </acceptance_criteria>
  <done>
    The 7 single-operation PostGIS examples in docs/postgis.md and the one secondary example in
    docs/database.md use db.spatial.* helpers with correct signatures; the 4 genuinely-complex
    blocks remain raw SQL with an explanatory note; Sphinx build is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pycopg.spatial to autodoc, create docs/spatial.md narrative page, wire index toctree, add api-reference section</name>
  <files>docs/api-autodoc.md, docs/spatial.md, docs/index.md, docs/api-reference.md</files>
  <read_first>
    - docs/api-autodoc.md (the file being modified — current automodule list, lines 5–29)
    - docs/index.md (the file being modified — toctree at lines 5–21)
    - docs/api-reference.md (the file being modified — PostGIS Methods table at lines 154–159, to insert the new spatial section after it)
    - pycopg/spatial.py (for the 11 helper names and signatures: contains, within, intersects, dwithin, distance, nearest, area, perimeter, centroid, buffer, transform — plus the SpatialAccessor docstring at line 1023 covering into= routing and the PostGIS guard, and the module docstring at lines 1–24 covering the four geometry input forms point/wkt/geojson/ref and unit=)
    - .planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md (§C — autodoc gap + api-reference section requirement; §B "New doc page needed" — the spatial.md content outline)
  </read_first>
  <action>
    Per RESEARCH §C and §B:

    (a) `docs/api-autodoc.md`: inside the existing `{eval-rst}` block, add
    `.. automodule:: pycopg.spatial` with `:members:` immediately after the
    `.. automodule:: pycopg.async_database` directive (RESEARCH §C exact placement).

    (b) Create `docs/spatial.md` as a MyST markdown narrative page covering (RESEARCH §B
    "New doc page needed" outline):
      - The `db.spatial.*` / `async_db.spatial.*` access pattern (lazy init on first access,
        PostGIS guard raising `ExtensionNotAvailable` — from the SpatialAccessor docstring).
      - The `into=` parameter: `"rows"` → list[dict], `"gdf"` → GeoDataFrame (D-01).
      - The 4 geometry input forms: `point=(x, y)`, `wkt="..."`, `geojson={...}`, `ref=(table, col)` (D-05).
      - The `unit=` parameter: `"m"` (meters via ::geography) vs `"srid"` (native units) (D-09).
      - The scalar-vs-geometry distinction: `area`, `perimeter`, `distance`, `centroid` return
        scalars so `into="gdf"` is forbidden (raises ValueError, D-02).
      - At least one short example per helper for all 11 helpers (contains, within, intersects,
        dwithin, distance, nearest, area, perimeter, centroid, buffer, transform) using the real
        signatures from spatial.py. Use a top-level `# Spatial Helpers` H1 heading.

    (c) `docs/index.md`: add a `spatial` entry to the toctree between `postgis` and
    `timescaledb` (RESEARCH §B last line).

    (d) `docs/api-reference.md`: add a new `### Spatial Helpers (db.spatial.*)` subsection
    immediately after the existing "PostGIS Methods" table (line 159), as a markdown table
    listing the 11 accessor methods with their key parameters and return types
    (`list[dict] | GeoDataFrame`, or `list[dict]` for the scalar helpers area/perimeter/
    distance/centroid). Take parameter shapes from the SpatialAccessor method signatures.

    Use the existing MyST/markdown style of the surrounding doc files (fenced ```python
    blocks, `###` subsection headings).
  </action>
  <verify>
    <automated>uv run sphinx-build -W --keep-going -b html docs docs/_build/html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'automodule:: pycopg.spatial' docs/api-autodoc.md` returns 1.
    - `docs/spatial.md` exists and is ≥ 80 lines; `grep -c 'db.spatial.' docs/spatial.md` ≥ 11 (one per helper); contains the strings `into=`, `unit=`, `point=`, `wkt=`, `geojson=`, `ref=`.
    - `grep -nx 'spatial' docs/index.md` matches a toctree line between the `postgis` and `timescaledb` lines.
    - `docs/api-reference.md` contains a heading matching `Spatial Helpers (db.spatial`, and that section lists all 11 helper names (contains, within, intersects, dwithin, distance, nearest, area, perimeter, centroid, buffer, transform).
    - `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0 (no broken-ref or duplicate-object warnings from the new spatial autodoc surface).
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0 (spatial.py already carries full numpydoc docstrings; this confirms adding it to autodoc introduced no docstring regression).
  </acceptance_criteria>
  <done>
    pycopg.spatial is in the autodoc surface; docs/spatial.md exists with all 11 helpers
    documented and is reachable from the index toctree; api-reference.md has a db.spatial.*
    section; Sphinx build and interrogate are both green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only changes — no executable code, no new input handling, no network or build surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information disclosure | docs examples | accept | Docs only — no secrets/credentials in examples; coordinate literals are illustrative. No new attack surface; docs/config only. |
</threat_model>

<verification>
- `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
- `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
- No raw `db.execute(` PostGIS single-operation example remains for the 7 helper-covered cases in docs/postgis.md.
- `docs/spatial.md` exists, is in the toctree, and documents all 11 helpers.
</verification>

<success_criteria>
- REL-01 satisfied: PostGIS execute() examples replaced by helpers; spatial autodoc surface added; spatial.md created; api-reference updated.
- Sphinx build is green under `-W` (warnings-as-errors).
</success_criteria>

<output>
Create `.planning/phases/15-release-v0-4-0-pypi-rtd/15-01-SUMMARY.md` when done
</output>
