---
phase: 15-release-v0-4-0-pypi-rtd
plan: 05
type: execute
wave: 3
depends_on: ["15-04"]
files_modified: []
autonomous: false
requirements: [REL-02]
tags: [release, readthedocs, docs]

must_haves:
  truths:
    - ".readthedocs.yaml is valid and unchanged (RESEARCH confirmed no changes needed)"
    - "The v0.4.0 doc commits are pushed to main so RTD can build them"
    - "The live ReadTheDocs build for the latest commit is green"
  artifacts:
    - path: ".readthedocs.yaml"
      provides: "RTD build configuration (validated, no change required)"
      contains: "version: 2"
  key_links:
    - from: ".readthedocs.yaml"
      to: "docs/conf.py"
      via: "sphinx.configuration"
      pattern: "configuration: docs/conf.py"
---

<objective>
Confirm the ReadTheDocs build is green for the v0.4.0 docs (REL-02). RESEARCH §D confirmed
`.readthedocs.yaml` needs no changes; this plan validates the config, ensures the doc commits
are on `main`, and verifies the live RTD build passed via the RTD dashboard.

Purpose: RTD builds on ReadTheDocs infrastructure (pip-based, not uv) and cannot be asserted
from CI. The new `pycopg.spatial` autodoc surface and `docs/spatial.md` page must build green
on RTD, not just locally. Pushing to `main` is an outward-facing action, so this plan is
human-gated.

Output: confirmation that the live RTD build is green for the latest `main` commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md
@.planning/phases/15-release-v0-4-0-pypi-rtd/15-VALIDATION.md
</context>

## Artifacts this phase produces

This plan produces no new repo files. It validates the existing `.readthedocs.yaml` and
confirms the live RTD build state (an external observation, not a committed artifact).

<tasks>

<task type="auto">
  <name>Task 1: Validate .readthedocs.yaml and confirm the spatial autodoc surface is importable for the pip-based RTD build</name>
  <files>(verification only — no source files modified)</files>
  <read_first>
    - .readthedocs.yaml (the config being validated — build.os ubuntu-24.04, python 3.12, sphinx.configuration docs/conf.py, pip install of `.` + docs/requirements.txt)
    - docs/requirements.txt (the doc deps RTD installs)
    - pycopg/spatial.py (lines 26–37 — confirm the `if TYPE_CHECKING:` guard protects the optional `geopandas` import so RTD's pip env can import pycopg.spatial without geopandas at autodoc time)
    - .planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md (§D — RTD config analysis, the TYPE_CHECKING guard note, and the "no .readthedocs.yaml changes required" conclusion)
  </read_first>
  <action>
    Per RESEARCH §D, confirm `.readthedocs.yaml` is valid and requires NO change for v0.4.0
    (version 2 schema, ubuntu-24.04, python 3.12, sphinx config at docs/conf.py, pip install of
    the package + docs/requirements.txt). Confirm the YAML parses.

    Confirm the new autodoc surface is RTD-safe: `pycopg/spatial.py` imports `geopandas` only
    under `if TYPE_CHECKING:` (false at import time), so `.. automodule:: pycopg.spatial` will
    import on RTD's pip env even though geopandas may not be installed there. Validate locally
    that the module imports cleanly without geopandas import errors at runtime
    (`python -c "import pycopg.spatial"`).

    Do NOT modify `.readthedocs.yaml` unless this validation surfaces a concrete defect; if it
    does, record the defect and the minimal fix in the SUMMARY.
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.readthedocs.yaml')); print('rtd-yaml-ok')" && python -c "import pycopg.spatial; print('spatial-import-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.readthedocs.yaml` parses (`rtd-yaml-ok` printed) and still contains `version: 2`, `configuration: docs/conf.py`, and the pip install of `.` + `docs/requirements.txt`.
    - `python -c "import pycopg.spatial"` succeeds (`spatial-import-ok` printed) — confirming the TYPE_CHECKING guard keeps the module importable without geopandas.
    - The SUMMARY records that `.readthedocs.yaml` needs no change (or the specific minimal fix if a defect was found).
  </acceptance_criteria>
  <done>
    .readthedocs.yaml is confirmed valid and unchanged; pycopg.spatial imports without optional
    deps, so the RTD pip build can autodoc it.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [HUMAN-GATED] Push v0.4.0 docs to main and confirm the live ReadTheDocs build is green</name>
  <files>(no repo files — push of existing commits + external RTD dashboard verification)</files>
  <action>
    Human-gated, outward-facing — do not automate: push the prepared v0.4.0 doc commits to
    `main` and confirm the live ReadTheDocs build for the new commit is green. Steps are in
    <how-to-verify> below.
  </action>
  <what-built>
    All v0.4.0 doc changes (postgis helper swaps, pycopg.spatial autodoc surface, docs/spatial.md,
    index toctree entry, api-reference spatial section) plus the version bump and CHANGELOG are
    prepared and verified green locally (Wave 2 gate passed). `.readthedocs.yaml` is validated and
    unchanged. The only remaining step is the outward-facing push to `main` that triggers the
    live RTD build.
  </what-built>
  <how-to-verify>
    1. Ensure the Wave 1/Wave 2 commits (plans 15-01, 15-02, 15-03) are committed locally and the
       Wave 2 verification gate (15-04) reported GATE-GREEN.
    2. Push to main: `git push origin main`.
    3. Open the RTD builds dashboard: https://readthedocs.org/projects/pycopg/builds/
    4. Wait for the build triggered by the push (if it does not start within ~5 minutes, trigger
       it manually from the RTD dashboard — RESEARCH Open Question 3 notes the webhook may be stale).
    5. Confirm the latest build status is "Passed" (green) and that the live site
       https://pycopg.readthedocs.io/ renders the new "Spatial Helpers" page and the
       pycopg.spatial API autodoc entries.
  </how-to-verify>
  <verify>
    <human-check>RTD dashboard shows the latest build "Passed" and https://pycopg.readthedocs.io/ renders the Spatial Helpers page + pycopg.spatial autodoc entries.</human-check>
  </verify>
  <done>The v0.4.0 docs are pushed to main and the live RTD build is confirmed green by the maintainer.</done>
  <resume-signal>Type "approved" once the RTD build is green and the spatial page renders, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → public main / RTD | Pushing to `main` is outward-facing and triggers the public docs build; it is human-gated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering | push to main triggers public RTD build | accept | The push is human-gated (Task 2 checkpoint) and only after the Wave 2 green gate. RTD renders docs only — no package distribution, no secrets. No new code attack surface; docs/config only. |
</threat_model>

<verification>
- `.readthedocs.yaml` parses and is unchanged.
- `import pycopg.spatial` succeeds without geopandas.
- Live RTD build for the latest main commit is green (human-confirmed).
</verification>

<success_criteria>
- REL-02 satisfied: ReadTheDocs build is green and live for the v0.4.0 docs.
</success_criteria>

<output>
Create `.planning/phases/15-release-v0-4-0-pypi-rtd/15-05-SUMMARY.md` when done
</output>
