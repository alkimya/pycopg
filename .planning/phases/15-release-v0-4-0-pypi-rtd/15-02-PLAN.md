---
phase: 15-release-v0-4-0-pypi-rtd
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - docs/conf.py
  - uv.lock
  - CHANGELOG.md
  - MIGRATION.md
autonomous: true
requirements: [REL-03]
tags: [release, version, changelog, migration]

must_haves:
  truths:
    - "The package version is 0.4.0 in pyproject.toml and the Sphinx release string"
    - "uv.lock is consistent with the bumped pyproject.toml (uv lock --check passes)"
    - "python -c 'import pycopg; print(pycopg.__version__)' prints 0.4.0 after uv sync"
    - "CHANGELOG.md has a complete [0.4.0] section and corrected footer links"
    - "MIGRATION.md documents the 3 real v0.3.x→v0.4.0 breaking changes"
  artifacts:
    - path: "pyproject.toml"
      provides: "version = \"0.4.0\""
      contains: "version = \"0.4.0\""
    - path: "docs/conf.py"
      provides: "release = '0.4.0'"
      contains: "release = '0.4.0'"
    - path: "CHANGELOG.md"
      provides: "[0.4.0] release section"
      contains: "## [0.4.0]"
    - path: "MIGRATION.md"
      provides: "v0.3.x → v0.4.0 migration section"
      contains: "v0.3.x to v0.4.0"
  key_links:
    - from: "pyproject.toml"
      to: "pycopg.__version__"
      via: "importlib.metadata after uv sync"
      pattern: "version = \"0.4.0\""
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock --check"
      pattern: "version = \"0.4.0\""
---

<objective>
Bump the package version to 0.4.0 everywhere it is hand-maintained, regenerate the lockfile,
write the CHANGELOG v0.4.0 section, and add the v0.3.x→v0.4.0 migration notes for the 3 real
breaking changes (REL-03).

Purpose: A clean, consistent version surface is the prerequisite for the irreversible publish
in Wave 4. `__version__` is dynamic (importlib.metadata) so only 2 files carry a hand-edited
version string; the lockfile must be regenerated so `uv lock --check` (the first step of
publish.yml's build job) does not fail at release time.

Output: bumped `pyproject.toml` + `docs/conf.py`, regenerated `uv.lock`, a complete CHANGELOG
`[0.4.0]` section with fixed footer links, and a new MIGRATION.md section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md
@.planning/phases/15-release-v0-4-0-pypi-rtd/15-VALIDATION.md
</context>

## Artifacts this phase produces

These are NEW/changed release artifacts created by this plan — drift/coverage verification
must treat them as intentional:

- `## [0.4.0] - 2026-06-14` section heading in `CHANGELOG.md`, plus corrected `[Unreleased]`,
  new `[0.4.0]` and `[0.3.1]` footer link references.
- New `# Migration Guide: v0.3.x to v0.4.0` section appended to `MIGRATION.md`.
- Bumped version strings in `pyproject.toml` (line 7) and `docs/conf.py` (line 17).
- Regenerated `uv.lock` reflecting the 0.4.0 package version.

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.4.0 in pyproject.toml + docs/conf.py and regenerate uv.lock</name>
  <files>pyproject.toml, docs/conf.py, uv.lock</files>
  <read_first>
    - pyproject.toml (the file being modified — line 7 `version = "0.3.1"`; confirm there is exactly one hand-maintained version string)
    - docs/conf.py (the file being modified — line 17 `release = '0.3.1'`)
    - pycopg/__init__.py (lines 31–34 — confirm `__version__` is computed via importlib.metadata and must NOT be hand-edited, per CLAUDE.md)
    - .planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md (§A — exact version bump surface: only these 2 files; §G/§J — lockfile must be regenerated so `uv lock --check` passes in publish.yml)
  </read_first>
  <action>
    Per RESEARCH §A, change EXACTLY two hand-maintained version strings:
    - `pyproject.toml` line 7: `version = "0.3.1"` → `version = "0.4.0"`.
    - `docs/conf.py` line 17: `release = '0.3.1'` → `release = '0.4.0'`.

    Do NOT hand-edit `pycopg/__init__.py` `__version__` (it is resolved at runtime via
    `importlib.metadata.version("pycopg")` per CLAUDE.md and RESEARCH §A — editing it is wrong).
    Do NOT add a version string to README.md (RESEARCH §A confirmed none exists).

    Then regenerate the lockfile so it matches the bumped version (RESEARCH §G/§J): run
    `uv lock` (or `uv sync` which also relocks). Commit the updated `uv.lock`. The goal is that
    `uv lock --check` exits 0 — this is the first step of publish.yml's build job, and a stale
    lockfile would fail the release build.
  </action>
  <verify>
    <automated>uv sync --all-extras --dev && uv lock --check && python -c "import pycopg; print(pycopg.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'version = "0.4.0"' pyproject.toml` matches line 7; `grep -c 'version = "0.3.1"' pyproject.toml` returns 0.
    - `grep -n "release = '0.4.0'" docs/conf.py` matches line 17.
    - `uv lock --check` exits 0 (lockfile current).
    - After `uv sync`, `python -c "import pycopg; print(pycopg.__version__)"` prints `0.4.0`.
    - `pycopg/__init__.py` is unchanged (no hand-edited version literal introduced).
  </acceptance_criteria>
  <done>
    Version is 0.4.0 in pyproject.toml and docs/conf.py; uv.lock is regenerated and current;
    the installed package resolves __version__ to 0.4.0; __init__.py untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write CHANGELOG [0.4.0] section + fix footer links, and add MIGRATION.md v0.3.x→v0.4.0 section</name>
  <files>CHANGELOG.md, MIGRATION.md</files>
  <read_first>
    - CHANGELOG.md (the file being modified — Keep-a-Changelog structure; `[Unreleased]` at line 8, `[0.3.1]` section, footer links at lines 82–84 with the missing [0.3.1] ref and stale v0.3.0...HEAD compare)
    - MIGRATION.md (the file being modified — existing v0.2.0→v0.3.0 guide; the new section is APPENDED, the old one is preserved)
    - .planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md (§E — full v0.4.0 CHANGELOG content outline phases 9–14 + exact footer link replacement; §F — the 3 confirmed breaking changes + the MIGRATION section content)
  </read_first>
  <action>
    Per RESEARCH §E: add a `## [0.4.0] - 2026-06-14` section to CHANGELOG.md directly under
    `## [Unreleased]` (leave `[Unreleased]` present and empty above it), using the §E content
    outline: `### Added` (db.spatial.* 11 helpers + SpatialAccessor/AsyncSpatialAccessor exports
    + the newly-added async methods + the newly-added sync methods + PooledDatabase.execute commit
    + DatabaseExists + new validate_* utils + interrogate/mypy in CI + uv.lock/.python-version),
    `### Changed` (the 3 BREAKING items: async engine URL `postgresql+psycopg_async://`,
    `AsyncDatabase.close()` disposes engine, custom exceptions replace RuntimeError/ValueError;
    plus signature alignment, numpydoc migration, uv tooling), `### Fixed` (session() exception
    masking, atomic migrations, subprocess.os.environ, async create_role validation, async
    from_dataframe primary_key, __version__ via importlib.metadata), and `### Security`.

    Replace the footer link block (current lines 82–84) with the §E 5-line block:
    `[Unreleased]` → compare `v0.4.0...HEAD`; add `[0.4.0]` → `v0.3.1...v0.4.0`; add the missing
    `[0.3.1]` → `v0.3.0...v0.3.1`; keep `[0.3.0]` and `[0.2.0]`.

    Per RESEARCH §F: APPEND a `# Migration Guide: v0.3.x to v0.4.0` section to MIGRATION.md
    (preserve the existing v0.2.0→v0.3.0 content above it), documenting the 3 real breaking
    changes with before/after where useful:
    1. AsyncDatabase engine URL now `postgresql+psycopg_async://` (impact: low — only affects
       code reading `async_engine.url`).
    2. `AsyncDatabase.close()` now disposes the engine (was a no-op).
    3. Custom exception types (`ExtensionNotAvailable`, `DatabaseExists`) replace
       `RuntimeError`/`ValueError` — show the `except RuntimeError:` → `except ExtensionNotAvailable:`
       update.
    Do NOT invent breaking changes beyond these 3 (RESEARCH §F marks the signature alignments
    and __version__ fix as non-breaking → "Changed", not migration items).
  </action>
  <verify>
    <automated>uv run sphinx-build -W --keep-going -b html docs docs/_build/html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## \[0.4.0\]' CHANGELOG.md` returns 1, dated `2026-06-14`.
    - The CHANGELOG `[0.4.0]` section contains `### Added`, `### Changed`, `### Fixed`, and at least one `**BREAKING**` marker referencing the async driver URL and the custom-exception change.
    - `grep -c '\[0.4.0\]: https://github.com/alkimya/pycopg/compare/v0.3.1...v0.4.0' CHANGELOG.md` returns 1; `grep -c '\[0.3.1\]: https://github.com/alkimya/pycopg/compare/v0.3.0...v0.3.1' CHANGELOG.md` returns 1; the `[Unreleased]` footer now compares `v0.4.0...HEAD`.
    - `grep -c 'Migration Guide: v0.3.x to v0.4.0' MIGRATION.md` returns 1; that section names all 3 breaking changes (psycopg_async, close() disposes, ExtensionNotAvailable/DatabaseExists) and shows an `except ExtensionNotAvailable` example.
    - The existing `Migration Guide: v0.2.0 to v0.3.0` section is still present in MIGRATION.md (appended, not replaced).
    - `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0 (CHANGELOG/MIGRATION are not in the Sphinx toctree, but the build must stay green).
  </acceptance_criteria>
  <done>
    CHANGELOG has a complete, correctly-linked [0.4.0] section; MIGRATION.md documents exactly
    the 3 real breaking changes while preserving the prior guide; Sphinx build is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Version/changelog/migration edits plus a lockfile regeneration — no new executable surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering | uv.lock regeneration | accept | `uv lock` only updates the package's own version metadata; no dependency set change is in scope. `uv lock --check` in the verify step confirms the lockfile is consistent with pyproject.toml. No new attack surface; config only. The full Package Legitimacy / supply-chain gate for installs lives in the publish plan (15-06). |
</threat_model>

<verification>
- `uv lock --check` exits 0.
- `python -c "import pycopg; print(pycopg.__version__)"` prints 0.4.0 after `uv sync`.
- `grep '## \[0.4.0\]' CHANGELOG.md` matches; footer links corrected.
- `grep 'Migration Guide: v0.3.x to v0.4.0' MIGRATION.md` matches.
</verification>

<success_criteria>
- REL-03 satisfied: version bumped consistently (2 files + lockfile), CHANGELOG v0.4.0 written, MIGRATION notes for the 3 breaking changes present.
</success_criteria>

<output>
Create `.planning/phases/15-release-v0-4-0-pypi-rtd/15-02-SUMMARY.md` when done
</output>
