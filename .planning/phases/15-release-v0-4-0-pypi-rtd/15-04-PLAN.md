---
phase: 15-release-v0-4-0-pypi-rtd
plan: 04
type: execute
wave: 2
depends_on: ["15-01", "15-02", "15-03"]
files_modified: []
autonomous: true
requirements: [REL-03]
tags: [release, verification, build, gate]

must_haves:
  truths:
    - "The full pytest suite passes at the configured coverage gate"
    - "interrogate docstring coverage passes at the configured threshold"
    - "Sphinx builds green under -W (warnings-as-errors) with all v0.4.0 doc changes"
    - "uv build produces dist/pycopg-0.4.0.tar.gz and dist/pycopg-0.4.0-py3-none-any.whl"
    - "The built wheel contains pycopg/spatial.py and carries version 0.4.0"
  artifacts:
    - path: "dist/pycopg-0.4.0-py3-none-any.whl"
      provides: "Release wheel for v0.4.0"
      contains: "pycopg/spatial.py"
    - path: "dist/pycopg-0.4.0.tar.gz"
      provides: "Release sdist for v0.4.0"
  key_links:
    - from: "pyproject.toml"
      to: "dist/pycopg-0.4.0-py3-none-any.whl"
      via: "uv build"
      pattern: "0.4.0"
---

<objective>
Run the complete pre-publish go/no-go verification (REL-03 build verification). This is the
[BLOCKING] gate before any irreversible step: full test suite, docstring coverage, Sphinx
warnings-as-errors build, lockfile check, and `uv build` with wheel-content inspection
confirming the artifact is the real 0.4.0 wheel including the spatial module.

Purpose: Everything outward-facing in Wave 4 (tag, GitHub Release, PyPI publish) is
irreversible. This plan proves the release candidate is green BEFORE the human gate, so the
maintainer pushes the tag with confidence. Nothing here is irreversible — it builds locally
into dist/ and asserts on the contents.

Output: a verified-green release candidate and a local `dist/` containing the 0.4.0 wheel +
sdist (build artifacts; not committed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md
@.planning/phases/15-release-v0-4-0-pypi-rtd/15-VALIDATION.md
</context>

## Artifacts this phase produces

- `dist/pycopg-0.4.0-py3-none-any.whl` and `dist/pycopg-0.4.0.tar.gz` (local build artifacts
  produced by `uv build`; they are NOT committed — they are regenerated by publish.yml's build
  job at release time. Drift verification must not flag `dist/` as a source change).

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Full pre-publish verification — tests, interrogate, sphinx -W, lockfile, uv build + wheel inspection</name>
  <files>(verification only — no source files modified)</files>
  <read_first>
    - pyproject.toml (the `[tool.pytest.ini_options]` addopts — the authoritative `--cov-fail-under` value; do NOT change it, per the planning guidance the coverage ratchet is out of scope for REL-*)
    - .planning/phases/15-release-v0-4-0-pypi-rtd/15-VALIDATION.md (Sampling Rate → "Before the publish task: full suite + interrogate + sphinx-build -W + uv build all green"; Per-Task Verification Map rows for build/wheel)
    - .planning/phases/15-release-v0-4-0-pypi-rtd/15-RESEARCH.md (§G — exact pre-publish validation sequence + the wheel-content inspection command; §J — irreversibility table)
  </read_first>
  <action>
    Run the full go/no-go sequence from VALIDATION (Sampling Rate) and RESEARCH §G, in order.
    All must be green; STOP and report red if any fails (do not proceed toward release):

    1. Ensure the environment is synced to the bumped version: `uv sync --all-extras --dev`.
    2. Lockfile current: `uv lock --check` (first step publish.yml runs — must pass).
    3. Full test suite at the existing coverage gate: `uv run pytest` (uses the pyproject
       `--cov-fail-under` value as-is; do NOT pass a different threshold and do NOT modify the
       gate — out of scope per planning guidance §3).
    4. Docstring coverage: `uv run interrogate pycopg --fail-under 95 --quiet`.
    5. Sphinx warnings-as-errors build: `uv pip install -r docs/requirements.txt` then
       `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` (exit 0).
    6. Build the release artifacts: `uv build` → expect `dist/pycopg-0.4.0.tar.gz` and
       `dist/pycopg-0.4.0-py3-none-any.whl`.
    7. Inspect the wheel contents (RESEARCH §G): `python -m zipfile -l dist/pycopg-0.4.0-py3-none-any.whl`
       must list `pycopg/spatial.py`, and the wheel filename must carry `0.4.0`.

    Note on the local environment (per project MEMORY): a small number of full-suite DB tests are
    known-flaky in the local env (pre-existing, not regressions). If `uv run pytest` shows ONLY
    those known-flaky DB failures and nothing else, record them explicitly in the SUMMARY as
    pre-existing/flaky and treat the gate as green for release purposes; any NEW failure is a
    red gate. For targeted re-runs use `-o addopts=""` as documented. Do not silently pass over
    unexpected failures.

    This plan modifies NO source files — it is purely a verification gate that produces the
    local dist/ artifacts.
  </action>
  <verify>
    <automated>uv lock --check && uv run interrogate pycopg --fail-under 95 --quiet && uv run sphinx-build -W --keep-going -b html docs docs/_build/html && uv build && python -m zipfile -l dist/pycopg-0.4.0-py3-none-any.whl | grep -q 'pycopg/spatial.py' && echo GATE-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `uv lock --check` exits 0.
    - `uv run pytest` passes the coverage gate (or fails only on the documented pre-existing/flaky DB tests, recorded as such in the SUMMARY — no NEW failures).
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
    - `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
    - `uv build` produces `dist/pycopg-0.4.0.tar.gz` AND `dist/pycopg-0.4.0-py3-none-any.whl` (both filenames carry `0.4.0`).
    - `python -m zipfile -l dist/pycopg-0.4.0-py3-none-any.whl` lists `pycopg/spatial.py`.
    - The combined verify command prints `GATE-GREEN`.
    - The SUMMARY explicitly states GREEN / NO-GO and lists any pre-existing flaky-test exceptions.
  </acceptance_criteria>
  <done>
    Tests, interrogate, and the -W Sphinx build are green; the lockfile is current; uv build
    produced the 0.4.0 wheel+sdist; the wheel contains pycopg/spatial.py. The release candidate
    is verified GREEN and safe to hand to the irreversible Wave 4 gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → dist artifact | `uv build` produces the wheel/sdist that (in publish.yml) become the PyPI artifact; the integrity check here is the last automated chance to inspect them before release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | release wheel contents | mitigate | Inspect `dist/pycopg-0.4.0-py3-none-any.whl` with `python -m zipfile -l` before any publish: confirm `pycopg/spatial.py` is present and the version is 0.4.0; confirm `uv lock --check` passes so the build is reproducible. No stray files / no missing module → safe to release. |
| T-15-04b | Repudiation | unverified release candidate | mitigate | Full green gate (tests + interrogate + sphinx -W + build) is recorded in the SUMMARY as the auditable go/no-go before the irreversible Wave 4. |
</threat_model>

<verification>
- The combined automated gate prints `GATE-GREEN`.
- `uv run pytest` passes (no new failures).
- Wheel contains pycopg/spatial.py and is named with 0.4.0.
</verification>

<success_criteria>
- REL-03 build verification satisfied: version-in-wheel and wheel-contents proven; full suite + interrogate + sphinx -W green. Release candidate cleared for the human-gated Wave 4.
</success_criteria>

<output>
Create `.planning/phases/15-release-v0-4-0-pypi-rtd/15-04-SUMMARY.md` when done
</output>
