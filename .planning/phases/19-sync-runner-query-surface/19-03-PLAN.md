---
phase: 19-sync-runner-query-surface
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - tests/test_etl.py
  - tests/test_etl_accessor.py
autonomous: true
requirements: [ETL-10, ETL-11, ETL-15, ETL-17]
must_haves:
  truths:
    - "_row_to_result is unit-tested DB-free (D-10 mapper correctness)"
    - "SC-1: run() returns a RunResult whose fields match the pipeline_runs row (ETL-10)"
    - "SC-2: history() returns list[RunResult], newest-first, two runs -> two entries (ETL-11)"
    - "SC-3: last_run() returns most-recent RunResult, or None when no runs (ETL-17)"
    - "SC-4: dry_run=True skips load, writes no pipeline_runs row, returns status='dry_run'/rows_loaded=0/run_id=None (ETL-15)"
    - "Existing run_id=db.etl.run(p) assertions migrated to the RunResult return type with no regressions"
  artifacts:
    - path: "tests/test_etl.py"
      provides: "TestRowToResult pure-unit class"
      contains: "class TestRowToResult"
    - path: "tests/test_etl_accessor.py"
      provides: "TestRunResultSurface integration class + migrated existing assertions"
      contains: "class TestRunResultSurface"
  key_links:
    - from: "TestRunResultSurface"
      to: "db.etl.run/history/last_run"
      via: "real pycopg_test DB fixtures (db, cleanup_pipeline_runs, etl_table)"
      pattern: "db.etl"
---

<objective>
Prove the Phase 19 surface against the real pycopg_test database: a DB-free unit class for the
`_row_to_result` mapper, an integration class covering SC-1..SC-4, and the migration of every
existing `run_id = db.etl.run(p)` assertion to the new `RunResult` return type. This is the
Wave-0 test scaffolding (per 19-VALIDATION.md) materialized after the implementation it verifies.

Purpose: close ETL-10/11/15/17 with green automated checks; guarantee no Phase 17/18 regression.
Output: `tests/test_etl.py` (+TestRowToResult), `tests/test_etl_accessor.py` (+TestRunResultSurface, migrated asserts).
</objective>

<artifacts_this_phase_produces>
This plan adds tests only. The Phase 19 production NEW-symbol set (exclude from drift verification)
is created in Plans 01-02: `RunResult`, `_row_to_result`, `ETL_GET_RUN`, `ETLAccessor.history`,
`ETLAccessor.last_run`, the `dry_run` branch, and `run() -> RunResult`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-sync-runner-query-surface/19-CONTEXT.md
@.planning/phases/19-sync-runner-query-surface/19-VALIDATION.md
@.planning/phases/19-sync-runner-query-surface/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TestRowToResult (DB-free unit) + import wiring</name>
  <files>tests/test_etl.py</files>
  <read_first>
    - 19-CONTEXT.md (D-10 — mapper contract: error_message->error, drops error_traceback+watermark)
    - 19-RESEARCH.md (section "Detailed New Tests Required (Wave 0 Gaps)" — TestRowToResult body with _sample_row helper and the 5 test methods)
    - 19-PATTERNS.md (section "TestRowToResult" — import-block extension + TestPipeline analog)
    - tests/test_etl.py lines 1-57 (import block to extend; TestPipeline class style to mirror)
  </read_first>
  <action>
    Extend the `from pycopg.etl import (...)` block in `tests/test_etl.py` to also import `RunResult` and `_row_to_result` (keep alphabetical/existing ordering so ruff is clean).

    Add a `class TestRowToResult` (pure, no fixtures, no DB) mirroring the 19-RESEARCH body. Include a `_sample_row(**overrides)` helper returning a full valid pipeline_runs dict (all 10 columns incl. error_message/error_traceback/watermark), and these tests: maps all 8 fields; `error_message` -> `error`; `error_traceback` not on result (`not hasattr`); `watermark` not on result; result is frozen (assigning a field raises). Use `datetime(..., tzinfo=timezone.utc)` for the timestamp columns.
  </action>
  <acceptance_criteria>
    - `tests/test_etl.py` imports `RunResult` and `_row_to_result` from `pycopg.etl`.
    - `class TestRowToResult` exists with the all-8-fields, error-rename, error_traceback-dropped, watermark-dropped, and frozen tests.
    - `uv run pytest tests/test_etl.py -k "row_to_result or RowToResult" -x -o addopts=""` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_etl.py -k "RowToResult or row_to_result" -x -o addopts="" && uv run ruff check tests/test_etl.py</automated>
  </verify>
  <done>TestRowToResult passes DB-free and proves the mapper contract (rename + drops + frozen); ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate existing run_id assertions to RunResult return type</name>
  <files>tests/test_etl_accessor.py</files>
  <read_first>
    - 19-RESEARCH.md (section "State of the Art" deprecated-tests list; section "Existing tests to update")
    - 19-PATTERNS.md (section "Existing Tests That Must Be Updated" table)
    - tests/test_etl_accessor.py lines 1-13 (import block — add RunResult), and the exact capture sites: line 241, 458-459, 470, 535, 854, 878 (`run_id = db.etl.run(p)`), plus line 459 `assert isinstance(run_id, int)`
  </read_first>
  <action>
    Add `RunResult` to `from pycopg.etl import ETLAccessor, Pipeline` (-> `..., RunResult`).

    Audit the file for every `run_id = db.etl.run(` and `isinstance(run_id, int)`. The known capture-then-use sites are lines 241, 458, 470, 535, 854, 878. For each: change `run_id = db.etl.run(p)` to `result = db.etl.run(p)` followed by `run_id = result.run_id` (so the downstream `WHERE run_id = %s` SELECTs still work unchanged). At line 459, change `assert isinstance(run_id, int)` to `result = db.etl.run(p); assert isinstance(result, RunResult)` (test_run_accepts_pipeline_object). Re-grep after editing — `grep -n "isinstance(run_id, int)" tests/test_etl_accessor.py` must return nothing and no bare `run_id = db.etl.run(` capture site may remain that is later used as a raw int.

    Do NOT add new tests here (Task 3). Do NOT touch sites that call `db.etl.run(p)` without capturing a return value (e.g. line 219, 486, append/replace idempotency tests) — those need no change.
  </action>
  <acceptance_criteria>
    - `grep -n "isinstance(run_id, int)" tests/test_etl_accessor.py` returns nothing.
    - `grep -nc "run_id = db.etl.run(" tests/test_etl_accessor.py` returns 0 (all rewritten to `result = db.etl.run(p); run_id = result.run_id`).
    - `RunResult` is imported in the test file.
    - `uv run pytest tests/test_etl_accessor.py::TestRunPipelineIntegration tests/test_etl_accessor.py::TestETLAccessorIntegration -x -o addopts=""` exits 0 (no regression).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && test "$(grep -c 'isinstance(run_id, int)' tests/test_etl_accessor.py)" = "0" && test "$(grep -c 'run_id = db.etl.run(' tests/test_etl_accessor.py)" = "0" && uv run pytest tests/test_etl_accessor.py::TestRunPipelineIntegration tests/test_etl_accessor.py::TestETLAccessorIntegration -x -o addopts="" && uv run ruff check tests/test_etl_accessor.py</automated>
  </verify>
  <done>No isinstance(run_id,int) or raw run_id capture sites remain; existing integration suites stay green with the RunResult return type.</done>
</task>

<task type="auto">
  <name>Task 3: TestRunResultSurface integration class (SC-1..SC-4)</name>
  <files>tests/test_etl_accessor.py</files>
  <read_first>
    - 19-CONTEXT.md (D-06/D-07/D-08 — history/last_run/dry_run expected behavior)
    - 19-RESEARCH.md (section "Detailed New Tests Required" — TestRunResultSurface method list; "Validation Architecture" -> "Phase Requirements -> Test Map")
    - 19-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map for SC-1..SC-4)
    - 19-PATTERNS.md (section "TestRunResultSurface" — skeleton + fixtures + integration test style)
    - tests/test_etl_accessor.py lines 423-443 (etl_table / etl_src fixtures), lines 446+ (TestRunPipelineIntegration style to mirror), lines 28-34 (cleanup_pipeline_runs fixture)
  </read_first>
  <action>
    Add `class TestRunResultSurface` using fixtures `db`, `cleanup_pipeline_runs`, `etl_table` (and `etl_src` where a table source is needed). Cover:

    SC-1 (ETL-10): run() returns a RunResult (isinstance); its fields equal the pipeline_runs row re-SELECTed by `result.run_id` (pipeline_name/status/rows_extracted/rows_loaded/error vs error_message). status == 'success' (proves D-11 re-SELECT reads DB-stored values).

    SC-2 (ETL-11): history(name) returns a list of RunResult; two runs of the same pipeline -> two entries; newest-first (the later run's row is history[0]); default-limit and unknown-pipeline-returns-empty-list cases.

    SC-3 (ETL-17): last_run(name) returns the most-recent RunResult (equals history(name)[0]); returns None when no runs exist for that name; last_run is not the older run when two exist.

    SC-4 (ETL-15): dry_run=True returns a RunResult with status=='dry_run', run_id is None, rows_loaded==0, rows_extracted reflects the actual extract; NO pipeline_runs row is written (assert count for that pipeline_name == 0 after the dry run); the target table is unchanged.

    Use `%s`-bound SELECTs (`db.execute("SELECT ... WHERE run_id = %s", [result.run_id])`) for cross-checks. Follow the existing integration-test cleanup style (fixtures drop the temp table / pipeline_runs).
  </action>
  <acceptance_criteria>
    - `class TestRunResultSurface` exists covering SC-1, SC-2, SC-3, SC-4 (run/history/last_run/dry_run).
    - SC-4 includes an explicit "no pipeline_runs row written" assertion and a "target unchanged" assertion.
    - `uv run pytest tests/test_etl_accessor.py::TestRunResultSurface -x -o addopts=""` exits 0.
    - Full ETL suite green: `uv run pytest tests/test_etl_accessor.py tests/test_etl.py -o addopts=""` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run pytest tests/test_etl_accessor.py::TestRunResultSurface -x -o addopts="" && uv run pytest tests/test_etl_accessor.py tests/test_etl.py -o addopts="" && uv run ruff check tests/test_etl_accessor.py</automated>
  </verify>
  <done>TestRunResultSurface proves SC-1..SC-4 against pycopg_test (incl. dry_run writes-no-row); the full targeted ETL suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> DB | Tests run against the local pycopg_test PostgreSQL; no untrusted external input. Test-only code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | (verification) | SC-4 no-row assertion | mitigate | The dry_run no-write invariant (D-09) is the security-relevant behavior; the SC-4 test asserts zero pipeline_runs rows for the pipeline_name after a dry run, catching any regression that would leave a 'running' row. |
| T-19-07 | Tampering | test SELECT cross-checks | accept | Test SELECTs use `%s`-bound params (run_id, pipeline_name); test-only, no production surface. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (test-only edits). No install step. |
</threat_model>

<success_criteria>
- `TestRowToResult` (DB-free) proves the D-10 mapper contract.
- `TestRunResultSurface` proves SC-1..SC-4 against pycopg_test, incl. dry_run writes-no-row.
- All existing `isinstance(run_id, int)` / `run_id = db.etl.run(p)` assertions migrated; no regressions.
- `uv run pytest tests/test_etl_accessor.py tests/test_etl.py -o addopts=""` green; ruff clean.
- Phase gate (run by orchestrator, not this plan): `uv run pytest` full suite, coverage >= 94.
</success_criteria>

<output>
Create `.planning/phases/19-sync-runner-query-surface/19-03-SUMMARY.md` when done.
</output>
