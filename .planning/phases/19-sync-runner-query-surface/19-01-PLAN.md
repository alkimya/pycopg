---
phase: 19-sync-runner-query-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/queries.py
  - pycopg/etl.py
autonomous: true
requirements: [ETL-10, ETL-11, ETL-15, ETL-17]
must_haves:
  truths:
    - "RunResult is a frozen dataclass with exactly the 8 SC-1 fields (D-01/D-02)"
    - "_row_to_result maps a pipeline_runs dict_row to a RunResult, renaming error_message->error (D-10)"
    - "ETL_GET_RUN selects a single pipeline_runs row keyed on run_id (D-11)"
  artifacts:
    - path: "pycopg/etl.py"
      provides: "RunResult dataclass + _row_to_result mapper"
      contains: "class RunResult"
    - path: "pycopg/queries.py"
      provides: "ETL_GET_RUN constant"
      contains: "ETL_GET_RUN"
  key_links:
    - from: "_row_to_result"
      to: "RunResult"
      via: "constructs RunResult, error=row['error_message']"
      pattern: "error_message"
---

<objective>
Lay the pure, DB-free core of the Phase 19 return surface: the `RunResult` value object, the
single `_row_to_result` mapper, and the `run_id`-keyed `ETL_GET_RUN` constant. No accessor-method
or run() changes here (those are Plan 02) — this plan is fully unit-testable without a DB.

Purpose: one source of truth (the DB row), one mapper (D-10) — so run/history/last_run can never
drift. ETL_GET_RUN exists because the existing constants key on pipeline_name, unsafe under
concurrent same-name runs (D-11).
Output: `pycopg/etl.py` (+RunResult, +_row_to_result), `pycopg/queries.py` (+ETL_GET_RUN).
</objective>

<artifacts_this_phase_produces>
NEW symbols (exclude from drift verification — none pre-existed). Full phase set listed in Plan 02;
this plan creates: `RunResult` dataclass, `_row_to_result` mapper (both `etl.py`), `ETL_GET_RUN`
constant (`queries.py`).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-sync-runner-query-surface/19-CONTEXT.md
@.planning/phases/19-sync-runner-query-surface/19-RESEARCH.md
@.planning/phases/19-sync-runner-query-surface/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ETL_GET_RUN + RunResult dataclass + _row_to_result mapper</name>
  <files>pycopg/queries.py, pycopg/etl.py</files>
  <read_first>
    - 19-CONTEXT.md (D-01..D-05, D-10, D-11 — LOCKED RunResult shape + mapper)
    - 19-RESEARCH.md (sections "ETL_GET_RUN constant", "Module-Level Pure-Builder Precedent", the "_row_to_result column-to-field mapping" table, "Code Examples")
    - 19-PATTERNS.md (sections "RunResult dataclass", "_row_to_result", "ETL_GET_RUN constant")
    - pycopg/queries.py lines 246-296 (ETL QUERIES; add after ETL_GET_LAST_RUN ends line 295 — WHERE run_id = %s at line 278 is ETL_UPDATE_RUN, not a SELECT)
    - pycopg/etl.py lines 70-199 (Pipeline = style precedent), 414-440 (_step_label = placement), line 443 (class ETLAccessor = insert mapper before it)
  </read_first>
  <action>
    queries.py: add `ETL_GET_RUN` after `ETL_GET_LAST_RUN`. SQL `SELECT * FROM pipeline_runs WHERE run_id = %s`. `%s`-param, no `LIMIT` (PK = one row), no identifier interpolation (D-11).

    etl.py: add `RunResult` `@dataclass(frozen=True)` right after the `Pipeline` block (~line 202). EXACTLY 8 fields in order (D-02): `run_id: int | None` (D-05), `pipeline_name: str`, `status: str`, `rows_extracted: int`, `rows_loaded: int`, `started_at: datetime`, `finished_at: datetime`, `error: str | None` (D-03). NO `watermark` (D-04), NO `__post_init__` (D-02). numpydoc docstring.

    etl.py: add module-level `_row_to_result(row: dict) -> RunResult` after `_step_label`, before `class ETLAccessor`. Map each field from the same-named column EXCEPT `error=row["error_message"]` (rename — D-10; do NOT use `row["error"]`, Pitfall 4). Drop `error_traceback` and `watermark`. Pure, dict-testable. numpydoc docstring.

    Do NOT change run()/history/last_run/init/_start_run/_end_run here — Plan 02.
  </action>
  <acceptance_criteria>
    - `queries.ETL_GET_RUN` contains `run_id = %s` and no `LIMIT`.
    - `RunResult` field names == `['run_id','pipeline_name','status','rows_extracted','rows_loaded','started_at','finished_at','error']`; instance is frozen; no `watermark`/`error_traceback` attrs.
    - `_row_to_result` populates `error` from `error_message` and exposes neither dropped column.
    - Task 1 `<automated>` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pycopg import queries; s=queries.ETL_GET_RUN; assert 'run_id = %s' in s and 'LIMIT' not in s" && python -c "from pycopg.etl import RunResult,_row_to_result as m; from dataclasses import fields; from datetime import datetime,timezone as tz; assert [f.name for f in fields(RunResult)]==['run_id','pipeline_name','status','rows_extracted','rows_loaded','started_at','finished_at','error']; r=m({'run_id':7,'pipeline_name':'p','status':'success','rows_extracted':3,'rows_loaded':3,'started_at':datetime(2026,1,1,tzinfo=tz.utc),'finished_at':datetime(2026,1,1,tzinfo=tz.utc),'error_message':None,'error_traceback':'tb','watermark':None}); assert r.error is None and r.run_id==7 and not hasattr(r,'error_traceback') and not hasattr(r,'watermark')" && uv run ruff check pycopg/etl.py pycopg/queries.py</automated>
  </verify>
  <done>ETL_GET_RUN, RunResult (8 frozen fields), and _row_to_result exist; the import/field-order/mapper check and ruff both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> RunResult/mapper | `_row_to_result` consumes DB-issued `dict_row` data only; `RunResult` is an in-memory value object. No external input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Injection (Tampering) | `ETL_GET_RUN` SELECT | mitigate | `run_id` bound as a single `%s` param (DB-issued int, never user text); no identifier interpolation. ASVS V5 satisfied. |
| T-19-02 | Information Disclosure | `RunResult.error` | accept | `error` is the message string only; `error_traceback` is dropped from the value object (D-03) — less surface, not more. No PII; pipeline-run metadata only. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (REQUIREMENTS.md Out-of-Scope: no new runtime deps). No install step, no package-legitimacy surface. |
</threat_model>

<success_criteria>
- `ETL_GET_RUN` added to `pycopg/queries.py` (`WHERE run_id = %s`, no `LIMIT`).
- `RunResult` frozen dataclass with exactly the 8 ordered fields in `pycopg/etl.py` (D-01/D-02/D-03/D-04/D-05).
- `_row_to_result` pure mapper in `pycopg/etl.py` (D-10): `error_message -> error`, drops `error_traceback`/`watermark`.
- Task 1 `<automated>` exits 0; `ruff check` clean.
</success_criteria>

<output>
Create `.planning/phases/19-sync-runner-query-surface/19-01-SUMMARY.md` when done.
</output>
