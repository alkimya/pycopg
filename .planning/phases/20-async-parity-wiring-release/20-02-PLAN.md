---
phase: 20-async-parity-wiring-release
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - tests/test_parity.py
  - tests/test_etl_accessor.py
  - docs/etl.md
  - docs/index.md
autonomous: true
requirements: [ETL-12, ETL-13]
must_haves:
  truths:
    - "TestEtlParity enumerates ETLAccessor vs AsyncETLAccessor public surfaces with inspect.getmembers and asserts full parity; it passes (SC-4)"
    - "A behavioral async test class exercises await async_db.etl.run/history/last_run/dry_run against the real pycopg_test DB so the async code path is covered by the coverage gate, not just structurally enumerated"
    - "docs/etl.md renders the db.etl.* / async_db.etl.* surface (sync + async examples for run/history/last_run/dry_run) following the docs/spatial.md prose+examples convention"
    - "docs/etl.md is listed in the docs/index.md toctree so the Sphinx -W build does not warn about an orphan document"
  artifacts:
    - path: "tests/test_parity.py"
      provides: "TestEtlParity structural surface-parity class"
      contains: "class TestEtlParity"
    - path: "tests/test_etl_accessor.py"
      provides: "behavioral async parity test class against real DB"
      contains: "async def test_"
    - path: "docs/etl.md"
      provides: "Sphinx ETL documentation page"
      contains: "async_db.etl"
    - path: "docs/index.md"
      provides: "toctree entry for etl"
      contains: "etl"
  key_links:
    - from: "tests/test_parity.py TestEtlParity"
      to: "pycopg.etl.ETLAccessor / AsyncETLAccessor"
      via: "inspect.getmembers public-surface enumeration"
      pattern: "inspect\\.getmembers"
    - from: "docs/index.md toctree"
      to: "docs/etl.md"
      via: "toctree entry between spatial and timescaledb"
      pattern: "^etl$"
---

<objective>
Prove async/sync ETL parity and document the surface. Add `TestEtlParity` (structural surface enumeration, SC-4) to `tests/test_parity.py`; add a behavioral async test class to `tests/test_etl_accessor.py` that exercises `await async_db.etl.run/history/last_run/dry_run` against the real `pycopg_test` DB (so the async code path is exercised by the coverage gate, not dead code); create `docs/etl.md` and register it in the `docs/index.md` toctree.

Purpose: Close the test half of ETL-12/ETL-13 (SC-4) and produce the SC-5 doc page. The structural test alone (RESEARCH Open Question 1) risks passing while the async code is never executed — the behavioral class de-risks the coverage gate.
Output: `TestEtlParity` class, a behavioral async ETL test class, `docs/etl.md`, updated toctree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-async-parity-wiring-release/20-RESEARCH.md
@.planning/phases/20-async-parity-wiring-release/20-VALIDATION.md
@pycopg/etl.py
@tests/test_parity.py
@tests/test_etl_accessor.py
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan:

| Symbol | Kind | Status | Location |
|--------|------|--------|----------|
| `TestEtlParity` | test class | NEW | `tests/test_parity.py` |
| async ETL behavioral test class (e.g. `TestAsyncRunResultSurface`) | test class | NEW | `tests/test_etl_accessor.py` |
| `docs/etl.md` | doc page | NEW | `docs/` |
| `etl` toctree entry | doc nav | NEW | `docs/index.md` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TestEtlParity (structural) + behavioral async ETL test class</name>
  <files>tests/test_parity.py, tests/test_etl_accessor.py</files>
  <read_first>
    - `tests/test_parity.py` — the `TestAsyncParity` pattern, especially `test_all_database_public_methods_exist_in_async` (lines 34-52): `inspect.getmembers(cls)` filtered by `not name.startswith("_")`. This is the exact idiom `TestEtlParity` mirrors. Note the module already imports `inspect`.
    - `tests/test_etl_accessor.py` — the existing fixtures and integration patterns: the `db` fixture (lines 20-25) and `cleanup_pipeline_runs` (lines 27-34) at module scope; the `etl_table`/`etl_src` fixtures (lines 422-443); `TestRunPipelineIntegration` (line 446) and `TestRunResultSurface` for the sync behavioral assertions to mirror. Note imports at lines 11-13 (`Database`, `Pipeline`, `RunResult`, exceptions).
    - `tests/test_integration.py` — the async test idiom: `db = AsyncDatabase(db_config)` inside an `async def test_...` (lines 74-99). Note `pyproject.toml` sets `asyncio_mode = "auto"` so async tests need NO `@pytest.mark.asyncio` marker.
    - `pycopg/etl.py` — the `AsyncETLAccessor` added in Plan 01 (bottom of file) and `Pipeline`/`RunResult` shapes.
  </read_first>
  <action>
    In `tests/test_parity.py`, add a new top-level class `TestEtlParity` (after `TestAsyncParity`). Its primary test `test_etl_accessor_public_methods_match` imports `ETLAccessor, AsyncETLAccessor` from `pycopg.etl`, builds `sync_methods` and `async_methods` sets via `inspect.getmembers(...)` filtered by `not name.startswith("_")`, and asserts BOTH directions: `sync_methods - async_methods == set()` (nothing missing in async) AND `async_methods - sync_methods == set()` (no extra in async). Use clear assertion messages naming the offending members (mirror the `TestAsyncParity` message style). This is the SC-4 structural proof.

    In `tests/test_etl_accessor.py`, add a behavioral async test class (name it `TestAsyncRunResultSurface` to mirror Phase 19's `TestRunResultSurface`). Add an `async_db` fixture that yields `AsyncDatabase(db_config)` (mirror the sync `db` fixture; import `AsyncDatabase` from `pycopg`). Reuse the existing `cleanup_pipeline_runs` and `etl_table` fixtures. Add 4 async tests (no asyncio marker needed — `asyncio_mode = "auto"`), each constructing a `Pipeline` and awaiting the async accessor, asserting equivalence to the sync semantics:
    - run: `r = await async_db.etl.run(p)` → `isinstance(r, RunResult)`, `r.status == "success"`, `r.run_id is not None`, `r.rows_extracted`/`r.rows_loaded` correct.
    - history: run twice, then `h = await async_db.etl.history(p.name)` → `len(h) == 2`, newest-first (`h[0].started_at >= h[1].started_at`), each item a `RunResult`.
    - last_run: after a run `await async_db.etl.last_run(p.name)` returns the most recent `RunResult`; `await async_db.etl.last_run("nonexistent_pipeline_xyz")` returns `None`.
    - dry_run: `r = await async_db.etl.run(p, dry_run=True)` → `r.status == "dry_run"`, `r.rows_loaded == 0`, `r.run_id is None`, and assert no pipeline_runs row was written for it (mirror the sync dry_run assertion).
    Optionally include a 5th test asserting the transform path runs through `asyncio.to_thread` by passing a single transform callable and checking the output DataFrame was transformed (behavioral SC-2 evidence). Keep these async-DB connections cleaned up (the fixture yields; rely on `cleanup_pipeline_runs` + `etl_table` teardown).

    Run `uv run ruff check tests/test_parity.py tests/test_etl_accessor.py` and `uv run black tests`; fix lint.
  </action>
  <behavior>
    - TestEtlParity: `inspect.getmembers(ETLAccessor)` public set == `inspect.getmembers(AsyncETLAccessor)` public set (both directions).
    - Async run returns success RunResult with run_id; history newest-first len==2 after two runs; last_run returns latest or None; dry_run returns status='dry_run' rows_loaded=0 run_id=None and writes no row.
  </behavior>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && grep -q "class TestEtlParity" tests/test_parity.py && uv run pytest tests/test_parity.py::TestEtlParity -o addopts="" -x -q && uv run pytest tests/test_etl_accessor.py::TestAsyncRunResultSurface -o addopts="" -x -q && uv run ruff check tests/test_parity.py tests/test_etl_accessor.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_parity.py` contains `class TestEtlParity`.
    - `uv run pytest tests/test_parity.py::TestEtlParity -o addopts="" -x` exits 0.
    - `uv run pytest tests/test_etl_accessor.py::TestAsyncRunResultSurface -o addopts="" -x` exits 0 (run/history/last_run/dry_run async tests pass against real DB).
    - `uv run ruff check tests/test_parity.py tests/test_etl_accessor.py` exits 0.
  </acceptance_criteria>
  <done>
    `TestEtlParity` asserts full ETLAccessor/AsyncETLAccessor surface parity (both directions) and the behavioral async class exercises run/history/last_run/dry_run against the real DB. All green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docs/etl.md and add it to the docs/index.md toctree</name>
  <files>docs/etl.md, docs/index.md</files>
  <read_first>
    - `docs/spatial.md` — the page-structure template: `# <title>`, `## Access Pattern` with a sync + async code block, then per-helper `### <method>` sections, a `## Async Usage` section, and a `## Security` section. Prose + fenced code examples ONLY (no `autoclass::`/autodoc directives — Assumption A1). Mirror this shape.
    - `docs/index.md` — the toctree block (lines 5-22). `etl` must be inserted between `spatial` and `timescaledb` (Pitfall 6: an unreferenced doc triggers a Sphinx `-W` warning → build failure).
    - `pycopg/etl.py` — the `Pipeline` constructor signature (name/source/target/load_mode/conflict_columns/schema/extract_limit/transform), `RunResult` fields, and the `run`/`history`/`last_run` docstrings — source the accurate prose and example arguments from here. Note `db.etl` / `async_db.etl` usage.
    - `docs/conf.py` — confirm the Sphinx config (myst-parser for .md) so the `-W` build will parse `docs/etl.md`.
  </read_first>
  <action>
    Create `docs/etl.md` following the `docs/spatial.md` structure (prose + code examples, NO autodoc directives — A1). Sections:
    - `# ETL Pipeline Runner` intro paragraph: declarative extract → transform → load with run tracking via `db.etl.*` / `async_db.etl.*`, built on existing pycopg primitives, zero new runtime deps, full sync/async parity.
    - `## Access Pattern`: a sync example (`db.etl.run(Pipeline(...))`) and an async example (`await async_db.etl.run(Pipeline(...))`), noting lazy `db.etl` / `async_db.etl`.
    - `## Defining a Pipeline`: show `Pipeline(name=, source=, target=, load_mode=)`, the three load modes (append/replace/upsert with conflict_columns), source forms (SQL string vs table name), and `transform=` (None / single callable / list).
    - Per-method sections `### run`, `### history`, `### last_run`, and `### Dry runs` (`run(pipeline, dry_run=True)`), each with a short code example and the returned `RunResult` fields.
    - `## Async Usage`: note all methods are awaitable on `async_db.etl`, and that sync transform callables are dispatched via `asyncio.to_thread` so a slow transform does not block the event loop.
    - `## Security`: state that all load SQL is built by parameterized builders with `validate_identifiers` on table/column names — no f-string identifier interpolation.
    Keep code examples copy-paste-correct against the real API (verify argument names against `Pipeline`/`RunResult` in etl.py).

    In `docs/index.md`, add `etl` to the toctree between `spatial` and `timescaledb`.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && test -f docs/etl.md && grep -q "async_db.etl" docs/etl.md && grep -qx "etl" docs/index.md && uv run sphinx-build -W --keep-going -b html docs docs/_build/html</automated>
  </verify>
  <acceptance_criteria>
    - `docs/etl.md` exists and references `async_db.etl`.
    - `docs/index.md` toctree contains a line that is exactly `etl`.
    - `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0 (no orphan-document warning, no warnings-as-errors failure).
  </acceptance_criteria>
  <done>
    `docs/etl.md` documents the sync+async ETL surface following the spatial.md convention and is registered in the toctree; the `-W` Sphinx build is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/docs → library | tests and docs read the public ETL surface; no new runtime input boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | n/a (test+docs only) | `TestEtlParity`, `docs/etl.md` | accept | This plan adds tests and documentation only — no production code, no new SQL, no new identifier paths. No attack surface introduced. The documented API is the already-audited builder/accessor layer. |
| T-20-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (RESEARCH §Package Legitimacy Audit: N/A). |

Per RESEARCH §Security Domain: no new security surface in Phase 20. No `high` threats.
</threat_model>

<verification>
- `uv run pytest tests/test_parity.py::TestEtlParity tests/test_etl_accessor.py -o addopts="" -x -q` green.
- `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
- `uv run ruff check tests` passes.
</verification>

<success_criteria>
- SC-4: `TestEtlParity` enumerates and asserts full ETL surface parity and passes.
- Async code path covered behaviorally (run/history/last_run/dry_run) against real DB.
- SC-5 (docs portion): `docs/etl.md` renders without `-W` warnings and is in the toctree.
</success_criteria>

<output>
Create `.planning/phases/20-async-parity-wiring-release/20-02-SUMMARY.md` when done.
</output>
