---
phase: 24-exports-docs-release-v0-6-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/api-autodoc.md
  - docs/roles-permissions.md
  - docs/backup-restore.md
  - docs/timescaledb.md
  - docs/database.md
  - docs/index.md
autonomous: true
requirements: [REORG-05]

must_haves:
  truths:
    - "README shows the db.X.* accessor surfaces with their method names (criterion #2, REORG-05)"
    - "Each of the 5 new accessor modules is documented via autodoc (criterion #2, REORG-05)"
    - "Sphinx/RTD builds cleanly with -W warnings-as-errors green (criterion #2, REORG-05)"
    - "Flat-method prose examples in the 4 per-topic doc pages now use accessor paths"
  artifacts:
    - path: "README.md"
      provides: "Accessor Namespaces overview table + rewritten flat sections (per D-01)"
      contains: "Accessor Namespaces"
    - path: "docs/api-autodoc.md"
      provides: "automodule blocks for the 5 accessor modules (per D-02)"
      contains: ".. automodule:: pycopg.timescale"
    - path: "docs/roles-permissions.md"
      provides: "admin accessor prose (db.admin.*) + deprecation notice"
      contains: "db.admin."
    - path: "docs/backup-restore.md"
      provides: "backup accessor prose (db.backup.*) + deprecation notice"
      contains: "db.backup."
    - path: "docs/timescaledb.md"
      provides: "timescale accessor prose (db.timescale.*) + deprecation notice"
      contains: "db.timescale."
    - path: "docs/database.md"
      provides: "schema/maint accessor prose (db.schema.* / db.maint.*) + deprecation notice"
      contains: "db.schema."
  key_links:
    - from: "docs/api-autodoc.md"
      to: "pycopg.timescale/admin/maint/backup/schema modules"
      via: "automodule autodoc"
      pattern: "automodule:: pycopg\\.(timescale|admin|maint|backup|schema)"
    - from: "README.md Accessor Namespaces table"
      to: "live accessor method lists"
      via: "method-name enumeration"
      pattern: "db\\.(schema|admin|maint|backup|timescale|spatial)\\."
---

<objective>
Make the 5 new accessor surfaces publicly documented: rewrite the flat-method examples in README and the 4 per-topic Sphinx prose pages to accessor paths, add a consolidated "Accessor Namespaces" overview to README, add 5 `automodule` blocks to `docs/api-autodoc.md`, and hold the Sphinx `-W` gate green.

Purpose: Satisfies ROADMAP Phase 24 criterion #2 (REORG-05) — README lists the `db.X.*` accessor surfaces with their method names; Sphinx/RTD builds cleanly (`-W` green) with each accessor documented.

Output: Rewritten README.md (Namespaces overview + accessor-path examples), 4 rewritten prose doc pages, 5 new automodule blocks in api-autodoc.md, verified index.md toctree, all under a green `-W` build.

This phase ADDS NO CODE LOGIC. Every task is a prose edit + autodoc wiring + gate hold. Do NOT add new accessor methods, helpers, or "improvements" — that is scope creep (RESEARCH Pitfall 7).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-exports-docs-release-v0-6-0/24-CONTEXT.md
@.planning/phases/24-exports-docs-release-v0-6-0/24-RESEARCH.md
@.planning/phases/24-exports-docs-release-v0-6-0/24-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This docs/release phase creates DOCUMENTATION artifacts only — NO new source symbols (exports already exist from Phases 21-23). The source-grounding / drift verification pass should EXCLUDE these new doc artifacts from "drift against source" checks; they are intentionally new prose describing already-shipped code:

- README.md "Accessor Namespaces" overview table (new section)
- README.md rewritten flat sections (Database Exploration, Roles & Permissions, Backup & Restore, async sections) → accessor paths
- 5 new `automodule` blocks in `docs/api-autodoc.md` (pycopg.timescale/admin/maint/backup/schema)
- Rewritten accessor-path prose + deprecation notices in docs/roles-permissions.md, docs/backup-restore.md, docs/timescaledb.md, docs/database.md
- (Possible) toctree adjustment in docs/index.md

No new `pycopg/*.py` symbols are produced. The method names referenced in docs are the ALREADY-SHIPPED accessor methods.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 accessor automodule blocks to docs/api-autodoc.md + verify index.md toctree</name>
  <files>docs/api-autodoc.md, docs/index.md</files>
  <read_first>
    - docs/api-autodoc.md (the existing eval-rst fence block, lines ~5-32, to mirror its shape and `:members:` convention)
    - docs/index.md (current toctree — to decide if accessor pages already covered; likely verify-only)
    - .planning/phases/24-exports-docs-release-v0-6-0/24-PATTERNS.md (the exact 5-block insertion in the `docs/api-autodoc.md` section)
  </read_first>
  <action>
    Inside the existing single `eval-rst` fence in `docs/api-autodoc.md`, insert 5 `automodule` directives — one each for `pycopg.timescale`, `pycopg.admin`, `pycopg.maint`, `pycopg.backup`, `pycopg.schema` — placed after the existing `.. automodule:: pycopg.spatial` block and before `.. automodule:: pycopg.base`, each with `:members:` and the same blank-line spacing as the surrounding blocks (see PATTERNS.md for the exact block text). Do NOT add `:noindex:` and do NOT add `undoc-members: False` — RESEARCH VERIFIED a live `-W` build with all 5 modules produces zero warnings (accessor methods and the deprecated database.py stubs have distinct qualified names; interrogate is 100%). For `docs/index.md`: read its toctree; the accessor content lives in the existing per-topic pages (timescaledb / roles-permissions / backup-restore / database) which are rewritten in Task 2, so a new toctree entry is likely NOT required (Claude's Discretion per D-02 / CONTEXT.md). Add a toctree entry ONLY if needed to keep `-W` green and the nav coherent; otherwise leave index.md unchanged. Before declaring the build clean, clear the stale Sphinx cache (RESEARCH Pitfall 3): `rm -rf docs/_build/`.
  </action>
  <verify>
    <automated>grep -c "automodule:: pycopg.timescale\|automodule:: pycopg.admin\|automodule:: pycopg.maint\|automodule:: pycopg.backup\|automodule:: pycopg.schema" docs/api-autodoc.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'automodule:: pycopg.timescale' docs/api-autodoc.md` returns 1 (and likewise for admin, maint, backup, schema — 5 new blocks total)
    - `docs/api-autodoc.md` contains '.. automodule:: pycopg.timescale', '.. automodule:: pycopg.admin', '.. automodule:: pycopg.maint', '.. automodule:: pycopg.backup', '.. automodule:: pycopg.schema'
    - No `:noindex:` directive added to the new blocks
    - `rm -rf docs/_build/ && uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0 (zero warnings)
  </acceptance_criteria>
  <done>The 5 accessor modules are autodoc'd in api-autodoc.md; index.md toctree is coherent; a fresh-cache `-W` Sphinx build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite flat examples to accessor paths in the 4 per-topic Sphinx prose pages</name>
  <files>docs/roles-permissions.md, docs/backup-restore.md, docs/timescaledb.md, docs/database.md</files>
  <read_first>
    - docs/roles-permissions.md, docs/backup-restore.md, docs/timescaledb.md, docs/database.md (each file being rewritten)
    - docs/etl.md AND docs/spatial.md (the canonical accessor-prose model: opening paragraph, `## Access Pattern` sync-then-async, deprecation notice box)
    - pycopg/admin.py, pycopg/backup.py, pycopg/timescale.py, pycopg/schema.py, pycopg/maint.py (canonical method names per accessor — source of truth for the rewrite)
    - .planning/phases/24-exports-docs-release-v0-6-0/24-PATTERNS.md (the per-page accessor-substitution map + deprecation-notice template)
  </read_first>
  <action>
    For each page, replace every flat `db.method_name(...)` call with the accessor path and add the v0.6.0 deprecation notice box (text per PATTERNS.md), pointing at `../MIGRATION.md`. Substitution map: `docs/roles-permissions.md` → `db.admin.<method>` (create_role, drop_role, role_exists, list_roles, alter_role, grant_role, revoke_role, grant, revoke, list_role_members, list_role_grants); `docs/backup-restore.md` → `db.backup.<method>` (pg_dump, pg_restore, copy_to_csv, copy_from_csv); `docs/timescaledb.md` → `db.timescale.<method>` (create_hypertable, enable_compression, add_compression_policy, add_retention_policy, list_hypertables, hypertable_info); `docs/database.md` → `db.schema.<method>` for the 27 DDL/introspection names and `db.maint.<method>` for size/table_size/table_sizes/vacuum/analyze/explain. Apply the SAME treatment to async examples (`await async_db.admin.*` etc.). STRICT SCOPE (RESEARCH Pitfall 7): change flat calls to accessor paths and add the deprecation notice ONLY — do NOT add new examples, do NOT remove existing valid content, do NOT change method signatures, do NOT document methods outside the 5 accessors. Do NOT touch transactional-core or DataFrame examples (`execute`, `session`, `to_dataframe`, etc.) — they stay flat by design. After editing, clear cache and re-run the `-W` build.
  </action>
  <verify>
    <automated>uv run sphinx-build -W --keep-going -b html docs docs/_build/html 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `docs/roles-permissions.md` contains 'db.admin.' AND a deprecation notice mentioning 'v0.7.0'
    - `docs/backup-restore.md` contains 'db.backup.' AND a deprecation notice mentioning 'v0.7.0'
    - `docs/timescaledb.md` contains 'db.timescale.' AND a deprecation notice mentioning 'v0.7.0'
    - `docs/database.md` contains 'db.schema.' AND 'db.maint.'
    - `grep -n 'db\.create_role(\|db\.pg_dump(\|db\.create_hypertable(\|db\.list_schemas(' docs/roles-permissions.md docs/backup-restore.md docs/timescaledb.md docs/database.md` returns no bare-flat call lines (allowing lines inside deprecation notices that name the flat method as text)
    - `rm -rf docs/_build/ && uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0
  </acceptance_criteria>
  <done>All 4 prose pages use accessor paths in their examples, each carries the v0.6.0→v0.7.0 deprecation notice, transactional-core examples stay flat, and the `-W` build is green.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite README flat sections to accessor paths + add Accessor Namespaces overview</name>
  <files>README.md</files>
  <read_first>
    - README.md (the flat sections to rewrite: Database Exploration ~L78, Roles & Permissions ~L156, Backup & Restore ~L183, the Async Admin/Backup/Role/PostGIS+TimescaleDB sections ~L268-333)
    - docs/etl.md (the accessor-overview model for the new Namespaces section)
    - pycopg/schema.py, pycopg/admin.py, pycopg/maint.py, pycopg/backup.py, pycopg/timescale.py, pycopg/spatial.py (canonical method names + counts for the overview table)
    - .planning/phases/24-exports-docs-release-v0-6-0/24-PATTERNS.md (the README section-rewrite map + the Accessor Namespaces table template)
  </read_first>
  <action>
    Per D-01: (a) Add a consolidated "Accessor Namespaces" overview section near the top (after Quick Start, before Core Features) — a compact table mapping each accessor (`db.schema.*` 27, `db.admin.*` 11, `db.maint.*` 6, `db.backup.*` 4, `db.timescale.*` 6, `db.spatial.*` 13, `db.etl.*` 4) to its purpose and 3-4 representative method names, with a one-line note that all accessors mirror on `AsyncDatabase` and that flat methods are deprecated as of v0.6.0 (pointer to MIGRATION.md). Use the live method names from the accessor modules — NOT the stale SCOPE counts (admin is 11 not 12; schema is 27 not ~26). (b) Rewrite the flat-method sections to accessor paths per the PATTERNS map: Database Exploration → `db.schema.*`/`db.maint.*`; Roles & Permissions → `db.admin.*`; Backup & Restore → `db.backup.*`; Async Admin/Backup/Role/PostGIS+TimescaleDB → `await db.maint.*`/`await db.schema.*`/`await db.backup.*`/`await db.admin.*`/`await db.spatial.*`/`await db.timescale.*`. (c) Leave the transactional core FLAT — `execute`, `execute_many`, `fetch_one`, `fetch_val`, `insert_batch`, `copy_insert`, `session`, `to_dataframe`, `from_dataframe`, `to_geodataframe`, `from_geodataframe`, connection pooling stay as-is (PROJECT.md "reste à plat"). Exact wording/layout of the overview is Claude's Discretion — match the terse, code-first README tone.
  </action>
  <verify>
    <automated>grep -c "Accessor Namespaces" README.md; grep -c "db.admin.\|db.schema.\|db.maint.\|db.backup.\|db.timescale." README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains 'Accessor Namespaces'
    - README.md overview references all of db.schema.*, db.admin.*, db.maint.*, db.backup.*, db.timescale.*, db.spatial.*
    - The Roles & Permissions section uses `db.admin.create_role` (not `db.create_role`); Backup & Restore uses `db.backup.pg_dump`; Database Exploration uses `db.schema.*`/`db.maint.*`
    - README.md still shows `db.execute`, `db.session`, `db.to_dataframe` FLAT (transactional core not moved)
    - The overview table states admin has 11 methods and schema has 27 methods (live counts, not stale SCOPE "12"/"~26")
  </acceptance_criteria>
  <done>README has an Accessor Namespaces overview with live counts, all flat-method sections (sync + async) use accessor paths, and the transactional core remains flat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → public readers | Docs describe the public API surface; inaccurate counts/method names mislead callers but are not a security boundary |
| no external/untrusted input | This plan edits Markdown/RST docs only; no runtime code path, no user input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information disclosure | README/doc examples | accept | Docs describe an already-public, already-shipped API (Phases 21-23). No secrets, credentials, or connection strings introduced — examples use `Database.from_env()` placeholders, never literal credentials. |
| T-24-02 | Tampering | docs autodoc cross-refs | mitigate | `-W` warnings-as-errors gate (cleared-cache build) prevents broken/stale doc cross-references from shipping; method names sourced from live accessor modules, not stale SCOPE prose. |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan installs NO packages. No supply-chain surface in the docs-prose plan. (Publish-integrity threats handled in 24-03-PLAN.md.) |
</threat_model>

<verification>
Full doc-gate sequence (run before declaring the plan done):
```
rm -rf docs/_build/
uv run sphinx-build -W --keep-going -b html docs docs/_build/html   # MUST exit 0
uv run interrogate pycopg --fail-under 95 --quiet                    # MUST exit 0 (autodoc surfaces docstrings)
```
No new packages installed. No `pycopg/*.py` source edits. No `tests/` edits.
</verification>

<success_criteria>
- README.md has an "Accessor Namespaces" overview (live counts: admin 11, schema 27) + all flat sections rewritten to accessor paths; transactional core stays flat.
- `docs/api-autodoc.md` autodocs all 5 accessor modules (timescale/admin/maint/backup/schema).
- The 4 per-topic prose pages use accessor paths + carry the v0.6.0→v0.7.0 deprecation notice.
- A cleared-cache `sphinx-build -W --keep-going` exits 0 (criterion #2 of REORG-05).
</success_criteria>

<output>
Create `.planning/phases/24-exports-docs-release-v0-6-0/24-01-SUMMARY.md` when done.
</output>
