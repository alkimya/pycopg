---
phase: 24-exports-docs-release-v0-6-0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - MIGRATION.md
autonomous: true
requirements: [REORG-05]

must_haves:
  truths:
    - "CHANGELOG has a [0.6.0] entry noting the new accessor paths and the deprecation cycle (removal in v0.7.0) (criterion #3, REORG-05)"
    - "MIGRATION.md instructs callers how to update from flat names to accessor paths (criterion #3, REORG-05)"
    - "The 5 accessor classes (+ async variants) are confirmed importable from the top-level pycopg namespace (criterion #1, REORG-05)"
    - "The MIGRATION flat-name table is 1:1 with the live @deprecated_alias stubs (56 rows)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "[0.6.0] entry with Added/Deprecated/Changed buckets (per D-05)"
      contains: "## [0.6.0]"
    - path: "MIGRATION.md"
      provides: "Prepended v0.5→v0.6 migration guide with full 56-name table (per D-04)"
      contains: "Migration Guide: v0.5.0"
  key_links:
    - from: "MIGRATION.md 56-name table"
      to: "pycopg/database.py @deprecated_alias stubs"
      via: "1:1 flat-name → accessor-path mapping (regenerated via grep)"
      pattern: "@deprecated_alias"
    - from: "CHANGELOG.md [0.6.0] Changed bucket"
      to: "Phase 23 D-06 PostGIS-guard refinement"
      via: "deprecated spatial path ExtensionNotAvailable note"
      pattern: "ExtensionNotAvailable"
---

<objective>
Author the release-communication artifacts: write the CHANGELOG `[0.6.0]` entry (three Keep-a-Changelog buckets), prepend the `## Migration Guide: v0.5.0 → v0.6.0` section to `MIGRATION.md` (with the complete 56-name flat→accessor table regenerated from live source), and confirm criterion #1 (exports already wired) with a smoke-test assertion.

Purpose: Satisfies ROADMAP Phase 24 criterion #3 (CHANGELOG + MIGRATION note the deprecation cycle, removal v0.7.0) and confirms criterion #1 (REORG-05) — the 5 accessor classes + async variants import from `pycopg`.

Output: `[0.6.0]` CHANGELOG entry (kept under `[Unreleased]` until release stamps the date in 24-03), prepended v0.5→v0.6 MIGRATION section with the verified 56-row table, exports smoke-test confirmed.

This phase ADDS NO CODE LOGIC. The exports task is VERIFY-ONLY (RESEARCH confirmed `__all__` is already complete) — do NOT add any new export lines to `pycopg/__init__.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-exports-docs-release-v0-6-0/24-CONTEXT.md
@.planning/phases/24-exports-docs-release-v0-6-0/24-RESEARCH.md
@.planning/phases/24-exports-docs-release-v0-6-0/24-PATTERNS.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan creates RELEASE-COMMUNICATION artifacts only — NO new source symbols. The source-grounding / drift verification pass should EXCLUDE these as new prose describing already-shipped code:

- CHANGELOG.md `[0.6.0]` entry (Added: 5 namespaces + 10 exported classes; Deprecated: 56 flat names → v0.7.0 removal; Changed: D-06 PostGIS-guard refinement)
- CHANGELOG.md compare-link footer additions ([0.6.0] line)
- MIGRATION.md prepended `## Migration Guide: v0.5.0 → v0.6.0` section (56-name flat→accessor table + before/after examples + PostGIS-guard note)

The exports themselves (`TimescaleAccessor` … `AsyncSchemaAccessor` in `pycopg/__init__.py` `__all__`) ALREADY EXIST from Phases 21-23 — this plan VERIFIES them, it does not create them.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify exports are wired (smoke-test assertion, criterion #1)</name>
  <files>(none — verify-only; pycopg/__init__.py is NOT edited)</files>
  <read_first>
    - pycopg/__init__.py (confirm the 10 accessor classes are present in `__all__` under the # TimescaleDB / # Admin / # Maint / # Backup / # Schema comments — do NOT edit)
    - .planning/phases/24-exports-docs-release-v0-6-0/24-RESEARCH.md ("Critical Verification: Exports Already Complete" — the exact import-smoke command)
  </read_first>
  <action>
    Run the import smoke test asserting all 10 accessor classes (5 sync + 5 async) import from the top-level `pycopg` namespace. RESEARCH VERIFIED `__all__` is already complete (lines added incrementally by Phases 21-23). This task is VERIFY-ONLY: confirm and move on. Do NOT add any new lines to `pycopg/__init__.py` (RESEARCH Anti-Pattern: "Re-implementing exports"). Do NOT hardcode `__version__` (it resolves via `importlib.metadata`). If the smoke test fails, STOP and report — that would mean the exports regressed, which is out of this docs phase's scope to repair.
  </action>
  <verify>
    <automated>python -c "from pycopg import TimescaleAccessor, AsyncTimescaleAccessor, AdminAccessor, AsyncAdminAccessor, MaintAccessor, AsyncMaintAccessor, BackupAccessor, AsyncBackupAccessor, SchemaAccessor, AsyncSchemaAccessor; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from pycopg import TimescaleAccessor, AdminAccessor, SchemaAccessor, MaintAccessor, BackupAccessor, AsyncTimescaleAccessor, AsyncAdminAccessor, AsyncSchemaAccessor, AsyncMaintAccessor, AsyncBackupAccessor; print('OK')"` prints OK and exits 0
    - `git diff --stat pycopg/__init__.py` shows NO changes (file untouched)
  </acceptance_criteria>
  <done>All 10 accessor classes import from `pycopg` (criterion #1 confirmed); `pycopg/__init__.py` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Prepend the v0.5→v0.6 Migration Guide with the 56-name table to MIGRATION.md</name>
  <files>MIGRATION.md</files>
  <read_first>
    - MIGRATION.md (repo ROOT — the existing v0.2→v0.3 content, 464 lines; PREPEND above it, keep it unchanged. NOT docs/migrations.md — that is the Migrator-feature doc, RESEARCH Pitfall 5)
    - .planning/phases/24-exports-docs-release-v0-6-0/24-PATTERNS.md (the pre-populated 56-row table + before/after example per accessor + section template)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-CONTEXT.md (D-06 PostGIS-guard wording for the spatial note)
    - pycopg/database.py (the 56 `@deprecated_alias("<accessor>.<m>")` stubs — the authoritative flat-name → accessor-path source; regenerate the table from these, do NOT hand-type from SCOPE)
  </read_first>
  <action>
    Per D-04: PREPEND a new `## Migration Guide: v0.5.0 → v0.6.0` section ABOVE the existing `# Migration Guide: v0.2.0 to v0.3.0` H1 (keep all v0.2→v0.3 content unchanged below). The new section contains: (a) intro paragraph naming the 5 accessor namespaces and the "removed in v0.7.0" deprecation; (b) one before/after `python` example per accessor (timescale/admin/schema/maint/backup/spatial) per the PATTERNS template, including the D-06 note that the deprecated flat spatial path now raises `ExtensionNotAvailable` early when PostGIS is absent; (c) the COMPLETE 56-row flat-name → accessor-path table grouped by accessor, REGENERATED from live source — run `grep '@deprecated_alias' pycopg/database.py | sed 's/.*@deprecated_alias("\(.*\)")/\1/' | sort` and cross-check the table against it (counts: timescale 6 / admin 11 / schema 27 / maint 6 / backup 4 / spatial 2 = 56). Do NOT use the stale SCOPE counts (admin 12 / schema ~26 are wrong — RESEARCH Pitfall 4). Include the regeneration command in the doc so future maintainers can re-verify. Do NOT touch docs/migrations.md.
  </action>
  <verify>
    <automated>grep -c 'Migration Guide: v0.5.0' MIGRATION.md; grep -c '@deprecated_alias' pycopg/database.py; grep -c 'db\.[a-z_]* | .db\.\(timescale\|admin\|schema\|maint\|backup\|spatial\)\.' MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `MIGRATION.md` line-1 region contains 'Migration Guide: v0.5.0 → v0.6.0' ABOVE the existing 'Migration Guide: v0.2.0 to v0.3.0' section (which remains unchanged)
    - The 56-name table row count matches `grep -c '@deprecated_alias' pycopg/database.py` (= 56) — verify the table lists 56 flat→accessor mappings
    - `MIGRATION.md` contains 'removed in v0.7.0' (or equivalent removal notice) and 'ExtensionNotAvailable' (D-06 note)
    - `MIGRATION.md` table uses live counts: admin shows 11 mappings, schema shows 27 mappings (NOT 12 / ~26)
    - `git diff docs/migrations.md` shows NO changes (wrong file untouched)
  </acceptance_criteria>
  <done>MIGRATION.md has the prepended v0.5→v0.6 guide with the verified 56-row table (1:1 with the live aliases), before/after examples, and the D-06 PostGIS note; the v0.2→v0.3 content is intact below.</done>
</task>

<task type="auto">
  <name>Task 3: Write the CHANGELOG [0.6.0] entry (Added / Deprecated / Changed)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the `[0.5.0]` and `[0.4.0]` entries for the bucket style + the compare-link footer to update)
    - .planning/phases/24-exports-docs-release-v0-6-0/24-PATTERNS.md (the exact [0.6.0] entry text + footer update)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-CONTEXT.md (D-06 wording for the Changed bucket)
  </read_first>
  <action>
    Per D-05: add a `[0.6.0]` entry in Keep-a-Changelog three-bucket shape, matching the `[0.5.0]` style. Place it UNDER the existing `## [Unreleased]` header (do NOT stamp the release date here — 24-03 moves it to `## [0.6.0] - <date>` at release time). Buckets: **Added** — the 5 accessor namespaces (`db.timescale.*` 6, `db.admin.*` 11, `db.schema.*` 27, `db.maint.*` 6, `db.backup.*` 4, async variants, full parity) WITH method counts, plus the 10 accessor classes (TimescaleAccessor/AsyncTimescaleAccessor … SchemaAccessor/AsyncSchemaAccessor) exported from top-level `pycopg`; do NOT enumerate every method. **Deprecated** — one entry: all 56 legacy flat names on `db.*`/`async_db.*` now emit `DeprecationWarning` pointing to the accessor path, scheduled for removal in **v0.7.0**, pointer to MIGRATION.md. **Changed** — the D-06(P23) refinement: deprecated `db.create_spatial_index` / `db.list_geometry_columns` now raise `ExtensionNotAvailable` early via the `db.spatial` PostGIS guard (clearer failure on the deprecated path). Use live counts (admin 11, schema 27 — NOT the stale SCOPE 12/~26). Update the compare-link footer: replace the `[Unreleased]` line to compare from `v0.6.0...HEAD` and add a `[0.6.0]: ...compare/v0.5.0...v0.6.0` line (keep the rest of the footer unchanged).
  </action>
  <verify>
    <automated>grep -c '## \[0.6.0\]' CHANGELOG.md; grep -c 'v0.7.0\|ExtensionNotAvailable' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` contains '## [0.6.0]' with '### Added', '### Deprecated', and '### Changed' subsections
    - The Added bucket states method counts (timescale 6, admin 11, schema 27, maint 6, backup 4) and lists the 10 exported accessor classes
    - The Deprecated bucket mentions all 56 flat names, `DeprecationWarning`, removal in **v0.7.0**, and points to MIGRATION.md
    - The Changed bucket mentions `ExtensionNotAvailable` / the PostGIS-guard refinement on the deprecated spatial path
    - The compare-link footer contains a `[0.6.0]: https://github.com/alkimya/pycopg/compare/v0.5.0...v0.6.0` line and an `[Unreleased]: ...compare/v0.6.0...HEAD` line
  </acceptance_criteria>
  <done>CHANGELOG has a complete [0.6.0] entry (Added/Deprecated/Changed) with live counts, the v0.7.0 removal notice, the D-06 Changed note, and an updated compare-link footer; the date stamp is deferred to 24-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo release docs → public readers | CHANGELOG/MIGRATION are public release-communication; an inaccurate table misleads callers but is not a runtime boundary |
| MIGRATION table ↔ live source | The 56-row table must stay 1:1 with the `@deprecated_alias` stubs; drift would misdirect callers |
| no external/untrusted input | This plan edits Markdown only; no runtime code path, no user input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Tampering | MIGRATION 56-name table | mitigate | Table REGENERATED from live `grep '@deprecated_alias' pycopg/database.py` and cross-checked against `grep -c` (= 56) — not hand-typed from the stale SCOPE doc; prevents a misleading migration map shipping. |
| T-24-04 | Repudiation | CHANGELOG accuracy | mitigate | CHANGELOG counts/buckets sourced from live accessor modules + Phase 23 D-06; the [0.6.0] deprecation/removal-cycle is explicitly recorded with a MIGRATION pointer for auditability. |
| T-24-05 | Information disclosure | CHANGELOG/MIGRATION examples | accept | Examples use `Database.from_env()` placeholders; no secrets, credentials, or connection strings introduced. |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan installs NO packages. No supply-chain surface. (Publish-integrity threats handled in 24-03-PLAN.md.) |
</threat_model>

<verification>
```
grep -c '@deprecated_alias' pycopg/database.py        # 56 — table row-count anchor
python -c "from pycopg import TimescaleAccessor, AsyncSchemaAccessor; print('OK')"   # exports confirm
uv run interrogate pycopg --fail-under 95 --quiet     # unaffected, still green
```
No new packages installed. No `pycopg/*.py` source edits. No `tests/` edits. `pycopg/__init__.py` untouched. `docs/migrations.md` untouched.
</verification>

<success_criteria>
- CHANGELOG has a `[0.6.0]` entry (Added/Deprecated/Changed) with live counts, v0.7.0 removal notice, D-06 Changed note, and updated compare-link footer.
- MIGRATION.md has the prepended v0.5→v0.6 guide with the verified 56-row table (1:1 with live aliases) and before/after examples; v0.2→v0.3 content intact below.
- All 10 accessor classes import from `pycopg` (criterion #1) with `__init__.py` unchanged.
</success_criteria>

<output>
Create `.planning/phases/24-exports-docs-release-v0-6-0/24-02-SUMMARY.md` when done.
</output>
