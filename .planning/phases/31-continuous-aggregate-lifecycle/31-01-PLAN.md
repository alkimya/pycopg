---
phase: 31-continuous-aggregate-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/timescale.py
  - tests/test_timescale.py
autonomous: true
requirements: [TS-ADV-01]
must_haves:
  truths:
    - "User can call db.timescale.create_continuous_aggregate(view, select_sql) and the statement runs on a dedicated connect(autocommit=True) connection (not self._db.execute)"
    - "create_continuous_aggregate raises ValueError before any DB round-trip when select_sql lacks the substring time_bucket("
    - "materialized_only=True (default) renders timescaledb.materialized_only=true; with_no_data=False (default) renders trailing WITH DATA"
    - "async create_continuous_aggregate awaits self._db.schema.has_extension('timescaledb') (no silent guard bypass)"
    - "On the Apache build the live call raises FeatureNotSupported and the live test tolerates it (try/except)"
  artifacts:
    - path: "pycopg/timescale.py"
      provides: "create_continuous_aggregate on both TimescaleAccessor and AsyncTimescaleAccessor"
      contains: "def create_continuous_aggregate"
    - path: "tests/test_timescale.py"
      provides: "mock SQL-shape (sync+async) + live-tolerant integration tests for create"
      contains: "create_continuous_aggregate"
  key_links:
    - from: "TimescaleAccessor.create_continuous_aggregate"
      to: "self._db.connect(autocommit=True)"
      via: "autocommit seam (mirrors etl.py:787/811)"
      pattern: "connect\\(autocommit=True\\)"
    - from: "AsyncTimescaleAccessor.create_continuous_aggregate"
      to: "await self._db.schema.has_extension"
      via: "async extension guard"
      pattern: "await self\\._db\\.schema\\.has_extension"
---

<objective>
Deliver `create_continuous_aggregate` on both `TimescaleAccessor` (sync) and
`AsyncTimescaleAccessor` (async) in `pycopg/timescale.py`, using the
`connect(autocommit=True)` seam (per D-01/D-02), plus authoritative mock SQL-shape tests
and a license-tolerant live integration test in `tests/test_timescale.py`. Implements
TS-ADV-01.

Purpose: Continuous-aggregate creation runs an internal multi-transaction materialization
and cannot execute inside a transaction block — it must run on a dedicated autocommit
connection, isolated from any enclosing session transaction (the architectural element new
to this phase).

Output: `create_continuous_aggregate` (sync + async) in `pycopg/timescale.py`; mock + live
tests in `tests/test_timescale.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-CONTEXT.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-RESEARCH.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-PATTERNS.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols this phase (Phase 31, across all 3 plans) creates — excluded from drift verification:

- `TimescaleAccessor.create_continuous_aggregate` (sync) — this plan
- `AsyncTimescaleAccessor.create_continuous_aggregate` (async) — this plan
- `TimescaleAccessor.refresh_continuous_aggregate` (sync) — plan 02
- `AsyncTimescaleAccessor.refresh_continuous_aggregate` (async) — plan 02
- `TimescaleAccessor.add_continuous_aggregate_policy` (sync) — plan 03
- `AsyncTimescaleAccessor.add_continuous_aggregate_policy` (async) — plan 03
- `_check_offset_ordering` (module-level helper in `pycopg/timescale.py`) — plan 03
- `_OFFSET_RE` (module-level compiled regex in `pycopg/timescale.py`) — plan 03
- Test classes / functions in `tests/test_timescale.py`:
  - `TestCreateContinuousAggregateMock` + `test_create_continuous_aggregate_live` / `_async_live` — this plan
  - `TestRefreshContinuousAggregateMock` + `test_refresh_continuous_aggregate_live` / `_async_live` — plan 02
  - `TestAddContinuousAggregatePolicyMock` + `test_add_continuous_aggregate_policy_live` / `_async_live` — plan 03

This plan's contribution: the two `create_continuous_aggregate` methods + their mock and live test artifacts.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Implement create_continuous_aggregate (sync + async) via the autocommit seam</name>
  <files>pycopg/timescale.py</files>
  <read_first>
    - `pycopg/timescale.py` lines 622-668 (sync `add_reorder_policy`) and 1198-1241 (async `add_reorder_policy`) — the guard/validate/docstring shell to mirror; note the sync class ends at 668 (append the new sync method just before line 669 `class AsyncTimescaleAccessor:`) and the async method is the last method in the file (append after 1241).
    - `pycopg/etl.py` lines 787-789 + 811-817 (sync autocommit seam) and 1486-1488 + 1508-1514 (async autocommit seam) — the verbatim `with self._db.connect(autocommit=True) as conn:` / `async with ... await conn.execute(...)` precedent. Use plain `conn.execute(sql)` (no cursor wrapper — create fetches nothing, per RESEARCH Open-Q #3 / PATTERNS line 29).
    - `pycopg/database.py` line 379 and `pycopg/async_database.py` line 378 — `connect(autocommit=...)` signature (passes autocommit straight to psycopg).
    - `pycopg/utils.py` lines 107 (`validate_identifiers`) — already imported in `timescale.py` line 22.
    - 31-RESEARCH.md §"Recommended approach per method → create" (lines 296-320) and the rendered SQL template (lines 92-97); 31-PATTERNS.md §"create_continuous_aggregate ... AUTOCOMMIT SEAM" (lines 24-63).
  </read_first>
  <action>
    Add `create_continuous_aggregate(self, view_name: str, select_sql: str, schema: str = "public", materialized_only: bool = True, with_no_data: bool = False) -> None` to `TimescaleAccessor` (insert before the `class AsyncTimescaleAccessor:` line) and an `async def` mirror to `AsyncTimescaleAccessor` (append at end of class).

    Guard/validate order (both classes), per 31-RESEARCH lines 298-307:
    1. `validate_identifiers(view_name, schema)` (pre-DB).
    2. D-04 heuristic: `if "time_bucket(" not in select_sql: raise ValueError(...)` BEFORE any DB round-trip and BEFORE opening the seam. Wording per Claude's discretion — must name that a cagg select must contain a `time_bucket(...)` grouping (RESEARCH lines 300-304 gives a model message).
    3. Extension guard — sync: `if not self._db.schema.has_extension("timescaledb"): raise ExtensionNotAvailable(...)`; async: `if not await self._db.schema.has_extension("timescaledb"): raise ExtensionNotAvailable(...)`. Copy the exact two-line message from `add_reorder_policy` (timescale.py:657-660). The `await` on the async guard is MANDATORY (recurring Phase-23 bug).

    Render SQL (no `%s` values — all structure; D-03): set `mo = "true" if materialized_only else "false"` and `data = "NO DATA" if with_no_data else "DATA"`, then build
    `CREATE MATERIALIZED VIEW {schema}.{view_name}` newline
    `WITH (timescaledb.continuous, timescaledb.materialized_only={mo})` newline
    `AS {select_sql}` newline
    `WITH {data}`.
    `select_sql` is structural SQL passed through (document it as not-from-untrusted-input in the docstring, like `aggregates` in the Phase-32 helpers).

    Run via the autocommit seam (D-02) — sync: `with self._db.connect(autocommit=True) as conn: conn.execute(sql)`; async: `async with self._db.connect(autocommit=True) as conn: await conn.execute(sql)`. Do NOT route through `self._db.execute` — the seam IS the isolation guarantee. The license error (`FeatureNotSupported`/`0A000`) must propagate (no swallow), exactly like `add_reorder_policy`.

    Write full numpydoc docstrings (Parameters / Raises: `ExtensionNotAvailable`, `ValueError`, `psycopg.errors.FeatureNotSupported`) mirroring the `add_reorder_policy` docstring (timescale.py:629-654). No fenced code blocks. `datetime` is already imported; no new imports needed for this task.
  </action>
  <acceptance_criteria>
    - `pycopg/timescale.py` contains `def create_continuous_aggregate(` AND `async def create_continuous_aggregate(`.
    - The generated SQL contains `CREATE MATERIALIZED VIEW`, `WITH (timescaledb.continuous, timescaledb.materialized_only=true)`, and a trailing `WITH DATA` for the default flags; flipping flags yields `materialized_only=false` and `WITH NO DATA`.
    - Both methods open `self._db.connect(autocommit=True)` (grep: `grep -c "connect(autocommit=True)" pycopg/timescale.py` increases by 2) and do NOT call `self._db.execute` for the create statement.
    - The async method contains `await self._db.schema.has_extension(` — verify: `sed -n '/async def create_continuous_aggregate/,/CREATE MATERIALIZED VIEW/p' pycopg/timescale.py | grep -c "await self._db.schema.has_extension"` returns ≥1.
    - `uv run ruff check pycopg/timescale.py` exits 0; `uv run black --check pycopg/timescale.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check pycopg/timescale.py && uv run python -c "import pycopg.timescale as t; assert hasattr(t.TimescaleAccessor, 'create_continuous_aggregate') and hasattr(t.AsyncTimescaleAccessor, 'create_continuous_aggregate')"</automated>
  </verify>
  <done>Both sync and async `create_continuous_aggregate` exist, validate `time_bucket(`, guard the extension (async with `await`), render the documented WITH-clause SQL, and run on the autocommit seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mock SQL-shape tests + license-tolerant live tests for create_continuous_aggregate</name>
  <files>tests/test_timescale.py</files>
  <behavior>
    - sync SQL-shape: default flags → captured SQL contains `CREATE MATERIALIZED VIEW public.metrics_hourly`, `WITH (timescaledb.continuous, timescaledb.materialized_only=true)`, trailing `WITH DATA`; `connect` called with `autocommit=True`; `db.execute` NOT called for the create.
    - sync flags flipped (`materialized_only=False, with_no_data=True`) → SQL contains `materialized_only=false` and `WITH NO DATA`.
    - sync ValueError: `select_sql` without `time_bucket(` raises `ValueError` and the seam is never opened (`connect` not called).
    - sync no-extension: `has_extension` returns False → `ExtensionNotAvailable`; `connect` not called.
    - async SQL-shape mirror (AsyncMock seam, `await` the call).
    - async no-extension: `has_extension=AsyncMock(return_value=False)` → `ExtensionNotAvailable` (this fails if `await` is missing on the guard — the Phase-23 catch).
    - live + async-live: build a hypertable via `_make_hypertable`, call `create_continuous_aggregate` with a real `time_bucket(...)` select inside `try/except FeatureNotSupported: pass`; on a Community build additionally assert a `timescaledb_information.continuous_aggregates` row exists.
  </behavior>
  <read_first>
    - `tests/test_timescale.py` lines 936-1012 (`TestAddReorderPolicyMock` — the four-test sync/async mock shape) and 1140-1189 (`test_add_reorder_policy_live` + async live — the `try/except FeatureNotSupported` pattern). Mirror these exactly.
    - `tests/test_timescale.py` lines 46-84 (`ts_db`/`async_ts_db` skip-fixtures) and 85-onward (`_make_hypertable` helper); line 26 (`from psycopg.errors import FeatureNotSupported`).
    - 31-RESEARCH.md §"Test plan → Layer 1 / Layer 2" (lines 386-420) and §"Task 6" info-view columns (lines 214-233) for the `timescaledb_information.continuous_aggregates` assertion (`SELECT 1 FROM timescaledb_information.continuous_aggregates WHERE view_schema = %s AND view_name = %s`).
    - 31-PATTERNS.md §"mock SQL-shape" (lines 105-114) — the seam-specific divergence: the mock must intercept `self._db.connect(autocommit=True)` as a context manager whose `conn.execute` is a MagicMock capturing SQL; assert `connect` called with `autocommit=True`.
  </read_first>
  <action>
    Add a `TestCreateContinuousAggregateMock` class mirroring `TestAddReorderPolicyMock` (test_timescale.py:936-1012), but for the autocommit seam: build a `MagicMock(spec=SchemaAccessor)` (async: `AsyncSchemaAccessor`) with `has_extension` truthy; mock `db.connect` to return a context manager whose `__enter__` yields a `conn` whose `execute` is a `MagicMock` capturing the SQL string (async: `AsyncMock` / async context manager). Assert the captured SQL substrings (per Behavior), assert `db.connect.assert_called_once_with(autocommit=True)`, and assert `db.execute` was NOT used for the create. Add the ValueError test (no `time_bucket(` → `pytest.raises(ValueError)`, assert `db.connect` never called) and the sync+async no-extension tests (mirror reorder :962-977 / :997-1012).

    Add `test_create_continuous_aggregate_live(self, ts_db)` and `test_create_continuous_aggregate_async_live(self, async_ts_db)` mirroring `test_add_reorder_policy_live` (:1140-1189): `_make_hypertable(...)` inside `try: ... finally: DROP TABLE`, with an inner `try: create_continuous_aggregate(view, "SELECT time_bucket('1 hour', time) AS bucket, avg(value) AS avg_value FROM <table> GROUP BY 1") ; <Community-build assert via inlined timescaledb_information.continuous_aggregates query> except FeatureNotSupported: pass`. Inline the info-view assertion (no `queries.py` constant — RESEARCH Task 6).
  </action>
  <acceptance_criteria>
    - `tests/test_timescale.py` contains `class TestCreateContinuousAggregateMock` and `def test_create_continuous_aggregate_live` and `test_create_continuous_aggregate_async_live`.
    - `uv run pytest tests/test_timescale.py -k create_continuous_aggregate -o addopts=""` exits 0.
    - The async no-extension test fails if `await` is removed from the async guard (manual reasoning: AsyncMock coroutine is truthy without `await`).
    - `uv run pytest tests/test_timescale.py -x -q -o addopts=""` exits 0 (full file green).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_timescale.py -k create_continuous_aggregate -o addopts=""</automated>
  </verify>
  <done>Mock SQL-shape tests (sync+async, flags, ValueError, no-extension) assert the autocommit seam and SQL substrings; live + async-live tests tolerate `FeatureNotSupported` and assert the info-view row on Community builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `create_continuous_aggregate(view_name, schema, select_sql)` | `view_name`/`schema` are identifiers interpolated into DDL; `select_sql` is structural SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering (SQL injection) | `view_name` / `schema` interpolated into `CREATE MATERIALIZED VIEW` | mitigate | `validate_identifiers(view_name, schema)` pre-DB (utils.py:107) — same gate every timescale method uses |
| T-31-02 | Tampering | `select_sql` passed through as structural SQL | accept | Documented not-from-untrusted-input (like `aggregates` in Phase-32 helpers); caller-owned SQL, consistent with established pycopg contract |
| T-31-03 | Information disclosure | none — create returns nothing | accept | No data returned to caller |
</threat_model>

<verification>
- `uv run pytest tests/test_timescale.py -k create_continuous_aggregate -o addopts=""` exits 0.
- `uv run pytest tests/test_timescale.py tests/test_parity.py -o addopts=""` exits 0 (parity auto-covers the new method on both classes).
- `uv run ruff check pycopg/timescale.py tests/test_timescale.py` and `uv run black --check pycopg/timescale.py tests/test_timescale.py` exit 0.
</verification>

<success_criteria>
- `create_continuous_aggregate` exists on both `TimescaleAccessor` and `AsyncTimescaleAccessor`, runs on `connect(autocommit=True)`, raises `ValueError` pre-DB when `select_sql` lacks `time_bucket(`, and the async guard `await`s `has_extension`.
- Mock tests are authoritative for the generated SQL + seam usage; live tests are license-tolerant.
- ROADMAP success criterion #1 (autocommit-isolated create + `time_bucket(` ValueError) is structurally proven at the mock layer.
</success_criteria>

<output>
Create `.planning/phases/31-continuous-aggregate-lifecycle/31-01-SUMMARY.md` when done.
</output>
