---
phase: 31-continuous-aggregate-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - pycopg/timescale.py
  - tests/test_timescale.py
autonomous: true
requirements: [TS-ADV-02]
must_haves:
  truths:
    - "User can call db.timescale.refresh_continuous_aggregate(view) and it runs CALL refresh_continuous_aggregate(...) on a dedicated connect(autocommit=True) connection"
    - "Both window bounds None emit params [None, None] → SQL NULL, NULL (full refresh); one side None emits NULL for that side"
    - "A str window bound is rejected with a clear ValueError before any DB round-trip (deliberate divergence from drop_chunks — no str→%s::interval cast)"
    - "async refresh_continuous_aggregate awaits self._db.schema.has_extension('timescaledb')"
    - "Refresh from inside a db.session() raises only the license error on Apache (not a transaction-block error), proving the seam bypasses the enclosing transaction"
  artifacts:
    - path: "pycopg/timescale.py"
      provides: "refresh_continuous_aggregate on both TimescaleAccessor and AsyncTimescaleAccessor"
      contains: "def refresh_continuous_aggregate"
    - path: "tests/test_timescale.py"
      provides: "mock SQL-shape (sync+async) + live-tolerant integration tests for refresh"
      contains: "refresh_continuous_aggregate"
  key_links:
    - from: "TimescaleAccessor.refresh_continuous_aggregate"
      to: "self._db.connect(autocommit=True)"
      via: "autocommit seam (mirrors etl.py:811/1508)"
      pattern: "connect\\(autocommit=True\\)"
    - from: "refresh_continuous_aggregate"
      to: "CALL refresh_continuous_aggregate('{schema}.{view_name}', %s, %s)"
      via: "%s-bound window params [window_start, window_end]"
      pattern: "CALL refresh_continuous_aggregate"
---

<objective>
Deliver `refresh_continuous_aggregate` on both `TimescaleAccessor` (sync) and
`AsyncTimescaleAccessor` (async) in `pycopg/timescale.py`, using the
`connect(autocommit=True)` seam (D-02), with absolute-timestamp window bounds
(`datetime | None` only — D-05), plus authoritative mock tests and a license-tolerant live
test. Implements TS-ADV-02.

Purpose: Manual cagg refresh runs across two transactions (move invalidation threshold +
materialize) and batches by default on TSDB 2.28, so it cannot run inside a transaction
block — the autocommit seam isolates it from any enclosing `db.session()`.

Output: `refresh_continuous_aggregate` (sync + async) in `pycopg/timescale.py`; mock + live
tests in `tests/test_timescale.py`.

Depends on plan 01 (shared edits to `pycopg/timescale.py` and `tests/test_timescale.py` —
must run after 01 to avoid clobbering; see `depends_on`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-CONTEXT.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-RESEARCH.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-PATTERNS.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-VALIDATION.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this phase (Phase 31, across all 3 plans) creates — excluded from drift verification:

- `TimescaleAccessor.create_continuous_aggregate` / `AsyncTimescaleAccessor.create_continuous_aggregate` — plan 01
- `TimescaleAccessor.refresh_continuous_aggregate` (sync) — this plan
- `AsyncTimescaleAccessor.refresh_continuous_aggregate` (async) — this plan
- `TimescaleAccessor.add_continuous_aggregate_policy` / async — plan 03
- `_check_offset_ordering`, `_OFFSET_RE` (module-level in `pycopg/timescale.py`) — plan 03
- Test classes / functions in `tests/test_timescale.py`:
  - `TestCreateContinuousAggregateMock` + create live/async-live — plan 01
  - `TestRefreshContinuousAggregateMock` + `test_refresh_continuous_aggregate_live` / `_async_live` — this plan
  - `TestAddContinuousAggregatePolicyMock` + policy live/async-live — plan 03

This plan's contribution: the two `refresh_continuous_aggregate` methods + their mock and live test artifacts.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Implement refresh_continuous_aggregate (sync + async) via the autocommit seam</name>
  <files>pycopg/timescale.py</files>
  <read_first>
    - `pycopg/timescale.py` — the `create_continuous_aggregate` methods added by plan 01 (sync near end of `TimescaleAccessor`, async at end of `AsyncTimescaleAccessor`); add `refresh_continuous_aggregate` immediately after each, same class. Also lines 622-668 / 1198-1241 (`add_reorder_policy`) for the guard/docstring shell.
    - `pycopg/etl.py` lines 811-817 (sync seam) and 1508-1514 (async seam) — `with self._db.connect(autocommit=True) as conn:` with params; for refresh use `conn.execute(sql, [window_start, window_end])` (async: `await conn.execute(sql, [window_start, window_end])`). Plain `conn.execute` (no cursor — refresh fetches nothing).
    - `from datetime import datetime` is already imported at `pycopg/timescale.py` line 15 — use `isinstance(bound, datetime)` for the D-05 type guard.
    - 31-RESEARCH.md §"Task 3" (lines 103-135) — exact `pg_proc` procedure signature, `CALL` form, `NULL,NULL`=full refresh, absolute-timestamp windows; §"Recommended approach → refresh" (lines 322-337). 31-PATTERNS.md §"refresh pre-DB guard" (lines 65-70).
  </read_first>
  <action>
    Add `refresh_continuous_aggregate(self, view_name: str, window_start: datetime | None = None, window_end: datetime | None = None, schema: str = "public") -> None` to `TimescaleAccessor` and an `async def` mirror to `AsyncTimescaleAccessor`.

    Guard/validate order (both classes):
    1. `validate_identifiers(view_name, schema)` (pre-DB).
    2. D-05 type guard (DELIBERATE divergence from Phase-30 `drop_chunks` — do NOT copy the `str→%s::interval` cast): for each of `window_start`, `window_end`, `if bound is not None and not isinstance(bound, datetime): raise ValueError(...)`. The message must state bounds are `datetime` or `None` (absolute timestamps) and that relative interval strings are not accepted (RESEARCH lines 325-331 gives a model message). This runs BEFORE the seam opens.
    3. Extension guard — sync plain / async `await self._db.schema.has_extension(...)` (MANDATORY `await`), same `ExtensionNotAvailable` message as `add_reorder_policy`.

    Render SQL (D-06 — both positional window args always present):
    `CALL refresh_continuous_aggregate('{schema}.{view_name}', %s, %s)` where `{schema}.{view_name}` is interpolated after identifier validation (regclass literal — psycopg cannot bind it via `%s`, RESEARCH lines 133-135). Params are `[window_start, window_end]`; `None` binds to SQL `NULL` (full refresh when both None; one-sided NULL otherwise).

    Run via the autocommit seam (D-02) — sync: `with self._db.connect(autocommit=True) as conn: conn.execute(sql, [window_start, window_end])`; async: `async with self._db.connect(autocommit=True) as conn: await conn.execute(sql, [window_start, window_end])`. Do NOT route through `self._db.execute`. License error propagates.

    Full numpydoc docstrings (Parameters; Raises: `ExtensionNotAvailable`, `ValueError`, `FeatureNotSupported`). No fenced code blocks. No new imports.
  </action>
  <acceptance_criteria>
    - `pycopg/timescale.py` contains `def refresh_continuous_aggregate(` AND `async def refresh_continuous_aggregate(`.
    - Generated SQL equals `CALL refresh_continuous_aggregate('public.metrics_hourly', %s, %s)` for default schema, with params `[None, None]` when both bounds omitted.
    - A `str` window bound raises `ValueError` before the seam opens; a `datetime` is accepted.
    - Both methods open `self._db.connect(autocommit=True)` (grep count increases by 2) and do NOT call `self._db.execute` for the refresh.
    - Async method contains `await self._db.schema.has_extension(`.
    - `uv run ruff check pycopg/timescale.py` and `uv run black --check pycopg/timescale.py` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check pycopg/timescale.py && uv run python -c "import pycopg.timescale as t; assert hasattr(t.TimescaleAccessor,'refresh_continuous_aggregate') and hasattr(t.AsyncTimescaleAccessor,'refresh_continuous_aggregate')"</automated>
  </verify>
  <done>Both sync and async `refresh_continuous_aggregate` exist, reject str bounds with ValueError, emit `CALL refresh_continuous_aggregate(..., %s, %s)` with `[start, end]` params (None→NULL), guard the extension (async `await`), and run on the autocommit seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mock SQL-shape tests + license-tolerant live tests for refresh_continuous_aggregate</name>
  <files>tests/test_timescale.py</files>
  <behavior>
    - sync SQL-shape: default (both None) → captured SQL == `CALL refresh_continuous_aggregate('public.metrics_hourly', %s, %s)`, params == `[None, None]`; `connect` called with `autocommit=True`; `db.execute` NOT called.
    - sync with a `datetime` start and None end → params == `[<datetime>, None]`.
    - sync ValueError: a `str` bound (e.g. `window_start="7 days"`) raises `ValueError`; seam never opened (`connect` not called).
    - sync no-extension → `ExtensionNotAvailable`; `connect` not called.
    - async SQL-shape mirror (AsyncMock seam, `await` the call).
    - async no-extension: `AsyncMock(return_value=False)` → `ExtensionNotAvailable` (Phase-23 `await` catch).
    - live + async-live: create a hypertable, (optionally create a cagg) and call `refresh_continuous_aggregate` from inside a `db.session()` / `async with db.session()` block inside `try/except FeatureNotSupported: pass`; confirm no transaction-block error surfaces (only the license error is tolerated). On a Community build, assert materialized rows appear.
  </behavior>
  <read_first>
    - `tests/test_timescale.py` — the `TestCreateContinuousAggregateMock` class and live tests added by plan 01 (mirror their seam-mock structure); `TestAddReorderPolicyMock` (:936-1012) and `test_add_reorder_policy_live` (:1140-1189) as the underlying shape.
    - `tests/test_timescale.py` lines 46-84 (fixtures), 85+ (`_make_hypertable`), 26 (`FeatureNotSupported` import). For the session-isolation live proof, read how `db.session()` is used elsewhere — grep `db.session(` in `tests/` for an existing call shape.
    - 31-RESEARCH.md §"Test plan → Layer 1/2" (lines 386-420) and §"Autocommit-isolation proofs → STRUCTURAL" (lines 422-433); 31-CONTEXT.md D-10 (REVISED) lines 126-141. The mock-level structural proof (assert `connect(autocommit=True)` used, not the session path) is the AUTHORITATIVE isolation proof.
  </read_first>
  <action>
    Add a `TestRefreshContinuousAggregateMock` class mirroring the create mock class from plan 01: intercept `db.connect(autocommit=True)` as a context manager whose `conn.execute` captures `(sql, params)`; assert the exact `CALL ...` SQL and the params list per Behavior; assert `db.connect.assert_called_once_with(autocommit=True)` and `db.execute` not used. Add the str-bound ValueError test (assert `db.connect` never called) and sync+async no-extension tests.

    Add `test_refresh_continuous_aggregate_live(self, ts_db)` and `test_refresh_continuous_aggregate_async_live(self, async_ts_db)`: build a hypertable, and call `refresh_continuous_aggregate(view)` from INSIDE a `with ts_db.session():` / `async with async_ts_db.session():` block, wrapped in `try/except FeatureNotSupported: pass` (the structural-isolation observable part — D-10b). The test passes on Apache by tolerating `0A000` and NOT seeing a transaction-block error; on a Community build it additionally asserts materialized rows. Inline any info-view query (no `queries.py` constant).
  </action>
  <acceptance_criteria>
    - `tests/test_timescale.py` contains `class TestRefreshContinuousAggregateMock`, `def test_refresh_continuous_aggregate_live`, `test_refresh_continuous_aggregate_async_live`.
    - `uv run pytest tests/test_timescale.py -k refresh_continuous_aggregate -o addopts=""` exits 0.
    - Mock test asserts the seam is `connect(autocommit=True)` (the structural isolation proof, license-independent).
    - `uv run pytest tests/test_timescale.py -x -q -o addopts=""` exits 0 (full file green).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_timescale.py -k refresh_continuous_aggregate -o addopts=""</automated>
  </verify>
  <done>Mock tests authoritatively assert the `CALL` SQL, the `[start, end]` params (None→NULL), the seam, and the str-bound ValueError; live tests run refresh from inside a `db.session()` and tolerate `FeatureNotSupported`, proving the seam bypasses the enclosing transaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `refresh_continuous_aggregate(view_name, schema, window_start, window_end)` | `view_name`/`schema` interpolated into a `regclass` literal; window bounds bound as `%s` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering (SQL injection) | `view_name` / `schema` interpolated into the `CALL refresh_continuous_aggregate('...')` regclass literal | mitigate | `validate_identifiers(view_name, schema)` pre-DB (utils.py:107); regclass cannot be `%s`-bound, identifier-validation is the established pycopg gate |
| T-31-05 | Tampering | `window_start` / `window_end` runtime values | mitigate | Bound as `%s` (psycopg adapts datetime→timestamptz); D-05 type guard rejects non-datetime/non-None before binding |
| T-31-06 | Denial of service (full-table refresh) | both bounds None = full-range refresh | accept | Documented full-refresh semantics (D-06); refresh is read-from-source/materialize, not destructive |
</threat_model>

<verification>
- `uv run pytest tests/test_timescale.py -k refresh_continuous_aggregate -o addopts=""` exits 0.
- `uv run pytest tests/test_timescale.py tests/test_parity.py -o addopts=""` exits 0.
- `uv run ruff check pycopg/timescale.py tests/test_timescale.py` and `uv run black --check ...` exit 0.
</verification>

<success_criteria>
- `refresh_continuous_aggregate` exists on both classes, runs on `connect(autocommit=True)`, rejects str bounds with `ValueError`, emits `CALL refresh_continuous_aggregate(..., %s, %s)` with `[start, end]` params, and the async guard `await`s `has_extension`.
- The mock-level structural isolation proof (seam = autocommit connect) is authoritative; the live test runs refresh inside `db.session()` and tolerates the license error.
- ROADMAP success criterion #2 (autocommit-isolated refresh from inside a session) is structurally proven.
</success_criteria>

<output>
Create `.planning/phases/31-continuous-aggregate-lifecycle/31-02-SUMMARY.md` when done.
</output>
