---
phase: 31-continuous-aggregate-lifecycle
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - pycopg/timescale.py
  - tests/test_timescale.py
autonomous: true
requirements: [TS-ADV-03]
must_haves:
  truths:
    - "User can call db.timescale.add_continuous_aggregate_policy(view, start_offset, end_offset) and it runs SELECT add_continuous_aggregate_policy(...) via plain self._db.execute (NOT the autocommit seam — D-01)"
    - "if_not_exists=True (default) renders ', if_not_exists => true'; if_not_exists=False omits it"
    - "Same-unit start_offset shorter than end_offset (e.g. '1 hour' vs '7 hours') raises ValueError before any DB round-trip; mixed-unit/month/year defer to the DB"
    - "A None offset renders the SQL literal NULL (not INTERVAL 'None')"
    - "async add_continuous_aggregate_policy awaits self._db.schema.has_extension('timescaledb'); license error (FeatureNotSupported) propagates (no swallow)"
    - "test_accessor_parity passes — all 3 new methods mirrored on AsyncTimescaleAccessor"
  artifacts:
    - path: "pycopg/timescale.py"
      provides: "add_continuous_aggregate_policy on both classes + _check_offset_ordering/_OFFSET_RE module helper"
      contains: "def add_continuous_aggregate_policy"
    - path: "tests/test_timescale.py"
      provides: "mock SQL-shape (sync+async) + offset-ordering ValueError + live-tolerant tests for policy"
      contains: "add_continuous_aggregate_policy"
  key_links:
    - from: "TimescaleAccessor.add_continuous_aggregate_policy"
      to: "self._db.execute"
      via: "plain execute (NOT the autocommit seam — D-01)"
      pattern: "add_continuous_aggregate_policy\\("
    - from: "add_continuous_aggregate_policy"
      to: "_check_offset_ordering(start_offset, end_offset)"
      via: "same-unit best-effort ValueError guard (D-07)"
      pattern: "_check_offset_ordering"
---

<objective>
Deliver `add_continuous_aggregate_policy` on both `TimescaleAccessor` (sync) and
`AsyncTimescaleAccessor` (async) in `pycopg/timescale.py`, using the PLAIN
`self._db.execute()` path (D-01 — transaction-safe, like the 3 shipped policy methods),
plus the `_check_offset_ordering` best-effort guard (D-07), authoritative mock tests, a
license-tolerant live test, and the final `test_accessor_parity` confirmation for all 3 new
methods. Implements TS-ADV-03.

Purpose: The auto-refresh policy is an ordinary `SELECT add_continuous_aggregate_policy(...)`
function call (transaction-safe) — it does NOT need the autocommit seam, exactly like
`add_compression_policy`/`add_retention_policy`/`add_reorder_policy`. The same-unit offset
guard catches the unambiguous `start_offset <= end_offset` error in Python before any DB
round-trip (ROADMAP criterion #3), deferring calendar/mixed units to the DB.

Output: `add_continuous_aggregate_policy` (sync + async) + `_check_offset_ordering` helper in
`pycopg/timescale.py`; mock + live tests in `tests/test_timescale.py`.

Depends on plan 02 (shared edits to `pycopg/timescale.py` and `tests/test_timescale.py`).
This is the last plan — it runs the full 3-method parity gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-CONTEXT.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-RESEARCH.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-PATTERNS.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-VALIDATION.md
@.planning/phases/31-continuous-aggregate-lifecycle/31-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this phase (Phase 31, across all 3 plans) creates — excluded from drift verification:

- `TimescaleAccessor.create_continuous_aggregate` / async — plan 01
- `TimescaleAccessor.refresh_continuous_aggregate` / async — plan 02
- `TimescaleAccessor.add_continuous_aggregate_policy` (sync) — this plan
- `AsyncTimescaleAccessor.add_continuous_aggregate_policy` (async) — this plan
- `_check_offset_ordering` (module-level helper in `pycopg/timescale.py`) — this plan
- `_OFFSET_RE` (module-level compiled regex in `pycopg/timescale.py`) — this plan
- Test classes / functions in `tests/test_timescale.py`:
  - `TestCreateContinuousAggregateMock` + create live/async-live — plan 01
  - `TestRefreshContinuousAggregateMock` + refresh live/async-live — plan 02
  - `TestAddContinuousAggregatePolicyMock` + `test_add_continuous_aggregate_policy_live` / `_async_live` — this plan

This plan's contribution: the two policy methods, the `_check_offset_ordering`/`_OFFSET_RE` helper, and the policy mock/live test artifacts.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the _check_offset_ordering helper + import re</name>
  <files>pycopg/timescale.py</files>
  <read_first>
    - `pycopg/timescale.py` lines 13-26 (module header / imports) — add `import re` to the stdlib import group (top, before `from datetime import datetime` at line 15, alphabetical within the stdlib block). The helper is a module-level function (place it after the imports / before the first class, or just above `TimescaleAccessor` — match where other module-level helpers live; if none, put it directly after the `TYPE_CHECKING` block).
    - `pycopg/utils.py` line 125 (`validate_interval`) and line 16 (`_INTERVAL_PATTERN`) — the comparator runs AFTER `validate_interval` syntax-checks each offset.
    - 31-RESEARCH.md §"Task 4 → D-07 comparator" (lines 176-191) and the helper skeleton (lines 359-373); 31-PATTERNS.md §"No Analog Found" (lines 144-149) — this is new pure logic, no exact analog.
  </read_first>
  <action>
    Add `import re` to the stdlib import block at the top of `pycopg/timescale.py`. Define a module-level compiled regex `_OFFSET_RE = re.compile(r"^(\d+)\s+(second|minute|hour|day|week)s?$", re.IGNORECASE)` and a module-level function `_check_offset_ordering(start_offset, end_offset) -> None`:
    - If either offset is `None`, return (open-ended → DB authority).
    - Match each offset (stripped) against `_OFFSET_RE`. If BOTH match AND share the same unit (group 2, case-insensitive), compare the integer counts (group 1); raise `ValueError` if `start_offset_count <= end_offset_count` (start must cover a LONGER window than end). Message names both offsets (RESEARCH lines 367-371 gives a model).
    - Otherwise (no match, mixed units, or month/year) return — defer to the DB (it raises `22023` on a Community build).

    Give the function a numpydoc docstring stating it is a best-effort same-unit guard and that the DB is the final authority on offset ordering (D-07). No fenced code blocks. Cover the fixed-duration units `second|minute|hour|day|week` only; do NOT add an interval-to-seconds parser (zero new deps — only stdlib `re`).
  </action>
  <acceptance_criteria>
    - `pycopg/timescale.py` contains `import re`, `_OFFSET_RE = re.compile(`, and `def _check_offset_ordering(`.
    - `_check_offset_ordering("1 hour", "7 hours")` raises `ValueError`; `_check_offset_ordering("7 days", "1 day")` returns None; `_check_offset_ordering("1 day", "6 hours")` returns None (mixed-unit deferred); `_check_offset_ordering("1 month", "1 day")` returns None (calendar deferred); `_check_offset_ordering(None, "1 day")` returns None.
    - `uv run ruff check pycopg/timescale.py` and `uv run black --check pycopg/timescale.py` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import pytest; from pycopg.timescale import _check_offset_ordering as c; [c(*a) for a in [('7 days','1 day'),('1 day','6 hours'),('1 month','1 day'),(None,'1 day')]]; pytest.raises(ValueError, c, '1 hour', '7 hours')"</automated>
  </verify>
  <done>`_check_offset_ordering` raises `ValueError` for same-unit start≤end and returns for None/mixed/calendar units; `import re` added; lint+format clean.</done>
</task>

<task type="auto">
  <name>Task 2: Implement add_continuous_aggregate_policy (sync + async) via plain execute</name>
  <files>pycopg/timescale.py</files>
  <read_first>
    - `pycopg/timescale.py` lines 622-668 (sync `add_reorder_policy`) and 1198-1241 (async `add_reorder_policy`) — the EXACT analog: plain `self._db.execute(...)`, `ne = ", if_not_exists => true" if if_not_exists else ""` verbatim (line 664), license-tolerance docstring (lines 651-654), extension guard. Mirror line-for-line. Append the new policy methods after the `refresh_continuous_aggregate` methods added by plan 02 in each class.
    - The `_check_offset_ordering` helper added in Task 1; `validate_interval` (utils.py:125, already imported).
    - 31-RESEARCH.md §"Task 4" (lines 137-191) — exact `pg_proc` signature, named args, `None`→`NULL` rendering (Open-Q #2, lines 374-377); §"Recommended approach → policy" (lines 339-377). 31-PATTERNS.md §"add_continuous_aggregate_policy — PLAIN execute()" (lines 73-101).
  </read_first>
  <action>
    Add `add_continuous_aggregate_policy(self, view_name: str, start_offset: str | None, end_offset: str | None, schedule_interval: str = "1 hour", schema: str = "public", if_not_exists: bool = True) -> None` to `TimescaleAccessor` and an `async def` mirror to `AsyncTimescaleAccessor`.

    Validate order (both classes):
    1. `validate_identifiers(view_name, schema)`.
    2. `if start_offset is not None: validate_interval(start_offset)`; same for `end_offset`. (Skip `None` — open-ended.)
    3. `_check_offset_ordering(start_offset, end_offset)` (D-07 — raises `ValueError` pre-DB for same-unit start≤end).
    4. `validate_interval(schedule_interval)`.
    5. Extension guard — sync plain / async `await self._db.schema.has_extension(...)` (MANDATORY `await`), same `ExtensionNotAvailable` message.

    Render via PLAIN `self._db.execute(...)` (D-01 — NOT the autocommit seam; async: `await self._db.execute(...)`). Build the call with named args mirroring `add_reorder_policy`:
    `SELECT add_continuous_aggregate_policy('{schema}.{view_name}', start_offset => <frag_start>, end_offset => <frag_end>, schedule_interval => INTERVAL '{schedule_interval}'{ne}) AS job_id`
    where `ne = ", if_not_exists => true" if if_not_exists else ""` (verbatim), and each offset fragment is `NULL` when the offset is `None`, else `INTERVAL '{offset}'` (D-07 / RESEARCH Open-Q #2 — branch per-offset; do NOT emit `INTERVAL 'None'`).

    The license error (`FeatureNotSupported`/`0A000`) must PROPAGATE — no swallow in the method body (mirror the `add_reorder_policy` docstring `Raises` section, timescale.py:651-654). Full numpydoc docstrings. No fenced code blocks. No new imports (Task 1 added `re`).
  </action>
  <acceptance_criteria>
    - `pycopg/timescale.py` contains `def add_continuous_aggregate_policy(` AND `async def add_continuous_aggregate_policy(`.
    - Generated SQL contains `add_continuous_aggregate_policy(`, `start_offset =>`, `end_offset =>`, `schedule_interval => INTERVAL '1 hour'`, and `, if_not_exists => true` (default); `if_not_exists=False` omits the `if_not_exists` fragment.
    - A `None` offset renders `start_offset => NULL` (or `end_offset => NULL`), never `INTERVAL 'None'`.
    - Both methods use `self._db.execute` (NOT `connect(autocommit=True)`) — grep `connect(autocommit=True)` count does NOT increase in this task.
    - Async method contains `await self._db.schema.has_extension(`.
    - `add_continuous_aggregate_policy(view, start_offset="1 hour", end_offset="7 hours")` raises `ValueError` (start shorter than end, same-unit) before any execute.
    - `uv run ruff check pycopg/timescale.py` and `uv run black --check pycopg/timescale.py` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check pycopg/timescale.py && uv run python -c "import pycopg.timescale as t; assert hasattr(t.TimescaleAccessor,'add_continuous_aggregate_policy') and hasattr(t.AsyncTimescaleAccessor,'add_continuous_aggregate_policy')"</automated>
  </verify>
  <done>Both sync and async `add_continuous_aggregate_policy` exist, use plain `execute` (not the seam), render named-arg SQL with `if_not_exists` + `NULL`-for-None offsets, call `_check_offset_ordering`, guard the extension (async `await`), and propagate the license error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mock + offset-ordering + live-tolerant tests for policy, and the full 3-method parity gate</name>
  <files>tests/test_timescale.py</files>
  <behavior>
    - sync SQL-shape: captured SQL contains `add_continuous_aggregate_policy('public.metrics_hourly'`, `start_offset => INTERVAL '7 days'`, `end_offset => INTERVAL '1 hour'`, `schedule_interval => INTERVAL '1 hour'`, `, if_not_exists => true`; `db.execute` used; `db.connect` NOT called (plain execute, not the seam).
    - sync `if_not_exists=False` → SQL omits `if_not_exists =>`.
    - sync None offset → SQL renders `start_offset => NULL` (not `INTERVAL 'None'`).
    - sync offset-ordering ValueError: `start_offset="1 hour", end_offset="7 hours"` raises `ValueError` before any execute; mixed-unit `start_offset="1 day", end_offset="6 hours"` does NOT raise in Python.
    - sync no-extension → `ExtensionNotAvailable`; `db.execute` not called.
    - async SQL-shape mirror + async no-extension (Phase-23 `await` catch).
    - live + async-live: build a hypertable, call `add_continuous_aggregate_policy` inside `try/except FeatureNotSupported: pass`; on a Community build assert a `timescaledb_information.jobs` row (`proc_name='policy_refresh_continuous_aggregate'`) and `CALL run_job(job_id)` succeeds.
    - parity: `test_accessor_parity` passes — all 3 new methods exist on both classes.
  </behavior>
  <read_first>
    - `tests/test_timescale.py` lines 936-1012 (`TestAddReorderPolicyMock` — the policy mock uses plain `db.execute`, NOT a seam: this is the closest analog for the policy mock) and 1140-1189 (`test_add_reorder_policy_live`).
    - The create/refresh mock classes added by plans 01/02 (for the async no-extension shape).
    - `tests/test_timescale.py` lines 46-84 (fixtures), 85+ (`_make_hypertable`), 26 (`FeatureNotSupported`).
    - 31-RESEARCH.md §"Task 6" (lines 214-233) — `timescaledb_information.jobs` columns + assertion (`SELECT job_id FROM timescaledb_information.jobs WHERE hypertable_name = %s AND proc_name = 'policy_refresh_continuous_aggregate'`) and `CALL run_job(job_id)`; §"Test plan → policy" (lines 401-408).
    - `tests/test_parity.py` lines 24-36 — `ACCESSOR_PAIRS` already registers `(TimescaleAccessor, AsyncTimescaleAccessor)`; NO registry change.
  </read_first>
  <action>
    Add a `TestAddContinuousAggregatePolicyMock` class mirroring `TestAddReorderPolicyMock` (test_timescale.py:936-1012) — plain `db.execute` mock (NOT a seam): assert the SQL substrings per Behavior, assert `db.execute` used and `db.connect` NOT called. Add the offset-ordering ValueError test (same-unit start≤end raises; mixed-unit does not), the `if_not_exists=False` test, the `None`-offset → `NULL` test, and sync+async no-extension tests.

    Add `test_add_continuous_aggregate_policy_live(self, ts_db)` and `test_add_continuous_aggregate_policy_async_live(self, async_ts_db)` mirroring `test_add_reorder_policy_live` (:1140-1189): hypertable in `try/finally DROP`, inner `try: add_continuous_aggregate_policy(...) ; <Community-build assert: inlined timescaledb_information.jobs row + CALL run_job(job_id)> except FeatureNotSupported: pass`. Inline the info-view query (no `queries.py` constant).

    Do NOT modify `tests/test_parity.py` — `ACCESSOR_PAIRS` already covers the pair. Run `test_accessor_parity` as the final gate (it auto-discovers all 3 new methods now that they exist on both classes).
  </action>
  <acceptance_criteria>
    - `tests/test_timescale.py` contains `class TestAddContinuousAggregatePolicyMock`, `def test_add_continuous_aggregate_policy_live`, `test_add_continuous_aggregate_policy_async_live`.
    - `uv run pytest tests/test_timescale.py -k continuous_aggregate_policy -o addopts=""` exits 0.
    - `uv run pytest tests/test_parity.py -o addopts=""` exits 0 (parity auto-covers the 3 new methods; no registry change).
    - `uv run pytest tests/test_timescale.py -x -q -o addopts=""` exits 0 (full file green).
    - Full suite gate: `uv run pytest` exits 0 with coverage ≥94% (2 named pre-existing flaky DB tests aside — rerun targeted with `-o addopts=""` if they flake).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_timescale.py -k continuous_aggregate_policy -o addopts="" && uv run pytest tests/test_parity.py -o addopts=""</automated>
  </verify>
  <done>Policy mock tests authoritatively assert plain-execute SQL (named args, `if_not_exists`, `NULL`-for-None) + offset-ordering ValueError; live tests tolerate `FeatureNotSupported`; `test_accessor_parity` confirms all 3 new methods are mirrored on both classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `add_continuous_aggregate_policy(view_name, schema, start_offset, end_offset, schedule_interval)` | `view_name`/`schema` interpolated into the regclass literal; interval offsets interpolated as `INTERVAL '...'` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Tampering (SQL injection) | `view_name` / `schema` interpolated into the policy regclass literal | mitigate | `validate_identifiers(view_name, schema)` pre-DB (utils.py:107) |
| T-31-08 | Tampering (SQL injection) | `start_offset` / `end_offset` / `schedule_interval` interpolated as `INTERVAL '...'` literals | mitigate | `validate_interval(...)` on each non-None offset + `schedule_interval` (utils.py:125) — rejects any non-interval syntax before interpolation |
| T-31-09 | Tampering | offset-ordering guard `_check_offset_ordering` | mitigate | Pure stdlib `re` regex, no eval/exec; raises `ValueError` only |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | None — zero new dependencies this phase (D-07); only stdlib `re` used. No package installs → no slopcheck/legitimacy gate needed. |
</threat_model>

<verification>
- `uv run pytest tests/test_timescale.py -k continuous_aggregate_policy -o addopts=""` exits 0.
- `uv run pytest tests/test_timescale.py tests/test_parity.py -o addopts=""` exits 0 (3-method parity confirmed).
- `uv run pytest` (full suite) green, coverage ≥94%.
- `uv run ruff check pycopg/timescale.py tests/test_timescale.py` and `uv run black --check ...` exit 0.
</verification>

<success_criteria>
- `add_continuous_aggregate_policy` exists on both classes, uses PLAIN `self._db.execute` (not the seam), renders named-arg SQL with `if_not_exists` + `NULL`-for-None offsets, and propagates the license error.
- `_check_offset_ordering` raises `ValueError` for same-unit start≤end before any DB round-trip; mixed/calendar units defer to the DB.
- `test_accessor_parity` passes — all 3 new methods mirrored on `AsyncTimescaleAccessor` with the async `await` guard.
- ROADMAP success criteria #3 (offset-ordering ValueError + jobs row / run_job) and #4 (3-method async parity) are met.
</success_criteria>

<output>
Create `.planning/phases/31-continuous-aggregate-lifecycle/31-03-SUMMARY.md` when done.
</output>
