---
phase: 33-release-v0-8-0
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - docs/timescaledb.md
  - docs/api-reference.md
  - README.md
autonomous: true
requirements: [REL-08]

must_haves:
  truths:
    - "D-05: docs/timescaledb.md 'Time Bucketing', 'Gap Filling', and 'Continuous Aggregates' sections are rewritten to use first-class db.timescale.* methods instead of raw db.execute() SQL, and a new '## Advanced Chunk & Dimension Management' section documents show_chunks/drop_chunks/add_dimension/add_reorder_policy"
    - "D-06: all three docs surfaces reflect the 9 new methods — timescaledb.md narrative, the hand-written api-reference.md '### TimescaleDB Methods' table (9 new rows), and the Sphinx autodoc (api-autodoc.md automodule pycopg.timescale builds clean under -W in Plan 03)"
    - "D-07: README accessor-table row for db.timescale.* reads '(15 methods)' (not '(6 methods)') with an updated example-name list, plus a compact highlights block (time_bucket into=df, show_chunks/drop_chunks, create_continuous_aggregate) and a pointer to the RTD advanced guide"
    - "D-14: the advanced docs section notes time_bucket_gapfill and the continuous-aggregate methods require a Community/TSL-licensed TimescaleDB build (Apache builds raise FeatureNotSupported)"
    - "D-10: scope-fence grep over the edited docs returns ZERO matches for the banned deferred-capability keywords"
  artifacts:
    - path: "docs/timescaledb.md"
      provides: "Rewritten time-series sections + new Advanced Chunk & Dimension Management section (REL-08 advanced section)"
      contains: "## Advanced Chunk & Dimension Management"
    - path: "docs/api-reference.md"
      provides: "Hand-written TimescaleDB Methods table extended with 9 new rows"
      contains: "create_continuous_aggregate"
    - path: "README.md"
      provides: "Updated accessor count (15 methods) + compact TimescaleDB highlights"
      contains: "(15 methods)"
  key_links:
    - from: "docs/timescaledb.md examples"
      to: "the 9 db.timescale.* methods"
      via: "first-class method calls replacing raw SQL (D-05)"
      pattern: "db\\.timescale\\.(time_bucket|create_continuous_aggregate|show_chunks)"
    - from: "docs/api-reference.md TimescaleDB Methods table"
      to: "pycopg/timescale.py signatures"
      via: "9 new rows transcribed from the source signatures (D-06)"
      pattern: "time_bucket_gapfill"
    - from: "README.md TimescaleDB section"
      to: "docs/timescaledb.md advanced guide"
      via: "RTD pointer in the compact block (D-07)"
      pattern: "timescaledb"
---

<objective>
Update all three documentation surfaces so they cover the 9 new TimescaleDB methods:
rewrite docs/timescaledb.md's raw-SQL sections to the first-class API + add the
Advanced Chunk & Dimension Management section, extend the hand-written
api-reference.md table with 9 rows, and update README's accessor count + compact
highlights.

Purpose: This is the real work of REL-08 — making the already-shipped methods
discoverable and documented. Depends on Plan 01 (version 0.8.0 + CHANGELOG must exist
so docs are internally consistent).
Output: docs/timescaledb.md (rewritten + new section), docs/api-reference.md (9 rows),
README.md (15 methods + compact highlights + RTD pointer).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-release-v0-8-0/33-CONTEXT.md
@.planning/REQUIREMENTS.md
@pycopg/timescale.py
</context>

<artifacts_this_phase_produces>
Release-only phase — NO new code symbols. Expected artifacts for the source-grounding
verification pass (documentation/record artifacts, NOT code drift):

- The rewritten "Time Bucketing" / "Gap Filling" / "Continuous Aggregates" example
  blocks in docs/timescaledb.md (now using db.timescale.* calls).
- The new `## Advanced Chunk & Dimension Management` section in docs/timescaledb.md.
- The 9 new rows in the `### TimescaleDB Methods` table in docs/api-reference.md.
- The README `(15 methods)` count and the compact TimescaleDB highlights block.

The 9 methods documented here ALREADY EXIST in pycopg/timescale.py (show_chunks:581,
drop_chunks:640, add_dimension:736, add_reorder_policy:865, create_continuous_aggregate:912,
refresh_continuous_aggregate:992, add_continuous_aggregate_policy:1062, time_bucket:1172,
time_bucket_gapfill:1235). This plan documents them; it does NOT create them.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite timescaledb.md sections + add Advanced section (D-05, D-14, D-10)</name>
  <files>docs/timescaledb.md</files>
  <read_first>
    - docs/timescaledb.md (full file, 405 lines — "Time Bucketing" line 162, "Gap Filling" line 215, "Continuous Aggregates" line 231, all currently raw db.execute(""" ... """))
    - pycopg/timescale.py signatures: time_bucket:1172 (table, time_column, bucket_width, aggregates, where=None, schema="public", into="df"); time_bucket_gapfill:1235 (table, time_column, bucket_width, start, finish, aggregates, where=None, schema="public", into="df"); create_continuous_aggregate:912 (view_name, select_sql, schema="public", materialized_only=True, with_no_data=False); refresh_continuous_aggregate:992 (view_name, window_start=None, window_end=None, schema="public"); add_continuous_aggregate_policy:1062 (view_name, start_offset, end_offset, schedule_interval="1 hour", schema="public", if_not_exists=True); show_chunks:581 (table, older_than=None, newer_than=None, schema="public"); drop_chunks:640 (table, older_than=None, newer_than=None, schema="public", dry_run=False); add_dimension:736 (table, column, partition_type="hash", number_partitions=None, chunk_interval=None, schema="public", if_not_exists=True); add_reorder_policy:865 (table, index_name, schema="public", if_not_exists=True)
    - .planning/phases/33-release-v0-8-0/33-CONTEXT.md (D-05 rewrite-in-place + new section name; D-14 license note; D-10 banned keywords)
  </read_first>
  <action>
    Rewrite the example code blocks in three existing sections to call first-class methods:
    "Time Bucketing" (line 162 block) becomes a db.timescale.time_bucket("events", "time",
    "1 hour", aggregates="device_id, AVG(temperature) AS avg_temp, MAX(temperature) AS max_temp",
    where="time > NOW() - INTERVAL '1 day'", into="df") example; note into="rows" returns
    list[dict] and that aggregates/where are structural SQL (aggregates not from untrusted
    input; where value parameterized). "Gap Filling" (line 215 block) becomes a
    db.timescale.time_bucket_gapfill(...) example passing required start/finish as datetime
    args (NOT WHERE inference) with aggregates containing a locf(...) fragment. "Continuous
    Aggregates" (line 231 block) becomes db.timescale.create_continuous_aggregate("hourly_metrics",
    select_sql="SELECT time_bucket('1 hour', time) AS bucket, device_id, AVG(temperature) AS
    avg_temp FROM events GROUP BY bucket, device_id"), then
    db.timescale.refresh_continuous_aggregate("hourly_metrics"), then
    db.timescale.add_continuous_aggregate_policy("hourly_metrics", start_offset="3 hours",
    end_offset="1 hour", schedule_interval="1 hour"). These three rewritten blocks MUST NOT
    contain db.execute( . (The "Last Values", "Moving Average", "Complete Example", and
    "Best Practices" blocks may keep raw db.execute where there is no first-class equivalent —
    only the three D-05-named sections are rewritten.) THEN add a new top-level section
    "## Advanced Chunk & Dimension Management" after "Continuous Aggregates", before
    "Complete Example", with worked examples: db.timescale.show_chunks("events",
    older_than="30 days") (returns list[str] oldest-first); db.timescale.drop_chunks("events",
    older_than="90 days", dry_run=True) (note dry_run previews via show_chunks, and BOTH bounds
    None raises ValueError — DESTRUCTIVE/IRREVERSIBLE); db.timescale.add_dimension("events",
    "device_id", partition_type="hash", number_partitions=4) (2.x by_hash/by_range form,
    hash↔number_partitions / range↔chunk_interval mutual-exclusivity);
    db.timescale.add_reorder_policy("events", index_name="events_device_id_time_idx"). Inside
    this section add a license note (D-14) as a "> **Note:**" admonition: time_bucket_gapfill
    (and locf/interpolate) and the continuous-aggregate methods require a Community/TSL-licensed
    TimescaleDB build; Apache builds raise FeatureNotSupported. Do NOT introduce any D-10 banned
    keyword (no compress_chunk/decompress_chunk, drop_continuous_aggregate,
    remove_continuous_aggregate_policy, created_before/created_after, time_bucket origin/offset
    alignment, initial_watermark, CDC/WAL).
  </action>
  <acceptance_criteria>
    - docs/timescaledb.md contains the literal heading `## Advanced Chunk & Dimension Management`
    - The rewritten "Time Bucketing"/"Gap Filling"/"Continuous Aggregates" blocks each contain `db.timescale.` and do NOT contain `db.execute(`
    - All 9 method names appear in docs/timescaledb.md (grep each ≥1)
    - The Advanced section contains a license note: grep `Community` and `FeatureNotSupported` each ≥1 (D-14)
    - The drop_chunks text mentions `dry_run` and `ValueError`
    - Scope-fence grep over docs/timescaledb.md returns ZERO matches for the D-10 banned list (the substring "reorder" in add_reorder_policy is legitimate, not a match)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && grep -q '## Advanced Chunk & Dimension Management' docs/timescaledb.md && for m in show_chunks drop_chunks add_dimension add_reorder_policy create_continuous_aggregate refresh_continuous_aggregate add_continuous_aggregate_policy time_bucket time_bucket_gapfill; do grep -q "$m" docs/timescaledb.md || { echo "MISSING $m"; exit 1; }; done && grep -q 'Community' docs/timescaledb.md && grep -q 'FeatureNotSupported' docs/timescaledb.md && ! grep -Eiq 'initial_watermark|\bCDC\b|\bWAL\b|drop_continuous_aggregate|remove_continuous_aggregate_policy|compress_chunk|decompress_chunk|created_before|created_after' docs/timescaledb.md && echo OK</automated>
  </verify>
  <done>docs/timescaledb.md's three time-series sections use first-class db.timescale.* calls (no raw db.execute in those blocks), the Advanced Chunk & Dimension Management section documents all 4 chunk/dimension methods with a Community/TSL license note, all 9 methods named, no D-10 banned keyword.</done>
</task>

<task type="auto">
  <name>Task 2: Extend api-reference.md TimescaleDB Methods table with 9 rows (D-06)</name>
  <files>docs/api-reference.md</files>
  <read_first>
    - docs/api-reference.md (lines 184-193: hand-written `### TimescaleDB Methods` table listing the 6 v0.6 methods, ending with hypertable_info)
    - pycopg/timescale.py (the 9 signatures in Task 1 read_first — source of truth for Parameters/Returns columns)
    - .planning/phases/33-release-v0-8-0/33-CONTEXT.md (D-06 — add 9 rows: Method | Parameters | Returns)
  </read_first>
  <action>
    Append 9 new rows to the existing `### TimescaleDB Methods` table (currently ending at
    line 193 with hypertable_info). Keep the existing 6 rows. Add one row per new method using
    the exact source signatures (Method | Parameters | Returns): show_chunks |
    `table, older_than=None, newer_than=None, schema="public"` | `list[str]`; drop_chunks |
    `table, older_than=None, newer_than=None, schema="public", dry_run=False` | `list[str]`;
    add_dimension | `table, column, partition_type="hash", number_partitions=None,
    chunk_interval=None, schema="public", if_not_exists=True` | `-`; add_reorder_policy |
    `table, index_name, schema="public", if_not_exists=True` | `-`; create_continuous_aggregate |
    `view_name, select_sql, schema="public", materialized_only=True, with_no_data=False` | `-`;
    refresh_continuous_aggregate | `view_name, window_start=None, window_end=None,
    schema="public"` | `-`; add_continuous_aggregate_policy | `view_name, start_offset,
    end_offset, schedule_interval="1 hour", schema="public", if_not_exists=True` | `-`;
    time_bucket | `table, time_column, bucket_width, aggregates, where=None, schema="public",
    into="df"` | `DataFrame \| list[dict]`; time_bucket_gapfill | `table, time_column,
    bucket_width, start, finish, aggregates, where=None, schema="public", into="df"` |
    `DataFrame \| list[dict]`. Escape the inner pipe in the Returns cells as `\|`. Wrap method
    names and params in backticks to match the table style. Do NOT alter the Spatial Helpers
    table (line 161) or any other section.
  </action>
  <acceptance_criteria>
    - The `### TimescaleDB Methods` table region contains all 9 new method names (grep each ≥1)
    - The 6 original rows still present: create_hypertable, enable_compression, add_compression_policy, add_retention_policy, list_hypertables, hypertable_info (grep each ≥1)
    - Both time_bucket and time_bucket_gapfill rows present (distinct names)
    - The Spatial Helpers section's 13 rows are unchanged (git diff shows no change to the spatial table region)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && awk '/^### TimescaleDB Methods/{f=1} /^### Role Methods/{f=0} f' docs/api-reference.md > /tmp/ts_tbl.txt && for m in show_chunks drop_chunks add_dimension add_reorder_policy create_continuous_aggregate refresh_continuous_aggregate add_continuous_aggregate_policy time_bucket time_bucket_gapfill create_hypertable hypertable_info; do grep -q "$m" /tmp/ts_tbl.txt || { echo "MISSING $m"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>docs/api-reference.md's TimescaleDB Methods table has all 6 original + 9 new rows with source-accurate Parameters/Returns; the Spatial Helpers table is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Update README accessor count + compact highlights (D-07, D-10)</name>
  <files>README.md</files>
  <read_first>
    - README.md (line 86: accessor row `| db.timescale.* | TimescaleDB | create_hypertable, enable_compression, add_retention_policy, … (6 methods) |`; lines 551-569: `## TimescaleDB Support` compact example block)
    - .planning/phases/33-release-v0-8-0/33-CONTEXT.md (D-07 — bump count to 15, update example-name list, add compact highlights + RTD pointer, match compact style, do NOT add full worked examples for all 9; D-10 banned keywords)
  </read_first>
  <action>
    In README.md line 86, change the db.timescale.* row's `… (6 methods)` to `… (15 methods)`
    and update the example-name list to include 1-2 new high-value methods (e.g. add
    time_bucket, show_chunks) so it reads like `create_hypertable, enable_compression,
    time_bucket, show_chunks, … (15 methods)`. Then in the `## TimescaleDB Support` section
    (line 551) add a COMPACT highlights block AFTER the existing example (line 569 closing
    fence) per D-07 showing: db.timescale.time_bucket("events", "time", "1 hour",
    aggregates="device_id, AVG(temperature) AS avg_temp", into="df") (returns a DataFrame);
    db.timescale.show_chunks("events", older_than="30 days") / db.timescale.drop_chunks("events",
    older_than="90 days", dry_run=True) (operational pair); db.timescale.create_continuous_aggregate("hourly",
    select_sql="...") (lifecycle). Keep it compact — no full worked examples (those live in
    docs/timescaledb.md). Add a one-line pointer to the advanced guide (link to docs/timescaledb.md
    or the RTD timescaledb page). Do NOT introduce any D-10 banned keyword.
  </action>
  <acceptance_criteria>
    - `grep -c '(15 methods)' README.md` returns ≥1 AND the db.timescale.* row no longer reads `(6 methods)` (grep `db\.timescale\.\*.*\(6 methods\)` returns 0)
    - The README TimescaleDB section contains compact highlights for ≥3 of: time_bucket, show_chunks, drop_chunks, create_continuous_aggregate
    - README contains a pointer to the timescaledb advanced docs (grep `timescaledb` in the section ≥1)
    - Scope-fence grep over README.md returns ZERO matches for the D-10 banned keyword list
  </acceptance_criteria>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && grep -q '(15 methods)' README.md && ! grep -E 'db\.timescale\.\*.*\(6 methods\)' README.md && c=0; for m in time_bucket show_chunks drop_chunks create_continuous_aggregate; do grep -q "db.timescale.$m" README.md && c=$((c+1)); done; test "$c" -ge 3 && ! grep -Eiq 'initial_watermark|\bCDC\b|\bWAL\b|drop_continuous_aggregate|remove_continuous_aggregate_policy|compress_chunk|decompress_chunk|created_before|created_after' README.md && echo OK</automated>
  </verify>
  <done>README's db.timescale.* accessor row reads "(15 methods)" with an updated example-name list, the TimescaleDB section has a compact highlights block (≥3 of time_bucket/show_chunks/drop_chunks/create_continuous_aggregate) plus an RTD pointer, no D-10 banned keyword.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → published docs | Documentation becomes the public claim of what v0.8.0 can do; over-claiming a deferred capability is a trust/correctness threat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Tampering (correctness) | docs/timescaledb.md, README.md | mitigate | D-10 scope-fence grep (Tasks 1 & 3) asserts ZERO banned deferred-capability keywords in the edited docs. |
| T-33-04 | Safety guidance | drop_chunks documentation | mitigate | Advanced section documents drop_chunks as DESTRUCTIVE/IRREVERSIBLE and surfaces the both-bounds-None ValueError + dry_run preview, warning users before data-destroying operations. |
| T-33-05 | Information disclosure | example code in docs | accept | Examples use placeholder table/column names only; no secrets or real connection strings introduced. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — docs-only edits, no dependency surface touched. |

No new code paths, no user input handling, no new network surface. No high-severity threats.
</threat_model>

<verification>
- docs/timescaledb.md: three sections rewritten to db.timescale.* (no raw db.execute in
  those blocks), new Advanced section with all 4 methods + Community/TSL license note,
  all 9 methods named.
- docs/api-reference.md: 9 new rows added, 6 originals kept, Spatial table untouched.
- README.md: "(15 methods)", compact highlights, RTD pointer.
- D-10 scope-fence grep clean across all three files.
</verification>

<success_criteria>
- All three docs surfaces reflect the 9 new methods (D-05, D-06, D-07).
- Advanced Chunk & Dimension Management section exists with the license note (D-05, D-14).
- No D-10 banned keyword in any edited doc.
- Sphinx -W gate (Plan 03) will build these sections clean.
</success_criteria>

<output>
Create `.planning/phases/33-release-v0-8-0/33-02-SUMMARY.md` when done.
</output>
