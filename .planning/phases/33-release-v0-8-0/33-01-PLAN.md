---
phase: 33-release-v0-8-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - docs/conf.py
  - CHANGELOG.md
autonomous: true
requirements: [REL-08]

must_haves:
  truths:
    - "D-01: version is 0.8.0 in BOTH sources — pyproject.toml line 7 and docs/conf.py line 17 (the only two version sources)"
    - "D-08: CHANGELOG has a dated [0.8.0] section with `### Added` ONLY and no `### Breaking` (purely additive release)"
    - "D-09: the [0.8.0] Added entry is grouped by the three feature families — chunk & dimension management (4), continuous aggregate lifecycle (3), query helpers (2)"
    - "D-10: scope-fence grep over the CHANGELOG [0.8.0] section returns ZERO matches for every banned deferred-capability keyword"
    - "D-11: MIGRATION.md is NOT edited this phase (v0.8.0 has zero breaking changes)"
    - "D-04: `uv lock --check` passes after the version bump so the lockfile is current before tagging"
  artifacts:
    - path: "pyproject.toml"
      provides: "Canonical package version 0.8.0"
      contains: 'version = "0.8.0"'
    - path: "docs/conf.py"
      provides: "Sphinx docs release string 0.8.0"
      contains: "release = '0.8.0'"
    - path: "CHANGELOG.md"
      provides: "Keep-a-Changelog [0.8.0] Added entry covering the 9 new methods"
      contains: "## [0.8.0]"
  key_links:
    - from: "pyproject.toml"
      to: "docs/conf.py"
      via: "synchronized version string 0.8.0 (D-01)"
      pattern: "0\\.8\\.0"
    - from: "CHANGELOG.md [0.8.0]"
      to: "the 9 new db.timescale.* methods"
      via: "### Added entries naming each method (D-09)"
      pattern: "show_chunks|create_continuous_aggregate|time_bucket"
---

<objective>
Bump the package version to 0.8.0 in both canonical sources and write the
`CHANGELOG [0.8.0]` Added entry covering the 9 new TimescaleDB methods.

Purpose: Establishes the release version and the authoritative additive changelog
that the docs (Plan 02) and the publish (Plan 03) build on. This is the foundation
wave — version + changelog must land before docs reference them and before tagging.
Output: pyproject.toml (version 0.8.0), docs/conf.py (release 0.8.0), CHANGELOG.md
([0.8.0] dated, Added-only, family-grouped, scope-fence-clean), verified lockfile.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-release-v0-8-0/33-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
This is a release-only phase — it produces NO new code symbols. The source-grounding
verification pass MUST treat the following as the expected artifacts (do not flag them
as drift against missing code symbols):

- The version string change to `0.8.0` in `pyproject.toml` and `docs/conf.py`.
- The new `## [0.8.0]` section in `CHANGELOG.md` (record artifact, not a code symbol).

The 9 methods named in the CHANGELOG already exist and were verified in Phases 30–32
(`pycopg/timescale.py` lines 581/640/736/865/912/992/1062/1172/1235). They are NOT
created here — they are being documented.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.8.0 in both canonical sources (D-01)</name>
  <files>pyproject.toml, docs/conf.py</files>
  <read_first>
    - pyproject.toml (line 7 currently `version = "0.7.0"`)
    - docs/conf.py (line 17 currently `release = '0.7.0'`)
    - .planning/phases/33-release-v0-8-0/33-CONTEXT.md (D-01 — these are the ONLY two version sources)
  </read_first>
  <action>
    Change pyproject.toml line 7 from `version = "0.7.0"` to `version = "0.8.0"` (double
    quotes, exact key `version = `). Change docs/conf.py line 17 from `release = '0.7.0'`
    to `release = '0.8.0'` (single quotes, exact key `release = `). Do NOT touch any other
    version-shaped string (pyproject.toml line 84 `target-version = "py311"` and line 121
    `python_version = "3.11"` are NOT version sources — leave them). Do NOT edit CLAUDE.md's
    stale "Version" line — it is explicitly out of scope per CONTEXT.md Deferred and is not
    a release-gated source.
  </action>
  <acceptance_criteria>
    - `grep -c 'version = "0.8.0"' pyproject.toml` returns 1
    - `grep -c 'version = "0.7.0"' pyproject.toml` returns 0
    - `grep -c "release = '0.8.0'" docs/conf.py` returns 1
    - `grep -c "release = '0.7.0'" docs/conf.py` returns 0
    - `grep -c 'target-version = "py311"' pyproject.toml` still returns 1 (unchanged)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'version = "0.8.0"' pyproject.toml)" = "1" && test "$(grep -c "release = '0.8.0'" docs/conf.py)" = "1" && echo OK</automated>
  </verify>
  <done>Both pyproject.toml and docs/conf.py declare version/release 0.8.0; no 0.7.0 version string remains in either file; no unrelated version-shaped strings were altered.</done>
</task>

<task type="auto">
  <name>Task 2: Write CHANGELOG [0.8.0] Added-only, family-grouped entry (D-08, D-09)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-45: the `## [Unreleased]` placeholder at line 8, the `## [0.7.0] - 2026-06-22` entry as Keep-a-Changelog format precedent including its `### Breaking` and `### Added` shape)
    - .planning/phases/33-release-v0-8-0/33-CONTEXT.md (D-08 Added-only no-Breaking, D-09 three-family grouping, D-11 no migration entry)
    - .planning/REQUIREMENTS.md (the 9 method signatures: TS-ADV-04/05/08/09 chunk+dimension, TS-ADV-01/02/03 cagg lifecycle, TS-ADV-06/07 query helpers)
  </read_first>
  <action>
    Replace the empty `## [Unreleased]` placeholder region (line 8) by inserting a new
    `## [0.8.0] - 2026-06-23` section immediately below a now-empty `## [Unreleased]`
    heading (keep the `## [Unreleased]` heading present with no entries, matching how
    Keep-a-Changelog keeps an Unreleased section after a release; the [0.8.0] section goes
    between `## [Unreleased]` and `## [0.7.0]`). The [0.8.0] section MUST contain ONLY a
    `### Added` subsection — NO `### Breaking` subsection (D-08: v0.8.0 is purely additive;
    the breaking alias removal was v0.7.0). Under `### Added`, group the entries into exactly
    the three feature families per D-09, each introduced by a short bolded label line:
      - Chunk & dimension management (4 methods): name `db.timescale.show_chunks`,
        `db.timescale.drop_chunks` (note its `dry_run` preview + the ValueError both-bounds-None
        safety), `db.timescale.add_dimension` (by_hash/by_range 2.x form), `db.timescale.add_reorder_policy`.
      - Continuous aggregate lifecycle (3 methods): name `db.timescale.create_continuous_aggregate`,
        `db.timescale.refresh_continuous_aggregate`, `db.timescale.add_continuous_aggregate_policy`.
      - Query helpers (2 methods): name `db.timescale.time_bucket`,
        `db.timescale.time_bucket_gapfill` (start/finish required).
    State that all 9 methods have full sync/async parity on `AsyncTimescaleAccessor` and that
    there are zero new runtime dependencies. Add a closing pointer line to
    `docs/timescaledb.md` for the advanced guide. Do NOT add a `### Breaking` heading, a
    `### Removed`, `### Changed`, or `### Deprecated` heading. Do NOT add or edit any
    MIGRATION.md reference (D-11 — no migration entry; v0.8.0 has no breaking changes).
    The 2026-06-23 date is the planned tag date — the publish plan (Plan 03) confirms/adjusts
    it on actual tag day.
  </action>
  <acceptance_criteria>
    - `grep -c '## \[0.8.0\]' CHANGELOG.md` returns 1
    - The `[0.8.0]` section contains `### Added` (grep `### Added` appears in the [0.8.0] block)
    - The `[0.8.0]` section does NOT contain `### Breaking`: extracting lines from `## [0.8.0]` up to the next `## [` heading and grepping for `### Breaking` returns 0 matches
    - All 9 method names appear in the [0.8.0] section: `show_chunks`, `drop_chunks`, `add_dimension`, `add_reorder_policy`, `create_continuous_aggregate`, `refresh_continuous_aggregate`, `add_continuous_aggregate_policy`, `time_bucket`, `time_bucket_gapfill` (grep each, all ≥1)
    - The three family labels are present (grep `Chunk & dimension`, `Continuous aggregate`, `Query helper` each ≥1 within the [0.8.0] block)
    - `git diff MIGRATION.md` is empty (D-11 — MIGRATION.md unchanged)
  </acceptance_criteria>
  <verify>
    <automated>awk '/^## \[0.8.0\]/{f=1} /^## \[0.7.0\]/{f=0} f' CHANGELOG.md > /tmp/cl_080.txt && grep -q '### Added' /tmp/cl_080.txt && ! grep -q '### Breaking' /tmp/cl_080.txt && for m in show_chunks drop_chunks add_dimension add_reorder_policy create_continuous_aggregate refresh_continuous_aggregate add_continuous_aggregate_policy time_bucket time_bucket_gapfill; do grep -q "$m" /tmp/cl_080.txt || { echo "MISSING $m"; exit 1; }; done && test -z "$(git -C /home/loc/workspace/pycopg diff --name-only MIGRATION.md)" && echo OK</automated>
  </verify>
  <done>CHANGELOG.md has a dated `## [0.8.0]` section with a single `### Added` subsection grouped into the three families, naming all 9 new methods, no `### Breaking`; MIGRATION.md untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Scope-fence grep over [0.8.0] + verify lockfile current (D-10, D-04)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the [0.8.0] section authored in Task 2)
    - .planning/phases/33-release-v0-8-0/33-CONTEXT.md (D-10 banned keyword list; D-04 lockfile must be current before tagging)
    - .planning/REQUIREMENTS.md (Out of Scope + Future Requirements tables — TSDB-F01..F04, ETL-INC-F01..F05 define the banned keywords)
  </read_first>
  <action>
    Run the D-10 scope-fence grep over the extracted `[0.8.0]` CHANGELOG section. Banned
    keywords (case-insensitive, the [0.8.0] block must contain ZERO of these): `initial_watermark`,
    `CDC`, `WAL`, `drop_continuous_aggregate`, `remove_continuous_aggregate_policy`,
    `compress_chunk`, `decompress_chunk`, `origin`, `offset` (alignment-param sense),
    `created_before`, `created_after`. If any match is found, the entry over-claims a deferred
    capability and must be reworded to remove it before proceeding. Then run `uv lock --check`
    to confirm the lockfile is still current after the version bump (D-04); if it reports the
    lock is out of date, run `uv lock` to regenerate it and include uv.lock in the changes.
    Note for the executor: `add_reorder_policy` legitimately contains the substring "reorder"
    (not "order/origin/offset") — do not flag it; the banned `offset`/`origin` refer to
    time_bucket alignment params which are NOT documented this phase.
  </action>
  <acceptance_criteria>
    - Scope-fence grep over the extracted [0.8.0] block returns ZERO matches for the full banned list (initial_watermark, CDC, WAL, drop_continuous_aggregate, remove_continuous_aggregate_policy, compress_chunk, decompress_chunk, origin, created_before, created_after)
    - `uv lock --check` exits 0 (lockfile current)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && awk '/^## \[0.8.0\]/{f=1} /^## \[0.7.0\]/{f=0} f' CHANGELOG.md > /tmp/cl_080.txt && ! grep -Eiq 'initial_watermark|\bCDC\b|\bWAL\b|drop_continuous_aggregate|remove_continuous_aggregate_policy|compress_chunk|decompress_chunk|\borigin\b|created_before|created_after' /tmp/cl_080.txt && uv lock --check && echo OK</automated>
  </verify>
  <done>The [0.8.0] CHANGELOG section is free of all D-10 banned deferred-capability keywords and `uv lock --check` passes — the changelog is scope-accurate and the lockfile is current for tagging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → published changelog/version | The version string and CHANGELOG become the public-facing claims of what v0.8.0 ships; an over-claim is a trust/correctness threat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering (correctness) | CHANGELOG [0.8.0] entry | mitigate | D-10 scope-fence grep (Task 3) asserts ZERO banned deferred-capability keywords, preventing the changelog from advertising features that do not exist. |
| T-33-02 | Information disclosure | version sources | accept | Version strings are public by design; bumping to 0.8.0 discloses only the intended release number. No secret material involved. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; `uv lock --check` (Task 3) ensures the dependency lockfile is unchanged/current — no new or drifted dependency is introduced by the version bump. |

No new code paths, no user input handling, no new network surface introduced. No high-severity threats.
</threat_model>

<verification>
- Both version sources read 0.8.0; no 0.7.0 version string remains.
- CHANGELOG [0.8.0] is Added-only, family-grouped, names all 9 methods, no Breaking.
- D-10 scope-fence grep clean; MIGRATION.md untouched; `uv lock --check` passes.
</verification>

<success_criteria>
- pyproject.toml and docs/conf.py both declare 0.8.0 (D-01).
- CHANGELOG.md has a dated [0.8.0] `### Added`-only section grouped into the three
  feature families naming all 9 methods (D-08, D-09).
- No banned deferred-capability keyword in the [0.8.0] section (D-10).
- MIGRATION.md not edited (D-11); `uv lock --check` passes (D-04).
</success_criteria>

<output>
Create `.planning/phases/33-release-v0-8-0/33-01-SUMMARY.md` when done.
</output>
