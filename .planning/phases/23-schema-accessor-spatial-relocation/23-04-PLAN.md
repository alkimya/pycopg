---
phase: 23-schema-accessor-spatial-relocation
plan: 04
type: execute
wave: 4
depends_on: ["23-02", "23-03"]
files_modified:
  - tests/test_schema_aliases.py
  - tests/test_parity.py
  - tests/test_database.py
  - tests/test_async_database.py
  - tests/test_database_integration.py
  - tests/test_sql_injection.py
autonomous: true
requirements: [SCH-01]

must_haves:
  truths:
    - "tests/test_schema_aliases.py covers all 27 schema flat aliases sync AND async (>=54 DB-free tests): each emits exactly one DeprecationWarning naming db.schema.<m> (v0.7.0), at caller stacklevel, and delegates with identical args"
    - "(SchemaAccessor, AsyncSchemaAccessor) is present in ACCESSOR_PAIRS and test_accessor_parity passes for it"
    - "Every existing behavioral test that called a flat schema name now calls db.schema.<m> — the full suite passes under -W error::DeprecationWarning (modulo the 2 known-flaky DB tests)"
    - "Full uv run pytest is green with coverage >= 94% (the 27 thin schema stubs are exercised by the alias tests so the --cov-fail-under=94 ratchet holds)"
    - "Validated against 27 schema / 29 phase-total flat names (D-01) — not 26/28"
  artifacts:
    - path: "tests/test_schema_aliases.py"
      provides: "27 sync + 27 async DB-free MagicMock alias tests (warn + caller stacklevel + delegate on db._schema)"
      contains: "class TestSchemaAliases"
    - path: "tests/test_parity.py"
      provides: "(SchemaAccessor, AsyncSchemaAccessor) appended to ACCESSOR_PAIRS + import"
      contains: "SchemaAccessor"
  key_links:
    - from: "tests/test_schema_aliases.py parametrized cases"
      to: "db._schema MagicMock(spec=SchemaAccessor) / AsyncMock"
      via: "DB-free injection into the cache field, assert warn + delegate"
      pattern: "db\\._schema = "
    - from: "existing behavioral test call-sites (test_database.py etc.)"
      to: "db.schema.<m>(...)"
      via: "flat-to-accessor migration so -W error::DeprecationWarning stays green"
      pattern: "\\.schema\\.(list_tables|table_exists|create_index|has_extension)"
    - from: "27 thin schema stubs (plan 02)"
      to: "coverage >= 94%"
      via: "exercised by the 54 alias tests"
      pattern: "@deprecated_alias\\(\"schema\\."
---

<objective>
Lock the SCHEMA track's test surface (SCH-01): create the DB-free
`tests/test_schema_aliases.py` (27 sync + 27 async alias tests), append
`(SchemaAccessor, AsyncSchemaAccessor)` to `ACCESSOR_PAIRS`, migrate the flat-schema-name
call-sites across the existing test suite to `db.schema.*`, and hold ALL gates green —
full suite, coverage >= 94% (`--cov-fail-under=94`), and `-W error::DeprecationWarning`.

Purpose: This is Wave 3 of the schema track (D-03) — the proof layer. The 27 thin
`@deprecated_alias("schema.<m>")` stubs from plan 02 only count toward the coverage ratchet
once exercised; the 54 DB-free alias tests do exactly that AND prove each flat alias warns
toward `db.schema.*` and delegates. Migrating the existing behavioral tests to `db.schema.*`
keeps them from tripping the `-W error::DeprecationWarning` gate on their own internal calls.
After this plan, the entire schema track is verified end-to-end.

Output: new `tests/test_schema_aliases.py`; edits to `test_parity.py`, `test_database.py`,
`test_async_database.py`, `test_database_integration.py`, `test_sql_injection.py` (plus any
other test file grep finds calling the 27 flat schema names).

NOTE — sequencing: this plan and plan 03 BOTH edit `tests/test_parity.py` (each appends one
tuple to `ACCESSOR_PAIRS`). `depends_on` includes `23-03` so the two appends serialize: plan
03 lands `(SpatialAccessor, AsyncSpatialAccessor)` first, then this plan appends
`(SchemaAccessor, AsyncSchemaAccessor)` — no conflict. (It also genuinely needs plan 02's
schema stubs + accessor wiring to exist.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-CONTEXT.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-VALIDATION.md
@pycopg/schema.py
@tests/test_timescale_aliases.py
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
See 23-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase symbol list.
This plan (schema-track tests/parity/migration) produces:
- `tests/test_schema_aliases.py` — 27 sync + 27 async DB-free MagicMock alias tests.
- `(SchemaAccessor, AsyncSchemaAccessor)` appended to `ACCESSOR_PAIRS` in `tests/test_parity.py`
  (after plan 03's spatial tuple) + the import.
- Migrated flat-schema call-sites across the existing test suite to `db.schema.*`
  (test_database.py, test_async_database.py, test_database_integration.py, test_sql_injection.py,
  and any other file grep surfaces).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_schema_aliases.py (27 sync + 27 async DB-free MagicMock alias tests)</name>
  <files>tests/test_schema_aliases.py</files>
  <read_first>
    - tests/test_timescale_aliases.py (full file — THE template: `_SYNC_ALIAS_ARGS` dict mapping method to call args, the parametrized sync test injecting `db._<accessor> = MagicMock(spec=...)`, `warnings.catch_warnings(record=True)` + `warnings.simplefilter("always")`, the message filter `rec.category is DeprecationWarning and f"db.schema.{name}" in str(rec.message)`, the `"v0.7.0"` assertion, the `"test_" in os.path.basename(rec.filename)` stacklevel=2 assertion, `getattr(mock, name).assert_called_once_with(*args)`; the async test wraps each mock method with `AsyncMock()`)
    - tests/test_admin_aliases.py (the Phase 22 sibling — confirms conventions are uniform across the family)
    - pycopg/schema.py (the authoritative 27 public method names + their signatures, to build `_SYNC_ALIAS_ARGS` with valid positional args per method)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md "tests/test_schema_aliases.py (new DB-free alias test)" section (the full file structure + the complete `_SYNC_ALIAS_ARGS` dict for all 27 methods)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md "Alias test template" + "Async alias test" code examples (verified patterns) + the 27-method enumeration in 23-CONTEXT.md `<domain>`
  </read_first>
  <action>
    Create `tests/test_schema_aliases.py` mirroring `tests/test_timescale_aliases.py` exactly,
    parametrized over all 27 schema method names. Build `_SYNC_ALIAS_ARGS: dict[str, tuple]` with a
    valid positional-arg tuple per method (use the PATTERNS.md dict, e.g. `"create_database":
    ("mydb",)`, `"list_tables": ()`, `"add_foreign_key": ("orders", "user_id", "users", "id")`,
    `"add_unique_constraint": ("users", ["email"])`, `"create_index": ("users", ["email"])`, etc. —
    all 27 keys). Set `_ASYNC_ALIAS_ARGS = _SYNC_ALIAS_ARGS.copy()`.

    Class `TestSchemaAliases` with two parametrized tests:

    Sync `test_sync_alias_warns_and_delegates(self, name, config)` (param over
    `list(_SYNC_ALIAS_ARGS.keys())`): `db = Database(config)`; inject `db._schema =
    MagicMock(spec=SchemaAccessor)` (into the cache field — bypasses lazy construction, DB-free).
    Under `warnings.catch_warnings(record=True)` + `warnings.simplefilter("always")`, call
    `getattr(db, name)(*_SYNC_ALIAS_ARGS[name])`. Filter to `rec.category is DeprecationWarning and
    f"db.schema.{name}" in str(rec.message)`; assert exactly one; assert `"v0.7.0"` in the message;
    assert `"test_" in os.path.basename(rec.filename)` and the basename is neither `aliases.py` nor
    `database.py` (stacklevel=2 caller proof); assert `getattr(db._schema,
    name).assert_called_once_with(*args)`.

    Async `test_async_alias_warns_and_delegates(self, name, config)` (`@pytest.mark.asyncio`, param
    over `_ASYNC_ALIAS_ARGS`): `db = AsyncDatabase(config)`; `mock = MagicMock(spec=AsyncSchemaAccessor)`;
    set EACH of the 27 method names on the mock to `AsyncMock()`; inject `db._schema = mock`. `await
    getattr(db, name)(*args)`; same warning + stacklevel + delegation assertions.

    Import `Database, AsyncDatabase` from `pycopg` and `SchemaAccessor, AsyncSchemaAccessor` from
    `pycopg.schema`. Use the shared `config` fixture (same one test_timescale_aliases.py uses; from
    conftest). These 27x2 tests are what keep the 27 thin schema stubs covered for the
    `--cov-fail-under=94` ratchet (per D-01 / coverage vigilance).

    Validate against 27 (D-01) — `len(_SYNC_ALIAS_ARGS) == 27`, NOT 26.
  </action>
  <verify>
    <automated>uv run python -c "import ast; t=ast.parse(open('tests/test_schema_aliases.py').read()); assert 'TestSchemaAliases' in [n.name for n in ast.walk(t) if isinstance(n, ast.ClassDef)]" && uv run pytest tests/test_schema_aliases.py -q -o addopts="" && uv run ruff check tests/test_schema_aliases.py</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the class: `grep -c 'class TestSchemaAliases' tests/test_schema_aliases.py` == 1.
    - 27 methods enumerated: the `_SYNC_ALIAS_ARGS` dict has exactly 27 keys (D-01) — confirm `len(_SYNC_ALIAS_ARGS)==27` via a one-liner or by counting keys.
    - Covers all 27 both paths: `uv run pytest tests/test_schema_aliases.py -q -o addopts=""` collects and passes >= 54 tests (27 sync + 27 async).
    - Warning + delegation asserted: the file contains the `f"db.schema.{name}"` message filter, the `"v0.7.0"` assertion, the `os.path.basename` stacklevel assertion, and `assert_called_once_with`.
    - Mock injected into the cache field (DB-free): `grep -c 'db._schema = ' tests/test_schema_aliases.py` >= 2 (sync MagicMock + async mock inject).
    - `uv run ruff check tests/test_schema_aliases.py` exits 0.
  </acceptance_criteria>
  <done>tests/test_schema_aliases.py exists with TestSchemaAliases covering all 27 schema methods sync + async (>=54 DB-free tests); each asserts exactly one DeprecationWarning naming db.schema.<m> + v0.7.0, caller stacklevel, and delegation to db._schema; _SYNC_ALIAS_ARGS has 27 keys (D-01); ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Append (SchemaAccessor, AsyncSchemaAccessor) to ACCESSOR_PAIRS in test_parity.py</name>
  <files>tests/test_parity.py</files>
  <read_first>
    - tests/test_parity.py lines 11-28 (the accessor imports + ACCESSOR_PAIRS; after plan 03 this list contains 6 entries incl. the spatial pair — append the schema pair as the 7th; the comment at line 19 reads "Phases 22-24 APPEND here")
    - .planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md "tests/test_parity.py" section (the import + tuple-append pattern; the schema pair is THIS plan's, the spatial pair is plan 03's)
    - pycopg/schema.py (confirms SchemaAccessor/AsyncSchemaAccessor exist with the 27-method surface that test_accessor_parity will diff)
  </read_first>
  <action>
    Add `from pycopg.schema import AsyncSchemaAccessor, SchemaAccessor` to the accessor-import block
    in `tests/test_parity.py` (grouped with the existing accessor imports). Append exactly one tuple
    `(SchemaAccessor, AsyncSchemaAccessor)` to the `ACCESSOR_PAIRS` list (after the spatial pair plan
    03 added). Do NOT re-add the spatial pair (plan 03 owns it; `depends_on: 23-03` guarantees it is
    already there). `test_accessor_parity` will diff SchemaAccessor vs AsyncSchemaAccessor public
    surfaces — plan 01 guaranteed exact 27==27 parity, so this should pass directly.
  </action>
  <verify>
    <automated>grep -c '(SchemaAccessor, AsyncSchemaAccessor)' tests/test_parity.py && uv run pytest tests/test_parity.py -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - Import present: `grep -c 'from pycopg.schema import' tests/test_parity.py` == 1.
    - Tuple appended: `grep -c '(SchemaAccessor, AsyncSchemaAccessor)' tests/test_parity.py` == 1.
    - Both pairs present (spatial from plan 03 + schema from this plan): `grep -c '(SpatialAccessor, AsyncSpatialAccessor)' tests/test_parity.py` == 1 still holds (not clobbered).
    - Parity green: `uv run pytest tests/test_parity.py -q -o addopts=""` exits 0 (test_accessor_parity passes for `(SchemaAccessor, AsyncSchemaAccessor)` — 27==27 surface).
  </acceptance_criteria>
  <done>(SchemaAccessor, AsyncSchemaAccessor) is imported and appended to ACCESSOR_PAIRS (alongside the spatial pair from plan 03); test_accessor_parity passes for the schema pair at 27==27 parity.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate flat-schema call-sites across the test suite to db.schema.* and hold the -W error::DeprecationWarning gate</name>
  <files>tests/test_database.py, tests/test_async_database.py, tests/test_database_integration.py, tests/test_sql_injection.py</files>
  <read_first>
    - .planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md "Test / Gate Surface" → "Existing test call-sites to migrate" (the exhaustive per-file line list: test_database.py list_schemas:270/schema_exists:287,304/list_tables:328/... ; test_async_database.py ; test_database_integration.py ; test_sql_injection.py create_extension/drop_extension/has_extension/drop_index lines)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-CONTEXT.md `<canonical_refs>` "Call-sites to migrate" (enumerates the test files: test_database.py, test_async_database.py, test_database_integration.py, test_sql_injection.py, test_subprocess_env.py)
    - pycopg/schema.py (the 27 flat schema names — the migration target set)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md "-W error::DeprecationWarning gate" + "Note on the flaky test" (test_create_spatial_index_name_parameter touches a relocated method — confirm relocation did not change its pre-existing flakiness profile, which was UndefinedTable fixture-isolation, NOT PostGIS/code)
  </read_first>
  <action>
    Migrate every call-site in the existing behavioral test suite that invokes one of the 27 flat
    schema names on a `db`/`async_db` instance to the accessor path `db.schema.<m>(...)` /
    `await async_db.schema.<m>(...)`. This stops those tests from raising their own internal
    `DeprecationWarning` under the `-W error::DeprecationWarning` gate.

    First, grep the 27 flat schema names across `tests/` to get the authoritative current set (the
    RESEARCH per-file/per-line list is the verified starting map, but re-grep to catch any drift):
    enumerate occurrences in test_database.py, test_async_database.py, test_database_integration.py,
    test_sql_injection.py, and any other file grep surfaces (research also names test_subprocess_env.py).
    For each occurrence that is a CALL on a db/async_db object (not a string literal, not a def, not
    a `db.schema.*` already-migrated call), rewrite `db.<m>(` to `db.schema.<m>(` (and the async
    `await ...<m>(` mirror). Note: the 2 spatial flat names (`create_spatial_index`,
    `list_geometry_columns`) that appear in test_async_database.py / test_sql_injection.py /
    test_postgis_errors.py are NOT in this plan's scope to migrate to `db.spatial.*` — but if leaving
    them flat trips the `-W error` gate in a test you must run green, migrate those specific
    occurrences to `db.spatial.<m>(...)` too (they are covered by plan 03's relocation; the accessor
    exists). Prefer the minimal set needed to keep the gate green.

    Do NOT touch `tests/test_schema_aliases.py` (its whole purpose is to call the flat aliases and
    assert the warning — those calls MUST stay flat). Do NOT touch `tests/test_parity.py` (Task 2).
    Do NOT edit production code (`pycopg/`).
  </action>
  <verify>
    <automated>uv run pytest -W error::DeprecationWarning tests/test_sql_injection.py -q -o addopts="" && uv run pytest -W error::DeprecationWarning tests/test_schema_aliases.py tests/test_parity.py -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - No un-migrated flat schema CALL remains in the migrated behavioral test files (excluding test_schema_aliases.py): a grep for the 27 flat names as `db.<m>(` / `await self.<m>(` style calls in test_database.py / test_async_database.py / test_database_integration.py / test_sql_injection.py returns only `db.schema.<m>` (or `db.spatial.<m>` for the 2 spatial names) forms — confirm by re-grepping the flat names and inspecting that each remaining hit is either a `.schema.`/`.spatial.` accessor call or a non-call (string/comment).
    - Security regression held: `uv run pytest -W error::DeprecationWarning tests/test_sql_injection.py -q -o addopts=""` exits 0 (the validate_* guards still fire on the moved schema bodies via db.schema.*; this is the phase's security-regression check for the schema track).
    - Alias + parity tests pass under the gate: `uv run pytest -W error::DeprecationWarning tests/test_schema_aliases.py tests/test_parity.py -q -o addopts=""` exits 0.
    - Flaky-test profile unchanged: `test_create_spatial_index_name_parameter` (test_postgis_errors.py) still skips when PostGIS is absent / behaves as before — confirm the relocation did not alter its pre-existing UndefinedTable fixture-isolation flakiness (run it targeted with `-o addopts=""`).
  </acceptance_criteria>
  <done>All flat-schema CALL-sites in the existing behavioral test suite are migrated to db.schema.* (and the 2 spatial names to db.spatial.* where needed to keep the gate green); test_sql_injection.py passes under -W error::DeprecationWarning proving the guards still fire on the moved bodies; the schema-alias + parity suites pass under the gate; test_schema_aliases.py left flat by design.</done>
</task>

<task type="auto">
  <name>Task 4: Hold all phase gates — full suite green, coverage >= 94%, validated against 27/29</name>
  <files>tests/test_schema_aliases.py</files>
  <read_first>
    - pyproject.toml `[tool.pytest.ini_options]` (`addopts = "... --cov-fail-under=94"`, `asyncio_mode = "auto"`) — the coverage ratchet this task must hold
    - .planning/phases/23-schema-accessor-spatial-relocation/23-VALIDATION.md "Sampling Rate" + "Per-Task Verification Map" (the full-suite + coverage + -W error gate definitions; the 2 known-flaky DB tests)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md "Sampling Rate" + "Note on the flaky test" (the 2 pre-existing flaky tests: test_async_transaction_fix, test_create_spatial_index_name_parameter — UndefinedTable fixture-isolation, NOT v0.6.0 code; use `-o addopts=""` for targeted runs)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-CONTEXT.md "D-01" (validate against 27 schema / 29 phase-total, NOT 26/28)
  </read_first>
  <action>
    Run the full phase gate and confirm green. This task adds NO production code — it is the
    verification checkpoint that closes the schema track:

    1. Full suite: `uv run pytest` (enforces `--cov-fail-under=94`). The 27 schema stubs (plan 02) +
       the 2 spatial stubs (plan 03) are now exercised by the alias tests (Task 1 + plan 03 Task 5),
       so coverage must hold >= 94%. If coverage dipped below 94%, the most likely cause is an
       alias-test gap (a method name missing from `_SYNC_ALIAS_ARGS`) — fix the dict in
       tests/test_schema_aliases.py to cover the missing stub (D-01: all 27).
    2. The `-W error::DeprecationWarning` gate is held by Task 3 (call-site migration) + plan 03
       Task 4 (the 8 DataFrame call-sites) — confirm no internal deprecation warning leaks at the
       full-suite level: `uv run pytest -W error::DeprecationWarning -q` (modulo the 2 known-flaky DB
       tests — if they fail, re-run them targeted with `-o addopts=""` and confirm the failure is the
       pre-existing UndefinedTable fixture-isolation, NOT a new regression from this phase).
    3. Validate the phase count: 27 schema methods now live on SchemaAccessor + 2 spatial on
       SpatialAccessor = 29 flat names total each with a `@deprecated_alias` stub (D-01).

    If any gate is red for a reason OTHER than the 2 documented pre-existing flaky tests, do NOT
    paper over it — fix the underlying cause (missing alias coverage, an un-migrated call-site, or a
    real regression) before declaring the phase done.
  </action>
  <verify>
    <automated>uv run pytest -q; uv run python -c "from pycopg.schema import SchemaAccessor; from pycopg.spatial import SpatialAccessor; s=len([m for m in dir(SchemaAccessor) if not m.startswith('_')]); assert s==27, s; assert hasattr(SpatialAccessor,'create_spatial_index') and hasattr(SpatialAccessor,'list_geometry_columns'); print('phase count 27 schema + 2 spatial = 29 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green with coverage >= 94%: `uv run pytest` exits 0 (the `--cov-fail-under=94` ratchet in addopts is satisfied), modulo at most the 2 named pre-existing flaky DB tests (test_async_transaction_fix, test_create_spatial_index_name_parameter) — and if those fail, a targeted `-o addopts=""` re-run confirms the failure is the documented UndefinedTable fixture-isolation, not a phase regression.
    - No internal deprecation leak at suite level: `uv run pytest -W error::DeprecationWarning -q` passes (modulo the 2 known-flaky tests).
    - Phase count validated: the python verify prints `phase count 27 schema + 2 spatial = 29 OK` (D-01 — SchemaAccessor has exactly 27 public methods; the 2 spatial methods are present on SpatialAccessor).
    - SCH-01 satisfied end-to-end: db.schema.* exposes the 27 DDL/introspection methods, all 27 flat aliases warn + delegate (alias tests pass), parity green for the schema pair.
  </acceptance_criteria>
  <done>uv run pytest is green with coverage >= 94% (modulo the 2 documented pre-existing flaky tests, confirmed not regressions); the full-suite -W error::DeprecationWarning gate passes; the phase count validates at 27 schema + 2 spatial = 29 (D-01); SCH-01 is satisfied end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test caller to flat db.<m> schema alias | the alias tests deliberately call the deprecated stubs and assert warn + delegate; no real DB crossed (MagicMock on db._schema) |
| migrated behavioral test to db.schema.<m> | existing tests now reach the moved bodies through the accessor; the validate_* guards in those bodies are exercised by test_sql_injection.py |

## STRIDE Threat Register

This plan is TEST-ONLY (no production code changes). Its security value is the regression
check: re-running `tests/test_sql_injection.py` against the migrated `db.schema.*` paths
proves the `validate_*` guards still fire on the bodies moved verbatim in plan 01 — the
security-regression gate for the whole schema track.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Tampering (SQL injection regression) | the moved schema bodies reached via db.schema.* | mitigate | `tests/test_sql_injection.py` (uses create_extension/drop_extension/has_extension/drop_index, migrated to db.schema.*) is re-run under `-W error::DeprecationWarning` and must pass — proving the validate_identifier/validate_extension_name guards travelled with the bodies (plan 01) and still reject malicious identifiers through the accessor path. |
| T-23-07 | Repudiation (coverage ratchet silently dropping) | the 27 thin stubs | mitigate | The 54 alias tests exercise every stub; the `--cov-fail-under=94` gate fails loudly if any stub is uncovered (a missing `_SYNC_ALIAS_ARGS` key), forcing the dict to cover all 27 (D-01). |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pytest/pytest-asyncio/pytest-cov already present (Phase 21/22 infra). No new dependency surface. |

No new threats introduced; this plan strengthens the regression posture by re-exercising the
existing injection guards through the new accessor paths. Block on `high` — none present.
</threat_model>

<verification>
- `tests/test_schema_aliases.py` covers all 27 schema methods sync + async (>=54 tests); each warns
  toward `db.schema.<m>` (incl. "v0.7.0"), at caller stacklevel, and delegates with identical args.
- `(SchemaAccessor, AsyncSchemaAccessor)` is in `ACCESSOR_PAIRS`; `test_accessor_parity` passes for it
  (alongside the spatial pair plan 03 added).
- All existing behavioral test call-sites migrated to `db.schema.*`; the full suite passes under
  `-W error::DeprecationWarning` (modulo the 2 known-flaky DB tests).
- `uv run pytest` green with coverage >= 94% (the `--cov-fail-under=94` ratchet holds — stubs covered).
- Security regression: `tests/test_sql_injection.py` passes against the migrated `db.schema.*` paths.
- Phase count validated at 27 schema + 2 spatial = 29 (D-01).
</verification>

<success_criteria>
- `tests/test_schema_aliases.py` exists covering all 27 schema flat aliases sync + async (>=54 DB-free
  tests), each asserting warn (db.schema.<m>, v0.7.0) + caller stacklevel + delegation.
- `(SchemaAccessor, AsyncSchemaAccessor)` appended to `ACCESSOR_PAIRS`; parity green for the schema pair.
- All flat-schema call-sites in the existing test suite migrated to `db.schema.*`; the
  `-W error::DeprecationWarning` gate passes full-suite (modulo the 2 documented flaky tests).
- `uv run pytest` green with coverage >= 94%; validated against 27/29 (D-01), not 26/28.
- SCH-01 satisfied end-to-end; the schema track's security guards re-proven via test_sql_injection.py.
</success_criteria>

<output>
Create `.planning/phases/23-schema-accessor-spatial-relocation/23-04-SUMMARY.md` when done.
</output>
