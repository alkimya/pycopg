---
phase: 23-schema-accessor-spatial-relocation
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - pycopg/database.py
  - pycopg/async_database.py
  - pycopg/__init__.py
autonomous: true
requirements: [SCH-01]

must_haves:
  truths:
    - "db.schema returns a cached SchemaAccessor; async_db.schema returns a cached AsyncSchemaAccessor (lazy, mirrors db.timescale/db.admin)"
    - "Each of the 27 flat schema names on db.* / async_db.* is now an @deprecated_alias('schema.<m>') stub that warns + delegates"
    - "Sync and async stubs + cache fields + properties land together (TestAsyncParity stays green)"
    - "from pycopg import SchemaAccessor, AsyncSchemaAccessor succeeds (both in __all__)"
  artifacts:
    - path: "pycopg/database.py"
      provides: "_schema cache field + schema lazy property + 27 @deprecated_alias('schema.<m>') stubs"
      contains: "def schema"
    - path: "pycopg/async_database.py"
      provides: "_schema cache field + schema lazy property + 27 async @deprecated_alias('schema.<m>') stubs"
      contains: "def schema"
    - path: "pycopg/__init__.py"
      provides: "SchemaAccessor + AsyncSchemaAccessor import + __all__ entries"
      contains: "SchemaAccessor"
  key_links:
    - from: "Database.schema property"
      to: "pycopg.schema.SchemaAccessor"
      via: "lazy construction + cache into self._schema"
      pattern: "self\\._schema = SchemaAccessor\\(self\\)"
    - from: "27 flat db.* schema defs"
      to: "@deprecated_alias decorator"
      via: "decorator delegating to self.schema.<m>"
      pattern: "@deprecated_alias\\(\"schema\\."
---

<objective>
Wire the schema accessor into `Database`/`AsyncDatabase`: add the `_schema` cache
field and the lazy-cached `schema` property (sync + async, mirroring the existing
`_timescale`/`_admin` pattern), replace the 27 flat schema method definitions with
thin `@deprecated_alias("schema.<m>")` stubs, and export `SchemaAccessor` /
`AsyncSchemaAccessor` from `pycopg/__init__.py` `__all__`. This is Wave 2 of the
schema track (D-03).

Purpose: After this wave, `db.schema.create_database(...)` works via the real
implementation in `pycopg/schema.py` (plan 01), and every old flat
`db.create_database(...)` still works but emits a `DeprecationWarning` naming the
new path — zero breaking change (REORG-02 precedent). Replicate the LOCKED Phase
21/22 wiring exactly.

Output: edits to `database.py`, `async_database.py`, `__init__.py`.

NOTE — call-site rewrites are NOT in this plan. The 8 stay-flat D-05 call-sites in
`from_dataframe`/`from_geodataframe` (which touch BOTH schema methods AND a spatial
method on the same lines) are owned ATOMICALLY by plan 03 to avoid a 2-way edit of
the same lines. After this plan's stubs land, `from_geodataframe` will temporarily
call the deprecated aliases internally; the `-W error::DeprecationWarning` gate is
therefore NOT run green until plan 03 completes the call-site rewrites. Run the
quick alias/import checks here, not the global `-W error` gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-CONTEXT.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md
@.planning/phases/22-admin-maint-backup-accessors/22-CONTEXT.md
@pycopg/aliases.py
@pycopg/schema.py
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
See 23-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase symbol list.
This plan produces: `Database._schema` / `AsyncDatabase._schema` cache fields, the
`Database.schema` / `AsyncDatabase.schema` lazy cached properties, the 27
`@deprecated_alias("schema.<m>")` stubs on both classes, and the `SchemaAccessor` /
`AsyncSchemaAccessor` entries in `pycopg/__init__.py` (`from pycopg.schema import ...`
+ two `__all__` entries).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add _schema cache field + lazy schema property + 27 @deprecated_alias stubs (sync + async, same commit)</name>
  <files>pycopg/database.py, pycopg/async_database.py</files>
  <read_first>
    - pycopg/aliases.py (the `@deprecated_alias(target_path)` decorator — confirm it resolves `self.schema` lazily, branches sync/async via `iscoroutinefunction`, emits the v0.7.0-removal message at `stacklevel=2`; reuse verbatim, zero changes)
    - pycopg/database.py lines 83-90 (existing `_spatial`/`_etl`/`_timescale`/`_admin`/`_maint`/`_backup` cache fields in `__init__`) and the `timescale`/`admin` lazy `@property` blocks (the exact idiom to copy for `schema`)
    - pycopg/async_database.py lines 79-82 (async cache fields) + lines 96-168 (async `spatial`/`timescale`/`admin` lazy properties — async mirror)
    - pycopg/database.py the 27 flat schema method defs at the source lines (840-1465) being replaced — and the existing Phase 21/22 `@deprecated_alias("timescale.<m>")` / `@deprecated_alias("admin.<m>")` stubs already in this file (the exact one-line stub shape to copy)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md "pycopg/database.py (db wiring)" + "pycopg/async_database.py" sections (cache field, lazy property, and stub patterns, with the exact target-path strings)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md "Pitfall 5" (sync AND async stubs + properties MUST land in the SAME commit or TestAsyncParity fails mid-transition)
  </read_first>
  <action>
    Sync (`pycopg/database.py`):
    1. In `Database.__init__`, add `self._schema: SchemaAccessor | None = None`
       alongside the existing `_timescale`/`_admin`/… cache fields. Add the
       `if TYPE_CHECKING:` import for `SchemaAccessor` if the file uses that pattern
       for the other accessors (follow the existing convention exactly).
    2. Add a lazy `@property def schema(self) -> SchemaAccessor:` block copying the
       `timescale` property verbatim: `if self._schema is None: from pycopg.schema
       import SchemaAccessor; self._schema = SchemaAccessor(self); return
       self._schema`. Numpydoc docstring ("Get or create the schema accessor (lazy
       initialization). Provides access to DDL and introspection operations. ...
       Returns SchemaAccessor ...").
    3. Replace each of the 27 flat schema method definitions with a thin stub:
       `@deprecated_alias("schema.<m>")` + `def <m>(self, *args, **kwargs):` +
       one-line docstring `"""Deprecated: use ``db.schema.<m>`` instead."""`. The 27
       target-path strings are exactly: schema.create_database, schema.drop_database,
       schema.database_exists, schema.list_databases, schema.create_extension,
       schema.drop_extension, schema.list_extensions, schema.has_extension,
       schema.create_schema, schema.drop_schema, schema.list_schemas,
       schema.schema_exists, schema.list_tables, schema.table_exists,
       schema.list_columns, schema.columns_with_types, schema.drop_table,
       schema.truncate_table, schema.table_info, schema.row_count,
       schema.add_primary_key, schema.add_foreign_key, schema.add_unique_constraint,
       schema.create_index, schema.drop_index, schema.list_indexes,
       schema.list_constraints. Delete the now-moved method bodies (the real logic
       lives in `pycopg/schema.py` from plan 01).

    Async (`pycopg/async_database.py`): apply the exact same three edits — add
    `self._schema: AsyncSchemaAccessor | None = None`, the async `schema` lazy
    property (mirror the async `spatial`/`admin` property, importing
    `AsyncSchemaAccessor`), and the 27 `@deprecated_alias("schema.<m>")` stubs (same
    generic `(*args, **kwargs)` signature — the decorator auto-branches to the async
    path via `iscoroutinefunction`, so the stub is declared `async def`). Use the
    identical 27 target-path strings.

    CRITICAL (Pitfall 5): make BOTH files' edits in the same commit so
    `TestAsyncParity` (which diffs `inspect.getmembers(Database)` vs `AsyncDatabase`)
    never sees a half-migrated surface.

    Do NOT rewrite the `from_dataframe`/`from_geodataframe` call-sites here (plan 03
    owns those atomically — D-05/D-03). Do NOT touch `spatial.py` or `test_parity.py`
    here. Do NOT alter `aliases.py`.
  </action>
  <verify>
    <automated>uv run python -c "
import warnings
from pycopg import Database, AsyncDatabase
from pycopg.schema import SchemaAccessor, AsyncSchemaAccessor
from pycopg.config import Config
from unittest.mock import MagicMock
c = Config(host='h', dbname='d', user='u', password='p')
db = Database(c); db._schema = MagicMock(spec=SchemaAccessor)
with warnings.catch_warnings(record=True) as w:
    warnings.simplefilter('always'); db.list_tables('public')
assert any(x.category is DeprecationWarning and 'db.schema.list_tables' in str(x.message) for x in w), [str(x.message) for x in w]
db._schema.list_tables.assert_called_once_with('public')
print('sync stub OK')
" -W ignore && uv run ruff check pycopg/database.py pycopg/async_database.py</automated>
  </verify>
  <acceptance_criteria>
    - 27 sync stubs present: `grep -c '@deprecated_alias("schema\.' pycopg/database.py` == 27.
    - 27 async stubs present: `grep -c '@deprecated_alias("schema\.' pycopg/async_database.py` == 27.
    - Cache field added both files: `grep -c '_schema' pycopg/database.py` >= 2 and `grep -c '_schema' pycopg/async_database.py` >= 2.
    - Lazy property added both files: `grep -c 'def schema' pycopg/database.py` >= 1 and same for async_database.py (excluding the stub-arg `schema=` keyword — confirm a `@property`/`def schema(self)` exists).
    - No moved body left behind: `grep -nE 'self\.config\.with_database' pycopg/database.py` returns ZERO (the create_database/drop_database/database_exists bodies are gone — they now live in schema.py; the flat names are bare stubs).
    - Sync stub warns + delegates: the python verify command above prints `sync stub OK` and exits 0.
    - `uv run ruff check pycopg/database.py pycopg/async_database.py` exits 0.
  </acceptance_criteria>
  <done>Both Database and AsyncDatabase have the _schema cache field, the lazy schema property, and 27 @deprecated_alias("schema.<m>") stubs; the moved bodies are deleted from the flat surface; a sync stub provably warns (naming db.schema.<m>, v0.7.0) and delegates; ruff clean; sync+async landed together.</done>
</task>

<task type="auto">
  <name>Task 2: Export SchemaAccessor + AsyncSchemaAccessor from pycopg/__init__.py</name>
  <files>pycopg/__init__.py</files>
  <read_first>
    - pycopg/__init__.py lines 1-75 (the import block — line 7 `from pycopg.admin import AdminAccessor, AsyncAdminAccessor`, line 29 `from pycopg.spatial import ...` — and the `__all__` list with the `# Admin`/`# Schema`-style grouped accessor entries at lines 44-73)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md "pycopg/__init__.py" section (the exact import-line + `__all__` entry pattern)
  </read_first>
  <action>
    Add `from pycopg.schema import AsyncSchemaAccessor, SchemaAccessor` to the import
    block (alphabetical/grouped to match the existing accessor imports). Add two
    entries to `__all__` — `"SchemaAccessor"` and `"AsyncSchemaAccessor"` — placed
    alongside the other accessor groups (e.g. a `# Schema` comment block mirroring
    the `# Admin` / `# Backup` blocks). This is the pattern-dictated `__all__` entry
    only; formal README/Sphinx/CHANGELOG export hardening is Phase 24 (deferred — do
    NOT add doc/release work here).
  </action>
  <verify>
    <automated>uv run python -c "import pycopg; from pycopg import SchemaAccessor, AsyncSchemaAccessor; assert 'SchemaAccessor' in pycopg.__all__ and 'AsyncSchemaAccessor' in pycopg.__all__" && uv run ruff check pycopg/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - Import present: `grep -c 'from pycopg.schema import' pycopg/__init__.py` == 1.
    - Both in `__all__`: `grep -c '"SchemaAccessor"' pycopg/__init__.py` == 1 and `grep -c '"AsyncSchemaAccessor"' pycopg/__init__.py` == 1.
    - `from pycopg import SchemaAccessor, AsyncSchemaAccessor` works and both names are in `pycopg.__all__`: the python verify command exits 0.
    - `uv run ruff check pycopg/__init__.py` exits 0.
  </acceptance_criteria>
  <done>SchemaAccessor and AsyncSchemaAccessor are importable from the top-level pycopg package and present in __all__, matching the existing accessor export pattern; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → flat `db.<m>` alias | the deprecated stub forwards `*args, **kwargs` unchanged to `self.schema.<m>`; no validation logic lives in the stub (it lives in the moved body, plan 01) |
| Database → SchemaAccessor | lazy construction caches a single accessor instance per db; no new I/O surface |

## STRIDE Threat Register

This wave is pure wiring (cache field + property + thin delegating stubs + export).
It adds NO new SQL, NO new user-input path; the stubs forward verbatim to the
already-guarded accessor bodies.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02 | Tampering (validator bypass via stub) | the 27 thin stubs | mitigate | Stubs do NOT validate; they forward `*args, **kwargs` to `self.schema.<m>`, whose body retains the `validate_*` guards (plan 01). No validation is moved out of the data path. The sync-stub verify proves delegation forwards args unchanged. |
| T-23-03 | Repudiation (silent internal deprecated call) | half-migrated surface | mitigate | The `-W error::DeprecationWarning` gate is held by plan 03 (after the D-05 call-site rewrites land); this plan deliberately defers that gate because `from_geodataframe` still calls the flat aliases until plan 03. |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No package installs — no new dependency surface. |

No new threats introduced; the verbatim-move invariant (plan 01) preserves the
`validate_*` guards behind every stub. Block on `high` — none present.
</threat_model>

<verification>
- Sync `db.list_tables(...)` emits a `DeprecationWarning` naming `db.schema.list_tables` (incl. "v0.7.0") and delegates to the cached accessor (verified DB-free with a MagicMock).
- 27 `@deprecated_alias("schema.<m>")` stubs in each of database.py and async_database.py.
- `from pycopg import SchemaAccessor, AsyncSchemaAccessor` succeeds; both in `__all__`.
- `uv run ruff check pycopg/` exits 0.
- (The global `-W error::DeprecationWarning` gate and full suite are held by plan 03/04 — this wave intentionally leaves the from_geodataframe call-sites for plan 03.)
</verification>

<success_criteria>
- `Database`/`AsyncDatabase` each gain the `_schema` cache field, the lazy `schema` property, and 27 `@deprecated_alias("schema.<m>")` stubs (sync + async, same commit — TestAsyncParity safe).
- The 27 moved bodies are deleted from the flat surface (real logic lives in `pycopg/schema.py`).
- `SchemaAccessor`/`AsyncSchemaAccessor` exported from `pycopg/__init__.py` `__all__`.
- A sync flat alias provably warns (naming `db.schema.<m>`, v0.7.0) and delegates; ruff clean.
</success_criteria>

<output>
Create `.planning/phases/23-schema-accessor-spatial-relocation/23-02-SUMMARY.md` when done.
</output>
