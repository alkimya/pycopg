---
phase: 23-schema-accessor-spatial-relocation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/schema.py
autonomous: true
requirements: [SCH-01]

must_haves:
  truths:
    - "pycopg/schema.py defines SchemaAccessor and AsyncSchemaAccessor (D-01: 27 methods each)"
    - "Every moved body uses self._db.execute (never self.execute) — verbatim move with D-04 rewrite"
    - "The 3 database-level methods (create_database/drop_database/database_exists) use self._db.config (never self.config) — D-04"
    - "All validate_identifier/validate_identifiers/validate_extension_name/validate_index_method guards travel verbatim with the moved bodies (no SQL-injection vector reintroduced)"
  artifacts:
    - path: "pycopg/schema.py"
      provides: "SchemaAccessor + AsyncSchemaAccessor, 27 DDL/introspection methods each, moved verbatim"
      contains: "class SchemaAccessor"
      min_lines: 400
  key_links:
    - from: "pycopg/schema.py SchemaAccessor methods"
      to: "self._db.execute / self._db.config"
      via: "delegation to parent database"
      pattern: "self\\._db\\.(execute|config)"
    - from: "pycopg/schema.py"
      to: "pycopg.queries SQL constants + pycopg.utils validators"
      via: "verbatim travelling imports"
      pattern: "from pycopg import queries"
---

<objective>
Create the new `pycopg/schema.py` module holding `SchemaAccessor` and
`AsyncSchemaAccessor`, each with all 27 DDL + introspection methods moved
**verbatim** from `pycopg/database.py` / `pycopg/async_database.py`, rewriting
every `self.<X>` self-reference to `self._db.<X>` (D-04). This is the largest of
the three accessor extractions in v0.6.0 and the first wave of the schema track
(D-02/D-03). It is pure module creation — no wiring, no stubs, no tests yet.

Purpose: Establish the real implementation home for the schema surface so Wave 2
can replace the 27 flat `db.*` defs with thin deprecated aliases that delegate
here. This is the third (and largest) application of the LOCKED Phase 21/22
accessor pattern — favour line-for-line replication of `pycopg/admin.py`;
divergence is a smell (D-07 precedent: move, don't improve).

Output: one new file `pycopg/schema.py` (~27 methods × 2 classes).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-CONTEXT.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md
@.planning/phases/21-infrastructure-timescale-accessor/21-CONTEXT.md
@.planning/phases/22-admin-maint-backup-accessors/22-CONTEXT.md
@pycopg/admin.py
@pycopg/timescale.py
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This phase (23) produces, across its 4 plans:
- `pycopg/schema.py` — `SchemaAccessor` + `AsyncSchemaAccessor` (27 methods each)  ← **THIS PLAN**
- `Database.schema` / `AsyncDatabase.schema` lazy cached properties (sync + async) — plan 02
- `Database._schema` / `AsyncDatabase._schema` cache fields (sync + async) — plan 02
- 27 `@deprecated_alias("schema.<m>")` stubs on `Database`/`AsyncDatabase` (sync + async) — plan 02
- 2 relocated spatial methods (`create_spatial_index`, `list_geometry_columns`) added to `SpatialAccessor`/`AsyncSpatialAccessor` in `pycopg/spatial.py` — plan 03
- 2 `@deprecated_alias("spatial.<m>")` stubs on `Database`/`AsyncDatabase` (sync + async) — plan 03
- `SchemaAccessor` / `AsyncSchemaAccessor` entries in `pycopg/__init__.py` `__all__` — plan 02
- `tests/test_schema_aliases.py` (27 sync + 27 async alias tests) — plan 04
- `tests/test_spatial_aliases.py` (2 sync + 2 async alias tests) — plan 03
- `(SchemaAccessor, AsyncSchemaAccessor)` appended to `ACCESSOR_PAIRS` — plan 04
- `(SpatialAccessor, AsyncSpatialAccessor)` appended to `ACCESSOR_PAIRS` (NOT pre-existing — research correction #1) — plan 03

**This plan's outputs:** `pycopg/schema.py` with both accessor classes and 27 verbatim-moved method bodies each.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create pycopg/schema.py with SchemaAccessor (27 sync method bodies moved verbatim, D-04 rewrite)</name>
  <files>pycopg/schema.py</files>
  <read_first>
    - pycopg/admin.py (THE exact module template: module docstring, `from __future__ import annotations`, `from typing import TYPE_CHECKING`, the `from pycopg import queries` + `from pycopg.utils import ...` imports, the `if TYPE_CHECKING: from pycopg.database import Database / from pycopg.async_database import AsyncDatabase` guard, `class AdminAccessor: def __init__(self, db): self._db = db`, and the per-method `self._db.execute(...)` shape with numpydoc docstrings)
    - pycopg/timescale.py (second instance of the same module shape — confirm the conventions are uniform)
    - pycopg/database.py lines 840-1465 (the 27 sync schema method bodies to move — exact source; line map below)
    - pycopg/database.py lines 840-863 + 865-890 + 892-910 (the 3 database-level methods that use `self.config.with_database("postgres")` then `psycopg.connect(...)` — these need the `self.config` → `self._db.config` rewrite)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md "Claim 2: D-04 self.X enumeration" (the complete verified self.X inventory: 3 methods use self.config, 24 use self.execute, ZERO sibling-schema self-calls)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md "pycopg/schema.py (new accessor module)" section (module docstring text, import list, the create_database `self._db.config` pattern, the list_tables `self._db.execute` pattern)
  </read_first>
  <action>
    Create `pycopg/schema.py` mirroring `pycopg/admin.py` exactly. Header: module
    docstring (name SchemaAccessor/AsyncSchemaAccessor, "the real implementation of
    the 27 DDL + introspection helper methods, moved verbatim from Database /
    AsyncDatabase as part of the v0.6.0 accessor reorganisation"); `from __future__
    import annotations`; `from typing import TYPE_CHECKING`; `import psycopg`
    (required by the 3 database-level methods); `from pycopg import queries`; `from
    pycopg.utils import validate_identifier, validate_identifiers,
    validate_extension_name, validate_index_method` (import ONLY what the 27 bodies
    actually call — `validate_index_method` is used by `create_index` at
    database.py:1396, `validate_extension_name` by the extension methods); the
    `if TYPE_CHECKING:` guard importing `Database` and `AsyncDatabase`.

    Define `class SchemaAccessor` with `def __init__(self, db: Database) -> None:
    self._db = db` (no connection check — schema has NO PostGIS/extension guard,
    unlike SpatialAccessor).

    Move all 27 sync method bodies VERBATIM from `pycopg/database.py`, applying the
    D-04 rewrite to every `self.<X>` reference:
    - `self.execute(...)` → `self._db.execute(...)` (24 methods)
    - `self.config.with_database("postgres")` → `self._db.config.with_database("postgres")`
      in the 3 database-level methods ONLY: `create_database`, `drop_database`,
      `database_exists` (research-verified: self.config at sync lines 860/877/905).
    - Keep every `validate_identifier`/`validate_identifiers`/`validate_extension_name`/
      `validate_index_method` guard call exactly as-is (security invariant: a dropped
      validator reintroduces a SQL-injection vector). Keep all inline f-string SQL and
      all `queries.<CONST>` references UNCHANGED (D-07).
    - Keep each method's existing numpydoc docstring verbatim (interrogate ≥ 95).
    - Do NOT call any sibling schema method via the flat alias — research confirmed
      ZERO sibling-schema self-calls across all 27 bodies, so there is nothing to
      rewrite to `self._db.schema.<m>` here; if you find one the research missed,
      rewrite it to `self._db.schema.<m>` (NOT `self._db.<m>`, which would be the
      deprecated alias) and note it in the SUMMARY.

    The 27 methods (sync source lines in database.py): create_database(840),
    drop_database(865), database_exists(892), list_databases(911),
    create_extension(926), drop_extension(949), list_extensions(970),
    has_extension(980), create_schema(1000), drop_schema(1021), list_schemas(1040),
    schema_exists(1051), list_tables(1071), table_exists(1087), list_columns(1105),
    columns_with_types(1123), drop_table(1143), truncate_table(1168),
    table_info(1186), row_count(1204), add_primary_key(1228), add_foreign_key(1260),
    add_unique_constraint(1331), create_index(1362), drop_index(1408),
    list_indexes(1426), list_constraints(1443).

    Do NOT yet touch `database.py`/`async_database.py` (the flat defs stay until
    plan 02). Do NOT add `__init__.py` exports here (plan 02). Do NOT extract any
    new `queries.py` builder (D-07).
  </action>
  <verify>
    <automated>uv run python -c "from pycopg.schema import SchemaAccessor; ms=[m for m in dir(SchemaAccessor) if not m.startswith('_')]; assert len(ms)==27, ms; assert 'create_database' in ms and 'list_constraints' in ms" && uv run ruff check pycopg/schema.py</automated>
  </verify>
  <acceptance_criteria>
    - `pycopg/schema.py` exists and `grep -c "class SchemaAccessor" pycopg/schema.py` == 1.
    - SchemaAccessor has exactly 27 public methods: the python verify command above exits 0.
    - No `self.execute` / `self.config` leak in the moved bodies: `grep -nE 'self\.(execute|config)\b' pycopg/schema.py` returns ZERO matches (all rewritten to `self._db.`).
    - The 3 database-level methods reference `self._db.config`: `grep -c 'self\._db\.config' pycopg/schema.py` >= 3.
    - Validators preserved: `grep -c 'validate_index_method' pycopg/schema.py` >= 1 and `grep -c 'validate_identifiers' pycopg/schema.py` >= 1 (security invariant — guards travelled).
    - No deprecated-flat self-call leak: `grep -nE 'self\._db\.(create_database|list_tables|create_index|add_primary_key)\(' pycopg/schema.py` returns ZERO (no accessor body calls a flat schema alias on the parent).
    - `uv run ruff check pycopg/schema.py` exits 0.
  </acceptance_criteria>
  <done>pycopg/schema.py exists with SchemaAccessor containing all 27 verbatim-moved DDL/introspection methods; every self.execute → self._db.execute and self.config → self._db.config; all validators preserved; ruff clean; no flat-alias self-call leaks.</done>
</task>

<task type="auto">
  <name>Task 2: Add AsyncSchemaAccessor (27 async method bodies, sync↔async parity)</name>
  <files>pycopg/schema.py</files>
  <read_first>
    - pycopg/admin.py (the `class AsyncAdminAccessor` half — confirm the async accessor lives in the same module, `def __init__(self, db: AsyncDatabase)`, methods are `async def` and `await self._db.execute(...)`)
    - pycopg/async_database.py lines 712-1230 + 1740-1840 (the 27 async schema method bodies — source; async line map below)
    - pycopg/async_database.py lines 1740-1825 (the 3 async database-level methods using `self.config`)
    - The SchemaAccessor class just written in Task 1 (the async class mirrors it method-for-method)
  </read_first>
  <action>
    In the SAME `pycopg/schema.py`, add `class AsyncSchemaAccessor` mirroring
    SchemaAccessor exactly: `def __init__(self, db: AsyncDatabase) -> None: self._db
    = db` (no guard), then all 27 methods as `async def` with `await
    self._db.execute(...)` and the same D-04 rewrite:
    - `await self.execute(...)` → `await self._db.execute(...)` (24 methods).
    - `self.config` → `self._db.config` in the 3 database-level methods
      (create_database, drop_database, database_exists). Note: in async these still
      open a synchronous `psycopg.connect(...)` admin connection (verbatim move —
      do NOT convert to async psycopg; D-07 move-don't-improve).
    - Preserve all validators and `queries.<CONST>` references unchanged.
    - Keep each method's existing numpydoc docstring verbatim.

    The 27 async methods (source lines in async_database.py): list_schemas(712),
    schema_exists(723), create_schema(739), list_tables(760), table_exists(776),
    list_columns(794), columns_with_types(812), table_info(832), row_count(850),
    drop_schema(870), drop_table(889), truncate_table(914), add_primary_key(936),
    add_foreign_key(968), add_unique_constraint(1039), create_index(1070),
    drop_index(1116), list_indexes(1134), list_constraints(1151), has_extension(1172),
    create_extension(1188), list_extensions(1211), drop_extension(1221),
    create_database(1740), drop_database(1769), database_exists(1798),
    list_databases(1819).

    Parity requirement: AsyncSchemaAccessor's public-method set MUST equal
    SchemaAccessor's public-method set exactly (27 == 27, same names). This is what
    plan 04's `ACCESSOR_PAIRS` parity test will enforce.
  </action>
  <verify>
    <automated>uv run python -c "from pycopg.schema import SchemaAccessor, AsyncSchemaAccessor; import inspect; s={m for m in dir(SchemaAccessor) if not m.startswith('_')}; a={m for m in dir(AsyncSchemaAccessor) if not m.startswith('_')}; assert s==a, (s^a); assert len(s)==27; assert all(inspect.iscoroutinefunction(getattr(AsyncSchemaAccessor,m)) for m in a)" && uv run ruff check pycopg/schema.py && uv run black --check pycopg/schema.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class AsyncSchemaAccessor" pycopg/schema.py` == 1.
    - Sync↔async parity: the python verify command exits 0 (identical 27-method public surface; every async method is a coroutine).
    - No `self.execute`/`self.config` leak: `grep -nE 'self\.(execute|config)\b' pycopg/schema.py` still returns ZERO matches (covers both classes).
    - No flat-alias self-call leak: `grep -nE 'self\._db\.(create_database|list_tables|create_index|add_primary_key|has_extension)\(' pycopg/schema.py` returns ZERO.
    - interrogate: `uv run interrogate pycopg/schema.py -f 95` exits 0 (every method kept its docstring).
    - `uv run ruff check pycopg/schema.py` and `uv run black --check pycopg/schema.py` both exit 0.
  </acceptance_criteria>
  <done>AsyncSchemaAccessor exists in pycopg/schema.py with all 27 async methods at exact sync↔async parity; D-04 rewrites applied; validators preserved; interrogate ≥ 95; ruff + black clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → SchemaAccessor method args | identifiers (table/schema/index names) cross here; guarded by `validate_identifier(s)` / `validate_extension_name` / `validate_index_method` before interpolation into DDL |
| SchemaAccessor → PostgreSQL | DDL/introspection SQL executed via `self._db.execute` (or `psycopg.connect` admin conn for the 3 DB-level methods) |

## STRIDE Threat Register

This phase is a **pure internal refactor**: it MOVES existing method bodies between
classes, adds NO new I/O surface, NO new SQL, NO new user-input path. The moved
bodies keep their existing validation guards verbatim, travelling with the code.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering (SQL injection) | the verbatim move of the 27 bodies | mitigate | The verbatim-move invariant preserves every `validate_identifier`/`validate_identifiers`/`validate_extension_name`/`validate_index_method` guard. A dropped validator would reintroduce a SQL-injection vector. Acceptance criteria assert the validators are present in `schema.py`; plan 04 re-runs `tests/test_sql_injection.py` after call-site migration to prove the guards still fire on the moved bodies. |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pytest/pytest-cov/pytest-asyncio/interrogate already present (Phase 21/22 infra). No new dependency surface. |

No new threats introduced; the verbatim-move invariant preserves the existing
`validate_*` guards. Block on `high` — none present.
</threat_model>

<verification>
- `uv run python -c "from pycopg.schema import SchemaAccessor, AsyncSchemaAccessor; ..."` confirms 27 == 27 public methods at sync↔async parity.
- `grep -nE 'self\.(execute|config)\b' pycopg/schema.py` returns nothing (all D-04 rewrites applied).
- Validators present: `validate_index_method` and `validate_identifiers` both grep-found in schema.py.
- `uv run interrogate pycopg/schema.py -f 95`, `uv run ruff check pycopg/schema.py`, `uv run black --check pycopg/schema.py` all exit 0.
- (Wiring, stubs, and behavioural DB tests are proven in plans 02–04 once the flat defs are replaced and call-sites migrated.)
</verification>

<success_criteria>
- `pycopg/schema.py` exists with `SchemaAccessor` and `AsyncSchemaAccessor`, 27 methods each (D-01 count), at exact sync↔async parity.
- All 27 bodies moved verbatim with the D-04 rewrite (`self.execute`→`self._db.execute`, `self.config`→`self._db.config` in the 3 DB-level methods); no flat-alias self-call leak.
- All `validate_*` guards and `queries.*` constants travelled unchanged (D-07); no new builder extracted.
- ruff + black + interrogate (≥ 95) clean on the new module.
</success_criteria>

<output>
Create `.planning/phases/23-schema-accessor-spatial-relocation/23-01-SUMMARY.md` when done.
</output>
