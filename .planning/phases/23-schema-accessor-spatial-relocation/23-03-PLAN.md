---
phase: 23-schema-accessor-spatial-relocation
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - pycopg/spatial.py
  - pycopg/database.py
  - pycopg/async_database.py
  - tests/test_parity.py
  - tests/test_spatial_aliases.py
autonomous: true
requirements: [SCH-02]

must_haves:
  truths:
    - "db.spatial.create_spatial_index and db.spatial.list_geometry_columns are callable on SpatialAccessor and AsyncSpatialAccessor (SCH-02 relocation, D-07 verbatim move)"
    - "The 2 async relocated methods call self._check_postgis() as their first awaitable line (research finding #4 — AsyncSpatialAccessor.__init__ does NOT raise on missing PostGIS)"
    - "Old flat db.create_spatial_index / db.list_geometry_columns still work but emit a DeprecationWarning naming db.spatial.<m> (v0.7.0) and delegate to the cached accessor"
    - "ZERO internal DeprecationWarning: from_dataframe/from_geodataframe call the accessor paths (self.schema.<m> / self.spatial.<m>), never the flat self.<moved-name> alias (D-05 — the gate plan 02 deferred)"
    - "(SpatialAccessor, AsyncSpatialAccessor) is present in ACCESSOR_PAIRS (research correction #1 — it was ABSENT)"
  artifacts:
    - path: "pycopg/spatial.py"
      provides: "create_spatial_index + list_geometry_columns added to BOTH SpatialAccessor and AsyncSpatialAccessor (verbatim move, self.execute to self._db.execute; async prepends _check_postgis)"
      contains: "def create_spatial_index"
    - path: "tests/test_spatial_aliases.py"
      provides: "2 sync + 2 async DB-free MagicMock alias tests (warn + caller stacklevel + delegate on db._spatial)"
      contains: "class TestSpatialAliases"
    - path: "tests/test_parity.py"
      provides: "(SpatialAccessor, AsyncSpatialAccessor) appended to ACCESSOR_PAIRS + import"
      contains: "SpatialAccessor"
  key_links:
    - from: "from_geodataframe / from_dataframe internal call-sites"
      to: "self.schema.* / self.spatial.* accessor paths"
      via: "D-05 atomic rewrite (8 sites: 4 sync + 4 async)"
      pattern: "self\\.(schema|spatial)\\.(add_primary_key|has_extension|create_spatial_index)"
    - from: "2 flat spatial defs on Database/AsyncDatabase"
      to: "@deprecated_alias decorator delegating to self.spatial.<m>"
      via: "thin delegating stub"
      pattern: "@deprecated_alias\\(\"spatial\\."
    - from: "AsyncSpatialAccessor.create_spatial_index / list_geometry_columns"
      to: "self._check_postgis()"
      via: "deferred PostGIS guard, first awaitable line"
      pattern: "await self\\._check_postgis\\(\\)"
---

<objective>
Execute the SPATIAL-RELOCATION track (SCH-02): relocate the 2 PostGIS spatial-index
methods (`create_spatial_index`, `list_geometry_columns`) into the EXISTING
`SpatialAccessor` / `AsyncSpatialAccessor` in `pycopg/spatial.py` (D-07 verbatim move,
`self.execute` to `self._db.execute` per D-04), leave 2 `@deprecated_alias("spatial.<m>")`
stubs on `Database`/`AsyncDatabase`, append `(SpatialAccessor, AsyncSpatialAccessor)` to
`ACCESSOR_PAIRS`, add 2 DB-free spatial alias tests, AND atomically rewrite ALL 8
stay-flat D-05 call-sites in `from_dataframe`/`from_geodataframe` (those lines touch BOTH
a schema method AND a spatial method, so the single-owner constraint of D-03/D-05 routes
both rewrites through this plan).

Purpose: After this plan, `db.spatial.create_spatial_index(...)` /
`db.spatial.list_geometry_columns()` work via the relocated bodies; the old flat names
still work but warn toward `db.spatial.*`; and `from_geodataframe` no longer triggers an
internal `DeprecationWarning` — so the `-W error::DeprecationWarning` gate that plan 02
deliberately deferred now passes for the DataFrame paths. This is the one structurally
novel sub-task of the milestone: it edits an EXISTING accessor + EXISTING tests and
changes the deprecated path's failure mode (D-06 PostGIS guard, accepted).

Output: edits to `spatial.py`, `database.py`, `async_database.py`, `test_parity.py`, plus
new `tests/test_spatial_aliases.py`.

NOTE — this plan owns the 8 D-05 call-sites ATOMICALLY because `from_geodataframe`'s lines
1583/1619/1622 (and async 1568/1607/1610) call a schema method (`has_extension`,
`add_primary_key`, both stubbed by plan 02) AND a spatial method (`create_spatial_index`,
relocated here) on adjacent lines. Splitting that edit across two plans would mean a 2-way
edit of the same method body. This plan therefore depends on `23-02` (it needs `db.schema.*`
plus the schema stubs to already exist before rewriting those schema-method call-sites to
`self.schema.*`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-CONTEXT.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md
@.planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md
@pycopg/aliases.py
@pycopg/spatial.py
@pycopg/schema.py
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
See 23-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase symbol list.
This plan (the spatial-relocation track) produces:
- `create_spatial_index` + `list_geometry_columns` added to `SpatialAccessor` (sync) and
  `AsyncSpatialAccessor` (async) in `pycopg/spatial.py` — verbatim move with D-04 rewrite;
  the async pair prepends `await self._check_postgis()`.
- 2 `@deprecated_alias("spatial.create_spatial_index")` / `@deprecated_alias("spatial.list_geometry_columns")`
  stubs on `Database` AND `AsyncDatabase` (replacing the relocated flat bodies).
- `(SpatialAccessor, AsyncSpatialAccessor)` appended to `ACCESSOR_PAIRS` in `tests/test_parity.py`
  (NOT pre-existing — research correction #1) + the import.
- `tests/test_spatial_aliases.py` (2 sync + 2 async alias tests).
- The 8 rewritten D-05 call-sites in `from_dataframe`/`from_geodataframe` (4 sync + 4 async).
No new property/cache field needed — `db.spatial` / `async_db.spatial` already exist.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the 2 spatial methods into SpatialAccessor + AsyncSpatialAccessor (D-07 verbatim move; async prepends _check_postgis)</name>
  <files>pycopg/spatial.py</files>
  <read_first>
    - pycopg/spatial.py lines 1023-1077 (SpatialAccessor: the constructor guard at :1047 `if not db.has_extension("postgis"): raise ExtensionNotAvailable(_POSTGIS_GUARD_MSG)` — the 2 relocated SYNC methods inherit this automatically; and the existing `self._db.execute(...)` / `_run(...)` shape, so new methods go after the last existing SpatialAccessor method)
    - pycopg/spatial.py lines 1859-1891 (AsyncSpatialAccessor: `__init__` stores `self._postgis_ok = False` and does NOT raise; `async def _check_postgis(self)` at :1879 — every other async spatial method awaits it first; the 2 relocated ASYNC methods MUST do the same)
    - pycopg/database.py lines 1671-1718 (the 2 SYNC source bodies: `create_spatial_index` inline f-string `CREATE INDEX IF NOT EXISTS {index_name} ON {schema}.{table} USING GIST ({column})` guarded by `validate_identifiers(table, column, schema)` + `if name: validate_identifier(name)`; `list_geometry_columns` using `queries.LIST_GEOMETRY_COLUMNS.format(where_clause=where_clause)`)
    - pycopg/async_database.py lines 1246 (async create_spatial_index) and 1275 (async list_geometry_columns) — the async source bodies to move
    - .planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md "pycopg/spatial.py (existing-accessor edit)" section (the exact verbatim-move + `await self._check_postgis()` placement pattern)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md "Claim 4: Spatial relocation specifics" + "Pitfall 2" (the async _check_postgis mandate)
  </read_first>
  <action>
    In `pycopg/spatial.py`, add the 2 methods to BOTH accessor classes, moved VERBATIM from
    `database.py` / `async_database.py` (D-07: move, do NOT improve — keep the inline f-string
    SQL and the `queries.LIST_GEOMETRY_COLUMNS.format(...)` call exactly as-is; do NOT route them
    through the pure-builder `_run(...)` house style; do NOT extract any `queries.py` builder).

    SpatialAccessor (sync) — add `create_spatial_index(self, table, column="geometry",
    schema="public", name=None)` and `list_geometry_columns(self, schema=None)` after the last
    existing SpatialAccessor method, before `class AsyncSpatialAccessor`. Apply the D-04 rewrite:
    every `self.execute(...)` becomes `self._db.execute(...)`. Keep `validate_identifiers(table,
    column, schema)` and the `if name: validate_identifier(name)` guard verbatim (security
    invariant — a dropped validator reintroduces a SQL-injection vector). Keep each method's
    existing numpydoc docstring verbatim. The sync methods need NO explicit PostGIS guard — they
    inherit the constructor guard at spatial.py:1047 (D-06). Confirm `validate_identifier`,
    `validate_identifiers`, `queries`, and `ExtensionNotAvailable` are already imported in
    spatial.py (they are — used by existing spatial methods); add nothing new.

    AsyncSpatialAccessor (async) — add `async def create_spatial_index(...)` and `async def
    list_geometry_columns(...)` with the SAME signatures and bodies, using `await
    self._db.execute(...)`. RESEARCH CORRECTION #2 / Pitfall 2 (MANDATORY): each async method MUST
    have `await self._check_postgis()` as its FIRST awaitable line — `AsyncSpatialAccessor.__init__`
    does NOT raise on missing PostGIS (unlike the sync constructor), and every other async spatial
    method calls `_check_postgis()` explicitly. Place `await self._check_postgis()` before
    `validate_identifiers(...)` in both async methods.

    Do NOT yet replace the flat defs in database.py/async_database.py with stubs (Task 2). Do NOT
    edit the DataFrame call-sites here (Task 4). Do NOT add a new property/cache field — `db.spatial`
    already exists.
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from pycopg.spatial import SpatialAccessor as S, AsyncSpatialAccessor as A; assert all(hasattr(c,'create_spatial_index') and hasattr(c,'list_geometry_columns') for c in (S,A)); assert all(inspect.iscoroutinefunction(getattr(A,m)) for m in ('create_spatial_index','list_geometry_columns')); assert all('await self._check_postgis()' in inspect.getsource(getattr(A,m)) for m in ('create_spatial_index','list_geometry_columns')); print('OK')" && uv run ruff check pycopg/spatial.py && uv run black --check pycopg/spatial.py</automated>
  </verify>
  <acceptance_criteria>
    - Both methods present on both classes: the python verify command prints `OK` and exits 0.
    - Async guard present: `grep -c 'await self._check_postgis()' pycopg/spatial.py` increases by exactly 2 vs the pre-edit count (the 2 newly relocated async methods).
    - Verbatim SQL preserved: `grep -c 'USING GIST' pycopg/spatial.py` >= 1 and `grep -c 'LIST_GEOMETRY_COLUMNS' pycopg/spatial.py` >= 1.
    - D-04 rewrite applied (no bare self.execute leak in the relocated bodies): `grep -nE 'self\.execute\(' pycopg/spatial.py` returns ZERO (all relocated calls use `self._db.execute`).
    - Validators travelled: `grep -c 'validate_identifiers' pycopg/spatial.py` includes the relocated create_spatial_index guard.
    - Not conformed to builder style: the relocated methods call `self._db.execute(...)` directly (NOT `self._run(...)`) — confirm by reading the 2 new sync method bodies.
    - `uv run ruff check pycopg/spatial.py` and `uv run black --check pycopg/spatial.py` both exit 0.
  </acceptance_criteria>
  <done>create_spatial_index and list_geometry_columns exist on both SpatialAccessor and AsyncSpatialAccessor, moved verbatim with self.execute to self._db.execute; the 2 async methods prepend await self._check_postgis(); validators + inline SQL preserved; no builder/_run conformance; ruff + black clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the 2 flat spatial defs with @deprecated_alias("spatial.<m>") stubs on Database AND AsyncDatabase (same commit)</name>
  <files>pycopg/database.py, pycopg/async_database.py</files>
  <read_first>
    - pycopg/aliases.py (the `@deprecated_alias(target_path)` decorator — resolves `self.spatial` lazily, branches sync/async via `iscoroutinefunction`, emits the v0.7.0-removal message at `stacklevel=2`; reuse verbatim, zero changes)
    - pycopg/database.py lines 1671-1718 (the 2 SYNC flat bodies to replace) + lines 1724-1726 (the existing `@deprecated_alias("timescale.create_hypertable")` stub — the exact one-line stub shape: decorator + `def <m>(self, *args, **kwargs):` + one-line docstring)
    - pycopg/async_database.py lines 1246 / 1275 (the 2 ASYNC flat bodies to replace) + an existing async `@deprecated_alias("timescale.<m>")` stub in this file (the async stub is declared `async def` but the decorator auto-branches)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md "pycopg/database.py (db wiring)" — the 2 spatial-relocation stub target-path strings
    - .planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md "Pitfall 5" (sync AND async stubs MUST land in the SAME commit or TestAsyncParity fails mid-transition)
  </read_first>
  <action>
    Replace the relocated flat method bodies with thin deprecated stubs (the real logic now lives
    in `pycopg/spatial.py` from Task 1).

    Sync (`pycopg/database.py`): delete the bodies of `create_spatial_index` (1671) and
    `list_geometry_columns` (1700) and replace each with the stub shape:
    `@deprecated_alias("spatial.create_spatial_index")` + `def create_spatial_index(self, *args,
    **kwargs):` + docstring `"""Deprecated: use ``db.spatial.create_spatial_index`` instead."""`;
    and `@deprecated_alias("spatial.list_geometry_columns")` + `def list_geometry_columns(self,
    *args, **kwargs):` + `"""Deprecated: use ``db.spatial.list_geometry_columns`` instead."""`.
    The 2 target-path strings are exactly `spatial.create_spatial_index` and
    `spatial.list_geometry_columns`.

    Async (`pycopg/async_database.py`): apply the identical replacement to the async
    `create_spatial_index` (1246) and `list_geometry_columns` (1275) — same 2 target-path strings,
    same `(*args, **kwargs)` generic signature, declared `async def` (the decorator branches to the
    async path via `iscoroutinefunction`).

    CRITICAL (Pitfall 5): make BOTH files' edits in the SAME commit so `TestAsyncParity` (which
    diffs `inspect.getmembers(Database)` vs `AsyncDatabase`) never sees a half-migrated surface.

    Do NOT touch the `from_dataframe`/`from_geodataframe` call-sites here (Task 4 owns them). Do NOT
    alter `aliases.py`. Do NOT touch `test_parity.py` (Task 3).
  </action>
  <verify>
    <automated>uv run python -c "
import warnings
from pycopg import Database
from pycopg.spatial import SpatialAccessor
from pycopg.config import Config
from unittest.mock import MagicMock
db = Database(Config(host='h', dbname='d', user='u', password='p'))
db._spatial = MagicMock(spec=SpatialAccessor)
with warnings.catch_warnings(record=True) as w:
    warnings.simplefilter('always'); db.create_spatial_index('t', 'geom')
assert any(x.category is DeprecationWarning and 'db.spatial.create_spatial_index' in str(x.message) for x in w), [str(x.message) for x in w]
db._spatial.create_spatial_index.assert_called_once_with('t', 'geom')
print('sync spatial stub OK')
" -W ignore && test \$(grep -c '@deprecated_alias(\"spatial\.' pycopg/database.py) -eq 2 && test \$(grep -c '@deprecated_alias(\"spatial\.' pycopg/async_database.py) -eq 2 && uv run ruff check pycopg/database.py pycopg/async_database.py</automated>
  </verify>
  <acceptance_criteria>
    - 2 sync spatial stubs: `grep -c '@deprecated_alias("spatial\.' pycopg/database.py` == 2.
    - 2 async spatial stubs: `grep -c '@deprecated_alias("spatial\.' pycopg/async_database.py` == 2.
    - Exact target paths present: `grep -c '@deprecated_alias("spatial.create_spatial_index")' pycopg/database.py` == 1 and `grep -c '@deprecated_alias("spatial.list_geometry_columns")' pycopg/database.py` == 1 (same 2 in async_database.py).
    - Flat bodies gone: `grep -nE 'USING GIST' pycopg/database.py` returns ZERO and `grep -nE 'USING GIST' pycopg/async_database.py` returns ZERO (the inline SQL now lives only in spatial.py).
    - Sync stub warns + delegates (DB-free MagicMock on db._spatial): a `db.create_spatial_index('t')` call under `warnings.catch_warnings(record=True)` emits exactly one DeprecationWarning whose message contains `db.spatial.create_spatial_index` and `v0.7.0`, and `db._spatial.create_spatial_index` is called once with `('t',)`. (This is proven exhaustively by Task 5's alias test; confirm it manually here.)
    - `uv run ruff check pycopg/database.py pycopg/async_database.py` exits 0.
  </acceptance_criteria>
  <done>Both Database and AsyncDatabase have create_spatial_index/list_geometry_columns as @deprecated_alias("spatial.<m>") stubs (sync+async same commit); the inline GIST SQL is deleted from both database files; a sync stub provably warns naming db.spatial.<m> (v0.7.0) and delegates to db._spatial; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 3: Append (SpatialAccessor, AsyncSpatialAccessor) to ACCESSOR_PAIRS in test_parity.py</name>
  <files>tests/test_parity.py</files>
  <read_first>
    - tests/test_parity.py lines 11-28 (the accessor imports + the ACCESSOR_PAIRS list — RESEARCH CORRECTION #1: it currently has ONLY 5 pairs: Timescale, ETL, Admin, Maint, Backup; `(SpatialAccessor, AsyncSpatialAccessor)` is ABSENT despite the CONTEXT.md claim — the comment at line 19 reads "Phases 22-24 APPEND here")
    - .planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md "tests/test_parity.py" section (the import + tuple-append pattern; note the schema pair is added by plan 04, NOT here)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-RESEARCH.md "Claim 5: SpatialAccessor in ACCESSOR_PAIRS [FAIL]" + "Open Question 1" (the planner decision: add it in this spatial-relocation plan — low-cost, classes already exist)
  </read_first>
  <action>
    Add `from pycopg.spatial import AsyncSpatialAccessor, SpatialAccessor` to the accessor-import
    block in `tests/test_parity.py` (alphabetical/grouped with the existing accessor imports).
    Append exactly one tuple `(SpatialAccessor, AsyncSpatialAccessor)` to the `ACCESSOR_PAIRS` list.
    Do NOT add the schema pair here — plan 04 owns `(SchemaAccessor, AsyncSchemaAccessor)`. (This
    edit and plan 04's edit both touch ACCESSOR_PAIRS; plan 04 depends_on this plan so the two
    appends serialize and do not conflict.) The relocated 2 spatial methods (Task 1) MUST already
    be present on both classes for the parity test to pass — Task 1 guarantees this.
  </action>
  <verify>
    <automated>grep -c '(SpatialAccessor, AsyncSpatialAccessor)' tests/test_parity.py && uv run pytest tests/test_parity.py -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - Import present: `grep -c 'from pycopg.spatial import' tests/test_parity.py` == 1.
    - Tuple appended: `grep -c '(SpatialAccessor, AsyncSpatialAccessor)' tests/test_parity.py` == 1.
    - ACCESSOR_PAIRS now has 6 entries (was 5): the spatial pair appears in the list.
    - Parity green for the spatial pair: `uv run pytest tests/test_parity.py -q -o addopts=""` exits 0 (test_accessor_parity passes for `(SpatialAccessor, AsyncSpatialAccessor)`, proving the 2 relocated methods exist at sync to async parity).
  </acceptance_criteria>
  <done>(SpatialAccessor, AsyncSpatialAccessor) is imported and appended to ACCESSOR_PAIRS; test_accessor_parity passes for the spatial pair with the 2 newly relocated methods at parity.</done>
</task>

<task type="auto">
  <name>Task 4: Atomically rewrite the 8 D-05 stay-flat call-sites in from_dataframe/from_geodataframe (THE top correctness risk — the -W error gate plan 02 deferred)</name>
  <files>pycopg/database.py, pycopg/async_database.py</files>
  <read_first>
    - pycopg/database.py lines 1502-1503 (sync from_dataframe: `self.add_primary_key(table, primary_key, schema)`) and lines 1582-1622 (sync from_geodataframe: :1583 `if not self.has_extension("postgis"):`, :1619 `self.add_primary_key(...)`, :1622 `self.create_spatial_index(table, geometry_column, schema)`)
    - pycopg/async_database.py lines 1479 (async from_dataframe `await self.add_primary_key(...)`) and 1568/1607/1610 (async from_geodataframe: `await self.has_extension("postgis")`, `await self.add_primary_key(...)`, `await self.create_spatial_index(...)`)
    - pycopg/schema.py (confirms `add_primary_key` and `has_extension` live on SchemaAccessor — db.schema.* targets exist from plans 01/02)
    - pycopg/spatial.py (confirms `create_spatial_index` now lives on SpatialAccessor — db.spatial.* target exists from Task 1)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-CONTEXT.md "D-05" (the exhaustive 8-site enumeration) + 23-RESEARCH.md "Claim 3" + "Pitfall 1"
    - .planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md "Stay-flat call-site rewrites (D-05)" (the exact 8 rewrites for sync + async)
  </read_first>
  <action>
    Rewrite all 8 internal call-sites (which stay-flat methods make to methods moved this phase) to
    the accessor paths. These methods (`from_dataframe`/`from_geodataframe`) stay flat on `db.*` by
    design (DataFrame surface, PROJECT.md), but their INTERNAL calls now hit deprecated aliases —
    under `-W error::DeprecationWarning` that is a hard failure (Pitfall 1).

    Sync (`pycopg/database.py`):
    - :1503 `self.add_primary_key(table, primary_key, schema)` to `self.schema.add_primary_key(table, primary_key, schema)`
    - :1583 `if not self.has_extension("postgis"):` to `if not self.schema.has_extension("postgis"):`
    - :1619 `self.add_primary_key(table, primary_key, schema)` to `self.schema.add_primary_key(table, primary_key, schema)`
    - :1622 `self.create_spatial_index(table, geometry_column, schema)` to `self.spatial.create_spatial_index(table, geometry_column, schema)`

    Async (`pycopg/async_database.py`) — the exact same 4 rewrites, keeping `await`:
    - :1479 `await self.add_primary_key(...)` to `await self.schema.add_primary_key(...)`
    - :1568 `if not await self.has_extension("postgis"):` to `if not await self.schema.has_extension("postgis"):`
    - :1607 `await self.add_primary_key(...)` to `await self.schema.add_primary_key(...)`
    - :1610 `await self.create_spatial_index(...)` to `await self.spatial.create_spatial_index(...)`

    Note: `add_primary_key`/`has_extension` route to `self.schema.*` (the schema accessor), and
    `create_spatial_index` routes to `self.spatial.*` (the spatial accessor) — do NOT cross them.
    `from_geodataframe` already guards `has_extension("postgis")` itself before calling
    `create_spatial_index`, so routing the latter through the PostGIS-guarded `db.spatial` accessor
    is safe (D-06). Make all 8 rewrites in the SAME commit. This is the gate plan 02 deferred.
  </action>
  <verify>
    <automated>uv run pytest -W error::DeprecationWarning tests/test_database.py -k "from_dataframe or from_geodataframe" -q -o addopts="" ; uv run pytest -W error::DeprecationWarning tests/test_async_database.py -k "from_dataframe or from_geodataframe" -q -o addopts="" ; uv run ruff check pycopg/database.py pycopg/async_database.py</automated>
  </verify>
  <acceptance_criteria>
    - No flat self-call to a moved name remains in the DataFrame methods: `grep -nE 'self\.(add_primary_key|has_extension|create_spatial_index)\(' pycopg/database.py` returns ZERO inside from_dataframe/from_geodataframe (only `self.schema.*` / `self.spatial.*` remain); same for async (`await self.add_primary_key(` etc. ZERO).
    - Accessor paths present: `grep -c 'self.schema.add_primary_key' pycopg/database.py` >= 2, `grep -c 'self.schema.has_extension' pycopg/database.py` >= 1, `grep -c 'self.spatial.create_spatial_index' pycopg/database.py` >= 1 (and the async mirrors with `await`).
    - The -W error gate plan 02 deferred now passes for the DataFrame paths: both targeted `uv run pytest -W error::DeprecationWarning ... -k "from_dataframe or from_geodataframe" -q -o addopts=""` commands exit 0 (ZERO internal DeprecationWarning raised as error). (If the env lacks PostGIS, the from_geodataframe tests skip — that still satisfies the no-internal-warning invariant; confirm at least the from_dataframe path runs and emits zero warnings.)
    - `uv run ruff check pycopg/database.py pycopg/async_database.py` exits 0.
  </acceptance_criteria>
  <done>All 8 D-05 call-sites in from_dataframe/from_geodataframe (4 sync + 4 async) are rewritten to self.schema.* / self.spatial.* accessor paths; no flat self-call to a moved name remains in those methods; the targeted -W error::DeprecationWarning gate passes (zero internal deprecation warnings); ruff clean.</done>
</task>

<task type="auto">
  <name>Task 5: Add tests/test_spatial_aliases.py (2 sync + 2 async DB-free MagicMock alias tests)</name>
  <files>tests/test_spatial_aliases.py</files>
  <read_first>
    - tests/test_timescale_aliases.py (full file — the DB-free MagicMock alias-test template: `_SYNC_ALIAS_ARGS` dict, `warnings.catch_warnings(record=True)` + `warnings.simplefilter("always")`, filter by `rec.category is DeprecationWarning and f"db.spatial.{name}" in str(rec.message)`, assert `"v0.7.0"` in message, assert `"test_" in os.path.basename(rec.filename)` for stacklevel=2, and `getattr(mock_accessor, name).assert_called_once_with(*args)`; async wraps each mock method with `AsyncMock()`)
    - tests/test_admin_aliases.py (the Phase 22 sibling — same template, confirms conventions are uniform)
    - tests/test_spatial.py lines 1-25 (the existing spatial test module is DB-free builder/guard + PostGIS integration — a separate concern; PATTERNS.md decided the alias tests get their own file `tests/test_spatial_aliases.py` so they do not clutter it)
    - .planning/phases/23-schema-accessor-spatial-relocation/23-PATTERNS.md "tests/test_spatial_aliases.py (new test)" section (the full structure, including injecting the mock into `db._spatial` to bypass the PostGIS guard)
  </read_first>
  <action>
    Create `tests/test_spatial_aliases.py` mirroring `tests/test_timescale_aliases.py` for the 2
    relocated spatial methods. Define `_SYNC_ALIAS_ARGS = {"create_spatial_index": ("my_table",),
    "list_geometry_columns": ()}` and `_ASYNC_ALIAS_ARGS = _SYNC_ALIAS_ARGS.copy()`. Class
    `TestSpatialAliases` with a parametrized sync test and a parametrized async test:

    Sync `test_sync_alias_warns_and_delegates(self, name, config)`: build `db = Database(config)`,
    inject `db._spatial = MagicMock(spec=SpatialAccessor)` (injecting into the cache field bypasses
    the PostGIS constructor guard — DB-free). Under `warnings.catch_warnings(record=True)` +
    `warnings.simplefilter("always")`, call `getattr(db, name)(*_SYNC_ALIAS_ARGS[name])`. Filter
    warnings to `rec.category is DeprecationWarning and f"db.spatial.{name}" in str(rec.message)`;
    assert exactly one; assert `"v0.7.0"` in the message; assert `"test_" in
    os.path.basename(rec.filename)` and the basename is neither `aliases.py` nor `database.py`
    (stacklevel=2 proof); assert `getattr(db._spatial, name).assert_called_once_with(*args)`.

    Async `test_async_alias_warns_and_delegates(self, name, config)` (`@pytest.mark.asyncio`):
    build `db = AsyncDatabase(config)`, `mock = MagicMock(spec=AsyncSpatialAccessor)`, set each
    method on the mock to `AsyncMock()`, inject `db._spatial = mock` (bypasses the deferred guard),
    `await getattr(db, name)(*args)`, same warning + stacklevel + delegation assertions.

    Import `Database, AsyncDatabase` from `pycopg` and `SpatialAccessor, AsyncSpatialAccessor` from
    `pycopg.spatial`. Use the shared `config` fixture (same as test_timescale_aliases.py / the
    conftest). These 2x2 tests are what keep the 2 spatial thin stubs covered for the
    `--cov-fail-under=94` ratchet.
  </action>
  <verify>
    <automated>uv run pytest tests/test_spatial_aliases.py -q -o addopts="" && uv run ruff check tests/test_spatial_aliases.py</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the class: `grep -c 'class TestSpatialAliases' tests/test_spatial_aliases.py` == 1.
    - Covers both methods both paths: `uv run pytest tests/test_spatial_aliases.py -q -o addopts=""` collects and passes >= 4 tests (2 sync + 2 async).
    - Warning + delegation asserted: the file contains `db.spatial.` in the message-filter assertion, `v0.7.0` assertion, the `os.path.basename` stacklevel assertion, and `assert_called_once_with`.
    - Mock injected into the cache field (DB-free): `grep -c 'db._spatial' tests/test_spatial_aliases.py` >= 2 (sync + async inject).
    - `uv run ruff check tests/test_spatial_aliases.py` exits 0.
  </acceptance_criteria>
  <done>tests/test_spatial_aliases.py exists with TestSpatialAliases covering create_spatial_index + list_geometry_columns sync and async (>=4 DB-free tests); each asserts exactly one DeprecationWarning naming db.spatial.<m> + v0.7.0, caller stacklevel, and delegation to db._spatial; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller to create_spatial_index args | `table`/`column`/`schema`/`name` identifiers cross here; guarded by `validate_identifiers` / `validate_identifier` before interpolation into the inline `CREATE INDEX ... USING GIST` f-string |
| SpatialAccessor to PostgreSQL | DDL/introspection SQL executed via `self._db.execute` |
| caller to flat db.<m> spatial alias | the deprecated stub forwards `*args, **kwargs` to `self.spatial.<m>`; no validation in the stub (it lives in the moved body) |

## STRIDE Threat Register

This plan MOVES 2 existing method bodies into an existing accessor + rewrites internal
call-sites. It adds NO new SQL, NO new user-input path. The moved bodies keep their
`validate_*` guards verbatim. The only behavior change is the D-06 accepted earlier-`ExtensionNotAvailable`
on the deprecated path (the alias now constructs the PostGIS-guarded `db.spatial`).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Tampering (SQL injection) | the verbatim move of create_spatial_index / list_geometry_columns | mitigate | The verbatim-move invariant preserves `validate_identifiers(table, column, schema)` + `validate_identifier(name)`. Acceptance criteria assert the validators are present in spatial.py. Re-run `tests/test_sql_injection.py` (uses create_spatial_index at lines 72/77/168) after the move to prove the guards still fire on the relocated body. |
| T-23-05 | Denial of Service / behavior change | D-06 PostGIS guard on the deprecated path | accept | Relocating into SpatialAccessor means the deprecated flat alias now raises `ExtensionNotAvailable` early (via `db.spatial` construction / async `_check_postgis`) instead of a raw psycopg error — a strictly clearer, thematically-correct failure. No guard bypass added (D-06). Re-run `tests/test_postgis_errors.py` (create_spatial_index, list_geometry_columns) to confirm the guard semantics. CHANGELOG/MIGRATION note deferred to Phase 24. |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pytest/pytest-asyncio/interrogate already present (Phase 21/22 infra). No new dependency surface. |

No new threats introduced; the verbatim-move invariant preserves the existing `validate_*`
guards. Block on `high` — none present.
</threat_model>

<verification>
- `db.spatial.create_spatial_index` / `db.spatial.list_geometry_columns` callable on both
  SpatialAccessor and AsyncSpatialAccessor; the 2 async methods await `self._check_postgis()` first.
- Old flat `db.create_spatial_index(...)` warns naming `db.spatial.create_spatial_index` (incl. "v0.7.0")
  and delegates (DB-free MagicMock proof in test_spatial_aliases.py).
- `(SpatialAccessor, AsyncSpatialAccessor)` is in `ACCESSOR_PAIRS`; `test_accessor_parity` passes for it.
- The 8 D-05 call-sites are rewritten; `uv run pytest -W error::DeprecationWarning` passes for the
  from_dataframe/from_geodataframe paths (the gate plan 02 deferred).
- Security regression: `tests/test_sql_injection.py` + `tests/test_postgis_errors.py` still pass against
  the relocated bodies (guards travelled; D-06 guard semantics confirmed).
- `uv run ruff check pycopg tests` and `uv run black --check pycopg/spatial.py` clean.
</verification>

<success_criteria>
- The 2 spatial-index methods are relocated into `SpatialAccessor`/`AsyncSpatialAccessor` verbatim
  (D-07; `self.execute` to `self._db.execute`); the async pair prepends `await self._check_postgis()`.
- 2 `@deprecated_alias("spatial.<m>")` stubs replace the flat bodies on `Database`/`AsyncDatabase`
  (sync + async, same commit — TestAsyncParity safe); a flat alias provably warns toward `db.spatial.*`.
- `(SpatialAccessor, AsyncSpatialAccessor)` appended to `ACCESSOR_PAIRS`; parity green for it.
- All 8 D-05 call-sites rewritten; the targeted `-W error::DeprecationWarning` gate passes.
- `tests/test_spatial_aliases.py` covers both methods sync + async (>=4 DB-free tests).
</success_criteria>

<output>
Create `.planning/phases/23-schema-accessor-spatial-relocation/23-03-SUMMARY.md` when done.
</output>
