---
phase: 25-alias-removal
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - MIGRATION.md
  - CHANGELOG.md
  - docs/index.md
  - docs/getting-started.md
  - docs/postgis.md
  - docs/async-database.md
  - docs/roles-permissions.md
  - docs/backup-restore.md
  - docs/timescaledb.md
  - docs/database.md
autonomous: true
requirements: [ALIAS-RM-03, ALIAS-RM-04]
must_haves:
  truths:
    - "MIGRATION.md has a v0.6.0 → v0.7.0 section with a 1:1 flat→accessor replacement table covering all 56 names, framed as removed (now raises AttributeError)"
    - "CHANGELOG.md has a [0.7.0] section with a Breaking entry pointing to the MIGRATION v0.6→v0.7 section"
    - "The docs/*.md flat-name code examples are corrected to accessor paths and deprecation notes updated to 'removed in v0.7.0'"
  artifacts:
    - path: "MIGRATION.md"
      provides: "v0.6→v0.7 migration section with 56-name removal table"
      contains: "v0.6.0 → v0.7.0"
    - path: "CHANGELOG.md"
      provides: "[0.7.0] Breaking entry"
      contains: "[0.7.0]"
  key_links:
    - from: "CHANGELOG.md"
      to: "MIGRATION.md"
      via: "Breaking entry links to the v0.6→v0.7 migration section anchor"
      pattern: "MIGRATION\\.md"
---

<objective>
Document the removal: add a `Migration Guide: v0.6.0 → v0.7.0` section to `MIGRATION.md` with a
1:1 flat→accessor replacement table covering all 56 names (reframed as "removed in v0.7.0 — now
raises AttributeError"), add a CHANGELOG `[0.7.0]` section with a `### Breaking` entry pointing to
it, and correct the stale flat-name code examples + deprecation notes in the docs source files
(the docs slice of IN-02).

Purpose: Deliver ALIAS-RM-03 (D-09 MIGRATION + D-10 CHANGELOG) and the docs portion of
ALIAS-RM-04 / IN-02 (D-08 docs examples). No version bump here — that is Phase 29.

Output: MIGRATION v0.6→v0.7 section, CHANGELOG `[0.7.0]` Breaking entry, and corrected docs
examples. Sphinx `-W` stays clean (the examples are in fenced code blocks, not cross-refs).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-alias-removal/25-CONTEXT.md
@.planning/phases/25-alias-removal/25-RESEARCH.md
@.planning/phases/25-alias-removal/25-PATTERNS.md
</context>

<artifacts_this_phase_produces>
CREATES (documentation anchors):
- `MIGRATION.md` section `## Migration Guide: v0.6.0 → v0.7.0` with the 56-row flat→accessor
  removal table.
- `CHANGELOG.md` section `## [0.7.0] - TBD` with a `### Breaking` subsection linking to the
  MIGRATION v0.6→v0.7 anchor.

MODIFIES (IN-02 docs examples):
- Flat-name code examples in `docs/index.md`, `docs/getting-started.md`, `docs/postgis.md`,
  `docs/async-database.md` → accessor paths.
- Deprecation notes in `docs/roles-permissions.md`, `docs/backup-restore.md`,
  `docs/timescaledb.md`, `docs/database.md` → "removed in v0.7.0".

Downstream drift-verification: this plan adds doc anchors and edits markdown only; no code symbols
change. The `[0.7.0]` date stays `TBD` (Phase 29 sets the version — no bump here).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add MIGRATION v0.6→v0.7 section + CHANGELOG [0.7.0] Breaking entry</name>
  <files>MIGRATION.md, CHANGELOG.md</files>
  <read_first>
    - MIGRATION.md — the existing `## Complete Flat-Name → Accessor-Path Table` at L81–L153 is the
      56-name source of truth (timescale 6 / admin 11 / backup 4 / maint 6 / schema 27 / spatial 2).
      Reuse it verbatim, reframed for removal. The existing top heading is
      `# Migration Guide: v0.5.0 → v0.6.0`.
    - CHANGELOG.md — `## [Unreleased]` at L8 (currently empty), `## [0.6.0] - 2026-06-19` at L10.
      The `[0.7.0]` entry goes ABOVE `[0.6.0]` (replacing/following `[Unreleased]`).
    - 25-CONTEXT.md D-09 (MIGRATION 56-name table, reuse existing) + D-10 (CHANGELOG `[0.7.0]`
      Breaking pointing to MIGRATION; no code version bump — Phase 29).
    - 25-PATTERNS.md "MIGRATION.md (add v0.6→v0.7 section)" + "CHANGELOG.md (add [0.7.0] Breaking
      entry)" — the exact section/anchor structure and the `[0.7.0] - TBD` / `### Breaking` shape.
  </read_first>
  <action>
    In `MIGRATION.md`, add a new `## Migration Guide: v0.6.0 → v0.7.0` section. Reuse the existing
    56-name flat→accessor table verbatim from the `## Complete Flat-Name → Accessor-Path Table`
    (L81–L153), reframed: state that these 56 flat names are REMOVED in v0.7.0 and now raise
    `AttributeError` (no warning, no delegation), and that the accessor path is the replacement.
    Keep the "Total: 56 deprecated names (timescale 6 / admin 11 / backup 4 / maint 6 / schema 27
    / spatial 2)" breakdown. In `CHANGELOG.md`, add a `## [0.7.0] - TBD` section ABOVE `[0.6.0]`
    with a `### Breaking` subsection stating all 56 flat aliases on `Database`/`AsyncDatabase`
    (deprecated in v0.6.0) are permanently removed and now raise `AttributeError`, with a link to
    the MIGRATION v0.6→v0.7 section anchor (`MIGRATION.md#migration-guide-v060--v070`). Leave the
    date as `TBD` — no version bump in this phase (Phase 29). No `### Added` subsection (Phase 25
    adds no features).
  </action>
  <acceptance_criteria>
    - `grep -c "Migration Guide: v0.6.0 → v0.7.0\|v0.6.0 → v0.7.0" MIGRATION.md` → `≥1`.
    - The v0.6→v0.7 table contains 56 flat→accessor rows: under the new section, the count of table rows mapping a flat name to an accessor path is 56 (verify by counting `db.*` → `db.*.*` rows in that section).
    - `grep -c "## \[0.7.0\]" CHANGELOG.md` → `1`.
    - `grep -A6 "## \[0.7.0\]" CHANGELOG.md | grep -ci "Breaking"` → `≥1` AND that block references `MIGRATION.md`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test "$(grep -c "v0.6.0 → v0.7.0" MIGRATION.md)" -ge "1" && test "$(grep -c "## \[0.7.0\]" CHANGELOG.md)" = "1" && grep -A8 "## \[0.7.0\]" CHANGELOG.md | grep -qi "breaking" && grep -A8 "## \[0.7.0\]" CHANGELOG.md | grep -q "MIGRATION.md"'</automated>
  </verify>
  <done>MIGRATION.md has a v0.6→v0.7 section with the 56-name removal table; CHANGELOG `[0.7.0]` has a Breaking entry linking to MIGRATION; no version bump.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale flat-name code examples + deprecation notes in docs/*.md (IN-02)</name>
  <files>docs/index.md, docs/getting-started.md, docs/postgis.md, docs/async-database.md, docs/roles-permissions.md, docs/backup-restore.md, docs/timescaledb.md, docs/database.md</files>
  <read_first>
    - 25-RESEARCH.md §5 "Stale docs code examples" — the exact per-file/per-line table of flat-name
      examples to fix (16 code-example sites across docs/index.md L53, docs/getting-started.md L88,
      docs/postgis.md L18/112/115/308, docs/async-database.md L263/266/283/338/363/384/390/404/418/465)
      and the 4 deprecation-note files to reword (docs/roles-permissions.md:26, docs/backup-restore.md:26,
      docs/timescaledb.md:34, docs/database.md:187). NOTE: these are fenced code blocks, NOT `:meth:`
      cross-refs — they do NOT break Sphinx `-W`, but are factually wrong post-v0.7.0.
    - 25-RESEARCH.md §7 "Sphinx -W Gate Analysis" — confirms `sphinx-build -W --keep-going` passes
      regardless; do NOT edit `docs/_build/` (generated). Each accessor mapping (e.g.
      `db.list_tables` → `db.schema.list_tables`, `db.vacuum` → `db.maint.vacuum`,
      `db.pg_dump` → `db.backup.pg_dump`, `db.create_role` → `db.admin.create_role`,
      `db.create_hypertable` → `db.timescale.create_hypertable`) is given in the §5 table.
  </read_first>
  <action>
    Update each stale flat-name code example listed in 25-RESEARCH.md §5 "Stale docs code examples"
    to its accessor path: `db.list_tables` → `db.schema.list_tables`,
    `db.create_extension` → `db.schema.create_extension`, `db.create_index` → `db.schema.create_index`,
    `db.create_schema` → `db.schema.create_schema`, `db.create_database` → `db.schema.create_database`,
    `db.vacuum` → `db.maint.vacuum`, `db.pg_dump` → `db.backup.pg_dump`,
    `db.copy_to_csv` → `db.backup.copy_to_csv`, `db.create_role` → `db.admin.create_role`,
    `db.create_hypertable` → `db.timescale.create_hypertable` (preserve the `await` prefix where
    present in async examples). Then update the 4 deprecation-note files
    (`docs/roles-permissions.md`, `docs/backup-restore.md`, `docs/timescaledb.md`, `docs/database.md`)
    from "deprecated as of v0.6.0, will be removed in v0.7.0" wording to "removed in v0.7.0". Edit
    ONLY the `docs/*.md` source files — do NOT touch `docs/_build/` (generated HTML). Verify no flat
    `db.create_extension(` / `db.list_tables(` / `db.create_hypertable(` examples remain in the
    edited source files.
  </action>
  <acceptance_criteria>
    - `grep -rn "db\.create_extension(" docs/*.md` → `0` results (all converted to `db.schema.create_extension(`).
    - `grep -rn "db\.create_hypertable(" docs/*.md | grep -v "timescale\.create_hypertable"` → `0` results.
    - `grep -rn "will be removed in v0.7.0\|deprecated as of v0.6.0" docs/roles-permissions.md docs/backup-restore.md docs/timescaledb.md docs/database.md` → `0` results (notes updated to "removed in v0.7.0").
    - No edits to any file under `docs/_build/`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test "$(grep -rn "db\.create_extension(" docs/*.md | wc -l)" = "0" && test "$(grep -rn "will be removed in v0.7.0" docs/roles-permissions.md docs/backup-restore.md docs/timescaledb.md docs/database.md | wc -l)" = "0"'</automated>
  </verify>
  <done>All stale flat-name code examples in docs/*.md use accessor paths; the 4 deprecation notes say "removed in v0.7.0"; `docs/_build/` untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → reader | Markdown docs are static; no executable trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Spoofing | Documentation showing removed flat-name API as valid | mitigate | Correcting the stale examples to accessor paths prevents callers from copying API calls that now raise AttributeError; the MIGRATION/CHANGELOG entries explicitly state the removal. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero new dependencies, D-11). |
</threat_model>

<verification>
- `grep -c "v0.6.0 → v0.7.0" MIGRATION.md` → ≥1; 56-row removal table present.
- `grep -c "## \[0.7.0\]" CHANGELOG.md` → 1; `### Breaking` present, links MIGRATION.md.
- `grep -rn "db\.create_extension(" docs/*.md` → 0.
- 4 deprecation notes reworded to "removed in v0.7.0".
- No `docs/_build/` edits.
</verification>

<success_criteria>
- MIGRATION v0.6→v0.7 section with the full 56-name removal table.
- CHANGELOG `[0.7.0]` Breaking entry linking to MIGRATION (date TBD, no version bump).
- All docs/*.md flat-name examples + deprecation notes corrected.
</success_criteria>

<output>
Create `.planning/phases/25-alias-removal/25-05-SUMMARY.md` when done.
</output>
