---
phase: 25-alias-removal
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/spatial.py
  - pycopg/timescale.py
  - tests/test_sql_injection.py
autonomous: true
requirements: [ALIAS-RM-04]
must_haves:
  truths:
    - "The PostGIS guard message in spatial.py references db.schema.create_extension"
    - "All 12 TimescaleDB guard messages in timescale.py reference db.schema.create_extension"
    - "No flat db.create_extension( string remains in spatial.py or timescale.py"
    - "The stale alias-routing comment in test_sql_injection.py is corrected (test bodies unchanged, suite stays green)"
  artifacts:
    - path: "pycopg/timescale.py"
      provides: "12 TimescaleDB extension guards naming the accessor path"
      contains: "db.schema.create_extension('timescaledb')"
    - path: "pycopg/spatial.py"
      provides: "PostGIS guard message naming the accessor path"
      contains: "db.schema.create_extension('postgis')"
  key_links:
    - from: "pycopg/timescale.py"
      to: "pycopg.schema accessor"
      via: "error message references db.schema.create_extension"
      pattern: "db\\.schema\\.create_extension"
---

<objective>
Close the remaining IN-02 source slices: fix the PostGIS guard error string in
`pycopg/spatial.py` (1 site, the `_POSTGIS_GUARD_MSG` constant) and the 12 TimescaleDB guard
error strings in `pycopg/timescale.py`, changing `Run db.create_extension(...)` →
`Run db.schema.create_extension(...)`. Also correct the stale alias-routing comment in
`tests/test_sql_injection.py` (the test bodies already call accessor paths; only the comment is
wrong).

Purpose: Complete ALIAS-RM-04 / IN-02 (D-08) for the accessor-implementation files. Combined with
the database.py / async_database.py PostGIS fixes in plans 25-01/25-02, this brings the source
tree to zero flat `db.create_extension(` references in any guard/error string.

Output: `spatial.py` and `timescale.py` error strings name accessor paths; `test_sql_injection.py`
comment is accurate; the full suite stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-alias-removal/25-CONTEXT.md
@.planning/phases/25-alias-removal/25-RESEARCH.md
@.planning/phases/25-alias-removal/25-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates NO new symbols. It MODIFIES string literals only:
- `pycopg/spatial.py`: 1 PostGIS guard string (`_POSTGIS_GUARD_MSG` at ~L966).
- `pycopg/timescale.py`: 12 TimescaleDB guard strings (~L80, 124, 177, 214, 240, 268, 332, 376,
  431, 468, 494, 522).
- `tests/test_sql_injection.py`: the stale alias-routing comment (~L38).

Downstream drift-verification: no symbols are added or removed; only error-message and comment
text changes. `tests/test_sql_injection.py` test bodies are UNCHANGED (they already use accessor
paths). The accessor method behavior is unchanged — guards still fire, only the human-readable
remediation hint changes.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Fix the PostGIS guard (spatial.py) + 12 TimescaleDB guards (timescale.py) error strings (IN-02)</name>
  <files>pycopg/spatial.py, pycopg/timescale.py</files>
  <read_first>
    - pycopg/spatial.py — `_POSTGIS_GUARD_MSG` at L965–967 holds
      `"PostGIS extension not installed. Run db.create_extension('postgis')"` (consumed at L1049
      and L1939). Fix the constant; both call sites inherit it.
    - pycopg/timescale.py — 12 `ExtensionNotAvailable(...)` guard strings each containing
      `Run db.create_extension('timescaledb')` at ~L80, 124, 177, 214, 240, 268, 332, 376, 431,
      468, 494, 522.
    - 25-RESEARCH.md §5 "IN-02 Sweep" — the full 15-site table (3 PostGIS + 12 TimescaleDB); the
      spatial.py + timescale.py rows are this task's scope (database.py/async_database.py are
      handled in plans 25-01/25-02). Substitution is uniform:
      `Run db.create_extension` → `Run db.schema.create_extension`.
    - 25-PATTERNS.md "IN-02 PostGIS" + "IN-02 TimescaleDB" — corrected-form excerpts; a single
      `sed -i "s/Run db\.create_extension/Run db.schema.create_extension/g" pycopg/spatial.py pycopg/timescale.py`
      covers all 13 sites in these two files.
  </read_first>
  <action>
    Apply the uniform substitution `Run db.create_extension` → `Run db.schema.create_extension`
    to every guard/error string in `pycopg/spatial.py` (the `_POSTGIS_GUARD_MSG` constant) and
    `pycopg/timescale.py` (all 12 TimescaleDB guard messages). Use a single
    `sed -i "s/Run db\.create_extension/Run db.schema.create_extension/g" pycopg/spatial.py pycopg/timescale.py`
    pass, then re-grep to confirm zero flat references remain. Do NOT change the accessor method
    logic or signatures — only the string literals inside the guards. Run
    `uv run ruff check pycopg/spatial.py pycopg/timescale.py` and
    `uv run black --check pycopg/spatial.py pycopg/timescale.py`.
  </action>
  <acceptance_criteria>
    - `grep -c "db\.create_extension(" pycopg/spatial.py` → `0`.
    - `grep -c "db\.create_extension(" pycopg/timescale.py` → `0`.
    - `grep -c "db\.schema\.create_extension('postgis')" pycopg/spatial.py` → `1`.
    - `grep -c "db\.schema\.create_extension('timescaledb')" pycopg/timescale.py` → `12`.
    - `uv run ruff check pycopg/spatial.py pycopg/timescale.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test "$(grep -c "db\.create_extension(" pycopg/spatial.py)" = "0" && test "$(grep -c "db\.create_extension(" pycopg/timescale.py)" = "0" && test "$(grep -c "db\.schema\.create_extension(.timescaledb.)" pycopg/timescale.py)" = "12"'</automated>
  </verify>
  <done>Zero flat `db.create_extension(` in `spatial.py`/`timescale.py`; 1 corrected PostGIS string + 12 corrected TimescaleDB strings; ruff/black clean.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the stale alias-routing comment in test_sql_injection.py (IN-02)</name>
  <files>tests/test_sql_injection.py</files>
  <read_first>
    - tests/test_sql_injection.py — the `sync_db` fixture comment at ~L38 reads
      "the deprecated flat spatial aliases now route through the PostGIS-guarded SpatialAccessor
      (D-06)" — stale post-removal. The test bodies already call accessor paths and must NOT
      change (D-06: this file is NOT an alias test file and stays green).
    - 25-RESEARCH.md §5 "Stale test comment (fix, do not delete)" — update the comment to reflect
      that the fixture patches `has_extension` because `SpatialAccessor.__init__` guards PostGIS;
      remove the alias-routing note.
  </read_first>
  <action>
    In `tests/test_sql_injection.py`, replace the stale comment near L38 that references
    "deprecated flat spatial aliases now route through ..." with an accurate description: the
    fixture patches `has_extension` because `SpatialAccessor.__init__` performs a PostGIS guard
    check; remove any mention of flat-alias routing. Edit ONLY the comment text — do NOT modify
    any test body, fixture logic, import, or assertion (D-06). The suite must remain green.
  </action>
  <acceptance_criteria>
    - `grep -c "deprecated flat spatial alias\|flat spatial aliases now route" tests/test_sql_injection.py` → `0`.
    - `uv run pytest tests/test_sql_injection.py -q -o addopts=""` → green (test bodies unchanged).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test "$(grep -c "deprecated flat spatial alias\|flat spatial aliases now route" tests/test_sql_injection.py)" = "0"' && uv run pytest tests/test_sql_injection.py -q -o addopts=""</automated>
  </verify>
  <done>The stale alias-routing comment is gone; `test_sql_injection.py` bodies unchanged and the file's tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accessor guard → caller | `ExtensionNotAvailable` guard messages are returned to the caller; only the remediation hint text changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Information Disclosure | Editing guard/error string literals in `spatial.py`/`timescale.py` | accept | Only the human-readable remediation hint changes (`db.create_extension` → `db.schema.create_extension`); no behavior, no validation, no exception type changes. No new information is exposed. |
| T-25-08 | Tampering | Editing `test_sql_injection.py` (the SQL-injection guard suite) | mitigate | Comment-only edit; acceptance asserts test bodies unchanged via the suite staying green. The SQL-injection guards being verified live in the accessors and are not touched. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero new dependencies, D-11). |
</threat_model>

<verification>
- `grep -rn "db\.create_extension(" pycopg/spatial.py pycopg/timescale.py` → 0 results.
- `grep -c "db\.schema\.create_extension('timescaledb')" pycopg/timescale.py` → 12.
- `grep -c "flat spatial aliases now route" tests/test_sql_injection.py` → 0.
- `uv run pytest tests/test_sql_injection.py -q -o addopts=""` green.
- `uv run ruff check pycopg/spatial.py pycopg/timescale.py` exits 0.
</verification>

<success_criteria>
- Zero flat `db.create_extension(` in `spatial.py`/`timescale.py` guard strings.
- 12 corrected TimescaleDB guards + 1 corrected PostGIS guard.
- `test_sql_injection.py` comment accurate; its tests green; bodies unchanged.
</success_criteria>

<output>
Create `.planning/phases/25-alias-removal/25-04-SUMMARY.md` when done.
</output>
