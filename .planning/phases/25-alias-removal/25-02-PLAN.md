---
phase: 25-alias-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/async_database.py
autonomous: true
requirements: [ALIAS-RM-01, ALIAS-RM-04]
must_haves:
  truths:
    - "Calling any removed flat name on a live AsyncDatabase instance raises a plain AttributeError (no warning, no delegation)"
    - "The public AsyncDatabase surface exposes only accessor-namespaced methods with real signatures (no *args/**kwargs stub)"
    - "The interleaved real methods (DATAFRAME, BATCH, STREAMING, LISTEN/NOTIFY) still resolve on AsyncDatabase"
    - "PostGIS guard error messages on AsyncDatabase reference db.schema.create_extension, not db.create_extension"
  artifacts:
    - path: "pycopg/async_database.py"
      provides: "AsyncDatabase class with 0 @deprecated_alias stubs, 0 *args/**kwargs, import line removed, real methods + lifecycle preserved"
      contains: "async def to_dataframe"
  key_links:
    - from: "pycopg/async_database.py"
      to: "pycopg.schema accessor"
      via: "error message references db.schema.create_extension"
      pattern: "db\\.schema\\.create_extension"
---

<objective>
Remove all 56 `@deprecated_alias` flat stubs from `pycopg/async_database.py`, delete the
`from pycopg.aliases import deprecated_alias` import line, delete the now-empty section-header
comments, and fix the 1 stale PostGIS-guard error string in this file (IN-02). The MORE
EXTENSIVE interleaving here (DATAFRAME, BATCH, STREAMING, LISTEN/NOTIFY real-method sections)
plus the trailing `close`/`__aenter__`/`__aexit__` lifecycle methods MUST be preserved.

Purpose: Hard-remove the v0.6.0 deprecated flat API surface from the async class (D-01, D-02),
closing WR-01 (D-07) and the async_database.py slice of IN-02 (D-08).

Output: A `pycopg/async_database.py` whose public surface is accessor-only, where every removed
flat name resolves to `AttributeError`, and whose error strings name accessor paths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-alias-removal/25-CONTEXT.md
@.planning/phases/25-alias-removal/25-RESEARCH.md
@.planning/phases/25-alias-removal/25-PATTERNS.md
@.planning/phases/25-alias-removal/25-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan does NOT create new symbols — it is a deletion slice. It REMOVES:
- 56 `@deprecated_alias`-decorated flat stub methods from `AsyncDatabase` (same 56 names as
  the sync class).
- The `from pycopg.aliases import deprecated_alias` import line at `pycopg/async_database.py:28`.
- The orphaned section-header comments grouping the alias stubs.

Downstream drift-verification: these 56 async flat names are INTENTIONALLY GONE. Do not flag
them as missing — `tests/test_alias_removal.py` (plan 25-03) asserts they raise `AttributeError`.
PRESERVED real-method sections: `# DATAFRAME OPERATIONS` (to_dataframe, from_dataframe,
to_geodataframe, from_geodataframe), `# BATCH OPERATIONS` (insert_many, upsert_many),
`# STREAMING` (stream), `# LISTEN/NOTIFY` (listen, notify), and lifecycle (close, __aenter__,
__aexit__).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 56 @deprecated_alias stubs + import line + orphaned headers in async_database.py</name>
  <files>pycopg/async_database.py</files>
  <read_first>
    - pycopg/async_database.py — the file being modified. CRITICAL (D-02): async interleaving is
      MORE EXTENSIVE than sync. The alias region runs ~L732–L1340, but FOUR real-method sections
      sit inside/after it and MUST be preserved: `# DATAFRAME OPERATIONS` (~L940–L1160:
      to_dataframe, from_dataframe, to_geodataframe, from_geodataframe), `# BATCH OPERATIONS`
      (~L1161–L1246: insert_many, upsert_many), `# STREAMING` (~L1247–L1284: stream), and
      `# LISTEN/NOTIFY` (~L1341+: listen, notify) — plus trailing
      `close`/`__aenter__`/`__aexit__` lifecycle (~L1383+).
    - 25-RESEARCH.md §1 "Removal Mechanics — async_database.py — Exact Boundaries" (interleaved-
      block table) and Pitfalls #3 (async has 4 interleaved sections vs 1 in sync), #4 (orphaned
      headers), #5 (import-line survival), #6 (test_parity false-positive risk — listen is the
      only async-only real method and must survive).
    - 25-PATTERNS.md "stub deletion" — the exact 3-line stub shape uses `async def name(self,
      *args, **kwargs):` for the async class.
  </read_first>
  <action>
    Delete each `@deprecated_alias`-decorated stub in `pycopg/async_database.py` BY TEXT PATTERN
    as a 3-line atomic unit (`@deprecated_alias(...)` line + `async def name(self, *args, **kwargs):`
    line + one-line `"""Deprecated: use ...``"""` docstring). Use
    `grep -n '@deprecated_alias' pycopg/async_database.py` to enumerate the 56 stub sites, then
    delete each via the Edit tool targeting that exact 3-line block. NEVER delete by line range —
    the async file has 4 interleaved real-method sections (DATAFRAME, BATCH, STREAMING,
    LISTEN/NOTIFY) that line-range deletion would destroy (D-02, Pitfall #3). Delete the
    `from pycopg.aliases import deprecated_alias` import line at L28. Delete the orphaned
    section-header comments that group alias stubs (same header set as the sync file). PRESERVE
    all 4 real-method sections and their methods, and PRESERVE the trailing
    `close`/`__aenter__`/`__aexit__` lifecycle methods. After editing run
    `uv run ruff check pycopg/async_database.py` and `uv run black pycopg/async_database.py`.
    Do NOT touch the accessor classes.
  </action>
  <acceptance_criteria>
    - `grep -c deprecated_alias pycopg/async_database.py` → `0` (stubs + import line gone; baseline was 57).
    - `grep -c '\*args, \*\*kwargs' pycopg/async_database.py` → `0` (WR-01 / D-07).
    - `grep -c 'async def to_dataframe\|async def from_dataframe\|async def to_geodataframe\|async def from_geodataframe' pycopg/async_database.py` → `4` (DATAFRAME preserved).
    - `grep -c 'async def insert_many\|async def upsert_many\|async def stream\|async def listen\|async def notify' pycopg/async_database.py` → `5` (BATCH + STREAMING + LISTEN/NOTIFY preserved).
    - `grep -c 'async def close\|async def __aenter__\|async def __aexit__' pycopg/async_database.py` → `3` (lifecycle preserved).
    - `uv run ruff check pycopg/async_database.py` exits 0; `uv run black --check pycopg/async_database.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test "$(grep -c deprecated_alias pycopg/async_database.py)" = "0" && test "$(grep -c "\*args, \*\*kwargs" pycopg/async_database.py)" = "0" && test "$(grep -c "async def insert_many\|async def upsert_many\|async def stream\|async def listen\|async def notify" pycopg/async_database.py)" = "5" && uv run ruff check pycopg/async_database.py'</automated>
  </verify>
  <done>0 `deprecated_alias` and 0 `*args, **kwargs` in `async_database.py`; DATAFRAME (4) + BATCH/STREAMING/LISTEN-NOTIFY (5) + lifecycle (3) preserved; ruff + black clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the stale PostGIS-guard flat-name error string in async_database.py (IN-02)</name>
  <files>pycopg/async_database.py</files>
  <read_first>
    - pycopg/async_database.py — the PostGIS guard string at ~L1119 reads
      `"PostGIS extension not installed. Run db.create_extension('postgis')"`. (Line number
      shifts after Task 1's deletions; locate by literal text.)
    - 25-RESEARCH.md §5 "IN-02 Sweep" — the async_database.py PostGIS-guard row; substitution is
      `Run db.create_extension` → `Run db.schema.create_extension`.
  </read_first>
  <action>
    In `pycopg/async_database.py`, replace the stale PostGIS guard error string
    `"PostGIS extension not installed. Run db.create_extension('postgis')"` with
    `"PostGIS extension not installed. Run db.schema.create_extension('postgis')"` (substitute
    `Run db.create_extension` → `Run db.schema.create_extension`). Locate by literal text since
    Task 1 shifted line numbers. This is the async_database.py slice of the IN-02 sweep (D-08).
  </action>
  <acceptance_criteria>
    - `grep -c "db\.create_extension(" pycopg/async_database.py` → `0`.
    - `grep -c "db\.schema\.create_extension('postgis')" pycopg/async_database.py` → `1`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test "$(grep -c "db\.create_extension(" pycopg/async_database.py)" = "0" && test "$(grep -c "db\.schema\.create_extension(.postgis.)" pycopg/async_database.py)" -ge "1"'</automated>
  </verify>
  <done>No flat `db.create_extension(` string remains in `async_database.py`; the corrected `db.schema.create_extension('postgis')` form is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → AsyncDatabase public API | Removed flat names now raise AttributeError at attribute lookup (no I/O reached) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Tampering | Removal of `@deprecated_alias` delegation layer in `async_database.py` | accept | The async stubs were pure `*args/**kwargs` delegators with NO validation of their own; all guards live in the unchanged async accessors. Removing the indirection removes no security control. |
| T-25-04 | Denial of Service | Accidental deletion of real DATAFRAME/BATCH/STREAMING/LISTEN-NOTIFY/lifecycle methods via line-range delete | mitigate | D-02 enforced: delete stubs by 3-line text pattern only; acceptance greps assert all 4 interleaved real-method sections + lifecycle still resolve. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero new dependencies, D-11). |
</threat_model>

<verification>
- `grep -c deprecated_alias pycopg/async_database.py` → 0
- `grep -c '\*args, \*\*kwargs' pycopg/async_database.py` → 0
- `grep -c "db\.create_extension(" pycopg/async_database.py` → 0
- The 4 interleaved real-method sections (DATAFRAME, BATCH, STREAMING, LISTEN/NOTIFY) + lifecycle resolve.
- `uv run ruff check pycopg/async_database.py` exits 0.
</verification>

<success_criteria>
- 0 `@deprecated_alias` stubs and 0 `*args/**kwargs` on the `AsyncDatabase` public surface.
- All 4 interleaved real-method sections and `close`/`__aenter__`/`__aexit__` preserved.
- PostGIS guard string in `async_database.py` references `db.schema.create_extension`.
- ruff + black clean on `async_database.py`.
</success_criteria>

<output>
Create `.planning/phases/25-alias-removal/25-02-SUMMARY.md` when done.
</output>
