---
phase: 25-alias-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/database.py
autonomous: true
requirements: [ALIAS-RM-01, ALIAS-RM-04]
must_haves:
  truths:
    - "Calling any removed flat name on a live Database instance raises a plain AttributeError (no warning, no delegation)"
    - "The public Database surface exposes only accessor-namespaced methods with real signatures (no *args/**kwargs stub)"
    - "The DATAFRAME OPERATIONS real methods (from_dataframe, to_dataframe, from_geodataframe, to_geodataframe) still resolve on Database"
    - "PostGIS guard error messages on Database reference db.schema.create_extension, not db.create_extension"
  artifacts:
    - path: "pycopg/database.py"
      provides: "Database class with 0 @deprecated_alias stubs, 0 *args/**kwargs, import line removed, real methods + lifecycle preserved"
      contains: "def from_dataframe"
  key_links:
    - from: "pycopg/database.py"
      to: "pycopg.schema accessor"
      via: "error message references db.schema.create_extension"
      pattern: "db\\.schema\\.create_extension"
---

<objective>
Remove all 56 `@deprecated_alias` flat stubs from `pycopg/database.py`, delete the
`from pycopg.aliases import deprecated_alias` import line, delete the now-empty section-header
comments, and fix the 1 stale PostGIS-guard error string in this file (IN-02). The real
interleaved DATAFRAME OPERATIONS methods and the trailing `close`/`__enter__`/`__exit__`
lifecycle methods MUST be preserved.

Purpose: Hard-remove the v0.6.0 deprecated flat API surface from the sync class (D-01, D-02),
closing WR-01 (IDE signature erasure from `*args/**kwargs` stubs, D-07) and the database.py
slice of IN-02 (D-08).

Output: A `pycopg/database.py` whose public surface is accessor-only, where every removed
flat name resolves to `AttributeError`, and whose error strings name accessor paths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-alias-removal/25-CONTEXT.md
@.planning/phases/25-alias-removal/25-RESEARCH.md
@.planning/phases/25-alias-removal/25-PATTERNS.md
@.planning/phases/25-alias-removal/25-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan does NOT create new symbols — it is a deletion slice. It REMOVES:
- 56 `@deprecated_alias`-decorated flat stub methods from `Database` (e.g. `create_database`,
  `create_hypertable`, `create_extension`, `pg_dump`, `vacuum`, `list_tables`, …).
- The `from pycopg.aliases import deprecated_alias` import line at `pycopg/database.py:29`.
- The orphaned section-header comments (`# DATABASE ADMINISTRATION`, `# EXTENSIONS`,
  `# SCHEMAS & TABLES`, `# CONSTRAINTS & INDEXES`, `# POSTGIS SPATIAL OPERATIONS`,
  `# TIMESCALEDB OPERATIONS`, `# MAINTENANCE & STATS`, `# ROLES & USERS`,
  `# ROLE MANAGEMENT`, `# BACKUP & RESTORE`).

Downstream drift-verification: these 56 names are INTENTIONALLY GONE from `Database`. Do not
flag them as missing — `tests/test_alias_removal.py` (plan 25-03) asserts they raise
`AttributeError`. The `# DATAFRAME OPERATIONS` header and its 4 real methods are PRESERVED.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 56 @deprecated_alias stubs + import line + orphaned headers in database.py</name>
  <files>pycopg/database.py</files>
  <read_first>
    - pycopg/database.py — the file being modified. CRITICAL (D-02): stubs are INTERLEAVED
      with real methods. The alias region runs ~L859–L1329, but the `# DATAFRAME OPERATIONS`
      block (~L983–L1192: `from_dataframe`, `to_dataframe`, `from_geodataframe`,
      `to_geodataframe`) sits INSIDE it and MUST be preserved, as must the trailing
      `close`/`__enter__`/`__exit__` lifecycle methods (~L1330+).
    - 25-RESEARCH.md §1 "Removal Mechanics — database.py — Exact Boundaries" (the 56-name list,
      interleaved-block table) and Pitfalls #2 (line-range deletion destroys real methods) and
      #4 (orphaned section headers) and #5 (import-line survival → ModuleNotFoundError).
    - 25-PATTERNS.md "pycopg/database.py / pycopg/async_database.py (stub deletion)" — the exact
      3-line stub shape (decorator + `def name(self, *args, **kwargs):` + one-line docstring) and
      the section-headers-to-delete vs keep (`# DATAFRAME OPERATIONS` stays).
  </read_first>
  <action>
    Delete each `@deprecated_alias`-decorated stub in `pycopg/database.py` BY TEXT PATTERN as a
    3-line atomic unit (the `@deprecated_alias(...)` decorator line + the
    `def name(self, *args, **kwargs):` line + the one-line `"""Deprecated: use ...``"""` docstring).
    Use `grep -n '@deprecated_alias' pycopg/database.py` to enumerate the 56 stub sites, then
    delete each via the Edit tool targeting that exact 3-line block. NEVER delete by line range
    (`sed Ld`) — that would destroy the interleaved DATAFRAME OPERATIONS real methods (D-02,
    Pitfall #2). Delete the `from pycopg.aliases import deprecated_alias` import line at L29.
    Delete the orphaned section-header comments that have no remaining body
    (`# DATABASE ADMINISTRATION`, `# EXTENSIONS`, `# SCHEMAS & TABLES`, `# CONSTRAINTS & INDEXES`,
    `# POSTGIS SPATIAL OPERATIONS`, `# TIMESCALEDB OPERATIONS`, `# MAINTENANCE & STATS`,
    `# ROLES & USERS`, `# ROLE MANAGEMENT`, `# BACKUP & RESTORE`). PRESERVE the
    `# DATAFRAME OPERATIONS` header and all 4 of its real methods, and PRESERVE the trailing
    `close`/`__enter__`/`__exit__` lifecycle methods. After editing run
    `uv run ruff check pycopg/database.py` and `uv run black pycopg/database.py` to catch any
    leftover reference or formatting drift. Do NOT touch the accessor classes — they are unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c deprecated_alias pycopg/database.py` → `0` (both stubs and import line gone; baseline was 57).
    - `grep -c '\*args, \*\*kwargs' pycopg/database.py` → `0` (WR-01 / D-07: every varargs was an alias stub).
    - `grep -c 'def from_dataframe\|def to_dataframe\|def from_geodataframe\|def to_geodataframe' pycopg/database.py` → `4` (DATAFRAME OPERATIONS real methods preserved).
    - `grep -c 'def close\|def __enter__\|def __exit__' pycopg/database.py` → `3` (lifecycle preserved).
    - `uv run ruff check pycopg/database.py` exits 0; `uv run black --check pycopg/database.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test "$(grep -c deprecated_alias pycopg/database.py)" = "0" && test "$(grep -c "\*args, \*\*kwargs" pycopg/database.py)" = "0" && test "$(grep -c "def from_dataframe\|def to_dataframe\|def from_geodataframe\|def to_geodataframe" pycopg/database.py)" = "4" && uv run ruff check pycopg/database.py'</automated>
  </verify>
  <done>0 `deprecated_alias` occurrences and 0 `*args, **kwargs` in `database.py`; the 4 DATAFRAME methods and 3 lifecycle methods preserved; ruff + black clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the stale PostGIS-guard flat-name error string in database.py (IN-02)</name>
  <files>pycopg/database.py</files>
  <read_first>
    - pycopg/database.py — the PostGIS guard string at ~L1108 reads
      `"PostGIS extension not installed. Run db.create_extension('postgis')"`. (Line number
      shifts after Task 1's deletions; locate by the literal text, not the line number.)
    - 25-RESEARCH.md §5 "IN-02 Sweep" — the database.py PostGIS-guard row; substitution is
      `Run db.create_extension` → `Run db.schema.create_extension`.
    - 25-PATTERNS.md "IN-02 PostGIS" — the corrected form for all 3 PostGIS sites.
  </read_first>
  <action>
    In `pycopg/database.py`, replace the stale PostGIS guard error string
    `"PostGIS extension not installed. Run db.create_extension('postgis')"` with
    `"PostGIS extension not installed. Run db.schema.create_extension('postgis')"`
    (substitute `Run db.create_extension` → `Run db.schema.create_extension`). Locate the
    string by its literal text since Task 1 shifted line numbers. This is the database.py slice
    of the IN-02 sweep (D-08); the spatial.py / async_database.py / timescale.py slices are
    handled in plans 25-02 and 25-04.
  </action>
  <acceptance_criteria>
    - `grep -c "db\.create_extension(" pycopg/database.py` → `0` (no flat reference remains in any error string).
    - `grep -c "db\.schema\.create_extension('postgis')" pycopg/database.py` → `1` (corrected form present).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test "$(grep -c "db\.create_extension(" pycopg/database.py)" = "0" && test "$(grep -c "db\.schema\.create_extension(.postgis.)" pycopg/database.py)" -ge "1"'</automated>
  </verify>
  <done>No flat `db.create_extension(` string remains in `database.py`; the corrected `db.schema.create_extension('postgis')` form is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Database public API | Callers invoke `db.<method>(...)`; removed flat names now raise AttributeError at attribute lookup (no I/O reached) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | Removal of `@deprecated_alias` delegation layer in `database.py` | accept | The stubs were pure `*args/**kwargs` delegators with NO validation logic of their own — every identifier-validation / SQL-injection guard lives in the accessor methods (`db.schema.*`, etc.), which are UNCHANGED by this plan and remain covered by `tests/test_sql_injection.py`. Removing the indirection removes no security control. |
| T-25-02 | Denial of Service | Accidental deletion of real DATAFRAME/lifecycle methods via line-range delete | mitigate | D-02 enforced: delete stubs by 3-line text pattern only; acceptance grep asserts the 4 DATAFRAME methods + 3 lifecycle methods still resolve. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero new dependencies, D-11). No legitimacy audit required. |
</threat_model>

<verification>
- `grep -c deprecated_alias pycopg/database.py` → 0
- `grep -c '\*args, \*\*kwargs' pycopg/database.py` → 0
- `grep -c "db\.create_extension(" pycopg/database.py` → 0
- `uv run python -c "import pycopg.database"` succeeds only AFTER plan 25-02 also removes its
  import and plan 25-03 deletes aliases.py; within this plan, `database.py` still imports
  `deprecated_alias`-free, so `import pycopg.database` will fail on the missing import ONLY if
  `aliases.py` is already gone — it is not yet, so import still succeeds here.
- `uv run ruff check pycopg/database.py` exits 0.
</verification>

<success_criteria>
- 0 `@deprecated_alias` stubs and 0 `*args/**kwargs` on the `Database` public surface.
- DATAFRAME OPERATIONS (4 methods) and lifecycle (`close`/`__enter__`/`__exit__`) preserved.
- PostGIS guard string in `database.py` references `db.schema.create_extension`.
- ruff + black clean on `database.py`.
</success_criteria>

<output>
Create `.planning/phases/25-alias-removal/25-01-SUMMARY.md` when done.
</output>
