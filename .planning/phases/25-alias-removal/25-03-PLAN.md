---
phase: 25-alias-removal
plan: 03
type: execute
wave: 2
depends_on: ["25-01", "25-02"]
files_modified:
  - pycopg/aliases.py
  - tests/test_admin_aliases.py
  - tests/test_maint_aliases.py
  - tests/test_schema_aliases.py
  - tests/test_backup_aliases.py
  - tests/test_timescale_aliases.py
  - tests/test_spatial_aliases.py
  - tests/test_alias_removal.py
autonomous: true
requirements: [ALIAS-RM-01, ALIAS-RM-02, ALIAS-RM-04]
must_haves:
  truths:
    - "pycopg/aliases.py no longer exists and nothing in pycopg/ imports deprecated_alias"
    - "import pycopg succeeds with no ModuleNotFoundError"
    - "Every one of the 56 removed flat names raises AttributeError on a live Database AND AsyncDatabase instance"
    - "No public method on Database or AsyncDatabase has *args/**kwargs in its signature (WR-01 proven via inspect)"
    - "The 6 warn+delegate alias test files are deleted; test_parity.py is unmodified and green; -W error::DeprecationWarning is clean"
  artifacts:
    - path: "tests/test_alias_removal.py"
      provides: "Parametrized AttributeError proof for all 56 removed names on Database + AsyncDatabase, plus WR-01 inspect assertions"
      contains: "REMOVED_FLAT_NAMES"
      min_lines: 60
  key_links:
    - from: "tests/test_alias_removal.py"
      to: "pycopg.Database / pycopg.AsyncDatabase"
      via: "getattr(db, name) raises AttributeError under pytest.raises"
      pattern: "pytest\\.raises\\(AttributeError\\)"
---

<objective>
Complete the source-side removal by deleting `pycopg/aliases.py` (both importers are gone after
plans 25-01/25-02), then swap the test layer: delete the 6 warn+delegate alias test files and
add `tests/test_alias_removal.py` — the positive proof that all 56 removed flat names raise
`AttributeError` on live `Database` and `AsyncDatabase` instances, plus the WR-01 `inspect`
assertion that no public method retains `*args/**kwargs`.

Purpose: Finish ALIAS-RM-01 (delete aliases.py, D-03), deliver ALIAS-RM-02 (AttributeError proof
+ remove warn+delegate tests, D-04/D-05), and prove ALIAS-RM-04/WR-01 closure (D-07). This is the
Wave-0-style test that FAILS while stubs exist and PASSES only after plans 25-01/25-02 land —
hence wave 2.

Output: `pycopg/aliases.py` deleted, 6 alias test files deleted, `tests/test_alias_removal.py`
created and green, `test_parity.py` untouched and green, `-W error::DeprecationWarning` clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-alias-removal/25-CONTEXT.md
@.planning/phases/25-alias-removal/25-RESEARCH.md
@.planning/phases/25-alias-removal/25-PATTERNS.md
@.planning/phases/25-alias-removal/25-VALIDATION.md
@tests/conftest.py
@tests/test_admin_aliases.py
</context>

<artifacts_this_phase_produces>
CREATES:
- `tests/test_alias_removal.py` with the module constant `REMOVED_FLAT_NAMES` (56 names) and
  the test functions:
  - `test_removed_flat_name_raises_attribute_error_sync` (parametrized over 56 names)
  - `test_removed_flat_name_raises_attribute_error_async` (parametrized over 56 names)
  - `test_no_varargs_on_database_public_surface` (WR-01)
  - `test_no_varargs_on_async_database_public_surface` (WR-01)

REMOVES:
- `pycopg/aliases.py` (the `deprecated_alias` decorator module).
- `tests/test_admin_aliases.py`, `tests/test_maint_aliases.py`, `tests/test_schema_aliases.py`,
  `tests/test_backup_aliases.py`, `tests/test_timescale_aliases.py`,
  `tests/test_spatial_aliases.py` (the 6 warn+delegate test files, D-04).

Downstream drift-verification: `aliases.py` and the 6 alias test files are INTENTIONALLY GONE.
`tests/test_sql_injection.py` is NOT one of the deleted files (it stays; its stale comment is
fixed in plan 25-04). `tests/test_parity.py` is NOT modified (D-06).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete pycopg/aliases.py and the 6 warn+delegate alias test files</name>
  <files>pycopg/aliases.py, tests/test_admin_aliases.py, tests/test_maint_aliases.py, tests/test_schema_aliases.py, tests/test_backup_aliases.py, tests/test_timescale_aliases.py, tests/test_spatial_aliases.py</files>
  <read_first>
    - 25-CONTEXT.md D-03 (delete aliases.py only after confirming no other importer) and D-04
      (the EXACTLY 6 alias test files — NOT 7; `tests/test_sql_injection.py` is not an alias
      test file and stays).
    - 25-RESEARCH.md §"Open Questions" #1 (delete aliases.py only after both import lines are
      removed — they are, by plans 25-01/25-02) and Pitfall #1 (the 6-vs-7 off-by-one: live
      filesystem has exactly 6 alias test files).
  </read_first>
  <action>
    First confirm no remaining importer: `grep -rl "from pycopg.aliases import\|import deprecated_alias" pycopg/`
    MUST return nothing (plans 25-01/25-02 removed both import lines). Then delete
    `pycopg/aliases.py` with `git rm pycopg/aliases.py` (or `rm`). Delete the 6 warn+delegate
    test files: `tests/test_admin_aliases.py`, `tests/test_maint_aliases.py`,
    `tests/test_schema_aliases.py`, `tests/test_backup_aliases.py`,
    `tests/test_timescale_aliases.py`, `tests/test_spatial_aliases.py`. Do NOT delete
    `tests/test_sql_injection.py` (D-04, Pitfall #1) and do NOT touch `tests/test_parity.py`
    (D-06). After deletion, verify the package still imports cleanly:
    `uv run python -c "import pycopg"` (this catches the Pitfall #5 ModuleNotFoundError if any
    `deprecated_alias` import survived).
  </action>
  <acceptance_criteria>
    - `test ! -f pycopg/aliases.py` (file gone).
    - `grep -rq deprecated_alias pycopg/` returns non-zero (no `deprecated_alias` anywhere in `pycopg/`).
    - `ls tests/test_*_aliases.py 2>/dev/null | wc -l` → `0` (all 6 alias test files gone).
    - `test -f tests/test_sql_injection.py` AND `test -f tests/test_parity.py` (both preserved).
    - `uv run python -c "import pycopg"` exits 0 (no ModuleNotFoundError).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test ! -f pycopg/aliases.py && ! grep -rq deprecated_alias pycopg/ && test "$(ls tests/test_*_aliases.py 2>/dev/null | wc -l)" = "0" && test -f tests/test_sql_injection.py && test -f tests/test_parity.py && uv run python -c "import pycopg"'</automated>
  </verify>
  <done>`aliases.py` and the 6 alias test files are deleted; no `deprecated_alias` reference remains in `pycopg/`; `test_sql_injection.py` and `test_parity.py` preserved; `import pycopg` succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests/test_alias_removal.py — AttributeError proof for 56 names + WR-01 inspect assertions</name>
  <files>tests/test_alias_removal.py</files>
  <behavior>
    - For each of the 56 `REMOVED_FLAT_NAMES`, `getattr(Database(config), name)` raises `AttributeError`.
    - For each of the 56 names, `getattr(AsyncDatabase(config), name)` raises `AttributeError`.
    - No public (non-underscore) callable on `Database` has a VAR_POSITIONAL or VAR_KEYWORD parameter (WR-01).
    - No public callable on `AsyncDatabase` has a VAR_POSITIONAL or VAR_KEYWORD parameter (WR-01).
    - This test FAILS if run against a pre-removal tree (the names resolve, no AttributeError) and
      PASSES against the post-25-01/25-02 tree.
  </behavior>
  <read_first>
    - 25-PATTERNS.md "tests/test_alias_removal.py (NEW)" — CRITICAL: use the bare `config` fixture
      directly (`Database(config)` / `AsyncDatabase(config)`); do NOT add `patch("pycopg.database.psycopg")`.
      Attribute lookup fails before any connection, so no DB/mock is needed. The 56-name
      `REMOVED_FLAT_NAMES` list is hardcoded (do NOT derive at runtime — RESEARCH §Open Questions #2).
    - 25-RESEARCH.md "Validation Architecture → Wave 0 Gaps" — the full recommended test structure
      (parametrized AttributeError tests + the two `inspect.getmembers` WR-01 tests). NOTE: drop the
      `with patch(...)` fixture wrapper shown in RESEARCH; PATTERNS.md supersedes it (not needed).
    - tests/conftest.py — the `config` fixture (host/port/database/user/password) used by alias tests.
    - 25-RESEARCH.md §1 — the canonical 56-name list (source of truth for `REMOVED_FLAT_NAMES`).
  </read_first>
  <action>
    Create `tests/test_alias_removal.py`. Define module-level `REMOVED_FLAT_NAMES` as a hardcoded
    list of the 56 removed flat names (verbatim from 25-RESEARCH.md §1: `add_compression_policy`
    … `vacuum`). Import `Database`, `AsyncDatabase` from `pycopg` and `inspect`/`pytest`. Add two
    `@pytest.mark.parametrize("name", REMOVED_FLAT_NAMES)` test functions —
    `test_removed_flat_name_raises_attribute_error_sync(name, config)` constructing
    `Database(config)` and asserting `getattr(db, name)` under `pytest.raises(AttributeError)`, and
    the async mirror constructing `AsyncDatabase(config)`. Add
    `test_no_varargs_on_database_public_surface()` and
    `test_no_varargs_on_async_database_public_surface()` that iterate
    `inspect.getmembers(cls, predicate=callable)`, skip `_`-prefixed names, and assert no parameter
    is `VAR_POSITIONAL`/`VAR_KEYWORD` (wrap `inspect.signature` in try/except `(ValueError,
    TypeError)`). Use the bare `config` fixture — NO psycopg patch. Add a numpydoc module docstring.
    Run `uv run black tests/test_alias_removal.py` and `uv run ruff check tests/test_alias_removal.py`.
  </action>
  <acceptance_criteria>
    - `tests/test_alias_removal.py` exists and defines `REMOVED_FLAT_NAMES` with exactly 56 entries:
      `python -c "import ast,sys; m=ast.parse(open('tests/test_alias_removal.py').read()); print(sum(len(n.value.elts) for n in ast.walk(m) if isinstance(n,ast.Assign) and getattr(n.targets[0],'id','')=='REMOVED_FLAT_NAMES'))"` → `56`.
    - `uv run pytest tests/test_alias_removal.py -q -o addopts=""` → all green (112 parametrized AttributeError cases + 2 WR-01 tests).
    - `uv run pytest tests/test_parity.py -q -o addopts=""` → green (D-06: parity unaffected, unmodified).
    - `uv run pytest -W error::DeprecationWarning tests/ -o addopts=""` → exit 0 (no stub left to fire a DeprecationWarning).
    - `uv run ruff check tests/test_alias_removal.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_alias_removal.py tests/test_parity.py -q -o addopts="" && uv run pytest -W error::DeprecationWarning tests/ -o addopts=""</automated>
  </verify>
  <done>`test_alias_removal.py` exists with 56-name `REMOVED_FLAT_NAMES`; all its tests pass; `test_parity.py` green and unmodified; `-W error::DeprecationWarning` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → Database/AsyncDatabase | Tests construct instances from a static `config` fixture; no live DB, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Information Disclosure | `deprecated_alias` decorator module deletion | accept | `aliases.py` contained only a pure delegation/warning decorator — no secrets, no validation logic. Its deletion removes no security control; the AttributeError-on-missing-name behavior is Python's default and leaks no information. |
| T-25-06 | Repudiation | Removing 6 alias test files reduces coverage of the removed surface | mitigate | The deleted tests asserted warn+delegate behavior that no longer exists; `test_alias_removal.py` replaces them with positive AttributeError + WR-01 proof, and `test_parity.py` (unchanged) continues to guard sync/async surface symmetry. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero new dependencies, D-11). |
</threat_model>

<verification>
- `test ! -f pycopg/aliases.py` and `! grep -rq deprecated_alias pycopg/`.
- `ls tests/test_*_aliases.py | wc -l` → 0; `test_sql_injection.py` + `test_parity.py` preserved.
- `uv run python -c "import pycopg"` succeeds.
- `uv run pytest tests/test_alias_removal.py tests/test_parity.py -q -o addopts=""` green.
- `uv run pytest -W error::DeprecationWarning tests/ -o addopts=""` exit 0.
</verification>

<success_criteria>
- `aliases.py` + 6 alias test files deleted; package imports cleanly.
- `test_alias_removal.py` proves all 56 names raise AttributeError on both classes + WR-01 inspect green.
- `test_parity.py` unmodified and green; `-W error::DeprecationWarning` clean.
</success_criteria>

<output>
Create `.planning/phases/25-alias-removal/25-03-SUMMARY.md` when done.
</output>
