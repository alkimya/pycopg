---
phase: 36-release-v090
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - docs/conf.py
  - CHANGELOG.md
  - README.md
  - docs/api-reference.md
  - docs/database.md
  - docs/async-database.md
  - docs/index.md
  - CLAUDE.md
  - pycopg/schema.py
  - pycopg/maint.py
  - pycopg/admin.py
  - pycopg/backup.py
  - pycopg/timescale.py
autonomous: true
requirements: [REL-09]

must_haves:
  truths:
    - "Version 0.9.0 is set in the single canonical package source (pyproject.toml) and pycopg.__version__ resolves to it dynamically (no hardcoded __version__)"
    - "CHANGELOG has an Added-only [0.9.0] section covering all 12 new methods (7 CRUD + 5 introspection) with signatures matching the code exactly"
    - "README method counts reflect the new surface (db.schema.* 27 to 32) and a CRUD note is present"
    - "docs/api-reference.md, docs/database.md, docs/async-database.md, docs/index.md cover the new CRUD + introspection methods"
    - "Cosmetic debt is cleared: CLAUDE.md version line reads v0.9.0, no pycopg.aliases xref remains in pycopg/*.py docstrings"
    - "All 4 gates measure green (coverage >=94, interrogate >=95, Sphinx -W clean, no DeprecationWarning on import)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Canonical package version 0.9.0"
      contains: "version = \"0.9.0\""
    - path: "CHANGELOG.md"
      provides: "[0.9.0] Added-only release notes"
      contains: "## [0.9.0]"
    - path: "README.md"
      provides: "Updated accessor method counts + CRUD note"
      contains: "(32 methods)"
    - path: "docs/conf.py"
      provides: "Sphinx display version 0.9.0"
      contains: "release = '0.9.0'"
  key_links:
    - from: "pycopg/__init__.py"
      to: "pyproject.toml version"
      via: "importlib.metadata.version(\"pycopg\")"
      pattern: "version\\(\"pycopg\"\\)"
    - from: "CHANGELOG [0.9.0]"
      to: "pycopg/database.py + pycopg/schema.py method signatures"
      via: "verbatim signature copy (D-36-03 drift guard)"
      pattern: "def (upsert|delete_where|update_where|exists|count|fetch_all|paginate|primary_key|foreign_keys|sequences|views|describe)"
---

<objective>
Prepare the v0.9.0 release content: bump the version, write the Added-only CHANGELOG section for the 12 new public methods, update every docs surface, clear the soldered cosmetic debt (D-36-04), and measure that all 4 quality gates are green BEFORE handing off to the release plan (36-02).

This is the content/docs half of the locked 2-plan split (D-36-02). It is purely additive — no Changed/Removed/Deprecated, no MIGRATION guide. There are NO irreversible steps here, so this plan is autonomous.

Purpose: Package v0.9.0 (final phase of the CRUD + introspection milestone). All production code, tests, and docstrings are already green from Phases 34/35; this plan documents and version-stamps the release so 36-02 can tag and publish without housekeeping.
Output: pyproject.toml @ 0.9.0, refreshed uv.lock, docs/conf.py @ 0.9.0, CHANGELOG [0.9.0], updated README + docs/*.md, cleaned CLAUDE.md + accessor docstrings, and a recorded 4-gate baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-release-v090/36-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This phase (36) produces release artifacts, not new runtime code. Plan 36-01 produces:
- The bumped version string `0.9.0` in `pyproject.toml` (canonical), `uv.lock`, and `docs/conf.py` (Sphinx display).
- The `CHANGELOG.md` `## [0.9.0]` Added-only section.
- Updated documentation surfaces (README, docs/*.md).
Plan 36-02 produces the git tag `v0.9.0`, the GitHub Release, and the published PyPI release.
These are expected, intentional artifacts — a source-grounding pass should not flag a missing implementation file for them.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.9.0 across the three version strings + refresh lockfile</name>
  <files>pyproject.toml, uv.lock, docs/conf.py, CLAUDE.md</files>
  <read_first>
    - pyproject.toml — line 7 currently `version = "0.8.0"` (the ONLY canonical package source per D-36-01).
    - pycopg/__init__.py — lines 39-44: `__version__` is derived via `importlib.metadata.version("pycopg")` with a `PackageNotFoundError` fallback. DO NOT edit this file. Do NOT add a hardcoded `__version__ = "0.9.0"` — that shadows dynamic resolution and is a regression (D-36-01).
    - docs/conf.py — line 17 `release = '0.8.0'` (Sphinx display version, separate from the package version; bump it so RTD/Sphinx render 0.9.0).
    - uv.lock — line ~698 `version = "0.8.0"` under `name = "pycopg"`; refresh by running `uv lock`, NOT by hand-editing (CI `uv lock --check` in publish.yml will fail if stale).
    - CLAUDE.md — line 13 `**pycopg v0.5.0**` (stale since v0.6.0; D-36-04 cosmetic debt).
  </read_first>
  <action>
    Set `pyproject.toml` line 7 to exactly `version = "0.9.0"`. Set `docs/conf.py` line 17 to exactly `release = '0.9.0'`. Run `uv lock` to refresh `uv.lock` so the pycopg entry reads `version = "0.9.0"` (do not hand-edit the lockfile). Update `CLAUDE.md` line 13 so the Version line reads `**pycopg v0.9.0**` (keep the rest of that line's descriptive text). Per D-36-01, do NOT touch `pycopg/__init__.py` — `__version__` must continue resolving dynamically from the package metadata.
  </action>
  <verify>
    <automated>grep -q 'version = "0.9.0"' pyproject.toml && grep -q "release = '0.9.0'" docs/conf.py && grep -q '\*\*pycopg v0.9.0\*\*' CLAUDE.md && grep -A1 'name = "pycopg"' uv.lock | grep -q 'version = "0.9.0"' && ! grep -q '__version__ = "0.9.0"' pycopg/__init__.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `version = "0.9.0"`.
    - `docs/conf.py` contains `release = '0.9.0'`.
    - `uv.lock` pycopg entry contains `version = "0.9.0"` (refreshed via `uv lock`, exits 0).
    - `CLAUDE.md` contains `**pycopg v0.9.0**`.
    - `pycopg/__init__.py` does NOT contain a hardcoded `__version__ = "0.9.0"` line (dynamic resolution preserved, D-36-01).
  </acceptance_criteria>
  <done>All three version strings read 0.9.0, the lockfile is current, CLAUDE.md version line is fixed, and __init__.py is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write CHANGELOG [0.9.0] Added-only section with exact signatures (D-36-03)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md — lines 1-61: `## [Unreleased]` (empty) is at line 8, the `## [0.8.0] - 2026-06-23` block (lines 10-60) is the STYLE TEMPLATE. Match its style exactly: group by feature area with a bold heading, one bullet per method, each method line carries its FULL signature followed by a shallow one-line description, and a closing parity/dependency sentence.
    - pycopg/database.py — read the sync CRUD signatures verbatim (param names, defaults, return types). They are: `upsert(self, table, row, conflict_columns, update_columns=None, schema="public") -> dict | None` (line 647); `delete_where(self, table, where, schema="public") -> int` (702); `update_where(self, table, values, where, schema="public") -> int` (742); `exists(self, table, where, schema="public") -> bool` (792); `count(self, table, where=None, schema="public") -> int` (833); `fetch_all(self, sql, params=None) -> list[dict]` (1077); `paginate(self, table, limit, offset=0, order_by=None, where=None, descending=False, schema="public") -> list[dict]` (1114). Confirm each against the file before writing — do not trust these line numbers for the signature text.
    - pycopg/schema.py — read the introspection signatures verbatim: `primary_key(self, table, schema="public") -> dict | None` (676); `foreign_keys(self, table, schema="public") -> list[dict]` (701); `sequences(self, schema="public") -> list[str]` (741); `views(self, schema="public") -> list[str]` (759); `describe(self, table, schema="public") -> dict` (777).
    - pycopg/async_database.py + pycopg/schema.py async twins — confirm the async signatures match the sync ones (parity) so the closing "full sync/async parity" sentence is accurate.
  </read_first>
  <action>
    Insert a new `## [0.9.0] - 2026-06-25` section directly after the `## [Unreleased]` line (line 8) and before `## [0.8.0]`. Under an `### Added` heading, mirror the [0.8.0] block style with two bold feature-area groups. Group one, "CRUD ergonomics (7 new methods on `db.*` / `async_db.*`, flat on `Database`/`AsyncDatabase`)": one bullet each for `upsert`, `delete_where`, `update_where`, `exists`, `count`, `fetch_all`, `paginate`, each carrying the EXACT signature read from pycopg/database.py and a shallow description. Group two, "Schema introspection (5 new methods on `db.schema.*` / `async_db.schema.*`)": one bullet each for `primary_key`, `foreign_keys`, `sequences`, `views`, `describe`, each carrying the EXACT signature read from pycopg/schema.py. Close with a sentence noting all 12 methods have full sync/async parity and zero new runtime dependencies. Added-only — no Changed/Removed/Deprecated and no MIGRATION reference (purely additive, D-36-03).
  </action>
  <verify>
    <automated>grep -q '## \[0.9.0\]' CHANGELOG.md && for m in upsert delete_where update_where exists count fetch_all paginate primary_key foreign_keys sequences views describe; do grep -q "$m" CHANGELOG.md || { echo "MISSING $m"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` contains `## [0.9.0]` positioned between `## [Unreleased]` and `## [0.8.0]`.
    - Each of the 12 method names (`upsert`, `delete_where`, `update_where`, `exists`, `count`, `fetch_all`, `paginate`, `primary_key`, `foreign_keys`, `sequences`, `views`, `describe`) appears under the [0.9.0] section.
    - Each method's signature in the CHANGELOG matches the live code signature exactly (param names, defaults, return type) — verified by reading the source files, not from memory.
    - The section is Added-only (no `### Changed`, `### Removed`, `### Deprecated`, no MIGRATION link).
  </acceptance_criteria>
  <done>The [0.9.0] CHANGELOG section documents all 12 methods with code-exact signatures, Added-only, in the [0.8.0] style.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs surfaces (README counts + CRUD, api-reference rows, database/async-database/index pages)</name>
  <files>README.md, docs/api-reference.md, docs/database.md, docs/async-database.md, docs/index.md</files>
  <read_first>
    - README.md — lines 76-91: the "Accessor Namespaces" table. Line 82 reads `db.schema.* | ... (27 methods)`. The CRUD methods are flat on `Database`/`AsyncDatabase`, NOT an accessor, so they need a note/row rather than a count bump on an accessor.
    - docs/api-reference.md — section map: `## Database` (line 58), with sub-tables `### Query Methods` (81), `### Schema Methods` (97), `### Table Methods` (106), and `## AsyncDatabase` (256) with its parity note (264) and `### Async-Only Methods` (266). Add new method rows in the same table format (`| name | params | return |`).
    - docs/database.md and docs/async-database.md — read the existing structure/headings so the new CRUD subsection matches the page's conventions.
    - docs/index.md — read the toctree so any new content is reachable (no new page is added; api-autodoc regenerates from docstrings — do not hand-edit docs/api-autodoc.md).
    - pycopg/database.py + pycopg/schema.py — re-use the exact signatures confirmed in Task 2 for the per-method rows.
  </read_first>
  <action>
    In README.md line 82, change `(27 methods)` to `(32 methods)` for `db.schema.*` (5 introspection helpers added) and add `describe` to that row's example method list. Add a short note or row to the Accessor Namespaces section (or immediately after the table) stating that `Database`/`AsyncDatabase` now also expose flat CRUD helpers `upsert`, `delete_where`, `update_where`, `exists`, `count`, `fetch_all`, `paginate`. In docs/api-reference.md, add a `### CRUD Methods (v0.9.0)` table under `## Database` listing the 7 CRUD methods with their params and return types, and add the 5 introspection methods (`primary_key`, `foreign_keys`, `sequences`, `views`, `describe`) as rows under the Schema/Table methods area; rely on the existing AsyncDatabase parity note (line 264) for async coverage. In docs/database.md and docs/async-database.md, add a CRUD section documenting the 7 methods with shallow, numpydoc-consistent descriptions. Confirm docs/index.md toctree still resolves (no orphan). Do NOT edit docs/api-autodoc.md (auto-generated).
  </action>
  <verify>
    <automated>grep -q '(32 methods)' README.md && for m in upsert delete_where update_where exists count fetch_all paginate; do grep -q "$m" docs/api-reference.md || { echo "api-reference missing $m"; exit 1; }; done && for m in primary_key foreign_keys sequences views describe; do grep -q "$m" docs/api-reference.md || { echo "api-reference missing $m"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `(32 methods)` for the `db.schema.*` row and references the new flat CRUD helpers.
    - `docs/api-reference.md` contains rows for all 7 CRUD methods and all 5 introspection methods.
    - `docs/database.md` and `docs/async-database.md` each document the new CRUD methods.
    - `docs/index.md` toctree resolves with no orphan document (validated by the Sphinx -W gate in Task 5).
    - `docs/api-autodoc.md` is NOT hand-edited.
  </acceptance_criteria>
  <done>All documentation surfaces reflect the v0.9.0 CRUD + introspection methods with correct counts and per-method rows.</done>
</task>

<task type="auto">
  <name>Task 4: Clear soldered cosmetic debt — stale pycopg.aliases xrefs + WR-01/WR-03 (D-36-04)</name>
  <files>pycopg/schema.py, pycopg/maint.py, pycopg/admin.py, pycopg/backup.py, pycopg/timescale.py</files>
  <read_first>
    - pycopg/schema.py:11, pycopg/maint.py:10, pycopg/admin.py:11, pycopg/backup.py:11, pycopg/timescale.py:10 — each module docstring contains `as thin deprecated aliases (see :mod:\`pycopg.aliases\`) until v0.7.0.` The `aliases.py` module was DELETED in v0.7.0, so this `:mod:` cross-reference is a dangling Sphinx xref that must be removed/rewritten so the `-W` build (Task 5) stays clean.
    - 36-CONTEXT.md D-36-04 — WR-01 (case-sensitive `time_bucket(` guard in the continuous-aggregate check) and WR-03 (INTERVAL-literal-vs-`%s`) are v0.8.0 advisory review warnings. Fold in ONLY if low-risk and non-behavioral; DEFER any change that touches runtime behavior and note the defer.
    - pycopg/timescale.py — locate the `time_bucket(` guard (WR-01) and the INTERVAL handling (WR-03) to assess whether a fix is purely cosmetic (e.g., a docstring/comment clarification) or behavioral.
  </read_first>
  <action>
    In all five accessor modules, remove the dangling `(see :mod:\`pycopg.aliases\`) until v0.7.0` cross-reference from the module docstring — either delete the clause entirely or rewrite the sentence to drop the dead `pycopg.aliases` reference, so no `pycopg.aliases` token remains in any pycopg/*.py docstring. Assess WR-01 and WR-03 in pycopg/timescale.py: apply ONLY if the change is documentation/comment-level and non-behavioral; if either would alter runtime behavior, do NOT apply it and instead record a one-line defer note in the SUMMARY (these are advisory, not blocking). Keep all of this housekeeping in 36-01 — none of it belongs in the release plan 36-02.
  </action>
  <verify>
    <automated>! grep -rn 'pycopg\.aliases' pycopg/ && uv run ruff check pycopg && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'pycopg.aliases' pycopg/` returns nothing.
    - `uv run ruff check pycopg` exits 0 (no lint regression from the docstring edits).
    - WR-01/WR-03 are either applied (non-behavioral) or explicitly deferred with a one-line note in the SUMMARY (no silent behavioral change).
  </acceptance_criteria>
  <done>No dangling pycopg.aliases xref remains; WR-01/WR-03 are resolved or documented-as-deferred; lint is clean.</done>
</task>

<task type="auto">
  <name>Task 5: Measure the 4 quality gates and confirm green baseline before handoff</name>
  <files>(measurement only — no source edits unless a gate fails)</files>
  <read_first>
    - pyproject.toml — `[tool.pytest.ini_options]` line 90 `addopts = "-v --cov=pycopg ... --cov-fail-under=94"` (the coverage gate runs on the bare `uv run pytest`; do NOT pass `-o addopts=""` for the coverage gate or the cov plugin is disabled and reports nothing). `[tool.interrogate]` line 106-118 `fail-under = 95`, excludes tests/docs.
    - docs/conf.py — confirm `release = '0.9.0'` (from Task 1) so the Sphinx build renders the new version.
    - 36-CONTEXT.md "Test conventions" and "Gates" — baseline from v0.8.0: coverage 95.11% (target >=94), interrogate 100% (target >=95), Sphinx -W clean, DeprecationWarning green. Default `pycopg_test` DB is BROKEN; for any TARGETED live-DB run use `PGDATABASE=pycopg_test2` and `-o addopts=""`. 2-3 pre-existing flaky full-suite DB tests (fixture-isolation, NOT v0.9.0 code) are NOT blocking.
  </read_first>
  <action>
    Run the four release gates and record each measured value in the SUMMARY. GATE A (coverage): run the bare full suite `uv run pytest` (carries `--cov-fail-under=94`). If it exits non-zero, inspect WHICH tests failed — if they are exactly the 2-3 known fixture-isolation flaky DB tests (e.g. `test_async_transaction_fix`, `test_create_spatial_index_name_parameter`) and NOT in test_database*.py / test_parity.py for the v0.9.0 methods, re-run them in isolation with `-o addopts=""` (and `PGDATABASE=pycopg_test2` if the default DB is broken) to confirm environment flakiness, and read the printed coverage % to confirm >=94. Any failure in the v0.9.0 code path is a real regression — fix it; do NOT lower the threshold. GATE B (docstrings): `uv run interrogate pycopg --fail-under 95 --quiet` must exit 0. GATE C (Sphinx): `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` must exit 0 (warnings-as-errors; this is where any leftover pycopg.aliases xref or docs orphan surfaces). GATE D (DeprecationWarning): `uv run python -W error::DeprecationWarning -c "import pycopg"` must exit 0 (no DeprecationWarning raised on import). Confirm all four are green; this is the gate baseline that 36-02 re-confirms before tagging.
  </action>
  <verify>
    <automated>uv run interrogate pycopg --fail-under 95 --quiet && uv run sphinx-build -W --keep-going -b html docs docs/_build/html && uv run python -W error::DeprecationWarning -c "import pycopg" && echo GATES-BCD-OK</automated>
  </verify>
  <acceptance_criteria>
    - GATE A: `uv run pytest` reports coverage >=94% (any non-zero exit is attributable solely to the known flaky DB tests, confirmed by isolated re-run; no v0.9.0-path failures).
    - GATE B: `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
    - GATE C: `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
    - GATE D: `uv run python -W error::DeprecationWarning -c "import pycopg"` exits 0.
    - All four measured values are recorded in the SUMMARY for 36-02 to re-confirm.
  </acceptance_criteria>
  <done>All 4 gates measure green and their values are recorded; the release content is ready for tagging in 36-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 36 adds NO new runtime attack surface — it bumps a version, edits docs/CHANGELOG, and cleans dangling docstring xrefs. The CRUD/introspection surface was reviewed in Phases 34/35. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | docs/CHANGELOG signature drift | mitigate | D-36-03 guard: every CHANGELOG signature is copied verbatim from the live source files (Task 2 read_first), not from memory. |
| T-36-SC | Tampering | pip installs | n/a | No NEW packages installed; only the existing pycopg is documented/version-bumped (release supply-chain integrity is handled in 36-02 via OIDC trusted publishing — no long-lived token). |
</threat_model>

<verification>
- Version: `grep 'version = "0.9.0"' pyproject.toml`, `grep "release = '0.9.0'" docs/conf.py`, `uv lock --check` exits 0, `__init__.py` not hardcoded.
- CHANGELOG: `## [0.9.0]` present; all 12 method names present; signatures match source.
- Docs: README `(32 methods)`; api-reference rows for all 12 methods; Sphinx -W clean.
- Cleanup: no `pycopg.aliases` token in `pycopg/`; CLAUDE.md `**pycopg v0.9.0**`.
- Gates: coverage >=94, interrogate >=95, Sphinx -W exits 0, `-W error::DeprecationWarning` import exits 0.
</verification>

<success_criteria>
- pyproject.toml @ 0.9.0 (canonical), uv.lock + docs/conf.py @ 0.9.0, __init__.py untouched (D-36-01).
- CHANGELOG [0.9.0] Added-only, all 12 methods, code-exact signatures (D-36-03).
- README counts + CRUD note; docs/api-reference.md, docs/database.md, docs/async-database.md, docs/index.md updated.
- Cosmetic debt cleared (D-36-04): CLAUDE.md version line, no pycopg.aliases xref, WR-01/WR-03 applied-or-deferred.
- All 4 gates measure green and are recorded for 36-02.
</success_criteria>

<output>
Create `.planning/phases/36-release-v090/36-01-SUMMARY.md` when done — include the four measured gate values (coverage %, interrogate %, Sphinx -W result, DeprecationWarning import result) and any WR-01/WR-03 defer note.
</output>
