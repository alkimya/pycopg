---
phase: 36-release-v090
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified: []
autonomous: false
requirements: [REL-09]

must_haves:
  truths:
    - "All 4 quality gates are re-confirmed green immediately before tagging (coverage >=94, interrogate >=95, Sphinx -W clean, no DeprecationWarning on import)"
    - "A local build produces pycopg-0.9.0 wheel + sdist and uv lock --check passes (CI publish gate requirement)"
    - "Tag v0.9.0 is created and pushed, and a GitHub Release for v0.9.0 is published, triggering the OIDC PyPI publish — gated behind explicit human sign-off (irreversible)"
    - "A clean-venv smoke confirms pip install pycopg==0.9.0 resolves and pycopg.__version__ == '0.9.0'"
    - "An import smoke confirms at least one new CRUD method and one new introspection method are present on the public classes"
  artifacts:
    - path: "(git tag v0.9.0)"
      provides: "Release tag that anchors the GitHub Release"
    - path: "(GitHub Release v0.9.0)"
      provides: "Triggers .github/workflows/publish.yml OIDC PyPI publish"
    - path: "(PyPI release pycopg 0.9.0)"
      provides: "Published wheel + sdist installable via pip install pycopg==0.9.0"
  key_links:
    - from: "git tag v0.9.0 + GitHub Release"
      to: ".github/workflows/publish.yml"
      via: "on: release: types: [published] -> uv build -> pypa/gh-action-pypi-publish (OIDC)"
      pattern: "release:|gh release create"
    - from: "pip install pycopg==0.9.0"
      to: "pycopg.__version__"
      via: "importlib.metadata.version('pycopg') resolving the installed wheel"
      pattern: "__version__ == '0.9.0'"
---

<objective>
Ship pycopg v0.9.0: re-confirm the 4 gates one last time, build locally, then (behind a blocking human checkpoint) tag v0.9.0, push, publish the GitHub Release that triggers the OIDC PyPI publish, and finally run a clean-venv smoke proving the published wheel resolves to 0.9.0 with the new surface importable.

This is the release/smoke half of the locked 2-plan split (D-36-02), depends on 36-01 (all content + gate baseline). It contains an IRREVERSIBLE outward-facing step (PyPI publish — a version can never be re-uploaded), so this plan is `autonomous: false` and the publish task is human-gated. No housekeeping lives here — that all stayed in 36-01 (D-36-04).

Purpose: Final phase of the v0.9.0 milestone — package the already-documented, gate-green release and publish it.
Output: tag `v0.9.0`, GitHub Release, published PyPI release, and a passing clean-venv smoke.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-release-v090/36-CONTEXT.md
@.planning/phases/36-release-v090/36-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces release artifacts, not source files:
- The git tag `v0.9.0`.
- The GitHub Release for `v0.9.0` (whose publication triggers `.github/workflows/publish.yml`).
- The published PyPI release `pycopg 0.9.0` (wheel + sdist via OIDC trusted publishing).
A source-grounding pass should expect no new implementation file from this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm all 4 gates green and build pycopg-0.9.0 locally</name>
  <files>(measurement + local build — produces dist/ artifacts, no committed file edits)</files>
  <read_first>
    - .planning/phases/36-release-v090/36-01-SUMMARY.md — the recorded 4-gate baseline values from 36-01. This task re-confirms them on the final tree.
    - pyproject.toml — line 7 must read `version = "0.9.0"` (set by 36-01); `[tool.pytest.ini_options]` line 90 carries `--cov-fail-under=94`; `[tool.interrogate]` `fail-under = 95`.
    - .github/workflows/publish.yml — the CI flow: `uv lock --check` (line 17-18, fails if lockfile stale) -> `uv build` -> upload -> `pypa/gh-action-pypi-publish@release/v1` via OIDC. It triggers `on: release: types: [published]` (and `workflow_dispatch`). Note: the tag alone does NOT publish — publishing the GitHub *Release* does.
    - 36-CONTEXT.md "Gates" + "Test conventions" — targets and the flaky-DB-test caveat (2-3 fixture-isolation tests are not blocking; default `pycopg_test` DB broken -> use `PGDATABASE=pycopg_test2` for any targeted live-DB re-run).
  </read_first>
  <action>
    Re-run the four release gates on the final tree (do not assume 36-01's values still hold). Coverage: `uv run pytest` (carries `--cov-fail-under=94`); interpret any non-zero exit against the known flaky-DB-tests caveat exactly as 36-01 did, confirm coverage >=94. Docstrings: `uv run interrogate pycopg --fail-under 95 --quiet` exits 0. Sphinx: `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0. DeprecationWarning: `uv run python -W error::DeprecationWarning -c "import pycopg"` exits 0. Then confirm `uv lock --check` exits 0 (CI publish gate) and run `uv build` to produce `dist/pycopg-0.9.0-py3-none-any.whl` and `dist/pycopg-0.9.0.tar.gz` for pre-tag verification. Do NOT tag or publish in this task.
  </action>
  <verify>
    <automated>uv run interrogate pycopg --fail-under 95 --quiet && uv run sphinx-build -W --keep-going -b html docs docs/_build/html && uv run python -W error::DeprecationWarning -c "import pycopg" && uv lock --check && uv build && ls dist/pycopg-0.9.0-py3-none-any.whl dist/pycopg-0.9.0.tar.gz && echo BUILD-OK</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` reports coverage >=94% (non-zero exit attributable solely to known flaky DB tests, confirmed by isolated re-run).
    - `uv run interrogate pycopg --fail-under 95 --quiet` exits 0.
    - `uv run sphinx-build -W --keep-going -b html docs docs/_build/html` exits 0.
    - `uv run python -W error::DeprecationWarning -c "import pycopg"` exits 0.
    - `uv lock --check` exits 0.
    - `dist/pycopg-0.9.0-py3-none-any.whl` and `dist/pycopg-0.9.0.tar.gz` exist.
  </acceptance_criteria>
  <done>All 4 gates re-confirmed green, lockfile current, and a local 0.9.0 wheel + sdist built for verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Tag v0.9.0, push, publish GitHub Release (triggers OIDC PyPI publish) — IRREVERSIBLE, human sign-off</name>
  <files>(release VCS actions — git tag, push, GitHub Release; no repo-file edits beyond the already-committed 36-01 changes)</files>
  <what-built>
    All v0.9.0 content is committed (version bump, CHANGELOG [0.9.0], docs, cosmetic-debt cleanup from 36-01). All 4 gates are re-confirmed green and a local `dist/pycopg-0.9.0` wheel + sdist were built (Task 1). `uv lock --check` passes. The release is ready to tag and publish.
  </what-built>
  <read_first>
    - .github/workflows/publish.yml — confirm the trigger is `on: release: types: [published]`. The publish fires when the GitHub *Release* is published, not on raw tag push.
    - CHANGELOG.md — the `## [0.9.0]` section body is the Release notes source.
    - 36-CONTEXT.md D-36-02 — release is human-gated at the irreversible publish step.
  </read_first>
  <how-to-verify>
    This is the IRREVERSIBLE publish step. A version can NEVER be re-uploaded to PyPI. Before approving, confirm:
    1. `pyproject.toml` reads `version = "0.9.0"` and `uv lock --check` exits 0.
    2. All 4 gates are green (from Task 1): coverage >=94, interrogate >=95, Sphinx -W clean, DeprecationWarning import clean.
    3. The `dist/pycopg-0.9.0-py3-none-any.whl` and `dist/pycopg-0.9.0.tar.gz` artifacts were produced locally.
    4. CHANGELOG `## [0.9.0]` content is correct and Added-only.
  </how-to-verify>
  <action>
    On "approved" ONLY, perform the release sequence: commit the version bump + CHANGELOG + docs if not already committed; `git tag v0.9.0`; `git push origin <current-branch>` and `git push origin v0.9.0`; create and publish the GitHub Release for tag `v0.9.0` via `gh release create v0.9.0 --title "v0.9.0"` with the CHANGELOG [0.9.0] section as the body (e.g. `--notes-from-tag` or an explicit notes file). Publishing the Release triggers `.github/workflows/publish.yml` (`on: release: types: [published]`) -> `uv lock --check` -> `uv build` -> `pypa/gh-action-pypi-publish@release/v1` via OIDC trusted publishing (no long-lived token). Do NOT proceed without explicit "approved".
  </action>
  <verify>
    <automated>git tag --list v0.9.0 | grep -q v0.9.0 && echo TAG-PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - After approval: `git tag` lists `v0.9.0`.
    - The tag and current branch are pushed to origin.
    - A GitHub Release for `v0.9.0` is published, triggering the publish workflow.
    - The publish workflow run completes successfully (wheel + sdist uploaded to PyPI via OIDC).
  </acceptance_criteria>
  <done>v0.9.0 is tagged, pushed, the GitHub Release is published, and the OIDC publish workflow has uploaded the wheel + sdist to PyPI.</done>
  <resume-signal>Type "approved" to tag + push + publish the GitHub Release (this publishes to PyPI and is IRREVERSIBLE), or describe blocking issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Clean-venv smoke — pip install pycopg==0.9.0 + version + new-surface import</name>
  <files>(clean-venv smoke — no repo edits)</files>
  <read_first>
    - pycopg/__init__.py — lines 39-44: `__version__` resolves via `importlib.metadata.version("pycopg")`; the installed wheel must report `0.9.0`.
    - pycopg/database.py — the 7 CRUD methods (`upsert`, `count`, etc.) are attributes of `Database` (and `AsyncDatabase`).
    - pycopg/schema.py — `SchemaAccessor.describe` (and the other 4 introspection methods) — used for the introspection import-smoke.
    - 36-CONTEXT.md — smoke verifies `pycopg.__version__ == "0.9.0"` resolves correctly post-build (no live DB needed for import smoke).
  </read_first>
  <action>
    In a FRESH virtual environment created outside the project (e.g. `python -m venv /tmp/pycopg-smoke && /tmp/pycopg-smoke/bin/pip install pycopg==0.9.0`), wait for the new release to be available on PyPI if needed, then run two import smokes against the installed wheel (NOT the working-tree source): (1) version smoke — `python -c "import pycopg; assert pycopg.__version__ == '0.9.0'"` exits 0; (2) surface smoke — assert at least one new CRUD method and one new introspection method exist on the public classes: `from pycopg import Database; assert hasattr(Database, 'upsert') and hasattr(Database, 'count')` and `from pycopg.schema import SchemaAccessor; assert hasattr(SchemaAccessor, 'describe')`. No live DB connection is required for the import smoke. Record the result in the SUMMARY.
  </action>
  <verify>
    <automated>python -m venv /tmp/pycopg-smoke && /tmp/pycopg-smoke/bin/pip install --quiet "pycopg==0.9.0" && /tmp/pycopg-smoke/bin/python -c "import pycopg; assert pycopg.__version__ == '0.9.0'; from pycopg import Database; assert hasattr(Database,'upsert') and hasattr(Database,'count'); from pycopg.schema import SchemaAccessor; assert hasattr(SchemaAccessor,'describe'); print('SMOKE-OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pip install pycopg==0.9.0` resolves from PyPI into a clean venv.
    - `python -c "import pycopg; assert pycopg.__version__ == '0.9.0'"` exits 0.
    - The surface smoke confirms `Database.upsert`, `Database.count`, and `SchemaAccessor.describe` exist on the installed wheel.
    - The smoke result is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The published v0.9.0 wheel installs in a clean venv, reports __version__ == "0.9.0", and exposes the new CRUD + introspection surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> PyPI / GitHub Release | Publishing an artifact to a public index (supply-chain boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02 | Tampering / Supply chain | PyPI publish artifact | mitigate | Publish goes through `pypa/gh-action-pypi-publish@release/v1` using OIDC trusted publishing (no long-lived PyPI token); `uv lock --check` enforces a current lockfile before build; the publish is gated behind a blocking human checkpoint (Task 2). |
| T-36-03 | Repudiation | release provenance | accept | GitHub Release + git tag provide an auditable release record (standard for the project; v0.5.0-v0.8.0 precedent). |
| T-36-SC | Tampering | pip installs | n/a | No NEW packages installed; only the existing pycopg package is built/published. No new attack surface — release phase only. |
</threat_model>

<verification>
- Gates re-confirmed: coverage >=94, interrogate >=95, Sphinx -W exits 0, `-W error::DeprecationWarning` import exits 0; `uv lock --check` exits 0; local `dist/pycopg-0.9.0*` built.
- Release: `git tag` lists `v0.9.0`; tag + branch pushed; GitHub Release published; publish workflow run succeeds.
- Smoke: clean venv `pip install pycopg==0.9.0`; `pycopg.__version__ == "0.9.0"`; `Database.upsert`/`Database.count`/`SchemaAccessor.describe` present.
</verification>

<success_criteria>
- All 4 gates green on the final tree; lockfile current; local 0.9.0 wheel + sdist built.
- v0.9.0 tagged + pushed; GitHub Release published; OIDC PyPI publish triggered and succeeded (human-gated).
- Clean-venv smoke: `pip install pycopg==0.9.0` resolves, `__version__ == "0.9.0"`, new CRUD + introspection surface importable.
</success_criteria>

<output>
Create `.planning/phases/36-release-v090/36-02-SUMMARY.md` when done — include the re-confirmed gate values, the tag/Release/publish-run confirmation, and the clean-venv smoke result.
</output>
