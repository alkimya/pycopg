---
phase: 10-s-curit-r-siduelle-robustesse
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04"]
files_modified:
  - pyproject.toml
  - tests/test_pool.py
  - tests/test_migrations.py
  - tests/test_session_edge_cases.py
autonomous: true
requirements: [SEC-06]
must_haves:
  truths:
    - "Measured total coverage is >= 80 with the full suite green"
    - "pyproject.toml --cov-fail-under is raised from 70 to 80 (ratchet, never lowered) only after measured coverage crosses 80"
    - "Added coverage-fill tests target pool.py / migrations.py / session() and are not fragile"
  artifacts:
    - path: "pyproject.toml"
      provides: "Coverage gate raised to 80"
      contains: "--cov-fail-under=80"
  key_links:
    - from: "pyproject.toml addopts"
      to: "CI tests.yml job"
      via: "--cov-fail-under=80 enforced by uv run pytest in CI"
      pattern: "cov-fail-under=80"
---

<objective>
[BLOCKING] Raise the coverage ratchet from 70 to 80 (D-07), the final gate-flip of Phase 10. This plan runs AFTER all fix+test plans (01-04) have landed. Measure total coverage with the full suite (the new B1/B2/B3/B5 red->green tests already raise it). If coverage is below 80, add TARGETED, non-fragile tests to the easiest/most-critical uncovered code in `pool.py`, `migrations.py`, and the `session()` paths of `database.py`/`async_database.py`. Only once measured coverage actually reaches >= 80 (with the suite green), flip `--cov-fail-under` from 70 to 80 in `pyproject.toml:88`. Never lower the bar; never freeze a threshold that is not actually met. NO refactoring (Phase 12), no fragile tests.

Purpose: Lock in the quality gain from the residual-fix tests with a permanent coverage floor.
Output: Updated `pyproject.toml`; targeted coverage-fill tests as needed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-s-curit-r-siduelle-robustesse/10-CONTEXT.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure coverage and add targeted, non-fragile tests to reach >= 80</name>
  <read_first>
    - pyproject.toml (lines 86-99 — the pytest addopts cov-fail-under=70 and the coverage report config; the omit/exclude rules)
    - tests/test_pool.py (existing pool tests — extend here for pool.py gaps)
    - tests/test_migrations.py (existing migration tests — extend here for migrations.py gaps)
    - tests/test_session_edge_cases.py (existing session tests — extend here for session() gaps)
    - .planning/phases/10-s-curit-r-siduelle-robustesse/10-CONTEXT.md (D-07: tests first, then targeted fill; no refactoring, no fragile tests)
  </read_first>
  <files>tests/test_pool.py, tests/test_migrations.py, tests/test_session_edge_cases.py</files>
  <action>
Run `uv run pytest --cov=pycopg --cov-report=term-missing -q` (do NOT trip the existing 70 gate; the suite includes the new tests from Plans 01-04). Read the term-missing report to find the lowest-hanging uncovered lines concentrated in `pool.py`, `migrations.py`, and the `session()`/`cursor()` paths of `database.py`/`async_database.py`. Add targeted tests to the EXISTING files `tests/test_pool.py`, `tests/test_migrations.py`, and `tests/test_session_edge_cases.py` (do not create new modules unless a clearly distinct concern warrants it) that exercise those uncovered branches via the established mock/real-DB patterns already in those files. Prioritize meaningful behavior assertions over line-touching: each added test must assert an observable behavior (a return value, a raised exception, a method called), never just instantiate to bump the counter. Do NOT refactor production code (Phase 12 is the only place restructuring is allowed). Do NOT write timing-dependent, ordering-dependent, or over-mocked fragile tests. Stop adding tests once measured total coverage is >= 80 with the full suite green. If 80 cannot be reached with non-fragile tests against the current code without refactoring, STOP and report the measured ceiling and the specific hard-to-mock lines (subprocess/network I/O) blocking it rather than writing fragile tests or lowering scope — coverage report caps justify deferral, never fragile tests.
  </action>
  <acceptance_criteria>
    - `uv run pytest --cov=pycopg --cov-report=term-missing -q` reports TOTAL coverage >= 80 with the suite passing (capture the TOTAL line in the SUMMARY).
    - Every added test asserts an observable behavior (return value / exception / call), verifiable by reading the test bodies — no instantiation-only tests.
    - No production file under `pycopg/` is modified by this task (`git diff --name-only pycopg/` is empty for this plan) — coverage is raised by tests only, no refactoring.
    - Added tests live in the existing `tests/test_pool.py` / `tests/test_migrations.py` / `tests/test_session_edge_cases.py` (or a justified new file noted in the SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest --cov=pycopg --cov-report=term-missing -q</automated>
  </verify>
  <done>Measured total coverage is >= 80 with the full suite green, achieved with targeted non-fragile tests and zero production refactoring.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Flip --cov-fail-under from 70 to 80 (ratchet) only once 80 is measured</name>
  <read_first>
    - pyproject.toml (line 88 — `addopts = "... --cov-fail-under=70"`; this is the single line to change)
  </read_first>
  <files>pyproject.toml</files>
  <action>
ONLY after Task 1 has confirmed measured total coverage >= 80 with the suite green: change `--cov-fail-under=70` to `--cov-fail-under=80` on the `addopts` line at `pyproject.toml:88`. Change nothing else on that line (keep `-v --cov=pycopg --cov-report=term-missing --cov-report=html`). Then run the full suite under the new gate to confirm it passes. This is the ratchet: 80 is the new permanent floor; it must never be lowered in this or later phases. If, when this task runs, coverage is NOT >= 80, do NOT change the threshold — report the shortfall and the blocking lines instead (the gate is flipped only when the bar is genuinely cleared).
  </action>
  <acceptance_criteria>
    - `grep 'cov-fail-under' pyproject.toml` shows `--cov-fail-under=80` (and no `--cov-fail-under=70` remains: `grep -c "cov-fail-under=70" pyproject.toml` returns 0).
    - `uv run pytest` exits 0 under the new 80 gate (coverage >= 80 actually met, not just the threshold lowered/raised on paper).
    - The rest of the `addopts` line is unchanged (`grep -c "\-\-cov=pycopg" pyproject.toml` still returns 1).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'cov-fail-under=80' pyproject.toml && test "$(grep -c 'cov-fail-under=70' pyproject.toml)" = "0" && uv run pytest</automated>
  </verify>
  <done>The coverage gate is raised to 80 and the full suite passes under it; 70 no longer appears; ratchet locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate -> merge | The coverage gate is the automated guard that future changes do not silently drop tested behavior; a paper-only threshold (raised without real coverage) is a false guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Repudiation (Process integrity) | coverage ratchet (D-07) | mitigate | Flip gate to 80 ONLY after measured coverage >= 80 with suite green (Task 2 gated on Task 1's measurement); never lower the bar |
| T-10-06-FT | Tampering (Test reliability) | added coverage-fill tests | mitigate | Tests assert observable behavior, no instantiation-only/fragile tests; refactoring deferred to Phase 12 |
</threat_model>

<verification>
- `uv run pytest` exits 0 under `--cov-fail-under=80`.
- `grep -c "cov-fail-under=70" pyproject.toml` returns 0; `grep -q "cov-fail-under=80" pyproject.toml` succeeds.
- All Plan 01-04 fix tests still green within the full suite.
</verification>

<success_criteria>
- Measured total coverage >= 80, full suite green.
- `--cov-fail-under` raised 70 -> 80 in pyproject.toml; ratchet locked, never lowered.
- Coverage raised by targeted non-fragile tests only; no production refactoring.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)

New production symbols: NONE (test-only + config change; no production code touched — refactoring is Phase 12).
New test files/functions:
- Targeted coverage-fill tests added to `tests/test_pool.py`, `tests/test_migrations.py`, `tests/test_session_edge_cases.py` (names assigned at execution per the term-missing report).
Config change (the raised threshold value, listed so source-grounding excludes it from drift verification):
- `pyproject.toml` addopts `--cov-fail-under` changed from `70` to `80`.
</artifacts_produced>

<output>
Create `.planning/phases/10-s-curit-r-siduelle-robustesse/10-05-SUMMARY.md` when done
</output>
