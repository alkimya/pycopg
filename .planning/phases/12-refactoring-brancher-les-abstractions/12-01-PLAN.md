---
phase: 12-refactoring-brancher-les-abstractions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/base.py
  - pycopg/database.py
  - pycopg/async_database.py
autonomous: true
requirements: [REF-03]
must_haves:
  truths:
    - "build_pg_dump_cmd, build_pg_restore_cmd, build_role_options exist as module-level functions in base.py taking explicit args (no self)"
    - "Database.pg_dump / pg_restore / create_role build argv via the shared builders; env-dict + subprocess stay in the method"
    - "AsyncDatabase.pg_dump / pg_restore / create_role call the SAME builders, keeping their own async I/O shell"
    - "PGPASSWORD never enters any builder body; the role password value never enters build_role_options"
    - "Full pytest suite stays green (behavior unchanged)"
  artifacts:
    - path: "pycopg/base.py"
      provides: "3 module-level pure builders"
      contains: "def build_pg_dump_cmd("
    - path: "pycopg/database.py"
      provides: "pg_dump/pg_restore/create_role rewired to the shared builders"
      contains: "build_pg_dump_cmd("
    - path: "pycopg/async_database.py"
      provides: "async pg_dump/pg_restore/create_role rewired to the shared builders"
      contains: "build_pg_dump_cmd("
  key_links:
    - from: "pycopg/database.py"
      to: "pycopg.base.build_pg_dump_cmd"
      via: "import + call returning argv list"
      pattern: "build_pg_dump_cmd\\("
    - from: "pycopg/async_database.py"
      to: "pycopg.base.build_pg_dump_cmd"
      via: "same shared builder, async I/O shell retained"
      pattern: "build_pg_dump_cmd\\("
---

<objective>
Extract three pure, stateless argv/option builders into base.py as module-level functions and rewire sync + async backup/role methods to call them (REF-03, D-03/D-04). DB-free, dependency-free foundation slice: the only net-new symbols of the phase, coverage fuel for REF-05, touched before any inheritance/wiring slice rearranges these files.

Purpose: Kill the byte-identical sync/async cmd-build duplication (single source of truth) and create 100%-coverable pure functions, keeping every secret out of the builders.
Output: build_pg_dump_cmd, build_pg_restore_cmd, build_role_options in base.py; six call sites (3 sync + 3 async) rewired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-refactoring-brancher-les-abstractions/12-CONTEXT.md
@.planning/phases/12-refactoring-brancher-les-abstractions/12-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
</context>

<artifacts_produced>
The ONLY net-new symbols of Phase 12:

- pycopg/base.py::build_pg_dump_cmd(...) — module-level, returns list[str] argv.
- pycopg/base.py::build_pg_restore_cmd(...) — module-level, returns list[str] argv.
- pycopg/base.py::build_role_options(...) — module-level, returns list[str] of role options.

No new test files here (builder tests land in 12-04). Every other Phase 12 change is DELETE or REWIRE.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add three module-level pure builders to base.py</name>
  <read_first>
    - pycopg/base.py (full; QueryMixin._build_* ~lines 69-173 = pure-builder analog: validation first, explicit args, return value, no I/O, no self)
    - pycopg/database.py lines 1975-2062 (create_role options), 2381-2468 (pg_dump argv), 2470-2566 (pg_restore argv) — SOURCE to extract
    - 12-PATTERNS.md section "pycopg/base.py — add 3 module-level builders"
    - pycopg/utils.py (validate_timestamp — keep inside build_role_options)
  </read_first>
  <action>
    Add three module-level functions at the END of base.py (after SessionMixin), in the QueryMixin pure-builder shape (validation first, explicit args, return a value, zero I/O, no self). NO leading underscore (D-03).

    build_pg_dump_cmd(host, port, user, database, output_file, format="custom", schema_only=False, data_only=False, tables=None, exclude_tables=None, schemas=None, compress=6, jobs=1) -> list[str]: replicate EXACTLY the argv lines in Database.pg_dump (cmd from ["pg_dump"] through the final extend of -f and str(output_file)), substituting explicit host/port/user/database for self.config values. output_file -> Path -> str. Preserve format_map, the gate (compress and format=="custom"), the gate (jobs>1 and format=="directory"), the -t/-T/-n loops verbatim. Return cmd. No env, no subprocess, no PGPASSWORD (D-04).

    build_pg_restore_cmd(host, port, user, database, input_file, clean=False, if_exists=True, create=False, data_only=False, schema_only=False, tables=None, schemas=None, jobs=1, no_owner=False, no_privileges=False) -> list[str]: replicate Database.pg_restore argv (cmd from ["pg_restore"] through final append of str(input_file)), explicit params. The .sql/non-exists early-return-to-_psql_restore branch and env+subprocess STAY in the method. Return cmd.

    build_role_options(login=True, superuser=False, createdb=False, createrole=False, inherit=True, replication=False, connection_limit=-1, password=None, valid_until=None) -> list[str]: replicate create_role options logic (LOGIN/NOLOGIN, SUPERUSER, CREATEDB, CREATEROLE, NOINHERIT when not inherit, REPLICATION, CONNECTION LIMIT n when connection_limit!=-1, the literal placeholder "PASSWORD %s" when password truthy, VALID UNTIL valid_until when set). Keep validate_timestamp(valid_until) INSIDE the builder. password is a truthiness flag ONLY (decides whether to append the literal "PASSWORD %s"); the secret value is NEVER stored in or returned from the builder (D-04). Return options.

    Do NOT force-merge alter_role's divergent on/off form into build_role_options (covers create_role only). Leave alter_role untouched. Copy docstrings in existing Google style (numpydoc = Phase 13).
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run ruff check pycopg/base.py && uv run python -c "from pycopg.base import build_pg_dump_cmd,build_pg_restore_cmd,build_role_options"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '^def build_pg_dump_cmd(\|^def build_pg_restore_cmd(\|^def build_role_options(' pycopg/base.py returns 3
    - none of the three functions take a self parameter
    - grep 'PGPASSWORD' pycopg/base.py returns nothing (exit 1)
    - grep 'subprocess\|os.environ\|create_subprocess' pycopg/base.py returns nothing
    - uv run ruff check pycopg/base.py exits 0
  </acceptance_criteria>
  <done>Three pure module-level builders in base.py: importable, no self, no I/O, no secret; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire sync + async pg_dump / pg_restore / create_role to the shared builders</name>
  <read_first>
    - pycopg/base.py (the 3 builders just added — confirm arg names/order)
    - pycopg/database.py lines 1975-2062, 2381-2566 (inline build to replace; env-dict + subprocess.run lines STAY)
    - pycopg/async_database.py lines 1391-1494 (async create_role options), 2344-2438 (async pg_dump — uses asyncio.create_subprocess_exec, NOT subprocess.run), 2439+ (async pg_restore)
    - 12-PATTERNS.md sections "Async symmetry" and "Pure-builder split"
    - tests/test_database.py, tests/test_async_database.py, tests/test_subprocess_env.py (do NOT modify; safety net per D-06)
  </read_first>
  <action>
    pycopg/database.py: add "from pycopg.base import build_pg_dump_cmd, build_pg_restore_cmd, build_role_options" (CONVENTIONS.md import order). Replace pg_dump inline argv (~2424-2459) with build_pg_dump_cmd(host=self.config.host, port=self.config.port, user=self.config.user, database=self.config.database, output_file=output_file, format=format, schema_only=schema_only, data_only=data_only, tables=tables, exclude_tables=exclude_tables, schemas=schemas, compress=compress, jobs=jobs). KEEP env-dict + subprocess.run + returncode check verbatim (D-04). Same for pg_restore (keep the .sql/non-exists early-return-to-_psql_restore branch and env+subprocess). create_role: replace the inline options=[] block (~2025-2050) with options = build_role_options(login=..., superuser=..., createdb=..., createrole=..., inherit=..., replication=..., connection_limit=..., password=password, valid_until=valid_until); keep options_str join and the parameterized execute (password value still bound by cur.execute(..., [password])). Preserve validate_identifier(name).

    pycopg/async_database.py: add the same import. Rewire async pg_dump, pg_restore, create_role to the SAME builders with identical args. CRITICAL: keep each async I/O shell unchanged — async uses asyncio.create_subprocess_exec(*cmd, env=env, ...) + await proc.communicate(), NOT subprocess.run. Only cmd-assembly is shared (D-04). If any async argv build diverges from sync, NOTE it in SUMMARY rather than alter behavior (verified byte-identical on disk).

    No signature/return-shape/behavior change (D-06). A red existing test = REAL regression — STOP, do not edit the test.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run ruff check pycopg && uv run pytest tests/test_database.py tests/test_async_database.py tests/test_subprocess_env.py tests/test_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - grep 'build_pg_dump_cmd(' pycopg/database.py and pycopg/async_database.py each match >= 1
    - build_pg_restore_cmd( and build_role_options( present in both files
    - subprocess.run still in database.py; asyncio.create_subprocess_exec still in async_database.py
    - PGPASSWORD still in database.py/async_database.py method bodies, NOT in base.py
    - uv run pytest tests/test_database.py tests/test_async_database.py tests/test_subprocess_env.py tests/test_parity.py exits 0
  </acceptance_criteria>
  <done>Six call sites build argv/options via shared base.py builders; I/O shells unchanged; targeted suites + parity green; behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process -> pg_dump/pg_restore/psql subprocess | PGPASSWORD crosses into child env; argv must never carry it |
| caller -> CREATE ROLE SQL | role password is bound as a parameter, never interpolated into SQL text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | build_pg_dump_cmd / build_pg_restore_cmd argv | mitigate | Builders return argv ONLY; PGPASSWORD stays in the method env dict (D-04). Grep: no PGPASSWORD/subprocess/os.environ in base.py. |
| T-12-02 | Information Disclosure | build_role_options | mitigate | Builder appends only the literal "PASSWORD %s"; secret value bound via parameterized execute in the method (D-04). |
| T-12-03 | Tampering / Injection | role name + valid_until into CREATE ROLE | mitigate | Preserve validate_identifier(name) in method and validate_timestamp(valid_until) in builder (PATTERNS.md "Validation preservation"). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs this phase (behavior-preserving refactor; zero external deps). |
</threat_model>

<verification>
- base.py has no subprocess/env/os.environ/PGPASSWORD token (pure builders).
- Sync subprocess.run and async asyncio.create_subprocess_exec both retained at call sites.
- Phase-10 validation preserved on every rewired path.
- Targeted suites + test_parity green (D-06). A red test = real regression, STOP.
</verification>

<success_criteria>
- REF-03 satisfied: three pure module-level builders in base.py, unit-testable without a DB.
- Six call sites consume the shared builders; zero API/signature/return-shape/behavior change.
- No secret transits any pure builder.
</success_criteria>

<output>
Create .planning/phases/12-refactoring-brancher-les-abstractions/12-01-SUMMARY.md when done.
</output>
