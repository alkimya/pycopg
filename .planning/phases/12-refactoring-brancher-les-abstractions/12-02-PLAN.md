---
phase: 12-refactoring-brancher-les-abstractions
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - pycopg/database.py
  - pycopg/async_database.py
  - pycopg/queries.py
autonomous: true
requirements: [REF-02, REF-04]
must_haves:
  truths:
    - "Database and AsyncDatabase inherit (DatabaseBase, QueryMixin); concrete from_env/from_url/__repr__ deleted from both subclasses"
    - "Each subclass keeps its own __init__ calling super().__init__(config) then setting engine attrs"
    - "Database.from_env / from_url still return the correct subclass (cls(...) resolution), behavior unchanged"
    - "TABLE_INFO_SIMPLE and LIST_ROLES_SIMPLE removed from queries.py (grep-confirmed zero refs)"
    - "Full pytest suite + test_parity stay green"
  artifacts:
    - path: "pycopg/database.py"
      provides: "class Database(DatabaseBase, QueryMixin) with factories inherited"
      contains: "class Database(DatabaseBase, QueryMixin)"
    - path: "pycopg/async_database.py"
      provides: "class AsyncDatabase(DatabaseBase, QueryMixin) with factories inherited"
      contains: "class AsyncDatabase(DatabaseBase, QueryMixin)"
    - path: "pycopg/queries.py"
      provides: "orphan *_SIMPLE constants removed"
  key_links:
    - from: "pycopg/database.py::Database"
      to: "pycopg.base.DatabaseBase"
      via: "class inheritance + super().__init__(config)"
      pattern: "class Database\\(DatabaseBase, QueryMixin\\)"
    - from: "pycopg/async_database.py::AsyncDatabase"
      to: "pycopg.base.DatabaseBase"
      via: "class inheritance + super().__init__(config)"
      pattern: "class AsyncDatabase\\(DatabaseBase, QueryMixin\\)"
---

<objective>
Adopt base.py: make Database and AsyncDatabase inherit (DatabaseBase, QueryMixin), delete the duplicated concrete from_env/from_url/__repr__ (now inherited), and remove the orphan *_SIMPLE constants from queries.py (REF-02/D-01, REF-04/D-05). Depends on 12-01 (the base.py builders must already exist so the module is in its final shape before subclasses route through it).

Purpose: Collapse the inheritance duplication onto the socle (single source of factories + repr) and delete grep-proven-dead SQL constants. Behavior-preserving (D-06).
Output: both subclasses inherit the base; from_env/from_url/__repr__ live only on DatabaseBase; queries.py loses two dead constants.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-refactoring-brancher-les-abstractions/12-CONTEXT.md
@.planning/phases/12-refactoring-brancher-les-abstractions/12-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_produced>
This plan produces NO net-new symbols — it is pure inheritance collapse + dead-constant deletion. The 3 builders are owned by 12-01.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Collapse Database + AsyncDatabase onto (DatabaseBase, QueryMixin)</name>
  <read_first>
    - pycopg/base.py lines 18-59 (DatabaseBase: __init__, from_env, from_url, __repr__ — the canonical versions to inherit)
    - pycopg/database.py lines 51, 85-124, 2731 (class decl; __init__; from_env; from_url; __repr__ — current concrete copies to delete)
    - pycopg/async_database.py lines 50, 73-105, 2784 (class decl; __init__; from_env; from_url; __repr__; KEEP the async_engine property ~83-90 untouched)
    - 12-PATTERNS.md section "Inheritance collapse (DatabaseBase)"
    - tests/test_base.py, tests/test_config.py, tests/test_parity.py (safety net — do NOT modify)
  </read_first>
  <action>
    Confirm the base factories are byte-identical to the concrete ones before deleting (compare base.py:33-59 vs database.py:96-124,2731; async equivalents). Then:

    pycopg/database.py: change "class Database:" (line 51) to "class Database(DatabaseBase, QueryMixin):". Ensure "from pycopg.base import DatabaseBase, QueryMixin" is imported (add if absent; merge with the 12-01 builder import line, respecting CONVENTIONS.md order). DELETE the concrete from_env (~95-110), from_url (~111-124), and __repr__ (~2731+) — they become inherited. The "-> Database" return annotations vanish with them (base uses cls(...), so the right subclass is returned, D-01). KEEP __init__ (~85-93) but change its body to call super().__init__(config) (which sets self.config) then keep the subclass-specific attrs self._engine = None and self._session_conn = None. Do not touch any other method.

    pycopg/async_database.py: change "class AsyncDatabase:" (line 50) to "class AsyncDatabase(DatabaseBase, QueryMixin):" and import DatabaseBase, QueryMixin. DELETE concrete from_env (~93), from_url (~105), __repr__ (~2784). KEEP __init__ (~73-81): super().__init__(config) then self._session_conn = None / self._async_engine = None. KEEP the async_engine property (~83-90) untouched (async-only by design; test_parity ASYNC_ONLY).

    MRO: single base each today, no conflict. Exported surface unchanged (__init__.py exports the class names). No behavior change (D-06). A red test = real regression, STOP.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run ruff check pycopg && uv run pytest tests/test_base.py tests/test_config.py tests/test_database.py tests/test_async_database.py tests/test_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'class Database(DatabaseBase, QueryMixin)' pycopg/database.py returns 1
    - grep -c 'class AsyncDatabase(DatabaseBase, QueryMixin)' pycopg/async_database.py returns 1
    - grep -c 'def from_env' pycopg/database.py returns 0; grep -c 'def from_url' pycopg/database.py returns 0; grep -c 'def __repr__' pycopg/database.py returns 0
    - grep -c 'def from_env' pycopg/async_database.py returns 0; grep -c 'def from_url' pycopg/async_database.py returns 0; grep -c 'def __repr__' pycopg/async_database.py returns 0
    - grep 'super().__init__(config)' present in both files; async_engine property still present in async_database.py
    - uv run python -c "from pycopg import Database, AsyncDatabase; print(repr(Database.from_url('postgresql://u:p@h:5432/d')))" exits 0 and prints a Database(...) repr
    - targeted suites + test_parity exit 0
  </acceptance_criteria>
  <done>Both subclasses inherit (DatabaseBase, QueryMixin); concrete factories/repr deleted; __init__ calls super then sets engines; behavior preserved; suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphan *_SIMPLE constants from queries.py</name>
  <read_first>
    - pycopg/queries.py lines 41-54 (TABLE_INFO canonical), 63-74 (TABLE_INFO_SIMPLE to delete), 162-175 (LIST_ROLES canonical), 177-188 (LIST_ROLES_SIMPLE to delete)
    - 12-PATTERNS.md section "pycopg/queries.py — remove orphan *_SIMPLE constants"
  </read_first>
  <action>
    First re-confirm dead (D-07: only grep-proven-dead deletion): run grep across pycopg/ and tests/ for TABLE_INFO_SIMPLE and LIST_ROLES_SIMPLE; both must appear ONLY at their own definition in queries.py. If either has a reference elsewhere, STOP and report — do not delete. Once confirmed, delete the TABLE_INFO_SIMPLE block (and its "# Simplified version for AsyncDatabase" comment) and the LIST_ROLES_SIMPLE block (and its comment). No other edit to queries.py. The canonical TABLE_INFO / LIST_ROLES are untouched.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && ! grep -rq 'TABLE_INFO_SIMPLE\|LIST_ROLES_SIMPLE' pycopg/ tests/ && uv run python -c "from pycopg import queries; queries.TABLE_INFO; queries.LIST_ROLES"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn 'TABLE_INFO_SIMPLE\|LIST_ROLES_SIMPLE' pycopg/ tests/ returns nothing (exit 1)
    - queries.TABLE_INFO and queries.LIST_ROLES still import and resolve
    - uv run ruff check pycopg/queries.py exits 0
  </acceptance_criteria>
  <done>Both orphan *_SIMPLE constants removed after grep-confirmation; canonical constants intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> Config.from_url / from_env | connection string (may contain a password) is parsed by the inherited factory; no change to handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | inheritance collapse (MRO / factory resolution) | mitigate | Verify base factories byte-identical before deleting concretes; cls(...) returns the right subclass; test_parity + factory smoke test confirm behavior unchanged. |
| T-12-05 | Information Disclosure | deleting *_SIMPLE constants | accept | Pure deletion of grep-proven-dead SQL text; no runtime path touched; net-safe (Phase-11 aligned async to the rich form). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs this phase. |
</threat_model>

<verification>
- Both classes inherit (DatabaseBase, QueryMixin); no concrete from_env/from_url/__repr__ remain in subclasses.
- Factory smoke test returns the correct subclass repr (cls(...) resolution intact).
- *_SIMPLE constants grep-clean; canonical TABLE_INFO/LIST_ROLES intact.
- Suites + test_parity green (D-06).
</verification>

<success_criteria>
- REF-02 (D-01) inheritance collapse done; duplicated factories/repr removed.
- REF-04 (D-05) orphan *_SIMPLE constants removed.
- Zero API/signature/return-shape/behavior change.
</success_criteria>

<output>
Create .planning/phases/12-refactoring-brancher-les-abstractions/12-02-SUMMARY.md when done.
</output>
