---
phase: 12-refactoring-brancher-les-abstractions
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - pycopg/database.py
  - pycopg/async_database.py
autonomous: true
requirements: [REF-01, REF-02, REF-04]
must_haves:
  truths:
    - "The ~25 remaining inline SQL strings are replaced by queries.* constants (single source of truth)"
    - "Async table_info / list_roles route through canonical queries.TABLE_INFO / queries.LIST_ROLES (D-05)"
    - "insert_many / upsert_many (sync and async) build the batch INSERT via QueryMixin._build_batch_insert_sql (D-02)"
    - "Stale 'Phase 3' comments and grep-proven dead code (unread stdout, no-op try/except: raise) removed (REF-04)"
    - "All validate_* calls preserved on every rewired path; full pytest suite + test_parity stay green"
  artifacts:
    - path: "pycopg/database.py"
      provides: "inline SQL wired to queries.* + batch insert via _build_batch_insert_sql"
      contains: "queries."
    - path: "pycopg/async_database.py"
      provides: "inline SQL wired to queries.* (incl. table_info/list_roles) + batch insert via _build_batch_insert_sql"
      contains: "queries.TABLE_INFO"
  key_links:
    - from: "pycopg/database.py"
      to: "pycopg.queries.*"
      via: "self.execute(queries.NAME ...)"
      pattern: "queries\\.[A-Z_]+"
    - from: "pycopg/database.py::insert_many"
      to: "QueryMixin._build_batch_insert_sql"
      via: "inherited builder call"
      pattern: "_build_batch_insert_sql"
---

<objective>
Wire the existing abstractions into the hot paths: replace the ~25 remaining inline SQL strings with queries.* constants (REF-01), route async table_info/list_roles through the canonical constants (D-05), build the batch INSERT in insert_many/upsert_many via the inherited QueryMixin._build_batch_insert_sql (REF-02/D-02), and remove the remaining grep-proven dead code (REF-04). Depends on 12-02 (subclasses must already inherit QueryMixin so _build_batch_insert_sql is available; same files, so serialized after the collapse).

Purpose: Make queries.py the single source of SQL truth, use the batch-insert builder the milestone names, and clean the residual dead code — all without changing behavior (D-06).
Output: inline SQL gone in favor of constants; batch-insert routed through the mixin; stale comments / unread stdout / no-op re-raise removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-refactoring-brancher-les-abstractions/12-CONTEXT.md
@.planning/phases/12-refactoring-brancher-les-abstractions/12-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
</context>

<artifacts_produced>
NO net-new symbols — pure rewire (inline SQL -> constants, batch INSERT -> existing builder) + dead-code removal.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Wire inline SQL to queries.* + route batch INSERT through _build_batch_insert_sql</name>
  <read_first>
    - pycopg/queries.py (full list of constant names; canonical TABLE_INFO/LIST_ROLES; the {where_clause} slot in LIST_ROLES line 173 and LIST_GEOMETRY_COLUMNS line ~246)
    - pycopg/base.py lines 96-131 (_build_batch_insert_sql signature/return: returns (sql, params))
    - pycopg/database.py: existing queries. call sites 1018,1031,1046 (the wiring pattern); table_info ~1083; list_roles ~2089-2112; insert_many 471-513; upsert_many 515-558
    - pycopg/async_database.py: existing queries. call sites ~659,672,687; table_info ~690-716; list_roles ~1369-1385; insert_many ~2039; upsert_many ~2083
    - 12-PATTERNS.md sections "Constant-wiring pattern (REF-01)" and "SQL-constant wiring"
    - pycopg/utils.py (validate_* — must stay on every rewired path)
  </read_first>
  <action>
    REF-01 wiring (both files): for each remaining inline SQL string, replace the f-string/triple-quote with the canonical queries.<NAME> constant. Two shapes: (1) param-only -> use directly as database.py:1018 already does (self.execute(queries.NAME, [...])); (2) constants carrying a {where_clause} slot (LIST_ROLES, LIST_GEOMETRY_COLUMNS) -> keep the existing where_clause = "" if include_system else "WHERE rolname NOT LIKE 'pg_%'" line and use queries.NAME.format(where_clause=where_clause). Match each inline literal to its canonical constant by content; the byte text must be equivalent (behavior-preserving). PRESERVE every validate_identifier(s)/validate_* call — do NOT strip validation when swapping to a constant (Phase-10 carry-over, PATTERNS.md "Validation preservation"). It is Claude's discretion which precise inline strings map to which constant (CONTEXT.md), but do not invent new SQL — only route to constants whose text matches.

    D-05 (async): async table_info (~690-716) currently inlines the full TABLE_INFO form -> route to queries.TABLE_INFO. async list_roles (~1369-1385) inlines the full LIST_ROLES form -> route to queries.LIST_ROLES.format(where_clause=where_clause). Both already produce the post-Phase-11 rich shape, so net-safe; test_parity asserts the full field set.

    D-02 batch INSERT (both files): in insert_many, replace the manual cols_str/placeholders/sql assembly with sql, params = self._build_batch_insert_sql(table, columns, rows, schema, on_conflict) (inherited from QueryMixin via 12-02), then execute. NOTE: the current sync insert_many uses execute_many with a params-per-row sequence and a single-row placeholder template; _build_batch_insert_sql produces a single multi-VALUES statement with a flat params list — adopt the builder's (sql, params) and call self.execute(sql, params) (NOT execute_many), since that is what the builder is shaped for. Verify row-count return semantics are preserved (rowcount of the single multi-row INSERT). upsert_many already delegates to insert_many via on_conflict — keep that delegation; it inherits the builder routing automatically. If adopting the builder would change the return value or behavior for any case (e.g. empty rows early-return), preserve the existing guard (if not rows: return 0). Do the same in async insert_many/upsert_many against the async execute.

    No signature/return-shape/behavior change (D-06). A red existing test = REAL regression — STOP, do not edit the test.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run ruff check pycopg && uv run pytest tests/test_database.py tests/test_async_database.py tests/test_parity.py tests/test_sql_injection.py -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'queries\.' pycopg/database.py is substantially higher than the pre-phase baseline of 3 (expect roughly 15+; exact count is Claude's discretion)
    - grep -c 'queries\.' pycopg/async_database.py likewise rises well above 3
    - grep 'queries.TABLE_INFO' pycopg/async_database.py matches (async table_info routed); grep 'queries.LIST_ROLES' pycopg/async_database.py matches
    - grep '_build_batch_insert_sql' pycopg/database.py and pycopg/async_database.py each match >= 1
    - grep -c 'validate_identifier' pycopg/database.py is >= its pre-phase count (no validation dropped)
    - uv run pytest tests/test_database.py tests/test_async_database.py tests/test_parity.py tests/test_sql_injection.py exits 0
  </acceptance_criteria>
  <done>Inline SQL routed to queries.* constants; async table_info/list_roles on canonical constants; insert_many/upsert_many (sync+async) use _build_batch_insert_sql; validation preserved; suites + parity + injection tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove grep-proven dead code (Phase-3 comments, unread stdout, no-op try/except)</name>
  <read_first>
    - pycopg/async_database.py lines 1891, 1981 (stale "available in Phase 3" comments), 2434/2537/2569 (stdout, stderr = await proc.communicate() where stdout is unread), 2653-2654 (bare try/except Exception: raise)
    - pycopg/database.py and pycopg/async_database.py: re-grep for 'import re' and bare 'raise' before touching
    - 12-CONTEXT.md D-07 (NO speculative deletion — only grep-proven-dead/no-op)
  </read_first>
  <action>
    REF-04, with D-07 discipline (delete ONLY grep-proven-dead/no-op; if anything looks dead but has a non-obvious ref, LEAVE it and note in SUMMARY):

    1. Stale comments: delete the two "Note: Requires add_primary_key (available in Phase 3)." lines (async_database.py ~1891, ~1981). add_primary_key shipped in Phase 11, so the comments are stale.

    2. Unread stdout: at the three async sites where "stdout, stderr = await proc.communicate()" is followed by use of only stderr (pg_dump ~2434, pg_restore ~2537, _psql_restore ~2569), replace the unused stdout binding with "_" (i.e. "_, stderr = await proc.communicate()"). Confirm by reading each site that stdout is genuinely unused after the call before changing.

    3. No-op try/except: at async_database.py ~2653 the form "except Exception: raise" with no other handling in that except block is a no-op re-raise — remove the redundant try/except wrapper IF and ONLY IF the try body has no finally and no other except clause and removing the wrapper does not change control flow (the body simply runs and any exception propagates naturally). Read the full try block first. DO NOT touch database.py:1498 / async_database.py:2011 ("except ValueError: raise") — those are intentional re-raises that prevent a broader except Exception below from masking the ValueError; deleting them WOULD change behavior.

    4. import re: re-grep 'import re' in database.py / async_database.py. Verified at planning time it is NOT present in these files (only in utils/pool/migrations). If grep confirms absent, this REF-04 sub-item is N/A here — record that in SUMMARY; do not add or remove anything. If a stray 'import re' is found AND grep shows zero re. usage in that file, remove it.

    Behavior-preserving (D-06). A red test = real regression, STOP.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && ! grep -q 'available in Phase 3' pycopg/async_database.py && uv run ruff check pycopg && uv run pytest tests/test_async_database.py tests/test_database.py tests/test_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - grep 'available in Phase 3' pycopg/async_database.py returns nothing (exit 1)
    - the three async subprocess sites no longer bind an unused 'stdout' (grep 'stdout, stderr = await' pycopg/async_database.py returns 0; '_, stderr = await' present)
    - database.py:~1498 and async_database.py:~2011 'except ValueError:\n raise' STILL present (intentional re-raise preserved)
    - uv run ruff check pycopg exits 0 (no unused-variable / unused-import warnings introduced or remaining in target files)
    - uv run pytest tests/test_async_database.py tests/test_database.py tests/test_parity.py exits 0
  </acceptance_criteria>
  <done>Stale Phase-3 comments gone; unread stdout bindings replaced with _; genuine no-op try/except removed; intentional ValueError re-raises preserved; ruff clean; suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> rewired SQL (queries.* constants) | identifiers/values still cross into SQL; validation must remain in front of every rewired path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering / SQL Injection | inline SQL -> queries.* rewire | mitigate | Preserve every validate_identifier(s)/validate_* call when swapping to a constant (PATTERNS.md "Validation preservation"); test_sql_injection.py must stay green. Acceptance asserts validate_identifier count does not drop. |
| T-12-07 | Tampering / SQL Injection | _build_batch_insert_sql routing in insert_many/upsert_many | mitigate | The builder calls validate_identifiers(table, schema, *columns) up front (base.py); routing through it strengthens, never weakens, validation. Confirm injection tests green. |
| T-12-08 | Denial of Service | removing no-op try/except | mitigate | Remove ONLY a truly no-op re-raise (no finally, no other except, no control-flow change); preserve intentional ValueError re-raises. Read full block before editing (D-07). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs this phase. |
</threat_model>

<verification>
- queries.* usage rises well above the pre-phase baseline of 3 per file; async table_info/list_roles on canonical constants.
- insert_many/upsert_many (both sides) route through _build_batch_insert_sql; validation count not reduced.
- test_sql_injection.py + test_parity + DB suites green (D-06).
- Dead-code removals are grep-proven; intentional re-raises preserved.
</verification>

<success_criteria>
- REF-01 satisfied: ~25 inline SQL replaced by constants; single source of truth.
- REF-02 (D-02) satisfied: _build_batch_insert_sql wired into insert_many/upsert_many sync + async.
- REF-04 satisfied for the remaining named targets (Phase-3 comments, unread stdout, no-op try/except), grep-proven only.
- Zero API/signature/return-shape/behavior change.
</success_criteria>

<output>
Create .planning/phases/12-refactoring-brancher-les-abstractions/12-03-SUMMARY.md when done.
</output>
