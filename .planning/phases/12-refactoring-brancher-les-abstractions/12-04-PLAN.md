---
phase: 12-refactoring-brancher-les-abstractions
plan: 04
type: execute
wave: 4
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - tests/test_base.py
  - pyproject.toml
autonomous: true
requirements: [REF-05]
must_haves:
  truths:
    - "DB-free unit tests exist for build_pg_dump_cmd, build_pg_restore_cmd, build_role_options covering the branchy paths"
    - "Real coverage is MEASURED (uv run pytest --cov) before any gate change"
    - "pyproject.toml --cov-fail-under is flipped 90 -> 95 ONLY after >= 95 is genuinely measured"
    - "Full pytest suite passes under the 95 gate"
  artifacts:
    - path: "tests/test_base.py"
      provides: "DB-free unit tests for the 3 pure builders (the 90->95 coverage fuel)"
      contains: "build_pg_dump_cmd"
    - path: "pyproject.toml"
      provides: "coverage ratchet at 95"
      contains: "cov-fail-under=95"
  key_links:
    - from: "tests/test_base.py"
      to: "pycopg.base.build_pg_dump_cmd"
      via: "import + call + assert (no DB, no fixture)"
      pattern: "build_pg_dump_cmd"
    - from: "pyproject.toml"
      to: "coverage gate"
      via: "--cov-fail-under=95"
      pattern: "cov-fail-under=95"
---

<objective>
Land the coverage ratchet to 95 (REF-05) using the "measure then flip" discipline (D-07): add DB-free unit tests for the three pure builders extracted in 12-01, MEASURE real coverage, and flip --cov-fail-under 90 -> 95 in pyproject.toml ONLY once >= 95 is genuinely measured. Depends on all prior plans (the builders, inheritance collapse, and wiring must all be landed so coverage reflects the final code). This is the BLOCKING final task of the phase.

Purpose: Cross 95% coverage honestly (never freeze an unmet gate) using the new pure builders as the fuel, and ratchet the gate so it can never regress.
Output: builder tests in tests/test_base.py; --cov-fail-under=95 in pyproject.toml (conditional on measurement).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-refactoring-brancher-les-abstractions/12-CONTEXT.md
@.planning/phases/12-refactoring-brancher-les-abstractions/12-PATTERNS.md
</context>

<artifacts_produced>
Net-new test symbols only: DB-free test functions for the 3 builders in tests/test_base.py (e.g. test_build_pg_dump_cmd_* / test_build_pg_restore_cmd_* / test_build_role_options_*). No new production symbols (the builders are owned by 12-01). pyproject.toml gate value changes 90 -> 95.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add DB-free unit tests for the three builders (coverage fuel)</name>
  <read_first>
    - pycopg/base.py (final signatures of build_pg_dump_cmd / build_pg_restore_cmd / build_role_options from 12-01 — exact arg names, defaults, branches)
    - tests/test_base.py (existing DB-free test style: import + instantiate/call + assert; no db_config fixture, no @pytest.mark.asyncio — this is the home for builder tests)
    - tests/test_parity.py lines 33-52 (the DB-free class-level analog) and 12-PATTERNS.md section "tests/test_parity.py — add DB-free builder tests"
    - 12-CONTEXT.md D-06 (add tests ONLY for the new builders; NO characterization tests for already-covered paths)
  </read_first>
  <action>
    Add synchronous, DB-free unit tests to tests/test_base.py (import from pycopg.base). Cover the branchy paths of each builder — these are the 90->95 fuel (D-07):

    build_pg_dump_cmd: one test per format value (plain/custom/directory/tar) asserting the correct -F flag; schema_only and data_only flags; compress applied only for custom; jobs applied only for directory; tables -> repeated -t; exclude_tables -> repeated -T; schemas -> repeated -n; output_file -> trailing -f path; assert the leading connection args (pg_dump, -h, -p, -U, -d).

    build_pg_restore_cmd: clean/if_exists/create/data_only/schema_only/no_owner/no_privileges flags each toggled; jobs>1 -> -j; tables -> -t; schemas -> -n; trailing input_file; leading pg_restore + connection args.

    build_role_options: login True->LOGIN / False->NOLOGIN; superuser/createdb/createrole flags; inherit False -> NOINHERIT (and inherit True -> no NOINHERIT); replication; connection_limit != -1 -> "CONNECTION LIMIT n" (and -1 -> absent); password truthy -> literal "PASSWORD %s" present in the list AND assert the real password value is NOT anywhere in the returned list (T-12-02 regression guard); valid_until -> "VALID UNTIL '...'" plus a validate_timestamp-rejects-bad-input test (assert it raises for a malformed timestamp).

    Assert on list membership / positions (e.g. cmd.index("-F")+1 == "c"), not on full-string equality, so the tests are robust. Do NOT add characterization tests for already-covered non-builder paths (D-06). Do NOT weaken test_parity.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && uv run pytest tests/test_base.py -q && uv run ruff check tests/test_base.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_base.py imports build_pg_dump_cmd, build_pg_restore_cmd, build_role_options
    - at least one test asserts each pg_dump format maps to its -F flag (p/c/d/t)
    - a test asserts the real password value does NOT appear in build_role_options(...) output while "PASSWORD %s" does
    - a test asserts build_role_options raises for a malformed valid_until (validate_timestamp preserved)
    - uv run pytest tests/test_base.py exits 0
    - uv run ruff check tests/test_base.py exits 0
  </acceptance_criteria>
  <done>DB-free builder tests added to tests/test_base.py covering all branches; secret-not-in-argv and validate_timestamp guards included; tests + ruff green.</done>
</task>

<task type="auto">
  <name>Task 2: Measure coverage, then flip the ratchet 90 -> 95 only if >= 95</name>
  <read_first>
    - pyproject.toml line ~88 (addopts: the --cov-fail-under=90 to flip)
    - 12-CONTEXT.md D-07 ("measure then flip"; never freeze an unmet gate) and Phase 11 D-08 precedent
    - 12-environment note (full suite + coverage gate via uv run pytest; measured 91.62% at Phase 11)
  </read_first>
  <action>
    Run the FULL suite with coverage to MEASURE the real number: uv run pytest --cov=pycopg --cov-report=term-missing (the existing addopts already enable --cov; this surfaces the total %). Read the reported total coverage.

    CONDITIONAL FLIP (D-07):
    - IF measured total coverage >= 95: edit pyproject.toml addopts, changing --cov-fail-under=90 to --cov-fail-under=95. Then re-run uv run pytest to confirm the suite passes UNDER the 95 gate.
    - IF measured total coverage < 95: do NOT flip. Return to Task 1 and add MORE DB-free builder tests (or, if the gap is in a builder branch not yet exercised, add the missing branch test) targeting the uncovered lines shown by term-missing — focus on the new builders and other genuinely-coverable pure paths, NEVER hard-to-mock I/O (subprocess/network) which is explicitly capped out (REQUIREMENTS "Out of Scope"). Re-measure. Repeat until >= 95 is genuinely crossed, THEN flip. NEVER flip an unmet gate.

    Do not touch coverage by deleting tests or excluding files. The flip is the LAST edit of the phase.
  </action>
  <verify>
    <automated>cd /home/loc/workspace/pycopg && grep -q 'cov-fail-under=95' pyproject.toml && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - coverage was measured with uv run pytest --cov before the flip (record the measured % in SUMMARY)
    - grep 'cov-fail-under=95' pyproject.toml returns a match
    - grep 'cov-fail-under=90' pyproject.toml returns nothing (exit 1)
    - uv run pytest exits 0 (full suite passes under the 95 gate — proves >= 95 genuinely reached)
  </acceptance_criteria>
  <done>Real coverage measured; gate flipped 90->95 in pyproject.toml only after >= 95 confirmed; full suite green under the new gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test inputs -> build_role_options | the secret-not-in-argv assertion guards the D-04 boundary at the unit-test level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Repudiation / Integrity | coverage gate flip | mitigate | "Measure then flip" (D-07): flip 90->95 ONLY after uv run pytest --cov genuinely reports >= 95; never freeze an unmet gate; full suite must pass under 95. |
| T-12-02 | Information Disclosure | build_role_options test coverage | mitigate | Explicit unit test asserts the real password value is absent from the returned options list while only the "PASSWORD %s" placeholder is present (regression guard for the D-04 boundary). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs this phase. |
</threat_model>

<verification>
- DB-free builder tests cover all builder branches and include the secret-not-in-argv + validate_timestamp guards.
- Coverage measured before any gate change; flip is conditional on >= 95.
- pyproject.toml gate = 95, no remaining 90; full suite green under 95.
</verification>

<success_criteria>
- REF-05 satisfied: --cov-fail-under raised to 95 and passing, reached honestly via new builder tests (D-07).
- Coverage fuel comes from the new pure builders, not from characterization tests of already-covered paths (D-06).
</success_criteria>

<output>
Create .planning/phases/12-refactoring-brancher-les-abstractions/12-04-SUMMARY.md when done.
</output>
