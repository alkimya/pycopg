---
phase: 04-async-extensions-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/async_database.py
  - tests/test_async_database.py
autonomous: true

must_haves:
  truths:
    - "User can call async create_role(), drop_role(), alter_role() for role lifecycle"
    - "User can call async grant(), revoke(), grant_role(), revoke_role() for privilege management"
    - "User can call async list_role_members(), list_role_grants() for role inspection"
  artifacts:
    - path: "pycopg/async_database.py"
      provides: "Async role management, privilege, and inspection methods"
      contains: "async def create_role"
    - path: "tests/test_async_database.py"
      provides: "Unit tests for all 9 role/privilege methods"
      contains: "TestAsyncDatabaseRoles"
  key_links:
    - from: "pycopg/async_database.py::create_role"
      to: "self.execute / self.cursor"
      via: "await with autocommit=True"
      pattern: "autocommit=True"
    - from: "pycopg/async_database.py::grant"
      to: "self.execute"
      via: "await with autocommit=True"
      pattern: "await self\\.execute.*autocommit=True"
    - from: "pycopg/async_database.py::list_role_members"
      to: "self.execute"
      via: "await (no autocommit, SELECT query)"
      pattern: "await self\\.execute.*pg_auth_members"
---

<objective>
Add async role management, privilege management, and role inspection methods to AsyncDatabase.

Purpose: Achieve full sync/async parity for PostgreSQL role and privilege operations (ASYNC-15, ASYNC-16, ASYNC-17, ASYNC-18).
Output: 9 new async methods on AsyncDatabase + unit tests for all methods.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-async-extensions-parity/04-RESEARCH.md
@pycopg/async_database.py
@pycopg/database.py (lines 1666-2029 for sync role/privilege methods)
@tests/test_async_database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add async role management and privilege methods to AsyncDatabase</name>
  <files>pycopg/async_database.py</files>
  <action>
Add 9 async methods to AsyncDatabase in `pycopg/async_database.py`, placed between the existing ROLES section (after `list_roles`) and the SIZE & STATS section. Convert directly from the sync Database equivalents in `database.py` lines 1666-2029.

**Role lifecycle methods (autocommit=True for DDL):**

1. `async def create_role(self, name, password=None, login=True, superuser=False, createdb=False, createrole=False, inherit=True, replication=False, connection_limit=-1, valid_until=None, in_roles=None, if_not_exists=True) -> None` — Exact async mirror of Database.create_role (line 1666). Key details:
   - Call `await self.role_exists(name)` (not `self.role_exists` — must await)
   - Build options list identically to sync version
   - When password is set: use `async with self.cursor(autocommit=True) as cur: await cur.execute(...)` with parameterized password `[password]`
   - When no password: use `await self.execute(..., autocommit=True)`
   - If `in_roles`: loop and `await self.grant_role(role, name)` for each

2. `async def drop_role(self, name, if_exists=True) -> None` — validate_identifier, build IF EXISTS clause, `await self.execute(f"DROP ROLE {if_clause}{name}", autocommit=True)`

3. `async def alter_role(self, name, password=None, login=None, superuser=None, createdb=None, createrole=None, connection_limit=None, valid_until=None, rename_to=None) -> None` — Exact async mirror of Database.alter_role (line 1804). Key details:
   - If `rename_to`: `await self.execute(f"ALTER ROLE {name} RENAME TO {rename_to}", autocommit=True)` then return
   - Build options/params list identically to sync
   - When params exist (password set): use `async with self.cursor(autocommit=True) as cur: await cur.execute(...)` with params
   - When no params: use `await self.execute(f"ALTER ROLE {name} WITH {options_str}", autocommit=True)`

**Privilege methods (autocommit=True):**

4. `async def grant(self, privileges, on, to, object_type="TABLE", schema="public", with_grant_option=False) -> None` — Exact async mirror of Database.grant (line 1894). Handle all 4 branches: SCHEMA, DATABASE, ALL TABLES/SEQUENCES/FUNCTIONS, and default table. All use `autocommit=True`.

5. `async def revoke(self, privileges, on, from_role, object_type="TABLE", schema="public", cascade=False) -> None` — Exact async mirror of Database.revoke (line 1949). Handle same 4 branches with `autocommit=True`.

6. `async def grant_role(self, role, member, with_admin=False) -> None` — validate_identifiers, build admin clause, `await self.execute(f"GRANT {role} TO {member}{admin_clause}", autocommit=True)`

7. `async def revoke_role(self, role, member) -> None` — validate_identifiers, `await self.execute(f"REVOKE {role} FROM {member}", autocommit=True)`

**Role inspection methods (no autocommit — SELECT queries):**

8. `async def list_role_members(self, role) -> list[str]` — Exact async mirror of Database.list_role_members (line 1992). Query pg_auth_members joined with pg_roles, return `[r["member"] for r in result]`.

9. `async def list_role_grants(self, role) -> list[dict]` — Exact async mirror of Database.list_role_grants (line 2011). Query information_schema.role_table_grants, return result directly.

**Section organization:** Add a new comment section header `# ROLE MANAGEMENT` between the existing `# ROLES` section and `# SIZE & STATS` section. Place methods 1-7 (DDL/privilege) together, then methods 8-9 (inspection).

**Also update `from_geodataframe`:** Now that `create_spatial_index` will exist (added in plan 04-02), do NOT update `from_geodataframe` yet — plan 04-02 will handle that after `create_spatial_index` is implemented.

Import note: `validate_identifier`, `validate_identifiers` are already imported. No new imports needed.
  </action>
  <verify>
Run `python -c "from pycopg import AsyncDatabase; db = AsyncDatabase.__new__(AsyncDatabase); methods = [m for m in dir(db) if not m.startswith('_')]; print([m for m in ['create_role', 'drop_role', 'alter_role', 'grant', 'revoke', 'grant_role', 'revoke_role', 'list_role_members', 'list_role_grants'] if m in methods])"` — should list all 9 methods.
  </verify>
  <done>AsyncDatabase has all 9 role/privilege/inspection async methods matching Database signatures exactly. create_role uses parameterized password queries with autocommit. All DDL/privilege methods use autocommit=True. Inspection methods use standard execute.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for all role/privilege/inspection methods</name>
  <files>tests/test_async_database.py</files>
  <action>
Add test classes to `tests/test_async_database.py` following the established mock-based testing pattern used by TestAsyncDatabaseDDL, TestAsyncDatabaseAdmin, and TestAsyncDatabaseMaintenance.

**Test class: `TestAsyncDatabaseRoles`** (mark with `@pytest.mark.asyncio`)

Tests for create_role:
1. `test_create_role_basic` — Mock `role_exists` returning False, mock `execute`. Verify CREATE ROLE SQL contains LOGIN, uses autocommit=True.
2. `test_create_role_with_password` — Mock `role_exists` returning False, mock `cursor` context manager. Verify SQL contains "PASSWORD %s" and params include the password. Verify autocommit=True passed to cursor.
3. `test_create_role_if_not_exists_returns_early` — Mock `role_exists` returning True. Verify `execute` is NOT called.
4. `test_create_role_with_options` — Test superuser=True, createdb=True, connection_limit=10. Verify SQL contains SUPERUSER, CREATEDB, CONNECTION LIMIT 10.
5. `test_create_role_with_in_roles` — Mock `role_exists` returning False, mock `execute` and `grant_role`. Verify grant_role called for each role in in_roles.

Tests for drop_role:
6. `test_drop_role_basic` — Verify SQL is "DROP ROLE IF EXISTS {name}" with autocommit=True.
7. `test_drop_role_no_if_exists` — Verify SQL is "DROP ROLE {name}" (no IF EXISTS) with autocommit=True.

Tests for alter_role:
8. `test_alter_role_rename` — Verify "ALTER ROLE name RENAME TO newname" with autocommit=True.
9. `test_alter_role_with_password` — Mock cursor. Verify SQL contains "PASSWORD %s" and params=[password]. Verify autocommit=True.
10. `test_alter_role_attributes` — Set login=False, createdb=True. Verify SQL contains NOLOGIN, CREATEDB.

**Test class: `TestAsyncDatabasePrivileges`** (mark with `@pytest.mark.asyncio`)

Tests for grant:
11. `test_grant_table` — `grant("SELECT", "users", "readonly")`. Verify SQL: "GRANT SELECT ON TABLE public.users TO readonly" with autocommit=True.
12. `test_grant_schema` — `grant("USAGE", "myschema", "appuser", object_type="SCHEMA")`. Verify SQL contains "GRANT USAGE ON SCHEMA myschema TO appuser".
13. `test_grant_all_tables` — `grant("SELECT", "ALL TABLES", "readonly", schema="public")`. Verify SQL contains "GRANT SELECT ON ALL TABLES IN SCHEMA public TO readonly".
14. `test_grant_with_grant_option` — Verify SQL contains "WITH GRANT OPTION".
15. `test_grant_list_privileges` — `grant(["SELECT", "INSERT"], "users", "appuser")`. Verify SQL contains "SELECT, INSERT".

Tests for revoke:
16. `test_revoke_table` — Verify "REVOKE {privileges} ON TABLE {schema}.{table} FROM {role}" with autocommit=True.
17. `test_revoke_cascade` — Verify SQL contains "CASCADE".

Tests for grant_role/revoke_role:
18. `test_grant_role_basic` — Verify "GRANT role TO member" with autocommit=True.
19. `test_grant_role_with_admin` — Verify SQL contains "WITH ADMIN OPTION".
20. `test_revoke_role_basic` — Verify "REVOKE role FROM member" with autocommit=True.

**Test class: `TestAsyncDatabaseRoleInspection`** (mark with `@pytest.mark.asyncio`)

21. `test_list_role_members` — Mock execute returning `[{"member": "analyst"}, {"member": "viewer"}]`. Verify returns `["analyst", "viewer"]`. Verify SQL queries pg_auth_members.
22. `test_list_role_grants` — Mock execute returning `[{"schema": "public", "object_name": "users", "privilege": "SELECT"}]`. Verify result is returned directly. Verify SQL queries role_table_grants.

**Testing pattern:** Use the established pattern from existing tests:
- `db = AsyncDatabase(config)` with the `config` fixture
- `db.execute = AsyncMock(...)` for mocking execute
- `db.role_exists = AsyncMock(return_value=...)` for mocking role_exists
- `db.grant_role = AsyncMock()` for mocking grant_role
- For password tests (need cursor): create cursor mock with `autocommit` check via the cursor context manager pattern from TestAsyncDatabaseAdmin
  </action>
  <verify>Run `cd /home/loc/workspace/pycopg && python -m pytest tests/test_async_database.py::TestAsyncDatabaseRoles tests/test_async_database.py::TestAsyncDatabasePrivileges tests/test_async_database.py::TestAsyncDatabaseRoleInspection -v` — all tests pass.</verify>
  <done>All role, privilege, and inspection methods have comprehensive unit tests covering: basic operations, edge cases (if_not_exists, password handling, rename), all grant/revoke object types, autocommit verification, and parameterized password security.</done>
</task>

</tasks>

<verification>
1. `python -c "from pycopg import AsyncDatabase"` — no import errors
2. `python -m pytest tests/test_async_database.py -v` — all tests pass (existing + new)
3. All 9 methods exist: create_role, drop_role, alter_role, grant, revoke, grant_role, revoke_role, list_role_members, list_role_grants
4. All DDL/privilege methods use autocommit=True
5. create_role and alter_role use parameterized queries for passwords
</verification>

<success_criteria>
- AsyncDatabase has 9 new async role/privilege/inspection methods matching Database signatures
- create_role uses parameterized password (not f-string) with cursor(autocommit=True)
- All DDL/privilege methods pass autocommit=True
- Role inspection methods use standard execute (no autocommit)
- 22+ unit tests covering all methods and edge cases pass
- Existing tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/04-async-extensions-parity/04-01-SUMMARY.md`
</output>
