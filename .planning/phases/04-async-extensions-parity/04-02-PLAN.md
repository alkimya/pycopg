---
phase: 04-async-extensions-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pycopg/async_database.py
  - tests/test_async_database.py
autonomous: true

must_haves:
  truths:
    - "User can call async create_spatial_index(), list_geometry_columns() for PostGIS operations"
    - "User can call async create_hypertable(), enable_compression(), add_retention_policy(), list_hypertables() for TimescaleDB operations"
  artifacts:
    - path: "pycopg/async_database.py"
      provides: "Async PostGIS and TimescaleDB methods"
      contains: "async def create_hypertable"
    - path: "tests/test_async_database.py"
      provides: "Unit tests for PostGIS and TimescaleDB methods"
      contains: "TestAsyncDatabasePostGIS"
  key_links:
    - from: "pycopg/async_database.py::create_hypertable"
      to: "self.has_extension"
      via: "await self.has_extension('timescaledb') with RuntimeError on missing"
      pattern: "await self\\.has_extension.*timescaledb"
    - from: "pycopg/async_database.py::create_spatial_index"
      to: "self.execute"
      via: "await self.execute with GIST index"
      pattern: "USING GIST"
    - from: "pycopg/async_database.py::from_geodataframe"
      to: "self.create_spatial_index"
      via: "await self.create_spatial_index after writing data"
      pattern: "await self\\.create_spatial_index"
---

<objective>
Add async PostGIS spatial operations and TimescaleDB time-series methods to AsyncDatabase, plus update from_geodataframe to use create_spatial_index.

Purpose: Complete extension parity for ASYNC-19, ASYNC-20, ASYNC-21, ASYNC-22, ASYNC-23 and fix the from_geodataframe spatial_index warning from Phase 2.
Output: 7 new async methods on AsyncDatabase + from_geodataframe update + unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-async-extensions-parity/04-RESEARCH.md
@pycopg/async_database.py
@pycopg/database.py (lines 1296-1530 for sync PostGIS/TimescaleDB methods)
@pycopg/utils.py (validate_identifier, validate_identifiers, validate_interval)
@tests/test_async_database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add async PostGIS and TimescaleDB methods to AsyncDatabase</name>
  <files>pycopg/async_database.py</files>
  <action>
Add 7 async methods to AsyncDatabase in `pycopg/async_database.py`. Also update `from_geodataframe` to use the new `create_spatial_index`. Convert directly from the sync Database equivalents in `database.py` lines 1296-1530.

**Import addition:** Add `validate_interval` to the existing import from `pycopg.utils`:
```python
from pycopg.utils import validate_identifier, validate_identifiers, validate_index_method, validate_interval
```

**PostGIS methods (no extension validation — matches sync Database pattern):**

Add a new section `# POSTGIS SPATIAL OPERATIONS` after the EXTENSIONS section (after `list_extensions`):

1. `async def create_spatial_index(self, table, column="geometry", schema="public", name=None) -> None` — Exact async mirror of Database.create_spatial_index (line 1296). Generate index_name as `f"idx_{table}_{column}_gist"` if name is None. Execute `CREATE INDEX IF NOT EXISTS {index_name} ON {schema}.{table} USING GIST ({column})`. No extension validation (matches sync pattern).

2. `async def list_geometry_columns(self, schema=None) -> list[dict]` — Exact async mirror of Database.list_geometry_columns (line 1314). Build WHERE clause conditionally on schema param. Query geometry_columns view for f_table_schema, f_table_name, f_geometry_column, coord_dimension, srid, type. No extension validation.

**TimescaleDB methods (all require extension validation with RuntimeError):**

Add a new section `# TIMESCALEDB OPERATIONS` after the PostGIS section:

3. `async def create_hypertable(self, table, time_column, schema="public", chunk_time_interval="1 day", if_not_exists=True, migrate_data=True) -> None` — Exact async mirror of Database.create_hypertable (line 1342). Key details:
   - `if not await self.has_extension("timescaledb"):` raise RuntimeError (must await!)
   - `validate_identifiers(table, schema, time_column)`
   - `validate_interval(chunk_time_interval)`
   - Execute `SELECT create_hypertable(...)` with all params

4. `async def enable_compression(self, table, segment_by=None, order_by=None, schema="public") -> None` — Exact async mirror of Database.enable_compression (line 1382). Key details:
   - Extension validation with await
   - Build settings list: `["timescaledb.compress"]`
   - Handle segment_by/order_by as str or list[str], validate each column name
   - Execute `ALTER TABLE {schema}.{table} SET ({settings})`

5. `async def add_compression_policy(self, table, compress_after="7 days", schema="public") -> None` — Exact async mirror of Database.add_compression_policy (line 1427). Extension validation, then execute `SELECT add_compression_policy(...)`.

6. `async def add_retention_policy(self, table, drop_after, schema="public") -> None` — Exact async mirror of Database.add_retention_policy (line 1456). Extension validation, then execute `SELECT add_retention_policy(...)`.

7. `async def list_hypertables(self) -> list[dict]` — Exact async mirror of Database.list_hypertables (line 1485). Extension validation, then query `timescaledb_information.hypertables`.

**Bonus for full parity:**

8. `async def hypertable_info(self, table, schema="public") -> dict` — Exact async mirror of Database.hypertable_info (line 1508). Extension validation, query hypertable_size and hypertable_detailed_size, return first result or empty dict.

**Update from_geodataframe (lines 1004-1010):**

Replace the warning block:
```python
if spatial_index and if_exists != "append":
    import logging
    logger = logging.getLogger(__name__)
    logger.warning(
        "spatial_index parameter ignored — create_spatial_index not yet available in AsyncDatabase. "
        "Use db.execute('CREATE INDEX ...') manually or wait for Phase 4."
    )
```

With actual implementation:
```python
if spatial_index and if_exists != "append":
    await self.create_spatial_index(table, geometry_column, schema)
```

This makes from_geodataframe fully functional with spatial indexing, matching the sync Database behavior.
  </action>
  <verify>
Run `python -c "from pycopg import AsyncDatabase; db = AsyncDatabase.__new__(AsyncDatabase); methods = [m for m in dir(db) if not m.startswith('_')]; print([m for m in ['create_spatial_index', 'list_geometry_columns', 'create_hypertable', 'enable_compression', 'add_compression_policy', 'add_retention_policy', 'list_hypertables', 'hypertable_info'] if m in methods])"` — should list all 8 methods.
  </verify>
  <done>AsyncDatabase has all PostGIS and TimescaleDB async methods matching Database signatures. TimescaleDB methods validate extension with await. PostGIS methods have no extension validation (matches sync). from_geodataframe now calls create_spatial_index instead of logging a warning.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for PostGIS, TimescaleDB, and updated from_geodataframe</name>
  <files>tests/test_async_database.py</files>
  <action>
Add test classes to `tests/test_async_database.py` following established mock patterns.

**Test class: `TestAsyncDatabasePostGIS`** (mark with `@pytest.mark.asyncio`)

1. `test_create_spatial_index_basic` — Mock execute. Call `create_spatial_index("parcels", "geom")`. Verify SQL contains `CREATE INDEX IF NOT EXISTS idx_parcels_geom_gist ON public.parcels USING GIST (geom)`.
2. `test_create_spatial_index_custom_name` — Call with `name="custom_idx"`. Verify SQL uses `custom_idx` instead of auto-generated name.
3. `test_create_spatial_index_custom_schema` — Call with `schema="geo"`. Verify SQL contains `ON geo.parcels`.
4. `test_list_geometry_columns_all` — Mock execute returning `[{"schema": "public", "table_name": "parcels", "column_name": "geom", "dimensions": 2, "srid": 4326, "geometry_type": "POLYGON"}]`. Call `list_geometry_columns()`. Verify result and SQL queries `geometry_columns` with no WHERE clause.
5. `test_list_geometry_columns_with_schema` — Call with `schema="geo"`. Verify SQL contains `WHERE f_table_schema = %s` and params include `"geo"`.

**Test class: `TestAsyncDatabaseTimescaleDB`** (mark with `@pytest.mark.asyncio`)

6. `test_create_hypertable_basic` — Mock `has_extension` returning True, mock `execute`. Verify SQL contains `create_hypertable` with correct params. Verify `has_extension` called with `"timescaledb"`.
7. `test_create_hypertable_no_extension_raises` — Mock `has_extension` returning False. Verify `RuntimeError` raised with "TimescaleDB extension not installed".
8. `test_create_hypertable_custom_interval` — Call with `chunk_time_interval="1 week"`. Verify SQL contains `INTERVAL '1 week'`.
9. `test_enable_compression_basic` — Mock has_extension True. Call `enable_compression("events", segment_by="device_id")`. Verify SQL contains `ALTER TABLE` and `timescaledb.compress` and `timescaledb.compress_segmentby`.
10. `test_enable_compression_with_order_by` — Call with `order_by=["timestamp DESC"]`. Verify SQL contains `timescaledb.compress_orderby`.
11. `test_enable_compression_no_extension_raises` — Mock has_extension False. Verify RuntimeError.
12. `test_add_compression_policy_basic` — Mock has_extension True. Verify SQL contains `add_compression_policy` and `compress_after => INTERVAL '7 days'`.
13. `test_add_compression_policy_no_extension_raises` — RuntimeError on missing extension.
14. `test_add_retention_policy_basic` — Mock has_extension True. Call with `drop_after="90 days"`. Verify SQL contains `add_retention_policy` and `drop_after => INTERVAL '90 days'`.
15. `test_add_retention_policy_no_extension_raises` — RuntimeError on missing extension.
16. `test_list_hypertables_basic` — Mock has_extension True, mock execute returning hypertable info. Verify result returned and SQL queries `timescaledb_information.hypertables`.
17. `test_list_hypertables_no_extension_raises` — RuntimeError on missing extension.
18. `test_hypertable_info_basic` — Mock has_extension True, mock execute returning `[{"total_size": "100 MB", "detailed_size": "..."}]`. Verify returns first dict.
19. `test_hypertable_info_no_extension_raises` — RuntimeError on missing extension.

**Update existing test: `TestAsyncDatabaseGeoDataFrame.test_from_geodataframe_basic`**

20. Add a new test `test_from_geodataframe_with_spatial_index` — Mock has_extension True, mock async_engine, mock `create_spatial_index` as AsyncMock. Call `from_geodataframe(gdf, "parcels", spatial_index=True)`. Verify `create_spatial_index` was called with `("parcels", "geometry", "public")`. This replaces the old warning behavior.

**Testing pattern:** Same as established:
- `db.execute = AsyncMock(...)` for mocking
- `db.has_extension = AsyncMock(return_value=True/False)` for extension checks
- `pytest.raises(RuntimeError, match="TimescaleDB extension not installed")` for extension validation
  </action>
  <verify>Run `cd /home/loc/workspace/pycopg && python -m pytest tests/test_async_database.py::TestAsyncDatabasePostGIS tests/test_async_database.py::TestAsyncDatabaseTimescaleDB -v` — all tests pass.</verify>
  <done>All PostGIS and TimescaleDB methods have comprehensive tests covering: basic operations, extension validation (RuntimeError on missing), custom parameters, and from_geodataframe spatial index integration. All existing tests remain green.</done>
</task>

</tasks>

<verification>
1. `python -c "from pycopg import AsyncDatabase"` — no import errors
2. `python -m pytest tests/test_async_database.py -v` — all tests pass (existing + new)
3. All 8 methods exist: create_spatial_index, list_geometry_columns, create_hypertable, enable_compression, add_compression_policy, add_retention_policy, list_hypertables, hypertable_info
4. All TimescaleDB methods check `await self.has_extension("timescaledb")` and raise RuntimeError
5. PostGIS methods do NOT validate extension (matches sync Database)
6. from_geodataframe now calls create_spatial_index instead of logging warning
</verification>

<success_criteria>
- AsyncDatabase has 8 new async PostGIS/TimescaleDB methods matching Database signatures
- All TimescaleDB methods validate extension with `await self.has_extension("timescaledb")`
- PostGIS methods have no extension validation (parity with sync Database)
- from_geodataframe calls create_spatial_index when spatial_index=True
- 20+ unit tests covering all methods, extension checks, and edge cases pass
- Existing tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/04-async-extensions-parity/04-02-SUMMARY.md`
</output>
