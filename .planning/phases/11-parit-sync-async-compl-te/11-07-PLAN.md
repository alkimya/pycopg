---
phase: 11-parit-sync-async-compl-te
plan: 07
type: execute
wave: 6
depends_on: ["11-06"]
files_modified:
  - tests/test_database.py
  - tests/test_async_database.py
  - pyproject.toml
autonomous: true
requirements: [PAR-09]
must_haves:
  truths:
    - "Measured total coverage from `uv run pytest` is >= 90% BEFORE the gate is changed (D-08, ratchet)"
    - "Targeted tests fill remaining coverage gaps (no refactor — Phase 12; no fragile mock-only tests) (D-08)"
    - "pyproject.toml --cov-fail-under is 90 (raised from 80), and the suite passes at that gate"
    - "The gate is never set above the actually-measured coverage (never freeze an unmet threshold)"
  artifacts:
    - path: "pyproject.toml"
      provides: "coverage ratchet at 90"
      contains: "--cov-fail-under=90"
  key_links:
    - from: "pyproject.toml addopts"
      to: "measured coverage"
      via: "gate raised only after >=90 confirmed"
      pattern: "--cov-fail-under=90"
---

<objective>
Implement PAR-09: raise the coverage ratchet to 90 — but ONLY once measured coverage actually crosses 90 (D-08, never freeze an unmet threshold). The implementations + parity tests from Plans 02-06 add substantial coverage (the previously-uncovered DDL/admin/batch lines). This plan: (1) measure current coverage; (2) if below 90, add TARGETED tests for the still-uncovered lines (no refactor — that is Phase 12; no fragile tests); (3) flip `--cov-fail-under` 80 → 90 in pyproject.toml and confirm the suite passes at the new gate.

Purpose: PAR-09 is the milestone's locked coverage ratchet. Baseline at planning time was 80.85%.
Output: any targeted gap-fill tests + `--cov-fail-under=90`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-parit-sync-async-compl-te/11-CONTEXT.md
@pyproject.toml
@tests/test_database.py
@tests/test_async_database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure coverage and fill targeted gaps to >= 90 (D-08)</name>
  <files>tests/test_database.py, tests/test_async_database.py</files>
  <read_first>
    - pyproject.toml — the `addopts` line (88) with `--cov=pycopg ... --cov-fail-under=80` and `--cov-report=term-missing`.
    - tests/test_database.py / tests/test_async_database.py — existing test style for adding targeted cases.
  </read_first>
  <action>
    Run `uv run pytest` (term-missing already enabled) and read the per-file Missing column. If TOTAL is already >= 90, do nothing here and proceed to Task 2. Otherwise, for the specific uncovered line ranges in `pycopg/database.py` and `pycopg/async_database.py` (and config.py if any), add the MINIMAL targeted tests needed to cross 90 — prefer real-DB assertions over mocks (D-08), no refactor of source (Phase 12), no fragile/over-specified tests. Focus on the newly-added methods' branches not already hit by Plan 06 (e.g. error paths, if_exists/if_not_exists False, cascade=True, optional-arg branches). Re-run `uv run pytest` until TOTAL coverage >= 90. Do NOT touch the gate value in this task.
  </action>
  <verify>
    <automated>uv run pytest -p no:cacheprovider 2>&1 | grep -E "Total coverage: ([9][0-9]|100)" </automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` reports `Total coverage:` >= 90%
    - Any added tests pass and are real-DB or behavior assertions (not fragile mock-only)
    - No source files under `pycopg/` were modified in this task (coverage gained via tests only)
  </acceptance_criteria>
  <done>Measured total coverage is >= 90% with the gate still at 80.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the coverage ratchet to 90 (PAR-09)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml — line 88 `addopts = "-v --cov=pycopg --cov-report=term-missing --cov-report=html --cov-fail-under=80"`.
  </read_first>
  <action>
    GATED ON Task 1 confirming >= 90. Change `--cov-fail-under=80` to `--cov-fail-under=90` on the `addopts` line in pyproject.toml. Change nothing else on that line. The ratchet only ever goes up (never lowered). Then run the full suite to confirm it passes at the new gate.
  </action>
  <verify>
    <automated>uv run pytest -p no:cacheprovider 2>&1 | grep -E "Required test coverage of 90% reached"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `--cov-fail-under=90`
    - `pyproject.toml` does NOT contain `--cov-fail-under=80`
    - `uv run pytest` exits 0 with the 90% gate active (does not fail the coverage check)
  </acceptance_criteria>
  <done>Coverage gate is 90 and the suite passes at it (ratchet locked at the new floor).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → merge protection | the coverage floor enforced in CI (tests.yml) governs what can merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Repudiation / false assurance | coverage gate set above actual coverage | mitigate | Task 2 is GATED on Task 1 measuring >= 90; the gate is only raised after a real measurement, never to freeze an unmet threshold (D-08). |
| T-11-14 | Tampering | gate lowered | accept | Ratchet policy is documented (never lowered); 80 → 90 is the only change; no mechanism here lowers it. |
</threat_model>

<verification>
- `uv run pytest` passes with `--cov-fail-under=90`.
- The 2 pre-existing failures noted in the phase verification (test_async_transaction_fix, test_create_spatial_index_name_parameter) are confirmed PRE-EXISTING (present at the Phase-11 baseline) and NOT introduced by this phase — if either is now passing, even better; if still failing, they remain out of phase scope per STATE.md (deferred integration failures). They must not be NEW regressions.
- `uv run ruff check` and `uv run black --check` clean.
</verification>

<success_criteria>
- Coverage measured >= 90, THEN gate flipped to 90 (D-08 ordering).
- Suite passes at the 90 gate; ratchet never set above measured coverage.
</success_criteria>

<output>
Create `.planning/phases/11-parit-sync-async-compl-te/11-07-SUMMARY.md` when done.
</output>
