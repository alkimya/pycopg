---
phase: 11-parit-sync-async-compl-te
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - pycopg/async_database.py
autonomous: true
requirements: [PAR-01]
must_haves:
  truths:
    - "AsyncDatabase.add_primary_key adds a PRIMARY KEY constraint, mirroring Database.add_primary_key (PAR-01)"
    - "AsyncDatabase.add_foreign_key adds a FK with ON DELETE/UPDATE validation, mirroring the sync twin (PAR-01)"
    - "AsyncDatabase.add_unique_constraint adds a UNIQUE constraint, mirroring the sync twin (PAR-01)"
    - "AsyncDatabase.truncate_table truncates a table, mirroring the sync twin (PAR-01)"
    - "add_primary_key EXISTS on AsyncDatabase before C1 (Plan 05) can call it (ordering constraint)"
  artifacts:
    - path: "pycopg/async_database.py"
      provides: "async add_primary_key, add_foreign_key, add_unique_constraint, truncate_table"
      contains: "async def add_primary_key"
  key_links:
    - from: "pycopg/async_database.py add_primary_key"
      to: "pycopg/utils.py validate_identifiers"
      via: "identifier validation before SQL build"
      pattern: "validate_identifiers\\("
---

<objective>
Implement PAR-01: mirror the four sync constraint/admin DDL methods into `AsyncDatabase`. Each is a mechanical mirror (D-01) of its sync twin in `database.py`, swapping `self.execute(...)` for `await self.execute(...)` and keeping the `validate_*` wiring identical.

Purpose: PAR-01 closes the constraint-DDL parity gap. CRITICAL ORDERING (CONTEXT <specifics>): the async `add_primary_key` created here MUST exist before Plan 05 fixes C1 (async from_dataframe/from_geodataframe must call it). This plan is Wave 2 (depends on Plan 01, which first touched async_database.py — file overlap forces sequencing).
Output: `AsyncDatabase.add_primary_key`, `add_foreign_key`, `add_unique_constraint`, `truncate_table`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-parit-sync-async-compl-te/11-CONTEXT.md
@pycopg/async_database.py
@pycopg/database.py
@pycopg/utils.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: async add_primary_key + add_unique_constraint (PAR-01)</name>
  <files>pycopg/async_database.py</files>
  <read_first>
    - pycopg/database.py — sync `add_primary_key` (lines 965-986) and `add_unique_constraint` (lines 1046-1066): exact signature + SQL + default constraint-name logic to mirror.
    - pycopg/async_database.py — an existing async DDL method (e.g. `create_schema` near line 500, or `create_spatial_index` near 761) for the established `await self.execute(...)` pattern and method placement (CONSTRAINTS & INDEXES region).
    - pycopg/utils.py — `validate_identifiers` / `validate_identifier`: REUSE, do not rewrite.
  </read_first>
  <behavior>
    - add_primary_key("t","id") → ALTER TABLE public.t ADD CONSTRAINT t_pkey PRIMARY KEY (id); afterwards the table has a PK.
    - add_primary_key with list columns and explicit `name=` → uses the given constraint name.
    - add_unique_constraint("t","email") → UNIQUE constraint applied; duplicate insert raises.
  </behavior>
  <action>
    Add `async def add_primary_key(self, table, columns, schema="public", name=None) -> None` mirroring sync (database.py:965): normalize str→list; `validate_identifiers(table, schema, *columns)`; `if name: validate_identifier(name)`; `constraint_name = name or f"{table}_pkey"`; `await self.execute(f"ALTER TABLE {schema}.{table} ADD CONSTRAINT {constraint_name} PRIMARY KEY ({cols_str})")`. Add `async def add_unique_constraint(self, table, columns, schema="public", name=None) -> None` mirroring database.py:1046 with the same normalize/validate pattern and the sync's UNIQUE SQL + default name. Place both in a CONSTRAINTS & INDEXES region of async_database.py. Keep `validate_*` identical to the sync twins. Docstrings follow EXISTING async_database.py style.
  </action>
  <verify>
    <automated>uv run pytest tests/test_async_database.py -k "add_primary_key or add_unique_constraint" -q -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - `async_database.py` contains `async def add_primary_key(` and `async def add_unique_constraint(`
    - `AsyncDatabase.add_primary_key` signature params == `['self','table','columns','schema','name']` (matches sync twin)
    - integration test: adding a PK then querying a constraint catalog shows the PK on the temp table
    - `test_parity.py::TestAsyncParity::test_all_database_public_methods_exist_in_async` no longer reports `add_primary_key`/`add_unique_constraint` as missing (allow-list edit lands in Plan 05; this asserts the method now EXISTS)
  </acceptance_criteria>
  <done>async add_primary_key + add_unique_constraint exist and apply real constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: async add_foreign_key + truncate_table (PAR-01)</name>
  <files>pycopg/async_database.py</files>
  <read_first>
    - pycopg/database.py — sync `add_foreign_key` (lines 988-1044, including the `valid_actions` ON DELETE/UPDATE allow-set and the multi-line FK SQL) and `truncate_table` (lines 903-913).
    - pycopg/async_database.py — `await self.execute(...)` pattern (any existing async DDL method).
    - pycopg/utils.py — `validate_identifiers`.
  </read_first>
  <behavior>
    - add_foreign_key("orders","user_id","users","id", on_delete="CASCADE") → FK created; deleting a parent row cascades.
    - add_foreign_key with an invalid `on_delete` (e.g. "BOGUS") → raises ValueError before any SQL runs.
    - truncate_table("t") → all rows removed; row_count == 0 afterwards.
  </behavior>
  <action>
    Add `async def add_foreign_key(self, table, columns, ref_table, ref_columns, schema="public", ref_schema="public", name=None, on_delete="NO ACTION", on_update="NO ACTION") -> None` mirroring sync (database.py:988): normalize both column args str→list; `validate_identifiers(table, schema, ref_table, ref_schema, *columns, *ref_columns)`; the SAME `valid_actions` set check raising ValueError for bad `on_delete`/`on_update`; default `constraint_name = name or f"{table}_{columns[0]}_fkey"`; `await self.execute(...)` of the multi-line ALTER TABLE ... ADD CONSTRAINT ... FOREIGN KEY ... REFERENCES ... ON DELETE ... ON UPDATE ... text. Add `async def truncate_table(self, name, schema="public", cascade=False) -> None` mirroring database.py:903: `validate_identifiers(name, schema)`, optional ` CASCADE`, `await self.execute(f"TRUNCATE TABLE {schema}.{name}{cascade_clause}")`. Keep `validate_*` identical to sync. Docstrings in existing async style.
  </action>
  <verify>
    <automated>uv run pytest tests/test_async_database.py -k "add_foreign_key or truncate_table" -q -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - `async_database.py` contains `async def add_foreign_key(` and `async def truncate_table(`
    - `AsyncDatabase.add_foreign_key` signature params match the sync twin exactly (same order, same defaults)
    - calling `add_foreign_key` with `on_delete="BOGUS"` raises `ValueError` and runs no SQL
    - integration test: `truncate_table` on a populated temp table leaves 0 rows
  </acceptance_criteria>
  <done>async add_foreign_key + truncate_table exist with the sync validation semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → DDL identifiers | table/schema/columns/ref_table/ref_columns/constraint name interpolated into ALTER/TRUNCATE SQL |
| caller → ON DELETE/UPDATE action | free-text action interpolated into FK SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering (SQL injection) | async add_primary_key/add_unique_constraint/add_foreign_key/truncate_table identifiers | mitigate | Mirror the sync twins' `validate_identifiers`/`validate_identifier` calls (Phase 10 helpers) BEFORE any f-string interpolation. No raw identifier reaches SQL unvalidated. |
| T-11-06 | Tampering | FK `on_delete`/`on_update` action string | mitigate | Mirror the sync `valid_actions` allow-set check; reject anything outside `{NO ACTION, RESTRICT, CASCADE, SET NULL, SET DEFAULT}` with ValueError before building SQL. |
</threat_model>

<verification>
- `uv run pytest tests/test_async_database.py -q` passes for the four new methods.
- `uv run pytest tests/test_parity.py::TestAsyncParity::test_all_database_public_methods_exist_in_async -q` — these four no longer appear as missing (allow-list cleanup in Plan 05).
- `uv run ruff check pycopg` and `uv run black --check pycopg` clean.
</verification>

<success_criteria>
- AsyncDatabase has working add_primary_key, add_foreign_key, add_unique_constraint, truncate_table.
- async add_primary_key exists, unblocking C1 in Plan 05.
</success_criteria>

<output>
Create `.planning/phases/11-parit-sync-async-compl-te/11-03-SUMMARY.md` when done.
</output>
