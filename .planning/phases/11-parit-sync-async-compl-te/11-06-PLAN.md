---
phase: 11-parit-sync-async-compl-te
plan: 06
type: tdd
wave: 5
depends_on: ["11-02", "11-03", "11-04", "11-05"]
files_modified:
  - tests/test_parity.py
  - tests/test_database.py
  - tests/test_async_database.py
autonomous: true
requirements: [PAR-08]
must_haves:
  truths:
    - "test_parity.py adds real-DB integration assertions (sync result == async result) for the 13 newly-mirrored methods + C1 + the 4 PAR-07-aligned methods (D-03)"
    - "SYNC_ONLY_METHODS no longer lists create, create_from_env, add_foreign_key, add_primary_key, add_unique_constraint, database_exists, drop_extension, list_databases, truncate_table"
    - "ASYNC_ONLY_METHODS no longer lists insert_many, upsert_many, stream, notify but STILL lists listen with an explanatory comment (D-06)"
    - "KNOWN_SIGNATURE_MISMATCHES no longer lists create_schema or create_extension (D-07)"
    - "Existing name/param introspection tests remain as the full-surface guard (D-03)"
  artifacts:
    - path: "tests/test_parity.py"
      provides: "integration parity assertions + updated allow-lists"
      contains: "def test_"
  key_links:
    - from: "tests/test_parity.py"
      to: "pycopg_test database"
      via: "db_config fixture (sync + async)"
      pattern: "db_config"
---

<objective>
Implement PAR-08 (D-03): extend `test_parity.py` — the central file of this phase — beyond name/param introspection to REAL-DB behavioral parity. Add integration assertions that run the same operation on both `Database` and `AsyncDatabase` against `pycopg_test` and assert IDENTICAL results, but ONLY for the pairs touched this phase: the 13 newly-mirrored methods (9 async PAR-01/02 + 4 sync PAR-03), C1 (primary_key actually applied), and the 4 PAR-07-aligned methods. Keep the existing introspection tests as the full-surface guard. Update the three allow-lists to reflect the new reality.

Purpose: PAR-08 is the verification spine of the phase and is consumed by the CI `tests.yml` job. The allow-list edits make `test_exception_lists_are_minimal` / `test_method_signatures_match` pass now that the methods exist and signatures match.
Output: extended test_parity.py + per-method tests in test_database.py / test_async_database.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-parit-sync-async-compl-te/11-CONTEXT.md
@tests/test_parity.py
@tests/conftest.py
@tests/test_database_integration.py
@pycopg/database.py
@pycopg/async_database.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update test_parity allow-lists (D-02, D-06, D-07)</name>
  <files>tests/test_parity.py</files>
  <read_first>
    - tests/test_parity.py — `SYNC_ONLY_METHODS` (17-28), `ASYNC_ONLY_METHODS` (30-37), `KNOWN_SIGNATURE_MISMATCHES` (40-43), and the four introspection tests that consume them.
    - pycopg/async_database.py — confirm the 9 new async methods now exist (Plans 03/04).
    - pycopg/database.py — confirm the 4 new sync methods now exist (Plan 02).
  </read_first>
  <behavior>
    - After edits, test_all_database_public_methods_exist_in_async passes (no missing methods).
    - test_exception_lists_are_minimal passes (no stale entries).
    - test_known_exceptions_documented passes (only `engine`/`async_engine`/`listen` remain as legitimate exceptions).
    - test_method_signatures_match passes with empty KNOWN_SIGNATURE_MISMATCHES contribution for create_schema/create_extension.
  </behavior>
  <action>
    From `SYNC_ONLY_METHODS` REMOVE: `create`, `create_from_env`, `add_foreign_key`, `add_primary_key`, `add_unique_constraint`, `database_exists`, `drop_extension`, `list_databases`, `truncate_table` (keep only `engine`). From `ASYNC_ONLY_METHODS` REMOVE: `insert_many`, `upsert_many`, `stream`, `notify` (keep `async_engine`; keep `listen` and add/keep a comment such as "blocking sync listener is an anti-pattern — async-only by design (D-06)"). From `KNOWN_SIGNATURE_MISMATCHES` REMOVE `create_schema` and `create_extension` (leave the dict empty or with only genuinely-remaining mismatches if any surface). Do not delete the introspection tests — they are the full-surface guard.
  </action>
  <verify>
    <automated>uv run pytest tests/test_parity.py -q -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - `test_parity.py` `SYNC_ONLY_METHODS` does NOT contain `create`, `create_from_env`, `add_primary_key`, `add_foreign_key`, `add_unique_constraint`, `truncate_table`, `drop_extension`, `database_exists`, `list_databases`
    - `test_parity.py` `ASYNC_ONLY_METHODS` does NOT contain `insert_many`, `upsert_many`, `stream`, `notify`, but STILL contains `listen` with a comment
    - `test_parity.py` `KNOWN_SIGNATURE_MISMATCHES` does NOT contain `create_schema` or `create_extension`
    - `uv run pytest tests/test_parity.py` exits 0
  </acceptance_criteria>
  <done>All four introspection parity tests pass with updated, minimal allow-lists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real-DB integration parity assertions (D-03)</name>
  <files>tests/test_parity.py</files>
  <read_first>
    - tests/test_parity.py — current structure (class TestAsyncParity); add a new integration class/section here.
    - tests/test_database_integration.py — the `db`/`temp_table_name`/`cleanup_table` fixtures + `db_config` usage pattern to follow for real-DB tests; mirror it for an async fixture (AsyncDatabase + `@pytest.mark.asyncio`).
    - tests/conftest.py — `db_config` fixture (real `pycopg_test`).
  </read_first>
  <behavior>
    - For each in-scope pair, the same operation on Database and AsyncDatabase yields identical observable results.
    - C1: from_dataframe(primary_key=...) produces a PK on BOTH sides.
    - PAR-07: table_info / list_roles return identical key sets and matching rows on both sides.
  </behavior>
  <action>
    Add real-DB integration parity assertions covering ONLY this phase's pairs (D-03 — do NOT re-assert the whole API). Use `db_config` to build a sync `Database` and an async `AsyncDatabase` against `pycopg_test`; clean up temp tables/databases. Cover: (a) the 4 sync mirrors — insert_many/upsert_many round-trip equals async; stream yields the same rows; notify runs without error on both; (b) the 9 async mirrors — add_primary_key/add_foreign_key/add_unique_constraint/truncate_table applied results match sync via a catalog/row check; database_exists/list_databases return the same truth as sync; drop_extension parity; create/create_from_env create-and-connect (use a throwaway db name, drop after); (c) C1 — from_dataframe/from_geodataframe with primary_key produce a PK on both sync and async; (d) PAR-07 — table_info and list_roles return identical dict-key sets on both sides, and create_extension(schema=)/create_schema(owner=) behave identically. Mark async cases `@pytest.mark.asyncio`. Keep assertions about RESULTS/FIELDS/BEHAVIOR, not just method names.
  </action>
  <verify>
    <automated>uv run pytest tests/test_parity.py -q -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - `test_parity.py` contains new integration tests that instantiate BOTH `Database` and `AsyncDatabase` against `pycopg_test`
    - tests assert equality of RESULTS/return-fields between sync and async for the in-scope pairs (not just method presence)
    - a C1 assertion verifies a PRIMARY KEY exists after async from_dataframe(primary_key=...)
    - a PAR-07 assertion compares `table_info`/`list_roles` dict keys across sync and async
    - `uv run pytest tests/test_parity.py` exits 0
  </acceptance_criteria>
  <done>Behavioral, real-DB parity is asserted for every pair this phase touched (D-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Per-method tests for new sync + async methods</name>
  <files>tests/test_database.py, tests/test_async_database.py</files>
  <read_first>
    - tests/test_database.py and tests/test_async_database.py — existing per-method test style (class grouping, fixtures, `@pytest.mark.asyncio` for async).
    - tests/test_database_integration.py — temp-table / cleanup pattern for any DB-touching cases.
  </read_first>
  <behavior>
    - Each new method has at least a happy-path test and a key edge case (empty rows, invalid action/identifier, if_not_exists, etc.).
  </behavior>
  <action>
    Add per-method tests for the sync mirrors (insert_many, upsert_many, stream, notify) in test_database.py and for the async methods (add_primary_key, add_foreign_key, add_unique_constraint, truncate_table, drop_extension, database_exists, list_databases, create, create_from_env, and the C2 close() dispose + signature-aligned create_extension/create_schema) in test_async_database.py. Cover edge cases that the integration parity tests do not: insert_many([]) == 0; add_foreign_key invalid on_delete raises ValueError; create(if_not_exists=False) on existing raises; close() idempotent / disposes engine; create_extension(schema=...) / create_schema(owner=...) build the expected clause. Follow the existing test style; do NOT write fragile mock-heavy tests where a real-DB assertion is cleaner (D-08).
  </action>
  <verify>
    <automated>uv run pytest tests/test_database.py tests/test_async_database.py -q -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - test_database.py has tests referencing `insert_many`, `upsert_many`, `stream`, `notify`
    - test_async_database.py has tests referencing each of the 9 new async methods + `close` + `create_extension`/`create_schema` signature behavior
    - `add_foreign_key` invalid-action test asserts `ValueError`
    - `uv run pytest tests/test_database.py tests/test_async_database.py` exits 0
  </acceptance_criteria>
  <done>Every new method has dedicated happy-path + edge-case coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → pycopg_test DB | tests create/drop temp tables/databases on the real test instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-12 | Tampering | test fixtures leaving residue | mitigate | Use unique temp names (uuid suffix) and cleanup fixtures (mirror test_database_integration.py); drop throwaway databases created by create/create_from_env tests so reruns are idempotent. |
| T-11-SC | Tampering | test deps (pytest/pytest-asyncio) | accept | No NEW package installs in this phase — uses existing test stack from pyproject.toml/uv.lock; no Package Legitimacy Gate triggered. |
</threat_model>

<verification>
- `uv run pytest tests/test_parity.py tests/test_database.py tests/test_async_database.py -q` passes.
- Full suite has no NEW failures vs the Plan-00 baseline (2 pre-existing failures noted in Phase-11 verification are tracked separately).
- `uv run ruff check tests` and `uv run black --check tests` clean.
</verification>

<success_criteria>
- test_parity.py verifies return fields + real-DB behavior for this phase's pairs (PAR-08, D-03).
- Allow-lists are minimal and accurate; `listen` documented as the sole async-only behavioral exception (D-06).
- Every new method has per-method tests.
</success_criteria>

<output>
Create `.planning/phases/11-parit-sync-async-compl-te/11-06-SUMMARY.md` when done.
</output>
