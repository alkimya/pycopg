---
phase: 37-dette-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - vulture_whitelist.py
  - uv.lock
autonomous: false
requirements: [DEBT-02, AUDIT-02]
must_haves:
  truths:
    - "uv run ruff check pycopg returns 0 errors (N818 suppressed via per-file-ignore, not rename)"
    - "ruff config lives under [tool.ruff.lint] with no deprecation warning"
    - "vulture and pytest-randomly are installed in the dev dependency group"
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.ruff.lint] migration + per-file-ignore for exceptions.py + dev-group tooling"
      contains: "[tool.ruff.lint.per-file-ignores]"
    - path: "vulture_whitelist.py"
      provides: "Documented false-positive allowlist for vulture (AUDIT-02 term)"
      contains: "pycopg.exceptions.TableNotFound"
  key_links:
    - from: "pyproject.toml [tool.ruff.lint.per-file-ignores]"
      to: "pycopg/exceptions.py N818 errors"
      via: "per-file ignore"
      pattern: "pycopg/exceptions.py.*N818"
    - from: "pyproject.toml [dependency-groups] dev"
      to: "vulture + pytest-randomly availability"
      via: "uv sync --all-extras --dev"
      pattern: "vulture|pytest-randomly"
---

<objective>
Establish the tooling and lint-config foundation for Phase 37: migrate the ruff config to the
modern `[tool.ruff.lint]` layout, suppress the 4 N818 errors in `pycopg/exceptions.py` via a
documented per-file-ignore (D-01a — public exception names, renaming is breaking), add `vulture`
and `pytest-randomly` to the dev dependency group (D-07 — zero new runtime deps), and seed the
documented vulture allowlist that Plan 05 will consume.

Purpose: This is the base every downstream plan builds on — Wave 2's flaky-determinism enforcement
needs `pytest-randomly` installed, Wave 3's dead-code scan needs `vulture` + the allowlist, and the
N818 suppression is half of DEBT-02 (the `pycopg/` half). Per D-01a this is suppression, NOT
renaming, because the exception names are exported in `__all__` and renaming would be a breaking
public-surface change (deferred to v1.0.0 API freeze).
Output: Updated `pyproject.toml` (ruff + dev-group), new `vulture_whitelist.py` at project root,
refreshed `uv.lock`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dette-audit/37-CONTEXT.md
@.planning/phases/37-dette-audit/37-RESEARCH.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 contributions)
- `pyproject.toml` `[tool.ruff.lint]` stanza (migrated from deprecated `[tool.ruff]` top-level `select`/`ignore`)
- `pyproject.toml` `[tool.ruff.lint.per-file-ignores]` stanza with `"pycopg/exceptions.py" = ["N818"]` + explanation comment
- `pyproject.toml` `[dependency-groups] dev` additions: `vulture>=2.9.1`, `pytest-randomly>=3.15.0`
- `vulture_whitelist.py` (NEW, project root) — documented dead-code false-positive allowlist
- `uv.lock` refreshed by `uv sync --all-extras --dev`
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm vulture + pytest-randomly package legitimacy ([ASSUMED] gate)</name>
  <read_first>
    - .planning/phases/37-dette-audit/37-RESEARCH.md (## Package Legitimacy Audit — both packages [ASSUMED], slopcheck unavailable)
  </read_first>
  <what-built>
    Nothing built yet — this is the package-legitimacy gate required before any package-manager
    install. RESEARCH.md classified both new dev-group packages as [ASSUMED] (slopcheck could not
    run; only PyPI registry verification was done).
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/vulture/ — confirm it is the canonical Python dead-code scanner
       (author: Jendrik Seipp), recent release, source repo linked.
    2. Open https://pypi.org/project/pytest-randomly/ — confirm it is the pytest order-randomization
       plugin (author: Adam Johnson / adamchainz), recent release, source repo linked.
    3. Confirm neither is a typosquat and both match their expected function in RESEARCH.md.
  </how-to-verify>
  <action>Package-legitimacy gate (no code change): present the two PyPI project pages for human confirmation that `vulture` and `pytest-randomly` are the canonical, non-typosquat packages before any dev-group install. This is a blocking-human checkpoint and is NOT auto-approvable (legitimacy gates ignore workflow.auto_advance).</action>
  <resume-signal>Type "approved" to proceed with the dev-group install, or name a concern.</resume-signal>
  <acceptance_criteria>
    - Human has confirmed both packages on their PyPI project pages and typed approval.
    - This checkpoint is NOT auto-approvable (legitimacy gates ignore workflow.auto_advance).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate ruff config to [tool.ruff.lint] + add N818 per-file-ignore + dev-group tooling</name>
  <read_first>
    - pyproject.toml (lines 62-66 [dependency-groups] dev; lines 82-90 [tool.ruff] + [tool.pytest.ini_options])
    - .planning/phases/37-dette-audit/37-RESEARCH.md (## DEBT-02 ruff inventory; ## Architecture Patterns → Ruff Config Migration Pattern; the exact target TOML stanza)
    - pycopg/exceptions.py (the 4 N818 sites: lines 24, 30, 36, 48)
  </read_first>
  <action>
    In `pyproject.toml`: (1) Remove the deprecated top-level `select = ["E","F","W","I","N","UP"]`
    and `ignore = ["E501"]` keys from `[tool.ruff]` (lines 85-86), keeping `line-length = 100` and
    `target-version = "py311"` under `[tool.ruff]`. (2) Add a new `[tool.ruff.lint]` section with
    `select = ["E", "F", "W", "I", "N", "UP"]` and `ignore = ["E501"]`. (3) Add a
    `[tool.ruff.lint.per-file-ignores]` section with `"pycopg/exceptions.py" = ["N818"]` and a
    two-line comment above it stating that the 4 exception names (`ExtensionNotAvailable`,
    `TableNotFound`, `InvalidIdentifier`, `DatabaseExists`) are exported in `__all__`, renaming is a
    breaking API change, reconsider at v1.0.0 API freeze. This implements D-01a (suppression, not
    rename). (4) In `[dependency-groups] dev` (line 63), add `"vulture>=2.9.1"` and
    `"pytest-randomly>=3.15.0"` to the list (D-07). Do NOT touch `--cov-fail-under=94` in
    `[tool.pytest.ini_options]` (the move to 95 is Phase 39). Run `uv sync --all-extras --dev` to
    refresh `uv.lock` and install the two packages.
  </action>
  <verify>
    <automated>uv run ruff check pycopg 2>&1 | grep -v '^#' | grep -ic "N818\|deprecat" | grep -qx 0 && uv run ruff check pycopg</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check pycopg` exits 0 with 0 errors and no deprecation warning about
      `lint.select`/`lint.per-file-ignores`.
    - `pyproject.toml` contains the literal line `[tool.ruff.lint.per-file-ignores]` and the entry
      `"pycopg/exceptions.py" = ["N818"]`.
    - `pyproject.toml` `[tool.ruff]` no longer has top-level `select`/`ignore` keys; they live under
      `[tool.ruff.lint]`.
    - `pyproject.toml` `[dependency-groups] dev` contains both `vulture` and `pytest-randomly`.
    - `uv run python -c "import vulture; import pytest_randomly"` exits 0 (both installed).
    - `[tool.pytest.ini_options]` `addopts` still contains `--cov-fail-under=94` (unchanged).
  </acceptance_criteria>
  <done>ruff is clean on pycopg/ (0 N818, 0 deprecation), and vulture + pytest-randomly are installed in the dev group without touching runtime deps or the coverage ratchet.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the documented vulture allowlist at project root</name>
  <read_first>
    - .planning/phases/37-dette-audit/37-RESEARCH.md (## AUDIT-02 vulture Allowlist Form — the `.py` whitelist recommendation + run command)
    - pycopg/exceptions.py (the 4 public exception names that are vulture false positives — never raised internally except TableNotFound after Plan 04)
  </read_first>
  <action>
    Create `vulture_whitelist.py` at the project root. It is a documented false-positive allowlist
    (the exact term AUDIT-02 uses). Per D-07 / Claude's-discretion (the `.py` whitelist form is the
    chosen shape over a config file). Include a header comment giving the run command
    `uv run vulture pycopg/ vulture_whitelist.py --min-confidence 80`, then reference the 4 public
    exception names (`pycopg.exceptions.ExtensionNotAvailable`, `pycopg.exceptions.TableNotFound`,
    `pycopg.exceptions.InvalidIdentifier`, `pycopg.exceptions.DatabaseExists`) as intentional
    false positives (exported in `__all__` for user-facing `except` clauses), with a comment noting
    `TableNotFound` gains an internal raise site in Plan 04 (DEBT-05). This is the SEED allowlist;
    Plan 05 refines it after the live vulture scan surfaces the real false-positive set. Do not add
    any entry that is not a confirmed false positive — Plan 05 owns the scan-driven refinement.
  </action>
  <verify>
    <automated>uv run vulture pycopg/ vulture_whitelist.py --min-confidence 80 >/dev/null 2>&1; echo "exit=$? (non-fatal; Plan 05 triages)"; test -f vulture_whitelist.py && grep -q "TableNotFound" vulture_whitelist.py</automated>
  </verify>
  <acceptance_criteria>
    - `vulture_whitelist.py` exists at the project root.
    - It contains the run command `uv run vulture pycopg/ vulture_whitelist.py --min-confidence 80`
      in a comment.
    - It references `pycopg.exceptions.TableNotFound` (and the other 3 public exceptions) as
      documented false positives.
    - `uv run vulture pycopg/ vulture_whitelist.py --min-confidence 80` runs without an import/parse
      error on the whitelist file itself (a nonzero exit from real findings is acceptable here —
      Plan 05 triages findings).
  </acceptance_criteria>
  <done>The vulture allowlist exists, is documented, and is consumable by `vulture`; Plan 05 will refine it against the live scan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds NO new external input path and NO new public API surface. The only boundary touched
is the developer toolchain (dev-group dependency install), not runtime.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-SC | Tampering | `uv sync` install of `vulture` + `pytest-randomly` (dev-group) | mitigate | Both are [ASSUMED] in RESEARCH.md (slopcheck unavailable); Task 1 is a blocking-human legitimacy checkpoint verifying both on pypi.org before any install. Dev-group only — never ships to runtime. |

No other STRIDE categories apply: ruff config + allowlist are dev-only static-analysis config with no
external inputs, no auth/session surface, no data flow.
</threat_model>

<verification>
- `uv run ruff check pycopg` exits 0 (N818 suppressed, no deprecation warning).
- `uv run python -c "import vulture; import pytest_randomly"` exits 0.
- `vulture_whitelist.py` exists and is parseable by vulture.
- `--cov-fail-under=94` unchanged in pyproject.toml.
</verification>

<success_criteria>
- DEBT-02 (pycopg/ half): `uv run ruff check pycopg` returns exactly 0 errors via per-file-ignore.
- AUDIT-02 (tooling half): vulture + pytest-randomly installed in dev-group; documented allowlist seeded.
- Zero new runtime dependencies; coverage ratchet untouched at 94.
</success_criteria>

<output>
Create `.planning/phases/37-dette-audit/37-01-SUMMARY.md` when done.
</output>
