---
phase: 37-dette-audit
plan: 05
type: execute
wave: 3
depends_on: ["37-01", "37-02", "37-03", "37-04"]
files_modified:
  - vulture_whitelist.py
  - .planning/phases/37-dette-audit/37-REVIEW.md
  - .planning/phases/37-dette-audit/37-DECISIONS.md
  - .planning/milestones/v0.6.0-MILESTONE-AUDIT.md
  - .planning/STATE.md
autonomous: false
requirements: [AUDIT-01, AUDIT-02, NYQ-01, DEBT-03]
must_haves:
  truths:
    - "A severity-classified code-review report for pycopg/ exists at 37-REVIEW.md"
    - "Every HIGH finding is fixed in-phase; every MEDIUM is fixed or deferred-to-v1.0.0 with justification; LOW is logged"
    - "vulture scan of pycopg/ is complete; confirmed dead code removed; false positives documented in the allowlist"
    - "v0.6.0-MILESTONE-AUDIT.md nyquist block lists 22,23,24 as compliant and overall=compliant"
    - "37-DECISIONS.md consolidates every justified closure (DEBT-03b, audit MEDIUM dispositions, NYQ-01 sign-off, vulture allowlist rationale, any new isolation finding)"
  artifacts:
    - path: ".planning/phases/37-dette-audit/37-REVIEW.md"
      provides: "AUDIT-01 severity-classified report for pycopg/"
    - path: ".planning/phases/37-dette-audit/37-DECISIONS.md"
      provides: "D-09 single consolidated decisions journal"
      contains: "NYQ-01"
    - path: ".planning/milestones/v0.6.0-MILESTONE-AUDIT.md"
      provides: "nyquist block flipped partial→compliant for 22-24 (NYQ-01)"
      contains: "compliant_phases"
    - path: "vulture_whitelist.py"
      provides: "AUDIT-02 refined allowlist after the live scan"
  key_links:
    - from: "/gsd-code-review pycopg/"
      to: "37-REVIEW.md + disposition bar in 37-DECISIONS.md"
      via: "capture → triage → apply"
      pattern: "HIGH|MEDIUM|LOW"
    - from: "v0.6.0-MILESTONE-AUDIT.md surviving evidence"
      to: "NYQ-01 sign-off in 37-DECISIONS.md"
      via: "retroactive nyquist promotion"
      pattern: "nyquist_compliant|compliant_phases"
---

<objective>
Run the tooled audit pass and close the sign-off debt: (1) `/gsd-code-review` over `pycopg/` →
severity-classified `37-REVIEW.md`, then apply the D-06 disposition bar (HIGH fixed in-phase, MEDIUM
fixed-or-deferred-with-justification, LOW logged); (2) run `vulture` over `pycopg/`, remove confirmed
dead code and refine the documented allowlist with the real false-positive set (AUDIT-02); (3) flip the
nyquist block in `v0.6.0-MILESTONE-AUDIT.md` from partial→compliant for phases 22-24 and record the
formal sign-off citing surviving evidence (NYQ-01 / D-08); (4) write the single consolidated
`37-DECISIONS.md` (D-09) capturing every justified closure — DEBT-03b behavioral closures, audit MEDIUM
dispositions, the NYQ-01 rationale, the vulture allowlist reason, any DEBT-05 fallback (unused — the
truncate_table site landed), and any new isolation finding surfaced by Plan 03's randomization — then
roll a short summary into STATE.md → Deferred Items.

Purpose: ROADMAP success criteria 3 (advisory closures recorded), 4 (resolution documented), and 5 (audit
report with dispositions + vulture allowlist + phases-22-24 at nyquist_compliant). This is the convergence
plan — it depends on Plans 01-04 so the reviewer/scanner see the cleaned tree (ruff clean, dead
monkeypatches gone, raise site landed). Per D-08, do NOT fabricate VALIDATION.md files for 22-24 — the
legitimate sign-off is editing the surviving authority document (the milestone audit) + recording in
37-DECISIONS.md. This plan is autonomous:false because the code-review run + MEDIUM-disposition triage is
human-gated.
Output: 37-REVIEW.md, 37-DECISIONS.md, refined vulture_whitelist.py, flipped milestone audit, STATE.md roll-up.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dette-audit/37-CONTEXT.md
@.planning/phases/37-dette-audit/37-RESEARCH.md
@.planning/milestones/v0.6.0-MILESTONE-AUDIT.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 05 contributions)
- `.planning/phases/37-dette-audit/37-REVIEW.md` (NEW) — AUDIT-01 severity-classified code-review report for `pycopg/`
- `.planning/phases/37-dette-audit/37-DECISIONS.md` (NEW) — D-09 consolidated decisions journal
- `vulture_whitelist.py` — refined after the live `vulture` scan (AUDIT-02 documented false positives)
- `.planning/milestones/v0.6.0-MILESTONE-AUDIT.md` — nyquist block flipped partial→compliant for 22-24 (NYQ-01)
- `.planning/STATE.md` — Deferred Items roll-up of the consolidated dispositions
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Run vulture scan, remove confirmed dead code, refine the documented allowlist (AUDIT-02)</name>
  <read_first>
    - .planning/phases/37-dette-audit/37-RESEARCH.md (## AUDIT-02 — vulture allowlist form, run command, --min-confidence 80; Architectural Responsibility Map row for vulture)
    - vulture_whitelist.py (the seed allowlist from Plan 01)
  </read_first>
  <action>
    Run `uv run vulture pycopg/ vulture_whitelist.py --min-confidence 80` and capture the findings.
    For each reported item: if it is genuinely unreachable/dead code in `pycopg/`, REMOVE it (confirmed
    dead code removed per AUDIT-02); if it is a false positive (dynamic attribute access in accessor
    patterns, public names exported in `__all__`, names used only by `except` clauses), ADD it to
    `vulture_whitelist.py` with a one-line comment explaining WHY it is a false positive (the
    documented allowlist — AUDIT-02's exact term). The 4 public exceptions are already seeded;
    `TableNotFound` is now raised internally (Plan 04) so update its comment to note it has a raise
    site. Keep removing/allowlisting until `uv run vulture pycopg/ vulture_whitelist.py
    --min-confidence 80` reports no UNEXPLAINED dead code (every remaining report is either removed or
    in the documented allowlist). Record the allowlist rationale summary for 37-DECISIONS.md (Task 4).
    Do NOT remove anything that is part of the public API surface (non-breaking constraint) — public
    but internally-unused names go in the allowlist, never deleted.
  </action>
  <verify>
    <automated>uv run vulture pycopg/ vulture_whitelist.py --min-confidence 80; echo "vulture exit=$?"; test -f vulture_whitelist.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run vulture pycopg/ vulture_whitelist.py --min-confidence 80` produces no unexplained dead-code
      report (every finding is either removed from source or present in the documented allowlist).
    - `vulture_whitelist.py` has a comment for each false-positive entry explaining why it is allowlisted.
    - No public API symbol (anything in a module `__all__`) was deleted (non-breaking — `uv run python -c "import pycopg; from pycopg import TableNotFound, ExtensionNotAvailable, InvalidIdentifier, DatabaseExists"` exits 0).
    - `uv run ruff check pycopg tests` still 0 and `PGDATABASE=pycopg_test2 uv run pytest -q` still green after any dead-code removal.
  </acceptance_criteria>
  <done>vulture scan is clean, confirmed dead code removed without touching public surface, and the allowlist documents every false positive.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run /gsd-code-review on pycopg/ and triage findings against the disposition bar (AUDIT-01)</name>
  <read_first>
    - .planning/phases/37-dette-audit/37-RESEARCH.md (## AUDIT-01 — skill at /home/loc/.claude/agents/gsd-code-reviewer.md; D-06 disposition bar; report at 37-REVIEW.md; run AFTER ruff fixes)
    - 37-CONTEXT.md D-06 (HIGH fixed in phase / MEDIUM fixed-or-deferred-justified / LOW logged)
  </read_first>
  <what-built>
    Plans 01-04 already landed: ruff-clean tree, dead monkeypatches removed, TableNotFound raise site,
    WR-01/upsert/sequences fixes, flaky tests de-flaked, vulture scan clean (Task 1). The tree is now in
    its cleanest state so the reviewer does not re-flag already-resolved issues.
  </what-built>
  <how-to-verify>
    1. Run the code review over the full `pycopg/` source: invoke `/gsd-code-review` scoped to `pycopg/`
       (skill at `/home/loc/.claude/agents/gsd-code-reviewer.md`).
    2. Capture its severity-classified output to `.planning/phases/37-dette-audit/37-REVIEW.md`
       (BLOCKER/WARNING/INFO ≈ HIGH/MEDIUM/LOW).
    3. Apply the D-06 disposition bar:
       - HIGH (BLOCKER): fix in this phase now (make the code change, add/adjust a test if behavioral).
       - MEDIUM (WARNING): fix now OR defer to v1.0.0 with a written justification (the justification
         goes into 37-DECISIONS.md in Task 4).
       - LOW (INFO): log only (also into 37-DECISIONS.md).
    4. Confirm: no HIGH finding remains unaddressed; every MEDIUM has a disposition (fixed or
       deferred-with-justification); the full suite + ruff are still green after any fixes.
  </how-to-verify>
  <action>Run `/gsd-code-review` scoped to `pycopg/` (skill at `/home/loc/.claude/agents/gsd-code-reviewer.md`), capture the severity-classified output to `.planning/phases/37-dette-audit/37-REVIEW.md`, then apply the D-06 disposition bar: fix every HIGH in-phase now; fix or defer-to-v1.0.0-with-justification every MEDIUM (justification → 37-DECISIONS.md, Task 4); log every LOW. Re-run `uv run ruff check pycopg tests` and the full suite green after any fixes. This task is human-gated because the review run and the MEDIUM triage require operator judgment.</action>
  <resume-signal>Type "approved" once 37-REVIEW.md exists, all HIGH are fixed, and every MEDIUM has a disposition; or describe remaining items.</resume-signal>
  <acceptance_criteria>
    - `.planning/phases/37-dette-audit/37-REVIEW.md` exists and classifies findings by severity (HIGH/MEDIUM/LOW or BLOCKER/WARNING/INFO).
    - Every HIGH finding is fixed in-phase (no open HIGH); after fixes `uv run ruff check pycopg tests` is 0 and `PGDATABASE=pycopg_test2 uv run pytest` is green.
    - Every MEDIUM finding has a recorded disposition (fixed, or deferred-to-v1.0.0 with justification captured for 37-DECISIONS.md).
    - LOW findings are captured for logging in 37-DECISIONS.md.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: NYQ-01 — flip the v0.6.0 nyquist block to compliant after spot-checking the accessor reqs</name>
  <read_first>
    - .planning/phases/37-dette-audit/37-RESEARCH.md (## Open Item 3 + ## NYQ-01 Validation Architecture — exact frontmatter edits; spot-check table ADM-01/MNT-01/BKP-01/SCH-01/SCH-02; do-NOT-fabricate rule)
    - .planning/milestones/v0.6.0-MILESTONE-AUDIT.md (frontmatter nyquist block: compliant_phases ["21"], partial_phases ["22","23","24"], overall partial — lines 26-30; tech_debt note about missing sign-off — lines 22-24)
  </read_first>
  <action>
    Spot-check that the five v0.6.0 accessor requirements still hold in current (v0.9.0+) code before
    signing off (RESEARCH confirms they do): `ADM-01` (`AdminAccessor`/`AsyncAdminAccessor` in
    `pycopg/admin.py`), `MNT-01` (`MaintAccessor` in `pycopg/maint.py`), `BKP-01` (`BackupAccessor` in
    `pycopg/backup.py`), `SCH-01` (`SchemaAccessor`/`AsyncSchemaAccessor` in `pycopg/schema.py`),
    `SCH-02` (`create_spatial_index`/`list_geometry_columns` on `SpatialAccessor` in `pycopg/spatial.py`)
    — grep each class/method to confirm it still exists. Then edit `v0.6.0-MILESTONE-AUDIT.md`
    frontmatter (the surviving authority document — editing it is the legitimate sign-off, NOT
    fabrication): set `compliant_phases: ["21", "22", "23", "24"]`, `partial_phases: []`, and
    `overall: compliant` in the `nyquist:` block. Do NOT create fake VALIDATION.md files for 22-24
    (D-08 — they were never archived; backdating them would be false history). Update the body's
    tech_debt note about the missing Nyquist sign-off to reflect it is now closed (reference
    37-DECISIONS.md). The formal sign-off prose + spot-check results go into 37-DECISIONS.md in Task 4.
  </action>
  <verify>
    <automated>grep -q 'overall: compliant' .planning/milestones/v0.6.0-MILESTONE-AUDIT.md && grep -A1 'compliant_phases' .planning/milestones/v0.6.0-MILESTONE-AUDIT.md | grep -q '24' && uv run python -c "import pycopg; from pycopg import admin, maint, backup, schema, spatial"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v0.6.0-MILESTONE-AUDIT.md` `nyquist:` block has `compliant_phases: ["21", "22", "23", "24"]`, `partial_phases: []`, `overall: compliant`.
    - The five accessor classes still import: `uv run python -c "import pycopg; from pycopg import admin, maint, backup, schema, spatial"` exits 0.
    - No new VALIDATION.md files were created for phases 22-24 (D-08 — `find .planning/phases/2[234]-* -name '*VALIDATION*'` returns nothing).
    - The milestone audit body note about the missing Nyquist sign-off now references that it is closed in 37-DECISIONS.md.
  </acceptance_criteria>
  <done>Phases 22-24 are retroactively promoted to nyquist_compliant via the surviving authority document, with no fabricated artifacts; the success criterion wording is reconciled in 37-DECISIONS.md.</done>
</task>

<task type="auto">
  <name>Task 4: Write the consolidated 37-DECISIONS.md (D-09) and roll up into STATE.md</name>
  <read_first>
    - .planning/phases/37-dette-audit/37-RESEARCH.md (## DEBT-03 → D-03b closures WR-03/%-structural/IN-03 with justifications; ## NYQ-01 sign-off mechanism; Assumptions/Open Questions)
    - 37-CONTEXT.md (D-03b, D-06, D-08, D-09 — the closures and the single-file requirement)
    - .planning/STATE.md (## Deferred Items table — the roll-up target)
    - The Plan 03 SUMMARY (any NEW isolation finding to log) and the Task 2 triage output (audit MEDIUM/LOW dispositions)
  </read_first>
  <action>
    Create `.planning/phases/37-dette-audit/37-DECISIONS.md` (D-09 single consolidated file). Sections:
    (1) DEBT-03b behavioral closures with justification (deferred to v1.0.0): WR-03 (INTERVAL literal vs
    `%s` — protected by `validate_interval`, behavioral change risky), `%`/`%s` in caller-supplied
    structural SQL (`aggregates`/`where` — accepted as caller-error UX, not injection), IN-03 fragile
    `chunk_seq` test helper (test-only, parses internal TSDB chunk-name format) — cite the RESEARCH
    file:line evidence for each. (2) AUDIT-01 MEDIUM dispositions (from Task 2 triage) — each MEDIUM
    finding fixed or deferred-to-v1.0.0-with-justification; LOW findings logged. (3) NYQ-01 formal
    sign-off: cite `v0.6.0-MILESTONE-AUDIT.md` as surviving evidence, state the VALIDATION.md files for
    22-24 were never archived, list the ADM-01/MNT-01/BKP-01/SCH-01/SCH-02 spot-check results, and
    reconcile the success-criterion wording ("VALIDATION.md ... nyquist_compliant: true") with artifact
    reality (the milestone-audit edit IS the legitimate sign-off). (4) DEBT-05 note: the
    `truncate_table` raise site was used (the `__all__` removal fallback was NOT needed). (5) vulture
    allowlist rationale (why the documented false positives are intentional). (6) Any NEW isolation bug
    surfaced by pytest-randomly in Plan 03 — logged for disposition per D-05 (do NOT chase). Then add a
    short roll-up to `.planning/STATE.md` → `## Deferred Items` table referencing 37-DECISIONS.md as the
    consolidated journal. The verifier checks the ROADMAP success criteria against this file.
  </action>
  <verify>
    <automated>test -f .planning/phases/37-dette-audit/37-DECISIONS.md && grep -q 'NYQ-01' .planning/phases/37-dette-audit/37-DECISIONS.md && grep -q 'WR-03' .planning/phases/37-dette-audit/37-DECISIONS.md && grep -q 'IN-03' .planning/phases/37-dette-audit/37-DECISIONS.md && grep -q '37-DECISIONS' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/37-dette-audit/37-DECISIONS.md` exists and contains the DEBT-03b closures (WR-03, `%`-structural, IN-03) each with a justification.
    - It contains the AUDIT-01 MEDIUM dispositions and LOW log from Task 2.
    - It contains the NYQ-01 formal sign-off (surviving-evidence citation + spot-check results + wording reconciliation).
    - It records that DEBT-05 used the truncate_table raise site (fallback unused) and the vulture allowlist rationale.
    - Any new isolation finding from Plan 03 is logged (or an explicit "none surfaced" statement).
    - `.planning/STATE.md` `## Deferred Items` references `37-DECISIONS.md`.
  </acceptance_criteria>
  <done>A single consolidated decisions journal records every justified closure, sign-off, and disposition; STATE.md points to it; the verifier can check all 5 success criteria against it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new external input path and no public API surface change. This plan runs dev-only tooling (vulture,
code-review skill), edits planning/audit documents, and removes confirmed dead code. The one runtime-
adjacent action is dead-code removal in `pycopg/` — guarded by the non-breaking constraint (never delete
a public `__all__` symbol; allowlist instead) and re-verified green.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05-01 | Tampering | dead-code removal in `pycopg/` driven by vulture | mitigate | Only confirmed-dead, non-public code is removed; anything in a module `__all__` goes to the documented allowlist, never deleted. Full suite + ruff re-run green after removal, and a `from pycopg import ...` smoke confirms the public surface is intact (non-breaking constraint). |
| (other) | — | vulture / gsd-code-review (dev-only), planning-doc + milestone-audit edits | accept | Dev tooling and documentation/sign-off edits have no runtime attack surface; the NYQ-01 milestone-audit edit changes a planning record, not code. |

The audit pass itself (AUDIT-01) is the security-review instrument; any HIGH security finding it surfaces is
fixed in-phase per the D-06 disposition bar before the plan closes.
</threat_model>

<verification>
- `uv run vulture pycopg/ vulture_whitelist.py --min-confidence 80` reports no unexplained dead code.
- `.planning/phases/37-dette-audit/37-REVIEW.md` exists; all HIGH fixed; every MEDIUM dispositioned.
- `v0.6.0-MILESTONE-AUDIT.md` nyquist block: `overall: compliant`, 22/23/24 in `compliant_phases`.
- `.planning/phases/37-dette-audit/37-DECISIONS.md` exists with all required sections; STATE.md references it.
- After all edits: `uv run ruff check pycopg tests` is 0 and `PGDATABASE=pycopg_test2 uv run pytest` is green.
</verification>

<success_criteria>
- AUDIT-01: severity-classified report for `pycopg/` exists with every HIGH fixed and every MEDIUM dispositioned.
- AUDIT-02: vulture scan complete, confirmed dead code removed, false positives in the documented allowlist.
- NYQ-01: phases 22-24 promoted to nyquist_compliant via the surviving authority doc + formal sign-off recorded.
- DEBT-03 (close half / D-03b): WR-03, %-structural, IN-03 closed with justification in 37-DECISIONS.md.
</success_criteria>

<output>
Create `.planning/phases/37-dette-audit/37-05-SUMMARY.md` when done.
</output>
