---
phase: 37-dette-audit
plan: 03
type: execute
wave: 2
depends_on: ["37-01", "37-02"]
files_modified:
  - tests/test_integration.py
  - tests/test_postgis_errors.py
  - tests/test_etl_accessor.py
autonomous: true
requirements: [DEBT-01]
must_haves:
  truths:
    - "The full suite (uv run pytest) passes deterministically with no -o addopts='' and no special order"
    - "test_async_transaction_fix no longer leaks connection state (application_name reset)"
    - "test_create_spatial_index_name_parameter uses a per-run UUID table name (no hardcoded collision)"
    - "The ~2.7% bound-param watermark test is mock-call-order deterministic"
    - "pytest-randomly randomizes order and the suite still passes (re-flake prevention)"
  artifacts:
    - path: "tests/test_integration.py"
      provides: "test_async_transaction_fix with application_name RESET in finally (no pool leak)"
    - path: "tests/test_postgis_errors.py"
      provides: "test_create_spatial_index_name_parameter with UUID-ized table name"
    - path: "tests/test_etl_accessor.py"
      provides: "watermark bound-param test(s) with mock.reset_mock() / last-call assertion"
  key_links:
    - from: "pytest-randomly (Plan 01)"
      to: "deterministic full-suite ordering"
      via: "uv run pytest -p randomly"
      pattern: "randomly"
---

<objective>
Fix the root-cause fixture-isolation flakiness in the three known flaky tests (D-05) so the full suite
passes deterministically without `-o addopts=""` and without depending on test order, then let
`pytest-randomly` (installed in Plan 01) randomize order to prove determinism and prevent re-flake.

Purpose: ROADMAP success criterion 1 — `uv run pytest` must pass deterministically; the three flaky
tests (`test_async_transaction_fix`, `test_create_spatial_index_name_parameter`, the ~2.7%
bound-param watermark test) are the surviving fixture-isolation debt carried since v0.6.0. Each has a
specific root cause identified in RESEARCH.md Open Item 2: connection-state leakage (application_name),
a hardcoded temp-table name collision, and mock call-order sensitivity. Per D-05, if randomization
surfaces a NEW large/unrelated isolation bug, it is LOGGED for disposition in 37-DECISIONS.md (Plan 05),
NOT fixed unboundedly in this phase.
Output: 3 test files de-flaked; randomized full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dette-audit/37-CONTEXT.md
@.planning/phases/37-dette-audit/37-RESEARCH.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 contributions)
- `tests/test_integration.py` — `TestAsyncIntegration::test_async_transaction_fix` hardened with an
  `application_name` reset in a `finally` (connection-state leak fix)
- `tests/test_postgis_errors.py` — `TestPostGISErrorHandling::test_create_spatial_index_name_parameter`
  uses a per-run UUID table name (no hardcoded `test_spatial_custom_name` collision)
- `tests/test_etl_accessor.py` — `test_incremental_watermark_as_bound_param` (and async twin)
  hardened with `mock.reset_mock()` / explicit last-call assertion
- (Plan 05 input) Any NEW isolation bug surfaced by randomization is recorded for 37-DECISIONS.md
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: De-flake test_async_transaction_fix and test_create_spatial_index_name_parameter</name>
  <read_first>
    - .planning/phases/37-dette-audit/37-RESEARCH.md (## Open Item 2 — root-cause analysis + fixes for both tests; Pitfall 1/4/5; A3 reproduce-before-fix note)
    - tests/test_integration.py (line 98, `TestAsyncIntegration::test_async_transaction_fix` — the SET application_name / SHOW application_name assertion)
    - tests/test_postgis_errors.py (line 112, `test_create_spatial_index_name_parameter` — hardcoded `table_name = "test_spatial_custom_name"` + `CREATE TEMP TABLE`)
    - tests/test_database_integration.py (~line 27 `temp_table_name` fixture — the established UUID naming pattern to mirror)
  </read_first>
  <action>
    First REPRODUCE each flake before fixing (per RESEARCH A3):
    `PGDATABASE=pycopg_test2 uv run pytest tests/test_postgis_errors.py::TestPostGISErrorHandling::test_create_spatial_index_name_parameter -x` and the integration test, optionally under
    `-p randomly --randomly-seed=random` a few times.
    (a) `test_async_transaction_fix` (test_integration.py:98): the test SETs
    `application_name = 'pycopg_test_trans'` then asserts `SHOW application_name` — flaky under pool
    connection reuse. Fix: wrap the body so it RESETs `application_name` in a `finally` (issue
    `RESET application_name` on the same session before the session closes), or assert against the
    same connection that ran the SET; the minimal robust fix per RESEARCH is the `finally` reset so
    no recycled pool connection carries the stale value forward. Do NOT change the public transaction
    behavior under test — only the cleanup.
    (b) `test_create_spatial_index_name_parameter` (test_postgis_errors.py:112): replace the
    hardcoded `table_name = "test_spatial_custom_name"` with a per-run UUID name following the
    existing pattern `f"test_spatial_{uuid.uuid4().hex[:8]}"` (mirror the `temp_table_name` fixture in
    test_database_integration.py). Keep the `index_name` lookup keyed on the (now-unique) table name.
    Ensure `import uuid` is available at the top of test_postgis_errors.py (add if absent). The E722
    cleanup blocks in this file were already fixed in Plan 02 — do not re-touch lines 50/146/182.
    After both fixes, run each test in isolation AND under randomization to confirm green.
  </action>
  <verify>
    <automated>PGDATABASE=pycopg_test2 uv run pytest tests/test_integration.py::TestAsyncIntegration::test_async_transaction_fix tests/test_postgis_errors.py::TestPostGISErrorHandling::test_create_spatial_index_name_parameter -v -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_postgis_errors.py` `test_create_spatial_index_name_parameter` no longer contains the
      literal `"test_spatial_custom_name"` hardcoded name; it uses a `uuid.uuid4().hex` table name.
    - `tests/test_integration.py` `test_async_transaction_fix` issues a `RESET application_name`
      (or equivalent connection-state reset) in a `finally`/cleanup path.
    - Both tests pass in isolation: the command above exits 0.
    - Both tests pass under randomization across ≥3 runs:
      `PGDATABASE=pycopg_test2 uv run pytest tests/test_integration.py tests/test_postgis_errors.py -p randomly --randomly-seed=random -q` green each run.
  </acceptance_criteria>
  <done>Both connection-state / temp-table-collision flakes are root-caused and fixed; green in isolation and under randomization.</done>
</task>

<task type="auto">
  <name>Task 2: De-flake the ~2.7% bound-param watermark test(s) + prove full-suite determinism under pytest-randomly</name>
  <read_first>
    - .planning/phases/37-dette-audit/37-RESEARCH.md (## Open Item 2 → ~2.7% bound-param; Open Question 1 — reproduce procedure; Open Question 2 — pytest-randomly × asyncio_mode auto)
    - tests/test_etl_accessor.py (~line 1830 `test_incremental_watermark_as_bound_param` sync; ~line 2579 async twin — patch db.execute + inspect call_args for named param "wm")
  </read_first>
  <action>
    Reproduce the ~2.7% flake:
    `PGDATABASE=pycopg_test2 uv run pytest tests/test_etl_accessor.py -p randomly --randomly-seed=random -x` run several times (per RESEARCH Open Question 1). The flake is mock
    call-order sensitivity in `test_incremental_watermark_as_bound_param` (~line 1830) and its async
    twin `test_async_incremental_watermark_as_bound_param` (~line 2579): the test patches `db.execute`
    and inspects `call_args` to assert the watermark was passed as named param `"wm"`. Fix: call
    `mock.reset_mock()` before the run sequence under test, OR capture `call_args_list` explicitly and
    assert on the specific (e.g. last) call rather than `call_args` of an ambiguous call. Apply the
    same fix to both sync and async twins for parity. Then run the FULL suite under randomization to
    prove determinism. If randomization surfaces a NEW large/unrelated isolation bug beyond these
    three tests, do NOT chase it — capture the failing test name + seed + symptom in the SUMMARY for
    Plan 05 to log in 37-DECISIONS.md (D-05 boundary: log for disposition, do not extend the phase
    unboundedly).
  </action>
  <verify>
    <automated>PGDATABASE=pycopg_test2 uv run pytest tests/test_etl_accessor.py -k "watermark_as_bound_param" -p randomly --randomly-seed=random -q -o "addopts=-p randomly"</automated>
  </verify>
  <acceptance_criteria>
    - Both `test_incremental_watermark_as_bound_param` and its async twin call `reset_mock()` (or
      assert on an explicit `call_args_list` entry) — grep confirms `reset_mock` or `call_args_list`
      present at both sites.
    - The watermark tests pass across ≥3 randomized runs (the verify command green each time).
    - FULL randomized suite is green:
      `PGDATABASE=pycopg_test2 uv run pytest -p randomly --randomly-seed=random` exits 0 (run it; if a
      NEW unrelated isolation failure appears, record it in the SUMMARY for Plan 05 — do not fix here).
    - The full suite passes WITHOUT `-o addopts=""` and without manual ordering:
      `PGDATABASE=pycopg_test2 uv run pytest` exits 0.
  </acceptance_criteria>
  <done>All three known flaky tests are deterministic; the randomized full suite is green; any new isolation finding is logged (not chased) for Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new external input, no public API surface change. All edits are test-side isolation hardening.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | test fixture-isolation fixes (UUID table name, application_name reset, mock reset) | accept | No new external inputs or runtime behavior; changes harden test isolation only. The UUID table name and application_name reset reduce cross-test state leakage — a reliability improvement, not a new attack surface. |

No STRIDE category is newly exposed by this housekeeping.
</threat_model>

<verification>
- `PGDATABASE=pycopg_test2 uv run pytest` exits 0 (full suite, default opts, deterministic).
- `PGDATABASE=pycopg_test2 uv run pytest -p randomly --randomly-seed=random` exits 0 (randomized).
- The three named flaky tests pass in isolation and under randomization.
</verification>

<success_criteria>
- DEBT-01: the three flaky tests pass deterministically in the full suite, no `-o addopts=""`, no order
  dependency; `pytest-randomly` confirms determinism; any new isolation finding logged for Plan 05.
</success_criteria>

<output>
Create `.planning/phases/37-dette-audit/37-03-SUMMARY.md` when done — include any NEW isolation findings
surfaced by randomization for Plan 05 to log in 37-DECISIONS.md.
</output>
