---
phase: 37-dette-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/setup_test_db.py
  - tests/test_async_database.py
  - tests/test_database.py
  - tests/test_pool.py
  - tests/test_pool_stress.py
  - tests/test_session_edge_cases.py
  - tests/test_postgis_errors.py
  - tests/test_sql_injection.py
autonomous: true
requirements: [DEBT-02, DEBT-04]
must_haves:
  truths:
    - "uv run ruff check tests returns 0 errors (all 31 W291/F841/E722 corrected)"
    - "The async fixture in test_sql_injection.py no longer carries the two dead flat-method monkeypatches"
    - "The SQL injection async tests still pass (the live has_extension patch is preserved)"
  artifacts:
    - path: "tests/setup_test_db.py"
      provides: "W291 trailing-whitespace fixed (lines 103-105, 116-117)"
    - path: "tests/test_sql_injection.py"
      provides: "DEBT-04 — dead db.role_exists / db.has_extension monkeypatches removed; live real_schema patch kept"
  key_links:
    - from: "tests/ lint state"
      to: "uv run ruff check tests"
      via: "mechanical fixes (remove unused vars, trailing ws, bare except)"
      pattern: "ruff check tests"
---

<objective>
Solder the test-side of DEBT-02 and all of DEBT-04: mechanically fix the 31 residual ruff errors in
`tests/` (5×W291 trailing whitespace, 21×F841 unused locals, 5×E722 bare except — D-01b) and remove
the two dead flat-method monkeypatches from the async fixture in `tests/test_sql_injection.py` (D-04 /
WR-02), while preserving the one live `real_schema.has_extension` patch.

Purpose: Together with Plan 01 (the `pycopg/` N818 half), this makes `uv run ruff check pycopg tests`
return exactly 0 errors — ROADMAP success criterion 2. The dead monkeypatches are stale since v0.7.0
(flat `role_exists`/`has_extension` removed; they now live on `db.admin.*` / `db.schema.*`), so the
patches assign to attributes no production code reads — pure housekeeping. These are mechanical
corrections (D-01b: fix, not suppress), so no behavior changes.
Output: 8 test files cleaned; ruff clean on `tests/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dette-audit/37-CONTEXT.md
@.planning/phases/37-dette-audit/37-RESEARCH.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 contributions)
- `tests/setup_test_db.py` — W291 trailing whitespace removed (lines 103, 104, 105, 116, 117)
- F841 unused-local removals across `tests/test_async_database.py`, `tests/test_database.py`,
  `tests/test_pool.py`, `tests/test_pool_stress.py`, `tests/test_session_edge_cases.py`
- E722 → `except Exception:` in `tests/test_pool_stress.py` (48, 128) and
  `tests/test_postgis_errors.py` (50, 146, 182)
- `tests/test_sql_injection.py` — two dead monkeypatch lines (`db.role_exists`, `db.has_extension`)
  removed (DEBT-04 / WR-02)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 31 ruff errors in tests/ (W291, F841, E722)</name>
  <read_first>
    - .planning/phases/37-dette-audit/37-RESEARCH.md (## DEBT-02 tests/ errors — the full file:line tables for W291, F841, E722; the E722 fix pattern note)
    - tests/setup_test_db.py (lines 103-105, 116-117 — trailing whitespace)
    - tests/test_session_edge_cases.py (lines 54, 68 — F841 `inner_session`, `exc_info`)
    - tests/test_pool_stress.py (lines 40 F841 `conn3`; 48, 128 E722)
    - tests/test_postgis_errors.py (lines 50, 146, 182 — E722)
  </read_first>
  <action>
    Run `uv run ruff check tests` to get the authoritative current list (RESEARCH.md notes the live
    list was truncated at 27 of the F841 results — use the day-of run as the source of truth, the
    pattern is the same: `db`/`conn`/`result`/`cur`/`inner_session`/`exc_info`/`conn3` assigned but
    never read in mock-based tests). Apply D-01b mechanical fixes: (a) W291 — strip trailing
    whitespace at `tests/setup_test_db.py` lines 103-105, 116-117; (b) F841 — remove the unused
    assignment OR drop the binding (replace `x = expr` with `expr` when the call has a side effect
    that must run, otherwise delete the line) across `test_async_database.py`, `test_database.py`,
    `test_pool.py`, `test_pool_stress.py`, `test_session_edge_cases.py` — preserve any
    side-effecting calls (e.g. context-manager `with db.session() as s:` that is entered for its
    effect); (c) E722 — replace each bare `except:` with `except Exception:` at
    `test_pool_stress.py` lines 48, 128 and `test_postgis_errors.py` lines 50, 146, 182 (these are
    intentional cleanup blocks; `except Exception:` keeps `KeyboardInterrupt`/`SystemExit`
    unswallowed). Do NOT change `tests/test_postgis_errors.py` line 112 logic — the flaky-name fix
    there is Plan 03's territory; only the E722 lines (50/146/182) are in scope here. After fixing,
    `uv run ruff check tests` must exit 0.
  </action>
  <verify>
    <automated>uv run ruff check tests 2>&1 | tee /dev/stderr | grep -qE "All checks passed|^$" && uv run ruff check tests</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check tests` exits 0 with 0 errors.
    - No W291 remains in `tests/setup_test_db.py`.
    - No bare `except:` remains in `tests/test_pool_stress.py` or `tests/test_postgis_errors.py`
      (grep `'except:'` in those two files returns nothing once filtered for the bare form).
    - The quick suite still imports/collects: `uv run pytest tests/ --collect-only -q -o addopts=""`
      exits 0 (no syntax break from removed lines).
  </acceptance_criteria>
  <done>tests/ is ruff-clean (0 errors), with side-effecting calls preserved and no collection breakage.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead flat-method monkeypatches from test_sql_injection.py async fixture (DEBT-04 / WR-02)</name>
  <read_first>
    - .planning/phases/37-dette-audit/37-RESEARCH.md (## DEBT-04 — exact lines 72-73 dead; lines 77-79 live; Pitfall 3)
    - tests/test_sql_injection.py (the `async_db` fixture, lines 59-81 — confirm the two dead lines and the live real_schema patch before editing)
  </read_first>
  <action>
    In `tests/test_sql_injection.py`, inside the `async_db` fixture (~lines 59-81): remove ONLY the
    two dead monkeypatch lines that patch `db.role_exists = AsyncMock(...)` and
    `db.has_extension = AsyncMock(...)` (~lines 72-73). These are dead since v0.7.0 — the flat
    `role_exists`/`has_extension` names were removed and now live at `db.admin.role_exists` /
    `db.schema.has_extension`; patching the flat attributes on the `AsyncDatabase` object affects
    nothing any production path reads (D-04 / WR-02). KEEP the live patch block that creates
    `real_schema = AsyncSchemaAccessor(db)` and sets `real_schema.has_extension = AsyncMock(...)`
    (~lines 77-79) — that one IS read by `SpatialAccessor._check_postgis()` via the accessor object;
    removing it would force a real DB connection and break the injection tests (Pitfall 3). Confirm
    the two target lines reference the bare `db.<name>` form (not the `real_schema.<name>` form)
    before deleting.
  </action>
  <verify>
    <automated>PGDATABASE=pycopg_test2 uv run pytest tests/test_sql_injection.py -x -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_sql_injection.py` no longer contains `db.role_exists =` or `db.has_extension =`
      (the bare-`db` flat patches) inside the async fixture.
    - `tests/test_sql_injection.py` STILL contains the `real_schema.has_extension = AsyncMock`
      patch (grep confirms `real_schema.has_extension` is present).
    - `PGDATABASE=pycopg_test2 uv run pytest tests/test_sql_injection.py -x -q -o addopts=""` passes
      (all injection tests green).
  </acceptance_criteria>
  <done>The two dead monkeypatches are gone, the live one is preserved, and the SQL-injection suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new external input path, no public API surface change. All edits are inside the test suite.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | test-only lint fixes + dead monkeypatch removal | accept | No new external inputs; changes are mechanical lint fixes and removal of dead test code. The one security-relevant test file (`test_sql_injection.py`) keeps the live `real_schema.has_extension` patch, so SQL-injection coverage is unchanged — verified by re-running the full injection suite green. |

This is housekeeping with no runtime impact; no STRIDE category is newly exposed.
</threat_model>

<verification>
- `uv run ruff check tests` exits 0.
- `PGDATABASE=pycopg_test2 uv run pytest tests/test_sql_injection.py -x -q -o addopts=""` passes.
- `uv run pytest tests/ --collect-only -q -o addopts=""` exits 0 (no collection breakage).
</verification>

<success_criteria>
- DEBT-02 (tests/ half): `uv run ruff check tests` returns exactly 0 errors.
- DEBT-04: the two dead flat-method monkeypatches removed; live patch preserved; injection suite green.
</success_criteria>

<output>
Create `.planning/phases/37-dette-audit/37-02-SUMMARY.md` when done.
</output>
