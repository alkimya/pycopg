---
phase: 28-incremental-etl-extract-runresult-async-parity
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - pycopg/etl.py
  - tests/test_etl_accessor.py
autonomous: true
requirements: [ETL-INC-03, ETL-INC-04, ETL-INC-07, ETL-INC-08, ETL-INC-09, ETL-INC-11]
must_haves:
  truths:
    - "async_db.etl.run() on an incremental pipeline applies WHERE col > watermark (bound param) identically to sync"
    - "async ETLError message text for missing-column and float-dtype is byte-for-byte identical to sync"
    - "async run() / dry_run / history() / last_run() surface watermark_used and watermark_recorded with the same None semantics as sync"
    - "async _read_watermark and async _end_run(watermark=) exist on an isolated autocommit connection"
    - "test_accessor_parity stays green — the new public surface is balanced across both accessors"
  artifacts:
    - path: "pycopg/etl.py"
      provides: "async _read_watermark; async _end_run(watermark=); async run() filtered extract + watermark capture + record; async incremental dry_run"
      contains: "async def _read_watermark"
    - path: "tests/test_etl_accessor.py"
      provides: "Async integration coverage mirroring the sync incremental tests"
      contains: "TestAsyncRunResultSurface"
  key_links:
    - from: "pycopg/etl.py AsyncETLAccessor.run"
      to: "_build_incremental_extract_sql"
      via: "shared async filtered-extract path fed by await self._read_watermark(name)"
      pattern: "_build_incremental_extract_sql"
    - from: "pycopg/etl.py AsyncETLAccessor._end_run"
      to: "queries.ETL_UPDATE_RUN_WATERMARK"
      via: "Jsonb(watermark) bind on the success path"
      pattern: "ETL_UPDATE_RUN_WATERMARK"
---

<objective>
Port the ENTIRE finalized sync incremental stack into `AsyncETLAccessor` as a
strict 1:1 behavioral mirror (D-A3). Async currently has ZERO watermark wiring:
no async `_read_watermark`, `_end_run` has no `watermark=` param, async `run()`
does no filter/capture/record, and `dry_run` is watermark-blind. After this plan
the async surface matches sync exactly, differing only by `await` / `async with`
/ `asyncio.to_thread` for transforms.

Purpose: Close ETL-INC-11 (full sync/async parity, the project Core Value) and
extend ETL-INC-03/-04/-07/-08/-09 to the async accessor.

Output: async `_read_watermark`, async `_end_run(watermark=)`, the async shared
filtered-extract path, async watermark capture block, async incremental
`dry_run`, async `RunResult` population, and async live-DB integration tests
mirroring the sync ones. Structural parity is auto-verified by
`test_accessor_parity` (no edit to `tests/test_parity.py`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-incremental-etl-extract-runresult-async-parity/28-CONTEXT.md
@.planning/phases/28-incremental-etl-extract-runresult-async-parity/28-PATTERNS.md
@.planning/phases/28-incremental-etl-extract-runresult-async-parity/28-01-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's subset)

NEW in Phase 28 — exclude from "pre-existing symbol" drift checks. This plan (02):

- `AsyncETLAccessor._read_watermark` (async, net-new) — mirror of sync `_read_watermark`
- `AsyncETLAccessor._end_run` gains a `watermark: dict | None = None` parameter
- The async shared filtered-extract path feeding `_build_incremental_extract_sql`
- The async watermark capture block (`max(col)` + guards, copied verbatim from sync)
- The async incremental `dry_run` branch

The two `RunResult` fields (`watermark_used`, `watermark_recorded`) and
`_row_to_result` mapping were produced in Plan 01 — async reuses them as-is (the
async result surfaces flow through the same `_row_to_result`).

<tasks>

<task type="auto">
  <name>Task 1: async _read_watermark + async _end_run(watermark=)</name>
  <files>pycopg/etl.py</files>
  <read_first>
    - pycopg/etl.py:960-998 — sync `_read_watermark` (the source-of-truth analog); body 992-998 is the exact block to mirror.
    - pycopg/etl.py:795-882 — sync `_end_run` with `watermark` param; the `if watermark is None: ETL_UPDATE_RUN else: ETL_UPDATE_RUN_WATERMARK + Jsonb(watermark)` fork (body ~853-882, `Jsonb` bind ~878) is the template.
    - pycopg/etl.py:1412-1460 — async `_end_run` AS-IS (no `watermark` param, always `ETL_UPDATE_RUN`) — the site to extend.
    - pycopg/etl.py:1505-1534 — async `history` / `last_run` showing the async autocommit + `dict_row` isolation pattern (`async with self._db.connect(autocommit=True)` / `await cur.execute` / `await cur.fetchone()`) to reuse.
    - pycopg/queries.py:303-321 — `ETL_GET_LAST_WATERMARK`, `ETL_UPDATE_RUN_WATERMARK` (both exist, Phase 27; async reuses them).
    - .planning/phases/28-incremental-etl-extract-runresult-async-parity/28-PATTERNS.md §5, §6 (exact sync templates + async ports).
  </read_first>
  <action>
    Add async `_read_watermark` mirroring sync (etl.py:960). Place it among the
    async run-log readers (e.g. after `last_run` at 1534, before `run` at 1536).
    Signature `async def _read_watermark(self, name: str) -> datetime | int | str
    | None`. Body mirrors sync 992-998 mechanically: `async with
    self._db.connect(autocommit=True) as conn:` / `async with
    conn.cursor(row_factory=dict_row) as cur:` / `await
    cur.execute(queries.ETL_GET_LAST_WATERMARK, [name])` / `row = await
    cur.fetchone()`; then `if row is None or row["watermark"] is None: return
    None` / `return _decode_watermark(row["watermark"])`. Reuse the frozen
    `_decode_watermark`. Docstring: shallow numpydoc mirroring sync (note it reads
    the last successful non-NULL watermark on an isolated autocommit connection).

    Extend async `_end_run` (1412): add `watermark: dict | None = None` to the
    signature (after `error_traceback`), matching the sync signature order
    (795-803). Switch the body (1447-1460) to the same fork as sync `_end_run`:
    when `watermark is None` keep `queries.ETL_UPDATE_RUN` with the current
    7-element param list; else use `queries.ETL_UPDATE_RUN_WATERMARK` with
    `[status, datetime.now(UTC), rows_extracted, rows_loaded, error_message,
    error_traceback, Jsonb(watermark), run_id]`. Use `await cur.execute(...)`.
    Reuse the already-imported `Jsonb`. Add the `watermark` param to the docstring
    Parameters block. Only mechanical diffs from sync (`async with` / `await`).
  </action>
  <verify>
    <automated>uv run python -c "import inspect, asyncio, pycopg.etl as e; assert asyncio.iscoroutinefunction(e.AsyncETLAccessor._read_watermark); sig=inspect.signature(e.AsyncETLAccessor._end_run); assert 'watermark' in sig.parameters; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `pycopg/etl.py` contains `async def _read_watermark` on `AsyncETLAccessor`, returning `datetime | int | str | None`, using `ETL_GET_LAST_WATERMARK` on an isolated autocommit connection with the NULL→`_decode_watermark` guard identical to sync.
    - Source: async `_end_run` signature includes `watermark: dict | None = None`; body forks to `ETL_UPDATE_RUN_WATERMARK` + `Jsonb(watermark)` only when `watermark is not None`, else keeps `ETL_UPDATE_RUN`.
    - Parity: the async `_read_watermark` body differs from sync ONLY by `async with` / `await`; the async `_end_run` fork differs ONLY by `async with` / `await`.
    - Quality: `uv run ruff check pycopg` exits 0; `uv run black pycopg` clean; `interrogate pycopg` ≥ 95.
  </acceptance_criteria>
  <done>Async `_read_watermark` exists and mirrors sync; async `_end_run` accepts and binds `watermark` via `ETL_UPDATE_RUN_WATERMARK` + `Jsonb`; signature/import check passes.</done>
</task>

<task type="auto">
  <name>Task 2: async run() — filtered extract, watermark capture (verbatim), record, incremental dry_run</name>
  <files>pycopg/etl.py</files>
  <read_first>
    - pycopg/etl.py:1000-1331 — the FINALIZED sync `run()` (post-Plan-01) — THE source of truth to mirror 1:1: shared filtered-extract path, capture block (1191-1223), success encode+record (1329-1330), dry-run incremental branch, `RunResult` field population. Read Plan 01's changes to this method before porting.
    - pycopg/etl.py:1536-1803 — async `run()` AS-IS: dry-run fork (1598-1661), real extract (1672-1698), `rows_extracted = len(df)` (1700), transform chain (1706-1721), empty-batch early return (1731-1733), failed path (1791-1800), success `_end_run` (1802, no watermark).
    - pycopg/etl.py:513-574 — `_build_incremental_extract_sql` (the builder to call; positional `%s` watermark).
    - pycopg/async_database.py:731+ — async `to_dataframe(table, schema, sql, params)` — confirm the SAME named-param (`:lim` dict) binding contract as sync `to_dataframe`, so the same positional-`%s`→named-`:wm` reconciliation from Plan 01 Task 2 applies.
    - .planning/phases/28-incremental-etl-extract-runresult-async-parity/28-PATTERNS.md §3, §4 (the verbatim capture block + the critical `pd.isna` before `is_float` ordering), §7 (dry_run D-A2).
    - .planning/phases/28-incremental-etl-extract-runresult-async-parity/28-CONTEXT.md — D-A3 (strict mirror; byte-for-byte ETLError text), D-A2/D-A2a.
  </read_first>
  <action>
    Mirror the finalized sync `run()` into async `run()` (1536) 1:1. Apply the
    SAME structure Plan 01 introduced on the sync side. Specifically:

    1. Shared async filtered-extract path (D-A2a): factor identically to the sync
       choice (helper vs inline) — call `await self._read_watermark(pipeline.name)`
       for the floor `wm`, then `_build_incremental_extract_sql(pipeline.source,
       pipeline.incremental_column, pipeline.schema, watermark=wm)` ONLY when
       `incremental_column is not None`; reconcile the builder's positional `%s`
       to a named `:wm` bind for async `to_dataframe` (same reconciliation as sync
       Plan 01 Task 2); compose `extract_limit` as a LIMIT on the filtered
       subquery (`:lim`). Replace the async real extract (1672-1698) and async
       dry-run extract (1602-1628). Watermark stays a BOUND param (SC-1 /
       T-28-01); identifiers validated before interpolation (T-28-02).

    2. Watermark capture block: COPY the sync 1191-1223 block VERBATIM into async
       `run()` right after `rows_extracted = len(df)` (1700) and BEFORE the
       transform chain (1706). Mechanical diffs ONLY (none needed — it is pure
       pandas, no `await`). CRITICAL: preserve the exact branch ordering
       `pd.isna(m)` BEFORE `pd.api.types.is_float(m)` (Phase-27 WR-02 — NaN is a
       float). The `ETLError` message strings (missing-column and float-dtype)
       MUST be BYTE-FOR-BYTE identical to sync — do not reword for async.

    3. Success record: replace the async success `_end_run` (1802) with the sync
       pattern (1329-1330): `wm_env = _encode_watermark(raw_watermark) if
       raw_watermark is not None else None` then `await self._end_run(run_id,
       "success", rows_extracted, rows_loaded, watermark=wm_env)`. The empty-batch
       early return (1731-1733) and the failed path (1791-1800) MUST pass NO
       watermark (column stays NULL — no-advance / preserve invariants).

    4. `RunResult` population: set `watermark_used = wm` on the returned result for
       the real path (same `dataclasses.replace`/pass-through approach Plan 01
       chose); `watermark_recorded` flows from `_fetch_run_result` →
       `_row_to_result`. For the async incremental `dry_run` branch (1652-1661),
       reuse the capture logic to compute `watermark_recorded` = max(col) of the
       FILTERED batch (None if empty/all-NULL) and `watermark_used = wm`; write NO
       `pipeline_runs` row. Non-incremental → both fields `None` everywhere.

    Keep the existing `await asyncio.to_thread(step, df)` transform dispatch
    (1716) unchanged. Do not alter the load seam (1783-1789).
  </action>
  <verify>
    <automated>uv run ruff check pycopg && uv run black --check pycopg/etl.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: async `run()` calls `await self._read_watermark(...)` and `_build_incremental_extract_sql(...)` on both the real and dry-run extract paths (same shared factoring as sync).
    - Parity: the async watermark capture block is character-identical to sync 1191-1223 except for any unavoidable mechanical diff; `pd.isna(m)` precedes the `is_float` branch; the missing-column and float-dtype `ETLError` message strings are byte-for-byte identical to sync (verifiable by diffing the two string literals).
    - Behavior: async success path records the watermark via `_end_run(..., watermark=wm_env)`; empty-batch and failed paths pass NO watermark.
    - Behavior: async incremental `dry_run` reports `watermark_used` + `watermark_recorded` (filtered max) and writes no `pipeline_runs` row.
    - Param-safety: the async watermark value reaches `to_dataframe` as a bound param, never interpolated (SC-1 / T-28-01).
    - Quality: `uv run ruff check pycopg` exits 0; `uv run black --check pycopg/etl.py` exits 0; `interrogate pycopg` ≥ 95.
  </acceptance_criteria>
  <done>Async `run()` mirrors the finalized sync `run()` 1:1: filtered extract, verbatim capture (guard ordering preserved, identical ETLError text), success-only record, `RunResult` fields, and incremental dry_run — only `await`/`async with`/`to_thread` differ.</done>
</task>

<task type="auto">
  <name>Task 3: async integration tests mirroring sync + parity confirmation</name>
  <files>tests/test_etl_accessor.py</files>
  <read_first>
    - tests/test_etl_accessor.py:1728+ — `TestAsyncRunResultSurface` (the async seam) + its async fixture / cleanup conventions.
    - tests/test_etl_accessor.py:1378-1660 — the Phase-27 sync incremental tests to mirror into async (missing-col 1598, float-col 1613, all-NULL 1636, jsonb roundtrip 1537).
    - tests/test_etl_accessor.py:1062-1130 — the SYNC incremental tests added in Plan 01 Task 3 (mirror each into async).
    - tests/test_parity.py:24-36, 516 — `ACCESSOR_PAIRS` + `test_accessor_parity`; the `# TestEtlParity removed` comment. DO NOT edit this file; DO NOT restore `TestEtlParity`.
    - .planning/phases/28-incremental-etl-extract-runresult-async-parity/28-PATTERNS.md §Test Pattern Map.
  </read_first>
  <action>
    Mirror the Plan-01 sync incremental tests (#1-#7) into `TestAsyncRunResultSurface`
    using the existing async fixtures + cleanup conventions (follow the existing
    `TestAsyncRunResultSurface` precedent — reuse async fixtures or define async
    mirrors per the established pattern):

    - second-run-pulls-only-new (ETL-INC-03), watermark-as-bound-param (SC-1),
      RunResult fields (ETL-INC-07), history()/last_run() surfacing (ETL-INC-08),
      incremental dry_run no-row (ETL-INC-09), tz-aware offset preservation.
    - Async ETL-INC-04 guards: mirror `test_incremental_column_missing_raises_etlerror`
      (1598), `test_float_incremental_column_raises_etlerror` (1613),
      `test_all_null_incremental_column_preserves_watermark` (1636) into async,
      and ASSERT the async `ETLError` message text equals the sync message text
      byte-for-byte (compare against the sync raise, or against the exact expected
      string) — this is the behavioral parity check (D-A3).

    Confirm structural parity holds WITHOUT editing `tests/test_parity.py`:
    `test_accessor_parity` over `ACCESSOR_PAIRS` automatically covers the new
    public surface because `run`/`history`/`last_run` are already paired and the
    new fields/methods are either on the shared `RunResult` or private
    (`_read_watermark`). Do NOT create or restore a `TestEtlParity` class.
  </action>
  <verify>
    <automated>uv run pytest tests/test_etl_accessor.py -k "AsyncRunResultSurface or async" -o addopts="" -q && uv run pytest tests/test_parity.py -k accessor_parity -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - Test: `TestAsyncRunResultSurface` covers second-run-filter, bound-param, RunResult fields, history/last_run surfacing, incremental dry_run, tz-offset, and the three ETL-INC-04 guards — each mirroring a sync test.
    - Parity (behavioral): the async missing-column and float-dtype tests assert the `ETLError` message is byte-for-byte identical to the sync message.
    - Parity (structural): `uv run pytest tests/test_parity.py -k accessor_parity -o addopts="" -q` exits 0 with `tests/test_parity.py` UNMODIFIED (no `TestEtlParity` added).
    - Test: `uv run pytest tests/test_etl_accessor.py -k "AsyncRunResultSurface or async" -o addopts="" -q` exits 0.
    - Quality: `uv run ruff check tests` exits 0; `uv run black tests` clean.
  </acceptance_criteria>
  <done>Async incremental surface fully tested and behaviorally parity-checked against sync; `test_accessor_parity` green and unmodified; no `TestEtlParity` restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → async `run()` filtered extract / async `to_dataframe` | Watermark VALUE and incremental COLUMN identifier cross into SQL text on the async path (mirror of sync boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-A1 | Injection (Tampering) | async `run()` filtered extract — watermark value | mitigate | Watermark VALUE is always a bound parameter (positional `%s` from the builder reconciled to a named `:wm` bind), never interpolated — identical invariant to sync T-28-01; asserted by the async bound-param test. |
| T-28-A2 | Injection (Tampering) | `_build_incremental_extract_sql` identifiers on the async path | mitigate | Same `validate_identifiers`-before-interpolation invariant as sync; reused builder, no async-specific interpolation introduced. |
| T-28-A3 | Spoofing/Tampering | async `_end_run` watermark write (`Jsonb(watermark)`) | mitigate | Watermark bound via `Jsonb()` on a dedicated autocommit connection; written ONLY on the success path (failed/empty pass none) — preserves no-advance-on-failure. |

No new external surface (pure library; async mirror of the sync surface only).
</threat_model>

<verification>
- `uv run pytest tests/test_etl_accessor.py -k "AsyncRunResultSurface or async" -o addopts="" -q` exits 0.
- `uv run pytest tests/test_parity.py -k accessor_parity -o addopts="" -q` exits 0 (unmodified).
- `uv run ruff check pycopg tests` exits 0; `uv run black pycopg tests` clean; `interrogate pycopg` ≥ 95.
- Async `ETLError` message text byte-for-byte identical to sync (asserted in tests).
</verification>

<success_criteria>
- `async_db.etl.run(pipeline)` mirrors the full incremental surface (filter, capture, record, RunResult fields, dry_run) — ETL-INC-11 / SC-5.
- Async guard semantics + `ETLError` text are byte-for-byte equal to sync (D-A3).
- `test_accessor_parity` passes with the incremental surface included, `tests/test_parity.py` unmodified.
</success_criteria>

<output>
Create `.planning/phases/28-incremental-etl-extract-runresult-async-parity/28-02-SUMMARY.md` when done.
</output>
